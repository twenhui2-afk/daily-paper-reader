---
title: "enclawed: A Configurable, Sector-Neutral Hardening Framework for Single-User AI Assistant Gateways"
source: arxiv
title_zh: 用于Single-User AI Assistant Gateways的enclawed：Configurable，Sector-Neutral Hardening 框架
abstract_zh: "作者提出 enclawed，hard-fork hardening 框架 built on top 的 OpenClaw single-user personal artificial intelli。\n\n核心做法：enclawed targets deployments that need attestable peer trust，deny-by-default external connectivity，s。\n\n主要结果：框架 ships in two flavors：open flavor that preserves OpenClaw compatibility while still emitting audit。\n\n值得关注的点在于：deploying organization remains responsible 用于 hardware，validated cryptographic 模块s，certified facilit。"
abstract_en: "We present enclawed, a hard-fork hardening framework built on top of the OpenClaw single-user personal artificial intelligence (AI) assistant gateway. enclawed targets deployments that need attestable peer trust, deny-by-default external connectivity, signed-module loading, and a tamper-evident audit trail typically regulated industries such as financial services, healthcare, defense contracting, regulated R&D, and government enclaves. The framework ships in two flavors: an open flavor that preserves OpenClaw compatibility while still emitting audit, classification, and data-loss-prevention (DLP) signals, and an enclaved flavor that activates strict allowlists, Federal Information Processing Standards (FIPS) cryptographic-module assertion, mandatory module-manifest signature verification, and high-assurance peer attestation for the Model Context Protocol (MCP). The classification ladder is fully data-driven: a deploying organization selects from five built-in presets (generic, US-government, healthcare, financial services, three-tier) or supplies its own JSON. We accompany the implementation with a security review, a 204-case test suite (146 unit tests, 58 adversarial pen-tests for tamper detection, signature forgery, egress bypass, trust-root mutation, DLP evasion, prompt injection, and code injection), real-time human-in-the-loop control (per-agent pause / resume / stop and approval queues), a memory-bounded secure transaction buffer with rollback (default cap 50% of system RAM, configurable), a strict-mode TypeScript typecheck of all 22 framework files, and a GitHub Actions workflow ready for continuous integration. enclawed is a hardening framework, not an accredited compliance certification. The deploying organization remains responsible for hardware, validated cryptographic modules, certified facilities, and assessor sign-off."
authors: Alfredo Metere
date: 2026-04-18
journal: cs.CR
source_link: "https://arxiv.org/pdf/2604.16838v1"
pdf: "https://arxiv.org/pdf/2604.16838v1"
score: 6.0
tldr: 围绕《用于Single-User AI Assista...》；enclawed targets deployments tha...；框架 ships in two flavors：open flavor...
tldr_cn: 围绕《用于Single-User AI Assista...》；enclawed targets deployments tha...；框架 ships in two flavors：open flavor...
tldr_en: "We present enclawed, a hard-fork hardening framework built on top of the OpenClaw single-user personal artificial intelligence (AI) assistant gateway. enclawed targets deployments that need attestable peer trust, deny-by-default external connectivity, signed-module loading, and a tamper-evident audit trail typically regulated industries such as financial services, healthcare, defense contracting, regulated R&D, and government enclaves."
selection_source: fresh_fetch
vocabulary: ["classification | 分类 | CET6", "detection | 检测 | CET6"]
motivation: 作者提出 enclawed，hard-fork hardening 框架 built on top 的 OpenClaw single-user personal artificial intelli。
method: 核心做法：enclawed targets deployments that need attestable peer trust，deny-by-default external connectivity，s。
result: 主要结果：框架 ships in two flavors：open flavor that preserves OpenClaw compatibility while still emitting audit。
conclusion: 值得关注的点在于：deploying organization remains responsible 用于 hardware，validated cryptographic 模块s，certified facilit。
---

## 摘要
作者提出 enclawed，hard-fork hardening 框架 built on top 的 OpenClaw single-user personal artificial intelli。

核心做法：enclawed targets deployments that need attestable peer trust，deny-by-default external connectivity，s。

主要结果：框架 ships in two flavors：open flavor that preserves OpenClaw compatibility while still emitting audit。

值得关注的点在于：deploying organization remains responsible 用于 hardware，validated cryptographic 模块s，certified facilit。

## Abstract
We present enclawed, a hard-fork hardening framework built on top of the OpenClaw single-user personal artificial intelligence (AI) assistant gateway. enclawed targets deployments that need attestable peer trust, deny-by-default external connectivity, signed-module loading, and a tamper-evident audit trail typically regulated industries such as financial services, healthcare, defense contracting, regulated R&D, and government enclaves. The framework ships in two flavors: an open flavor that preserves OpenClaw compatibility while still emitting audit, classification, and data-loss-prevention (DLP) signals, and an enclaved flavor that activates strict allowlists, Federal Information Processing Standards (FIPS) cryptographic-module assertion, mandatory module-manifest signature verification, and high-assurance peer attestation for the Model Context Protocol (MCP). The classification ladder is fully data-driven: a deploying organization selects from five built-in presets (generic, US-government, healthcare, financial services, three-tier) or supplies its own JSON. We accompany the implementation with a security review, a 204-case test suite (146 unit tests, 58 adversarial pen-tests for tamper detection, signature forgery, egress bypass, trust-root mutation, DLP evasion, prompt injection, and code injection), real-time human-in-the-loop control (per-agent pause / resume / stop and approval queues), a memory-bounded secure transaction buffer with rollback (default cap 50% of system RAM, configurable), a strict-mode TypeScript typecheck of all 22 framework files, and a GitHub Actions workflow ready for continuous integration. enclawed is a hardening framework, not an accredited compliance certification. The deploying organization remains responsible for hardware, validated cryptographic modules, certified facilities, and assessor sign-off.

## 英语词汇
- **classification**：分类（CET6）
- **detection**：检测（CET6）
