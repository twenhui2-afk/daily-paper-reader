---
title: Truncated Plethystic Exponentials Preserve Power Sum Constraints
source: arxiv
title_zh: Truncated Plethystic Exponentials Preserve Power Sum Constraints
abstract_zh: "Given arbitrary sequence (α_1，...，α_n) \\in \\mathbb{C}^n，we show that degree-n truncation 的 formal ex。\n\n核心做法：This truncation-exactness property is algebraic identity in ring 的 formal power series，proved by coe。\n\n主要结果：It defines natural embedding 的 sequences 为 multisets 的 complex numbers 与 yields O (n^2) algorithm 用于。\n\n值得关注的点在于：We apply result to polylogarithm family α_k = k^{1-s}，where associated exponential \\exp (-\\mathrm{Li。"
abstract_en: "Given an arbitrary sequence $(α_1, \\ldots, α_n) \\in \\mathbb{C}^n$, we show that the degree-$n$ truncation of the formal exponential $\\exp\\bigl(-\\sum_{k=1}^{\\infty} \\frac{α_k}{k} x^k\\bigr)$ produces a polynomial whose roots $ρ_1, \\ldots, ρ_n$ satisfy $\\sum_{i=1}^n ρ_i^{-k} = α_k$ exactly for $k = 1, \\ldots, n$. This truncation-exactness property is an algebraic identity in the ring of formal power series, proved by coefficient matching. It defines a natural embedding of sequences into multisets of complex numbers and yields an $O(n^2)$ algorithm for computing the polynomial from the prescribed power sums. We apply the result to the polylogarithm family $α_k = k^{1-s}$, where the associated exponential $\\exp(-\\mathrm{Li}_s(x))$ produces factorial-integer coefficient sequences for $s \\leq 0$ and encodes values of the Riemann zeta function through $\\lim_{n\\to\\infty} P_n^{(s)}(1) = \\exp(-ζ(s))$ for $\\mathrm{Re}(s) > 1$."
authors: Yogesh Phalak
date: 2026-03-29
source_link: "https://arxiv.org/pdf/2603.28828v1"
pdf: "https://arxiv.org/pdf/2603.28828v1"
score: 7.55
tldr: 围绕《Truncated Plethystic Exp...》；This truncation-exactness proper...；It defines natural embedding 的 seque...
tldr_cn: 围绕《Truncated Plethystic Exp...》；This truncation-exactness proper...；It defines natural embedding 的 seque...
tldr_en: "Given an arbitrary sequence $(α_1, \\ldots, α_n) \\in \\mathbb{C}^n$, we show that the degree-$n$ truncation of the formal exponential $\\exp\\bigl(-\\sum_{k=1}^{\\infty} \\frac{α_k}{k} x^k\\bigr)$ produces a polynomial whose roots $ρ_1, \\ldots, ρ_n$ satisfy $\\sum_{i=1}^n ρ_i^{-k} = α_k$ exactly for $k = 1, \\ldots, n$. This truncation-exactness property is an algebraic identity in the ring of formal power..."
selection_source: fresh_fetch
vocabulary: ["embedding | 嵌入表示 | 学术高频"]
motivation: "Given arbitrary sequence (α_1，...，α_n) \\in \\mathbb{C}^n，we show that degree-n truncation 的 formal ex。"
method: 核心做法：This truncation-exactness property is algebraic identity in ring 的 formal power series，proved by coe。
result: 主要结果：It defines natural embedding 的 sequences 为 multisets 的 complex numbers 与 yields O (n^2) algorithm 用于。
conclusion: "值得关注的点在于：We apply result to polylogarithm family α_k = k^{1-s}，where associated exponential \\exp (-\\mathrm{Li。"
---

## 摘要
Given arbitrary sequence (α_1，...，α_n) \in \mathbb{C}^n，we show that degree-n truncation 的 formal ex。

核心做法：This truncation-exactness property is algebraic identity in ring 的 formal power series，proved by coe。

主要结果：It defines natural embedding 的 sequences 为 multisets 的 complex numbers 与 yields O (n^2) algorithm 用于。

值得关注的点在于：We apply result to polylogarithm family α_k = k^{1-s}，where associated exponential \exp (-\mathrm{Li。

## Abstract
Given an arbitrary sequence $(α_1, \ldots, α_n) \in \mathbb{C}^n$, we show that the degree-$n$ truncation of the formal exponential $\exp\bigl(-\sum_{k=1}^{\infty} \frac{α_k}{k} x^k\bigr)$ produces a polynomial whose roots $ρ_1, \ldots, ρ_n$ satisfy $\sum_{i=1}^n ρ_i^{-k} = α_k$ exactly for $k = 1, \ldots, n$. This truncation-exactness property is an algebraic identity in the ring of formal power series, proved by coefficient matching. It defines a natural embedding of sequences into multisets of complex numbers and yields an $O(n^2)$ algorithm for computing the polynomial from the prescribed power sums. We apply the result to the polylogarithm family $α_k = k^{1-s}$, where the associated exponential $\exp(-\mathrm{Li}_s(x))$ produces factorial-integer coefficient sequences for $s \leq 0$ and encodes values of the Riemann zeta function through $\lim_{n\to\infty} P_n^{(s)}(1) = \exp(-ζ(s))$ for $\mathrm{Re}(s) > 1$.

## 英语词汇
- **embedding**：嵌入表示（学术高频）
