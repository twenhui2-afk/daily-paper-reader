Title: Asymptotic Theory for Graphical SLOPE: Precision Estimation and Pattern Convergence

URL Source: https://arxiv.org/pdf/2604.12771v1

Published Time: Wed, 15 Apr 2026 01:33:47 GMT

Number of Pages: 38

Markdown Content:
# Asymptotic Theory for Graphical SLOPE: Precision Estimation and Pattern Convergence ∗

# Ivan Hejn´ y1, Giovanni Bonaccolto 2, Philipp Kremer 3, 4 , Sandra Paterlini 4,Ma lgorzata Bogdan 5, and Jonas Wallin 11Department of Statistics, Lund University, Sweden 

2Department of Economics and Law, “Kore” University of Enna, Italy 

3La Francaise Systematic Asset Management GmbH, Frankfurt am Main, Germany 

4Department of Economics and Management, University of Trento, Italy 

5Institute of Mathematics, University of Wroclaw, Poland 

Abstract 

This paper studies Graphical SLOPE for precision matrix estimation, with empha-sis on its ability to recover both sparsity and clusters of edges with equal or similar strength. In a fixed-dimensional regime, we establish that the root-n scaled estima-tion error converges to the unique minimizer of a strictly convex optimization problem defined through the directional derivative of the SLOPE penalty. We also establish convergence of the induced SLOPE pattern, thereby obtaining an asymptotic charac-terization of the clustering structure selected by the estimator. A comparison with GLASSO shows that the grouping property of SLOPE can substantially improve esti-mation accuracy when the precision matrix exhibits structured edge patterns. To assess the effect of departures from Gaussianity, we then analyze Gaussian-loss precision matrix estimation under elliptical distributions. In this setting, we derive the limiting distribution and quantify the inflation in variability induced by heavy tails relative to the Gaussian benchmark. We also study TSLOPE, based on the multivari-ate t-loss, and derive its limiting distribution. The results show that TSLOPE offers clear advantages over GSLOPE under heavy-tailed data-generating mechanisms. Sim-ulation evidence suggests that these qualitative conclusions persist in high-dimensional settings, and an empirical application shows that SLOPE-based estimators, especially TSLOPE, can uncover economically meaningful clustered dependence structures. 

> ∗

The opinions expressed in this article are those of the authors and do not necessarily reflect the views of La Francaise Systematic Asset Management GmbH or any of its affiliates. 

1

> arXiv:2604.12771v1 [math.ST] 14 Apr 2026

# 1 Introduction 

Graphical models provide a parsimonious representation of conditional dependence among the components of a random vector X = ( X1, . . . , X p)⊤. For Gaussian graphical models (GGMs) X ∼ N p(0 , Σ), conditional independencies are encoded by zeros in the precision matrix Θ 0 := Σ −1: (Θ 0)ij = 0 if and only if Xi ⊥⊥ Xj | X−(i,j ) [18]. Estimating Θ 0 from n

i.i.d. observations is therefore a central task in network learning and multivariate analysis, with applications in genomics, neuroscience, and financial econometrics. A widely used approach estimates Θ 0 by solving a convex penalized likelihood problem over the cone Sym +(p). In particular, the graphical Lasso (GLasso) augments the Gaus-sian negative log-likelihood with an entrywise ℓ1 penalty on off-diagonal elements, yielding a sparse precision estimate and a sparse conditional independence graph [11]. A large litera-ture establishes statistical guarantees for GLasso and related estimators in high-dimensional regimes under sparsity assumptions; see, e.g., [20] and references therein. In many applications, however, the goal is not only to identify which edges are present, but also to recover groups of edges with comparable or identical strength . Such grouping arises in block-structured dependence, latent factor models, and hub-like networks, and is closely related to the equality-constrained structures studied in colored graphical models (see, e.g., [16]), where edges sharing the same “color” are constrained to have identical parameters. In these settings, an estimator may achieve a small global error yet still fragment truly homogeneous groups into slightly different magnitudes, thereby complicating interpretation. This motivates distinguishing overall estimation accuracy from the ability to recover the underlying clustering structure implied by Θ 0.Graphical SLOPE (GSLOPE) is designed to target this second objective. It replaces the entrywise ℓ1 penalty by a sorted ℓ1 (SLOPE) penalty on the off-diagonal precision entries, thereby encouraging sparsity while allowing multiple edges to be tied to the same magnitude. To formalize and assess this behavior, we use the notion of patterns for polyhedral regularizers [5, 13, 15, 14], which captures both sparsity and equality relations among nonzero coefficients. This viewpoint suggests a natural empirical diagnostic: in addition to the usual RMSE, we track a clustering RMSE that measures deviation from the clustering structure implied by the target precision matrix. The resulting RMSE versus clustering-RMSE curves quantify the trade-off induced by tuning: stronger regularization can improve clustering at the cost of increased global error, and vice versa. The paper makes three contributions. First, in a fixed-p asymptotic regime ( n → ∞ 

with p fixed), we characterize the limiting distribution of √n ( bΘn − Θ0) for GSLOPE under a general smooth loss. The limit is given as the unique minimizer of a strictly convex quadratic-plus-penalty problem involving the population Hessian and the directional derivative of the SLOPE penalty, and we also establish convergence of the induced SLOPE pattern. Second, we specialize the limit to Gaussian-loss precision estimation under elliptical mod-els and derive explicit expressions for the Hessian and the score covariance, thereby quan-tifying variance inflation under heavy tails. We further analyze TSLOPE, based on the multivariate t loss, and obtain the corresponding limiting quantities under a condition on the radial distribution. Third, we complement the theory with simulations that (i) validate the asymptotic RMSE approximation, (ii) decompose error into a component compatible with the target pattern and 2a clustering-error component, and (iii) compare GSLOPE and TSLOPE under t-distributed data across degrees of freedom.  

> 1.1 Organization of the paper

The paper is organized as follows. Section 2 reviews the related literature. Section 3 presents the main theoretical results, describing a general asymptotic framework for graphical SLOPE (Theorem 3.1). Within this section, we apply the framework to establish the exact asymp-totic error distribution for GSLOPE under elliptically distributed data (Theorem 3.2 and Proposition 3.3) and derive a similar asymptotic formula for TSLOPE using a multivariate t-distribution penalty (Theorem 3.8). Section 4 includes simulation studies that illustrate the asymptotic framework, highlighting TSLOPE’s advantage in estimation and pattern recov-ery under heavy-tailed, block-structured data relative to GSLOPE, GLASSO, and TLASSO. Section 5 applies these estimators to empirical financial data to identify economically mean-ingful groupings among stock portfolios. Section 6 concludes the paper. Auxiliary results, proofs, and figures are provided in Appendix 7. 

# 2 Related literature 

Sparse precision matrix estimation and Gaussian graphical modeling have been studied ex-tensively over the last two decades. In Gaussian graphical models, conditional independencies correspond to zeros in the precision matrix, which motivates convex estimators that com-bine the Gaussian log-likelihood with a sparsity-inducing penalty; the graphical Lasso is the canonical example [11]. A large body of work establishes statistical guarantees for such esti-mators in high-dimensional regimes under sparsity and regularity conditions, including rates and support recovery results for ℓ1-penalized log-determinant programs [20]. Closely related approaches include neighborhood selection based on nodewise Lasso regressions [19] and ℓ1-constrained precision estimation such as CLIME [6]. These contributions collectively provide the methodological and theoretical baseline against which more structured regularizers for graphical models can be compared. Beyond entrywise sparsity, a growing literature develops convex regularizers that encode additional structure—such as grouping, fusion, hierarchical constraints, and hub patterns— in graphical model estimation. From a geometric perspective, many of these regularizers are polyhedral norms or can be expressed via atomic norms, enabling unified analyses of estimation and model selection in both low- and high-dimensional settings. Recent work connects graphical Lasso-type estimators to a broader family of atomic-norm formulations and studies their high-dimensional pattern recovery properties, emphasizing the role of non-differentiability and the induced low-dimensional structure of the solution [13]. This line of research motivates penalties that recover structured dependence patterns, not merely sparse edge sets. In regression, the sorted ℓ1 penalty (SLOPE) was introduced as a convex procedure that adapts to unknown sparsity and, with suitable tuning, can control the false discovery rate [4, 2]. Independently of its multiple testing interpretation, SLOPE (and the related class of ordered weighted ℓ1 norms) exhibits an important geometric property: by penalizing ordered 3absolute values with a decreasing weight sequence, it naturally induces clustering of coef-ficients, producing groups of equal magnitudes. Recent work formalizes this phenomenon through the concept of patterns and provides guarantees for pattern recovery under SLOPE, highlighting how polyhedral geometry governs the equality and ordering relations recovered by the estimator [5]. These developments provide the conceptual foundation for transporting SLOPE to matrix-valued problems such as precision matrix estimation, where clustering cor-responds to equality of edge strengths, closely related to the equality-constrained structures studied in colored graphical models [16]. The present paper also relates to the broader literature on low-dimensional asymptotics for estimators defined by non-smooth convex regularization. Classical results for Lasso-type estimators in the fixed-p regime characterize the limit distribution of the rescaled error via an auxiliary convex optimization problem obtained from a local quadratic approximation of the loss and the directional derivative of the penalty [12, 24]. Recent work generalizes this program to a wide class of loss functions and polyhedral regularizers, and further develops asymptotic results for pattern-valued functionals, establishing convergence of the induced patterns and providing tractable descriptions of their limiting behavior [15, 14]. Our analysis of graphical SLOPE can be viewed as a concrete specialization of this framework to precision matrix estimation, with a focus on how clustering patterns arise in the estimation error. Finally, our heavy-tail analysis connects to robust graphical modeling under distributional misspecification. In many empirical applications, Gaussianity is an imperfect approximation and tail heaviness can inflate variability when Gaussian likelihood losses are employed. Ellip-tical models provide a tractable generalization that preserves a form of radial symmetry while allowing for heavy tails, and the multivariate t distribution is a standard benchmark within this class. Likelihood-based procedures tailored to t models can therefore serve as robust alternatives to Gaussian-loss estimators in heavy-tailed settings. Our GSLOPE/TSLOPE comparison fits into this agenda by quantifying, through limiting distributions, how tail heaviness affects the score covariance and how loss specification interacts with SLOPE-type regularization. 

# 3 Theoretical results  

> 3.1 Precision estimation by SLOPE

Let X(1) , . . . , X (n) be n i.i.d. observations of a random vector X = ( X1, . . . , X p)T ∈ Rp. We assume throughout that E[X] = 0 and that the covariance matrix Cov( X) has full rank. We consider regularized M-estimators targeting an underlying structural parameter Θ 0, defined as solutions to empirical optimization problems of the form 

bΘn ∈ argmin 

> Θ∈Sym+( p)

(

1

n

> n

X

> i=1

ℓ X(i), Θ + n−1/2 Pen(Θ) 

)

, (1) where ℓ(X, Θ) denotes a prespecified loss function and Pen(Θ) is a suitable, problem-specific regularizer. The target parameter Θ 0 is fundamentally defined as the unique minimizer of the population risk, E[ℓ(X, Θ)], and often encodes conditional independencies (e.g., coinciding 4with the precision matrix under Gaussianity). Here, Sym +(p) denotes the set of p×p positive definite matrices. For example, in the Gaussian model X ∼ N (0 , Σ), the negative log-likelihood loss is given by 

ℓ(x, Θ) := −12 log det(Θ) + 12xT Θx. (2) The most widely studied regularization is the graphical Lasso [11], Pen(Θ) = λ X

> i<j

|Θij |, λ > 0,

for which extensive theory on estimation and selection consistency has been developed [20]. Recent work by [13] extends this study to more general polyhedral and atomic norms, in-cluding the Fused Lasso, Group Lasso, and the SLOPE norm. Let θ+ := vec +(Θ) = (Θ ij )i>j denote the vectorization of the strictly lower-triangular components of Θ as in [13]. The SLOPE norm is determined by an ordered sequence of hyperparameters λ1 > λ 2 > · · · > λ p(p−1) /2 > 0, Pen(Θ) = 

> p(p−1) /2

X

> j=1

λj | vec +(Θ) |(j),

where | vec +(Θ) |(1) ≥ | vec +(Θ) |(2) ≥ · · · ≥ | vec +(Θ) |(p(p−1) /2) denote the ordered absolute values of the strictly lower-triangular entries of Θ. Unlike the Lasso penalty, graphical SLOPE can cluster coefficients; i.e., it can yield estimates bΘij that are equal in magnitude. We are interested in dimensionality reduction by identifying the SLOPE pattern of the vector 

θ+ = vec +(Θ), given by 

patt (θ+)i = rank( |θ+|)i sign( θ+)i, ∀i ∈ { 1, . . . , p (p − 1) /2}.

As illustrated in Example 7.1 in the Appendix, the proper identification of the SLOPE pattern allows for the proper identification of true edges as well as for the identification of the groups of edges with the same absolute value of the elements on the precision matrix. This allows for a more efficient dimensionality reduction and improved estimates of Θ 0 by using appropriate constraints in the corresponding optimization problem. More generally, one can formally define the pattern of a polyhedral penalty Pen( x) in terms of its subdifferential via the relation 

patt (x) = patt (y) ⇐⇒ ∂ Pen( x) = ∂ Pen( y),

see [5, 13, 14, 15]. For notational simplicity, we abbreviate patt (vec +(Θ)) as patt (Θ), by which we refer strictly to the p(p − 1) /2-dimensional vector corresponding to the pattern of the lower-triangular matrix entries. In this work, we fix the dimension p of the vector X = ( X1, . . . , X p)T and analyze the asymptotic behavior of the rescaled error 

√n( bΘn − Θ0) as n → ∞ .

5This classical asymptotic regime is studied for the Lasso estimator in linear models in [12] and developed in greater generality in [14]. The asymptotic distribution of the error is expressed in terms of the directional derivative at Θ 0 in the direction U ∈ Sym( p), defined by Pen ′(Θ 0; U ) = lim 

> ε↓0

1

ε (Pen(Θ 0 + εU ) − Pen(Θ 0)) .

Note that by the convexity of the penalty Pen(Θ), the directional derivative always exists. For an explicit expression of the directional derivative for graphical SLOPE, we refer to Appendix 7.2. The key asymptotic result is Corollary 3.4 in [14], which we apply in the context of the graphical Lasso and SLOPE. For completeness, we restate the result in its graphical form as Theorem 3.1. Our focus is on the SLOPE penalty, although the result remains valid for any polyhedral regularizer as defined in [14] or [13]. Let bΘn denote the minimizer of the objective function in (1), characterized by a loss function ℓ(x, Θ) and the SLOPE penalty Pen(Θ). Let Θ 0 minimize the risk function 

G(Θ) = E[ℓ(X, Θ)] .

Assume there exists an open neighborhood B of Θ 0 such that the map Θ 7 → ℓ(x, Θ) is twice continuously differentiable on B for every x. We further impose the following regularity conditions: (i) For all Θ ∈ B, ∥∇ 2Θℓ(X, Θ) ∥ ≤ M (X) for some M with E[M (X)2] < ∞.(ii) G(Θ) is thrice continuously differentiable on B, and the Hessian C := ∇2G(Θ 0) is positive definite. (iii) The expected gradient satisfies E[∇Θℓ(X, Θ0)] = 0, and the covariance matrix 

C△ := E vec( ∇Θℓ(X, Θ0)) vec( ∇Θℓ(X, Θ0)) T 

is finite. (iv) Θ 0 lies in the interior of Sym +(p), and the sequence ( bΘn)n∈N is uniformly tight. (v) For any compact K ⊂ Sym +(p), sup Θ∈K |ℓ(X, Θ) |≤ L(X) for some L with E[L(X)] <∞.

Theorem 3.1. Assume that conditions (i)–(v) hold. Then, the sequence bUn = √n( bΘn − Θ0)

converges in distribution to 

bU = argmin  

> U∈Sym( p)

12 vec( U )T C vec( U ) − W T vec( U ) + Pen ′(Θ 0; U )



, (3) 

where C = ∇2G(Θ 0), W ∼ N p2 (0 , C △), and C△ is the covariance matrix defined in (iii). Moreover, 

lim  

> n→∞

P

h

patt  √n( bΘn − Θ0) = p

i

= P

h

patt ( bU ) = p

i

, (4) 

for every SLOPE pattern p ∈ { patt (U ) : U ∈ Sym( p)}.

The theorem follows from Corollary 3.4 [14]. 63.2 Graphical SLOPE with the Gaussian loss (GSLOPE) 

The following result is a direct consequence of Theorem 3.1 and applies beyond normally distributed data. 

Theorem 3.2. Let bΘn be the graphical SLOPE estimator, which minimizes the objective (1) 

with the loss function (2) . Assume that E[X4 

> i

] < ∞ for all i ∈ { 1, . . . , p }. Then, for the target parameter Θ0 = ( E[XX T ]) −1, the rescaled error √n( bΘn −Θ0) converges in distribution to the minimizer bU defined in (3) , where 

C = 12(Σ ⊗ Σ) ,C△ = Cov(vec( XX T )) /4.

Moreover, the SLOPE pattern of the error converges in the sense (4) .

In the special case where X ∼ N p(0 , Σ), the theorem above holds with C△ = C. Further-more, since Σ is positive definite, C is positive definite. Consequently, the objective in (3) is strictly convex, as it consists of a strictly convex quadratic form and the convex directional derivative U 7 → Pen ′(Θ 0; U ). It follows that the minimization problem (3) admits a unique solution. As a primary application of Theorem 3.2, we consider the case where X follows an elliptical distribution 

X = Σ 1/2uR, 

where u is uniformly distributed on the unit sphere Sp−1 ⊂ Rp and is independent of the random radius R ≥ 0. Observe that since E[u] = 0, we have E[X] = 0. Furthermore, using the fact that E[uu T ] = p−1Ip, we obtain Cov( X) = E[R2Σ1/2uu T Σ1/2] = E[R2]p−1Σ = Σ ,

provided that the radial component satisfies E[R2] = p. We obtain the following asymptotic result for elliptical distributions: 

Proposition 3.3. Assume that X = Σ 1/2uR follows an elliptical distribution with E[R2] = p.Then the asymptotic distribution is determined by (3) , with C = (Σ ⊗ Σ) /2 and C△ given by: 

C△ = C +

 E[R4]

p(p + 2) − 1

  

C + 14 vec(Σ) vec(Σ) T



. (5) We establish the identity (5) in Appendix 7.3. 

Example 3.4. (Gaussian distribution) The most common setting is the Gaussian model 

X ∼ N p(0 , Σ). A key property of this model is that the sparsity pattern of the precision matrix Θ 0 := Σ −1 encodes conditional independencies: (Θ 0)ij = 0 if and only if Xi ⊥⊥ Xj |

X−(i,j ) (see, for example, [18]). In the Gaussian case X ∼ N p(0 , Σ), the squared radius follows 7a chi-squared distribution, R2 ∼ χ2

> p

, with E[R2] = p. Moreover, since E[R4] = p(p + 2), the correction term in (5) vanishes, and the asymptotic distribution (3) holds with 

C△ = 12(Σ ⊗ Σ) = C. 

Alternatively, the fact that C = C△ follows from the standard result that, under correct model specification, the covariance of the score equals the expected Hessian of the loss function. When the condition E[R2] = p is not satisfied, such as for the multivariate t-distribution, where E[R2]/p = ν/ (ν − 2), the theoretical machinery still applies, but necessitates either a rescaling of the data or an adjustment to the target parameter. We illustrate these dynamics in the following example. 

Example 3.5. (Multivariate t-distribution) Building on the previous proposition, we now re-lax the assumption that E[R2] = p to consider a general elliptical distribution X = Θ −1/20 uR .However, for the minimizer of the Gaussian population risk to correctly identify the true pa-rameter Θ 0 governing the data distribution, we require E[XX T ] = Θ −10 , which holds if and only if E[R2] = p. If this condition is violated, the Gaussian loss targets a scaled version of the truth, and by Theorem 3.2, one estimates E[XX T ]−1̸ = Θ 0. Consequently, one must either rescale the target parameter or rescale the data to adjust for the inflation factor 

γ := E[R2]/p . We opt to rescale the data as ˜X = γ−1/2X = Θ −1/20 u ˜R, 

where ˜R = γ−1/2R. Since E[ ˜R2] = p, this setting reduces to the setup in Proposition 3.3 with X and R replaced by ˜X and ˜R, respectively. We have C = (Σ ⊗ Σ) /2, with Σ = Θ −10 =Cov( ˜X), and 

C△ = C +

γ−2E[R4]

p(p + 2) − 1

  

C + 14 vec(Σ) vec(Σ) T



.

For the multivariate t-distribution with degrees of freedom ν > 4, represented as X ∼Np(0 , Σ) /pχ2 

> ν

/ν , we have X = Σ 1/2uR with R =

q

χ2

> p

/(χ2 

> ν

/ν ). The scaling constant is 

γ = E[R2]/p = ν/ (ν − 2). Furthermore, 

E[R4] = ν2E[( χ2

> p

)2]E[( χ2 

> ν

)−2] = ν2 p(p + 2) (ν − 2)( ν − 4) < ∞,

where we used the identity E[( χ2 

> ν

)−2] = (( ν−2)( ν−4)) −1. This yields the following expression for the rescaled data ˜X = γ−1/2X = p1 − 2/νX :

C△ = C + 2

ν − 4



C + 14 vec(Σ) vec(Σ) T



.

We remark that C△ − C is positive definite; hence, the asymptotic covariance for the t-distributed data is larger (in the partial order on symmetric matrices) than that of the correctly specified Gaussian model. However, this difference is of order O(1 /ν ) as ν → ∞ .83.3 Graphical SLOPE with loss induced by the t-distribution (TSLOPE) 

Consider the multivariate t-distribution 

X ∼ Np(0 , Σ) 

pχ2 

> ν

/ν ,

with ν > 2, where Np(0 , Σ) and χ2 

> ν

denote independent random variables. This construction represents a Gaussian vector scaled by an independent scalar derived from the inverse chi-squared distribution. Equivalently, if Z ∼ N p(0 , I p) and v = ( χ2 

> ν

/ν )−1/2 are independent, then X = Θ −1/20 Z v follows this distribution, with covariance Cov( X) = νν−2 Σ. Normalizing Z to obtain a unit vector u = Z/ ∥Z∥ and separating the radial compo-nent yields the canonical elliptical representation X = Θ −1/20 u R 0. Here, R0 = ∥Z∥v ∼q

χ2

> p

/(χ2 

> ν

/ν ) is independent of u.More generally, we consider elliptical distributions of the form 

X = Θ −1/20 u R, 

where u is uniformly distributed on the unit sphere Sp−1 and R is a nonnegative random variable representing the radius. For the t-distribution, the negative log-likelihood induces, up to an additive constant, the loss function 

ℓ(x, Θ) = −12 log det(Θ) + ν + p

2 log  ν + x⊤Θx. (6) We consider the minimizer bΘn of (1) with the loss defined in (6). The subsequent theorem establishes that bΘn consistently estimates the parameter Θ 0 of the elliptical distribution 

X = Θ −1/20 uR , provided that 

E

 R2

ν + R2



= pν + p. (7) Note that the parameter Θ 0 is not the precision matrix Cov( X)−1 of the t-distributed vector 

X, but rather a scalar multiple thereof: Cov( X)−1 = p

E[R2]Θ0.

Consequently, the estimated precision matrix can be recovered as a scalar multiple of bΘn. For the special case R = R0, where E[R2]/p = ν/ (ν − 2), the precision is given by Cov( X)−1 =(1 − 2/ν )Θ 0.

Lemma 3.6. Assume that X = Θ −1/20 uR follows an elliptical distribution satisfying (7) .Then the Hessian C := ∇2ΘE[ℓ(X, Θ0)] , and the covariance C△ := Cov(vec( ∇Θℓ(X, Θ0))) 

have explicit representations as 

C = 12(Σ ⊗ Σ) − E[ξR]

p(p + 2) 



Σ ⊗ Σ + 12 vec(Σ) vec(Σ) T



, (8) 

C△ = C +

ν + p + 2 

p(p + 2) E[ξR] − 1

  12(Σ ⊗ Σ) + 14 vec(Σ) vec(Σ) T



, (9) 

where Σ = Θ −10 , and ξR = ( ν + p)R4/(ν + R2)2. Moreover, the Hessian C is positive definite. 

9Remark 3.7. Observe that for the special case of a t-distributed variable X ∼ N p(0 , Σ) /pχ2 

> ν

/ν ,where R = R0 ∼

q

χ2

> p

/(χ2 

> ν

/ν ) and E [ξR0 ] = p(p + 2) /(ν + p + 2), the quantities simplify to 

C = C△ = 12(Σ ⊗ Σ) − 1

ν + p + 2 



Σ ⊗ Σ + 12 vec(Σ) vec(Σ) T



.

We observe that as ν → ∞ , the limiting quantities approach (Σ ⊗ Σ) /2, as in the Gaussian model. 

Theorem 3.8. Assume that X = Θ −1/20 uR follows an elliptical distribution satisfying (7) ,and E[log(1 + R2)] < ∞. The rescaled error √n( bΘn − Θ0) converges in distribution to 

ˆU = argmin  

> U∈Sym( p)

12 vec( U )T C vec( U ) − W T vec( U ) + Pen ′(Θ 0; U )



, (10) 

where W ∼ N p2 (0 , C △) with C and C△ given by (8) and (9) .Moreover, 

lim  

> n→∞

P

h

patt (√n( bΘn − Θ0)) = p

i

= P

h

patt ( bU ) = p

i

,

for every SLOPE pattern p ∈ { patt (U ) : U ∈ Sym( p)}.

# 4 Simulations 

To estimate the lower-triangular coefficients of the precision Θ 0, we use the SLOPE estimator with the penalty function: Pen(Θ) = α ·

> p(p−1) /2

X

> j=1

λj |Θ|(j),

where α > 0 is the penalty strength, |Θ|(1) ≥ | Θ|(2) ≥ · · · ≥ | Θ|(p(p−1) /2) denote the ordered absolute values of the strictly lower-triangular entries of Θ. We use the BH( q)-normalized penalty sequence: 

λj = Φ −1



1 − qj p(p − 1) 



/¯λ, j = 1 , 2, . . . , p (p − 1) /2, (11) where Φ −1(·) denotes the quantile function of the standard normal distribution, q ∈ (0 , 1), and the normalization constant is given by ¯λ = 2

p(p − 1) 

> p(p−1) /2

X

> i=1

Φ−1



1 − qj p(p − 1) 



.

The constant ¯λ rescales the penalty sequence such that the average penalty coefficient equals 1. Figure 1 illustrates the Root Mean Square Error (RMSE) of the estimator: RMSE( bΘn) = 

r

E

h

∥vech( bΘn) − vech(Θ 0)∥22

i

, (12) 10 where bΘn is the SLOPE estimator of Θ 0 and vech( ·) denotes the vectorization of the lower-triangular part (including the diagonal). We set q = 0 .5 and plot the RMSE( bΘn) as a function of the tuning parameter α for various sample sizes n. We further compare the empirical RMSE of bΘn to its asymptotic counterpart lim 

> n→∞

√n RMSE( bΘn) = 

r

E

h

∥vech( bU )∥22

i

. (13) The term on the right-hand side is approximated by averaging the squared norms of M = 100 independent realizations of the vector bU , which are obtained by solving the asymptotic optimization problem in equation (3) for corresponding realizations of the noise vector W .We assume the data follows a multivariate Gaussian distribution X ∼ N p(0 , Σ), where the covariance matrix has a block-diagonal structure Σ = I2 ⊗ Σ0. Here, Σ 0 is a 10 × 10 matrix with compound symmetry, where Σ 0 

> i,i

= 1 and Σ 0 

> i,j

= 0 .2 for i̸ = j. I2 denotes the 2 × 2 identity matrix, and ⊗ denotes the Kronecker product. 

Figure 1: Convergence of the rescaled empirical RMSE, defined as √n · RMSE( bΘn) (where RMSE is given by Equation (12)), to the asymptotic error (Equation (13)) as a function of the sample size n. The figure compares the performance of Graphical Lasso and Graphical SLOPE using the BH( q = 0 .5) normalized sequence.  

> 4.1 Clustering Measure

One method to quantify how well the estimator bΘn recovers the clusters of Θ 0 is to measure the ℓ2-distance from the pattern space of Θ 0. To formalize this, let θ0+ = vec +(Θ 0) = (Θ ij )i>j 

11 denote the strictly sub-diagonal vectorization of Θ 0 in Rp(p−1) /2. We define the pattern space at θ0+ as 

V0 := span {u ∈ Rp(p−1) /2 : patt (u) = patt (θ0+)}.

For a more comprehensive analysis of the pattern space, we refer to [14]. Let P denote the orthogonal projection onto V0, and consider the strictly sub-diagonal error ˆ un := √n(vec +( bΘn) − vec +(Θ 0)). This error decomposes as 

∥ˆun∥22 = ∥P ˆun∥22

| {z }

> bias error

+ ∥(I − P )ˆ un∥22

| {z }

> clustering error

.

The clustering error corresponds to the squared ℓ2-distance of ˆ un from the subspace V0. For an illustration of this concept, we refer to Example 7.1 in the appendix. We define the root-n

rescaled clustering RMSE as: RMSE-CL( bΘn) := 

q

E [∥(I − P )ˆ un∥22].

Analogously, using the limiting variable ˆ u := vec +( bU ) derived from (3), we define the asymp-totic clustering error as RMSE-CL := 

q

E [∥(I − P )ˆ u∥22]. (14) This quantity represents the asymptotic limit of RMSE-CL( bΘn). In Figure 2, we compare the asymptotic strictly sub-diagonal Root Mean Squared Error, given by ( E[∥vec +( bU )∥22]) 1/2, with the clustering error ( E[∥(I − P ) vec +( bU )∥22]) 1/2 defined in (14), as a function of the tuning parameter α. The comparison includes the graphical Lasso and graphical SLOPE; for the latter, we use the normalized BH sequence (11) with q = 0 .5. Figure 3 compares the asymptotic error of GSLOPE (using the Gaussian loss (2)) and TSLOPE (using the t-distribution loss (6)) when the data is generated by a multivariate 

t-distribution with degrees of freedom ν ∈ { 5, 10 , 1000 } and covariance parameter Σ = Θ −10 .The asymptotic distribution for both methods is given by (3), which is fully characterized by the Hessian C, the covariance C△, and the penalty sequence. We set the penalty sequence to the normalized BH sequence (11) with q = 0 .5. To ensure GSLOPE consistently estimates Θ 0, we rescale the data by p1 − 2/ν prior to estimation. The asymptotic RMSE for GSLOPE is determined by the matrices: 

C = 12(Σ ⊗ Σ) and C△ = C + 2

ν − 4



C + 14 vec(Σ) vec(Σ) T



,

as detailed in Example 3.5. For the correctly specified minimization using the t-loss, the RMSE is determined by 

C = C△ = 12(Σ ⊗ Σ) − 1

ν + p + 2 



Σ ⊗ Σ + 12 vec(Σ) vec(Σ) T



,

following Remark 3.7. As expected, for ν = 5, the inflation factor 2 /(ν − 4) = 2 in GSLOPE dominates, resulting in a large error. However, this factor decays rapidly; for ν = 10, it drops to 1 /3, and, somewhat surprisingly, the correction factor ( ν + p + 2) −1 in TSLOPE causes a slight reversal in the relative performance in favor of GSLOPE. Finally, for ν = 1000, both expressions converge to the Gaussian limit, yielding indistinguishable performance. 12 Figure 2: Comparison of the asymptotic clustering error RMSE-CL (Equation (14)) versus the asymptotic total RMSE (Equation (13)) for Graphical Lasso and Graphical SLOPE with the normalized BH( q = 0 .5) sequence.  

> 4.2 Hidden Factor Structure

In what follows, we want to investigate how GSlope and TSlope, in comparison to GLasso and TLasso, are able to extract the dependence structure among variables that follow a block correlation structure, i.e. variables having a high dependence among those belonging to the same group and a low dependence to those of other groups. To create such a simulated envi-ronment, we draw on the financial literature and utilize a so called hidden factor structure, in which time series of asset prices (for example stock equities) are said to have exposures to various underlying combinations of so called risk factors - i.e. systematic sources of risk for which investors may demand a compensation if they cannot diversify them (see, e.g. [22, 9, 10]). Our simulation set-up follows closely the set-up in [17]: That is, assume that the underlying data generating process follows a hidden factor structure, in which the observed time series is dependent on a linear combination of a set of risk factors, each of which can have a distinct loading. Furthermore, assume that there are k assets, r (risk) factors and t observations with Ft×r = [ f1 f2 ... fr] where fj is the t × 1 return vector of the jth (risk) factor. Given that 

Br×k holds the loadings of the individual risk factors, the t × k hidden factor time series is given as: 

RHF = F × B + ϵ (15) 13 (a) ν = 5 (b) ν = 10 (c) ν = 1000 

Figure 3: Comparison of the asymptotic GSLOPE and TSLOPE errors, defined as lim n→∞ E[∥vech( √n( bΘn − Θ0)) ∥22], for heavy-tailed t-distributed data. The degrees of free-dom are ν = 5 (left), ν = 10 (center), and ν = 1000 (right). TSLOPE uses the normalized BH( q = 0 .5) sequence. The GSLOPE penalty is rescaled by factors of 2 .7, 1 .5, and 1, re-spectively, such that the minima of the curves match. where ϵ is a t × k matrix of error terms. For our simulation, we assume the following set-up: 1. let t = 1000, k = 300, r = 6, and let there be three distinct groups of assets 2. each risk factor j is independent from each other and follows a multivariate t distri-bution fj ∼ tp(0 , Ir×r, ν ) with ν = 4 degrees of freedom and where Ir×r is the identity matrix 3. the loading matrix Br×k is constructed by repeating each of the following column vectors k 

> 3

times: [0 .5 0 .5 0 0 0 0] ′, [0 0 0 .5 0 .5 0 0] ′, [0 0 0 0 0 .5 0 .5] ′

4. the error vectors ϵj , for j = 1 , . . . , k , are mutually independent across assets and independent of the risk factors. They are assumed to follow a multivariate t-distribution with mean zero, scale matrix 0 .15 × Ik×k, and ν = 4 degrees of freedom. Given the set-up above, we generate three distinct groups of assets that each have an ex-posure to exactly two distinct risk factors to which assets from other groups do not have an exposure. Furthermore, from (15) it follows that the covariance matrix of assets can be written as: 

ΣHF = B′B + 0 .15 × Ik×k (16) Figure 4 plots on the left the so-called true or oracle correlation matrix for the chosen hidden factor set-up and on the right the oracle precision matrix, i.e. the inverse of the covariance matrix. The correlation matrix clearly resembles the resulting block correlation structure stemming from the underlying hidden factor exposures, with assets in the same group having a high correlation and zero among assets which do not belong to the same group. Further-more, also for the oracle precision matrix the hidden factor structure is preserved. As all 14 of the studied methods directly estimate the precision matrix, we investigate how well the respective procedures are able to reproduce the structure of the oracle precision matrix. 

Figure 4: The figure shows the oracle correlation matrix and the oracle precision matrix for the hidden factor set-up, when choosing t = 1000, k = 300, r = 6 and each group has an exposure to exactly two distinct risk factors, to which the other groups do not have an exposure. While GLasso and TLasso are build on a sequence of one repeated lambda parame-ter, GSlope and TSlope require to define a decreasing sequence of tuning parameters, i.e. 

λ = ( λ1, ..., λ k). For the latter two, we follow [21] and choose the sequence of lambda pa-rameters according to Benjamini-Hochberg sequence for which the j-th tuning parameter is given as: 

λBH j = tν−2(1 − α×j 

> 2m

)

q

ν − 2 + t2

> ν−2

(1 − α×j 

> 2m

)

, ∀j = 1 , ..., k. (17) As discussed in [23], for α = 0 .1 this sequence targets a False Discovery Rate of 10% for block-diagonal matrices. Since our aim here is to illustrate the potential of the different methods in terms of estimation error, rather than FDR control, we rescale the λ sequences using the factor 

γ = ρ

¯λ,

where ρ is a grid of 20 equally spaced values between 0 and 2.5, and ¯λ denotes the average of the λ values obtained from (17). We then choose the λ sequence that minimizes the Frobenius norm distance between the oracle precision matrix and its estimate. The same procedure is used to select the tuning parameters for GLasso and TLasso, taking λ = γλ 1,

where λ1 is the first element of the sequence in (17). 15 In what follows, we compare the optimal performance of GLasso, TLasso, GSlope, and TSlope by simulating m = 100 return time series from (15) and reporting the estimation error of these procedures for the corresponding optimal value of λ.

Figure 5: The Figure shows from top left to bottom right: the oracle precision matrix, the estimated precision matrices of the GLasso, TLasso, GSlope and TSlope method, as well as the boxplots of the minimum Frobenius norm distances wrt. the orcale precision matrix across the m = 100 simulation runs for the four methods, respectively. The matrices were selected based on the last simulation run and choosing the matrices across the grid of tuning parameters with the minimum Frobenius norm distance. Simulations have been performed by choosing t = 1000, k = 300 and r = 6, and assuming that each group has an exposure to exactly two distinct risk factors to which the other groups do not have an exposure. Figure 5 reports next to the estimated precision matrices for the GLasso, TLasso, GS-lope and TSlope procedures, again the oracle precision matrix, as well as the boxplots of the minimum Frobenius norm distances for the respective procedures across the m = 100 simu-lation runs. The reported matrices have been selected based on the last simulation run and choosing the matrix with the minimum Frobenius norm distance wrt. the oracle precision matrix, considering the grid of tuning parameters for the respective method. The figure shows that all methods are able to resemble the underlying grouping structure. Still, looking closer at the results, we can see that TSlope performs best in extracting not only the dependence of assets across groups, but also the strength of dependence between assets in the same group and when comparing the estimated matrix with the oracle precision matrix. Furthermore, while for the other methods it appears that they capture the inter-group dependence among assets well, the dependence among assets within the same group can deviates significantly in magnitude from the oracle precision matrix. This deviation appears to be especially prone for the GLasso procedure. The superiority of the TSlope 16 method is also confirmed by the boxplots from Figure 5, in which TSlope shows the lowest median Frobenius norm distance across the m = 100 simulation runs and across all methods. Furthermore, we can observe that GLasso shows the worst median across all methods. 17 5 Empirical Evidence from Portfolios Sorted by Size and Book-to-Market 

We complement the theoretical and simulation results presented in the previous sections with an empirical application aimed at assessing the clustering properties of the proposed estimators in a real-data setting. While the previous sections analyze the asymptotic be-havior and pattern recovery properties under controlled data-generating processes, empirical data imply a more challenging exercise, as the underlying clustering structure is unknown and cannot be directly observed. To mitigate this difficulty, we exploit the presence of eco-nomically meaningful characteristics that induce a natural ordering among the variables. In particular, the empirical application considered in this section relies on data structured along two important dimensions: firm size and book-to-market ratio. These two characteristics provide a plausible proxy for latent similarity across variables. This setting allows us to investigate clustering behavior in the estimated precision matrices even in the absence of an observable ground-truth clustering. Specifically, we employ the data provided by Kenneth R. French’s data library, and focus on the “25 Portfolios Formed on Size and Book-to-Market” dataset, which is widely used in the financial literature (see, e.g. [21]) and provides a natural setting for investigating structured dependence patterns. This dataset consists of 25 portfolios, which are constructed at the end of each June as the intersections of five portfolios sorted on firm size (market equity, ME) and five portfolios sorted on the book-to-market ratio (BE/ME). Size breakpoints for year t are defined using NYSE market equity quintiles at the end of June of year t,while BE/ME breakpoints are based on NYSE quintiles, where BE/ME for June of year 

t is computed as book equity for the last fiscal year ending in t − 1 divided by market equity in December of t − 1. The portfolios include all NYSE, AMEX, and NASDAQ stocks for which market equity data are available for December of t − 1 and June of t,together with positive book equity data for year t − 1. Further details on the construction of the portfolios and data availability can be found in the Kenneth R. French Data Library (https://mba.tuck.dartmouth.edu/pages/faculty/ken.french/ ). In our empirical analysis, we use daily value-weighted returns, expressed in percentage points (i.e., multiplied by 100). Our sample spans the period from January 3, 2000 to October 31, 2025, for a total of 6,498 trading days. We reorder the 25 portfolios according to the BE/ME dimension. Therefore, portfolios sharing similar BE/ME characteristics are placed closer to each other, highlighting potential block structures in the dependence matrix. The resulting ordering follows the natural progression from low to high BE/ME portfolios, while preserving the ME classification within each group. As a first descriptive overview of the data, Figure 6 reports boxplots of the daily returns across the 25 portfolios. The figure highlights substantial heterogeneity across portfolios. In particular, in terms of volatility, the BIG.HiBM portfolio exhibits the largest dispersion, with a standard deviation equal to 1.801, whereas ME5.BM2 displays the lowest volatility, with a standard deviation of 1.187. Moreover, all portfolios show the presence of pronounced extreme observations, both negative and positive, which is reflected in high kurtosis val-ues. Specifically, kurtosis ranges from a minimum of 6.695 for ME2.BM1 to a maximum of 17.935 for ME5.BM4. Finally, in terms of average returns, the distributions appear to be 18 substantially centered around zero. 

Figure 6: Boxplots of daily value-weighted returns for the 25 portfolios sorted by size and book-to-market (BE/ME) ratio computed from January 3, 2000 to October 31, 2025. To further investigate the structure of the data, we analyze the time dynamics of portfolio returns. Figure 7 reports the evolution over time of the average daily returns for each book-to-market (BE/ME) class. Several stylized facts emerge from the figure. First, the series display clear evidence of volatility clustering [8], with relatively tranquil periods alternating with episodes of pronounced turbulence. This pattern is consistent with well-known features of financial time series and suggests the presence of regime-like dynamics in return variabil-ity. Periods of high volatility are clearly associated with well-known tail events, including the burst of the dot-com bubble in the early 2000s, the global financial crisis of 2007–2009, and the COVID-19 pandemic. In the final part of the sample, increased fluctuations due to trade-policy tensions and tariff-related uncertainty are also observed. Overall, the figure highlights strong common dynamics across BE/ME classes, while still allowing for hetero-geneous behavior across groups, thereby motivating the subsequent analysis of dependence and clustering patterns in the estimated precision matrices. After describing the main features of the data, we now turn to the empirical strategy adopted to investigate clustering patterns in the estimated precision matrices. In line with the methodology developed in the previous sections, clustering is performed directly on the off-diagonal entries of the precision matrix. While in the simulation study this approach allows a direct comparison with the true underlying parameters, in the empirical application the true clustering structure is not observable, and therefore clustering quality must be assessed using internal validation criteria. To this end, we estimate GSLOPE and TSLOPE separately for each year in the sample period, from 2000 to 2025, obtaining a total of 26 precision matrix estimates for each model and each corresponding tuning-parameter sequence. Estimation on yearly subsamples allows 19 Figure 7: Time evolution of average daily value-weighted returns from January 3, 2000 to October 31, 2025; for each BE/ME class, the plotted series corresponds to the cross-sectional average of the five size-based portfolios. us to capture potential time variation in dependence structures and provides multiple real-izations that increase the stability of the clustering analysis. Before estimation, returns are standardized within each year. This step ensures comparability across subsamples and miti-gates the effect of the pronounced volatility regimes highlighted in Figure 7, where periods of relative stability alternate with episodes of elevated market turbulence. Standardization then prevents differences in scale from mechanically affecting the estimated dependence structure. The penalization framework follows the one introduced in the simulation study. In par-ticular, the overall level of regularization is controlled by a scalar parameter α multiplying a BH-type sequence. Differently from the simulation setting, where the BH( q)-normalized sequence is based on Gaussian quantiles, in the empirical analysis we adopt a finite-sample version constructed from Student-t quantiles. Let p denote the number of variables and 

m = p(p − 1) /2 the number of strictly upper-triangular entries of the precision matrix. In our empirical application, we have p = 25 (i.e., the number of portfolios), which yields 

m = 300 distinct off-diagonal entries. For k = 1 , . . . , m , we then define: 

tk = t−1

> n−2



1 − q k 

2m



,

and 

λk = tk

p(n − 2) + t2

> k

,

where t−1

> n−2

(·) denotes the quantile function of the Student-t distribution with n − 2 degrees of freedom; this construction yields a decreasing BH-type sequence of tuning parameters. 1  

> 1Throughout the empirical analysis we set q= 0 .05, in line with the conventional BH level used for

20 As in the simulation analysis, the global penalty strength is governed by the scalar parameter α, which is explored over a logarithmically spaced grid from 10 −4 to 4, yielding 200 points along the regularization path. This wide range of α values allows us to investigate the behavior of the estimated precision matrices across different levels of regularization, from weak to strong shrinkage, and to assess the stability of the clustering structure across sparsity regimes. For each model and for each tuning-parameter sequence, we retain the strictly upper-triangular part of the estimated precision matrix, excluding the main diagonal. Since p = 25, this yields m = 300 distinct edge coefficients for each yearly estimation. After vectoring these estimates across the 26 yearly subsamples, we construct a 300 × 26 matrix, where each row corresponds to a specific edge ( i, j ), with i < j , and each column represents a yearly realization. Hence, each edge is characterized by a vector describing its temporal evolution over the sample period. Clustering is then performed by applying k-means to this matrix, grouping together edges that exhibit similar temporal patterns. We implement the k-means clustering method for different numbers of clusters and, for each of them, assess the clustering quality using the Calinski–Harabasz index [7]. 2 This criterion balances within-cluster cohesion and between-cluster separation, with higher values indicating a more pronounced and well-separated clustering structure. Therefore, for each empirical setup, we compare the clustering performance of the proposed GSLOPE and TSLOPE estimators with the GLASSO benchmark. We stress the fact that GSLOPE and TSLOPE rely on a sequence of tuning parameters 

{λk}, whereas GLASSO is controlled by a single scalar regularization parameter. To ensure a meaningful comparison across methods, the GLASSO penalty level is calibrated using the average magnitude of the SLOPE tuning sequence. Specifically, we set the GLASSO tuning parameter equal to  3.5 α λ , where λ denotes the average value of the tuning-parameter sequence defined for the SLOPE penalty function. The multiplicative factor 3 .5 is empirically chosen in order to align the overall magnitude of the Calinski-Harabasz indicator across methods, thereby allowing a more informative comparison of clustering patterns along the regularization path. Figure 8 reports the Calinski-Harabasz index as a function of ln( α) for GSLOPE, TS-LOPE, and GLASSO in the case of three clusters. Similar patterns are obtained by imple-menting the k-means method with different numbers of clusters. 3 We can see from Figure 8 that TSLOPE systematically achieves the highest values of the Calinski–Harabasz index across the entire grid of α values, indicating a more pronounced and stable clustering struc-ture. GSLOPE follows closely, while GLASSO displays substantially lower values throughout the regularization path. This ordering suggests that the structured penalization induced by SLOPE-based estimators leads to more clearly separated clusters compared with the stan-     

> two-sided testing.
> 2Preliminary analyses showed that performing clustering on edge vectors constructed across the 26 yearly estimations (i.e., using 300 ×26 matrices) leads to substantially more stable Calinski-Harabasz values along the regularization path compared with clustering based on a single cross-sectional estimate (i.e., a 300 ×1vectors). This motivates the use of the temporal dimension to enhance the robustness of the clustering assessment.
> 3For brevity, these additional results are not reported here but are available from the authors upon request.

21 Figure 8: Calinski-Harabasz index as a function of ln( α) for GSLOPE, TSLOPE, and GLASSO, reported for the case of three clusters. dard sparsity-based benchmark. In particular, the Calinski–Harabasz index reaches its over-all maximum under TSLOPE, with a value of 444.775 attained at α = 0 .279 (corresponding to ln( α) = −1.277 in Figure 8), highlighting the strongest clustering structure along the regularization path. To assess whether the clustering results obtained from the Calinski-Harabasz analysis have an economically meaningful interpretation, we focus on the best-performing configu-ration while maintaining the analysis fixed at three clusters. Specifically, we consider the TSLOPE model estimated setting α = 0 .279, where the Calinski-Harabasz index reaches its maximum. Based on this specification, we extract the cluster allocation of the off-diagonal entries of the 26 precision matrices estimated for each year from 2000 to 2025, and visualize the resulting structure in Figure 9. Figure 9 displays the cluster assignment of each entry (i, j ), with i, j = 1 , . . . , 25 and i < j , represented within a 25 × 25 matrix whose ordering follows the size and book-to-market structure described above. The three clusters are iden-tified with three different colors: white (cluster 0), green (cluster 1) and orange (cluster 2). The label “cluster 0” is adopted purely for visualization purposes; as discussed below, this cluster predominantly corresponds to zero-valued entries across the underlying 26 precision matrices, representing the absence of conditional dependence between portfolio pairs. Clus-ters 1 and 2, in contrast, capture non-zero dependence patterns, highlighting heterogeneous interaction structures across portfolios. The representation given in Figure 9 provides an intuitive way to assess whether the statistically identified clusters exhibit economically in-terpretable patterns. In particular, it allows us to visually inspect the presence of localized dependence patterns that may be related to the economic characteristics underlying the portfolio construction. Figure 9 reveals a clear spatial organization of the identified clusters. Cluster 0, repre-22 sented in white, is predominantly located farther away from the main diagonal. As antici-pated, this cluster mainly corresponds to zero-valued entries of the underlying 26 precision matrices, indicating the absence of conditional dependence between portfolios that are rel-atively distant in terms of their economic characteristics. In contrast, cluster 1 tends to concentrate closer to the main diagonal. As will be shown below, this cluster is associated with the largest entries, in absolute value, of the estimated precision matrices, corresponding to stronger conditional dependence relationships. Given the ordering of portfolios according to size and book-to-market characteristics, this pattern suggests that stronger dependencies arise primarily among portfolios sharing similar economic features. In other words, proxim-ity in the ordering dimension appears to be associated with higher similarity in dependence structure. Overall, Figure 9 highlights a localized dependence pattern, where economically similar portfolios exhibit stronger interactions, while more distant portfolios tend to be con-ditionally independent. To further support the interpretation suggested by Figure 9, Figure 10 reports boxplots of the off-diagonal entries (upper-triangular part) of the 26 yearly precision matrices esti-mated using TSLOPE with α = 0 .279, corresponding to the best-performing configuration identified in Figure 8. For visual clarity, outliers are omitted from the boxplots in Figure 10 in order to facilitate the comparison of the central distributional patterns across clusters and years. The boxplots are shown separately for each year from 2000 to 2025 and grouped according to the cluster assignment obtained from the clustering analysis. A clear pattern emerges from the figure. Across all years, the strongest relationships in absolute value are systematically associated with cluster 1, confirming that this cluster captures the most pro-nounced conditional dependence structure. As discussed above, these links mainly involve portfolios that are closer in terms of size and book-to-market characteristics. Interestingly, the strongest relationships are observed during the COVID-19 pandemic (year 2020). Cluster 2 displays intermediate magnitudes, while cluster 0 remains largely concentrated around zero, consistently with its interpretation as representing weak or absent conditional dependence. As a final evidence supporting the previous findings, Figure 11 provides a network repre-sentation of the 25 portfolios based on the clustering structure obtained from the TSLOPE specification with α = 0 .279. Nodes are arranged on a circular layout and colored accord-ing to their BE/ME class, allowing a direct comparison between the economic grouping of portfolios and the dependence structure implied by the estimated precision matrix. Link colors follow the same cluster coding used in Figure 9: green links correspond to cluster 1, associated with stronger conditional dependence, while orange links correspond to cluster 2, capturing intermediate dependence levels. Links belonging to cluster 0 are displayed in white, reflecting near-zero relationships. The resulting network visualization highlights a clear structural pattern. The central area appears relatively sparse, whereas stronger con-nections tend to concentrate along the periphery of the network, linking portfolios that are closer in terms of their economic characteristics. This evidence is consistent with the pre-vious results and further supports the presence of localized dependence structures among economically similar portfolios. Overall, the empirical analysis presented in this section provides consistent evidence in favor of the proposed SLOPE-based estimators, and in particular TSLOPE, in capturing meaningful clustering structures in the precision matrix. The combination of internal val-idation criteria, heatmap visualization, distributional analysis, and network representation 23 highlights a clear pattern of localized dependence, where stronger conditional relationships emerge primarily among portfolios sharing similar economic characteristics. These findings suggest that the structured penalization induced by SLOPE not only improves statistical clustering performance but also reveals economically interpretable dependence structures in real financial data. 

# 6 Conclusion 

This paper develops an asymptotic theory for Graphical SLOPE in precision matrix es-timation, with particular emphasis on recovering structured dependence patterns beyond entrywise sparsity. In a fixed-p asymptotic framework, we derive the limiting distribution of the rescaled estimation error, √n( bΘn − Θ0), and of the asymptotic clustering pattern, for GSLOPE and TSLOPE and when the data are generated from multivariate elliptical distri-butions. A comparison of the asymptotic distributions highlights the advantage of gSLOPE over gLASSO when the precision matrix contains many equal entries. It also shows the advantage of TSLOPE over GSLOPE when the data are heavy-tailed. These results clarify the interaction between loss specification, tail behavior, and structured regularization, and show that asymptotic performance depends not only on sparsity but also on the extent to which the loss is adapted to the data-generating distribution. These qualitative findings are further supported by simulation results in a high-dimensional setting and by an empirical application to portfolios sorted by size and book-to-market. In that setting, SLOPE-based estimators, especially TSLOPE, reveal stable and economically interpretable clustered dependence structures that are less apparent under standard sparsity-based methods. This suggests that structured penalties may be useful not only for improving statistical efficiency, but also for enhancing the interpretability of estimated financial net-works. Overall, the paper provides a theoretical and empirical foundation for using Graphical SLOPE and TSLOPE as tools for sparse and clustered precision matrix estimation. Several directions remain open for future research. A natural next step is to extend the present low-dimensional asymptotic analysis to high-dimensional regimes in which both p and n

grow jointly. It would also be of interest to study analogous pattern-recovery questions for partial-correlation-based formulations and for other structured penalties in graphical models. More broadly, our results suggest that clustering-aware asymptotic theory may provide a useful framework for understanding how modern regularization methods recover interpretable dependence structures in multivariate data. 

# Acknowledgments 

IH, MB and JW acknowledge the support of the Swedish Research Council, grant no. 2020-05081. Sandra Paterlini would like to thank the EU COST Action CA21163 (HiTEc) “Text, functional and other high-dimensional data in econometrics: New models, methods, appli-cations”. 24 References 

[1] Theodore Wilbur Anderson. An introduction to multivariate statistical analysis , vol-ume 2. Wiley New York, 1958. [2] Yoav Benjamini and Yosef Hochberg. Controlling the false discovery rate: A practical and powerful approach to multiple testing. Journal of the Royal Statistical Society: Series B (Methodological) , 57(1):289–300, 1995. [3] Ma lgorzata Bogdan, Adam Chojecki, Ivan Hejn´ y, Bartosz Ko lodziejek, and Jonas Wallin. Identifying network hubs with the partial correlation graphical lasso. arXiv preprint arXiv:2508.12258 , 2025. [4] Ma lgorzata Bogdan, Ewout van den Berg, Chiara Sabatti, Weijie Su, and Emmanuel J. Cand` es. SLOPE—adaptive variable selection via convex optimization. The Annals of Applied Statistics , 9(3):1103–1140, 2015. [5] Ma lgorzata Bogdan, Xavier Dupuis, Piotr Graczyk, Bartosz Ko lodziejek, Tomasz Skalski, Patrick Tardivel, and Maciej Wilczy´ nski. Pattern recovery by slope. Applied and Computational Harmonic Analysis , 80:101810, 2026. [6] Tony Cai, Weidong Liu, and Xi Luo. A constrained ℓ1 minimization approach to sparse precision matrix estimation. Journal of the American Statistical Association ,106(494):594–607, 2011. [7] T. Calinski and J. Harabasz. A dendrite method for cluster analysis. Communications in Statistics - Theory and Methods , 3(1):1–27, 1974. [8] R. Cont. Empirical properties of asset returns: stylized facts and statistical issues. 

Quantitative Finance , 1(2):223–236, 2001. [9] Eugene F. Fama and Kenneth R. French. Common risk factors in the returns on stocks and bonds. Journal of Financial Economics , 33(1):3–56, 1993. [10] Eugene F. Fama and Kenneth R. French. A five-factor asset pricing model. Journal of Financial Economics , 116(1):1–22, 2015. [11] Jerome Friedman, Trevor Hastie, and Robert Tibshirani. Sparse inverse covariance estimation with the graphical lasso. Biostatistics , 9(3):432–441, 2008. [12] Wenjiang Fu and Keith Knight. Asymptotics for lasso-type estimators. Annals of Statistics , 28(5):1356–1378, 2000. [13] Piotr Graczyk, Hideto Nakashima, et al. From graphical lasso to atomic norms: High-dimensional pattern recovery. arXiv preprint arXiv:2506.13353 , 2025. [14] Ivan Hejn´ y, Jonas Wallin, and Ma lgorzata Bogdan. Asymptotic distribution of low-dimensional patterns induced by non-differentiable regularizers under general loss func-tions. arXiv preprint arXiv:2506.12621 , 2025. 25 [15] Ivan Hejn´ y, Jonas Wallin, Ma lgorzata Bogdan, and Micha l Kos. Unveiling low-dimensional patterns induced by convex non-differentiable regularizers. Annals of the Institute of Statistical Mathematics , pages 1–36, 2025. [16] Søren Højsgaard and Steffen L. Lauritzen. Graphical gaussian models with edge and vertex symmetries. Journal of the Royal Statistical Society: Series B , 70(5):1005–1027, 2008. [17] Philipp J. Kremer, Sangkyun Lee, Malgorzata Bogdan, and Sandra Paterlini. Sparse portfolio selection via the sorted ℓ1-norm. Journal of Banking and Finance , 110, 2020. [18] Steffen L Lauritzen. Graphical models , volume 17. Clarendon Press, 1996. [19] Nicolai Meinshausen and Peter B¨ uhlmann. High-dimensional graphs and variable selec-tion with the Lasso. The Annals of Statistics , 34(3):1436–1462, 2006. [20] Pradeep Ravikumar, Martin J. Wainwright, Garvesh Raskutti, and Bin Yu. High-dimensional covariance estimation by minimizing ℓ1-penalized log-determinant diver-gence. Electronic Journal of Statistics , 5:935–980, 2011. [21] Riccardo Riccobello, Giovanni Bonaccolto, Philipp J. Kremer, Piotr Sobczyk, Ma lgorzata Bogdan, and Sandra Paterlini. Sparse graphical modelling for global mini-mum variance portfolio. Computational Management Science , 22(2), 2025. [22] Stephen A. Ross. The arbitrage theory of capital asset pricing. Journal of Economic Theory , 13(3):341–360, 1976. [23] Piotr Sobczyk. Identifying Low-Dimensional Structures Through Model Selection in High-Dimensional Data . PhD thesis, Wroc law University of Science and Technology, Wroc law, Poland, 2019. [24] A. W. van der Vaart. Asymptotic Statistics . Cambridge Series in Statistical and Prob-abilistic Mathematics. Cambridge University Press, Cambridge, 1998. 26 7 Appendix 

7.1 Pattern, Pattern Space and Clustering Error of SLOPE 

Example 7.1. We illustrate the notion of a SLOPE pattern, the pattern space on the following example. Consider a precision matrix Θ 0 and its lower-triangular vectorization vec +(Θ 0): Θ0 =



1 0.6 0.6 0.40.6 1 0.6 −0.40.6 0.6 1 00.4 −0.4 0 1

 , vec +(Θ 0) = (0 .6, 0.6, 0.4, 0.6, −0.4, 0) The SLOPE pattern is given by 

patt (vec +(Θ 0)) = (2 , 2, 1, 2, −1, 0) ,

and the pattern space V0 := span {θ ∈ Rp(p−1) /2 : patt (θ) = patt (vec +(Θ 0)) } by 

V0 = {(a, a, b, a, −b, 0) : a, b ∈ R}

Example 7.2. Consider Θ 0 from the previous example. Suppose the rescaled error bUn =√n( bΘn − Θ0) is 

bΘn =



1.1 0.65 0.68 0.37 0.65 0.95 0.68 −0.37 0.68 0.68 1.05 0.04 0.37 −0.37 0.04 0.9

 bUn =



1 0.5 0.8 −0.30.5 −0.5 0.8 0.30.8 0.8 0.5 0.4

−0.3 0.3 0.4 −1



The lower-triangular vectorization ˆ un = vec( bUn) decomposes into ˆun = (0 .5, 0.8, −0.3, 0.8, 0.3, 0.4) = (0 .7, 0.7, −0.3, 0.7, 0.3, 0) 

| {z } 

> Pˆun∈V0

+ ( −0.2, 0.1, 0, 0.1, 0, 0.4) 

| {z }  

> (I−P)ˆ un∈V⊥
> 0

,

where P denotes the projection matrix onto V0. The total error is 

∥ˆun∥22 = 0 .52 + 0 .82 + ( −0.3) 2 + 0 .82 + 0 .32 + 0 .42 = 1 .87 ,

of which the clustering error is: 

∥(I − P )ˆ un∥22 = ( −0.2) 2 + 0 .12 + 0 .12 + 0 .42 = 0 .22 .

In this example, 0 .22 /1.87 = 11 .8% of the total estimation error are due to clustering error, the rest 88 .2% are due to the bias error. 27 7.2 Directional Derivative of Graphical SLOPE 

For the graphical SLOPE norm with λ1 > · · · > λ p(p−1) /2 > 0, Pen(Θ) = 

> p(p−1) /2

X

> j=1

λj | vec +(Θ) |(j),

we find the explicit expression for the directional SLOPE derivative Pen ′(Θ 0; U ) = lim 

> ε→0

1

ε (Pen(Θ 0 + εU ) − Pen(Θ 0)) .

Let θ0 = vec +(Θ 0) and u = vec +(U ) be the strictly subdiagonal vectorizations in Rp(p−1) /2.The directional derivative is Pen ′(Θ 0; U ) = 

> p(p−1) /2

X

> j=1

λπ(j)

uj sgn( θ0 

> j

)I[θ0

> j

̸ = 0] + |uj |I[θ0 

> j

= 0]  ,

where π is a permutation of size p(p − 1) /2, which sorts θ0 + εu by its absolute magnitudes; 

|θ0 + εu |π−1(1) ≥ · · · ≥ | θ0 + εu |π−1(p(p−1) /2) 

as ε ↘ 0. 

7.3 Proofs for the Gaussian Loss 

Proof of Theorem 3.2. The statement follows from Theorem 3.1. Conditions (i)–(v) have been verified in Theorem 3 of [3]. For completeness, we explicitly verify them here. First, observe that the loss function 

ℓ(X, Θ) = −12 log det(Θ) + 12 tr(Θ XX T )is a smooth map on the parameter space Sym +(p) for every fixed X ∈ Rp. The derivatives are given by 

∇Θℓ(X, Θ) = 12(−Θ−1 + XX T ),

∇2Θℓ(X, Θ) = 12(Θ −1 ⊗ Θ−1).

Furthermore, the risk function takes the explicit form 

G(Θ) = E[ℓ(X, Θ)] = −12 log det(Θ) + 12 tr(ΘΣ) ,

where Σ = E[XX T ]. We fix an open neighborhood B of Θ 0 = Σ −1 in Sym +(p) such that there exist constants r+, r − > 0 ensuring that all matrices in B have eigenvalues uniformly bounded: 

r− ≤ inf  

> Θ∈B

λmin (Θ) ≤ sup 

> Θ∈B

λmax (Θ) ≤ r+. (18) 28 This implies that the closure of B is a compact subset of Sym +(p). We now verify conditions (i)–(v) from Theorem 3.1: 

Condition (i) : Note that the Hessian ∇2Θℓ(X, Θ) = 12 (Θ −1 ⊗ Θ−1) does not depend on 

X. The continuity of the inverse map and the compactness of the closure of B imply that sup Θ∈B ∥∇ 2Θℓ(X, Θ) ∥ ≤ M for some constant M > 0. 

Condition (ii) : The function G(Θ) is smooth (and thus C3) on B. The Hessian C = 

> 12

(Θ −10 ⊗ Θ−10 ) is positive definite because Θ −10 = Σ is positive definite. 

Condition (iii) : This is satisfied because the expected gradient vanishes at the truth: 

E[∇Θℓ(X, Θ0)] = 12(−Θ−10 + E[XX T ]) = 12(−Σ + Σ) = 0 .

Furthermore, C△ = Cov(vec( XX T )) /4 is finite due to the finiteness of the fourth moment 

E[∥X∥4] < ∞.

Condition (iv) : To establish uniform tightness, observe that the unpenalized mini-mization in (1) yields the MLE, bΘMLE  

> n

= ( 1

> n

Pni=1 X(i)X(i)T

)−1, which converges to the true precision Θ 0 = Σ −1 by the Law of Large Numbers. The penalization term n−1/2 Pen(Θ) is of order O(n−1/2) and becomes negligible asymptotically. Consequently, the penalized estimator satisfies bΘnp

−→ Θ0 as n → ∞ . The sequence is consistent, hence also uniformly tight. 

Condition (v) : A uniform envelope bound is obtained by applying the Cauchy–Schwarz inequality: tr(Θ XX T ) ≤ ∥ Θ∥F ∥XX T ∥F .

Using the continuity of log det(Θ) and the Frobenius norm ∥Θ∥F , combined with the com-pactness of the closure of B, there exist constants c1, c 2 such that sup Θ∈B |ℓ(X, Θ) | ≤

c1 + c2∥XX T ∥F . This envelope is integrable due to the finiteness of the second moments. Thus, the loss satisfies all regularity conditions required in Corollary 3.4 of [14], and Theorem 3.2 follows. For the next proposition and subsequent results, we introduce the following notation: 

Notation. For any matrices M1, M 2 ∈ Rp2×p2

, we write 

M1 ≡ M2,

whenever M1 vec( A) = M2 vec( A) for every A ∈ Sym( p). In other words, M1 ≡ M2 if the linear maps coincide when restricted to the subspace of vectorized symmetric matrices. 

Proposition 7.3. Let u be a random vector uniformly distributed on the unit sphere Sp−1.Then 

E[uu T ⊗ uu T ] ≡ 1

p(p + 2) 

 2Ip2 + vec( I) vec( I)T  , (19) 

in the sense of the notation defined above. 

29 Proof. We begin with the general formula for the fourth moments of the spherical distribu-tion: 

E[uu T ⊗ uu T ] = 1

p(p + 2) (Ip2 + Kp + vec( I) vec( I)T ),

where Kp is the commutation matrix characterized by Kp vec( A) = vec( AT ) for every matrix 

A. This identity can be derived by expressing u = Z/ ∥Z∥, where Z ∼ N p(0 , I p). Since u is independent of the radius ∥Z∥, we have 

E[uu T ⊗ uu T ] = E[ZZ T ⊗ ZZ T ]

E[∥Z∥4] .

Using Isserlis’ theorem, the numerator expands to terms involving Kronecker deltas, which corresponds to Ip2 + Kp + vec( I) vec( I)T in matrix notation. The denominator is given by 

E[∥Z∥4] = p(p + 2) (see, e.g., Subsection 2.6.2 in [1]). Finally, observe that for any symmetric matrix A ∈ Sym( p), Kp vec( A) = vec( AT ) = vec( A). Thus, Kp ≡ Ip2 on the subspace of symmetric matrices, and the result follows. 

Proof of Proposition 3.3. It remains to establish the representation for the covariance in (5). From Theorem 3.2, we have 

C△ = 14 Cov(vec( XX T )) = 14

 E XX T ⊗ XX T  − vec(Θ −10 ) vec(Θ −10 )T 

= 14(Θ −1/20 ⊗ Θ−1/20 )  E[R4]E uu T ⊗ uu T  − vec( I) vec( I)T  (Θ −1/20 ⊗ Θ−1/20 ).

For the special case of the Gaussian distribution, where R20 ∼ χ2

> p

, we have the fourth moment 

E[R40] = p(p + 2), and the identity C = C△. We can express the general covariance by adding and subtracting C expressed as C△ above with R = R0. Note that the constant term involving vec( I) vec( I)T cancels out in the difference: 

C△ = C + ( C△ − C)= C + 14

 E[R4] − p(p + 2)  (Θ −1/20 ⊗ Θ−1/20 )  E uu T ⊗ uu T  (Θ −1/20 ⊗ Θ−1/20 )

≡ C + 14

 E[R4]

p(p + 2) − 1



(Θ −1/20 ⊗ Θ−1/20 )  2Ip2 + vec( I) vec( I)T  (Θ −1/20 ⊗ Θ−1/20 )= C +

 E[R4]

p(p + 2) − 1

  

C + 14 vec(Θ −10 ) vec(Θ −10 )T



,

where we have used the identity (19) and the definition of C in the final steps. 30 7.4 Proofs for the t-Loss 

Proof of Lemma 3.6. To derive the explicit expressions for the Hessian C and the covariance 

C△, we consider the derivatives of the loss (6): 

∇Θℓ(x, Θ) = −12Θ−1 + ν + p

21

ν + xT Θxxx T ,

∇2Θℓ(x, Θ) = 12(Θ −1 ⊗ Θ−1) − ν + p

21(ν + xT Θx)2 vec( xx T ) vec( xx T )T .

Step 1: (Derivation of the Hessian C). From the representation X = Θ −1/20 uR , we obtain: 

XT Θ0X = R2 and XX T

XT Θ0X = Θ −1/20 uu T Θ−1/20 .

Substituting these into the Hessian formula and taking expectations yields 

E[∇2Θℓ(X, Θ0)] = 12(Θ −10 ⊗ Θ−10 ) − E

"

ν + p

2(XT Θ0X)2

(ν + XT Θ0X)2

vec( XX T )

XT Θ0X vec 

 XX T

XT Θ0X

T !# 

= 12(Θ −1/20 ⊗ Θ−1/20 )  I ⊗ I − E[ξR] · E[uu T ⊗ uu T ] (Θ −1/20 ⊗ Θ−1/20 ),

where ξR = ( ν + p)R4/(ν + R2)2. Using identity (19) for E[uu T ⊗ uu T ] and simplifying leads directly to the expression for C in (8). 

Step 2: (Derivation of the covariance C△). First, we verify the consistency condition. From X = Θ −1/20 uR , we have: 

E

 XX T

ν + XT Θ0X



= E

 R2

ν + R2 Θ−1/20 uu T Θ−1/20



= E

 R2

p(ν + R2)



Θ−10 .

Therefore, the first-order condition holds: 

E[∇Θℓ(X, Θ0)] = 0 ⇐⇒ E

(ν + p)R2

p(ν + R2)



= 1 , (20) which matches the assumption (7). We now compute the covariance of the score vector: 

C△ := Cov(vec( ∇Θℓ(X, Θ0))) = Cov 

ν + p

2

XT Θ0Xν + XT Θ0X vec 

 XX T

XT Θ0X

 

= 14 Cov 

(ν + p)R2

ν + R2 vec 



Θ−1/20 uu T Θ−1/20



.

Using the definition Cov( Y ) = E[Y Y T ] − E[Y ]E[Y ]T and the fact that E[uu T ] = p−1Ip, we get 

C△ = 14



E

(ν + p)2R4

(ν + R2)2



E

h

vec(Θ −1/20 uu T Θ−1/20 ) vec(Θ −1/20 uu T Θ−1/20 )T

i

− vec(Θ −10 ) vec(Θ −10 )T



= 14



Θ−1/20 ⊗ Θ−1/20

 (ν + p)E[ξR]E uu T ⊗ uu T  − vec( I) vec( I)T  

Θ−1/20 ⊗ Θ−1/20



.

31 Using (19) again, we can relate this to C. The difference C△ − C simplifies to 



Θ−1/20 ⊗ Θ−1/20

  ν + p + 2 

p(p + 2) E[ξR] − 1

  12Ip2 + 14 vec( I) vec( I)T

 

Θ−1/20 ⊗ Θ−1/20



,

which matches the second term in (9). 

Step 3: (Positive definiteness of C). To verify that C is positive definite, we write 

C = (Σ 1/2 ⊗ Σ1/2)Q(Σ 1/2 ⊗ Σ1/2), with 

Q := 12Ip2 − E[ξR]

p(p + 2) 



Ip2 + 12vv T



,

where v = vec( Ip). It suffices to show Q ≻ 0. Note that 0 < E[ξR] = E

 R2

ν + R2 · (ν + p)R2

ν + R2



< E

(ν + p)R2

ν + R2



= p. (21) Any vector w ∈ Rp2

orthogonal to v is an eigenvector of Q with eigenvalue 

λ1 = 12 − E[ξR]

p(p + 2) > 12 − pp(p + 2) = 12 − 1

p + 2 > 0.

The vector v itself is an eigenvector with eigenvalue 

λ2 = 12 − E[ξR]

p(p + 2) 



1 + ∥v∥2

2



= 12 − E[ξR]

p(p + 2) 



1 + p

2



= 12 − E[ξR]2p .

Using the bound E[ξR] < p , we see that λ2 > 12 − 12 = 0. Thus, all eigenvalues of Q are positive, implying C is positive definite. 

Proof of Theorem 3.8. To establish asymptotic convergence, we verify the conditions in The-orem 3.1. The risk function is given by 

G(Θ) = E[ℓ(X, Θ)] = −12 log det(Θ) + ν + p

2 E[log  ν + X⊤ΘX].

Note that G(Θ) is minimized at Θ 0. The first-order condition ∇ΘG(Θ 0) = 0 holds due to the assumption (7) on R and the verification in (20). We fix an open neighborhood B around Θ 0 in Sym +(p), such that there exist positive constants r+, r − > 0 ensuring that all matrices in B have eigenvalues uniformly bounded: 

r− ≤ inf  

> Θ∈B

λmin (Θ) ≤ sup 

> Θ∈B

λmax (Θ) ≤ r+.

This implies that the closure of B is a compact subset of Sym +(p). We now verify conditions (i)–(v) of Theorem 3.1. 

Condition (i): Let ⪯ denote the Loewner partial order on symmetric matrices, where 

A1 ⪯ A2 if A2 − A1 is positive semi-definite. Recall the Hessian derived in Lemma 3.6: 

∇2Θℓ(X, Θ) = 12(Θ −1 ⊗ Θ−1) − ν + p

21(ν + XT ΘX)2 vec( XX T ) vec( XX T )T .

32 The second term is the subtraction of a positive semi-definite matrix. Therefore, 

∇2Θℓ(X, Θ) ⪯ 12(Θ −1 ⊗ Θ−1).

Using the operator norm (largest absolute eigenvalue), we have 

∥∇ 2Θℓ(X, Θ) ∥ ≤ 12∥Θ−1 ⊗ Θ−1∥ = 12∥Θ−1∥2 = 12λmin (Θ) 2 .

Given the bounds on B, this is bounded by M = 1 /(2 r2

> −

), satisfying condition (i). 

Condition (ii): The function G(Θ) is smooth on B, satisfying the C3 (thrice continuous differentiability) requirement. The positive definiteness of the Hessian C was established in Lemma 3.6. 

Condition (iii): The gradient condition E[∇Θℓ(X, Θ0)] = 0 holds by assumption (7) and the derivation in (20). Furthermore, C△ < ∞ is a consequence of the bound 0 < E[ξR] < p 

derived in (21). 

Condition (iv): The uniform tightness of the sequence bΘn is established in Lemma 7.4. 

Condition (v): We establish a uniform envelope over the compact closure of B. For any Θ ∈ B, we have r−∥X∥22 ≤ XT ΘX ≤ r+∥X∥22. The term log( ν + XT ΘX) is bounded from below by log( ν) and from above by log( ν + r+∥X∥22). Consequently, 

|ℓ(X, Θ) | ≤ 12| log det(Θ) | + ν + p

2 | log( ν + XT ΘX)|≤ p

2 max( | log r−|, | log r+|) + ν + p

2 max  | log( ν)|, log( ν + r+∥X∥22) .

This provides an integrable envelope. Specifically, for an elliptical vector X = Θ −1/20 uR , the expectation E[log( ν + r+∥X∥22)] behaves asymptotically like E[log( R2)], which is finite by the assumption E[log(1 + R2)] < ∞.Thus, all regularity conditions are satisfied, and the conclusion of Theorem 3.2 follows. 

Lemma 7.4. The sequence ( bΘn)n∈N is uniformly tight; i.e., for every ε > 0 there exists a compact set K ⊂ Sym +(p) such that 

P( bΘn ∈ K) ≥ 1 − ε, 

for every n ∈ N.Proof. We consider a compact set K of the form 

K = {Θ ∈ Sym +(p) : r− ≤ λmin (Θ) ≤ λmax (Θ) ≤ r+},

for some strictly positive constants r+, r − > 0. To ensure that bΘn remains within such a set 

K with high probability, it suffices to show the coercivity of the objective function. This means verifying that the objective loss tends to infinity as λmax (Θ) → ∞ or λmin (Θ) → 0. 33 Consider the spectral decomposition Θ = Ppj=1 λj vj vTj , where 0 < λ p ≤ · · · ≤ λ1 are the eigenvalues and {v1, . . . , v p} is an orthonormal basis. Let λmax (Θ) = λ1 = ∥Θ∥. For any fixed δ > 0, we have log( ν + XT ΘX) ≥ log( ν + λ1(vT 

> 1

X)2)

≥ log( ν + λ1(vT 

> 1

X)2)I{| vT 

> 1

X| ≥ δ}≥ log( λ1(vT 

> 1

X)2)I{| vT 

> 1

X| ≥ δ}

=  log( λ1) + log(( vT 

> 1

X)2) I{| vT 

> 1

X| ≥ δ}.

Using the inequality log det(Θ) ≤ p log( λmax (Θ)), the empirical loss Ln(Θ) = 1

> n

Pni=1 ℓ(Xi, Θ) can be bounded from below: 

Ln(Θ) = −12 log det(Θ) + ν + p

21

n

> n

X

> i=1

log( ν + XTi ΘXi)

≥ − p

2 log( λ1) + ν + p

21

n

> n

X

> i=1

 log( λ1) + log(( vT 

> 1

Xi)2) I{| vT 

> 1

Xi| ≥ δ}≥

ν + p

2 Sn − p

2



log( λmax (Θ)) + ν + p

2 log( δ2)Sn,

where 

Sn := 1

n

> n

X

> i=1

I{| vT 

> 1

Xi| ≥ δ}.

Since the logarithmic term involving δ is bounded for fixed δ, the dominant term is the coefficient of log( λmax (Θ)). We analyze Sn using uniform convergence. The class of in-dicator functions {I{| vT x| ≥ δ} : v ∈ Sp−1} has finite VC-dimension (as intersections of half-spaces), implying it is a Glivenko-Cantelli class (see Example 19.17 in [24]). Define 

fn(v) := 1

> n

Pni=1 I{| vT Xi| ≥ δ} and f (v) := P(|vT X| ≥ δ). For any fixed δ ≥ 0, the uniform law of large numbers implies sup v∈Sp−1 |fn(v) − f (v)| p

−→ 0. Since the infimum is a continuous functional with respect to the uniform norm, we obtain 

Sn ≥ inf  

> v∈Sp−1

fn(v) p

−→ inf  

> v∈Sp−1

f (v) = inf  

> v∈Sp−1

P(|vT X| ≥ δ).

It remains to bound the limit from below. Since X has a non-singular covariance, 

P(|vT X| ≥ δ) → 1 as δ → 0 for every fixed v. Moreover, the mapping δ 7 → P(|vT X| ≥ δ)is monotone. By Dini’s Theorem, this convergence is uniform on the compact sphere Sp−1.Thus, inf v∈Sp−1 P(|vT X| ≥ δ) → 1 as δ → 0. Consequently, we can choose δ > 0 sufficiently small such that inf  

> v∈Sp−1

P(|vT X| ≥ δ) > p + 1 

ν + p.

Then, with probability approaching 1, Sn ≥ p+1  

> ν+p

, and consequently: 

Ln(Θ) ≥ 12 log( λmax (Θ)) − Op(1) .

34 The loss tends to infinity as ∥Θ∥ → ∞ . As a result, the empirical loss is bounded from below uniformly for large ∥Θ∥ and n. Precisely, there exists N ∈ N such that: inf 

> ∥Θ∥≥ r+

inf  

> n≥N

Ln(Θ) → ∞ ,

as r+ → ∞ . The same uniform lower bound holds for the penalized loss, as the penalty is non-negative. For the finitely many terms n < N , tightness follows from the fact that any finite collection of random variables is tight. Consequently, for any ε > 0, we can choose 

r+ > 0 large enough such that 

P



∥ bΘn∥ ≤ r+



≥ 1 − ε ∀n ∈ N.

To establish uniform tightness of bΘn, it remains to bound the smallest eigenvalue λmin ( bΘn)away from zero. We condition on the event {∥ bΘn∥ ≤ r+}. Using the inequality log det(Θ) ≤

log( λmin (Θ)) + ( p − 1) log( λmax (Θ)) and observing that the term log( ν + XT ΘX) is bounded from below by log( ν), we have for any ∥Θ∥ ≤ r+:

Ln(Θ) ≥ − 12 log( λmin (Θ)) − p − 12 log( r+) + ν + p

2 log( ν).

As λmin (Θ) → 0, the loss tends to infinity. Thus, minimizing the loss requires the smallest eigenvalue to be bounded away from zero. We conclude that for every ε > 0, there exist constants r+, r − > 0 such that 

P



r− ≤ λmin ( bΘn) ≤ λmax ( bΘn) ≤ r+



≥ 1 − ε ∀n ∈ N,

which proves that the sequence bΘn is uniformly tight. 35 Figure 9: Heatmap of cluster assignments for the off-diagonal entries of the TSLOPE es-timated precision matrices corresponding to the value of α that maximizes the Calinski-Harabasz index for the case of three clusters. 36 Figure 10: Boxplots of the off-diagonal entries (upper-triangular part) of the yearly precision matrices estimated with TSLOPE ( α = 0 .279), reported for each year from 2000 to 2025 and grouped according to the cluster assignments obtained from the k-means procedure (with the number of clusters equal to three); outliers are omitted for visual clarity. 37 Figure 11: Network representation of the 25 portfolios based on the TSLOPE estimation at 

α = 0 .279 (number of clusters equal to three); nodes are colored according to their BE/ME class, and link colors correspond to the cluster assignments identified in Figure 9. 38