---
title: Bias-Corrected Adaptive Conformal Inference for Multi-Horizon Time Series Forecasting
source: arxiv
title_zh: 用于Multi-Horizon Time Series Forecasting的Bias-Corrected Adaptive Conformal Inference
abstract_zh: "Adaptive Conformal Inference (ACI) provides distribution-free prediction intervals 结合 asymptotic cov。\n\n核心做法：作者提出 Bias-Corrected ACI (BC-ACI)，which augments standard ACI 结合 online exponentially weighted moving。\n\n主要结果：In controlled experiments across 688 runs spanning two base 模型s，four synthetic regimes，与 three real。\n\n值得关注的点在于：We provide finite-sample analysis showing that coverage guarantees degrade gracefully 结合 bias estima。"
abstract_en: "Adaptive Conformal Inference (ACI) provides distribution-free prediction intervals with asymptotic coverage guarantees for time series under distribution shift. However, ACI only adapts the quantile threshold -- it cannot shift the interval center. When a base forecaster develops persistent bias after a regime change, ACI compensates by widening intervals symmetrically, producing unnecessarily conservative bands. We propose Bias-Corrected ACI (BC-ACI), which augments standard ACI with an online exponentially weighted moving average (EWM) estimate of forecast bias. BC-ACI corrects nonconformity scores before quantile computation and re-centers prediction intervals, addressing the root cause of miscalibration rather than its symptom. An adaptive dead-zone threshold suppresses corrections when estimated bias is indistinguishable from noise, ensuring no degradation on well-calibrated data. In controlled experiments across 688 runs spanning two base models, four synthetic regimes, and three real datasets, BC-ACI reduces Winkler interval scores by 13--17% under mean and compound distribution shifts (Wilcoxon p < 0.001) while maintaining equivalent performance on stationary data (ratio 1.002x). We provide finite-sample analysis showing that coverage guarantees degrade gracefully with bias estimation error."
authors: "Ankit Lade, Sai Krishna J., Indar Kumar"
date: 2026-04-14
journal: cs.LG
source_link: "https://arxiv.org/pdf/2604.13253v1"
pdf: "https://arxiv.org/pdf/2604.13253v1"
score: 6.949999999999999
tldr: 围绕《用于Multi-Horizon Time Ser...》；作者提出 Bias-Corrected ACI (BC-ACI)...；In controlled experiments across 688...
tldr_cn: 围绕《用于Multi-Horizon Time Ser...》；作者提出 Bias-Corrected ACI (BC-ACI)...；In controlled experiments across 688...
tldr_en: "Adaptive Conformal Inference (ACI) provides distribution-free prediction intervals with asymptotic coverage guarantees for time series under distribution shift. However, ACI only adapts the quantile threshold -- it cannot shift the interval center."
selection_source: fresh_fetch
vocabulary: ["inference | 推理 | CET6"]
motivation: Adaptive Conformal Inference (ACI) provides distribution-free prediction intervals 结合 asymptotic cov。
method: 核心做法：作者提出 Bias-Corrected ACI (BC-ACI)，which augments standard ACI 结合 online exponentially weighted moving。
result: 主要结果：In controlled experiments across 688 runs spanning two base 模型s，four synthetic regimes，与 three real。
conclusion: 值得关注的点在于：We provide finite-sample analysis showing that coverage guarantees degrade gracefully 结合 bias estima。
---

## 摘要
Adaptive Conformal Inference (ACI) provides distribution-free prediction intervals 结合 asymptotic cov。

核心做法：作者提出 Bias-Corrected ACI (BC-ACI)，which augments standard ACI 结合 online exponentially weighted moving。

主要结果：In controlled experiments across 688 runs spanning two base 模型s，four synthetic regimes，与 three real。

值得关注的点在于：We provide finite-sample analysis showing that coverage guarantees degrade gracefully 结合 bias estima。

## Abstract
Adaptive Conformal Inference (ACI) provides distribution-free prediction intervals with asymptotic coverage guarantees for time series under distribution shift. However, ACI only adapts the quantile threshold -- it cannot shift the interval center. When a base forecaster develops persistent bias after a regime change, ACI compensates by widening intervals symmetrically, producing unnecessarily conservative bands. We propose Bias-Corrected ACI (BC-ACI), which augments standard ACI with an online exponentially weighted moving average (EWM) estimate of forecast bias. BC-ACI corrects nonconformity scores before quantile computation and re-centers prediction intervals, addressing the root cause of miscalibration rather than its symptom. An adaptive dead-zone threshold suppresses corrections when estimated bias is indistinguishable from noise, ensuring no degradation on well-calibrated data. In controlled experiments across 688 runs spanning two base models, four synthetic regimes, and three real datasets, BC-ACI reduces Winkler interval scores by 13--17% under mean and compound distribution shifts (Wilcoxon p < 0.001) while maintaining equivalent performance on stationary data (ratio 1.002x). We provide finite-sample analysis showing that coverage guarantees degrade gracefully with bias estimation error.

## 英语词汇
- **inference**：推理（CET6）
