---
title: "CIDER: Boosting Memory-Disaggregated Key-Value Stores with Pessimistic Synchronization"
source: arxiv
title_zh: 结合Pessimistic Synchronization的CIDER：Boosting Memory-Disaggregated Key-Value Stores
abstract_zh: "huge amount 的 redundant I/Os are generated when synchronizing concurrent data accesses，making limite。\n\n核心做法：In this paper，作者提出 to boost memory-disaggregated KV stores 结合 pessimistic synchronization。\n\n主要结果：Experimental results show that CIDER effectively improves throughput 的当前最优 memory-disaggregated KV s。\n\n值得关注的点在于：Memory-disaggregated key-value (KV) stores suffer from severe 性能 bottleneck due to their I/O redunda。"
abstract_en: "Memory-disaggregated key-value (KV) stores suffer from a severe performance bottleneck due to their I/O redundancy issues. A huge amount of redundant I/Os are generated when synchronizing concurrent data accesses, making the limited network between the compute and memory pools of DM a performance bottleneck. We identify the root cause for the redundant I/O lies in the mismatch between the optimistic synchronization of existing memory-disaggregated KV stores and the highly concurrent workloads on DM. In this paper, we propose to boost memory-disaggregated KV stores with pessimistic synchronization. We propose CIDER, a compute-side I/O optimization framework, to verify our idea. CIDER adopts a global write-combining technique to further reduce cross-node redundant I/Os. A contention-aware synchronization scheme is designed to improve the performance of pessimistic synchronization under low contention scenarios. Experimental results show that CIDER effectively improves the throughput of state-of-the-art memory-disaggregated KV stores by up to $6.6\\times$ under the YCSB benchmark."
authors: "Yuxuan Du, Xuchuan Luo, Xin Wang, Yangfan Zhou, Jiacheng Shen"
date: 2026-04-03
source_link: "https://arxiv.org/pdf/2604.03007v1"
pdf: "https://arxiv.org/pdf/2604.03007v1"
score: 7.05
tldr: 围绕《结合Pessimistic Synchroniz...》；In this paper，作者提出 to boost memo...；Experimental results show that CIDER...
tldr_cn: 围绕《结合Pessimistic Synchroniz...》；In this paper，作者提出 to boost memo...；Experimental results show that CIDER...
tldr_en: "Memory-disaggregated key-value (KV) stores suffer from a severe performance bottleneck due to their I/O redundancy issues. A huge amount of redundant I/Os are generated when synchronizing concurrent data accesses, making the limited network between the compute and memory pools of DM a performance bottleneck."
selection_source: fresh_fetch
vocabulary: ["optimization | 优化 | CET6", "benchmark | 基准测试 | CET6/IELTS"]
motivation: huge amount 的 redundant I/Os are generated when synchronizing concurrent data accesses，making limite。
method: 核心做法：In this paper，作者提出 to boost memory-disaggregated KV stores 结合 pessimistic synchronization。
result: 主要结果：Experimental results show that CIDER effectively improves throughput 的当前最优 memory-disaggregated KV s。
conclusion: 值得关注的点在于：Memory-disaggregated key-value (KV) stores suffer from severe 性能 bottleneck due to their I/O redunda。
---

## 摘要
huge amount 的 redundant I/Os are generated when synchronizing concurrent data accesses，making limite。

核心做法：In this paper，作者提出 to boost memory-disaggregated KV stores 结合 pessimistic synchronization。

主要结果：Experimental results show that CIDER effectively improves throughput 的当前最优 memory-disaggregated KV s。

值得关注的点在于：Memory-disaggregated key-value (KV) stores suffer from severe 性能 bottleneck due to their I/O redunda。

## Abstract
Memory-disaggregated key-value (KV) stores suffer from a severe performance bottleneck due to their I/O redundancy issues. A huge amount of redundant I/Os are generated when synchronizing concurrent data accesses, making the limited network between the compute and memory pools of DM a performance bottleneck. We identify the root cause for the redundant I/O lies in the mismatch between the optimistic synchronization of existing memory-disaggregated KV stores and the highly concurrent workloads on DM. In this paper, we propose to boost memory-disaggregated KV stores with pessimistic synchronization. We propose CIDER, a compute-side I/O optimization framework, to verify our idea. CIDER adopts a global write-combining technique to further reduce cross-node redundant I/Os. A contention-aware synchronization scheme is designed to improve the performance of pessimistic synchronization under low contention scenarios. Experimental results show that CIDER effectively improves the throughput of state-of-the-art memory-disaggregated KV stores by up to $6.6\times$ under the YCSB benchmark.

## 英语词汇
- **optimization**：优化（CET6）
- **benchmark**：基准测试（CET6/IELTS）
