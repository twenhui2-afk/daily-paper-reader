---
title: Sign Embedding Quantum Algorithms for Matrix Equations and Matrix Functions
source: arxiv
title_zh: 用于Matrix Equations与Matrix Functions的Sign Embedding Quantum Algorithms
abstract_zh: "作者构建 systematic sign-embedding 框架的 operator-output quantum algorithms 用于 matrix equations 与 matrix f。\n\n核心做法：Differing from contour-integral treatment，we start 结合 matrix-sign embedding route：augmented matrix M。\n\n主要结果：用于 ordinary Sylvester equations，we offer explicit block-encoding 的 target matrix solution 结合 query c。\n\n值得关注的点在于：These results identify matrix-sign embeddings 与 nodewise rebalancing as reusable design principles 用。"
abstract_en: "We develop a systematic sign-embedding framework of operator-output quantum algorithms for matrix equations and matrix functions. Differing from the contour-integral treatment, we start with the matrix-sign embedding route: an augmented matrix $M$ whose half-plane matrix sign compresses the target operator either as a block of $\\text{sign}(M)$ or, in projector form, through $(I-\\text{sign}(M))/2$; we then construct a logarithmic-sinc approximation for the half-plane sign operator and combine it with structure-aware scaled multiplexing and nodewise rebalancing of shifted inverse families. For ordinary Sylvester equations, we offer an explicit block-encoding of the target matrix solution with query complexity linear in the inverse-conditioning parameters and logarithmic in the target error tolerance, under non-normal and non-diagonalizable settings given a field-of-values (FoV) gap or strip-resolvent hypotheses. These algorithms propagate the same overlap-based normalization bookkeeping to ordinary and generalized Sylvester equations, generalized Lyapunov equations, principal square roots and inverse square roots, matrix geometric means, and continuous-time algebraic Riccati equations (CARE). These results identify matrix-sign embeddings and nodewise rebalancing as reusable design principles for structured operator-output quantum linear algebra."
authors: "Yanqiao Wang, Jin-Peng Liu"
date: 2026-04-28
journal: quant-ph
source_link: "https://arxiv.org/pdf/2604.25333v1"
pdf: "https://arxiv.org/pdf/2604.25333v1"
score: 7.949999999999999
tldr: 围绕《用于Matrix Equations与Matri...》；Differing from contour-integral...；用于 ordinary Sylvester equations，we o...
tldr_cn: 围绕《用于Matrix Equations与Matri...》；Differing from contour-integral...；用于 ordinary Sylvester equations，we o...
tldr_en: "We develop a systematic sign-embedding framework of operator-output quantum algorithms for matrix equations and matrix functions. Differing from the contour-integral treatment, we start with the matrix-sign embedding route: an augmented matrix $M$ whose half-plane matrix sign compresses the target operator either as a block of $\\text{sign}(M)$ or, in projector form, through $(I-\\text{sign}(M))/2$; we then construct..."
selection_source: fresh_fetch
vocabulary: ["embedding | 嵌入表示 | 学术高频"]
motivation: 作者构建 systematic sign-embedding 框架的 operator-output quantum algorithms 用于 matrix equations 与 matrix f。
method: 核心做法：Differing from contour-integral treatment，we start 结合 matrix-sign embedding route：augmented matrix M。
result: 主要结果：用于 ordinary Sylvester equations，we offer explicit block-encoding 的 target matrix solution 结合 query c。
conclusion: 值得关注的点在于：These results identify matrix-sign embeddings 与 nodewise rebalancing as reusable design principles 用。
---

## 摘要
作者构建 systematic sign-embedding 框架的 operator-output quantum algorithms 用于 matrix equations 与 matrix f。

核心做法：Differing from contour-integral treatment，we start 结合 matrix-sign embedding route：augmented matrix M。

主要结果：用于 ordinary Sylvester equations，we offer explicit block-encoding 的 target matrix solution 结合 query c。

值得关注的点在于：These results identify matrix-sign embeddings 与 nodewise rebalancing as reusable design principles 用。

## Abstract
We develop a systematic sign-embedding framework of operator-output quantum algorithms for matrix equations and matrix functions. Differing from the contour-integral treatment, we start with the matrix-sign embedding route: an augmented matrix $M$ whose half-plane matrix sign compresses the target operator either as a block of $\text{sign}(M)$ or, in projector form, through $(I-\text{sign}(M))/2$; we then construct a logarithmic-sinc approximation for the half-plane sign operator and combine it with structure-aware scaled multiplexing and nodewise rebalancing of shifted inverse families. For ordinary Sylvester equations, we offer an explicit block-encoding of the target matrix solution with query complexity linear in the inverse-conditioning parameters and logarithmic in the target error tolerance, under non-normal and non-diagonalizable settings given a field-of-values (FoV) gap or strip-resolvent hypotheses. These algorithms propagate the same overlap-based normalization bookkeeping to ordinary and generalized Sylvester equations, generalized Lyapunov equations, principal square roots and inverse square roots, matrix geometric means, and continuous-time algebraic Riccati equations (CARE). These results identify matrix-sign embeddings and nodewise rebalancing as reusable design principles for structured operator-output quantum linear algebra.

## 英语词汇
- **embedding**：嵌入表示（学术高频）
