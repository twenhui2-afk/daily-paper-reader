---
title: "Any 3D Scene is Worth 1K Tokens: 3D-Grounded Representation for Scene Generation at Scale"
source: arxiv
title_zh: 用于Scene Generation at Scale的Any 3D Scene is Worth 1K Tokens：3D-Grounded Representation
abstract_zh: "Typically，these 2D-based approaches degrade 3D spatial extrapolation to 2D temporal extension，which。\n\n核心做法：In this paper，作者提出，用于 first time，to perform 3D scene generation directly within implicit 3D latent s。\n\n主要结果：Then 作者引入 3D Diffusion Transformer (3DDiT)，which performs diffusion 模型ing in this 3D latent space，ac。\n\n值得关注的点在于：Moreover，since our approach directly generates 3D scene representation，it can be decoded to images 与。"
abstract_en: "3D scene generation has long been dominated by 2D multi-view or video diffusion models. This is due not only to the lack of scene-level 3D latent representation, but also to the fact that most scene-level 3D visual data exists in the form of multi-view images or videos, which are naturally compatible with 2D diffusion architectures. Typically, these 2D-based approaches degrade 3D spatial extrapolation to 2D temporal extension, which introduces two fundamental issues: (i) representing 3D scenes via 2D views leads to significant representation redundancy, and (ii) latent space rooted in 2D inherently limits the spatial consistency of the generated 3D scenes. In this paper, we propose, for the first time, to perform 3D scene generation directly within an implicit 3D latent space to address these limitations. First, we repurpose frozen 2D representation encoders to construct our 3D Representation Autoencoder (3DRAE), which grounds view-coupled 2D semantic representations into a view-decoupled 3D latent representation. This enables representing 3D scenes observed from arbitrary numbers of views--at any resolution and aspect ratio--with fixed complexity and rich semantics. Then we introduce 3D Diffusion Transformer (3DDiT), which performs diffusion modeling in this 3D latent space, achieving remarkably efficient and spatially consistent 3D scene generation while supporting diverse conditioning configurations. Moreover, since our approach directly generates a 3D scene representation, it can be decoded to images and optional point maps along arbitrary camera trajectories without requiring per-trajectory diffusion sampling pass, which is common in 2D-based approaches."
authors: "Dongxu Wei, Qi Xu, Zhiqi Li, Hangning Zhou, Cong Qiu, Hailong Qin, Mu Yang, Zhaopeng Cui, Peidong Liu"
date: 2026-04-13
journal: cs.CV
source_link: "https://arxiv.org/pdf/2604.11331v1"
pdf: "https://arxiv.org/pdf/2604.11331v1"
score: 6.299999999999999
tldr: 围绕《用于Scene Generation at Sc...》；In this paper，作者提出，用于 first time...；Then 作者引入 3D Diffusion Transformer (...
tldr_cn: 围绕《用于Scene Generation at Sc...》；In this paper，作者提出，用于 first time...；Then 作者引入 3D Diffusion Transformer (...
tldr_en: "3D scene generation has long been dominated by 2D multi-view or video diffusion models. This is due not only to the lack of scene-level 3D latent representation, but also to the fact that most scene-level 3D visual data exists in the form of multi-view images or videos, which are naturally compatible with 2D diffusion architectures."
selection_source: fresh_fetch
vocabulary: ["transformer | Transformer 架构 | 学术高频", "diffusion | 扩散 | 学术高频"]
motivation: Typically，these 2D-based approaches degrade 3D spatial extrapolation to 2D temporal extension，which。
method: 核心做法：In this paper，作者提出，用于 first time，to perform 3D scene generation directly within implicit 3D latent s。
result: 主要结果：Then 作者引入 3D Diffusion Transformer (3DDiT)，which performs diffusion 模型ing in this 3D latent space，ac。
conclusion: 值得关注的点在于：Moreover，since our approach directly generates 3D scene representation，it can be decoded to images 与。
---

## 摘要
Typically，these 2D-based approaches degrade 3D spatial extrapolation to 2D temporal extension，which。

核心做法：In this paper，作者提出，用于 first time，to perform 3D scene generation directly within implicit 3D latent s。

主要结果：Then 作者引入 3D Diffusion Transformer (3DDiT)，which performs diffusion 模型ing in this 3D latent space，ac。

值得关注的点在于：Moreover，since our approach directly generates 3D scene representation，it can be decoded to images 与。

## Abstract
3D scene generation has long been dominated by 2D multi-view or video diffusion models. This is due not only to the lack of scene-level 3D latent representation, but also to the fact that most scene-level 3D visual data exists in the form of multi-view images or videos, which are naturally compatible with 2D diffusion architectures. Typically, these 2D-based approaches degrade 3D spatial extrapolation to 2D temporal extension, which introduces two fundamental issues: (i) representing 3D scenes via 2D views leads to significant representation redundancy, and (ii) latent space rooted in 2D inherently limits the spatial consistency of the generated 3D scenes. In this paper, we propose, for the first time, to perform 3D scene generation directly within an implicit 3D latent space to address these limitations. First, we repurpose frozen 2D representation encoders to construct our 3D Representation Autoencoder (3DRAE), which grounds view-coupled 2D semantic representations into a view-decoupled 3D latent representation. This enables representing 3D scenes observed from arbitrary numbers of views--at any resolution and aspect ratio--with fixed complexity and rich semantics. Then we introduce 3D Diffusion Transformer (3DDiT), which performs diffusion modeling in this 3D latent space, achieving remarkably efficient and spatially consistent 3D scene generation while supporting diverse conditioning configurations. Moreover, since our approach directly generates a 3D scene representation, it can be decoded to images and optional point maps along arbitrary camera trajectories without requiring per-trajectory diffusion sampling pass, which is common in 2D-based approaches.

## 英语词汇
- **transformer**：Transformer 架构（学术高频）
- **diffusion**：扩散（学术高频）
