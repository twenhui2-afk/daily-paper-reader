---
title: Accessibility Percolation with Rough Mount Fuji labels
source: arxiv
abstract_zh: "论文关注目标任务任务，目标是提升效果与可用性。\n\n作者提出一个新方法，并结合图结构建模。\n\n摘要没有给出明确数字，但报告了正向实验结果。\n\n如果你关注目标任务方向，这篇文章的价值在于把任务做得更自动化、更可落地。"
abstract_en: "Consider an infinite, rooted, connected graph where each vertex is labelled with an independent and identically distributed Uniform(0,1) random variable, plus a parameter $θ$ times its distance from the root $ρ$. That is, we label vertex $v$ with $X_v = U_v + θd(ρ,v)$. We say that accessibility percolation occurs if there is an infinite path started from $ρ$ along which the vertex labels are increasing.   When the graph is a Bienaymé-Galton-Watson tree, we give an exact characterisation of the critical value $θ_c$ such that there is accessibility percolation with positive probability if and only if $θ>θ_c$. We also give more explicit bounds on the value of $θ_c$. The lower bound holds for a much more general class of trees.   When the graph is the lattice $\\mathbb{Z}^n$ for $n\\ge 2$, we show that there is a non-trivial phase transition and give some first bounds on $θ_c$. To do this we introduce a novel coupling with oriented percolation."
authors: "Diana De Armas Bellon, Matthew I. Roberts"
date: 2026-03-31
journal: math.PR
source_link: "https://arxiv.org/pdf/2603.29561v1"
pdf: "https://arxiv.org/pdf/2603.29561v1"
score: 7.6
tldr: 论文关注目标任务任务，目标是提升效果与可用性；作者提出一个新方法，并结合图结构建模；摘要没有给出明确数字，但报告了正向实验结果。
tldr_cn: 论文关注目标任务任务，目标是提升效果与可用性；作者提出一个新方法，并结合图结构建模；摘要没有给出明确数字，但报告了正向实验结果。
tldr_en: "Consider an infinite, rooted, connected graph where each vertex is labelled with an independent and identically distributed Uniform(0,1) random variable, plus a parameter $θ$ times its distance from the root $ρ$. That is, we label vertex $v$ with $X_v = U_v + θd(ρ,v)$."
selection_source: fresh_fetch
motivation: 论文关注目标任务任务，目标是提升效果与可用性。
method: 作者提出一个新方法，并结合图结构建模。
result: 摘要没有给出明确数字，但报告了正向实验结果。
conclusion: 如果你关注目标任务方向，这篇文章的价值在于把任务做得更自动化、更可落地。
---

## 摘要
论文关注目标任务任务，目标是提升效果与可用性。

作者提出一个新方法，并结合图结构建模。

摘要没有给出明确数字，但报告了正向实验结果。

如果你关注目标任务方向，这篇文章的价值在于把任务做得更自动化、更可落地。

## Abstract
Consider an infinite, rooted, connected graph where each vertex is labelled with an independent and identically distributed Uniform(0,1) random variable, plus a parameter $θ$ times its distance from the root $ρ$. That is, we label vertex $v$ with $X_v = U_v + θd(ρ,v)$. We say that accessibility percolation occurs if there is an infinite path started from $ρ$ along which the vertex labels are increasing.   When the graph is a Bienaymé-Galton-Watson tree, we give an exact characterisation of the critical value $θ_c$ such that there is accessibility percolation with positive probability if and only if $θ>θ_c$. We also give more explicit bounds on the value of $θ_c$. The lower bound holds for a much more general class of trees.   When the graph is the lattice $\mathbb{Z}^n$ for $n\ge 2$, we show that there is a non-trivial phase transition and give some first bounds on $θ_c$. To do this we introduce a novel coupling with oriented percolation.
