---
title: "Search-Bound Proximity Proofs: Binding Encrypted Geographic Search to Zero-Knowledge Verification"
source: arxiv
title_zh: Search-Bound Proximity Proofs：Binding Encrypted Geographic Search to Zero-Knowledge Verification
abstract_zh: "Location-based systems that combine encrypted geographic search 结合 zero-knowledge proximity proofs t。\n\n核心做法：Under honest-but-curious server，this leaves authorization provenance gap：once session state is purge。\n\n主要结果：We formalize this gap as search-authorized proof (SAP) security notion 与 show 通过 concrete audit re-a。\n\n值得关注的点在于：Experiments on synthetic 与 real-world data (110,776 OpenStreetMap POIs) show sub-millisecond absolut。"
abstract_en: "Location-based systems that combine encrypted geographic search with zero-knowledge proximity proofs typically treat the two phases as independent. Under an honest-but-curious server, this leaves an authorization provenance gap: once session state is purged, no forensic procedure can attribute a proof to its originating search session, because the proof's public inputs encode no session-identifying information.   We formalize this gap as the search-authorized proof (SAP) security notion and show via a concrete audit re-association attack that proof-external mechanisms, where authorization evidence remains outside the proof, cannot prevent forensic misattribution when the same drop parameters recur across sessions. Search-Bound Proximity Proofs (SBPP) realize the SAP requirements without modifying the ZKP circuit: session nonce, Merkle-root result-set commitment, and signed receipt are decomposed into independently auditable components, enabling property-level fault isolation in offline audit. Experiments on synthetic and real-world data (110,776 OpenStreetMap POIs) show sub-millisecond absolute overhead on a 125 ms Groth16 baseline."
authors: Yoshiyuki Ootani
date: 2026-04-05
source_link: "https://arxiv.org/pdf/2604.03902v1"
pdf: "https://arxiv.org/pdf/2604.03902v1"
score: 7.5
tldr: 围绕《Search-Bound Proximity P...》；Under honest-but-curious server...；We formalize this gap as search-auth...
tldr_cn: 围绕《Search-Bound Proximity P...》；Under honest-but-curious server...；We formalize this gap as search-auth...
tldr_en: "Location-based systems that combine encrypted geographic search with zero-knowledge proximity proofs typically treat the two phases as independent. Under an honest-but-curious server, this leaves an authorization provenance gap: once session state is purged, no forensic procedure can attribute a proof to its originating search session, because the proof's public inputs encode no session-identifying information."
selection_source: fresh_fetch
motivation: Location-based systems that combine encrypted geographic search 结合 zero-knowledge proximity proofs t。
method: 核心做法：Under honest-but-curious server，this leaves authorization provenance gap：once session state is purge。
result: 主要结果：We formalize this gap as search-authorized proof (SAP) security notion 与 show 通过 concrete audit re-a。
conclusion: "值得关注的点在于：Experiments on synthetic 与 real-world data (110,776 OpenStreetMap POIs) show sub-millisecond absolut。"
---

## 摘要
Location-based systems that combine encrypted geographic search 结合 zero-knowledge proximity proofs t。

核心做法：Under honest-but-curious server，this leaves authorization provenance gap：once session state is purge。

主要结果：We formalize this gap as search-authorized proof (SAP) security notion 与 show 通过 concrete audit re-a。

值得关注的点在于：Experiments on synthetic 与 real-world data (110,776 OpenStreetMap POIs) show sub-millisecond absolut。

## Abstract
Location-based systems that combine encrypted geographic search with zero-knowledge proximity proofs typically treat the two phases as independent. Under an honest-but-curious server, this leaves an authorization provenance gap: once session state is purged, no forensic procedure can attribute a proof to its originating search session, because the proof's public inputs encode no session-identifying information.   We formalize this gap as the search-authorized proof (SAP) security notion and show via a concrete audit re-association attack that proof-external mechanisms, where authorization evidence remains outside the proof, cannot prevent forensic misattribution when the same drop parameters recur across sessions. Search-Bound Proximity Proofs (SBPP) realize the SAP requirements without modifying the ZKP circuit: session nonce, Merkle-root result-set commitment, and signed receipt are decomposed into independently auditable components, enabling property-level fault isolation in offline audit. Experiments on synthetic and real-world data (110,776 OpenStreetMap POIs) show sub-millisecond absolute overhead on a 125 ms Groth16 baseline.
