---
title: An Explicit Surrogate for Gaussian Mixture Flow Matching with Wasserstein Gap Bounds
source: arxiv
title_zh: 用于Gaussian Mixture Flow Matching 结合 Wasserstein Gap Bounds的Explicit Surrogate
abstract_zh: "这篇论文研究 training-free flow matching between two Gaussian mixture 模型s (GMMs) 使用 explicit velocity fiel。\n\n核心做法：To handle nonlocal regimes，作者引入 path-splitting strategy that localizes covariance evolution 与 enable。\n\n主要结果：Our baseline approach constructs component-wise Gaussian paths 结合 affine velocity fields satisfying。\n\n值得关注的点在于：We finally compare surrogate 结合 exact construction based on Gaussian Wasserstein geodesic 与 summariz。"
abstract_en: "We study training-free flow matching between two Gaussian mixture models (GMMs) using explicit velocity fields that transport one mixture into the other over time. Our baseline approach constructs component-wise Gaussian paths with affine velocity fields satisfying the continuity equation, which yields to a closed-form surrogate for the pairwise kinetic transport cost. In contrast to the exact Gaussian Wasserstein cost, which relies on matrix square-root computations, the surrogate admits a simple analytic expression derived from the kinetic energy of the induced flow. We then analyze how closely this surrogate approximates the exact cost. We prove second-order agreement in a local commuting regime and derive an explicit cubic error bound in the local commuting regime. To handle nonlocal regimes, we introduce a path-splitting strategy that localizes the covariance evolution and enables piecewise application of the bound. We finally compare the surrogate with an exact construction based on the Gaussian Wasserstein geodesic and summarize the results in a practical regime map showing when the surrogate is accurate and the exact method is preferable."
authors: "Elham Rostami, Taous-Meriem Laleg-Kirati, Hamidou Tembine"
date: 2026-03-30
source_link: "https://arxiv.org/pdf/2603.28992v1"
pdf: "https://arxiv.org/pdf/2603.28992v1"
score: 7.35
tldr: 围绕《用于Gaussian Mixture Flow...》；To handle nonlocal regimes，作者引入...；Our baseline approach constructs com...
tldr_cn: 围绕《用于Gaussian Mixture Flow...》；To handle nonlocal regimes，作者引入...；Our baseline approach constructs com...
tldr_en: "We study training-free flow matching between two Gaussian mixture models (GMMs) using explicit velocity fields that transport one mixture into the other over time. Our baseline approach constructs component-wise Gaussian paths with affine velocity fields satisfying the continuity equation, which yields to a closed-form surrogate for the pairwise kinetic transport cost."
selection_source: fresh_fetch
motivation: 这篇论文研究 training-free flow matching between two Gaussian mixture 模型s (GMMs) 使用 explicit velocity fiel。
method: 核心做法：To handle nonlocal regimes，作者引入 path-splitting strategy that localizes covariance evolution 与 enable。
result: 主要结果：Our baseline approach constructs component-wise Gaussian paths 结合 affine velocity fields satisfying。
conclusion: 值得关注的点在于：We finally compare surrogate 结合 exact construction based on Gaussian Wasserstein geodesic 与 summariz。
---

## 摘要
这篇论文研究 training-free flow matching between two Gaussian mixture 模型s (GMMs) 使用 explicit velocity fiel。

核心做法：To handle nonlocal regimes，作者引入 path-splitting strategy that localizes covariance evolution 与 enable。

主要结果：Our baseline approach constructs component-wise Gaussian paths 结合 affine velocity fields satisfying。

值得关注的点在于：We finally compare surrogate 结合 exact construction based on Gaussian Wasserstein geodesic 与 summariz。

## Abstract
We study training-free flow matching between two Gaussian mixture models (GMMs) using explicit velocity fields that transport one mixture into the other over time. Our baseline approach constructs component-wise Gaussian paths with affine velocity fields satisfying the continuity equation, which yields to a closed-form surrogate for the pairwise kinetic transport cost. In contrast to the exact Gaussian Wasserstein cost, which relies on matrix square-root computations, the surrogate admits a simple analytic expression derived from the kinetic energy of the induced flow. We then analyze how closely this surrogate approximates the exact cost. We prove second-order agreement in a local commuting regime and derive an explicit cubic error bound in the local commuting regime. To handle nonlocal regimes, we introduce a path-splitting strategy that localizes the covariance evolution and enables piecewise application of the bound. We finally compare the surrogate with an exact construction based on the Gaussian Wasserstein geodesic and summarize the results in a practical regime map showing when the surrogate is accurate and the exact method is preferable.
