Title: A Framework for Parameterized Subexponential-Subcubic-Time Algorithms for Weighted Problems in Planar Graphs

URL Source: https://arxiv.org/pdf/2604.00891v1

Published Time: Thu, 02 Apr 2026 01:04:18 GMT

Number of Pages: 55

Markdown Content:
# A Framework for Parameterized Subexponential-Subcubic-Time Algorithms for Weighted Problems in Planar Graphs 

Matthias Bentert Fedor V. Fomin Petr A. Golovach 

Abstract 

Many problems are known to be solvable in subexponential parameterized time when the input graph is planar. The bidimensionality framework of Demaine, Fomin, Hajiaghay, and Thilikos [JACM ’05] and the treewidth-pattern-covering approach by Fomin, Lokshtanov, Marx, Pilipczuk, Pilipczuk, and Saurabh [SICOMP ’22] give robust tools for designing such algorithms. However, there are still many problems for which we do not know whether subexponential para-meterized algorithms exist. The bidimensionality framework is not able to handle weights or directed graphs and the treewidth-pattern-covering approach only works for finding connected solutions. Building on a result by Nederlof [STOC ’20], we provide a framework that is able to solve a variety of problems in planar graphs in subexponential parameterized time for which this was previously not known (where the polynomial part of the running time is usually O(n2.49 )). Our framework can handle weights, does not require solutions to contain only few connected components, and applies to cases where the number of potential patterns of a solution is expo-nential in the parameter. We then use the framework to show that various weighted problems like Weighted Partial Vertex Cover , Maximum-Weight Induced Forest , Minimum-Weight Rooted Simple Minor , and Maximum-Weight Rooted Parallel Induced Minor allow for subexponen-tial parameterized algorithms. This was previously not known for any of them. Moreover, we present a very easy-to-use fragment of our framework that is still powerful enough to be applicable to problems like Densest k-Subgraph , Maximum-Weight Independent Set ,

Maximum (k, n − k)-Cut , and Weighted Partial Vertex Cover . This fragment allows for significantly simpler proofs in the case of Maximum-Weight Independent Set and Max-imum (k, n −k)-Cut and is able to show a subexponential parameterized algorithm for weighted versions of Densest k-Subgraph . Even the unweighted version was not known before and is stated as an open problem in the existing literature. 

1

> arXiv:2604.00891v1 [cs.DS] 1 Apr 2026

# Contents 

1 Introduction 3

1.1 Our Results . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 41.2 Our Methods . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 5

2 Preliminaries 9

2.1 Problem Definitions . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 10 2.2 Request Variants . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 12 2.3 Results Used in our Algorithm . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 13 

3 The Framework 15 

3.1 Algorithms and Invariants . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 16 3.2 Base Case . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 22 3.3 Reduce Portals . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 24 3.4 Main Algorithm . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 25 

4 Applications 29 

4.1 A Simple-to-use Fragment . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 30 4.2 Maximum-Weight Induced Forest . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 34 4.3 Simple Minors . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 39 4.4 Parallel Induced Minors . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 48 

5 Conclusion 52 

21 Introduction 

Over the past two decades, extensive research in parameterized complexity has focused on subexpo-nential algorithms for planar graphs and, more broadly, on various classes of sparse graphs. While for most parameterized NP -hard problems the best we can hope for under the Exponential Time Hypothesis (ETH) of Impagliazzo and Paturi [IP01] is an exponential dependence on the parameter, there are nevertheless many problems that admit subexponential parameterized algorithms when restricted to planar graphs. This phenomenon was first demonstrated in 2000 [ABF +02], where the authors gave a subexponential parameterized algorithm for deciding whether a given n-vertex planar graph contains a dominating set of size k in 2 O(√k)nO(1) time. In 2005, Demaine, Fomin, Hajiaghayi, and Thilikos [DFHT05] introduced the bidimensionality 

framework, providing elegant tools for designing subexponential parameterized algorithms for prob-lems such as Vertex Cover , Feedback Vertex Set , Dominating Set , or Longest Path .Building on the grid minor theorem of Robertson, Seymour, and Thomas [RST94], this framework shows that for a planar graph G and an integer k, either G is a trivial yes- or no-instance of the problem at hand, or the treewidth of G is in O(√k). In the latter case, applying standard dynamic programming techniques on graphs of bounded treewidth yields a running time of 2 O(√k)nO(1) ,where n is the number of vertices. In many situations, the bidimensionality framework can be ex-tended to subexponential parameterized algorithms on H-minor-free graphs. Moreover, for a large class of problems, the running time provided by bidimensionality is essentially optimal under the ETH: one cannot expect a 2 o(√k)nO(1) -time algorithm. For an overview of bidimensionality and its applications, we refer to the standard text book on parameterized complexity [CFK +15]. However, the bidimensionality framework does not apply to directed graphs or to weighted problems, because the existence of large grid minors alone does not address these settings. A step toward overcoming this limitation is the treewidth-pattern-covering approach [FLM +22]. Central to this method is a procedure that randomly samples a vertex subset S in polynomial time with the following properties. The induced subgraph G[S] has treewidth O(√k log k) and for every 

connected subgraph P of G on at most k vertices, the probability that S contains all vertices of P

is at least 1/2O(√k log 2 k)nO(1) . By applying dynamic programming techniques on G[S], this yields a randomized algorithms running in 2 O(√k log 2 k)nO(1) time for problems such as Directed k-Path ,

Weighted k-Path , or Subgraph Isomorphism (when the sought subgraph is connected) for planar input graphs. A crucial requirement of the treewidth-pattern-covering approach is that the pattern P must be connected. To handle cases where P is disconnected, Nederlof [Ned20] extended the treewidth-pattern-covering approach to find or counts (induced) copies of a k-vertex pattern P in a planar graph in 2 ˜O(√k)poly( σ(P )n) time, where σ(P ) denotes the number of non-isomorphic separa-tions of P of size ˜O(√k). 1 In particular, Nederlof’s results yield an algorithm with running time 2 ˜O(√k)nO(1) when P is a matching, an independent set, or a connected bounded-degree graph. For any pattern P , he obtained algorithms running in 2 O(k/ log k)nO(1) time, which are essentially tight under the ETH. None of the above frameworks provides subexponential parameterized algorithms for problems on weighted graphs when the solution might be disconnected. The bidimensionality framework fails as the existence of a large grid minor does not reveal any information about a weighted solution. In particular, a solution for e.g. Maximum-Weight Independent Set may completely avoid the                               

> 1A separation of a graph is a pair ( X, Y ) of sets of vertices such that X∪Ycontains all vertices and there are no edges between X\Yand Y\X. The size of separation ( X, Y ) is |X∩Y|and two separations ( X, Y ) and ( X′, Y ′)are isomorphic if G[X] is isomorphic to G[X′] and X∩Y=X′∩Y′. See Section 2 for formal definitions.

3grid minor if the vertices in the grid minor are too light. The treewidth-pattern-covering approach fails because the solution cannot be covered by a small number of connected patterns of size O(k), and Nederlof’s approach focuses on counting problems and not on weighted problems. The addi-tional use of efficient inclusion-exclusion prohibits the approach to generalize to weighted problems. In fact, subexponential parameterized problems for weighted problems that might have solutions with many connected components seem to be very rare. We are only aware of such algorithms for 

Maximum-Weight Independent Set [vN20] and Weighted Vertex Cover [LPS +25]. This naturally raises the following question: Is there a general framework for subexponential parameterized algorithms that find a disconnected k-vertex pattern P of maximum/minimum weight? In this paper, we develop generic algorithmic tools that allow us not only to address this question, but also to tackle related problems involving (rooted/induced) minors. The approach is determin-istic and can also handle directed planar graphs. 

1.1 Our Results 

Our contribution is threefold. First, we introduce a framework to generate subexponential para-meterized algorithms (usually running in 2 ˜O(√k)n2.49 time) for problems on weighted planar graphs that search for a set of k vertices with a specified property. The framework also applies to cases where the number of potential patterns of a solution is exponential in the parameter. The frame-work is introduced in Section 3 and requires some definitions which we set up in Section 2. Second, we show how to apply this framework to prove that the following problem k-Subgraph Scor-ing can be solved in subexponential parameterized time when parameterized by k. This problem generalizes problems like Maximum-Weight Independent Set , Weighted Partial Vertex Cover , Densest k-Subgraph , Maximum (k, n − k)-Cut , and more. 

k-Subgraph Scoring 

Input: A directed graph G = ( V, A ), a vertex-cost function c : V → N, a weight function w : V ∪ A → R, an integer k, and values α1, α 2, α 3, β, W ∈ R.

Question: Is there a set S of cost exactly k, that is, P 

> v∈S

c(v) = k and 

α1

  X

> (u,v )∈Au,v ∈S

w(( u, v ))  + α2

  X

> (u,v )∈Au∈S,v / ∈S

w(( u, v ))  + α3

  X

> (u,v )∈Au / ∈S,v ∈S

w(( u, v ))  + β  X 

> v∈S

w(S) ≥ W ?Note that when c(v) = 1 for each vertex v, then k is the number of vertices in the solution. Third, we show more involved applications of the framework in the form of Maximum-Weight Induced Forest and different notions of minors. All applications of our framework are shown in Section 4 and summarized in the following (see Section 2 for problem definitions). 

Theorem 1. The following problems can be solved in 2 ˜O(√k)n2.49 time when parameterized by the size of the solution: 

• k-Subgraph Scoring 

• Densest k-Subgraph 

• Steiner Partition 

4• Weighted Partial Vertex Cover 

• Maximum-Weight Induced Matching 

• Maximum-Weight Induced Forest 

The following problems can be solved in 2O(k/log( k))n2.49 time when parameterized by the size of the model (or the size of the pattern graph in the case of Subgraph Isomorphism ): 

• Minimum-Weight Rooted Simple Minor 

• Maximum-Weight Rooted Parallel Induced Minor 

• Minimum-Weight Subgraph Isomorphism 

• Maximum-Weight Induced Subgraph Isomorphism 

Before we give an overview of how the framework works, let us be explicit about our perspective here. The main goal of our work is to improve the theoretical understanding of subexponential phenomena, and we focus exclusively on this aspect. We do not attempt to optimize our results from the perspective of algorithm engineering, and reducing the hidden constant in the exponent is an interesting challenge beyond the scope of this work. Moreover, the framework is technically involved and considerably less accessible than, for example, bidimensionality. This is perhaps unsurprising, since problems seem to become substantially more complicated as soon as one moves to weighted or directed graphs. Nonetheless, this raises the natural question of whether a simpler approach— perhaps based on a logic fragment such as MSO—could achieve similar results. However, pursuing such a direction is hindered by the delicate boundary between problems that admit subexponential parameterized algorithms on planar graphs and those that do not. In particular, while the above theorem shows that Steiner Partition can be solved in subexponential parameterized time, even slight generalizations may break this property. For instance, the closely related Steiner Forest 

problem—a variant where different terminal sets are not necessarily contained in distinct connected components—does not admit a subexponential parameterized algorithm even on unweighted planar graphs, as shown by Pilipczuk, Pilipczuk, Sankowski, and van Leeuwen [PPSvL18]. 

1.2 Our Methods 

We next give an overview of how the main results in this work are achieved. We start with a high-level overview of the general strategy using Independent Set as an illustrative example and show some potential pitfalls. This general strategy (avoiding the pitfalls) follows the algorithm by Nederlof [Ned20]. In a second step, we give a few more technical details regarding the algorithm and highlight the key distinctions between our algorithm and the one by Nederlof. Then, we show how to apply this general framework to different problems. The application for k-Subgraph Scoring 

will be quite simple and for Maximum-Weight Induced Forest and different variants of graph minors, we will set up increasingly complicated notions. We now give a high-level overview of the general strategy. As a starting point, consider the classic divide-and-conquer strategy pioneered by Lipton and Tarjan in the context of planar separ-ators [LT80]. Given a planar graph G = ( V, E ), an integer k, and a balanced (cycle) separator C

in G, we guess 2 which vertices of C appear in the solution. We then remove C and all neighbors of those chosen vertices from the graph, guess how many vertices of the solution lie in the interior 

> 2Whenever we say we “guess” something, we actually iterate over all possibilities, but for presentation and proof, we focus on the iteration that leads to an optimal solution.

5and exterior of C, and solve the resulting subproblems recursively. Once each subproblem becomes sufficiently small, we solve them by applying a brute-force procedure. Unfortunately, the general idea sketched above does not achieve the desired running time for three main reasons. First, if we guess which vertices of C belong to a solution and then make separate recursive calls for each guess, the running time takes the form f (k)log n for some (super-polynomial) function f .Even setting f (k) = k yields a running time of klog n, which is not contained in 2 ˜O(√k)poly( n). To overcome this, we only branch on the separator C and solve a more general problem on the interior and exterior, where we output for any set X of vertices in C the largest independent set containing exactly the vertices in X of C.The second issue is that the separator C could intersect the optimal solution in too many vertices. This leads to too many possible choices resulting in an exponential running time in k. We circumvent this by using a result due to Nederlof [Ned20] (see Lemma 6) that roughly allows us for any (even unknown) weight function to replace C by a set S of up to 2 O(log 2(k)) balanced cycle separators such that at least one of them is balanced for the weight function and does not intersect the solution in too many vertices. However, since 2 O(log 2(k)) > k , we again run into the problem that we cannot recursively solve all of them as this leads to a running time of O(klog n), which is too slow. This time, we circumvent the issue by observing that if C contains many vertices from the solution (more than 20 √k log( k)), then it is sufficiently balanced for the weight function that assigns a constant weight to each vertex in the (unknown) solution and zero to all other vertices. Hence, at least one cycle separator C′ ∈ S is also balanced for this weight function and does not intersect the solution in too many vertices. This allows us to reduce the parameter k by O(√k) in these cases, which can be shown to yield a sufficiently small number of recursive calls. Finally, the third issue relates to both of the above. Consider a recursive call for some separ-ator C and for the sake of argument let us assume we are considering the interior of C. We then find a separator C′ in the interior of C and branch into the interior and exterior of C′. Note that the exterior of C′ now has two boundaries: both the vertices in C and in C′. Even if each of these two cycle separators are small enough and contains few enough vertices of an optimal solution, together they can be too large and/or contain too many vertices of an optimal solutiuon. Technically, this does not happen at recursion depth one, but Figure 1 shows an example where one subgraph is incident to many cycle separators. To overcome this issue, we again use the lemma by Nederlof to find/guess a cycle separator that contains few vertices of an optimal solution and is balanced for the weight function that assigns a constant weight to all vertices in previous cycle separators that the current subgraph is incident to and zero to all other vertices. The blue line in Figure 1 shows an example of such a cycle separator. We then branch on this new cycle separator and show that the number of possible guesses is again small enough. So overall, the general strategy looks as follows. We want to solve the following generalization of 

Independent Set : Given a planar graph G, a set B of vertices (the union of some cycle separators), and an integer k, output a lookup table that reports for each sufficiently small set Z ⊆ B whether an independent set I of size (at most) k exists in G such that I ∩ B = Z. Initially B = ∅. If the graph has some constant size, then solve it using brute force and otherwise, compute a balanced cycle separator C. Guess if an optimal solution intersects C in many or few vertices. If it is in many, then find/guess a different cycle separator C′ using the lemma by Nederlof and reduce the parameter k

accordingly. If it is few, then consider C′ = C and do not reduce the parameter. Let Gin and Gex 

be the graphs induced by the interior and exterior of C′, respectively, and let B′ 

> in

and B′ 

> ex

be the subsets of B ∪ C′ in either graph. If B′ 

> in

and B′ 

> ex

are small enough, then recursively solve the two subinstances and do the following. For each sufficiently small set Z, compute the optimal solution for Z by finding the optimal solutions Zin , Z ex for both subinstances where Zin ∩ C = Z

6Figure 1: Illustration of a graph cut open by a set of cycle separators (red). The blue line shows a cycle separator that separates the red vertices in a balanced way (assuming that the number of vertices is roughly proportional to the length of the respective line). and Zex ∩ C = Z. The union of Zin and Zex is then an optimal solution for Z (and the vertices in Z are counted twice, so an optimal solution has size |Zin | + |Zex | − | Z|). If B′ 

> in

and/or B′ 

> ex

are too large, then find a separator that splits the set in a balanced way and solve the two subinstances instead. We mention that the above is oversimplified in two aspects. First, the lemma by Nederlof does not return a small cycle separator which only intersects any solution in sufficiently few vertices. Rather, it returns a cycle separator and a 3-partition of its vertices into heavy, light, and discarded vertices. The number of heavy vertices is small but a solution might contain any number of heavy vertices. The number of light vertices is larger (but still bounded) and there is a guarantee that the solution only intersects it in sufficiently few vertices so that we can still afford to iterate over all subsets of the respective size in 2 ˜O(√k) time. Lastly, the number of discarded vertices is unbounded, but we are guaranteed that the solution does not contain any discarded vertices. The second oversimplification is the brute-force step in case the graph is sufficiently small. In order to make progress when reducing the parameter, we also need an algorithm for the case where the parameter is a constant. Let us now give a few more details on our main algorithm and highlight some of the details where our approach differs from that of Nederlof. First and of least importance, Nederlof does not analyze the polynomial part of the running time. In particular, the use of a black-box algorithm from the treewidth-pattern-covering approach [FLM +22] incurs as stated a running time of O(n4)in each recursive call. We overcome this issue by replacing costly flow and dual-LP computations in their algorithm by recently discovered near-linear-time algorithms [CKL +25]. We then analyze the running time of both the algorithm from the treewidth-pattern-covering approach [FLM +22] and the one by Nederlof including the polynomial part of the running time (for the first time). Moreover, the base-case algorithm when the parameter becomes small enough is different in our algorithm. While both perform dynamic programming over certain tree decompositions, we use balanced tree decompositions in order to improve the polynomial part of the running time. Second, even ignoring the running time per recursive call, the analysis by Nederlof bounds the number of recursive calls by O(n4). Fortunately, this issue can be resolved by a more careful analysis. Unfortunately, we are not aware of a way to show it using statements of the form “there exists a 7sufficiently large constant c such that . . . ”. Instead, we carefully choose several constants in order for the analysis to work. The third issue stems from the nature of our undertaking. Since we wish to provide a general framework rather than solving a specific problem, some steps that can easily be shown for a specific problem like Subgraph Isomorphism require careful consideration in our setting. Two examples of this are the definition of the problem variant we wish to solve and the algorithm for the base case when either the parameter or the number of vertices is sufficiently small. Recall that for Independent Set , we wanted to solve a more general version where we return a lookup table. In order for our framework to be applicable as often as possible, we do not give a strict recipe of how to generalize any particular problem, but instead give a list of minimal requirements that the problem has to fulfill in order for our framework to work. For the algorithm for the base case when the parameter is constant, we show how to perform dynamic programming over a tree decomposition using only the minimal requirements. Finally, we give an overview of the different applications of our framework. We start by showing a general recipe of how to apply it and a subexponential parameterized algorithm for k-Subgraph Scoring is a first simple-to-show corollary. This one is simple as the way in which two partial solu-tions can be combined along a separator is quite trivial. It is easy to see that k-Subgraph Scor-ing generalizes Maximum-Weight Independent Set , Weighted Partial Vertex Cover ,

Densest k-Subgraph , Maximum (k, n − k)-Cut , as well as weighted variants of the latter two problems and thus, we get subexponential parameterized algorithms for all of them. We then turn our attention to Maximum-Weight Induced Forest . Here, we show how to encode connectivity and being acyclic using partitions and the join of two sets. For Minimum-Weight Rooted Simple Minor , the main new obstacles to overcome are the exponential number of possible models for a single node of H and how to encode information about which vertex in the separator plays which role (is part of the model of which node or edge of H). We deal with the latter issue in a similar way as Nederlof did for Subgraph Isomorphism , that is, we store for which subgraph of H we already have a model and also a separator of some fixed size for this subgraph to the rest of H. Since we assume that the current cycle separators only contain relatively few vertices from the solution and the separator in H also has a fixed size, we can show that the number of possible ways to assign the nodes in the separator to any sufficiently small subset of the vertices in the current cycle separator is small enough so that we can enumerate them all and find corresponding solutions in subexponential parameterized time. For the former issue, we do not store the model for H but only which vertices in the model are already connected (plus information about which incident edges in H are already part of the model) and again rely on our encoding for connectivity for Maximum-Weight Induced Forest .Finally, we consider parallel minors (where the pattern graph H can have parallel edges and one wishes for a minor model with at least (or exactly in the case of induced minors) that many connections within the model). Here, the main problem is that planar multigraphs can have an exponential number of non-isomorphic separations and hence the previous analysis cannot yield subexpontial running times. We overcome this issue by defining a new parameter for the analysis and show that if this parameter is exponential, then no planar (simple) graph with k vertices can have H as a minor. Thus, we have a simple win-win approach. Either the parameter is large and we can immediately answer no (the parameter will be easy to compute) or the parameter is small (subexponential) and we can adapt the analysis to show subexponential parameterized algorithms. 82 Preliminaries 

In this section, we first give an overview of the notation and basic concepts we use. Then, we give all formal problem definitions. In a third step, we formalize request variants of problems. Recall that for Independent Set , we want to solve a more general version where we output a lookup table that answers for each set Z of certain size (and certain interaction with heavy, light, and discarded vertices of the cycle separators), whether an independent set of a certain size exists where exactly the vertices in Z are contained from the cycle separators. This is the request variant for Independent Set and we give a definition (plus some intuition) of what type of generalized problem we can solve using our framework. Finally, we give an overview of the different results from the known literature that our framework relies on. We start with some notation and assume familiarity with standard notation from graph theory and theoretical computer science. In particular, we denote by n and m the number of vertices and edges in a graph, respectively. We use N to denote the set of positive integers and ˜O to hide polylogarithmic factors in the Bachmann–Landau (big-O) notation. For an integer ℓ, we use [ ℓ]to denote the set {1, 2, . . . , ℓ }. The base of all logarithmic functions in this work is 2 and we assume the word RAM model of computation (which means that operations like computing sums or minima over a constant number of real numbers take constant time independent of the size of the numbers). Given a graph G = ( V, E ) and a vertex set V ′ ⊆ V , we denote the graph induced 

by V ′ by G[V′] and the graph obtained by deleting all vertices in V ′ (and all edges incident to those vertices) by G − V′. For a set F ⊆ E of edges, we use G − F as a shorthand for ( V, E \ F ). Given a set W (not necessarily a subset of V ), we denote by W [G] = W ∩ V the intersection of W

and the vertex set of G.An embedding of a planar graph is 1-outerplanar if it is outerplanar, that is, all vertices lie on the outer face. For d ≥ 2, an embedding of a planar graph is d-outerplanar if removing all vertices on the outer face results in a ( d − 1)-outerplanar embedding. A graph is d-outerplanar if it has a d-outerplanar embedding. A planar graph is triangulated if each face is bounded by three edges. A separation of G is a pair ( X, Y ) of vertex subsets such that X ∪ Y = V and there are no edges between vertices in X \Y and Y \X. The set X ∩Y is called the separator and slightly overloading notation, we will sometimes also refer to some graph with vertex set X ∩ Y as the separator. The size of a separation ( X, Y ) is the number of vertices in its separator. Two separations ( X, Y )and ( X′, Y ′) are isomorphic if there is an isomorphism f : V → V such that S 

> v∈X

f (v) = X′

and f (v) = v for each v ∈ X ∩ Y . In a planar graph together with an embedding, (the vertex set of) each simple cycle C is a separator that separates the strict interior (the graph enclosed by C) from the strict exterior . We therefore prefer to use the term cycle separator rather than simple cycle. The union of the strict interior/exterior of C and C is denoted by the (non-strict) interior/exterior of C. For a cycle separator C, we denote the interior by Cin and the exterior by Cex .A tree decomposition of a graph G = ( V, E ) is a rooted tree T = ( U, A ) in which each node u

represents a subset Xu of vertices called the bag of u such that 

• each vertex in V is contained in at least one bag, 

• for each edge {v, w } ∈ E, there is a node u ∈ U with v, w ∈ Xu, and 

• for each v ∈ V it holds that all nodes u of T with v ∈ Xu form a connected subtree of T .The width of a tree decomposition is max u∈U |Xu| − 1 and the treewidth of G is the minimum width of all possible tree decompositions of G. The depth of a tree decomposition is the length of a longest path from the root to any other node and a tree decomposition is binary if each node has at most two children. Given a tree decomposition T = ( U, A ) of a graph G and a node u ∈ U , we denote by Yu the union of all bags of u and descendants of u in T . Note that for the root p of T it 9holds that Yp = V .A contraction of an edge {u, v } in a graph removes u and v from the graph, creates a new vertex uv and replaces each other edge {x, y } with x ∈ { u, v } with an edge {uv, y }. Depending on the context, one sometimes removes parallel edges and self loops and other times allows the resulting graph to be a multigraph. To show the versatility of our framework, we will consider both versions and say that a contraction is simple if it removes parallel edges and self loops and it is a parallel contraction otherwise. A (multi)graph H = ( U, F ) is a simple/parallel minor of a simple graph G = ( V, E ) if there is a subgraph G′ of G such that simple/parallel contractions of G′

result in a graph isomorphic to H. We say that H is an induced simple/parallel minor if there is an induced subgraph G′ of G that can be contracted to obtain H. In all cases, the graph G′ is called the model of H and we also study rooted versions. Here, additionally to the (multi)graph H,we are given a function η : U → 2V that describes for each node 3 of H a set of vertices of G that have to be contained in the desired model of H. More formally, for any u ∈ U , all vertices in η(u)have to be contracted into a single vertex u′ such that there is an isomorphism from the resulting graph to H mapping u′ to u.A proper weight assignment w is an assignment of non-negative weights at most 14 to vertices summing to 1. For q ∈ [0 , 1], a q-balanced cycle separator for a proper weight assignment w is a cycle C such that the sum of weights of all vertices in the strict interior of C is at most q and the sum of weights of all vertices in the strict exterior of C is at most q.We say a pair ( C, x ) consisting of a set C of vertices and partitioned into heavy vertices Che ,light vertices Cli , and discarded vertices Cdi and an integer x is almost measured with respect to two integers k and α if x ≤ max(200 log 7(k), 1), |Che | ≤ 40 xα √k, and |Cli | ≤ 60 xαk log( k). It is 

measured if it is almost measured and x ≤ max(40 log 7(k), 1) holds. A pebble set for ( C, k, α, x )is a set M ⊆ Che ∪Cli with |M∩ Cli | ≤ 20 xα √k log( k). Intuitively, pebble sets describe the possible sets of vertices in a collection of cycle separators for which we wish to store a solution in a lookup table. 

2.1 Problem Definitions 

Here, we define the problems mentioned in the introduction. We start with different variants of finding minors in a graph. We deliberately chose to use both simple and parallel contractions, minors and induced minors, minimization and maximization variants to illustrate the versatility of our framework. The other possible combinations can be solved similarly, but for the sake of conciseness, we do not explicitly study them. 

Minimum-Weight Rooted Simple Minor 

Input: A graph G = ( V, E ), an edge-weight function w : E → R+, a graph H = ( U, F ), a function η : U → 2V , an integer k, and a value W ∈ R.

Question: Is there a subgraph G′ of G with at most k vertices and of total weight at most W such that G′ is a model of H, that is, it contains all vertices in S 

> u∈U

η(u) and H is a rooted simple minor of G′?   

> 3We call the vertices in the pattern graph Hnodes in order to avoid confusion with the vertices in the graph G.

10 Maximum-Weight Rooted Parallel Induced Minor 

Input: A graph G = (V, E ), an edge-weight function w : E → R+, a multi-graph H = ( U, F ), a function η : U → 2V , an integer k, and a value W ∈ R.

Question: Is there an induced subgraph G′ of G with at most k vertices and of total weight at least W such that G′ contains all vertices in S 

> u∈U

η(u) and H is a rooted parallel induced minor of G′?The next problem we study is Maximum-Weight Induced Forest .

Maximum-Weight Induced Forest 

Input: A graph G = ( V, E ), a vertex-weight function w : V → R, an integer k, and a value W ∈ R.

Question: Is there a set S ⊆ V of at most k vertices of total weight at least W such that G[S] is a forest? The definition of k-Subgraph Scoring was already shown in the introduction. We repeat it here to have all problem definitions in one place. 

k-Subgraph Scoring 

Input: A directed graph G = ( V, A ), a vertex-cost function c : V → N, a weight function w : V ∪ A → R, an integer k, and values α1, α 2, α 3, β, W ∈ R.

Question: Is there a set S of cost exactly k, that is, P 

> v∈S

c(v) = k and 

α1

  X

> (u,v )∈Au,v ∈S

w(( u, v ))  + α2

  X

> (u,v )∈Au∈S,v / ∈S

w(( u, v ))  + α3

  X

> (u,v )∈Au / ∈S,v ∈S

w(( u, v ))  + β  X 

> v∈S

w(S) ≥ W ?When the parameters α1, α 2, α 3 and β are clear from the context, then we call the value for a particular set S the score of S. All other problems we study are simple corollaries of the above problems. We hence only give informal problem statements for these. In Weighted Partial Vertex Cover , the input consists of a graph G with positive integer costs and real-valued edge weights, an integer k, and a real value W . The question is whether there is a set of vertices of cost at most k such that the weight of covered edges is at least W . An edge is covered if at least one of its endpoints is contained in the solution. Note that we require all costs to be integers as otherwise the problem is para-NP-hard when parameterized by k (start with an instance ( G, k, W )of unweighted Partial Vertex Cover and set the weight of each vertex to 1 

> k

). In Maximum-Weight Independent Set , we are given a graph G with real-valued vertex weights, an integer k,and a real value W . The questions is whether an independent set of size at most k and weight at least W exists. In Densest k-Subgraph , we are given an undirected graph G and two integers k and W .The question is whether there is a set of exactly k vertices such that there are at least W edges with both endpoints in that set. Maximum (k, n − k)-Cut can be defined on both undirected and directed graphs together with two integers k and W . In the undirected setting, the question is whether there is a set S of exactly k vertices such that there are at least W edges with exactly one endpoint in S. In the directed setting, the question is whether there is a set S of exactly k vertices such that there are at least W edges ( u, v ) with u ∈ S and v / ∈ S.In Minimum-Weight Subgraph Isomorphism , the task is to find a subgraph of minimum weight that is isomorphic to a given pattern graph H. In Maximum-Weight Induced Subgraph Isomorphism , the task is to find an induced subgraph of maximum weight that is isomorphic to a 11 given pattern graph H. Note that the original work by Nederlof [Ned20] showed how to solve the unweighted variants of the above two problems (and also how to count the number of solutions). 

Steiner Partition is a generalization of Steiner Tree where the solution is disconnected. Here, we are given an undirected graph, a set T = {X1, X 2, . . . , X t} of sets of terminal vertices, an edge-weight function, an integer k, and a real value W . The task is to find a subgraph G′ of G

with at most k vertices of total weight at most W such that for each i ∈ [t], all vertices in Xi

are in the same connected component in G′ and for any i̸ = j, the vertices in Xi and the vertices in Xj are in distinct connected components in G′. Steiner Partition can be seen as the dual of 

Cut-Uncut , where the goal is to remove as few edges as possible to get to a subgraph satisfying the above. The special case of Cut-Uncut with only two sets X1 and X2 was recently studied for planar graphs [BDF +24]. 

2.2 Request Variants 

We now give a characterization of the type of problems our framework can solve. We call those 

request variants and for a regular problem π, we call its request variant π-Request . In order to allow the framework to be applicable in as many situations as possible, we do not give a strict recipe of how to generalize a problem π to π-Request , but instead our main result (Theorem 3) lists the requirements that the problem π-Request has to satisfy in order for the framework to work. Before we give a formal definition of requests variants, we will give some intuition regarding the different concepts we rely on and revisit the example Independent Set from the introduction. The first requirement we have is a notion of locality . This stems from the use of the lemma by Nederlof and basically says that the famous approach by Baker is applicable. In particular, we require that there is some constant απ depending on the problem such that the following holds. If we are looking for a solution with at most k vertices, and if we compute a breadth-first search from an arbitrary vertex r and then partition all vertices depending on their distance from r

modulo απk + 1, then there exist a solution in the entire graph if and only if for at least one of the απk + 1 parts, it holds that after removing all vertices from the respective part, there is a solution of size at most k in the remaining graph. In the case of Independent Set , this trivially holds for α = 1. The k vertices of a solution can be contained in at most k of the parts so one part does not contain any vertices from the solution. Removing all vertices in this part does neither create new independent sets nor does it invalidate the solution we consider. We say that the request variant of Independent Set has locality α = 1. This notion of locality will allow us to reduce the problem to a setting where the graph is απk-outerplanar. The next ingredient for request variants are requests . As stated above, we wish to solve problems where the output is not a single solution but rather a lookup table. We call the queries that this lookup table should be able to answer requests and for a problem π-Request , we denote the function that enumerates all possible requests by Rπ. These requests mainly depend on the following four objects: a parameter d, a graph G, a portal set B = ( Bhe , B li , B di ), and a pebble set M.Let us give some intuition about these. The graph G should be thought of as a subgraph of the input graph defined by cutting out some parts along certain cycle separators. The parameter d

describes an upper bound on both the number of vertices we allow in a solution and (together with απ) the outerplanarity of G. We will achieve subexponential parameterized algorithms for this parameter d. As we will reduce the value of d in some recursive calls, we prefer to distinguish it from the input parameter k. The portal set B should be thought of as the boundary of G to the rest of the input graph. For technical reasons, we always give the portal set as the union of three sets: heavy, light, and discarded vertices. The intuition is that in order to keep the number 12 of possible intersections of a solution with the portal set subexponential, we restrict ourselves to those solutions that do not contain any discarded vertices and sufficiently few light vertices. On the flip side, the number of heavy vertices will be quite small and the number of light vertices will be somewhat small. The pebble set describes which vertices in B should be contained in the solution. This should be seen as the interface of a partial solution to the rest of the graph. Each request variant will take an integer x as input which will limit how many vertices of the solution we allow to be contained in B. Intuitively, this simply counts how many cycle separators are contained in B but this intuition will not quite work due to technicalities when we compute cycle separators that separate other cycle separators in a balanced way. We then consider pebble sets for the tuple ( B, d, α π, x ). Recall that these are sets M ⊆ Bhe ∪ Bli with |M ∩ Bli | ≤ 20 xα π

√d log( d). We also allow requests to depend on one additional value, which we will use for problem specific information. We denote these by Sπ and we only have one minimal requirements regarding these: they should be maintainable in subexponential time. In many cases, the set Sπ simply contains weight functions and for Independent Set Request , this set will be empty. In particular, a request for Independent Set Request will only consist of a pebble set M. We can now formally define Independent Set Request and afterwards give a definition for request variants in general. 

Independent Set Request 

Input: An integer d, a d-outerplanar graph G = ( V, E ), a portal set B = ( Bhe , B li , B di ), and an integer x.

Task: Output a lookup table that returns for each request r = M for each pebble set M

for ( B, d, 1, x ), the maximum size of an independent set I in G where I ∩ (Bhe ∪ Bli ∪

Bdi ) = M (or −∞ if no such set exists). Recall that a pair ( B, x )—consisting of a set B of vertices partitioned into heavy, light, and discarded vertices and an integer x—is almost measured with respect to two integers d and απ

if x ≤ max(200 log 7(d), 1), |Bhe | ≤ 40 xα π

√d, and |Bli | ≤ 60 xα πd log( d). 

Definition 1. A request variant π-Request of locality απ takes a tuple (d, G, B, x, S π) as input where B = ( Bhe , B li , B di ) is partitioned into heavy, light, and discarded vertices, G is a απd-outerplanar graph, and (B, x ) is almost measured with respect to d and απ. It outputs a lookup table with an entry for each request r ∈ Rπ(d, G, B, M, S π) where M is a pebble set for (B, d, α π, x ).The output represents some solution where the vertices in the solution contained in Bhe ∪ Bli ∪ Bdi 

is exactly M.

2.3 Results Used in our Algorithm 

Finally, we state a number of results from the literature, which we will subsequently rely on. The first one is a well-known strategy due to Baker to transform a planar graph into several d-outerplanar graphs. 

Lemma 1 ([Bak94]) . Given a planar graph G and an integer d, one can compute in linear time a partition of the vertices of G into sets A1, A 2, . . . , A d such that for each i ≤ d, the graph G − Ai

is (d − 1) -outerplanar. 

The second result allows us to triangulate a d-outerplanar graph. 

Lemma 2 ([Bie15], Corollary 1) . Any d-outerplanar graph G can be triangulated in O(n2) time such that the resulting graph has outer-planarity at most d + 1 .

13 The third result will be used to compute balanced cycle separators. Therein, a fundamental cycle of an edge e with respect to a spanning tree T (which does not contain e) is the unique simple cycle that contains e and all its other edges are contained in T .

Lemma 3 ([KM], Lemma 5.3.2.) . There is a linear-time algorithm that, given a triangulated plane graph G, a spanning tree T of G, and a proper weight assignment, returns a nontree edge e such that the fundamental cycle of e with respect to T is a 3/4-balanced cycle separator for G.

We use this lemma in the following way. 

Corollary 1. Given a d-outerplanar triangulated graph G and a proper weight assignment, one can compute in O(n2) time a 3/4-balanced cycle separator of size at most 2d + 1 for G.Proof. First, compute in O(n2) time a d-outerplanar embedding of G [Kam07]. Let L(v) be the layer of v, that is, the vertices on the outer face have L(v) = 0 and when removing all vertices with L(v) < i for some i ∈ [d − 1], the vertices on the new outer face receive L(v) = i. Since G is triangulated, the outer face is a triangle. Build a spanning tree T for G in O(n) time as follows. Start with any two edges on the outer face and then iteratively for increasing i ∈ [d − 1] and each vertex v with L(v) = i add an edge between v and a vertex u with L(u) = i − 1 to T . Note that such an edge exists as G is triangulated. Now apply Lemma 3 to compute in O(n) time an edge e not contained in T such that the fundamental cycle of e with respect to T is a 3/4-balanced cycle separator for G. Note that the fundamental cycle of e can be computed in O(n) time and it contains at most two vertices of each layer (except for the outermost, where it can contain three) and therefore at most 2 d + 1 vertices in total. Next, we use a result that bounds the treewidth of k-outerplanar graphs. 

Lemma 4 ([Bod98]) . Each k-outerplanar graph has treewidth at most 3k − 1.

We use this in combination with a linear-time algorithm to compute an approximate tree decom-position in the case where the treewidth is a constant and a result to transform a tree decomposition into a binary tree decomposition of logarithmic depth without increasing the width too much. 

Theorem 2 ([Kor21]) . There is an algorithm that, given an n-vertex graph G and an integer k,in 2O(k)n time either outputs a tree decomposition of G of width at most 2k + 1 or determines that the treewidth of G is larger than k.

Lemma 5 ([BH98]) . There is an algorithm that, given an n-vertex graph G and a tree decomposition of width k, computes a binary tree decomposition of G of width at most 3k + 2 and depth O(log( n)) 

in 2O(k)n time. 

The main ingredient of our subexponential parameterized algorithm is a lemma due to Neder-lof [Ned20]. He mentions the lemma without formal statement or proof and he does not analyze the constants involved in the running time. However, the proof follows directly from the proof of his Lemma 3.7 and we will give a proof sketch for analyzing the running time. 

Lemma 6. There is an 2O(log 2(k)) n1+ o(1) -time algorithm that 

• given a triangulated graph G = ( V, E ) and a cycle separator C on at most k vertices that is q-balanced for a (possibly unknown) proper weight assignment w,

14 • outputs a set C of at most 244 log 2(k) cycle separators each partitioned into heavy, light, and discarded vertices such that for each C ∈ C , (C, 1) is measured for k and 1 and for each P ⊆ V

with |P | ≤ k, at least one C ∈ C is max( 3/4, q )-balanced for w, P ∩ Cdi = ∅, and 

|P ∩ Cli | ≤ 11 |P | log( k)2√k .

Analysis of Running time. The algorithm by Nederlof iteratively replaces one cycle separator by 4. The total number of cycle separators considered is in O(2 44 log 2(k)) and for each cycle separator, he applies a menger-type algorithm by Fomin, Lokshtanov, Marx, Pilipczuk, Pilipczuk, and Saur-abh [FLM +22]. Based on the outcome, only some vertices are added to either the heavy, light, or discarded vertices. The time needed for the latter is dominated by the menger-type algorithm, so it only remains to analyze this algorithm. This algorithm first computes in O(n + m) time an auxiliary graph H whose number of vertices and edges is linear in the number of vertices and edges in the original graph. Since the input graph is planar, this takes O(n) time and H also has O(n)edges (but is not necessarily planar). It then computes a minimum-cost flow in H, partitions the flow into walks, shortcuts these walks to paths if necessary, computes a solution to the dual LP of the minimum-cost flow problem, and partition the vertices based on the solution for the dual. Using recent improvements for almost-linear time algorithms for flows and their duals [CKL +25], we can compute an optimal integral solution to the minimum-cost flow problem and its dual LP in O(m1+ o(1) ) = O(n1+ o(1) ) time. All other steps can be performed in O(kn log n) time, so overall, the algorithm takes 2 O(log 2(k)) n1+ o(1) time. Finally, we summarize a couple of simple inequalities that we will use repeatedly. 

Lemma 7. If d ≥ 16 , then log 2(d) ≤ d. If d ≥ 54 , then ⌊d − 3√d⌋ ≥ 4d 

> 7

. If d ≥ 54 ,then 

p

D − 3√D + 1 − √D ≤ − 1/10 .Proof. Since the first statement is trivial, we will focus on the second and third. To this end, observe that ⌊d − 3√d⌋ ≥ d − 3√d − 1. As both sides of the inequality are positive for any d ≥ 11, we can square both sides and only show d − 3√d − 1 ≥ 4d 

> 7

. Note that 

d − 3√d − 1 ≥ (4/7)d

⇔ (3/7)d ≥ 3√d + 1 

⇔ √d ≥ 7 + 7/(3 √d),

> 7

/(3 √d) ≤ 1/3 for all d ≥ 49, and 7 + 1/3 ≤ √54. Note that the third inequality is equivalent to √D − 11 /10 ≥

p

D − 3√D. Since both sides are positive for any d ≥ 54 (as √D − 11 /10 ≥ √54 − 2 > 5 and 

p

D − 3√D ≥ p54 − 3√54 > 5), we can square both sides and only show ( √D − 11 /10 )2 ≥ D − 3√D. This holds as (√D − 11 /10 )2 = D − 22 10 

√D + 121 100 ≥ D − 30 10 

√D + 0 = D − 3√D. 

# 3 The Framework 

In this section, we show our main result, Theorem 3. It describes a general algorithmic framework for solving request variants on planar graphs in subexponential parameterized time. The framework can handle non-bidimensional weighted problems and does not require the solution to consist of a 15 small number of connected components. It can also handle directed graphs, but in order to keep the presentation as concise as possible, we will focus on undirected graphs here and mention that just considering the underlying undirected graph allows to extend all results to directed graphs as well. We next state our main theorem, but defer its proof to the end of this section. Note that in the following we consider arbitrary separators, not necessarily cycle separators. We still use the terms Cin and Cex to refer to the graphs induced by either side of the separation. 

Theorem 3. Let π-Request be a computable request variant, ρ ≥ 1 be a constant, and γ be a non-decreasing computable function such that R1 π-Request has constant locality απ, that is, for any instance I = ( d, G, B, x, S π) of π-Request , any request r ∈ Rπ(d, G, B, M, S π), and any BFS layering V1, V 2, V 3, . . . , V i of G

with i > α πd, the following holds. Let V j be the union of all sets Vℓ with ℓ ≡απ d+1 j for each j ∈ [απd + 1] and let I′ 

> j

= ( d, G ′ = G[V \ V j ], B [G′], x, S π[G′]) be the instance where all vertices from Vj are removed. Then, the output for instance I and request r is equal to either the maximum or the minimum of the outputs for all I′ 

> j

and r containing all vertices in M.R2 Rπ(d, G, B, M, S π) can be computed in O(γ(d)nρ) time. R3 Given a separator C, Sπ[Cin ] and Sπ[Cex ] can be computed in O(γ(d)nρ) time. R4 There is an algorithm that given a separator C = ( Che , C li , C di ) such that (C, 1) is measured for d and απ, a pebble set M for (B, d, α π, x ), and the solutions for instances 

(d, C in , (( Bhe ∪ Che )[ Cin ], (Bli ∪ Cli )[ Cin ], (Bdi ∪ Cdi )[ Cin ]) , x + 1 , S π[Cin ]) and 

(d, C ex , (( Bhe ∪ Che )[ Cex ], (Bli ∪ Cli )[ Cex ], (Bdi ∪ Cdi )[ Cex ]) , x + 1 , S π[Cex ]) ,

can compute the solutions for instance I for all requests r ∈ Rπ(d, G, B, M, S π) in O(γ(d)nρ)

time that do not intersect Cdi and intersect Cli in at most 20 απ

√d log( d) vertices. Then, π-Request can be solved in 2 ˜O(√d)γ(d)nmax(2 .49 ,ρ +ε) time for any ε > 0.

The rest of this section is structured as follows. First, we present three algorithms that recurs-ively call one another to solve the problem. One of them ( solve-π-r ) is the main algorithm, one (red-portals ) is for the special case in which the portal set B becomes too large, and the final (base-case ) handles the base cases when the parameter becomes small enough. We then show that a number of invariants are maintained throughout all algorithm calls. In the following three subsections, we analyze each of the three algorithms. There, we give an informal description of the respective algorithm and prove that it works correctly. This will yield the correctness proof of Theorem 3 in the end. In the next section, we will show how to apply this theorem to different problems. 

3.1 Algorithms and Invariants 

We start by formally stating the three aforementioned algorithms: solve-π-r (Algorithm 1), 

red-portals (Algorithm 2), and base-case (Algorithm 3). We present the algorithms for the case where π-Request is a maximization problem, but the minimization version can be solved by essentially the same algorithms. The aforementioned invariants are summarized in the following lemma. 

Lemma 8. Throughout all calls of solve-π-r , red-portals , and base-case , the following in-variants are maintained. 

16 Algorithm 1: solve-π-r (maximization) 

Input: (d, G = ( V, E ), x, B he , B li , B di , S π) as specified in Lemma 8.  

> 1

if |V | < 27απ then  

> 2

return brute force solution for instance ( d, G, B, x, S π) 

> 3

if d < 27απ then  

> 4

return base-case (d, G, x, B he , B li , B di , S π) 

> 5

if x ≥ 40 log 7(d) then  

> 6

return red-portals (d, G, x, 0, B he , ∅, B li , ∅, B di , ∅, S π) 

> 7

res ← −→∞ 

> 8

Use Lemma 2 to compute a set ∆ of edges such that G∆ = G + ∆ is triangulated and ( απd + 1)-outerplanar  

> 9

Use Corollary 1 to find a 3/4-balanced cycle separator C with a vertex set VC of at most 2 απd + 3 vertices in G∆ for the weight functions that assigns 1/|V | to each v ∈ V

/* case where C contains at most 20 √d log( d) vertices of the solution */  

> 10

Compute Sπ[Cin ] and Sπ[Cin ] using Requirement R3  

> 11

res 1 ← solve-π-r (d, C in − ∆, x + 1 , B he [Cin ], (Bli ∪ C)[ Cin ], B di [Cin ], S π[Cin ])  

> 12

res 2 ← solve-π-r (d, C ex − ∆, x + 1 , B he [Cex ], (Bli ∪ C)[ Cex ], B di [Cex ], S π[Cex ])  

> 13

foreach pebble set M for (B, d, α π, x ) do  

> 14

Compute Rπ(d, G, B, M, S π) using Requirement R2  

> 15

Use Requirement R4 to compute a solution s(r) for each r ∈ Rπ(d, G, B, M, S π) based on res 1 and res 2 

> 16

res( r) = max(res( r), s (r)) 

/* case where C contains more than 20 √d log( d) vertices of the solution */  

> 17

Use Lemma 6 with input G∆ and C to compute a set S of separators  

> 18

d′ = ⌊d − 3√d⌋ 

> 19

foreach C′ ∈ S with partition (C′

> he

, C ′

> li

, C ′

> di

) do  

> 20

Use Lemma 1 with απd′ on the interior of C′ to compute sets A1in , A 2in , . . . , A απ d′+1 in  

> 21

Use Lemma 1 with απd′ on the exterior of C′ to compute sets A1ex , A 2ex , . . . , A απ d′+1 ex  

> 22

foreach Ain , Aex do  

> 23

res 1 ← solve-π-r (d′, C ′ 

> in

− Ain − ∆, 2x + 2 , (Bhe ∪ C′

> he

)[ C′

> in

], (Bli ∪ C′

> li

)[ C′

> in

],

(Bdi ∪ C′)[ C′

> in

], S π[C′

> in

])  

> 24

res 2 ← solve-π-r (d′, C ′ 

> ex

− Aex − ∆, 2x + 2 , (Bhe ∪ C′

> he

)[ C′

> ex

], (Bli ∪ C′

> li

)[ C′

> ex

],

(Bdi ∪ C′)[ C′

> ex

], S π[C′

> ex

])  

> 25

foreach pebble set M for (B, d, α π, x ) do  

> 26

Compute Rπ(d, G, B, M, S π) using Requirement R2  

> 27

Use Requirement R4 to compute a solution s(r) for each r ∈ Rπ(d, G, B, M, S π)based on res 1 and res 2 

> 28

res( r) = max(res( r), s (r))  

> 29

return res 17 Algorithm 2: red-portals (maximization) 

Input: (d, G = ( V, E ), x, y, X he , Y he , X li , Y li , X di , Y di , S π) as specified in Lemma 8.  

> 1

res ← −→∞ 

> 2

if |Xhe ∪ Xli | ≤ 20 √d then  

> 3

return solve-π-r (d, G, 2y + 1 , X ∪ Y ) 

> 4

else  

> 5

Use Lemma 2 to compute a set ∆ of edges such that G∆ = G + ∆ is triangulated and ( απd + 1)-outerplanar  

> 6

Use Corollary 1 to find a 3/4-balanced cycle separator C with at most 2 απd + 3 vertices in G∆ for the weight functions that assigns 1/|X| to each vertex in X = Xhe ∪ Xli . 

> 7

Use Lemma 6 with input G∆ and C to compute a set S of separators  

> 8

foreach C′ ∈ S with partition (C′

> he

, C ′

> li

, C ′

> di

) do  

> 9

Compute Sπ[C′

> in

] and Sπ[C′

> ex

] using Requirement R3  

> 10

res 1 ← red-portals (d, C ′ 

> in

− ∆, x, y + 1 , X he [C′

> in

], (Yhe ∪ C′

> he

)[ C′

> in

],Xli [C′

> in

], (Yli ∪ C′

> li

)[ C′

> in

], X di [C′

> in

], (Ydi ∪ C′

> di

)[ C′

> in

], S π[C′

> in

])  

> 11

res 2 ← red-portals (d, C ′ 

> ex

− ∆, x, y + 2 , X he [C′

> ex

], (Yhe ∪ C′

> he

)[ C′

> ex

],Xli [C′

> ex

], (Yli ∪ C′

> li

)[ C′

> ex

], X di [C′

> ex

], (Ydi ∪ C′

> di

)[ C′

> ex

], S π[C′

> ex

])  

> 12

foreach pebble set M for (B, d, α π, x ) do  

> 13

Compute Rπ(d, G, B, M, S π) using Requirement R2  

> 14

Use Requirement R4 to compute a solution s(r) for each r ∈ Rπ(d, G, B, M, S π)based on res 1 and res 2 

> 15

res( r) = max(res( r), s (r))  

> 16

return res 

• The input to solve-π-r is a valid instance of π-Request and x ≤ 200 log 7(d) − 44 log( d).

• The input (d, G, x, y, X he , Y he , X li , Y li , X di , Y di , S π) given to red-portals satisfies the follow-ing: (d, G, x +2 y, X he ∪Yhe , X li ∪Yli , X di ∪Ydi , S π) is a valid instance of π-Request , d ≥ 27απ ,

x ≤ 200 log 7(d) − 44 log( d), y < 22 log( d), |Xhe ∪ Xli | ≤ (3/4)y · 50 d6, |Yhe | ≤ 40(2 y)απ

√d,and |Yli | ≤ 60(2 y)απd log( d).

• When solve-π-r with values d1 and z1 = x1 or red-portals with values d1 and z1 = x1 + 2 y1

calls solve-π-r with values d2 and z2 = x2 or red-portals with values d2 and z2 = x2 + 2 y2

and the results are used to combine results, then z1

√d1 log( d1) ≤ z2

√d2 log d2 holds. 

• The input to base-case is a valid instance of π-Request and d ≤ 27απ +2 .Proof. In the following, we show that if the invariant holds in a recursive call, then this recursive call will only invoke recursive calls for which the invariant also holds. Note that the initial call of solve-π-r fulfills the requirement that the input is a valid instance of π-Request by defini-tion. Since base-case does not call any other algorithm, we only need to show that solve-π-r 

and red-portals maintain the invariants. 

Main Algorithm ( solve-π-r ). We start with recursive calls made in solve-π-r . To this end, assume that we are in a recursive call of solve-π-r with input ( d, G, x, B he , B li , B di , S π), where x ≤ 200 log 7(d) − 22 log( d) and ( d, G, (Bhe , B li , B di ), x, S π) corresponds to a valid instance of 

π-Request . Note that base-case is only called in Line 1 and only if the check in Line 1 confirms 18 Algorithm 3: base-case 

Input: (d, G = ( V, E ), x, B he , B li , B di , S π) as specified in Lemma 8.  

> 1

Use Theorem 2 to compute a tree decomposition of width at most 6 απd − 1 

> 2

Use Lemma 5 to modify the tree decomposition into a binary tree decomposition T of width w ≤ 18 απd − 1 and depth O(log( n))  

> 3

D ← 4d2

/* Build relevant instances and brute force solution for pairs of bags */  

> 4

foreach node u except the root of the tree decomposition do  

> 5

Let u′ be the parent of u in T 

> 6

Zu ← Xu \ (Bli ∪ Bdi ) 

> 7

X′ 

> u

← Xu ∪ Xu′ 

> 8

Z′ 

> u

← X′ 

> u

\ (Bli ∪ Bdi ) 

> 9

Iu ← (D, G [Yu], (Zu, B li ∩ Xu, B di ∩ Xu), ∞, S π[G[Yu]])  

> 10

I′ 

> u

← (D, G [X′

> u

], (Z′

> u

, B li ∩ X′

> u

, B di ∩ X′

> u

), ∞, S π[G[X′

> u

]])  

> 11

res ′ 

> u

← brute force solution for I′

> u

/* Bottom-Up dynamic programming */  

> 12

foreach node u of the tree decomposition with at least one child from bottom to top do  

> 13

Let v1 be the first child of u in T 

> 14

I1 

> u

← (D, G 1 = G[Xu ∪ Yv1 ], (Bhe [G1] ∪ Zv1 , B li [G1], B di [G1]) , ∞, S π[G1])  

> 15

Use Requirement R4 with separator ( Zv1 , B li ∩ Xv1 , B di ∩ Xv1 ) and solutions res ′ 

> v1

(for instance I′ 

> v1

) and res v1 (for instance Iv1 ) to get solution res 1 

> u

for instance I1 

> u
> 16

if u has only one child then  

> 17

res u ← res 1 

> u
> 18

else  

> 19

Let v2 be the second child of u in T 

> 20

Ju ← (D, G ′ = G[Xu ∪ Yv2 ], (Bhe [G′] ∪ Zv2 , B li [G′], B di [G′]) , ∞, S π[G′])  

> 21

I2 

> u

← (D, G 2 = G[Yu], (Bhe [G2] ∪ Zv2 , B li [G2], B di [G2]) , ∞, S π[G2])  

> 22

Use Requirement R4 with separator ( Zv2 , B li ∩ Xv2 , B di ∩ Xvw ) and solutions res ′

> v2

(for instance I′ 

> v2

) and res v2 (for instance Iv2 ) to get solution su for instance Ju 

> 23

Use Requirement R4 with separator ( Zu, B li ∩ Xu, B di ∩ Xu) and solutions res 1 

> u

(for instance I1

> u

) and su (for instance Ju) to get solution res 2 

> u

for instance I2 

> u
> 24

res u ← res 2

> u

/* Removing Zp for root p from portal set */  

> 25

res ′ 

> r

← brute force solution for instance I′ 

> z

= ( D, G [Xp], (Xp \ (Bli ∪ Bdi ), B li ∩ Xp, B di ∩ Xp), ∞, S π[G[Xp]])  

> 26

Use Requirement R4 with separator ( Xp \ (Bli ∪ Bdi ), ∅, ∅) and solutions res ′ 

> p

for instance I′ 

> p

and solution res p for ( D, G, (Bhe ∪ (Xp \ (Bli ∪ Bdi )) , B li , B di ), ∞, S π) to compute solution res ∗ for ( D, G, (Bhe , B li , B di ), ∞, S π)

/* Discarding solutions for too large pebble sets */  

> 27

foreach pebble set M for (B, d, α π, x ) do  

> 28

Compute R = Rπ(d, G, B, M, S π) using Requirement R2  

> 29

foreach r ∈ R do  

> 30

res( r) ← res ∗(r) 

> 31

return res 19 that G has at most d ≤ 27απ . It is called with the same input ( d, G, x, B he , B li , B di , S π), which is a valid instance of π-Request by assumption. Hence, all requirements are met in this case. We next analyze the case where solve-π-r calls red-portals . Note that the only recurs-ive call to red-portals is in Line 1 with input ( d, G, x, 0, B he , ∅, B li , ∅, B di , ∅, S π). In this case, all invariants of red-portals except d ≥ 27απ and |Bhe ∪ Bli | ≤ (3/4)0 · 50 d6 hold by the as-sumption that ( d, G, (Bhe , B li , B di ), x, S π) is a valid instance of π-Request . Note that since we passed Line 1, we may assume that d ≥ 27απ . Since ( B, x ) is almost measured for k and απ,we have that x ≤ 200 log 7(d) and therefore |Bhe | ≤ 200 log 7(d) · 40 απ

√d = 8000 απ

√d log 7(d)and |Bli | ≤ 200 log 7(d) · 60 απd log( d) = 12000 απd log 8 d. These are at most 20000 απd log 7 d ver-tices combined, which is upper bounded by 31 d6 by Lemma 7 as d > 27απ ≥ 100. The third invariant also clearly holds here as z1 = x < x + 1 = x2 and d1 = d = d2.Let next I′ = ( d′, G ′, x ′, B ′

> he

, B ′

> li

, B ′

> di

, S ′

> π

) be the input to a recursive call of solve-π-r in Line 1 or 12. Recall that I′ is a valid instance of π-Request if 1. G′ is a απd′-outerplanar graph, 2. 1 ≤ x′ ≤ max(200 log 7(d′) − 44 log( d), 1), 3. |B′

> he

| ≤ 40 xα π

√d, and 4. |B′

> li

| ≤ 60 xα πd log( d). Since d′ = d and G′ is a subgraph of G, the first point trivially holds. Note that since we reached past Line 1, it holds that 1 ≤ x′ = x + 1 ≤ 40 log 7(d) + 1 ≤ 200 log 7(d) − 22 log( d). As we only potentially removed vertices from Bhe , the third point also holds trivially. Finally, since we only added 2 απd + 3 vertices to Bli , we have that |B′

> li

| = |Bli ∪ Che | ≤ | Bli | + |Cli | ≤ 40 xα πd log( d) + 2απd + 3 ≤ 40( x + 1) απd log( d), which shows the fourth condition. The third invariant again trivially holds as z1 < z 2 and d1 = d2.Let now I′ = ( d′, G ′, x ′, B ′

> he

, B ′

> li

, B ′

> di

, S ′

> π

) be the input to a recursive call in Line 1 or 24. Note that the application of Lemma 1 in Line 1 ensures that C′ 

> in

−Ain and C′ 

> ex

−Aex are d′απ-outerplanar graphs. Moreover, since we passed Line 1, we have x ≤ 40 log 7(d) implying 1 ≤ x′ = 2 x + 2 ≤ 2 · 40 log 7(d) + 2 

≤ 80 log 7(( 7/4)d′) + 2 = 80(log( d′) + log( 7/4)) 7 + 2 

< 80(log( d′) + 1) 7 + 2 = 80(log 7(d′) + 7 log 6(d′) + 21 log 5(d′) + 35 log 4(d′) + 35 log 3(d′) + 21 log 2(d′) + 7 log( d′) + 1) + 2 

≤ 80 log 7(d′)(1 + 7/7 + 21 /72 + 35 /73 + 35 /74 + 21 /75 + 7/76 + 3/77)

≤ 80 · 2.45 log 7(d′) ≤ 200 log 7(d′) − 44 log( d′),

where the third and second from last inequality hold as log( d) ≥ 7 as d ≥ 27απ ≥ 27. Moreover, Lemmas 6 and 7 yield 

|B′

> he

| ≤ | Bhe ∪ C′

> he

| ≤ 40 απx√d + 40 pαπd ≤ 40( x + 1) απ

√d

≤ 40(2 x + 2) απ

r d

4 ≤ 40 x′απ

r 4d

7

≤ 40 x′απ

√d′

We will next show that 7απ 

> 4

≤ ( 4d 

> 7

) 17 or equivalently d ≥ α7

> π

( 74 )6. Note that when απ = 1, then d ≥ 27 ≥ 32 ≥ 746 = 746α7

> π

. For all positive integers απ ≥ 2, it holds that απ ≤ 2απ −1.20 Hence, α7

> π

( 74 )6 ≤ (2 απ −1)7 · 32 = 2 7απ −7+5 ≤ d. Combined with Lemmas 6 and 7, this implies that 

|B′

> li

| ≤ | Bli ∪ C′

> li

| ≤ 60 απxd log( d) + 60 απd log( απd)

≤ 60( x + 1) απd log( απd)

≤ 30(2 x + 2) 74 απ

4d

7 log( 7απ

44d

7 )

≤ 30(2 x + 2) 74 απ

4d

7 (log( 4d

7 )8/7)

≤ 30 x′ 7 · 84 · 7 d′ log( d′)

≤ 60 x′d′ log( d′).

To show that the third invariant is also maintained in this case, note that 4d 

> 7

≥ d3/4 for all d > 10. Hence, we have 

x′√d′ log( d′) ≥ (2 x + 2) 

r 4d

7 log( 4d

7 )

≥ 2x

r 47

√d log( d3/4)

≥ x√d log( d)(2 

r 4734 )

≥ x√d log( d).

Reduce Portals ( red-portals ). We now consider a recursive call of red-portals with in-put ( d, G, x, y, X he , Y he , X li , Y li , X di , Y di , S π) which satisfies our invariant and show that the input to each call of solve-π-r and red-portals also satisfies the respective invariants. Observe that 

solve-π-r is only called in Line 2 and with input ( d, G, y +1 , X he ∪Yhe , X li ∪Yli , X di ∪Ydi , S π), which is by assumption a valid instance of π-Request since x ≥ 1 and |Xhe ∪ Xli | ≤ 20 √d. Moreover, the result is never used to combine solutions using Requirement R4, so the third invariant is not applicable here. We now consider the input ( d′, G ′, x ′, y ′, X ′

> he

, Y ′

> he

, X ′

> li

, Y ′

> li

, X ′

> di

, Y ′

> di

, S ′

> π

) given to red-portals in Line 2. We need to show that 1. G′ is a απd′-outerplanar graph, 2. 1 ≤ x′ ≤ 200 log 7(d′) − 22 log( d′), 3. y′ < 22 log( d′), 4. |X′

> he

| ≤ 40 x′απ

√d′,5. |Y ′

> he

| ≤ 40(2 y′)απ

√d′,6. |X′

> li

| ≤ 60 x′απd′ log( d′), 7. |Y ′

> li

| ≤ 60(2 y′)απd′ log( d′), 8. d′ ≥ 27απ ,9. |X′ 

> he

∪ Xli | ≤ (3/4)y′

· 50 d′6, and 10. ( x + 2 y)√d log( d) ≤ (x′ + 2 y′)√d′ log( d′). Note that points 1 , 2, 4, 6, 8, and 10 hold as d′ = d, x ′ = x, y ′ = y + 1 , X ′ 

> he

⊆ Xhe , X ′ 

> li

⊆ Xli ,and G′ is a subgraph of G. For point 9, note that |X′ 

> he

∪ X′

> li

| ≤ 3/4|Xhe ∪ Xli | as C′ is 3/4-balanced with respect to the weight function that assign weight 1/|Xhe ∪Xli | to each vertex in Xhe ∪ Xli and zero to all other vertices. Hence, 

|X′ 

> he

∪ X′

> li

| ≤ (3/4)|Xhe ∪ Xli | ≤ 3/4 · (3/4)y · 50 d6 = ( 3/4)y+1 · 50 d6 = ( 3/4)y′

· 50 d′6.

21 For the third point, notice that since |Xhe ∪ Xli | ≤ (3/4)y · 50 d6 by assumption and |Xhe ∪ Xli | ≥ 1 as we passed Line 2, it holds that ( 3/4)y · 50 d6 ≥ 1, which implies y ≤ − log(50 d6)log( 3/4) ≤ − log( d7) 

> −1/3

≤ 21 log( d). Thus, it holds that y′ = y + 1 < 22 log( d). Using Lemma 6, we next get that 

|Y ′

> he

| ≤ | Yhe ∪ C′

> he

|≤ 40(2 y)απ

√d + 40 pαπd

≤ 40(2 y + 1) απ

√d < 40(2 y′)απ

√d and 

|Y ′

> li

| ≤ | Yli ∪ C′

> li

| ≤ 60(2 y)απd log( d) + 60 απd log( απd)

≤ 60(2 y)απd log( d) + 60 απd log( d2)

≤ 60(2 y + 2) απd log( d)

≤ 60(2 y′)απd log( d).

This concludes the proof. 

3.2 Base Case 

Here, we prove that base-case works as intended. Before doing so, let us give an intuitive descrip-tion of what the algorithm does. As stated in Lemma 8, the algorithm is only called when d is constant (because απ is a constant). Recall that for a node u of the tree decomposition, Xu denotes the vertices in the bag of u and Yu denotes the union of Xv for all descendants of v (including u). We use standard dynamic programming ideas on the tree decomposition to solve request for pebble sets of any size where we consider all vertices in the bag of the current node as heavy vertices (in addition to the input portal set B); this can be viewed as setting x = ∞ = d. In order to improve the polynomial part of the running time, we will consider balanced binary tree decompositions, that is, binary tree decompositions of depth O(log n). For the dynamic program over the balanced binary tree decomposition, we first solve certain small instances using brute force and then use Requirement R4 to combine solutions. The small instances are those induced by vertices in Xu ∪ Xu′ for any node u (except the root) in the tree decomposition and where u′ is the parent of u. Since each bag has constant size and we assume that π-Request is computable, this results in a constant-time algorithm for each edge in the tree decomposition. We then combine solutions as follows. For each node u and each child vi of u,we combine the solutions for G[Yvi ] and G[Xu ∪ Xvi ] using Xvi as a separator consisting of heavy vertices using Requirement R4. We call these instances I1 

> u

(for v1) and Ju (for v2). Therein, we keep the vertices in Xu as heavy portal vertices. If u has two children, then we combine the solutions for both instances to get a solution for G[Yu]. After this is done for all nodes, we are left with two minor inconveniences. On the one hand, the vertices in Xp (p is the root of the tree decomposition) are currently treated as heavy portal vertices. However, using Requirement R4, we can view Xp \ B

as a separator for the separation ( G, X p \ B), that is, no vertices are separated from ( V \ Xp) ∪ B.Applying Requirement R4 then allows us to drop the vertices in Xp \ B from the portal set. A key step in the proof will be to show that (( Xu ∪ Xu′ , ∅, ∅), 1) is measured for 4 d2 and απ and hence Requirement R4 can be applied. On the other hand, we now have a lookup table storing too much 

information. We did not yet consider the size of possible pebble sets as we imagined x = ∞ = d.However, this is trivial to fix. We simple iterate over each pebble set for ( B, d, α, x ), compute all requests for it and copy the solution for it. We will show that all of the above can be done in O(nρ log( n)) time assuming d is a constant. 22 Lemma 9. Let (d, G = ( V, E ), x, B he , B li , B di , S π) be the input given to base-case satisfying the requirements of Lemma 8 and let π-Request satisfy all requirements of Theorem 3. Then, 

base-case solves the instance (d, G, (Bhe , B li , B di ), x, S π) of π-Request in O(nρ log( n)) time. Proof. We will use the same notation as in Algorithm 3. We first prove that for each separ-ator C = ( Che , C li , C di ) chosen by base-case , it holds that ( C, 1) is almost measured with respect to D = (2 απd)2 and απ ≥ 1. Note that the largest separator consists of the vertices of two bags in the binary tree decomposition T . We next show that each bag contains at most 18 απd vertices and hence, the largest separator contains 36 απd vertices. By Lemma 8, the graph G is απd-outerplanar. Lemma 4 then states that it has treewidth at most 3 απd−1. Theorem 2 then allows us to compute a tree decomposition of width at most 2(3 απd − 1) + 1 = 6 απd − 1 and Lemma 5 allows us to compute a binary tree decomposition of width at most 3(6 απd − 1) + 2 = 18 απd − 1 and depth O(n). Hence, each bag contains at most 18 απd vertices. Recall that ( C, 1) is almost measured for D and απ

if 1 ≤ max(1 , 200 log 7(d)), |Che | ≤ 40 απ

√D, and |Cli |60 απ

√D. The first point holds trivially and since |Che ∪ Cli ∪ Cdi | ≤ 36 απd < 40 απ

√D = 80 απd, the other two requirements are also met and ( C, 1) is measured with respect to D and απ and Requirement R4 can be applied in all cases. Requirement R4 then guarantees that all solutions for pebble sets that do not intersect Cdi 

and that intersect Cli in at most 20 απ

√D log( D) vertices are computed correctly. We next show that these are all subsets of Che ∪ Cli . To this end, we show |Che ∪ Cli | ≤ 20 απ

√D log( D). Since |Che ∪ Cli | ≤ | Che ∪ Cli ∪ Cdi |, we can reuse the above argument to conclude the following: 

|Che ∪ Cli | < 36 απd < 40 απd = 20 απ

√D < 20 απ

√D log( D). Hence, in each case where we apply Requirement R4, it correctly returns solutions for all subsets of heavy and light vertices of the separator independent of their size. We next show that base-case correctly computes the result for the instance of π-Request it is given as input and afterwards analyze the running time. The correctness of the brute-force step in Line 3 follows from the fact that π-Request is computable. Since the correctness of each single combination of results follows from Requirement R4, it only remains to show that Requirement R4 is applied in each step so that it gives the result for the instance it is supposed to solve. To this end, we make a case distinction whether a node u has 0, 1, or two children (it cannot have more as T is binary). If it has 0 children, then we do not apply Requirement R4, so nothing needs to be shown. If u has exactly one child v1, then we already computed the solutions for instances Iv1

and I′ 

> v1

. Note that Iv1 is the instance for G[Yv1 ] and I′ 

> v1

is the instance for G[Xv1 ∪ Xu]. Since these two are separated by Xv1 since T is a tree decomposition, we correctly solve the instance for G[Yv1 ∪Xu] = G[Yu] as u only has v1 as a child, which is precisely Iu = I1

> u

. If u has two children v1

and v2, then the same argument as above shows that we correctly compute solutions for instances I1

> u

(for G[Yv1 ∪ Xu]) and Ju (for G[Yv2 ∪ Xu]). As these two are separated by Xu, we correctly compute the solution for the instance I2 

> u

= Iu for G[Yv1 ∪ Xu ∪ Yv2 ] = G[Yu] in Line 3. In Line 3, we only remove some heavy vertices from the portal set by using a trivial separator ( Xu \ (Bli ∪ Bdi ), ∅, ∅). Finally, we output all solutions for pebble sets for ( B, d, α π, x ). As argued above, all of these were already correctly computed before, so the output of base-case is correct. It remains to analyze the running time. We will do so in three steps. First, we will analyze the running time outside the for-loops in Line 3. Second, we will show that the time spend for each node u in the tree decomposition T computed in Line 3 is in O(|Yu|ρ). Third, we will argue that summed over all nodes in T , this is still contained in O(nρ log( n)). For the first step, note that Lemma 8 states that d is a constant (as απ is a constant). This implies that D is a constant and by the definition of request variants, x is at most 200 log 7(d) and is therefore also a constant. Finally, this yields that |Bhe | and |Bli | are constant. Hence, the number of iterations in Line 3 is a constant. It is now easy to verify that each single line outside the for-loops in Line 3 take at 23 most O(γ(D)nρ) = O(nρ) time. For the second step, note that in an iteration of the two for-loops for a node u in T , each single line takes at most O(γ(D)|Yu|ρ) time as subgraphs can be computed in linear time and updating Sπ

and computing Rπ take O(γ(D)|Yu|ρ) time by requirements R2 and R3. The use of Requirement R4 also takes O(γ(D)|Yu|ρ) time and brute-forcing the solutions in Line 3 takes constant time as each bag contains a constant number of vertices. It remains to analyze the running time for nodes in T . To this end, we consider all nodes at a given depth t together. Let Ut be the set of all these nodes and note that we can assume without loss of generality that the number of nodes in T is at most n and thus |Ut| ≤ n. Note that since T is a tree decomposition, it holds that P 

> u∈Ut

|Yu| ≤ n + P 

> u∈Ut

|Xu| ≤ n(1 + 18 απd) ∈ O (n). Hence, the running time for all nodes at depth t combined is in O(nρ) and summed over the O(log( n)) different depths, this yields the final running time of O(nρ log( n)). 

3.3 Reduce Portals 

In this section, we show that red-portals works as intended, that it, it reduces an instance of π-Request with x ≥ 40 log 7(d) to 2 O(log 3(d)) instances of π-Request where x ≤ 22 log( d) holds in each instance. We start with a description of the main idea of the algorithm. We are given a partition of the portal sets ( Bhe , B li , B di ) into sets ( Xhe , X li , X di ) and ( Yhe , Y li , Y di ). Initially Xi = Bi

and Yi = ∅ for each i ∈ { he , li , di } and the goal is to call solve-π-r with a small set Bhe ∪ Bli .We do this by iteratively finding a 3/4-balanced cycle separator for the weight function that assigns weight 1/|Xhe ∪Xli | to each vertex in X = Xhe ∪ Xli and zero to all other vertices. For this cycle separator, we apply Lemma 6 to find a set of cycle separators that are balanced for the mentioned weight function. We then guess the cycle separator C′ that does not intersect the solution in too many vertices, add its heavy vertices C′ 

> he

to Yhe , its light vertices C′ 

> li

to Yli , and its discarded vertices C′ 

> di

to Ydi and call red-portals on the interior and the exterior. Clearly, the set X shrinks by a factor of 3/4 in each iteration. Hence, after calling red-portals for O(log( |X|)) times, the set X will have constant size. We also show that the set Y does not become too large in the process. Hence, we can then call solve-π-r with Bi = Xi ∪ Yi for each i ∈ { he , li , di } (modeled as an oracle in the following lemma). 

Lemma 10. Let (d, G = ( V, E ), x, y, X he , Y he , X li , Y li , X di , Y di , S π) be given to red-portals as in-put, and let it satisfy the requirements of Lemma 8. Let also π-Request satisfy the requirements of Theorem 3. Given access to an oracle for instances (d, G ′ = ( V ′, E ′), B ′, x ′, S ′

> π

) with |V ′| < |V |

and x′ ≤ 44 log( d), red-portals solves the instance (d, G, (Xhe ∪ Yhe , X li ∪ Yli , X di ∪ Ydi ), x + 2 y, S π)

of π-Request with at most 22000 log 3(d) oracle calls in 2 ˜O(√d)γ(d)nmax( ρ, 2) time. Proof. We will prove the statement by induction over z = 22 log( d) − y. If z = 1, then we have y = 22 log( d) − 1 and Lemma 8 yields 2 y + 1 ≤ 44 log( d) and 

|Xhe ∪ Xli | ≤ (3/4)22 log( d)−1 · 50 d6 ≤ 4/3 · 222 log( 3/4) log( d)+7 log( d) ≤ 4/3 · 2−2 log( d) ≤ 1and Line 2 is therefore reached. Note that if Line 2 is reached, then the oracle calls give the correct solution for the subinstances by assumption as 2 y + 1 ≤ 44 log( d). If Line 2 is not reached, then we let S be the set of cycle separators computed in Line 2. We call red-portals for each separator in S and in each call, the value of y is increased by 1, that is, the value of z is reduced by one. Hence, the induction hypothesis states that the recursive calls in Line 2 are computed correctly. For each cycle separator C ∈ S , it holds by Lemma 6 that ( C, 1) is measured for d and 1 and therefore also for d and απ. Thus, we can apply Requirement R4 to guarantee that we return the correct solution. This concludes the proof of correctness. 24 We next analyze the running time and the number of oracle calls. Consider any recursive call of red-portals . By Lemma 6, this calls red-portals at most 2 · 222 log 2(2 απ d+3) ≤ 288 log 2(d)

times and in each such call, the value of y is increased by one. Hence, the number of recursive calls of red-portals corresponds to the number of vertices in a tree of depth 22 log( d) − 1 and degree 2 88 log 2(d). Hence, the number of oracle calls (calls of solve-π-r and the number of leaves in the tree) is at most (2 88 log 2(d))(22 log( d)−1+1) ≤ 288 log 2(d)·22 log( d) ≤ 22000 log 3(d)

and the number of recursive calls of red-portals (the number of internal vertices in the tree) is at most 2 2000 log 3(d) − 1. It remains to analyze the running time of each recursive call. Note that each single line except for Line 2 can be executed in O(n2) time. Line 2 takes 2 O(log 2(απ d)) n1+ o(1) ⊆ 2O(log 2(d)) n2 time by Lemma 6. Since |V ′| ≤ n and d′ = d

for each recursive call, Requirements R2, R3, and R4 state that Line 2 each take O(γ(d)nρ) time. Since the number of separators in S is at most 2 44 log 2(2 dα π +3) ∈ 2 ˜O(1) and the number of possible pebble sets for (( Xhe ∪ Yhe , X he ∪ Yhe , X he ∪ Yhe ), x + 2 y) is by Lemma 8 at most 240(200 log 7(d)) απ 

> √d

·

 60(200 log 7(d)) απd log( d)20(200 log 7(d)) απ

√d log( d)



∈ 2 ˜O(√d)

(since |Xhe ∪ Yhe | ≤ 40(200 log 7(d)) απ

√d and |Xli ∪ Yli | ≤ 60(200 log 7(d)) απd log( d)) ,

we have that the running time for one recursive call is in 2 ˜O(√d)γ(d)nmax(2 ,ρ ).

3.4 Main Algorithm 

We are finally in a position to prove our main theorem by showing that solve-π-r solves π-Request . We start with a description of the algorithm and then proceed with the formal proof. Following the intuition presented in the introduction, solve-π-r works by first checking whether we can solve the base case. If |V | ≤ 27απ , then it can afford to brute-force the solution and if d ≤ 27απ , then it calls base-case . It then checks whether ( B, x ) is measured for d and απ, that is, whether the size of B and the value of x are small enough to proceed or whether red-portals has to be called. If ( B, x ) is measured, then solve-π-r finds a cycle separator C and guesses whether C

contains at most 20 √d log( d) vertices of the (unknown) solution Z or more. In the first case, it solves the corresponding instances in the interior and exterior of C and combines the results (Requirement R4 ensures that this can be done). Otherwise, it uses the fact that C contains many vertices of the solution to reduce the value of d as follows. Since C contains at least 20 √d log( d) vertices of Z, both the strict interior and the strict exterior of C contain at most |Z| − 20 √d log( d) vertices. Hence C is |Z|− 20 √d log( d) 

> |Z|

-balanced for the weight function that assigns weight 1/|Z| to each vertex in Z and zero to all other vertices. Using Lemma 6, the algorithm computes a set of cycle separators where one of those is also |Z|− 20 √d log( d) 

> |Z|

-balanced for the mentioned weight function and does not intersect the solution in more than 17 √d log( d) vertices. Thus, the algorithm applies Lemma 1 (which is permitted by Requirement R1) to compute a ⌊d − 3√d⌋-outerplanar subgraph G′ of both the interior and the exterior of each computed cycle separator, solves the problem recursively, and combines the solutions; again Requirement R4 allows us to combine solutions. Intuitively, we make progress in each recursive call as we either reduce the number of vertices by a constant factor or reduce the parameter d by roughly 3 √d. We will prove that this is sufficient for the claimed running time and now proceed with the formal proof of Theorem 3, which we restate here for convenience. 25 Theorem 3. Let π-Request be a computable request variant, ρ ≥ 1 be a constant, and γ be a non-decreasing computable function such that R1 π-Request has constant locality απ, that is, for any instance I = ( d, G, B, x, S π) of π-Request , any request r ∈ Rπ(d, G, B, M, S π), and any BFS layering V1, V 2, V 3, . . . , V i of G

with i > α πd, the following holds. Let V j be the union of all sets Vℓ with ℓ ≡απ d+1 j for each j ∈ [απd + 1] and let I′ 

> j

= ( d, G ′ = G[V \ V j ], B [G′], x, S π[G′]) be the instance where all vertices from Vj are removed. Then, the output for instance I and request r is equal to either the maximum or the minimum of the outputs for all I′ 

> j

and r containing all vertices in M.R2 Rπ(d, G, B, M, S π) can be computed in O(γ(d)nρ) time. R3 Given a separator C, Sπ[Cin ] and Sπ[Cex ] can be computed in O(γ(d)nρ) time. R4 There is an algorithm that given a separator C = ( Che , C li , C di ) such that (C, 1) is measured for d and απ, a pebble set M for (B, d, α π, x ), and the solutions for instances 

(d, C in , (( Bhe ∪ Che )[ Cin ], (Bli ∪ Cli )[ Cin ], (Bdi ∪ Cdi )[ Cin ]) , x + 1 , S π[Cin ]) and 

(d, C ex , (( Bhe ∪ Che )[ Cex ], (Bli ∪ Cli )[ Cex ], (Bdi ∪ Cdi )[ Cex ]) , x + 1 , S π[Cex ]) ,

can compute the solutions for instance I for all requests r ∈ Rπ(d, G, B, M, S π) in O(γ(d)nρ)

time that do not intersect Cdi and intersect Cli in at most 20 απ

√d log( d) vertices. Then, π-Request can be solved in 2 ˜O(√d)γ(d)nmax(2 .49 ,ρ +ε) time for any ε > 0.Proof. We first show that solve-π-r solves the instance ( d, G, x, B = ( Bhe , B li , B di ), S π) of π-Request it is given as input. We will later show that solve-π-r terminates after a finite number of recursive calls. We can therefore show the claim by induction and assume that all recursive subcalls of solve-π-r indeed solve the corresponding instance of π-Request correctly. For the sake of notational convenience, we will assume that ρ ≥ 2. If Line 1 is reached, then the computation is correct as we assume π-Request to be computable. If Line 1 is reached, then the computation is correct by Lemma 9. If Line 1 is reached, then Lemma 10 shows that the solution is correctly computed. So assume that Line 1 is reached and let C be the cycle separator computed in Line 1 and let S be the set of cycle separators computed in Line 1. Let M be a pebble set for ( B, x ) and let r ∈ Rπ(d, G, B, M, S π) be a request. If the (unknown) solution Z that determines the optimal value for request r intersects C in at most 20 √d log( d) vertices, then Requirement R4 guarantees that the solution is computed correctly in Line 1 as the solutions res 1 and res 2 are correct by the induction hypothesis. Moreover, as we only update res with valid solutions, the result is not overwritten in a later iteration of Line 1. If Z and C intersect in more than 20 √d log( d) vertices, then both the strict interior and the strict exterior of C contain at most d−20 √d log( d) vertices of Z,that is, C is |Z|− 20 √d log( d) 

> |Z|

-balanced with respect to the weight function that assigns weight 1/|Z| to each vertex in Z and zero to all other vertices. By Lemma 6, there exists a |Z|− 20 √d log( d) 

> |Z|

-balanced cycle separator C′ ∈ S (computed in Line 1) whose intersection with Z is of size at most 40 p2απd + 3 + 11 |Z| log( απd)2√2απd + 3 ≤ 40 

r 5dα π

2 + 11 d log( d2)2√2d

≤ 40 

r 5d log( d)14 + 11 d log( d)

√2d

26 ≤ (40 

s

514 log( d) + 11 

√2 )√d log( d)

≤ ( 40 7

r 52 + 11 

√2 ) ≤ 17 √d log( d).

Here, the second inequality is due to the fact that απ ≤ log( d)7 as d ≥ 27απ and the second to last inequality is due to the fact that log( d) ≥ 7 as d > 27. The (non-strict) interior and exterior of C′

hence contain at most 

⌊| Z| − 20 √d log( d) + 17 √d log( d)⌋ ≤ ⌊ d − 3√d log( d)⌋ ≤ ⌊ d − 3√d⌋ vertices from Z.Due to the induction hypothesis, the solutions res 1 and res 2 in Line 1 are correct. It only remains to show that the algorithm behind Requirement R4 can combine them. This is slightly non-trivial in this case as the value for d is reduced in the recursive calls, so we need to argue that all pebble sets for ( B, d, α π, x ) are also pebble sets for ( B, d ′ = ⌊d − 3√d⌋, α π, x ′ = 2 x + 1), that is, the algorithm can access all required results. By definition, each pebble set for ( B, d, α π, x ) contains at most 20 xα π

√d log( d) vertices in Bli . By Lemma 8, we have that x√d log( d) ≤ x′√d′ log( d′) and each such pebble set therefore contains at most 20 x′απ

√d′ log( d′) vertices from Bli . Thus, each such pebble set is also a pebble set for ( B, d ′, α π, x ′) and Requirement R4 applies. This concludes the proof of correctness. It remains to analyze the running time of solve-π-r . To this end, let ε > 0 be any constant. We start with the running time of one recursive call. In the following, we will again use the fact that απ ≤ log( d)7 . First, note that the number of possible pebble sets is upper bounded by 2|Bhe | ·

 |Bli |

20 xα π

√d log( d)



≤ 240(200 log 7(d)) απ 

> √d

· 2(20(200 log 7(d)απ   

> √dlog( d)) ·log(60(200 log 7(d)) απdlog( d))

≤ 2 8000 7   

> √dlog 8(d)+ 3200 7
> √dlog 8(d) log( d8)

≤ 2( 8000 7 log 2(d) + 8·3200 7 )√d log 9(d)

≤ 23681 √d log 6(d).

The number of separators in S is upper-bounded by 2 44 log 2(2 απ d+3) ≤ 244 log 2(d1.5) = 2 99 log 2(d) by Lemma 6. This bounds the number of iterations of the for-loops in one recursive call. If Line 1 is reached, then the number of operations is constant. By Lemmas 1 and 6, Corollary 1, and Requirements R2, R3, and R4, all lines can be computed in O(γ(d)nmax(2 ,ρ ) log( n)) ⊂ O (γ(d)nρ+ε)time each. Thus, the running time of a single recursive call is in 

O(2 3681 √d log 7(d)+99 log 2(d)γ(d)nρ+ε) ⊆ O (2 3682 √d log 7(d)γ(d)nρ+ε).

We next analyze the running time of all calls of solve-π-r and red-portals combined. To this end, we define three functions ϕ, T , and ξ. The function Φ( d, x ) = min( x 

> 40 log 7(d)

, 1) is a potential function measuring how close a recursive call is to calling red-portals .The function T [n, D, ϕ ] upper-bounds the number of operations performed by the current re-cursive call and all future recursive calls of solve-π-r and red-portals when the current re-cursive call solves an instance with ν vertices, d = D, and Φ evaluating to ϕ. Let κ be a constant such that each recursive call of solve-π-r , red-portals and base-case performs at 27 most κ23682 √D log 7(D)γ(D)νρ+ε operations and let λ = 2 2000 log 3(D)κ. We will show that 

T [ν, D, ϕ ] ≤



2κ23682 √D log 7(D)γ(D)νρ+ε if Line 1 is reached 22000 log 3(D)T [ν, D, 1/log 6(D)] + λ23682 √D log 7(D)γ(D)νρ+ε if Line 1 is reached 2T [ 34 ν, D, ϕ + 140 log 7(D) ] + 2 99 log 2(D)T [ν, ⌊D − 3√D⌋, 1] +κ23682 √D log 7(D)γ(D)νρ+ε else. Finally, ξ(ν, D, ϕ ) = λ24000 log 7(D)( √D+ 21 ϕ 

> 40 log 4(D)))

γ(D)νδ, where δ = max( ρ + ε, 2.49), will be an upper bound for T in closed form. Let us start by showing that T is indeed an upper bound on the number of operations performed by all future recursive calls (including the current one) combined. The number of operations per-formed by the current recursive call was analyzed above. If Line 1 is reached, then only one further call to base-case is made. If Line 1 is reached, then red-portals is called and the proof of Lemma 10 yields that this leads to at most 2 2000 log 3(D) − 1 calls of red-portals , these calls combined call solve-π-r at most 2 2000 log 3(D) times, and in each of these calls, the value of x is at most 44 log( D) by Lemma 8. This implies that ϕ is at most 44 40 log 7(D) ≤ 1log 6(D) . Moreover, the num-ber of operations performed by the current recursive call of solve-π-r and the at most 2 2000 log 3(D)

recursive calls of red-portals perform at most 22000 log 3(D)κ23682 √D log 7(D)γ(D)νρ+ε ≤ λ23682 √D log 7(D)γ(D)νδ.

This shows that T is indeed an upper bound in the second case. If neither of Line 1 are reached, then we call solve-π-r once each in Line 1 with the same value of D, the value of ν shrunk by a factor of 3/4, and x increased by one. Hence, the value of ϕ increases by at most 140 log 7(D) .Moreover, we call solve-π-r in Line 1 once for each cycle in S. As analyzed above, there are at most 2 99 log 2(D) cycles in S and in each of these recursive calls, the number ν of vertices does not increase, the value of D is decreased by ⌈3√D⌉, and in the worst case the value of ϕ increases to 1. The number of operation in the current recursive call is again at most κ23682 √D log 7(D)γ(D)νρ+ε

and hence T is an upper bound as claimed in all cases. To conclude the proof, we show that T ≤ ξ. We prove this by induction and therefore as-sume that T ≤ ξ in each recursive call. Note that if Line 1 is reached, then only one recurs-ive call is made to base-case which does not incur any other recursive calls. Hence, it holds trivially that T [ν, D, ϕ ] = 2 κ23682 √D log 7(D)γ(D)νρ+ε ≤ λ24000 √D log 7(D)γ(D)nδ ≤ ξ(ν, D, ϕ ) (note that 21Φ 40 log 4(D) might be very small but it is non-negative). If Line 1 is reached, then we have that T [ν, D, ϕ ] = 2 2000 log 3(D)T [ν, D, 1/log 6(D)] and ϕ = 1. The induction hypothesis then yields 

T [ν, D, ϕ ] = 2 2000 log 3(D)T [ν, D, 1/log 6(D)] + λ23682 √D log 7(D)γ(D)νρ+ε

≤ 22000 log 3(D) · λ24000 log 7(D)( √D+ 12 log 10 (D) )γ(D)νδ + λ23682 √D log 7(D)γ(D)νδ

≤ λγ (D)νδ(2 4000 log 7(D)( √D+ 21 40 log 10 (D) + 2000 log 3(D)4000 log 7(D) ) + 2 3682 √D log 7(D))

≤ λ(2 · 24000 log 7(D)( √D+ 21 40 log 10 (D) + 20 40 log 4(D) ))γ(D)νδ

≤ λ24000 log 7(D)( √D+  

> 21 log 6(D) +20+ 1100 log 3(D)40 log 4(D))

γ(D)νδ

≤ λ24000 log 7(D)( √D+ 21 40 log 4(D) )γ(D)νδ = ξ(ν, D, 1) = ξ(ν, D, ϕ ).

28 Recall that log( D) ≥ 7 and Lemma 7 states that 

p

D − 3√D + 1 ≤ √D − 110 . This yields for the last case 

T [ν, D, ϕ ] ≤ 2T [ 34 ν, D, ϕ + 140 log 7(D) ] + 2 99 log 2(D)T [ν, ⌊D − 3√D⌋, 1] + λ23682 √D log 7(D)γ(D)νρ+ε

≤ 2 · λ24000 log 7(D)( √D+  

> 21( ϕ+140 log 7(D) )40 log 4(D))

γ(D)( 34 ν)δ

+ 2 99 log 2(D) · λ24000 log 7(D−3√D)( 

√D−3√D+ 21 40 log 4(D) )γ(D − 3√D)νδ

+ κ23682 √D log 7(D)γ(D)νδ

≤ 2 · ( 34 )δ · λ24000 log 7(D)( √D+  

> 21( ϕ+140 log 7(D) )40 log 4(D))

γ(D)νδ

+ 2 99 log 2(D) · λ24000 log 7(D)( 

√D−3√D+1) γ(D)νδ

+ κ23682 √D log 7(D)γ(D)νδ

≤ 2 · ( 34 )δ · λ24000 log 7(D)( √D+ 21 ϕ   

> 40 log 4(D)+21 1600 log 11 (D))

γ(D)νδ

+ 2 99 log 2(D) · λ24000 log 7(D)( √D− 110 )γ(D)νδ

+ κ23682 √D log 7(D)γ(D)νδ

≤ λ24000 log 7(D)( √D+ 21 ϕ 

> 40 log 4(D))

γ(D)νδ·



2 · (3/4)δ · 2 21 ·4000 1600 log 4(D) + 2 99 log 2(D)+4000 log 7(D)( − 110 ) + κλ



≤ λ24000 log 7(D)( √D+ 21 ϕ 

> 40 log 4(D))

γ(D)νδ ·



2 · (3/4)2.49 · 2 210 4·74 + 2 −300 log 7(D) + 2 −2000 log 3(D)

≤ λ24000 log 7(D)( √D+ 21 ϕ 

> 40 log 4(D))

γ(D)νδ = ξ(ν, D, ϕ ).

This concludes the proof. Note that Theorem 3 only solves the decision version whether a set of at most k vertices with a certain property exists. However, the above algorithm can also easily be modified to output a set of vertices of an optimal solution by simply storing one representative of each partial solution. 

# 4 Applications 

In this section, we apply Theorem 3 to different problems to achieve subexponential-in-k and subcubic-in-n time algorithms for planar input graphs. We start with k-Subgraph Scoring ,which acts both as an easy example of to how to use the framework and a convenient way to avoid using the framework. In particular, we will list the different steps required to apply the framework and then show how this is done in the case of k-Subgraph Scoring . We will then show that problems like Maximum-Weight Independent Set , Weighted Partial Vertex Cover , Densest k-Subgraph , and Maximum (k, n −k)-Cut are all special cases of k-Subgraph Scoring . Hence, in order to show subexponential parameterized algorithms for these (as well as weighted and directed variant of the last two), one does not need to apply the framework but can just show how to use an algorithm for k-Subgraph Scoring instead. Afterwards, we apply the framework to Maximum-Weight Induced Forest , Minimum-Weight Rooted Simple 

29 Minor , and Maximum-Weight Rooted Parallel Induced Minor . These examples will get progressively more technical and we recommend to read them in order. Our aim is to provide the necessary ingredients one after another so that each step on its own is as simple as possible to follow. Most of the aforementioned problems also have relevant special cases which we mentioned in the introduction. We will discuss those along the way. 

4.1 A Simple-to-use Fragment 

Here, we give a general recipe for how to apply Theorem 3. We then show how to use this recipe for showing that k-Subgraph Scoring can be solved in 2 ˜O(√k)n2.49 time. Finally, we show how to use this fact to show that problems like Maximum-Weight Independent Set , Weighted Partial Vertex Cover , Densest k-Subgraph , and Maximum (k, n − k)-Cut can be solved in the same time. We note that subexponential parameterized algorithms were already known for 

Maximum-Weight Independent Set [vN20] and Maximum (k, n −k)-Cut for undirected planar graphs [FGIK24], but was asked as a specific open question for Densest k-Subgraph [FGIK24] and was not known for Weighted Partial Vertex Cover or Maximum (k, n − k)-Cut for directed planar graphs. This result also generalizes in a straight-forward way to weighted variants of Maximum (k, n − k)-Cut and Densest k-Subgraph and we suspect that there are many more applications of k-Subgraph Scoring .We now present the recipe for any problem π under consideration. 1. Decide for an appropriate function γ and constants ρ, α π ≥ 1. 2. Define Sπ and Rπ and show that they can be computed/maintained in O(γ(k)nρ) time (Re-quirements R2 and R3 in Theorem 3). 3. Define an appropriate request version π-Request .4. Show that Requirement R1 of Theorem 3 holds, that is, there is an index i such that we can remove each layer i + j · (απk + 1) of a BFS layering for any j.5. Show that Requirement R4 of Theorem 3 is fulfilled, that is, given a separator C and solutions to subproblems on either side of the separator, we can compute in O(γ(k)nρ) time all partial solutions that do not intersect C too much. 6. Design an algorithm that given access to an oracle for π-Request solves π and analyze its running time and how many oracle calls it makes. One also has to show that π-Request is computable but since this is obvious in all examples studied in this paper, we will ignore this step. We next show how to use this recipe and Theorem 3 to show a subexponential parameterized algorithm for k-Subgraph Scoring . Before doing so, we prove a small lemma that will be useful for all applications of Theorem 3. 

Lemma 11. Let (C, 1) be a pair that is measured with respect to two integers d and α, where α

is a constant. Then, the number of possible pebble sets M for (C, 1) is in O(2 80 α√d log 2(d)) and all pebble sets can be computed in O(2 80 α√d log 2(d)d) time. Proof. Note that by definition, |Che | ≤ 40 α√d, |Cli | ≤ 60 αd log( d), and the number of possible pebble sets for ( C, 1) is at most 2 |Che |  |Cli | 

> 20 α√dlog( d)

. If d ≤ (60 α)2, that is, d is constant, we get a con-stant number of possible pebble sets. Otherwise, it holds that 60 α log( d) < d because log( d) < √d

30 for any d ≥ 16. Thus, the number of pebble sets is upper bounded by 240 α√d(60 αd log( d)) 20 α√d log( d) ≤ 240 α√d+20 α√d log( d) log( d2) ≤ 280 α√d log 2(d).

We can compute all such sets by iterating over all subsets S1 of Che and all subsets S2 of Cli of size at most 60 αd log( d) in O(2 |Che |  |Cli | 

> 20 α√dlog( d)

) ⊆ O (2 80 α√d log 2(d)) time and computing S1 ∪ S2

in O(d) time. We are finally in a position to prove our result for k-Subgraph Scoring . To this end, recall that a request variant π-Request of locality απ takes a tuple ( d, G, B, x, S π) as input where B = ( Bhe , B li , B di ) is partitioned into heavy, light, and discarded vertices, G is a απd-outerplanar graph, and ( B, x ) is almost measured with respect to d and απ. The output is a lookup table with an entry for each request r ∈ Rπ(d, G, B, M, S π), where M is a pebble set for ( B, d, α π, x ) and the output represents some solution where the vertices in the solution con-tained in Bhe ∪ Bli ∪ Bdi is exactly M.

Theorem 4. k-Subgraph Scoring can be solved in 2 ˜O(√k)n2.49 time for planar input graphs. Proof. We follow the recipe from above and first choose γ(d) = 2 240 √d log 2(d)d2, ρ = 2, and απ = 3. The set Sπ will contain the weight function w and the cost function c for all edges and vertices in the currently considered graph as well as the values α1, α 2, α 3, and β. A request r ∈ Rπ(d, G, B, M, S π)will be a pair ( M, ℓ ) where ℓ ≤ d. The answer for such a request should correspond to the maximum score achieved by a set of vertices intersecting B in exactly M and of cost exactly ℓ. It is easy to verify that Sπ can be maintained in O(n2) time and Rπ(d, G, B, M, S π) can be computed in O(nk )time by iterating over all ℓ ≤ d and copying the vertices in M. For the third step of our recipe, we define k-Subgraph Scoring Request as follows. 

k-Subgraph Scoring Request 

Input: An integer d, a directed 3d-outerplanar graph G = (V, A ), a portal set B = ( Bhe , B li , B di ), an integer x, a vertex-cost function c : V → N, an arc-weight function w : A → R, and values α1, α 2, α 3, β ∈ R such that ( B, x ) is almost measured with respect to d and 3. 

Task: Output a lookup table that returns for each request r = ( M, ℓ ) for each pebble set M

for ( B, d, 3, x ) and each ℓ ≤ d, the maximum value 

α1

  X

> (u,v )∈Au,v ∈S

w(( u, v ))  + α2

  X

> (u,v )∈Au∈S,v / ∈S

w(( u, v ))  + α3

  X

> (u,v )∈Au / ∈S,v ∈S

w(( u, v ))  + β  X 

> v∈S

w(S)

of any set S of cost exactly ℓ and where S ∩ (Bhe ∪ Bli ∪ Bdi ) = M (or −∞ if no such set exists). Next, we show that Requirement R1 of Theorem 3 holds, that is, there is an index i such that we can remove the vertices from each layer i + j · (3 k + 1) for any j of any given BFS layering of the underlying undirected graph G↓. To this end, note that the score of a set S only depends on the vertices in S and the arcs incident to S. For each vertex v ∈ S and any BFS-layering of G↓,it holds that all arcs incident to v have all their endpoints in at most 3 (consecutive) layers: the layer of u, the one before, and the one after. Thus, all arcs relevant for a set S of size at most k are incident to at most 3 k layers and if there are more than 3 k layers, then there is an index i such that removing all layers i + j(3 k + 1) for any j does not invalidate S. Moreover, no new solutions are 31 created in the process, so Requirement R1 holds for απ = 3 for k-Subgraph Scoring Request 

(and k-Subgraph Scoring ). The penultimate step is to show that Requirement R4 holds, that is, there is an algorithm that given a separator C = ( Che , C li , C di ) such that ( C, 1) is measured for d and 3, a pebble set M

for ( B, d, 3, x ), and the solutions for instances (d, C in , (( Bhe ∪ Che )[ Cin ], (Bli ∪ Cli )[ Cin ], (Bdi ∪ Cdi )[ Cin ]) , x + 1 , S π[Cin ]) and (d, C ex , (( Bhe ∪ Che )[ Cex ], (Bli ∪ Cli )[ Cex ], (Bdi ∪ Cdi )[ Cex ]) , x + 1 , S π[Cex ]) ,

can compute the solutions for all requests r ∈ Rπ(d, G, B, M, S π) that do not intersect Cdi and intersect Cli in at most 60 √d log( d) vertices in O(γ(d)n2) time. So assume that we are given C,solutions res 1 for the interior instance and res 2 for the exterior instance, and a pebble set M

for ( B, d, 3, x ). Let Min and Mex be the subsets of M which appear in Cin and Cex , respectively. We iterate over all possible pebble sets M′ for ( C, d, 3, 1), that is, sets which do not contain any vertices in Cdi and at most 60 √d log( d) vertices in Cli . Let AC be the set of arcs with both endpoints in Che ∪ Cli ∪ Cdi . Let A1, A 2, A 3 ⊆ AC be the sets of arcs ( u, v ) ∈ AC such that (1) both u and v

belong to M′, (2) u ∈ M ′ and v / ∈ M ′, and (3) u / ∈ M ′ and v ∈ M ′. Then, for each such set M′,we iterate over all ℓ′ ≤ ℓ and lookup the result for the interior for request ( Min ∪ M ′, ℓ ′) and for the exterior for ( Mex ∪ M ′, ℓ − ℓ′ + P 

> v∈M ′

c(v)). Let s1 and s2 be these scores. We then compute s = s1 + s2 − (α1(P 

> a∈A1

w(a)) + α2(P 

> a∈A2

w(a)) + α3(P 

> a∈A3

w(a)) + β(P 

> v∈M ′

w(v))) and return the highest computed s as a solution for r.We next prove that our computation is correct, that is, (i) we show for each request and any solution for that request that we store a value that is at least as large as the score of the solution and (ii) we show that each time we update a request, there is a partial solution of that score. Before doing so, note that (( Bhe ∪ Che , B li ∪ Cli , B di ∪ Cdi ), x + 1) is measured for d and 3 as ( B, x ) and ( C, 1) are both measured for d and 3. Thus, the solutions res 1 and res 2 have entries for ( Min ∪ M ′, ℓ ′) and ( Mex ∪ M ′, ℓ ′) for each M′ with M′ ∩ Cdi = ∅ and |M ′ ∩ Cli | ≤ 60 √d log( d)and for each ℓ′ ≤ d, respectively. Now, consider a set Z of vertices corresponding to a solution for r with no vertices in Cdi and at most 60 √d log( d) vertices in Cli . Let Zin and Zex be the subsets of Z in Cin and Cex , respectively, and let sin and sex be the scores achieved by Zin in Cin and Zex in Cex , respectively, where M′ = Zin ∩Zex .Note that the queries ( Min ∪M ′, P 

> v∈Zin

c(v)) and ( Mex ∪M ′, P 

> v∈Zex

c(v)) return values at least sin 

and sex , respectively. Moreover, all arcs in A1 ∪ A2 ∪ A3 as well as w(v) for all v ∈ M ′ are counted in both sin and sex and no other arc is counted in both solutions as C is a separator. Thus, the score of Z is sin + sex − (α1(P 

> a∈A1

w(a)) + α2(P 

> a∈A2

w(a)) + α3(P 

> a∈A3

w(a)) + β(P 

> v∈M ′

w(v))), which is precisely what we computed. In the other direction, note that any value we compute corresponds to a set of vertices of cost precisely ℓ which does not intersect Cdi and which intersects Cli in at most 60 √d log( d) vertices. This is true as both res 1 and res 2 guarantee this property for the two solutions we combine and both solutions contain precisely the same vertices in C. The value we subtract from the sum of the two scores is precisely the amount we counted twice (once in each of the two scores s1 and s2). Thus, the computation is correct. We next analyze the running time of our algorithm for Requirement R4. The number of possible pebble sets M′ for ( C, 1) is by Lemma 11 in O(2 240 √d log 2(d)) and they can be enumer-ated in O(2 240 √d log 2(d)d) time. We then iterate over all possible values of ℓ ≤ d and all other computations can be performed in O(n2) time. The overall running time is therefore bounded by O(2 240 √d log 2(d)d2n2) = O(γ(d)n2) and Requirement R4 thus holds for k-Subgraph Scoring Request .32 Finally, we present a reduction from k-Subgraph Scoring to k-Subgraph Scoring Re-quest and analyze the running time. To this end, we apply Lemma 1 with d = 3 k + 1 to com-pute 3 k + 1 3 k-outerplanar graphs. We then solve k-Subgraph Scoring Request for each of these graphs using Theorem 3 with d = k, portal set B = ( ∅, ∅, ∅), x = 1, the input weight and cost functions, and input values α1, α 2, α 3 and β. For each such iteration, we query the optimal solution for request ( ∅, k ), which corresponds to an optimal solution consisting of vertices of cost exactly k. If any iteration finds a solution with score at least W , then we output yes and other-wise we output no. Since the correctness can easily be verified, it remains to analyze the running time. Using Theorem 3 and the arguments above, solving each instance of k-Subgraph Scoring Request takes 2 ˜O(√3k)γ(k)n2.49 time. The time for computing all inputs is in O(kn ) yielding an overall running time in O((3 k + 1)2 ˜O(√k)2240 √k log 2(k)k2n2.49 ) + O(nk ) = 2 ˜O(√k)n2.49 , concluding the proof. We next show that Maximum-Weight Independent Set , Weighted Partial Vertex Cover , Densest k-Subgraph , and Maximum (k, n −k)-Cut are all special cases of k-Subgraph Scoring and hence we can show subexponential parameterized algorithms for all of them without needing to apply the framework for each of them. Subexponential parameterized algorithms for planar graphs were already known for Maximum-Weight Independent Set [vN20], Max-imum (k, n − k)-Cut in undirected planar graphs [FGIK24], and (unweighted) Partial Ver-tex Cover [FGIK24] (even for apex-minor free graphs). They were not known for the direc-ted variant of Maximum (k, n − k)-Cut and Weighted Partial Vertex Cover . Moreover, the existence of such an algorithm was asked as an open problem for (unweighted) Densest k-Subgraph [FGIK24], which we resolve here in the affirmative. We mention is passing that an optimal solution for Densest k-Subgraph might contain many connected components and hence the treewidth-pattern-covering approach does not work. 

Corollary 2. Maximum-Weight Independent Set , Weighted Partial Vertex Cover ,

Densest k-Subgraph , and Maximum (k, n − k)-Cut can all be solved in 2 ˜O(√k)n2.49 time for planar input graphs. Proof. For the directed variant of Maximum (k, n −k)-Cut , we simply set c(v) = 1 for all vertices v,

α1 = α3 = β = 0, and α2 = 1. Then, a set S of cost exactly k is a set of size exactly k and the score of S is precisely the sum of weights of edges from S to the rest of the graph. For the undirected variant of Maximum (k, n − k)-Cut and for all remaining problems, we first arbitrarily direct all edges of the input graph (without changing the weight in case the problem has edge weights). For Maximum (k, n − k)-Cut , Densest k-Subgraph , and Maximum-Weight Independent Set , we again set c(v) = 1 for all vertices. For Maximum (k, n − k)-Cut , we set α1 = β = 0 and α2 = α3 = 1. For Densest k-Subgraph , we set α1 = 1 and α2 = α3 = β = 0. It is easy to verify that k-Subgraph Scoring for the chosen values of α1, α 2, α 3 and β corresponds to Maximum (k, n − k)-Cut and Densest k-Subgraph , respectively. For Weighted Partial Vertex Cover , we set the cost of a vertex to its input weight and set α1 = α2 = α3 = 1 and β = 0. We then solve for each ℓ ≤ k an instance of k-Subgraph Scoring to find whether there is a set of cost exactly ℓ that covers edges of weight at least W .The additional factor of k in the running time is subsumed in 2 ˜O(√k).Finally, we consider Maximum-Weight Independent Set . Here, we set α2 = α3 = 0, β = 1, and α1 = −nw , where w is the largest weight of any vertex. This ensures that any set which induces at least one edge, that is, it is not an independent set, has a negative score. Again, we iterate over all ℓ ≤ k and compute the maximum weight of any independent set of size exactly ℓ.The maximum over all of those gives the maximum-weight independent set of size at most k and 33 the additional factor of k in the running time is again subsumed in 2 ˜O(√k). This concludes the proof. 

4.2 Maximum-Weight Induced Forest 

We next study Maximum-Weight Induced Forest . Before giving the formal proof, let us first give a high-level overview. When combining solutions along a separator, we want to verify that the two partial solution (induced forests) are together also a forest. To this end, consider a cycle in the union of two partial solutions and let M′ be the set of vertices from the separator that are part of the cycle. Each consecutive pair is connected in (at least) one of the two partial solutions. Thus, the main idea will be to keep track of (i) which vertices of the portal set are contained in the partial solution (a pebble set) and (ii) which of these vertices are in the same connected component in the partial solution. We encode this information in a partition of a pebble set. To this end, we denote the set of all possible partitions of a set S by B(S) and each set in S a block . For a given subset A of vertices and a given partition P ∈ B (A), we say that a graph G′ has connected components for A as described by P if for each pair u, v ∈ A of vertices in A, it holds that u and v

belong to the same connected component in G′ if and only if u and v are contained in a common block p ∈ P . We also say that P describes the connected components of G′ for A. We will then be interested in keeping track of the connectivity of the union of two partial solutions. We will show that this is equivalent to the join of two partitions P and Q in case P and Q partition the same set of vertices. For notational convenience, we will also define the join for partitions of different sets of vertices and show that this generalized notion of join precisely describes the connectivity of the union of two partial solutions we desire. We now give a formal definition of joins of partitions. We denote the join of P and Q by P ⊔ Q.If P and Q partition the same set of vertices, then the join of P and Q is the finest common coarsen-ing of P and Q and is obtained by starting with P and iteratively merging two sets p1, p 2 ∈ P if there is a set q ∈ Q with p1∩q̸ = ∅ and p2∩q̸ = ∅. If P partitions a set V1 and Q partitions a different set V2,then we define the join of P and Q as the join of P ′ and Q′, where P ′ = P ∪ {{ x} | x ∈ V2 \ V1}

and Q′ = Q ∪ {{ x} | x ∈ V1 \ V2}, that is, we first add all elements which are not partitioned by P

or Q as singletons. The join of two partitions of sets of size n can be computed in O(nα (n)) time, where α(n) is the inverse Ackermann’s function [TvL84]. We next show that the join operation ac-curately describes the connectivity of the union of two partial solutions when these are joined along a common separator. This result is folklore but since we could not find a formal proof anywhere, we provide a proof for the sake of completeness. 

Lemma 12 (folklore) . Let G = ( V, E ) be an undirected graph and (X, Y ) be a separation of G.Let Z = X ∩ Y , M ⊆ Z, and let B ⊆ V be a set of vertices with B ∩ Z = M. Let BX = B ∩ X

and BY = B ∩ Y . Let PX ∈ B (BX ) and PY ∈ B (BY ) be partitions of Bx and BY , respect-ively. Let GX = ( VX , E X ) and GY = ( VY , E Y ) be subgraphs of G[X] and G[Y ], respectively, such that BX ⊆ VX , VX ∩ Z = M, GX has connected components for BX as described by PX ,

BY ⊆ VY , VY ∩ Z = M, and GY has connected components for BY as described by PY . Then, the graph G′ = ( VX ∪ VY , E X ∪ EY ) has connected components for B as described by PX ⊔ PY .Proof. First, we show that if two vertices u, v ∈ B are in the same block of PX ⊔ PY , then they are in the same connected component in G′. By definition, there is a sequence u = w1, w 2, . . . , w ℓ = v

in B such that for each i ∈ [ℓ − 1], wi and wi+1 are contained in a common block of PX or in a common block of PY . If wi and wi+1 are in a common block of PX , then note that wi, w i+1 ∈ BX

and there exists a path in GX connecting them, since GX has connected components for BX as described by PX . Similarly, if they are in a common block of PY , then there exists a path in GY

34 connecting them. Since GX and GY are both subgraphs of G′ and connectivity is transitive, this shows that u and v are in the same connected component of G′.Conversely, we next show that if u and v are in the same connected component of G′, then they are contained in a common block of PX ⊔ PY . So assume that u and v are in the same connected component in G′. Then there exists a path P = ( u = p1, p 2, . . . , p q = v) in G′. Let w1, w 2, . . . , w ℓ

be the vertices in Z that are contained in P (in the same order) and let w0 = u and wℓ+1 = v.Each edge of P is contained in EX or in EY as G′ is the union of GX and GY and since ( X, Y ) is a separation of G, all edges between wi−1 and wi are contained in the same set for each i ∈ [ℓ + 1]. Since PX and PY describe the connected components of GX and GY for BX and BY , respectively, it holds for each i ∈ [ℓ + 1] that wi−1 and wi are contained in a common block in PX or in PY (or both). Thus, u and v are contained in a common block of PX ⊔ PY by definition of joins. This concludes the proof. The next lemma will help us decide when the union of two forests G1 and G2 is acyclic. Intu-itively, this is the case if there is no sequence ( w1, w 2, . . . , w 2ℓ) of vertices such that w2i−1 and w2i

are connected in G1 for each i ∈ [ℓ] and w1 and w2ℓ as well as w2i and w2i+1 for each i ∈ [ℓ − 1] are connected in G2. We will next define a notion of acyclic pairs of partitions that captures this intuition. Unfortunately, the intuition does not quite work when G1 and G2 are not edge-disjoint. As a simple example, consider just two vertices u and v. If they are connected by the same path in G1 and G2, then the union of the two is just a path and therefore acyclic. If however, they are connected by two paths that differ by at least one edge, then the union is not acyclic. We circumvent this issue by keeping track of the set of edges in the intersection and ignoring those for one of the two graphs. We next define our notion of acyclic pairs of partitions. A pair ( P, Q ) of partitions is acyclic if the following graph FP,Q is acyclic. Start with a vertex vx for each element x

and a vertex up for each block p ∈ P ∪ Q. Then, add an edge between vx and up if x ∈ p. Note that deciding whether a pair ( P, Q ) is acyclic can be done in O(n) time, where n is the number of elements partitioned by P and Q, as we can build the bipartite graph in O(n) time and then check for a cycle in linear time. Since each element vertex is incident to at most 2 edges, the number of edges is at most 2 n and thus this can also be done in O(n) time. We next show that acyclic partitions accurately describes when the union of two partial solutions is acyclic. 

Lemma 13. Let G = ( V, E ) be an undirected graph and (X, Y ) be a separation of G with Z = X∩Y .Let M ⊆ Z and B ⊆ V be sets of vertices such that B ∩Z = M. Let BX = B ∩X and BY = B ∩Y .Let PX ∈ B (BX ) and PY ∈ B (BY ) be partitions of Bx and BY , respectively. Let EM be the set of edges of G[M]. Let GX = ( VX , E X ) and GY = ( VY , E Y ) be acyclic induced subgraphs of G[X]

and G[Y ], respectively, such that BX ⊆ VX , VX ∩ Z = M, GX has connected components for BX

as described by PX , BY ⊆ VY , VY ∩ Z = M, and GY has connected components for BY as described by PY . Let G′ 

> Y

= ( VY , E Y \ EM) and let P ′ 

> Y

be the partition that describes the connected components of G′ 

> Y

for BY . Then, the graph induced by VX ∪ VY is an acyclic induced subgraph of G with connected components for B as described by PX ⊔ PY if and only if the pair (Px, P ′ 

> Y

) is acyclic. Proof. Let H = G[VX ∪ VY ] be the graph induced by VX ∪ VY . For the first direction, assume that H is acyclic and has connected components for B as described by PX ⊔ PY . We will show that ( PX , P ′ 

> Y

) is acyclic. To this end, assume towards a contradiction that the graph FPX ,P ′

> Y

contains a cycle ( vx1 , u p1 , v x2 , u p2 , . . . , u pℓ ), where x1, x 2, . . . , x ℓ ∈ B and p1, p 2, . . . , p ℓ ∈ PX ∪ P ′ 

> Y

.Since we may assume without loss of generality that the cycle we consider is simple and each vertex vxi is only incident to two nodes upj (one for a block in PX and one for a block in P ′ 

> Y

), it holds that the nodes upj alternate between being blocks for PX and PY ′ . By construction, each 35 consecutive pair ( vxi , v xi+1 ) lies within the same connected component of GX or of G′ 

> Y

, that is, there are paths in GX or G′ 

> Y

connecting xi and xi+1 . Moreover, these paths are pairwise edge-disjoint as EX ∩ (EY \ EM) = ∅ and two paths in the same graph are edge disjoint as they belong to different connected components (we may assume that each node upj appears at most once in the cycle). Hence, we can concatenate all paths to get a closed walk in H that does not repeat any edges, contradicting the fact that H is acyclic. In the other direction, assume that ( PX , P ′ 

> Y

) is acyclic. We will show that H is acyclic and that it has connected components for B as described by PX ⊔ PY . Note that the latter is shown by Lemma 12. Hence, assume towards a contradiction that H contains a cycle C. Since GX and GY

are each acyclic, C must contain edges from both GX and GY . Let CX and C′ 

> Y

denote the subset of edges in EX and EY \ EM and let w1, w 2, . . . , w ℓ be the vertices at the end of maximal subpaths of C in CX and/or C′ 

> Y

. Note that wi ∈ M for each i ∈ [ℓ] as Z is a separator and GX and GY only contain the vertices in M in Z. By definition, wi and wi+1 (where wℓ+1 = w1) are contained in the same block pi in PX or P ′ 

> Y

, respectively. Note that ( w1, p 1, w 2, . . . , w ℓ, p ℓ) forms a cycle in FPX ,P ′ 

> Y

,a contradiction to ( PX , P ′ 

> Y

) being acyclic. Thus, H is acyclic. This concludes the proof. As a last step before showing how to solve Maximum-Weight Induced Forest in subex-ponential parameterized time, we analyze the number of possible partitions for a given pebble set. 

Lemma 14. Let (B, x ) be a pair that is almost measured with respect to two integers d and α,where α is a constant. Let M be a pebble set for (B, d, α, x ). Then, the number of possible partitions of M is in O(2 66000 √d log 10 (d)) and all partitions can be enumerated in the same time. Proof. Since ( B, x ) is almost measured with respect to d and α, it holds that x ≤ 200 log 7(d). Moreover, M ⊆ (Bhe ∪ Bli ), |Bhe | ≤ 40 xα √d and |M ∩ Bli | ≤ 20 xα √d log( d) because M is a pebble set for ( B, d, α, x ). Thus, |M| ≤ 60(200 log 7(d)) α√d log( d) = 12000 α√d log 8(d). The number of partitions for a set of size n is known as the nth Bell’s number and is known to be at most nn. There are even slightly better upper bounds [BT10], but this simple bound will be sufficient for our purpose. If d < 256 · 2α, then the number of partitions is constant, that is, in O(1). So assume that d ≥ 256 · 2α. Then, the number of possible partitions of M is upper-bounded by 

|M| |M| ≤ (12000 α√d log 8(d)) 12000 α√d log 8(d)

≤ (d2√d d 3)12000 α√d log 8(d)

= 2 12000 α√d log 8(d) log( d5.5)

≤ 266000 √d log 10 (d).

Here, the second inequality is due to the fact that (256 α)2 > 12000 α and log 8(d) ≤ d3 for all d ≥ 256. The last inequality is due to the fact that α ≤ log( d). Note that we can enumerate all partitions of M efficiently, that is, with constant delay on average by iteratively deciding for each vertex v ∈ M to which already existing block it is added or whether a new block consisting only of v is added. This concludes the proof. With the above three lemma at hand, we can now show how to apply Theorem 3 to Maximum-Weight Induced Forest . A request will consist of a pebble set, a partition of this pebble set, and an integer ℓ. The output describes the maximum weight of a forest with at most ℓ vertices containing exactly the pebble set in the portal set and having connected components for the pebbles 36 as described by the partition. That is, we basically keep track of how many vertices are contained in the partial solution, which vertices in the portal set make up the interface to the rest of the graph, and which of these vertices are connected in the partial solution. 

Theorem 5. Maximum-Weight Induced Forest can be solved in 2 ˜O(√k)n2.49 time for planar input graphs. Proof. We again follow the recipe presented at the start of this section. To this end, we first choose γ(d) = 2 80 √d log 2(d) 266000 √d log 10 (d)2d, ρ = 2, and απ = 1. The set Sπ will contain the weight function w. A request r ∈ Rπ(d, G, B, M, S π) will be a triple ( M, P, ℓ ), where P ∈ M 

is a partition of M and ℓ ≤ d. The output for a request is the maximum weight of an induced forest with at most ℓ vertices and which contains exactly the vertices in M from B. It is easy to verify that Sπ can be maintained in O(n2) time. We next show that Rπ(d, G, B, M, S π) can be computed in O(γ(d)nρ) time. By Lemma 14, the number of possible partitions for a given pebble set is in O(2 66000 √d log 10 (d)) and all these partitions can be enumerated in O(2 66000 √d log 10 (d)d) time. The number of possible values for ℓ ≤ d is d, yielding a running time of O(2 66000 √d log 10 (d)d2) for computing Rπ(d, G, B, M, S π). For the third step of our recipe, we define Maximum-Weight Induced Forest Request as follows. 

Maximum-Weight Induced Forest Request 

Input: An integer d, a d-outerplanar graph G = ( V, E ), a portal set B = ( Bhe , B li , B di ), an integer x, and a vertex-weight function w : V → R, such that ( B, x ) is almost measured with respect to d and 1. 

Task: Output a lookup table that returns for each request r = ( M, P, ℓ ), where P ∈ B (M)is a partition of M, the maximum weight of a set S with S ∩ (Bhe ∪ Bli ∪ Bdi ) = M,

|S| ≤ ℓ, and G[S] is a forest that has connected components for M as described by P

(or −∞ if no such set exists). Next, we show that Requirement R1 of Theorem 3 holds, that is, there is an index i such that we can remove each layer i + j(k + 1) of a BFS layering for any j. To this end, note that whether a set S is a partial solution or not only depends on the vertices in S and edges between these vertices. Thus, all edges relevant for a set S of size at most k are incident to at most k layers and if there are more than k layers, then there is an index i such that removing all layers i + j(k + 1) for any j

does not invalidate S. Moreover, no new solutions are created in the process, so Requirement R1 holds for απ = 1 for Maximum-Weight Induced Forest Request (and Maximum-Weight Induced Forest ). The penultimate step is to show that Requirement R4 holds, that is, there is an algorithm that given a separator C = ( Che , C li , C di ) such that ( C, 1) is measured for d and 1, a pebble set M

for ( B, d, 1, x ), and the solutions for instances (d, C in , (( Bhe ∪ Che )[ Cin ], (Bli ∪ Cli )[ Cin ], (Bdi ∪ Cdi )[ Cin ]) , x + 1 , S π[Cin ]) and (d, C ex , (( Bhe ∪ Che )[ Cex ], (Bli ∪ Cli )[ Cex ], (Bdi ∪ Cdi )[ Cex ]) , x + 1 , S π[Cex ]) ,

can compute the solutions for all requests r ∈ Rπ(d, G, B, M, S π) that do not intersect Cdi and intersect Cli in at most 20 √d log( d) vertices in O(γ(d)n2) time. So assume that we are given C,solutions res 1 for the interior instance and res 2 for the exterior instance, and a pebble set M

for ( B, d, 1, x ). We iterate over all values ℓ ≤ d and possible partitions P ∈ B (M) using Lemma 14. We then compute the optimal solution for request r = ( M, P, ℓ ) as follows. Let Min and Mex be the sub-sets of M which appear in Cin and Cex , respectively. We iterate over all possible pebble sets M′

37 for ( C, d, 1, 1), that is, sets which do not contain any vertices in Cdi and at most 20 √d log( d)vertices in Cli . We also iterate over all possible values |M ′| ≤ ℓ′ ≤ ℓ. Let M′ 

> in

= Min ∪ M ′

and M′ 

> ex

= Mex ∪ M ′. We then iterate over all possible partitions P1 ∈ B (M′

> in

) and P2 ∈ B (M′

> ex

)of these two sets, respectively. Let EM′ be the set of edges in G[M′], that is, edges in the input graph between two vertices in M′. Let G′ = ( V ′, E ′) be any graph with M′ 

> ex

⊆ V ′,

V ′ ∩ (Che ∪ Cli ∪ Cdi ) = M′, EM′ ⊆ E′, and G′ has connected components for M′ 

> ex

as described by P2. Let G′′ = ( V ′, E ′ \ EM′ ) and let P ′ 

> 2

be the partition that describes the connected com-ponents of G′′ for M′

> ex

. If P1 ⊔ P2 = P and ( P1, P ′

> 2

) is acyclic, then let W1 = res 1(( M′

> in

, P 1, ℓ ′)) and W2 = res 2(( M′

> ex

), P 2, ℓ − ℓ′ + |M ′|). We update the output for r with W1 + W2 − P 

> v∈M ′

w(v)in case this value is larger than the current value. This concludes the construction of our algorithm for Requirement R4. We next prove that our computation is correct and afterwards analyze the running time of the above algorithm. In order to prove the correctness, we first show that for any solution Z for a request r, we update the output for request r with the weight of Z unless a larger value was already found. Afterwards, we show that any value we update the output with corresponds to a solution of that weight. Before we show the correctness, first note that (( Bhe ∪ Che , B li ∪ Cli , B di ∪ Cdi ), x + 1) is measured for d and 1 as ( B, x ) and ( C, 1) are both measured for d and 1. Thus, the solutions res 1

and res 2 have entries for ( M′

> in

, P 1, ℓ ′) and ( M′

> ex

, P 2, ℓ − ℓ′ + |M ′|) for each |M ′| ≤ ℓ′ ≤ ℓ ≤ d,respectively. We next show that any valid solution is considered. To this end, consider a set Z of vertices corresponding to a solution for r. Let Zin and Zex be the subsets of Z in Cin and Cex , respectively, and let Win and Wex be the total weights of vertices in Zin and Zex , respectively. Let Pin and Pex be the partitions of M′ 

> in

and M′ 

> ex

describing the connected components of G[Zin ] and G[Zex ] for M′

> in

and M′

> ex

, respectively. Note that the queries for requests ( M′

> in

, P in , |Zin |) and ( M′

> ex

, P ex , |Zex |)return values which are at least Win and Wex , respectively. Moreover, as Zin and Zex intersect precisely in M′, it holds that |M ′| ≤ | Zin | ≤ ℓ′ and |Zex | ≤ ℓ − | Zin | + |M ′|. We will next show that ( Pin ,P ′

> ex

) (where P ′ 

> ex

is defined analogously to P ′ 

> 2

above) is acyclic. This will conclude the first direction of the proof of correctness as follows. By Lemma 13, Pin ⊔Pex = P . Moreover, as P1 = Pin 

and P2 = Pex is then considered by our algorithm, we store the value Win + Wex − P 

> v∈M ′

w(v). Note that Zin and Zex intersect precisely in M′ and hence 

X

> v∈Z1∪Z2

= ( X

> v∈Z1

w(v)) + ( X

> v∈Z2

w(v)) − ( X

> v∈M ′

w(v)) = W1 + W2 − X

> v∈M ′

w(v).

This is both the weight of all vertices in Z and the value we compute to update the output for request r. Hence, each solution Z is considered. So it remains to show that ( Pin ,P ′

> ex

) is acyclic. Assume towards a contradiction that FPin ,P ′

> ex

contains a cycle ( vx1 , u p1 , v x2 , u p2 , . . . , u pℓ ), where x1, x 2, . . . , x ℓ ∈ M ′ and p1, p 2, . . . , p ℓ ∈ Pin ∪ P ′

> ex

.Since each vertex vxi is only incident to two nodes upj (one for a block in Pin and one for a block in P ′

> ex

), it holds that the nodes upj alternate between being blocks for PX and PY ′ . By construction, each consecutive pair ( vxi , v xi+1 ) lies within the same connected component of G[Z1]or of G[Z2] − EM′ , where EM′ is the set of all edges between two vertices in M′. That is, there are paths in G[Z1] or G[Z2] − EM′ connecting xi and xi+1 . Moreover, these paths are pairwise edge-disjoint as the two graphs are edge disjoint and two paths in the same graph are edge disjoint as they belong to different connected components. Hence, the concatenation of all these paths yields a closed walk in G[Z] (the union of G[Z1] and G[Z2] − EM′ ), a contradiction to G[Z] being acyclic. In the other direction, we show that whenever we update a solution for a request r, this corres-ponds to an actual solution. So assume there is an ℓ′ and partitions P1 and P2 such that P1 ⊔P2 = P ,38 (P1,P ′

> 2

) is acyclic, and we update the result for request r. This means that neither W1 nor W2

are −∞ . Hence, there are vertex sets Z1 and Z2 corresponding to solutions for ( M′

> in

, P 1, ℓ ′)and ( M′

> ex

, P 2, ℓ − ℓ′ + |Z1 ∩ Z2|). By Lemma 13, G[Z1 ∪ Z2] is then acyclic and has connected components for M as described by P1 ⊔ P2 = P . Moreover, the weight of the union is exactly the sum of weights of Z1 and Z2 minus the weight of the intersection, that is, the weight of M′. Since this is precisely what we compute, this shows that every update we do is valid, concluding the proof of correctness. We next analyze the running time of our algorithm for Requirement R4. The number of pos-sible pebble sets M′ for ( C, 1) is by Lemma 11 in O(2 80 √d log 2(d)) and the pebble sets can be enumerated in O(2 80 √d log 2(d)d) time. Iterating over all ℓ ≤ d adds an additional factor of d. Com-puting Min , Mex , M′

> in

, M′ 

> ex

can all easily be done in O(n2) time. We then iterate over all possible partitions P1 ∈ B (M′

> in

) and P2 ∈ B (M′

> ex

). The number of possibilities is in O((2 66000 √d log 10 (d))2)by Lemma 14 and all partitions can be computed in O(2 66000 √d log 10 (d)d) time. Computing EM′ can be done in O(n2) time. Computing G′ and G′′ can also be done in the same time as follows. For G′,start with a graph which has a vertex for each v ∈ M ′ 

> ex

and each p ∈ P2. Then, add all edges in EM′ .For each p ∈ P2 and each v ∈ p, add an edge between the two corresponding vertices if (and only if) this edge does not create a cycle. Using union-find data structures, each such incidence can be checked in O(α(n)) time, where α is the inverse Ackermann’s function. Since each vertex appears in exactly one set in P2, this results in an overall running time of O(nα (n)) ⊆ O (n2). For G′′ , simply remove the edges in EM′ . Computing P ′ 

> 2

can then also be done in the same time by computing the connected components in G′′ . Finally, checking whether P1 ⊔ P2 = P and whether ( P1,P ′

> 2

) is acyclic also take O(nα (n)) ⊆ O (n2) time. Recall that γ(d) = 2 80 √d log 2(d) 266000 √d log 10 (d)2d and ρ = 2. The overall running time for our algorithm for Requirement R4 is in 

O(2 80 √d log 2(d)d + 2 80 √d log 2(d)d(n2 + 2 66000 √d log 10 (d)d + (2 66000 √d log 10 (d))2n2)) ⊆ O (γ(d)nρ).

The final step is to show a reduction from Maximum-Weight Induced Forest to Maximum-Weight Induced Forest Request and analyze the running time. To this end, we apply Lemma 1 with d = k + 1 to compute k + 1 k-outerplanar graphs. We then solve Maximum-Weight Induced Forest Request for each of these graphs using Theorem 3 with d = k, portal set B = ( ∅, ∅, ∅), x = 1, and the input weight function. For each such iteration, we query the op-timal solution for request ( ∅, ∅, k ), which corresponds to an optimal solution consisting of at most k

vertices. If any iteration finds a solution with weight at least W , then we output yes and otherwise we output no. Since the correctness can easily be verified, it remains to analyze the running time. Using Theorem 3 and the arguments above, solving each instance of Maximum-Weight Induced Forest Request takes time 2 ˜O(√k)280 √k log 2(k) 266000 √k log 10 (k)2kn 2.49 ⊆ 2 ˜O(√k)2132081 √d log 10 (k)n2.49 ⊆ 2 ˜O(√k)n2.49 .

The time for computing all inputs is in O(kn ) by Lemma 1 and because induced subgraphs can be computed in O(n + m) time and since m ∈ O (n) for planar graphs. The overall running time is therefore in O(( k + 1)2 ˜O(√k)n2.49 ) + O(nk ) = 2 ˜O(√k)n2.49 . This concludes the proof. 

4.3 Simple Minors 

In this section, we study Minimum-Weight Rooted Simple Minor . We again start with an intuitive description of the rough idea. To this end, we first briefly sketch how the algorithm works for Subgraph Isomorphism . This proof sketch follows the algorithm by Nederlof but ignores 39 the use of efficient inclusion-exclusion which is needed to also count the number of solutions. Let H = ( U, F ) be the (simple) pattern graph we try to find and let k = |U |. Note that since planar graphs are closed under taking minors, we can assume without loss of generality that H

is planar (otherwise the answer is no). Following the proof by Bodlaender, Nederlof, and van der Zanden [BNvdZ16], Nederlof [Ned20] observes that the number of non-isomorphic separations of H

of size √k is in 2 O(k/log( k)). A request (or subproblem in the language of Nederlof) is then a triple consisting of a pebble set M, a separation ( X, Y ), and a bijection c mapping the vertices in M to the nodes in X ∩ Y . The output indicates whether there exists a subgraph G′ = ( V ′, E ′) and an isomorphism f that maps the vertices in G′ to the nodes in X such that f (v) = c(v) for each v ∈ M .Two partial solutions for requests ( M1, (X1, Y 1), c 1) and ( M2, (X2, Y 2), c 2) and a separator C can then be combined to a solution for a request ( M, (X, Y ), c ) if M1 ∪ M 2 = M, M1 ∩ C = M2 ∩ C,

c1(v) = c(v) for each v ∈ M 1, c2(v) = c(v) for each v ∈ M 2, and H[X1 ∪ X2] = H[X]. Let us now describe how to generalize the above approach for Minimum-Weight Rooted Simple Minor . We also keep track of non-isomorphic separations ( X, Y ) of the pattern graph H,but instead of a yes/no-answer, we store the minimum weight of a subgraph that is a model of X.Additionally, there are three facts that complicate the algorithm slightly when considering (rooted) minors rather than subgraphs. First and of least importance, we can no longer assume that c is a bijection and we need to make sure that c maps each root v to the correct node in U . To this end, we consider coloring functions , which we introduce after the high-level overview. Second, we need to ensure that the model for a single node in H is connected. We handle this issue similar to how we handled connectivity-related issues in the previous subsection, that is, by including a partition of the pebble set in a request. Lastly, some edges in H[X ∩ Y ] might be part of the current partial solution while others might be implemented later. To keep track of this information, we store for a given separation ( X, Y ) a subset of the edges in H[X ∩Y ] as part of each request and allow a solution to not model these. An additional technicality is that pebble sets will have size ˜O(p|U |), so the lemma by Nederlof does not apply and we will need a slightly stronger version of his statement that also works for slightly larger separations. For this reason and because Nederlof only states that the result follows from a proof by Bodlaender, Nederlof, and van der Zanden [BNvdZ16] (which is slightly non-trivial), we will include a proof for the sake of completeness. 

Corollary 3. Given a (simple) planar graph H = ( U, F ) and a non-negative integer t ∈ O (|U | 23 ),the number σt(H) of non-isomorphic separations of size at most t of H is in 2O(|U |/log( |U |)). The separations can be enumerated in O(|U |3σt(H)) time. 

Unfortunately Corollary 3 does not hold for multigraphs. We show in the next subsection that there are simple examples where even the number of separations of size zero are exponential in |U |.Instead, we will introduce a parameter ψ that bounds the number of non-isomorphic separations of planar multigraphs and show that it leads to subexponential parameterized algorithms. Since Corollary 3 is a simple corollary of the statement for multigraphs, we defer its proof to the next subsection. We now define coloring functions . Given two sets U and V , a coloring function c : U → 2V

maps each element in U to a non-empty subset of V such that each v ∈ V appears in exactly one set c(u). Equivalently, each v ∈ V is assigned one “color” u ∈ U . As suggested by the variable names, we will use coloring functions from nodes in H to subsets of vertices in G. However, we will usually restrict the vertex sets in both G and H to subsets M ⊆ V and Z ⊆ U . We then consider coloring functions from Z to subsets of M. These should be thought of as extensions of the root function μ : U → 2V , that is, we assign each vertex v ∈ M to a node u ∈ Z and assume that v is part of the model for node u. Whenever μ requires that v is part of the model for some node u′, then we also want to enforce v ∈ c(u′). Formally, we say that c is an extension of μ

40 on the vertex set M if for all v ∈ M and all u ∈ U it holds that if v ∈ μ(u), then v ∈ c(u). In particular if v ∈ μ(u′′ ) for some u′′ ∈ U \ Z, then there is no extension of μ as v must be assigned to u′′ so it cannot be assigned to any node in Z. We next analyze the number of possible coloring functions and show how to enumerate them. In order to apply the lemma more conveniently later, we will focus on the case where M is a pebble set. We mention that the proof is basically the same as for Lemma 14. 

Lemma 15. Let (B, x ) be a pair that is almost measured with respect to two integers d and α and let M be a pebble set for (B, d, α, x ). Let V, U, and Z be sets such that M ⊆ V and Z ⊆ U .Let μ : U → 2V be a function. Then, the number of possible coloring functions c : Z → 2M

that are extensions of μ is in O(2 66000 √d log 10 (d)). All such coloring functions can be enumerated in O(2 66000 √d log 10 (d)d) time. Proof. Since ( B, x ) is almost measured with respect to d and α, it holds that x ≤ 200 log 7(d). Moreover, M ⊆ (Bhe ∪ Bli ), |Bhe | ≤ 40 xα √d and |M ∩ Bli | ≤ 20 xα √d log( d) because M is a pebble set for ( B, d, α, x ). Thus, |M| ≤ 60(200 log 7(d)) α√d log( d) = 12000 α√d log 8(d). We next show that the number of coloring functions from Z to M is in O(2 66000 √d log 10 (d)). This clearly also upper bounds the number of coloring functions that are extensions of μ. First, note that |Z| ≤ |M| as otherwise there is no coloring function from Z to M as each vertex in Z is assigned a non-empty subset of M and each vertex in M is assigned exactly one vertex in Z. Recall that an equivalent interpretation of coloring functions is to assign each vertex in M to exactly one vertex in Z. Thus, the number of coloring functions is at most |Z||M| ≤ |M| M. If d < 256 · 2α,then this number is constant, that is, in O(1). So assume that d ≥ 256 · 2α. Then, 

|M| |M| ≤ (12000 α√d log 8(d)) 12000 α√d log 8(d)

≤ (d2√d d 3)12000 α√d log 8(d)

≤ 266000 √d log 10 (d).

The second inequality is true because (256 α)2 > 12000 α and log 8(d) ≤ d3 for all d ≥ 256. The third inequality is due to the fact that α ≤ log( d). Note that we can enumerate all coloring functions in O(|Z||M| d) ⊆ O (2 66000 √d log 10 (d)d) time by iterating over all possible choices of assigning a “color” to each node in M. This takes O(|Z||M| )time. We then check in O(d) time whether the function extends μ and assigns a non-empty set to each node u ∈ Z. This concludes the proof. The last ingredient for the proof of the subexponential parameterized algorithm for Minimum-Weight Rooted Simple Minor is a restriction of partitions of M for coloring functions. Form-ally, we say that a partition P ∈ B (M) refines a coloring function c : Z → 2M if the following holds for each pair v1, v 2 ∈ M of vertices in M. Let u1, u 2 ∈ Z be the nodes such that v1 ∈ c(u1)and v2 ∈ c(v2). If u1̸ = u2, then u1 and u2 are contained in different blocks in P . Intuitively, P

refines c if whenever c assigns v1 and v2 different colors, then v1 and v2 are contained in different blocks in P . Note that we do not require that whenever c assigns two vertices the same color, then they also appear in the same block in P . It will be convenient for us to interpret such a partition as a collection of partitions, one for each node u. For a partition, we denote the subset of P containing vertices in c(u) for some u ∈ Z by P u. Note that P = S 

> u∈Z

P u. We next show how to use the above to design a subexponential parameterized algorithm for Minimum-Weight Rooted Simple Minor parameterized by both k and the number σt(H) of non-isomorphic sep-arations of H. Afterwards, we show how to use this result to get a subexponential parameterized algorithm for Minimum-Weight Rooted Simple Minor parameterized by only k.41 Theorem 6. Let t = 12000 √k log 8(k). Minimum-Weight Rooted Simple Minor can be solved in 2 ˜O(√k)(σt(H)) 2n2.49 time for planar graphs, where σt(H) denotes the number of non-isomorphic separations of H of size at most t.Proof. We follow the same recipe as usual. We choose ρ = 2 and απ = 1. The function γ

depends on the pattern graph H = ( F, U ) and we choose γH (d) = ( σt(H)) 225t|U |32264080 √d log 10 (d)d.The set Sπ contains the weight function w and the function μ defining the roots of the input graph G (both restricted to the current subgraph). A request r ∈ Rπ(d, G, B, M, S π) will be a 6-tuple ( M, (X, Y ), c, P, E, ℓ ). Let us give a description of the different entries. The set M is a pebble set and ( X, Y ) is a separation of H of size at most t. Let Z = X ∩ Y be the separator and H[Z] = ( Z, E Z ) be the graph induced by Z. The entry c : Z → 2M is a coloring function that is an extension of μ and P ∈ B (M) is a partition of M that refines c. The set E ⊆ EZ is a subset of the edges in H[Z] which are not yet realized by the model, and ℓ ≤ d is an upper bound on the number of vertices in a partial solution. The output for a request r = ( M, (X, Y ), c, P, E, ℓ )is defined as follows. Therein, we use GP = ( V, E ∪ {{ u, v } | c(u) = c(v) ∧ ∀ p ∈ P. {u, v }̸ ⊆ p})as a shorthand for the graph G where we additionally add all edges between two vertices that are mapped to the same node in H but do not appear together in a block in P . Recall that for a set E′

of edges, G − E′ denotes the graph where all edges in E′ have been removed. We can now state the conditions for when a subgraph G′ = ( V ′, E ′) of G fulfills r:

• |V ′| ≤ ℓ, and 

• there exists a function f : X → 2V ′

such that 1. for each u ∈ X, c(u) ⊆ f (u), 2. when contracting all edges e in G′ 

> P

for which there exists a u ∈ X with e ⊆ f (u), then the resulting graph is isomorphic to H[X] −E and for each node u ∈ X, the isomorphism maps the result of contracting all vertices in f (u) to u, and 3. for each u ∈ X, the graph G′[f (u)] has connected components for c(u) as described by P u.The weight of G′ is P 

> e∈E′

w(e) and the output for r is the minimum weight of any graph fulfilling r

(or ∞ if no such graph exists). We next show that Sπ[G′] for any subgraph G′ and Rπ(d, G, B, M, S π) can be computed in O(γH (d)nρ) time. Since Sπ only contains the weight function and the root function μ, it can be maintained trivially in O(n2) = O(nρ) time. For any given 5-tuple ( d, G, B, M, S π), we can compute Rπ(d, G, B, M, S π) as follows. Since M is part of the input for Rπ, we only need to copy the vertices, which takes O(n) time. By Corollary 3, we can enumerate all separ-ations ( X, Y ) of H of size at most t in O(|U |3σt(H)) time. All coloring functions that are an extension of μ can be computed in O(2 66000 √d log 10 (d)d) time by Lemma 15. All possible parti-tions of P can be enumerated in O(2 66000 √d log 10 (d)) time by Lemma 14 and we can check for each of them whether they refine c in O(n) time. Finally, since E is a subset of the edges in H[Z], we can enumerate all possible choices in O(2 3t) time because H[Z] contains at most t

vertices and is planar and therefore contains at most 5t/2 < 3t edges. Iterating over all possible values ℓ ≤ d takes O(d) time and iterating over all possible combinations of all of the above takes O(|U |3σt(H)2 66000 √d log 10 (d)266000 √d log 10 (d)23tdn 2) ⊆ O (γH (d)nρ) time. For the third step of our recipe, we define Minimum-Weight Rooted Simple Minor Re-quest as follows. 42 Minimum-Weight Rooted Simple Minor Request 

Input: An integer d, a d-outerplanar graph G = ( V, E ), a portal set B = ( Bhe , B li , B di ), an integer x, an edge-weight function w : E → R, a planar graph H = ( U, F ), and a function μ : U → 2V , such that ( B, x ) is almost measured with respect to d and 1. 

Task: Output a lookup table that returns for each request r = (M, (X, Y ), c, P, E, ℓ ), where r ∈ Rπ(d, G, B, M, {w, μ }) for any pebble set M for ( B, d, 1, x ), the minimum weight of a subgraph G′ of G fulfilling r (or ∞ if no such graph exists). Next, we show that Requirement R1 of Theorem 3 holds, that is, there is an index i such that we can remove each layer i + j(k + 1) of a BFS layering for any j. To this end, note that whether subgraph G′ fulfills a request r or not only depends on the subgraph itself and not on the rest of the graph. Thus, all vertices and edges in a partial solution of size at most k are incident to at most k

layers in any BFS layering and if there are more than k layers, then there is an index i such that removing all layers i + j(k + 1) for any j does not invalidate the partial solution. Moreover, no new solutions are created in the process, so Requirement R1 holds for απ = 1 for Minimum-Weight Rooted Simple Minor Request and Minimum-Weight Rooted Simple Minor .The penultimate step is to show that Requirement R4 holds, that is, there is an algorithm that given a separator C = ( Che , C li , C di ) such that ( C, 1) is measured for d and 1, a pebble set M

for ( B, d, 1, x ), and the solutions for instances (d, C in , (( Bhe ∪ Che )[ Cin ], (Bli ∪ Cli )[ Cin ], (Bdi ∪ Cdi )[ Cin ]) , x + 1 , S π[Cin ]) and (d, C ex , (( Bhe ∪ Che )[ Cex ], (Bli ∪ Cli )[ Cex ], (Bdi ∪ Cdi )[ Cex ]) , x + 1 , S π[Cex ]) ,

can compute the solutions for all requests r ∈ Rπ(d, G, B, M, S π) that do not intersect Cdi and intersect Cli in at most 20 √d log( d) vertices in O(γH (d)n2) time. So assume that we are given C,solutions res 1 for the interior instance and res 2 for the exterior instance, and a pebble set M

for ( B, d, 1, x ). We first present the algorithm, then prove its correctness and finally analyze its running time. Let Min and Mex be the subsets of M which appear in Cin and Cex , respectively. We iterate over all possible pebble sets M′ for ( C, d, 1, 1), that is, sets which do not contain any vertices in Cdi 

and at most 20 √d log( d) vertices in Cli . We also iterate over all possible values |M ′| ≤ ℓ′ ≤ ℓ.Let M′ 

> in

= Min ∪ M ′ and M′ 

> ex

= Mex ∪ M ′. In the following, we iterate over all possible requests for res 1 and res 2 and check whether they can be combined. Whenever one of the following conditions does not hold, we simply disregard the iteration and continue with the next. Hence, we implicitly as-sume that all of the following conditions hold. We start by iterating over all pairs of non-isomorphic separations ( X1, Y 1) and ( X2, Y 2) of H of size at most t and check whether H[X1 ∪X2] is isomorphic to H[X] and whether X1 ∩ X2 ⊆ Y1 ∩ Y2. Let Z1 = X1 ∩ Y1, Z2 = X2 ∩ Y2, H[Z1] = ( V1, E 1), and H[Z2] = ( V2, E 2). Next, we iterate over all possible pairs of coloring functions c1 : Z1 → 2M′

> in

and c2 : Z2 → 2M′ 

> ex

that are extensions of μ. We then check whether c(u) ∩ M in ⊆ c1(u) for all u ∈ Z, c(u) ∩ M ex ⊆ c2(u), and whether for all u ∈ U and all v ∈ M ′ it holds that v ∈ c1(u)if and only if v ∈ c2(u). Intuitively, the first two requirements ensure that whenever c assigns a vertex to the model of a particular node u in H, then c1 and c2 also assign the vertex to the model of u whenever the vertex v is part of their domain. The last check ensures that both c1

and c2 agree on the vertices in M′. We also iterate over all pairs of partitions P1 ∈ B (M′

> in

)and P2 ∈ B (M′

> ex

) that refine c1 and c2, respectively. Here, we also perform two checks. First, we check for all nodes u ∈ Z whether P u = P u 

> 1

⊔ P u 

> 2

. Second, for all nodes u ∈ (X1 ∩ X2) \ Y , we check whether P u 

> 1

⊔ P u 

> 2

consists of a single set. Finally, we iterate over all pairs of set E1 ⊆ E1

and E2 ⊆ E2 and check whether E1 ∩ E 2 = E. If all of the above conditions hold, then we com-pute res 1(M′

> in

, (X1, Y 1), c 1, P 1, E1, ℓ ′) + res 2(M′

> ex

, (X2, Y 2), c 1, P 1, E2, ℓ − ℓ′ + |M ′|) and update the 43 result for r if the computed value is smaller than the current entry. This concludes the description of the algorithm. We next show that the algorithm for Requirement R4 is correct. To this end, we first show that an optimal solution is found by the algorithm and then show that whenever the algorithm updates a solution, then this corresponds to a subgraph fulfilling r. So consider the minimum-weight subgraph G′ = ( V ′, E ′) of G such that G′ fulfills request r = ( M, (X, Y ), c, P, E, ℓ ), G′ does not contain any vertices in Cdi , and G′ contains at most 20 √d log( d) vertices from Cli . Moreover, let f be a function that maps X to subsets of V ′ that witnesses that G′ fulfills r. We will show that the value for request r is updated with the weight of G′. To this end, we first describe the iteration that leads to this update and then show that all checks from our algorithm are met and therefore the value is updated. Let Min and Mex be the subsets of M that appear in Cin and Cex , respectively. Let Vin 

and Vex be the vertices in G′ in Cin and Cex , respectively, and let M′ = Vin ∩ Vex be the vertices in G′ in C. Note that M′ ∩ Cdi = ∅ and |M ′ ∩ Cli | ≤ 20 √d log( d) by assumption (since Require-ment R4 only requires us to find solutions that satisfy these two properties). Let M′ 

> in

= Min ∪ M ′

and M′ 

> ex

= Mex ∪ M ′. Let Ein ⊆ E′ be the set of edges in G′ with both endpoints in Vin and let Eex = E′ \ Ein be the set of edges with at least one endpoint in Vex \ Vin . Let G′ 

> in

= ( Vin , E in )and G′ 

> ex

= ( Vex , E ex ). Let Xin and Xex be the set of nodes u ∈ U such that f (u) ∩ Vin ̸ = ∅

and f (u) ∩ Vex ̸ = ∅, respectively. Let Pin be the partitions of M′ 

> in

such that two nodes v1, v 2

appear in the same block in Pin if and only if there exists a u ∈ Xin with v1, v 2 ∈ f (u) and v1

and v2 are in the same connected component in G′

> in

[f (u)]. Let Ein be the set of edges {u1, u 2}

in H[Xin ] such that there is no edge {v1, v 2} in G′ with v1 ∈ f (u1) and v2 ∈ f (u2). Let Yin be the set of nodes u ∈ U such that (i) u / ∈ Xin , (ii) P u 

> in

contains more than one set, or (iii) at least one edge in Ein is incident to u. Let cin : Xin → 2Vin be defined as c(u) = f (u) ∩ M ′

> in

.Let Pex , Eex , Y ex and cex be defined analogously. Finally, let ℓ′ = |Vin |. Note that since |V ′| ≤ ℓ

and |Vin ∩ Vex | = |M ′|, it holds that |Vex | ≤ ℓ − ℓ′ + |M ′|. Let rin = ( M′

> in

, (Xin , Y in ), c in , P in , Ein , ℓ ′)and rex = ( M′

> ex

, (Xex , Y ex ), c ex , P ex , Eex , ℓ − ℓ′ + |M ′|). We next show that the above values satisfy all checks from our algorithm. In particular, we will show that G′ 

> in

and G′ 

> ex

fulfill requirements rin and rex . Some checks are done for the interior and exterior separately and others check combinations of both. Since the arguments for the former will always be identical, we only show them for rin . The first check is whether M′ is a pebble set for ( C, d, 1, 1). Since M′ ∩ Cdi = ∅ and |M ′ ∩ Cli | ≤ 20 √d log( d), the set M′ is indeed considered as a pebble set for ( C, d, 1, 1). The next check in our algorithm is whether H[Xin ∪ Xex ] is isomorphic to H[X] and whether Xin ∩ Xex ⊆ Yin ∩ Yex . Note that f assigns each node in X to a non-empty subset of V ′ by definition and does not assign any vertices to nodes u ∈ U \ X. Since Vin ∪ Vex = V ′,it holds that Xin ⊆ X and any node u ∈ X \Xin is assigned to at least one vertex Vex and is therefore contained in Xex , that is, Xin ∪Xex = X and therefore H[Xin ∪Xex ] = H[X]. Now assume towards a contradiction that Xin ∩Xex ̸ ⊆ Yin ∩Yex . Then, there exists a node u ∈ (Xin ∩Xex )\(Yin ∩Yex ). By the construction of Yin it holds that P u 

> in

contains at most one set and no edge in Ein is incident to u. Thus, 

G′ 

> P

[f (u)] = G′[f (u)] and we can contract all these vertices into a single node, in particular, G′[f (u)] is connected. However, u is not in the domain of cin or cex and f (u) therefore contains no vertices in C. Since C is a separator and u ∈ X1 ∩ X2, it holds that f (u) contains vertices in both Vin \ Vex 

and in Vex \ Vin , a contradiction to G′[f (u)] being connected. Hence, Xin ∩ Xex ⊆ Yin ∩ Yex . Our algorithm then checks whether cin is an extension of μ and whether c(u) ∩ Min ⊆ cin (u). Note that the former holds as f is an extension of μ by definition and cin is an extension of f (note that c

and cin are both extended by f and extensions of f as they assign each node in U to the same set of vertices just restricted to their respective domains). The latter holds as f is an extension of c.We also check that for all u ∈ U and all v ∈ M ′ that v ∈ cin (u) if and only if v ∈ cex (u). Note that 44 this trivially holds as both c1 and c2 are extensions of f , that is, it holds for all u ∈ U and v ∈ M ′

that v ∈ cin (u) if and only if v ∈ f (u) if and only if v ∈ cex (u). Then, we check whether Pin refines cin , whether P u 

> in

⊔ P u 

> ex

= P u for all u ∈ X ∩ Y , and P u 

> in

⊔ P u

> ex

consists of a single set for each u ∈ (Xin ∩ Xex ) \ Y . The first one is true as by definition we never add two vertices v1, v 2 to a common block if there are u1̸ = u2 such that v1 ∈ f (u1) and v2 ∈ f (u2). Since cin refines f , the same also holds for cin and therefore P refines cin . For the second check, consider any u ∈ X ∩ Y and the subgraph G′[f (u)]. By assumption this graph has connected components for c(u) as described by P u. By definition of Pin two nodes v1, v 2 appear in the same block in Pin if and only if there exists a u ∈ Xin with v1, v 2 ∈ f (u) and v1 and v2 are in the same connected component in G′

> in

[f (u)]. Thus G′

> in

[f (u)] has connected components for cin (u) as described by P u 

> in

and the same for cex (u) and P u

> ex

. Since G′[f (u)] is the union of G′

> in

[f (u)] and G′

> ex

[f (u)], Lemma 12 yields that P u 

> in

⊔ P u 

> ex

= P u. For the third check, note that since f witnesses that G′

fulfills r, it holds for all nodes u ∈ X \ Y that G′[f (u)] only has one connected component as u

is not in the domain of P and therefore G′ 

> P

does not add any edges between vertices in f (u) but they are still contracted to a single vertex. As shown above G′

> in

[f (u)] has connected components for cin (u) as described by P u 

> in

and the same for cex (u) and P u

> ex

. By Lemma 12 P u 

> in

⊔ P u 

> ex

describes a connected graph, that is, a partition containing only a single set. The final check of our algorithm is whether Ein ∩ E ex = E. Recall that Ein is the set of edges {u1, u 2} in H[Xin ] such that there is no edge {v1, v 2} in G′ with v1 ∈ f (u1) and v2 ∈ f (u2). For any edge {u1, u 2} ∈ E , it holds that there is no edge {v1, v 2} in G′ with v1 ∈ f (u1) and v2 ∈ f (u2) and thus the same holds for G′

> in

, that is, 

{u1, u 2} ∈ E in . As the same argument also applies to Eex , it holds that E ⊆ E in ∩ E ex . In the other direction, for any edge contained in Ein ∩ E ex it holds that there is no edge {v1, v 2} in G′ 

> in

or in G′

> ex

with v1 ∈ f (u1) and v2 ∈ f (u2). Since the edges in G′ are the union of the edges in G′ 

> in

and G′

> ex

,it also holds that there is no edge in G′ satisfying the above. Thus, the edge has to be contained in E as f does not yet model the edge but f witnesses that G′ satisfies r. All checks are therefore successful. Finally, we show that the correct value for the weight of G′ is computed. To this end, observe that G′ 

> in

and G′ 

> ex

do not share any edges and the union of both graphs is by definition G′. As G′

> in

and G′ 

> ex

fulfill requirements rin and rex as shown above and they do not contain any vertices from Cdi and at most 20 √d log( d) vertices from Cli as G′ has the same property, their weight (or something smaller) is returned by the two queries and their sum is stored. Thus, we return a result that is at most the weight of G, concluding the first direction of the correctness proof. Next, we show the other direction of the correctness proof, that is, we show that whenever we update a solution, then this corresponds to a subgraph of the given weight that fulfills request r.To this end, let r1 = ( M1, (X1, Y 1), c 1, P 1, E1, ℓ 1) and r2 = ( M2, (X2, Y 2), c 2, P 2, E2, ℓ 2) be the requests that are used to update a solution for request r = ( M, (X, Y ), c, P, E, ℓ ). Let G1 = ( V1, E 1)and G2 = ( V2, E 2) be the graphs of minimum weight that fulfill these requests and let f1 and f2 be the functions that witness that the graphs fulfill the requests. We show that G∗ = ( V1 ∪ V2, E 1 ∪ E2)fulfills requirement r. Since the weight of the union is clearly at most the sum of the two individual weights (which is the result we report), this will conclude the proof of correctness. To this end, let M′ = M1 ∩ (Che ∪ Cli ∪ Cdi ). Note that since we queried r1 and r2 for cycle C, it holds that M′ = M2 ∩ M 1 and both G1 and G2 contain precisely the vertices in M from C, that is, no vertices from Cdi and at most 20 √d log( d) vertices from Cli . Moreover, note that ℓ2 = ℓ − ℓ1 + |M ′|

and hence |V1 ∪V2| ≤ ℓ1 +ℓ2 −|M ′| = ℓ. It remains to show that there is a function f that witnesses that ( V1 ∪ V2, E 1 ∪ E2) fulfills request r. To this end, we will consider functions f ′ 

> 1

and f ′ 

> 2

which both have domain X and f ′

> 1

(u) = ∅ if u / ∈ X1 and f ′

> 1

(u) = f1(u) otherwise. The function f ′ 

> 2

is defined analogously and f (u) = f ′

> 1

(u) ∪ f ′

> 2

(u) for all u ∈ X. Note that each vertex in G∗ appears in exactly one set f (u) as the vertices in X1 ∩ X2 are exactly the vertices in M′ and c1 and c2 agree 45 on those vertices by construction. The three requirements we need to show are (i) for each u ∈ X it holds that c(u) ∪ μ(u) ⊆ f (u), (ii) when contracting all edges e in G∗ 

> P

for which there exists a u ∈ X with e ⊆ f (u), then the resulting graph is isomorphic to H[X] − E and the isomorphism maps for each node u ∈ X the vertex that is the result of contracting all vertices in f (u) to u, and (iii) for each u ∈ X, the graph G′[f (u)] has connected components for c(u) as described by P u. Let u ∈ X be any node. For the first requirement, note that each node v ∈ μ(u) is contained in f (u) as both c1 and c2 are extensions of μ and therefore v ∈ f1(u) if u ∈ X1 and otherwise v ∈ f2(u) as X = X1 ∪ X2 by construction. In either case, it holds that v ∈ f (u) and the first requirement is fulfilled. We next show the third requirement. This follows from Lemma 12 as follows. We make a case distinction whether u ∈ X1 ∩ X2 or not. If u / ∈ X1 ∩ X2, then assume without loss of generality that u ∈ X1 \ X2. Then, the following holds as G2 does not contain any vertices in f (u) and, in particular, M′ also does not contain any vertices from c1(u): G∗[f (u)] = G1[f (u)], c(u) = c1(u), and P u = P u 

> 1

. Note that f1 guarantees that G1[f (u)] = G∗[f (u)] has connected components for c1(u) = c(u) as described by P u 

> 1

= P u. So assume next that u ∈ X1 ∩ X2. Then, u is part of the domain of both c1 and c2 and c1(u) ∩ c2(u) = f (u) ∩ M ′. We have that G1[f (u)] has connected components for c1(u) as described by P u 

> 1

for each u ∈ X1 and G2[f (u)] has connected components for c2(u) as described by P u 

> 2

for each u ∈ X2. Moreover, P u = P u 

> 1

⊔ P u 

> 2

by construction. Thus, Lemma 12 states that the union of the two graphs (which is equal to G∗[f (u)]) has connected components for c(u) ⊆ c1(u) ∪ c2(u) \ M ′ as described by P u 

> 1

⊔ P u 

> 2

. This shows that the third requirement is fulfilled. Finally, we prove that the second requirement holds. To this end, notice that f1 witnesses that G1P1 is a model for H[X1] − E 1 = ( X1, E ′

> 1

) and f2 is a witness for the fact that G2P2 is a model for H[X2] − E 2 = ( X2, E ′

> 2

). We show that f witnesses that G∗ 

> P

is a model for H[X] − E .First, we show that G∗ 

> P

[f (u)] is connected for each u ∈ X. Afterwards, we show that each edge in H[X] − E is modeled by G∗ 

> P

. Towards the former, note that G∗ 

> P

[f (u)] is only potentially disconnected if G∗[f (u)] contains a connected component that does not contain any vertex in M

as all these vertices appear in P u and are either contained in the same block or not. In the former case, the vertices appear in the same connected component of G∗[f (u)] as given by the third requirement. In the latter case, an edge is added in G∗ 

> P

and thus the graph is trivially connected. So assume that G∗[f (u)] contains a connected component that does not contain any vertex in M.Note that this implies that u / ∈ Y by the definition of c and f . If u / ∈ X1 ∩ X2, then assume without loss of generality that u ∈ X1 \ X2. Then, P u = P u 

> 1

holds as G2 does not contain any vertices in f (u) and therefore G∗ 

> P

[f (u)] = G1P1 [f (u)] and since f1 witnesses that G1P1 [f (u)] is connected, the same holds for G∗ 

> P

[f (u)]. If u ∈ X1 ∩ X2, then u ∈ (X1 ∩ X2) \ Y as u / ∈ Y . Then, we require that P u 

> 1

⊔ P u 

> 2

consists only of a single set. Since G1[f (u)] and G2[f (u)] have connected components for c1(u) = c2(u) ⊆ M ′ as described by P u 

> 1

and P u 

> 2

and since P u 

> 1

⊔ P u 

> 2

only consists of a single set, Lemma 12 states that the union G∗[f (u)] has only one connected component. Thus, G∗ 

> P

[f (u)] indeed has only one connected component. Next, we show that each edge in H[X] − E is modeled. Since H[X1 ∪ X2] is isomorphic to H[X], it holds for all edges in H[X1] − (E1 ∪ E 2) that they are modeled in G∗. It remains to analyze the edges in E1 \ E 2 (the argument for edges in E2 \ E 1 is symmetric). Note that each such edge is modeled in G2 and therefore also by G∗. Hence, G∗ 

> P

is a model for H[X] − E as witnessed by f

and the second requirement is fulfilled. All three requirements are fulfilled and G∗ therefore fulfills request r, concluding the proof of correctness for our algorithm for Requirement R4. We next analyze the running time of the algorithm for requirement R4. The number of pos-sible pebble sets M′ for ( C, 1) is by Lemma 11 in O(2 80 √d log 2(d)) and they can be compute 46 in O(2 80 √d log 2(d)d) time. Iterating over all ℓ ≤ d adds an additional factor of d. Comput-ing Min , Mex , M′

> in

, and M′ 

> ex

can all easily be done in O(n2) time. We then iterate over all pairs of non-isomorphic separations of size at most t of H, which can be computed in O(|U |3σt(H)) time by Corollary 3. The number of possible pairs is in O(( σt(H)) 2) and for each pair, we can check whether they are isomorphic in O(|U |) time [HW74]. Computing Z1, Z 2, H [Z1], and H[Z2]take O(|U |) time. All coloring functions c1 and c2 can be computed in O(2 66000 √d log 10 (d)d) time by Lemma 15. The number of pairs is in O((2 66000 √d log 10 (d))2) = O(2 132000 √d log 10 (d)) and the check whether they satisfy our three requirements for them takes O(n) time. Next, we iterate over all pairs ( P1, P 2) of partitions for M′ 

> in

and M′ 

> ex

(which can be computed in O(2 66000 √d log 10 (d)) time by Lemma 14), respectively, and compute P u 

> 1

⊔ P u 

> 2

for all u ∈ Z and perform two simple checks. The number of pairs is in O(2 132000 √d log 10 (d)). Note that P u 

> 1

⊔ P u 

> 2

for all u ∈ Z can be computed in one step by computing P1 ⊔ P2 and therefore in O(n2) time. Finally, we iterate over all pairs of subsets E1, E2 of edges in E1 and E2, respectively. Since M′ 

> in

and M′ 

> ex

both have size at most t,the number of edges in E1 and E2 is at most 5t/2, each. Thus, the number of possible combinations is at most 2 5t. Checking whether E1 ∩ E 2 = E can clearly be done in O(n2) time and computing the sum of two numbers and checking whether it is smaller than the current entry takes O(1) time. Thus, the overall running time for our algorithm for Requirement R4 is in 

O(2 80 √d log 2(d)d(n2 + ( |U |3σt(H) + (( σt(H)) 2(|U | + 2 66000 √d log 10 (d)n

+ 2 132000 √d log 10 (d)(n + 2 66000 √d log 10 (d) + 2 132000 √d log 10 (d)(n2 + 2 5t(n2 + 1)))))))) = O(2 80 √d log 2(d)d2132000 √d log 10 (d)2132000 √d log 10 (d)|U |3(σt(H)) 225tn2)

⊆ O (2 264080 √d log 10 (d)d|U |3(σt(H)) 225tn2)= O(γH (d)nρ).

The final step is to present a reduction from Minimum-Weight Rooted Simple Minor to 

Minimum-Weight Rooted Simple Minor Request and analyze the running time. To this end, we apply Lemma 1 with d = k +1 to compute k +1 k-outerplanar graphs. We then solve Minimum-Weight Rooted Simple Minor Request for each of these graphs using Theorem 3 with d = k,portal set B = ( ∅, ∅, ∅), x = 1, and the input weight and root functions. For each such iteration, we query the optimal solution for request ( ∅, (U, ∅), c, ∅, ∅, k ), where c : ∅ → 2V is an empty function. If any iteration finds a solution with weight at most W , then we output yes and otherwise we output no. Note that any subgraph fulfilling the request ( ∅, (U, ∅), c, ∅, ∅, k ) corresponds to a subgraph G′

with at most k vertices that contains H[U ] − ∅ = H as a rooted minor. Since the weight of G′ is at most W , this shows that there is a subgraph of G of weight at most W that contains H as a rooted minor and hence we indeed have a yes-instance. In the other direction, any subgraph of weight at most W that contains H as a rooted minor also fulfills the given request and thus the algorithm is correct. Using Theorem 3, the arguments above, the fact that we may assume 4 |U | ≤ k ≤ n,and t = 12000 √k log 8(k), solving each instance of Minimum-Weight Rooted Simple Minor Request takes time 2 ˜O(√k)2264080 √k log 10 (k)k|U |3(σt(H)) 225tn2.49 ⊆ 2 ˜O(√k)(σt(H)) 2n2.49 .

The time for computing all inputs is in O(kn ) by Lemma 1, because induced subgraphs can be computed in O(n + m) time, and since m ∈ O (n) for planar graphs. The overall running time         

> 4If k < |U|, then the instance is trivially a no-instance as contracting edges can never increase the number of vertices and if k > n , then setting k=nyields an equivalent instance.

47 is therefore in O(nk ) + O(( k + 1)2 ˜O(√k)(σt(H)) 2n2.49 ) = 2 ˜O(√k)(σt(H)) 2n2.49 . This concludes the proof. We next show how to get a subexponential parameterized algorithm when only parameterizing by k and discuss relevant special cases of Minimum-Weight Rooted Simple Minor . Note that we can assume without loss of generality that k ≥ | U | as otherwise the answer is no as any graph with k vertices cannot be a model for H. Combined with Corollary 3 this immediately implies a subexponential algorithm for the parameter k.

Minimum-Weight Subgraph Isomorphism is a special case of Minimum-Weight Rooted Simple Minor where k = |U | and Steiner Partition is the special case where H consists of an independent set I = {u1, u 2 . . . , u t} of size t and μ(ui) = Xi for each i ∈ [t]. The number of non-isomorphic separations ( X, Y ) of size at most t for any graph with t vertices is at most 3 t as the separation selects for each vertex whether it is contained in X, Y , or both. Hence, we get the following corollary from Theorem 6. 

Corollary 4. Let t = 12000 √k log 8(k). Minimum-Weight Rooted Simple Minor can be solved in 2O(k/log( k))n2.49 time for planar graphs, Minimum-Weight Subgraph Isomorphism for planar graphs can be solved in 2 ˜O(√|U |)σt(H)n2.49 time and in 2O(k/log( k))n2.49 time, where σt(H) denotes the number of non-isomorphic separations of H of size at most t, and Steiner Partition can be solved in 2 ˜O(√k)n2.49 time for planar graphs. 

4.4 Parallel Induced Minors 

We next investigate induced minors with parallel contractions. Studying induced minors rather than minors only makes a small difference. We simply replace a subgraph by an induced subgraph and whenever we combine two solutions, we compute all induced edges in the separator and subtract them from the total. Dealing with a maximization problem rather than a minimization problem also poses no noticeable hurdle. The main obstacle are the parallel contractions for two reasons. First and foremost, the number of non-isomorphic separations of a planar multigraph is exponential in its number of vertices even for separations of size zero as we will show. Second, if one tries to keep track of which edges are already part of the model, the number of possibilities is exponential even for just a pair of vertices with many edges between them. Instead, we will keep track for each underlying edge (including self loops) how many parallel edges are already modeled using counting functions .Let us now discuss why the number of non-isomorphic separations of size zero of a planar multigraph H = ( U, F ) is exponential in |U | and how to resolve this issue. Let H consist of |U |/2 pairs of vertices, where the ith pair is connected by i parallel edges. Any separation of size zero partitions these pairs into two subsets and since each such partition is a non-isomorphic separation, the number of such separations is 2 |U |/2. Instead of using |U | as an upper bound on the number of non-isomorphic separations and a lower bound for k, we show that the parameter ψ(H) = max( |U |, 2|F | 

> 5

) can be used in the case of planar multigraphs. To this end, we first show that we can always assume that k ≥ ψ(H). 

Lemma 16. Let G be a simple planar graph and H be a planar multigraph. Any model for H in G

contains at least ψ(H) vertices. Proof. Assume towards a contradiction that a model G′ = ( V ′, E ′) with |V ′| < ψ (H) vertices exists. We make a case distinction whether ψ(H) = |U | or ψ(H) = 2|F |/5. In the former case, note that G′

contains |V ′| ≤ | U | vertices. Since G′ is a model of H, a sequence of (parallel) edge contractions 48 transforms G′ into H. However, the number of vertices reduces with each edge contraction. Thus, any graph H′ for which G′ is a model contains at most |V ′| < |U | vertices, contradicting the fact that G′ is a model for H.Now consider the case where ψ(H) = 2|F |/5. Since G′ is a subgraph of G, it holds that G′ is a simple planar graph and hence 5-degenerate. This implies |E′| ≤ 5|V ′|/2 < 5ψ(H)/2 = 5 2|F | 

> 5

/2 = |F |.Similar to the first case, observe that the number of edges decreases (by one) with each (parallel) edge contraction. This contradicts the fact that G′ is a model for H.We next show that for a planar multigraph H = ( U, F ), the number of non-isomorphic separa-tions of size t ∈ ˜O(p|U |) is in 2 O(ψ(H)/log( ψ(H)) ). Afterwards, we show that this also directly implies that the number of such separations is in 2 O(|U |/log( |U |)) when H is a simple graph (which we relied on in the previous subsection). 

Lemma 17. Given a planar multigraph H = ( U, F ) and a non-negative integer t ∈ O (ψ(H) 23 ),the number σt(H) of non-isomorphic separations of size at most t of H is in 2O(ψ(H)/log( ψ(H)) ). The separations can be enumerated in O(ψ(H)3σt(H)) time. Proof. We follow the proof strategy for simple planar graphs by Bodlaender, Nederlof, and van der Zanden [BNvdZ16] to count the number of non-isomorphic separations ( X, Y ) of H of size at most t. There are  |U |

> t

 ≤ ψ(H)t ∈ ψ(H)O(ψ(H)2/3) possible choices for the separator Z = X ∩ Y and all subsets of U of size at most t can be enumerated in O( |U |

> t

) ⊆ O (σt(H)) time as σt(H) ≥  |U |

> t

.Next, we compute the connected components of H[U \ Z] in O(|U | + |F |) ⊆ O (ψ(H)) time. Let the weight of a connected component C in H[U \ S] denote the number of edges with at least one endpoint in C, that is, the number of edges between two vertices in C plus the number of edges between vertices in C and vertices in Z. A connected component is heavy if its weight is at least log( ψ(H)) /180 and it is light otherwise. Next, we show how to construct a (simple, bipartite, and planar) graph H′ with Z as the vertices on one side of the bipartition and a new vertex set B on the other side. We start with the graph H, contract all connected components in H[U \ Z] into single vertices (the vertices in B), remove all self loops and parallel edges, and remove all edges between two vertices in Z. Note that the above can be computed in O(ψ(H)) time. It is known that there are at most 5 |Z| ≤ 5t vertices in B of degree at least 6. Moreover, there are at most 37 |Z| ≤ 37 t subsets S of Z such that there exists a vertex in B whose neighborhood is exactly S [GHO +13]. We say a connected component of H[U \ Z] is highly connected if the corresponding vertex in H′ has at least six neighbors in Z.Connected components that are light and not highly connected are small .We will next show that the number of non-isomorphic small connected components is at most 37 tψ (H)1/4. Note that a small connected component C is characterized by the following five characteristics: (i) the simple graph GC that is the result of removing parallel edges and self loops, (ii) the set of neighbors in Z that any vertex in C is adjacent to, (iii) for each ver-tex in C the information whether it contains at least one self loop, (iv) for each vertex v in C

the neighbors of v in Z, and (v) the multiplicity of each edge. A result by Amini, Fomin, and Saurabh [AFS12] states that the number of simple planar graphs with at most n vertices is at most 2 36 n and that they can be enumerated in O(2 36 n) time. Since each considered connected component is light, it contains less than log( ψ(H)) /180 edges and since it is connected by defin-ition, it contains at most log( ψ(H)) /180 vertices. The number of options for characteristic (i) is therefore at most 2 36 log( ψ(H)) /180 . Using the known upper bound on the number of possible neigh-borhoods [GHO +13], the number of options for characteristic (ii) is at most 37 t. The number of choices for (iii) and (iv) combined is then 2 6 log( ψ(H)) 180 = 2 log( ψ(H)) /30 as each vertex in C has 2 6

49 choices to be connected to each of the at most 5 neighbors in Z and itself (self loop). The num-ber of ways to distribute the at most log( ψ(H)) /180 edges incident to vertices in C to the set of edges in GC and the edges between vertices in C and vertices in Z such that each edge has mul-tiplicity at least one (the number of options for characteristic (v)) is equivalent to the number integer compositions 5 of the number ⌊log( ψ(H)) /180 ⌋, which is upper-bounded by 2 log( ψ(H)) /180 [Sta97]. Combining all of the above, the number of non-isomorphic small connected components is at most 2 36 log( ψ(H)) /180 · 37 t · 2log( ψ(H)) /30 · 2log( ψ(H)) /180 < 37 t245 log( ψ(H)) 180 = 37 tψ (H)1/4 ∈ O (ψ(H)11 /12 )as t ∈ O (ψ(H)2/3). Given the separator Z = X ∩ Y for unknown X and Y , the number of highly connected connected components in H[U \ Z] is at most 5 t [GHO +13]. We next analyze the number of heavy connected components. Note that the number of edges in H is at most 5ψ(H)/2. Since each heavy connected component is incident to at least log( ψ(H)) /180 edges and there are no edges between two such connected components (because Z is a separator), there are at most 450 ψ(H)/log( ψ(H) heavy connected components in H[U \ Z]. Hence, the number of possible partitions of highly connected and heavy connected components to either X or Y is at most 2 5t2450 ψ(H)/log( ψ(H)) ∈ 2O(ψ(H)) /log( ψ(H)) ).We now combine all previous steps. The number of possible separators Z is in ψ(H)O(ψ(H)2/3).The number of possible partitions of highly connected and heavy connected components to either X

or Y is in 2 O(ψ(H)) /log( ψ(H)) ). The number of non-isomorphic connected components is in O(ψ(H)11 /12 )and we decide for each of them how many are added to X. There are at most ψ(H) vertices in H

and this also upper bounds the total number of connected components in H[U \ Z]. Thus, the number of non-isomorphic separations of H is in 

ψ(H)O(ψ(H)2/3) · 2O(ψ(H)/log( ψ(H)) ) · ψ(H)O(ψ(H)11 /12 ) ⊆ 2O(ψ(H)/log( ψ(H)) ).

It remains to analyze the running time for enumerating all non-isomorphic separations of H.Note that |U | + |F | ∈ O (ψ(H)) by definition of ψ. The time required to iterate over all possible separators Z of size at most t is in O( |U |

> t

). Computing the connected components of H[U \ Z]takes O(ψ(H)) time. Next, we show how to partition all connected components of H[U \ Z] such that for two connected components with vertex sets C1 and C2 it holds that C1 and C2 are in the same block of the partition if and only if G[Z ∪ C1] is isomorphic to G[Z ∪ C2]. To this end, we first compute simple planar graphs G′ 

> i

for each connected component with vertex set Ci such that G[Z ∪ Ci] and G[Z ∪ Cj ] are isomorphic if and only if G′ 

> i

and G′ 

> j

are isomorphic. For each pair of graphs, we then check whether they are isomorphic in O(ψ(H)) time [HW74]. Since the number of connected components is at most |U | ≤ ψ(H), this takes O(ψ(H)3) time for all pairs combined and this immediately yields the desired partition. Note that σt(H) is precisely the sum of the number of combinations of how many connected components from each block of the partition are added to X over all separations Z of size at most t.Moreover, for each possible combination, we can compute the separation ( X, Y ) in O(ψ) time. Thus, the overall time required to compute all non-isomorphic separations of H is in O(ψ(H)3σt(H)). This concludes the proof. Note that when H is a simple graph, then it is 5-degenerate and therefore ψ(H) = |U |. Thus, the lemma also directly implies Corollary 3, which we restate here for convenience. 

Corollary 3. Given a (simple) planar graph H = ( U, F ) and a non-negative integer t ∈ O (|U | 23 ),the number σt(H) of non-isomorphic separations of size at most t of H is in 2O(|U |/log( |U |)). The separations can be enumerated in O(|U |3σt(H)) time.    

> 5An integer composition of an integer nis an ordered sequence of positive integers summing to n.

50 Proof. We show that ψ(H) = |U | for any simple planar graph H = ( U, F ). The corollary then is directly implied by Lemma 17. To this end, assume towards a contradiction that ψ(H)̸ = |U |.This implies that ψ(H) = 2|F |/5 and ψ(H) > |U |. Note that any d-degenerate simple graph with n

vertices contains at most 5n/2 edges. Hence, |F | ≤ 5|U |/2. This yields ψ(H) ≤ 2|F |/5 ≤ 2 5|U | 

> 2

/5 = |U |,a contradiction. We next define counting functions. Intuitively, these return for each edge e in H[X ∩ Y ] a number be and we want to find a model in which exactly be parallel edges between the endpoints of e are modeled. Formally, given a separations ( X, Y ) of a planar multigraph H, let Z = X ∩ Y

and EZ be the set of edges of H[Z] after removing parallel edges (but not self loops). Then, a counting function δ : EZ → N ∪ { 0} is any function such that δ(e) is at most the number of parallel edges with endpoints e in H[X ∩ Y ]. We next analyze the number of possible counting functions for a planar multigraph H.

Lemma 18. Let H = ( U, F ) be a planar multigraph and let Z ⊆ U . The number of possible counting functions for EZ is in 2O(log( ψ(H)) |U |). All such counting functions can be enumerated in 2O(log( ψ(H)) |U |)|U | time. Proof. Since H is planar, the graph resulting from removing parallel edges and self-loops is 5-degenerate and therefore contains at most 5|U |/2 edges. Combined with the at most |U | pos-sible self loops, this yields 7|U |/2 possible edges. For each of these edges, a counting function can assign an integer between 0 and |F | ∈ O (ψ(H)), that is, the number of possible choices is in ( ψ(H)) O(7|U |/2) ⊆ 2O(log( ψ(H)) |U |). We can iterate over all of them efficiently and for each check whether the multiplicity is at most the multiplicity in H in O(|U |) time. We can now present our algorithm for Maximum-Weight Rooted Parallel Induced Minor . Since it is only a slight adaptation of the subexponential parameterized algorithm for 

Minimum-Weight Rooted Simple Minor , we do not present the full (lengthy) proof. Instead, we give a high-level description featuring the key differences between the two algorithms and proofs. 

Theorem 7. Let t = 12000 √k log 8(k). For planar graphs, Maximum-Weight Rooted Paral-lel Induced Minor can be solved in 2 ˜O(√k log( ψ(H))) (σt(H)) 2n2.49 time, where σt(H) denotes the number of non-isomorphic separations of H of size at most t.Proof sketch. A request is a tuple r = ( M, (X, Y ), c, P, δ, ℓ ), where M is a pebble set, ( X, Y )is a separation of H of size at most 12000 √d log 8(d), c is a coloring function that assigns each vertex in M to a node in X ∩ Y (formally each node in X ∩ Y is assigned a non-empty subset of vertices in M), P is a partition of M that refines c, δ is a counting function for H[X ∩ Y ] ( απ is again 1), and ℓ ≤ d is an upper bound on the number of vertices in a solution. For a request r,let Z = X ∩ Y , let HδZ be the subgraph of H[Z] that has edge multiplicities as specified by δ, and let HδX be the subgraph of H[X] that has the same multiplicities as H for all edges incident to at least one node in X \Y and multiplicities as in HδZ for all other edges. Then, an optimal solution for request r corresponds to a set of at most ℓ vertices that induce a subgraph of maximum weight that models HδX exactly after adding the following edges: for each u ∈ Z, choose one representative from each set in P u and add a forest between all representatives for u. We say that a graph models HδX

exactly, if after contracting all vertices in the model of u along a spanning tree, the resulting edge multiplicities exactly coincide with HδX .When combining two solutions along a separator C = ( Che , C li , C di ), we iterate over all pebble sets M′ that contain at most 20 √d log( d) vertices from Cli and no vertices from Cdi . The procedure is similar to the case of Minimum-Weight Rooted Simple Minor Request with two exceptions. 51 First, when combining two solutions, we can no longer assume that the two subgraphs are edge-disjoint. Instead, we subtract the weight of all edges induced by M′ from the total sum as these are precisely the edges that are counted twice. Second, we need to compute which graph the union of two graphs models exactly. Here, we make a case distinction between self loops and other edges. For self loops, we use a slight adaptation of Lemma 13 to compute the number of self loops (the feedback edge number) of the union of two forests represented by two partitions. For each other edge e = {u1, u 2} between two nodes in Z, it is easy to verify that the union of two graphs models exactly δ1(e) + δ2(e) − g(e) parallel edges with endpoints e, where δ1(e) is the number of parallel edges modeled by the first graph, δ2(e) is the number of edges modeled by the second graph, and g(e) is the number of edges induced by M′ that have one endpoint v1 ∈ c1(u1)and one endpoint v2 ∈ c1(u2) (note that we require that v ∈ c1(u) if and only if v ∈ c2(u) for all u ∈ U and v ∈ M ′). Finally, we only consider counting functions for separations with at most t nodes and thus Lemma 18 allows us to enumerate them all in 2 O(log( ψ(H)) √d log 8(d) time. Choosing γH (d) ∈ 2O(log( ψ(H)) √d log 10 (d)) (σt(H)) 2 allows us to also iterate over all pairs of counting functions. The rest of the proof is nearly identical to the proof of Theorem 6. Combined with Lemmas 16 and 17, this also shows that Maximum-Weight Rooted Paral-lel Induced Minor can be solved in 2 O(k/log( k))n2.49 time. 

Corollary 5. For planar input graphs, Maximum-Weight Rooted Parallel Induced Minor 

can be solved in 2O(k/log k)n2.49 time. Proof. We simply compute ψ(H) in O(|U | + |F |) time. If ψ(H) > k , then we return no. This is correct by Lemma 16. Otherwise, the running time presented in Theorem 7 is upper bounded by 2 O(k/log k)n2.49 by Lemma 17. Similar to the previous subsection, Maximum-Weight Induced Subgraph Isomorphism is the special case of Maximum-Weight Rooted Parallel Induced Minor where k = |U |. Note that if H is not a simple graph, then the answer is always no in this case. Moreover, Maximum-Weight Induced Matching is a special case of Maximum-Weight Induced Subgraph Iso-morphism where the number of non-isomorphic separations of size at most 12000 √k log 8(k) is in 2 ˜O(√k). This yields the following. 

Corollary 6. For planar input graphs, Maximum-Weight Induced Subgraph Isomorphism 

can be solved in 2O(|U |/log( |U |))n2.49 time and in 2O(k/log( k))n2.49 time. Maximum-Weight Induced Matching can be solved in 2 ˜O(√k)n2.49 time for planar input graphs. 

# 5 Conclusion 

We introduced a general framework for developing subexponential parameterized algorithms for problems on planar graphs that ask for a set of at most k vertices satisfying some property. The framework is applicable to non-bidimensional problems where the solution can induce a graph with many connected components and it can handle directed graphs and weighted problems as well. We showed how to apply the framework to a selection of problems that were previously unknown to have subexponential parameterized algorithms for planar input graphs. In the case of (unweighted) 

Densest k-Subgraph , this resolves an open problem from the literature. Motivated by our positive result for Maximum-Weight Induced Forest , we conclude with two open problems of our own. Note that Maximum-Weight Induced Forest can also be defined as finding a maximum-weight induced subgraph of size at most k that does not contain a triangle as a minor. We ask the following. 52 Can our algorithm be extended to find an (induced) subgraph of size k that does not contain an arbitrary but fixed graph H as a minor? Is there a subexponential parameterized algorithm to find an (induced) subgraph of size k that excludes all graphs from a fixed family F as minors? Note that if we replace minor in the above questions by subgraph, then the question was recently settled in the affirmative [LPS +25]. 

Acknowledgements. We thank anonymous reviewers of previous versions of this paper for many helpful comments that helped significantly improve the clarity of the paper. They also pointed us to some additional related work like the existing subexponential parameterized algorithm for 

Weighted Independent Set [vN20]. 

# References 

[ABF +02] Jochen Alber, Hans L. Bodlaender, Henning Fernau, Ton Kloks, and Rolf Niedermeier. Fixed parameter algorithms for Dominating Set and related problems on planar graphs. 

Algorithmica , 33(4):461–493, 2002. [AFS12] Omid Amini, Fedor V. Fomin, and Saket Saurabh. Counting subgraphs via homo-morphisms. SIAM Journal on Discrete Mathematics , 26(2):695–717, 2012. [Bak94] Brenda S. Baker. Approximation algorithms for NP-complete problems on planar graphs. Journal of the ACM , 41(1):153–180, 1994. [BDF +24] Matthias Bentert, P˚ al Grøn˚ as Drange, Fedor V. Fomin, Petr A. Golovach, and Tuukka Korhonen. Two-sets cut-uncut on planar graphs. In Proceedings of the 51st Interna-tional Colloquium on Automata, Languages, and Programming (ICALP) , pages 22:1– 22:18. Schloss Dagstuhl - Leibniz-Zentrum f¨ ur Informatik, 2024. [BH98] Hans L. Bodlaender and Torben Hagerup. Parallel algorithms with optimal speedup for bounded treewidth. SIAM Journal on Computing , 27(6):1725–1746, 1998. [Bie15] Therese Biedl. On triangulating k-outerplanar graphs. Discrete Applied Mathematics ,181:275–279, 2015. [BNvdZ16] Hans L. Bodlaender, Jesper Nederlof, and Tom C. van der Zanden. Subexponential time algorithms for embedding H-minor free graphs. In Proceedings of the 43rd In-ternational Colloquium on Automata, Languages, and Programming (ICALP) , pages 9:1–9:14. Schloss Dagstuhl - Leibniz-Zentrum f¨ ur Informatik, 2016. [Bod98] Hans L. Bodlaender. A partial k-arboretum of graphs with bounded treewidth. The-oretical Computer Science , 209(1-2):1–45, 1998. [BT10] Daniel Berend and Tamir Tassa. Improved bounds on bell numbers and on moments of sums of random variables. Probability and Mathematical Statistics , 30(2):185–205, 2010. [CFK +15] Marek Cygan, Fedor V. Fomin, Lukasz Kowalik, Daniel Lokshtanov, D´ aniel Marx, Marcin Pilipczuk, Michal Pilipczuk, and Saket Saurabh. Parameterized Algorithms .Springer, 2015. 53 [CKL +25] Li Chen, Rasmus Kyng, Yang P. Liu, Richard Peng, Maximilian Probst Gutenberg, and Sushant Sachdeva. Maximum flow and minimum-cost flow in almost-linear time. 

Journal of the ACM , 72(3):19:1–19:103, 2025. [DFHT05] Erik D. Demaine, Fedor V. Fomin, MohammadTaghi Hajiaghayi, and Dimitrios M. Thilikos. Subexponential parameterized algorithms on graphs of bounded genus and 

H-minor-free graphs. Journal of the ACM , 52(6):866–893, 2005. [FGIK24] Fedor V. Fomin, Petr A. Golovach, Tanmay Inamdar, and Tomohiro Koana. FPT approximation and subexponential algorithms for covering few or many edges. Inform-ation Processing Letters , 185:106471, 2024. [FLM +22] Fedor V. Fomin, Daniel Lokshtanov, D´ aniel Marx, Marcin Pilipczuk, Michal Pilipczuk, and Saket Saurabh. Subexponential parameterized algorithms for planar and apex-minor-free graphs via low treewidth pattern covering. SIAM Journal on Computing ,51(6):1866–1930, 2022. [GHO +13] Jakub Gajarsk´ y, Petr Hlinen´ y, Jan Obdrz´ alek, Sebastian Ordyniak, Felix Reidl, Peter Rossmanith, Fernando S´ anchez Villaamil, and Somnath Sikdar. Kernelization using structural parameters on sparse graph classes. In Proceedings of the 21st Annual European Symposium on Algorithms (ESA) , pages 529–540. Springer, 2013. [HW74] John E. Hopcroft and J. K. Wong. Linear time algorithm for isomorphism of planar graphs (preliminary report). In Proceedings of the 6th Annual ACM Symposium on Theory of Computing (STOC) , pages 172–184. ACM, 1974. [IP01] Russell Impagliazzo and Ramamohan Paturi. On the complexity of k-SAT. Journal of the ACM , 62(2):367–375, 2001. [Kam07] Frank Kammer. Determining the smallest k such that G is k-outerplanar. In Proceed-ings of the 15th Annual European Symposium on Algorithms (ESA) , pages 359–370. Springer, 2007. [KM] Philip N. Klein and Shay Mozes. Optimization algorithms for planar graphs. Book Draft. URL: https://planarity.org .[Kor21] Tuukka Korhonen. A single-exponential time 2-approximation algorithm for treewidth. In Proceedings of the 62nd IEEE Annual Symposium on Foundations of Computer Science (FOCS) , pages 184–192. IEEE, 2021. [LPS +25] Daniel Lokshtanov, Fahad Panolan, Saket Saurabh, Jie Xue, and Meirav Zehavi. Subex-ponential parameterized algorithms for hitting subgraphs. In Proceedings of the 57th Annual ACM Symposium on Theory of Computing (STOC) , pages 1975–1984. ACM, 2025. [LT80] Richard J. Lipton and Robert Endre Tarjan. Applications of a planar separator the-orem. SIAM Journal on Computing , 9:615–627, 1980. [Ned20] Jesper Nederlof. Detecting and counting small patterns in planar graphs in subexponen-tial parameterized time. In Proccedings of the 52nd Annual ACM SIGACT Symposium on Theory of Computing (STOC) , pages 1293–1306. ACM, 2020. 54 [PPSvL18] Marcin Pilipczuk, Michal Pilipczuk, Piotr Sankowski, and Erik Jan van Leeuwen. Net-work sparsification for Steiner problems on planar and bounded-genus graphs. ACM Transaction on Algorithms , 14(4):53:1–53:73, 2018. [RST94] Neil Robertson, Paul D. Seymour, and Robin Thomas. Quickly excluding a planar graph. Journal of Combinatorial Theory, Series B , 62(2):323–348, 1994. [Sta97] Richard P. Stanley. Enumerative combinatorics . Cambridge University Press, Cam-bridge, 1997. [TvL84] Robert Endre Tarjan and Jan van Leeuwen. Worst-case analysis of set union algorithms. 

Journal of the ACM , 31(2):245–281, 1984. [vN20] Ruben W. A. van Niekerk. Parameterized preprocessing for weighted maximization problems. Master Thesis, Eindhoven University of Technology , 2020. 55