---
title: Log-Concavity and Infinite Log-Concavity of Linear Recurrent Sequences with Linear Coefficients via Companion Matrix Methods
source: arxiv
title_zh: 结合Linear Coefficients 通过 Companion Matrix 方法s的Linear Recurrent Sequences的Log-Concavity与Infinite Log-Concavity
abstract_zh: "这篇论文研究 log-concavity properties 的 real sequences (a_n)_{n \\ge 0} satisfying d-th order linear recurr。\n\n核心做法：Writing recurrence in companion-matrix form \\mathbf{v}_{n+1} = M_n\\,\\mathbf{v}_n 结合 M_n = nA + B，we。\n\n主要结果：用于 class 的 second-order recurrences 结合 constant coefficients，we prove tight (necessary 与 sufficient)。\n\n值得关注的点在于：We leave some further insight in case this criteria break down in full generality。"
abstract_en: "We study log-concavity properties of real sequences $(a_n)_{n \\ge 0}$ satisfying a $d$-th order linear recurrence whose coefficients are linear functions of $n$; the so-called P-recursive (or holonomic) sequences. Writing the recurrence in companion-matrix form $\\mathbf{v}_{n+1} = M_n\\,\\mathbf{v}_n$ with $M_n = nA + B$, we show that the log-concave operator value $\\mathcal{L}(a_n) = b_n \\coloneqq a_n^2 - a_{n+1}a_{n-1}$ is a quadratic form in the state vector $\\mathbf{v}_n$, and identify the matrix $Q_n = Q^{(0)} + nQ^{(1)}$ whose positive semi-definiteness gives a sufficient condition for log-concavity. For the class of second-order recurrences with constant coefficients, we prove a tight (necessary and sufficient) criterion for the sequence to be $\\infty$-log-concave, a consequence of the fact that $\\mathcal{L}(a_n)$ is itself a geometric sequence so that $\\mathcal{L}^2(a_n) = 0$ identically. We obtain analogous tight criteria for sequences fixed by $\\mathcal{L}$, and for P-recursive sequences satisfying a dominant-root asymptotic behaviour. We leave some further insight in case this criteria break down in full generality."
authors: Piero Giacomelli
date: 2026-04-15
journal: math.CO
source_link: "https://arxiv.org/pdf/2604.14391v1"
pdf: "https://arxiv.org/pdf/2604.14391v1"
score: 7.25
tldr: 围绕《结合Linear Coefficients 通过...》；Writing recurrence in companion-...；用于 class 的 second-order recurrences...
tldr_cn: 围绕《结合Linear Coefficients 通过...》；Writing recurrence in companion-...；用于 class 的 second-order recurrences...
tldr_en: "We study log-concavity properties of real sequences $(a_n)_{n \\ge 0}$ satisfying a $d$-th order linear recurrence whose coefficients are linear functions of $n$; the so-called P-recursive (or holonomic) sequences. Writing the recurrence in companion-matrix form $\\mathbf{v}_{n+1} = M_n\\,\\mathbf{v}_n$ with $M_n = nA + B$, we show that the log-concave operator value $\\mathcal{L}(a_n) = b_n \\coloneqq..."
selection_source: fresh_fetch
motivation: "这篇论文研究 log-concavity properties 的 real sequences (a_n)_{n \\ge 0} satisfying d-th order linear recurr。"
method: "核心做法：Writing recurrence in companion-matrix form \\mathbf{v}_{n+1} = M_n\\,\\mathbf{v}_n 结合 M_n = nA + B，we。"
result: 主要结果：用于 class 的 second-order recurrences 结合 constant coefficients，we prove tight (necessary 与 sufficient)。
conclusion: 值得关注的点在于：We leave some further insight in case this criteria break down in full generality。
---

## 摘要
这篇论文研究 log-concavity properties 的 real sequences (a_n)_{n \ge 0} satisfying d-th order linear recurr。

核心做法：Writing recurrence in companion-matrix form \mathbf{v}_{n+1} = M_n\,\mathbf{v}_n 结合 M_n = nA + B，we。

主要结果：用于 class 的 second-order recurrences 结合 constant coefficients，we prove tight (necessary 与 sufficient)。

值得关注的点在于：We leave some further insight in case this criteria break down in full generality。

## Abstract
We study log-concavity properties of real sequences $(a_n)_{n \ge 0}$ satisfying a $d$-th order linear recurrence whose coefficients are linear functions of $n$; the so-called P-recursive (or holonomic) sequences. Writing the recurrence in companion-matrix form $\mathbf{v}_{n+1} = M_n\,\mathbf{v}_n$ with $M_n = nA + B$, we show that the log-concave operator value $\mathcal{L}(a_n) = b_n \coloneqq a_n^2 - a_{n+1}a_{n-1}$ is a quadratic form in the state vector $\mathbf{v}_n$, and identify the matrix $Q_n = Q^{(0)} + nQ^{(1)}$ whose positive semi-definiteness gives a sufficient condition for log-concavity. For the class of second-order recurrences with constant coefficients, we prove a tight (necessary and sufficient) criterion for the sequence to be $\infty$-log-concave, a consequence of the fact that $\mathcal{L}(a_n)$ is itself a geometric sequence so that $\mathcal{L}^2(a_n) = 0$ identically. We obtain analogous tight criteria for sequences fixed by $\mathcal{L}$, and for P-recursive sequences satisfying a dominant-root asymptotic behaviour. We leave some further insight in case this criteria break down in full generality.
