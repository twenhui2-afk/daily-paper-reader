---
title: "AXELRAM: Quantize Once, Never Dequantize"
source: arxiv
title_zh: AXELRAM：Quantize Once，Never Dequantize
abstract_zh: "作者提出 AXELRAM，smart SRAM macro 架构 that computes attention scores directly from quantized KV cache ind。\n\n核心做法：key enabler is design-time fixed codebook：orthogonal-transform-based quantization concentrates each。\n\n主要结果：asymmetric path design -- transform on write，table-lookup on read 结合 no inverse transform -- reduces。\n\n值得关注的点在于：All source code is available at https://github.com/Axelidea/AXELRAM。"
abstract_en: "We propose AXELRAM, a smart SRAM macro architecture that computes attention scores directly from quantized KV cache indices without dequantization. The key enabler is a design-time fixed codebook: orthogonal-transform-based quantization concentrates each coordinate's distribution to N(0,1/d), so the optimal quantizer depends only on dimension d and bit-width b, not on input data. The asymmetric path design -- transform on write, table-lookup on read with no inverse transform -- reduces per-query multiplications by 102.4x (a mathematical identity).   Through multi-seed evaluation (10 seeds x 3 models), we discover that sign pattern sensitivity causes catastrophic PPL spikes (Delta > 50) on certain models (Qwen2.5-3B), while others (LLaMA-3.1-8B) are fully stable. This phenomenon extends SpinQuant's observation of rotation variance in weight quantization to the KV cache domain, where the effect is qualitatively more severe. We trace the root cause to layer-wise norm heterogeneity and propose a gradient-free sign pattern selection (200 candidates, 8 calibration samples, one-time) that eliminates catastrophic spikes with zero additional hardware cost. All source code is available at https://github.com/Axelidea/AXELRAM."
authors: Yasushi Nishida
date: 2026-04-03
source_link: "https://arxiv.org/pdf/2604.02638v1"
pdf: "https://arxiv.org/pdf/2604.02638v1"
score: 6.85
tldr: 围绕《AXELRAM：Quantize Once，Ne...》；key enabler is design-time fixed...；asymmetric path design -- transform...
tldr_cn: 围绕《AXELRAM：Quantize Once，Ne...》；key enabler is design-time fixed...；asymmetric path design -- transform...
tldr_en: "We propose AXELRAM, a smart SRAM macro architecture that computes attention scores directly from quantized KV cache indices without dequantization. The key enabler is a design-time fixed codebook: orthogonal-transform-based quantization concentrates each coordinate's distribution to N(0,1/d), so the optimal quantizer depends only on dimension d and bit-width b, not on input data."
selection_source: fresh_fetch
motivation: 作者提出 AXELRAM，smart SRAM macro 架构 that computes attention scores directly from quantized KV cache ind。
method: 核心做法：key enabler is design-time fixed codebook：orthogonal-transform-based quantization concentrates each。
result: 主要结果：asymmetric path design -- transform on write，table-lookup on read 结合 no inverse transform -- reduces。
conclusion: "值得关注的点在于：All source code is available at https://github.com/Axelidea/AXELRAM。"
---

## 摘要
作者提出 AXELRAM，smart SRAM macro 架构 that computes attention scores directly from quantized KV cache ind。

核心做法：key enabler is design-time fixed codebook：orthogonal-transform-based quantization concentrates each。

主要结果：asymmetric path design -- transform on write，table-lookup on read 结合 no inverse transform -- reduces。

值得关注的点在于：All source code is available at https://github.com/Axelidea/AXELRAM。

## Abstract
We propose AXELRAM, a smart SRAM macro architecture that computes attention scores directly from quantized KV cache indices without dequantization. The key enabler is a design-time fixed codebook: orthogonal-transform-based quantization concentrates each coordinate's distribution to N(0,1/d), so the optimal quantizer depends only on dimension d and bit-width b, not on input data. The asymmetric path design -- transform on write, table-lookup on read with no inverse transform -- reduces per-query multiplications by 102.4x (a mathematical identity).   Through multi-seed evaluation (10 seeds x 3 models), we discover that sign pattern sensitivity causes catastrophic PPL spikes (Delta > 50) on certain models (Qwen2.5-3B), while others (LLaMA-3.1-8B) are fully stable. This phenomenon extends SpinQuant's observation of rotation variance in weight quantization to the KV cache domain, where the effect is qualitatively more severe. We trace the root cause to layer-wise norm heterogeneity and propose a gradient-free sign pattern selection (200 candidates, 8 calibration samples, one-time) that eliminates catastrophic spikes with zero additional hardware cost. All source code is available at https://github.com/Axelidea/AXELRAM.
