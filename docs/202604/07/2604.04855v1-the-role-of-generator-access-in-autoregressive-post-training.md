---
title: The Role of Generator Access in Autoregressive Post-Training
source: arxiv
title_zh: Generator Access in Autoregressive Post-Training的Role
abstract_zh: "这篇论文研究 how generator access constrains autoregressive post-training。\n\n核心做法：central question is whether learner is confined to fresh root-start rollouts or can return to previo。\n\n主要结果：In root-start regime，output sampling，generated-token log probabilities，top-k reports，与 full next-tok。\n\n值得关注的点在于：Changing only generator interface creates exponential gap 用于 KL-regularized outcome-reward post-trai。"
abstract_en: "We study how generator access constrains autoregressive post-training. The central question is whether the learner is confined to fresh root-start rollouts or can return to previously built prefixes and query the next-token rule there. In the root-start regime, output sampling, generated-token log probabilities, top-$k$ reports, and full next-token distributions along sampled trajectories all reduce to one canonical experiment, limited by the on-policy probability of reaching informative prefixes. Weak prefix control breaks this barrier, and once control is available, richer observations such as conditional sampling or logits can outperform top-$1$ access. Changing only the generator interface creates an exponential gap for KL-regularized outcome-reward post-training."
authors: Amit Kiran Rege
date: 2026-04-06
source_link: "https://arxiv.org/pdf/2604.04855v1"
pdf: "https://arxiv.org/pdf/2604.04855v1"
score: 7.5
tldr: 围绕《Generator Access in Auto...》；central question is whether lear...；In root-start regime，output sampling...
tldr_cn: 围绕《Generator Access in Auto...》；central question is whether lear...；In root-start regime，output sampling...
tldr_en: We study how generator access constrains autoregressive post-training. The central question is whether the learner is confined to fresh root-start rollouts or can return to previously built prefixes and query the next-token rule there.
selection_source: fresh_fetch
motivation: 这篇论文研究 how generator access constrains autoregressive post-training。
method: 核心做法：central question is whether learner is confined to fresh root-start rollouts or can return to previo。
result: 主要结果：In root-start regime，output sampling，generated-token log probabilities，top-k reports，与 full next-tok。
conclusion: 值得关注的点在于：Changing only generator interface creates exponential gap 用于 KL-regularized outcome-reward post-trai。
---

## 摘要
这篇论文研究 how generator access constrains autoregressive post-training。

核心做法：central question is whether learner is confined to fresh root-start rollouts or can return to previo。

主要结果：In root-start regime，output sampling，generated-token log probabilities，top-k reports，与 full next-tok。

值得关注的点在于：Changing only generator interface creates exponential gap 用于 KL-regularized outcome-reward post-trai。

## Abstract
We study how generator access constrains autoregressive post-training. The central question is whether the learner is confined to fresh root-start rollouts or can return to previously built prefixes and query the next-token rule there. In the root-start regime, output sampling, generated-token log probabilities, top-$k$ reports, and full next-token distributions along sampled trajectories all reduce to one canonical experiment, limited by the on-policy probability of reaching informative prefixes. Weak prefix control breaks this barrier, and once control is available, richer observations such as conditional sampling or logits can outperform top-$1$ access. Changing only the generator interface creates an exponential gap for KL-regularized outcome-reward post-training.
