---
title: Small Independent Sets versus Small Separator in Geometric Intersection Graphs
source: arxiv
title_zh: Small Independent Sets versus Small Separator in Geometric Intersection Graphs
abstract_zh: "While most classical NP-hard graph problems cannot be solved in time 2^{o (n)} on general graphs und。\n\n核心做法：developed general algorithmic 框架用于 such problems on intersection graphs 的 similarly sized fat object。\n\n主要结果：As concrete examples，we show that problems 2-Subcoloring 与 Two Sets Cut-Uncut exhibit this behavior。\n\n值得关注的点在于：To facilitate design 的 these algorithms，作者引入 new graph parameter，α-modulator number，which generalize。"
abstract_en: "While most classical NP-hard graph problems cannot be solved in time $2^{o(n)}$ on general graphs under the Exponential Time Hypothesis (ETH), many exhibit the square-root phenomenon and admit optimal algorithms running in time $2^{O(\\sqrt{n})}$ on certain geometric intersection graphs, such as planar graphs or unit disk graphs. In 2018, de Berg et al. developed a general algorithmic framework for such problems on intersection graphs of similarly sized fat objects in $\\mathbb{R}^d$, achieving running times of the form $2^{O(n^{1-1/d})}$, along with matching lower bounds under ETH. In this paper, we identify problems that do not exhibit the square-root phenomenon, yet still admit subexponential algorithms on intersection graphs of similarly sized fat objects in $\\mathbb{R}^d$, for every fixed dimension $d \\geqslant 2$. We introduce the notion of a weak square-root phenomenon: problems that can be solved in time $2^{\\tilde{O}(n^{1-1/(d+1)})}$, and for which matching lower bounds hold under ETH. We develop both an algorithmic framework and a corresponding lower bound framework. As concrete examples, we show that the problems 2-Subcoloring and Two Sets Cut-Uncut exhibit this behavior. Our algorithms rely on a new win-win structural theorem, which can be informally stated as follows: every such graph admits a sublinear separator whose removal leaves connected components with sublinear independence number. To facilitate the design of these algorithms, we introduce a new graph parameter, the $α$-modulator number, which generalizes both the independence number and the vertex cover number."
authors: "Malory Marin, Rémi Watrigant"
date: 2026-04-29
journal: cs.DS
source_link: "https://arxiv.org/pdf/2604.26533v1"
pdf: "https://arxiv.org/pdf/2604.26533v1"
score: 8.2
tldr: 围绕《Small Independent Sets v...》；developed general algorithmic 框架...；As concrete examples，we show that pr...
tldr_cn: 围绕《Small Independent Sets v...》；developed general algorithmic 框架...；As concrete examples，we show that pr...
tldr_en: "While most classical NP-hard graph problems cannot be solved in time $2^{o(n)}$ on general graphs under the Exponential Time Hypothesis (ETH), many exhibit the square-root phenomenon and admit optimal algorithms running in time $2^{O(\\sqrt{n})}$ on certain geometric intersection graphs, such as planar graphs or unit disk graphs. In 2018, de Berg et al."
selection_source: fresh_fetch
motivation: "While most classical NP-hard graph problems cannot be solved in time 2^{o (n)} on general graphs und。"
method: 核心做法：developed general algorithmic 框架用于 such problems on intersection graphs 的 similarly sized fat object。
result: 主要结果：As concrete examples，we show that problems 2-Subcoloring 与 Two Sets Cut-Uncut exhibit this behavior。
conclusion: 值得关注的点在于：To facilitate design 的 these algorithms，作者引入 new graph parameter，α-modulator number，which generalize。
---

## 摘要
While most classical NP-hard graph problems cannot be solved in time 2^{o (n)} on general graphs und。

核心做法：developed general algorithmic 框架用于 such problems on intersection graphs 的 similarly sized fat object。

主要结果：As concrete examples，we show that problems 2-Subcoloring 与 Two Sets Cut-Uncut exhibit this behavior。

值得关注的点在于：To facilitate design 的 these algorithms，作者引入 new graph parameter，α-modulator number，which generalize。

## Abstract
While most classical NP-hard graph problems cannot be solved in time $2^{o(n)}$ on general graphs under the Exponential Time Hypothesis (ETH), many exhibit the square-root phenomenon and admit optimal algorithms running in time $2^{O(\sqrt{n})}$ on certain geometric intersection graphs, such as planar graphs or unit disk graphs. In 2018, de Berg et al. developed a general algorithmic framework for such problems on intersection graphs of similarly sized fat objects in $\mathbb{R}^d$, achieving running times of the form $2^{O(n^{1-1/d})}$, along with matching lower bounds under ETH. In this paper, we identify problems that do not exhibit the square-root phenomenon, yet still admit subexponential algorithms on intersection graphs of similarly sized fat objects in $\mathbb{R}^d$, for every fixed dimension $d \geqslant 2$. We introduce the notion of a weak square-root phenomenon: problems that can be solved in time $2^{\tilde{O}(n^{1-1/(d+1)})}$, and for which matching lower bounds hold under ETH. We develop both an algorithmic framework and a corresponding lower bound framework. As concrete examples, we show that the problems 2-Subcoloring and Two Sets Cut-Uncut exhibit this behavior. Our algorithms rely on a new win-win structural theorem, which can be informally stated as follows: every such graph admits a sublinear separator whose removal leaves connected components with sublinear independence number. To facilitate the design of these algorithms, we introduce a new graph parameter, the $α$-modulator number, which generalizes both the independence number and the vertex cover number.
