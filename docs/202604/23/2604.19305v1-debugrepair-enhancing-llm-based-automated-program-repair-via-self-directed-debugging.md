---
title: "DebugRepair: Enhancing LLM-Based Automated Program Repair via Self-Directed Debugging"
source: arxiv
title_zh: 通过Self-Directed Debugging实现的DebugRepair：Enhancing LLM-Based Automated Program Repair
abstract_zh: "To address this limitation，作者提出 DebugRepair，self-directed debugging 框架用于 LLM-based APR。\n\n核心做法：Existing feedback-based APR 方法s iteratively refine candidate patches 使用 test execution feedback 与 ha。\n\n主要结果：However，most rely on outcome-level failure symptoms，such as stack traces，which show how failures are。\n\n值得关注的点在于：Ablation studies further confirm effectiveness 的 all components。"
abstract_en: "Automated Program Repair (APR) has benefited from the code understanding and generation capabilities of Large Language Models (LLMs). Existing feedback-based APR methods iteratively refine candidate patches using test execution feedback and have shown promising results. However, most rely on outcome-level failure symptoms, such as stack traces, which show how failures are observed but fail to expose the intermediate runtime states critical for root-cause analysis. As a result, LLMs often infer bug causes without sufficient runtime evidence, leading to incorrect patches. To address this limitation, we propose DebugRepair, a self-directed debugging framework for LLM-based APR. DebugRepair enhances patch refinement with intermediate runtime evidence collected through simulated debugging. It consists of three components: test semantic purification, simulated instrumentation, and debugging-driven conversational repair. Together, they reduce noisy test context, collect runtime traces through targeted debugging statements with rule-based fallback, and progressively refine candidate patches using prior attempts and newly observed runtime states. We evaluate DebugRepair on three benchmarks across Java and Python. Experiments show that DebugRepair achieves state-of-the-art performance against 15 approaches. With GPT-3.5, it correctly fixes 224 bugs on Defects4J, outperforming prior SOTA LLM-based methods by 26.2%. With DeepSeek-V3, it correctly fixes 295 Defects4J bugs, surpassing the second-best baseline by 59 bugs. Across five additional backbone LLMs, DebugRepair improves repair performance by 51.3% over vanilla settings. Ablation studies further confirm the effectiveness of all components."
authors: "Linhao Wu, Yifei Pei, Zhen Yang, Kainan Li, Zhonghang Lu, Hao Tan, Xiran Lyu, Jia Li, Yizhou Chen, Pengyu Xue, Kunwu Zheng, Dan Hao"
date: 2026-04-21
journal: cs.SE
source_link: "https://arxiv.org/pdf/2604.19305v1"
pdf: "https://arxiv.org/pdf/2604.19305v1"
score: 6.6499999999999995
tldr: 围绕《通过Self-Directed Debuggin...》；Existing feedback-based APR 方法s...；However，most rely on outcome-level f...
tldr_cn: 围绕《通过Self-Directed Debuggin...》；Existing feedback-based APR 方法s...；However，most rely on outcome-level f...
tldr_en: Automated Program Repair (APR) has benefited from the code understanding and generation capabilities of Large Language Models (LLMs). Existing feedback-based APR methods iteratively refine candidate patches using test execution feedback and have shown promising results.
selection_source: fresh_fetch
motivation: To address this limitation，作者提出 DebugRepair，self-directed debugging 框架用于 LLM-based APR。
method: 核心做法：Existing feedback-based APR 方法s iteratively refine candidate patches 使用 test execution feedback 与 ha。
result: 主要结果：However，most rely on outcome-level failure symptoms，such as stack traces，which show how failures are。
conclusion: 值得关注的点在于：Ablation studies further confirm effectiveness 的 all components。
---

## 摘要
To address this limitation，作者提出 DebugRepair，self-directed debugging 框架用于 LLM-based APR。

核心做法：Existing feedback-based APR 方法s iteratively refine candidate patches 使用 test execution feedback 与 ha。

主要结果：However，most rely on outcome-level failure symptoms，such as stack traces，which show how failures are。

值得关注的点在于：Ablation studies further confirm effectiveness 的 all components。

## Abstract
Automated Program Repair (APR) has benefited from the code understanding and generation capabilities of Large Language Models (LLMs). Existing feedback-based APR methods iteratively refine candidate patches using test execution feedback and have shown promising results. However, most rely on outcome-level failure symptoms, such as stack traces, which show how failures are observed but fail to expose the intermediate runtime states critical for root-cause analysis. As a result, LLMs often infer bug causes without sufficient runtime evidence, leading to incorrect patches. To address this limitation, we propose DebugRepair, a self-directed debugging framework for LLM-based APR. DebugRepair enhances patch refinement with intermediate runtime evidence collected through simulated debugging. It consists of three components: test semantic purification, simulated instrumentation, and debugging-driven conversational repair. Together, they reduce noisy test context, collect runtime traces through targeted debugging statements with rule-based fallback, and progressively refine candidate patches using prior attempts and newly observed runtime states. We evaluate DebugRepair on three benchmarks across Java and Python. Experiments show that DebugRepair achieves state-of-the-art performance against 15 approaches. With GPT-3.5, it correctly fixes 224 bugs on Defects4J, outperforming prior SOTA LLM-based methods by 26.2%. With DeepSeek-V3, it correctly fixes 295 Defects4J bugs, surpassing the second-best baseline by 59 bugs. Across five additional backbone LLMs, DebugRepair improves repair performance by 51.3% over vanilla settings. Ablation studies further confirm the effectiveness of all components.
