Title: Stacking and clearing in graph pebbling

URL Source: https://arxiv.org/pdf/2604.22341v1

Published Time: Mon, 27 Apr 2026 00:37:41 GMT

Number of Pages: 28

Markdown Content:
STACKING AND CLEARING IN GRAPH PEBBLING 

TAMÁS CSERNÁK AND LAJOS SOUKUP 

Abstract. Suppose that pebbles are distributed on the vertices of a graph 

G. A pebbling step along an edge uv removes two pebbles from u and places one pebble on v. We introduce two new graph parameters: the stacking num-ber stack( G), which is the least integer t such that every configuration with t

pebbles can be transformed by a finite sequence of pebbling steps into a config-uration with all pebbles on a single vertex, and the clearing number clear( G),defined analogously by requiring that from every configuration with t pebbles, all but one pebble can be removed. We prove that stack( G) is defined exactly for connected graphs, and that 

clear( G) is defined exactly for connected non-bipartite graphs. We also estab-lish general upper bounds for these parameters; in particular, we prove that 

stack( G), clear( G) ≤ 2 · | V (G)| · 2diam( G), where diam( G) denotes the diameter of G.Among our exact results are the equalities stack( Kn) = clear( Kn) = n + 1 ,

stack( Km,n ) = 3 max {m, n } + 1 , and stack( Pn) = 2 n − 1. We also establish general lower bounds in terms of the independence number and odd closed walks. For cycles, the situation is more delicate. We prove the lower bounds 

stack( C2n) ≥ 2n+1 − 1, clear( C2n+1 ) ≥ 3 · 2n − 2,

and we formulate the Almost Stacked Hypothesis, motivated by Sjöstrand’s cover pebbling theorem. Assuming this hypothesis, we obtain 

stack( C2n) = 2 n+1 − 1, clear( C2n+1 ) = 3 · 2n − 2.

At present, we do not have a conjecture for the exact value of stack( C2n+1 ).Finally, computational evidence leads us to a conjectural closed formula for the stacking number of a tree in terms of the distances and degrees of the vertices relative to a chosen root. 

1. Introduction 

Let G be a graph whose vertices can carry pebbles. A pebbling step along an edge 

uv removes two pebbles from u and places one pebble on v. We introduce two new graph parameters: the stacking number stack( G), which is the least integer t such that every configuration with t pebbles can be transformed by a finite sequence of pebbling steps into a configuration with all pebbles on a single vertex, and the clearing number clear( G), defined analogously by requiring that from every configuration with t pebbles, all but one pebble can be removed. Our main goal is to determine when these parameters are defined, to derive general bounds for them, and to compute them exactly for several natural families of graphs. 

History. The pebbling number π(G), introduced in [2], is the least integer m

such that from every configuration of m pebbles on G one can move a pebble to any prescribed vertex. Graph pebbling has since developed in several directions, 

Date : April 27, 2026. 2020 Mathematics Subject Classification. Primary 05C57; Secondary 05C05, 05C38. 

Key words and phrases. graph pebbling, stacking number, clearing number, cover pebbling, paths, cycles, trees.  

> 1
> arXiv:2604.22341v1 [math.CO] 24 Apr 2026 2T. CSERNÁK AND L. SOUKUP

including the cover pebbling number [3, 10], the fractional pebbling number [5], and the target pebbling number [3]. In [5], Hurlbert introduced a general framework in which one fixes a family D of target configurations and asks for the least integer m such that every configuration of size m can be transformed into some member of D. From this point of view, the parameters studied here also prescribe a class of admissible final configurations. The difference is that in our setting the final location is not specified: for stacking, the reachable configuration is required only to be supported on a single vertex, and for clearing it is required only to consist of a single pebble. To the best of our knowledge, these parameters have not been investigated before. Another important contribution is Sjöstrand’s cover pebbling theorem [10], which may be formulated in terms of demand functions w : V (G) → ω. It implies that, for positive demands, extremal obstructions may be taken to be stacked configurations. This result motivates the Almost Stacked Hypothesis that we introduce later for our setting, where an almost stacked configuration is one in which all vertices except possibly one carry at most one pebble. The introduction of these new versions of pebbling was motivated by Hurlbert’s seminar talk [6]. We subsequently carried out our own computational experiments, and these helped guide the present work. 

Notations. A configuration on G is a function c : V (G) → N, where c(v) denotes the number of pebbles at v. Its size is 

∥c∥ = X 

> v∈V(G)

c(v),

and its support is 

supp( c) = {v ∈ V (G) : c(v) > 0}.

We write α(G) for the independence number of a graph G. For a configuration c

on G, let 

α(c) = α(G[supp( c)]) ,

that is, the independence number of the subgraph induced by the support of c. Let 

Conf ( G) denote the set of all configurations on G, and let 

Conf t(G) = {c ∈ Conf ( G) : ∥c∥ = t}.

For configurations c, d ∈ Conf ( G), define c ⊕ d by 

(c ⊕ d)( v) = c(v) + d(v) for all v ∈ V (G).

For a nonnegative integer m, the configurations m · c and c ⊖ d are defined in the natural way. For U ⊆ V (G), let c ↾ U denote the restriction of c to U .For v ∈ V (G), let ev = eGv denote the unit configuration at v, that is, the configuration defined by 

ev (v) = 1 and ev (w) = 0 for w ∈ V (G) \ { v}.

If c ∈ Conf ( G) and uv ∈ E(G) with c(u) ≥ 2, then the pebbling step u ⇒ v is 

applicable to c. Its result is the configuration c′ = c[u ⇒ v] defined by 

c′(u) = c(u) − 2, c′(v) = c(v) + 1 , c′(w) = c(w) for w / ∈ { u, v }.

For configurations c, c ′ ∈ Conf ( G) we write 

c 7 → c′

to mean that c′ is reachable from c by a sequence of pebbling steps. When the graph needs to be indicated explicitly, we write G : c 7 → c′. We also define 

Pebl G (c) = {c′ ∈ Conf ( G) : c 7 → c′}.STACKING AND CLEARING IN GRAPH PEBBLING 3

A configuration is stacked if its support has size 1, and it is stacked at v if 

supp( c) = {v}. A configuration is cleared if its size is 1. A configuration c is 

stackable if some stacked configuration lies in Pebl G (c), and clearable if some cleared configuration lies in Pebl G (c).The stacking number stack( G) is the least integer t ≥ 2 such that every configura-tion in Conf t(G) is stackable. The clearing number clear( G) is defined analogously by requiring every configuration in Conf t(G) to be clearable. 

Main results. The paper begins with the structural characterization given in The-orem 2.10. In particular, the stacking number stack( G) is defined if and only if G

is connected, while the clearing number clear( G) is defined if and only if G is con-nected and non-bipartite. The key technical input is Theorem 2.1, showing that on every connected graph, a configuration with at least two pebbles on each vertex is stackable, and on every connected non-bipartite graph such a configuration is even clearable. Combining this with Sjöstrand’s theorem gives, by Theorem 2.10, the upper bounds 

stack( G), clear( G) ≤ 2 · | V (G)| · 2diam( G)

where diam( G) denotes the diameter of G.We next prove several general lower bounds. First, we relate the stacking number to the classical pebbling number by proving in Theorem 3.1 that 

stack( G) ≥ π(G) + 1 

for every finite connected graph G. As an immediate consequence, we obtain the diameter bound stack( G) ≥ 2diam( G) + 1 . We also derive in Theorem 5.1 a lower bound in terms of the independence number, namely 

stack( G) ≥ 3 · α(G) + 1 

for all connected graphs G̸ = K2. For the clearing number, we prove a lower bound for connected non-bipartite graphs in terms of odd-girth-type parameters, where 

odd( G) denotes the maximum, over all vertices v, of the length of a shortest odd closed walk from v to itself, and og( G) denotes the odd girth of G. In Theorem 6.1 we prove 

clear( G) ≥ 3 · 2 odd( G)−12 − 2,

and hence also 

clear( G) ≥ 3 · 2 og( G)−12 − 2.

We then turn to exact computations. For complete graphs we show in Theo-rem 8.1 that 

stack( Kn) = clear( Kn) = n + 1 .

For complete bipartite graphs with at least three vertices we prove in Theorem 8.2 that 

stack( G) = 3 · α(G) + 1 ,

and for complete k-partite graphs with k ≥ 3 we show in Theorem 8.3 that 

clear( G) = stack( G) = max  |V (G)| + 1 , 3 · α(G) + 1 .

For paths we determine the exact stacking number and prove in Theorem 7.4 that 

stack( Pn) = 2 n − 1

for every n ≥ 2.For cycles, the complete picture seems to be more delicate. We prove the lower bounds 

stack( C2n) ≥ 2n+1 − 1, clear( C2n+1 ) ≥ 3 · 2n − 2.4 T. CSERNÁK AND L. SOUKUP 

by Corollaries 7.5 and 6.3. Motivated by Sjöstrand’s cover pebbling theorem, we introduce the Almost Stacked Hypothesis (ASH), which asserts that extremal coun-terexamples may be taken to be almost stacked. Assuming ASH, we aim to prove 

stack( C2n) = 2 n+1 − 1, clear( C2n+1 ) = 3 · 2n − 1.

Finally, we discuss trees. Computer computations suggest a simple explicit for-mula for the stacking number of a tree in terms of the distances and degrees of the vertices relative to a chosen root. This leads to a conjectural expression for stack( T )

for every finite connected tree, and our computations verify this conjecture for all trees with at most seven vertices. 2. Stacking and Clearing: Existence and Bounds 

In this section we characterize when the stacking and clearing numbers are defined, and we derive general upper bounds for them. The main technical input is the Two-Pebble Clearing Theorem. 

Theorem 2.1 (Two-Pebble Clearing Theorem) . Let G be a finite connected graph and let c be a configuration on G such that c(v) ≥ 2 for all v ∈ V (G).(1) The configuration c is stackable. (2) If G is non-bipartite, then c is clearable. 

For part (1) , we reduce to a spanning tree and show that in a tree one can eliminate leaves one by one while preserving at least two pebbles on every remaining vertex. For part (2) , we first gather all pebbles onto an odd cycle and then clear that cycle. 

Tree part. Lemma 2.2 (Leaf reduction) . Let T be a tree, let r be a leaf with unique neighbor 

s, and let c be a configuration on T such that c(v) ≥ 2 for all v ∈ V (T ). Then there exists ˜c ∈ Pebl T (c) such that 

˜c(r) = 0 , ˜c(v) ≥ 2 for all v ∈ V (T ) \ { r},

and every move used from c to ˜c takes place along the edge rs .Proof. Let a = c(r) ≥ 2 and b = c(s) ≥ 2. Apply the move r ⇒ s exactly ⌊a/ 2⌋

times. This produces a configuration c1 with 

c1(r) ∈ { 0, 1}, c1(s) = b + ⌊a/ 2⌋ ≥ 3.

If c1(r) = 0 , we are done. If c1(r) = 1 , then one move s ⇒ r followed by one move 

r ⇒ s yields a configuration ˜c with 

˜c(r) = 0 and ˜c(s) ≥ 2.

All other vertices are unchanged. □

Lemma 2.3 (Tree stacking) . Let T be a finite tree and let c be a configuration on T such that c(v) ≥ 2 for all v ∈ V (T ). Then for every w ∈ V (T ) there exists 

c′ ∈ Pebl T (c) such that 

c′(w) ≥ 2, c′(u) = 0 for all u ∈ V (T ) \ { w}.

Proof. We argue by induction on |V (T )|. The case |V (T )| = 1 is immediate. Fix a leaf r̸ = w with neighbor s. By the previous lemma, there exists ˜c ∈

Pebl T (c) such that ˜c(r) = 0 and 

˜c(v) ≥ 2 for all v ∈ V (T ) \ { r}.STACKING AND CLEARING IN GRAPH PEBBLING 5

Now pass to the smaller tree T − r. By induction, the restriction ˜c ↾ T − r can be stacked at w within T − r. Since the vertex r remains empty, the same pebbling sequence is valid in T . □

Proof of Theorem 2.1(1). Let T be a spanning tree of G and let w ∈ V (T ) be arbitrary. By the preceding lemma, the configuration c ↾ T can be stacked at w

using only edges of T . Since T is a subgraph of G, the same sequence of moves is valid in G. This proves part (1) . □

Cycle part. To deal with clearing, we first recall the imbalance invariant for bi-partite graphs. 

Definition 2.4. Let G be a bipartite graph with bipartition (V0, V 1). For a con-figuration c on G, define 

imb( c) = X

> v∈V0

c(v) − X

> v∈V1

c(v).

Thus, imb( c) depends on the chosen bipartition, which will always be regarded as fixed. 

Lemma 2.5 (Imbalance invariance) . Let G be a bipartite graph with bipartition 

(V0, V 1). If c′ ∈ Pebl G (c), then 

imb( c′) ≡ imb( c) (mod 3) .

Proof. Each pebbling step changes imb( c) by ±3. □

We next analyze odd cycles. 

Lemma 2.6 (Cycle reduction) . Let Cn be a cycle and let c be a configuration on 

Cn such that c(v) ≥ 2 for all v ∈ V (Cn). Then there exists c′ ∈ Pebl Cn (c) such that 

2 ≤ c′(v) ≤ 3 for all v ∈ V (Cn),

and 

c′(u) = 2 for some u ∈ V (Cn).

Proof. Repeatedly perform pebbling moves from vertices carrying at least four peb-bles. This strictly decreases 

Φ( c) = X 

> v∈V(Cn)

max {0, c (v) − 3},

while preserving the inequality c(v) ≥ 2 at every vertex. Hence, the process ter-minates. At termination either some vertex carries two pebbles, in which case we are done, or every vertex carries exactly three pebbles. In the latter case, per-forming one move along each edge of the cycle in cyclic order yields the constant configuration 2. □

Lemma 2.7 (Path clearing) . Let Pk be a path and let c be a configuration on Pk

such that c(v) ≥ 2 for all v ∈ V (Pk). If 

imb( c)̸ ≡ 0 (mod 3) ,

then c is clearable. Proof. By part (1) of the Two-Pebble Clearing Theorem, some stacked configuration is reachable from c. Choose one of minimal size, say d, stacked at a vertex w.If d(w) ≥ 4, then with a neighbor u of w on the path, the three moves 

w ⇒ u, w ⇒ u, u ⇒ w6 T. CSERNÁK AND L. SOUKUP 

produce a smaller stacked configuration, contradicting minimality. Hence, d(w) ≤

3.If d(w) = 2 , one more move reduces the size, again a contradiction. Thus, 

d(w) ∈ { 1, 3}. Since 

imb( d) ≡ imb( c)̸ ≡ 0 (mod 3) 

by Lemma 2.5, the case d(w) = 3 is impossible, because a stack of size three has imbalance ±3 ≡ 0 (mod 3) . Therefore, d(w) = 1 . □

Lemma 2.8 (Odd-cycle clearing) . Let n be odd and let c be a configuration on Cn

such that c(v) ≥ 2 for all v ∈ V (Cn). Then c is clearable. Proof. By the cycle-reduction lemma, we may assume that 

2 ≤ c ≤ 3

pointwise and that c(vk) = 2 for some k.For each j, define 

Sj =

> n−1

X

> i=0

(−1) ic(vj+i) (mod 3) .

Since n is odd, 

Sk+1 ≡ − Sk + 2 c(vk) ≡ − Sk + 1 (mod 3) .

Hence, at least one of Sk and Sk+1 is nonzero modulo 3. Choose j ∈ { k, k + 1 }

such that Sj̸ ≡ 0 (mod 3) , and delete the edge vj−1vj . The resulting graph is a path, and its imbalance is either Sj or −Sj modulo 3. Hence, this path has nonzero imbalance, so the previous lemma applies and clears the configuration. Since all moves use edges of the path, they are also valid on the cycle. □

The last ingredient is a reduction from a general connected non-bipartite graph to an odd cycle. 

Lemma 2.9 (Gathering onto a cycle) . Let G be a connected graph, let C ⊆ G be a cycle, and let c be a configuration on G such that c(v) ≥ 2 for all v ∈ V (G). Then there exists c′ ∈ Pebl G (c) such that 

c′(v) = 0 for v / ∈ V (C), c′(v) ≥ 2 for v ∈ V (C).

Proof. Let H be the set of connected components of G \ V (C). For each H ∈ H ,choose a vertex vH ∈ V (C) adjacent to some vertex of H. Then G[H ∪ { vH }]

is connected. Applying the tree-stacking lemma to a spanning tree of this graph moves all pebbles from H ∪ { vH } onto vH , and throughout this process at least two pebbles remain on vH . After doing this for every H ∈ H , each vertex of C still carries at least two pebbles, and all remaining pebbles lie on C. □

Proof of Theorem 2.1(2). Since G is connected and non-bipartite, it contains an odd cycle C. By the gathering lemma, we may move all pebbles onto C while preserving at least two pebbles on each vertex of C. Lemma 2.8 then clears the resulting configuration to a single pebble. Hence, the original configuration is clear-able. □

Existence and bounds. With the Two-Pebble Clearing Theorem in hand, the proof of the Stacking–Clearing Theorem becomes short. 

Theorem 2.10 (Stacking–Clearing Theorem) . Let G be a finite simple graph. (1) The stacking number stack( G) is defined if and only if G is connected. (2) The clearing number clear( G) is defined if and only if G is connected and non-bipartite. STACKING AND CLEARING IN GRAPH PEBBLING 7

Moreover, whenever these parameters are defined, they satisfy 

stack( G), clear( G) ≤ 2 · | V (G)| · 2diam( G).

Proof. If G is disconnected, then pebbles cannot move between components, so 

stack( G) is undefined. If G is bipartite, then imbalance modulo 3 is preserved, and therefore a configuration with imbalance 0 cannot be reduced to a single pebble. Thus, clear( G) is undefined. Conversely, assume first that G is connected. Let w2 be the constant function 2

on V (G). By Sjöstrand’s theorem, every configuration of size at least 

Γw2 (G) = 2 · max  

> u∈V(G)

X 

> v∈V(G)

2d(u,v )

can be transformed into one with at least two pebbles on each vertex. By the Two-Pebble Clearing Theorem, such a configuration is stackable. Hence, 

stack( G) ≤ Γw2 (G) ≤ 2 · | V (G)| · 2diam( G).

If G is also non-bipartite, the same argument together with part (2) of the Two-Pebble Clearing Theorem gives 

clear( G) ≤ Γw2 (G) ≤ 2 · | V (G)| · 2diam( G).

□

3. The Pebbling Number and the Stacking Number 

We first relate the stacking number to the classical pebbling number. 

Theorem 3.1. For every finite connected graph G, we have 

stack( G) ≥ π(G) + 1 .

Proof. Suppose, toward a contradiction, that 

stack( G) ≤ π(G).

Then there exists a configuration c on G with 

∥c∥ = stack( G) − 1

and a vertex v ∈ V (G) that is not reachable from c.Let ev be the configuration placing one pebble on v. Then 

∥c ⊕ ev ∥ = stack( G),

so the configuration c ⊕ ev is stackable. Let 

c ⊕ ev = c0, c 1, . . . , c r = c′

be a pebbling sequence ending in a stacked configuration c′.Since v is not reachable from c, no pebble from c can ever be moved onto v.Hence, the extra pebble initially placed at v can never take part in a pebbling move, because there is never a second pebble at v with which to move one pebble away from v. Therefore, this pebble remains at v throughout the whole sequence. Since c′ is stacked, its support is a single vertex, so necessarily c′ is stacked at v.In particular, all pebbles of c have been moved to v, contradicting the choice of c

and v.This contradiction proves the theorem. □

Corollary 3.2. For all finite connected graphs G, we have 

stack( G) ≥ 2diam( G) + 1 .8 T. CSERNÁK AND L. SOUKUP 

Proof. By Chung [2, Fact 2], the classical pebbling number satisfies 

π(G) ≥ 2diam( G).

The result follows from Theorem 3.1. □

4. Valuation Method 

Valuations provide a convenient way to prove lower bounds for stacking and clearing numbers. The basic idea is to assign positive weights to the vertices of a graph in such a way that every pebbling move does not increase the total weighted sum of a configuration. Consequently, if a target configuration has larger weighted sum than the initial configuration, then it cannot be reached by pebbling moves. We shall use this method repeatedly in the sequel. 

Definition 4.1. Let G be a finite graph. A valuating function on G is a map 

R : V (G) −→ R>0

such that for every edge uv ∈ E(G),

R(v) ≤ 2R(u).

For an edge uv ∈ E(G) and a valuating function R, we define the loss of a pebbling step u ⇒ v by 

ℓR(u ⇒ v) = 2 R(u) − R(v) ≥ 0.

Thus ℓR(u ⇒ v) measures the amount by which the R-valuation decreases when the move u ⇒ v is performed. For a configuration c on G, the R-valuation (or R-weight ) of c is defined by 

wR(c) = X 

> v∈V(G)

R(v) c(v).

Lemma 4.2. Let G be a finite graph and let R be a valuating function on G. If 

c0 7 → c1, then 

wR(c0) ≥ wR(c1).

Proof. For a single pebbling step u ⇒ v taking c to c′, we have 

wR(c′) − wR(c) = −2R(u) + R(v) ≤ 0,

since R(v) ≤ 2R(u) whenever uv ∈ E(G). Applying this along a pebbling sequence gives the claim. □

Therefore, if c′ ∈ Pebl G (c), then 

wR(c′) ≤ wR(c).

In particular, if a configuration d satisfies wR(d) > w R(c), then d / ∈ Pebl G (c). This simple observation will be the basis of several lower-bound arguments below. 

Lemma 4.3. Let G be a graph, and let c, c ′

> 0

, c ′ 

> 1

be configurations on G such that 

c 7 → c′ 

> 0

⊕ c′

> 1

.

Then there exist configurations c0, c 1 such that 

c0 7 → c′

> 0

, c1 7 → c′

> 1

, c = c0 ⊕ c1.STACKING AND CLEARING IN GRAPH PEBBLING 9

Proof. Fix a pebbling sequence 

c = c(0) 7 → c(1) 7 → · · · 7 → c(ℓ) = c′ 

> 0

⊕ c′

> 1

.

Label the pebbles of c by 1, . . . , ∥c∥. Whenever a pebbling move uses two pebbles, one with label-set A and the other with label-set B, we assign to the new pebble the label-set A ∪ B. Thus at every stage the label-sets of the pebbles form a partition of {1, . . . , ∥c∥} .Let T0 be the union of the label-sets of the pebbles in c′

> 0

, and let T1 be the union of the label-sets of the pebbles in c′

> 1

. Then T0, T 1 form a partition of {1, . . . , ∥c∥} .Let ci be the subconfiguration of c consisting of pebbles whose labels lie in Ti. Then 

c = c0 ⊕ c1.If some move used pebbles whose label-sets intersect both T0 and T1, the result-ing pebble would contain labels from both sets, and so would all its descendants, contradicting the fact that the final pebbles belong either to c′ 

> 0

or to c′

> 1

. Hence, every move uses pebbles from only one of the two parts, and the sequence splits into two subsequences yielding c0 7 → c′ 

> 0

and c1 7 → c′

> 1

. □

5. The Independence Number 

We next derive a general lower bound on stack( G) in terms of the independence number α(G).

Theorem 5.1. If G is a finite connected graph and G̸ = K2, then 

stack( G) ≥ 3 · α(G) + 1 .

Proof. If G is complete and G̸ = K2, then |V (G)| ≥ 3 and α(G) = 1 , so 

stack( G) ≥ | V (G)| + 1 ≥ 4 = 3 · α(G) + 1 .

Thus, we may assume that G is not complete. Let k = α(G), and fix an independent set X ⊆ V (G) with |X| = k. Then k ≥ 2.Define a valuation R : V (G) → { 1, 2} by 

R(w) = 

(

1 if w ∈ X, 

2 if w / ∈ X. 

Recall that wR(d) denotes the R-valuation of the configuration d.Now define a configuration c by 

c(x) = 3 (x ∈ X), c(v) = 0 (v ∈ V (G) \ X).

Then 

∥c∥ = 3 k and wR(c) = 3 k. 

We claim that c is not stackable. Suppose, toward a contradiction, that c′ ∈

Pebl G (c) is stacked. Choose a pebbling sequence 

(c0, . . . , c r )

from c0 = c to cr = c′, and write 

ci+1 = ci[ui ⇒ vi] (i = 0 , . . . , r − 1) .

Let 

I = { i < r : vi ∈ X }.

Since X is independent, every move counted in I comes from a vertex outside X.Hence, for every i ∈ I,

wR(ci) − wR(ci+1 ) = 2 R(ui) − R(vi) = 2 · 2 − 1 = 3 .10 T. CSERNÁK AND L. SOUKUP 

Since c′ is nonempty, we have wR(c′) ≥ 1, and therefore 

3 · | I| = X

> i∈I

 wR(ci) − wR(ci+1 ) ≤ wR(c) − wR(c′) ≤ 3k − 1.

Thus, |I| < k .Since |X| = k, there is a vertex x ∈ X such that no move places a pebble onto x.As c(x) = 3 , the only possible moves involving x are moves out of x, and at most one such move can occur. Hence, 

c′(x) ∈ { 1, 3}.

In particular, c′(x) > 0.Because k ≥ 2, the initial configuration c is not stacked, so the pebbling sequence contains at least one move. Let ur−1 ⇒ vr−1 be the last move. Then c′(vr−1) > 0.Since vr−1̸ = x, the final configuration has a positive pebble count at a vertex distinct from x. But also c′(x) > 0, so c′ is not stacked, a contradiction. Therefore, c is not stackable, and so 

stack( G) ≥ 3k + 1 = 3 · α(G) + 1 .

□

6. The Odd Invariant 

Let G be a connected non-bipartite graph. For each vertex v ∈ V (G), let 

odd( v, G ) denote the length of a shortest odd closed walk from v to itself. We then define 

odd( G) = max {odd( v, G ) : v ∈ V (G)}. (6.1) Thus, odd( G) measures how far a vertex can be from the nearest odd obstruction. The odd girth og( G) is the corresponding minimum, namely 

og( G) = min {odd( v, G ) : v ∈ V (G)}. (6.2) In particular, 

odd( G) ≥ og( G).

The point of the invariant odd( G) is that it yields a general lower bound for 

clear( G) that is stronger than the corresponding bound in terms of odd girth. 

Theorem 6.1. If G is a non-bipartite, connected, finite graph, then 

clear( G) ≥ 3 · 2 odd( G)−12 − 2.

Proof. Let odd( G) = 2 k + 1 , and choose a vertex v0 ∈ V (G) such that 

odd( v0, G ) = 2 k + 1 .

It is enough to show the following Claim. 

Claim. Let m be a positive integer divisible by 3, and let c be the stacked config-uration at v0 with ∥c∥ = m. If m < 3 · 2k, then c is not clearable. 

Proof of the claim. Assume, toward a contradiction, that c is clearable, and choose 

c′ ∈ Pebl G (c) with ∥c′∥ = 1 . Let v∗ be the vertex such that c′(v∗) = 1 .Define 

Uℓ = {v ∈ V (G) : d(v0, v ) = ℓ} (ℓ < k ), Uk = {v ∈ V (G) : d(v0, v ) ≥ k},

and let 

R(v) = 2 ℓ (v ∈ Uℓ, ℓ ≤ k).

Then 

wR(c) = m. STACKING AND CLEARING IN GRAPH PEBBLING 11 

We first note that there is no edge inside Uℓ for any ℓ < k . Indeed, if u, v ∈ Uℓ

with ℓ < k and uv ∈ E(G), then two shortest paths from v0 to u and v, together with the edge uv , form an odd closed walk through v0 of length at most 2ℓ + 1 < 2k + 1 ,contradicting the choice of v0.Let 

c0, . . . , c M

be a pebbling sequence from c to c′, where 

ci+1 = ci[ui ⇒ vi] (i < M ).

Let ei = uivi for i < M , and let F be the graph spanned by the edges ei.Since c′ consists of a single pebble, we have 

vM −1 = v∗.

Subclaim 1. The graph F is not bipartite. 

Proof. If F were bipartite, then by Imbalance invariance Lemma 2.5, since m is divisible by 3,

imb( c′) ≡ imb( c) ≡ 0 (mod 3) .

But ∥c′∥ = 1 , so imb( c′)̸ ≡ 0 (mod 3) , a contradiction. Therefore, there is some index s < M such that 

us, v s ∈ Uk.

Fix the smallest such index s.

Subclaim 2. There exists r̸ = s such that vr = vs.

Proof. Assume the contrary. Then after the move us ⇒ vs, the vertex vs contains exactly one pebble, and since no later move ends at vs, this pebble remains there for the rest of the sequence. Hence, vs = v∗ and s = M − 1.Thus, F contains exactly one edge inside Uk, namely eM −1, and the vertex vM −1

is not incident with any other edge of F . All other edges go between consecutive layers 

U0, U 1, . . . , U k.

Hence, F is bipartite, contradicting Subclaim 1. Fix the smallest r̸ = s such that vr = vs. Let t be the minimal index such that 

ut = vs.Then s, r < t . We can delay the move us ⇒ vs until the move ut ⇒ vt, because 

vs is not used as a source of any move between times s and t. After this delay, the move us ⇒ vs is still applicable at time t − 1, and the resulting configuration is the same as if we had performed us ⇒ vs at time s and then delayed ut ⇒ vt until time t.Hence, we obtain t = s + 1 and so r < s .Since the move us ⇒ vs is applicable at time s, the configuration cs contains at least two pebbles at us. Applying Lemma 4.3 repeatedly to the segment from c to 

cs, we obtain configurations 

c = d1 ⊕ d2 ⊕ d3 ⊕ d4

such that 

d1 7 → eus , d2 7 → eus , d3 7 → evs .

Since R is a valuation, pebbling does not increase wR. Therefore, 

wR(d1) ≥ wR(eus ) = 2 k, wR(d2) ≥ wR(eus ) = 2 k, wR(d3) ≥ wR(evs ) = 2 k.

Hence, 

m = wR(c) = wR(d1) + wR(d2) + wR(d3) + wR(d4) ≥ 2k + 2 k + 2 k = 3 · 2k,12 T. CSERNÁK AND L. SOUKUP 

contradicting m < 3 · 2k. This proves Subclaim 2. By the claim, a configuration of size 3 · 2k − 3 stacked at v0 is not clearable. Therefore, 

clear( G) ≥ (3 · 2k − 3) + 1 = 3 · 2k − 2 = 3 · 2 odd( G)−12 − 2.

□

Since og( G) ≤ odd( G), we immediately obtain the following weaker bound in terms of odd girth. 

Corollary 6.2. If G is a non-bipartite, connected, finite graph, then 

clear( G) ≥ 3 · 2 og( G)−12 − 2.

Applying this to odd cycles yields the following. 

Corollary 6.3. For each natural number n,

clear( C2n+1 ) ≥ 3 · 2n − 2.

Remark . The invariant odd( v, G ) is easily computable. Indeed, define a graph G′

with vertex set V (G) × 2 by declaring (u, i ) and (v, j ) adjacent whenever i̸ = j and 

uv ∈ E(G). Then 

odd( v, G ) = dG′ (( v, 0) , (v, 1)) 

for every v ∈ V (G). Hence, odd( G) and og( G) can be computed in polynomial time by using the equation (6.1) and (6.2), respectively, together with a shortest-path algorithm on G′.7. Stacking Number of Paths 

In this section we determine the stacking number of paths. Whenever Pn denotes a path, we write 

V (Pn) = {v1, . . . , v n}

in natural order. We begin with a simple homomorphism lemma, then investigate a family of extremal configurations yielding the lower bound, derive the exact value of stack( Pn), and conclude with an application to even cycles. Assume that φ : V [G0] ↠ V [G1] is a homomorphism from the graph G0 onto the graph G1. For c ∈ Conf t(G0) define φ(c) ∈ Conf t(G1) by 

φ(c)( w) = X{c(v) : φ(v) = w}.

The following lemma is straightforward. 

Homomorphism Lemma 7.1. Assume that G0 and G1 are graphs, and φ :

V [G0] ↠ V [G1] is a homomorphism from G0 onto G1.(1) G0 : c 7 −→ c′ implies G1 : φ(c) 7 −→ φ(c′).(2) stack( G0) ≤ stack( G1) and clear( G0) ≤ clear( G1).

Definition 7.2. Let n, m ∈ N, and let Pn have vertices v1, . . . , v n in natural order. We denote by cmn the configuration on Pn with m pebbles on v1, one pebble on vn,and no pebbles on the remaining vertices. Equivalently, 

cmn = m · ev1 ⊕ evn .

Theorem 7.3. Let 2 ≤ n and let m < 2n with m ≡ 2n (mod 3) . Then the configuration cmn is not stackable. STACKING AND CLEARING IN GRAPH PEBBLING 13 

Proof. We argue by induction on n.

Base case. If n = 2 , then m < 4 and m ≡ 4 (mod 3) , so m = 1 . Hence, 

c12 = (1 , 1) ,

which is not stackable. 

Induction step. Assume the statement holds for n, and let 

c = cmn+1 ,

where 

m < 2n+1 and m ≡ 2n+1 (mod 3) .

Suppose, toward a contradiction, that there exists a stacked configuration c′ ∈

Pebl Pn+1 (c). Fix a pebbling sequence 

c = c0, c 1, . . . , c r = c′, ci+1 = ci[ui ⇒ wi] (i < r ).

Then 

supp( c′) = {wr−1}.

Step 1: a valuation bound at the right endpoint. Define a valuation R on 

Pn+1 by 

R(vi) = 2 i−1 (1 ≤ i ≤ n + 1) .

Then 

wR(c) = m + 2 n < 2n+1 + 2 n = 3 · 2n.

Since pebbling does not increase valuations, every c∗ ∈ Pebl Pn+1 (c) satisfies 

wR(c∗) < 3 · 2n.

Therefore, 

c∗(vn+1 ) ≤ 2 (7.1) for every c∗ ∈ Pebl Pn+1 (c).

Step 2: the final stack is not at vn+1 . We may assume that 

supp( c′)̸ = {vn+1 }.

Indeed, if supp( c′) = {vn+1 }, then by (7.1) we have c′(vn+1 ) ∈ { 1, 2}. If c′(vn+1 ) = 1, then cr−1 is stacked at vn. If c′(vn+1 ) = 2 , then one move vn+1 ⇒ vn pro-duces a configuration stacked at vn. In either case we may replace c′ by a stacked configuration whose support is not {vn+1 }.

Step 3: the first move into and the first move out of vn+1 . Since c(vn+1 ) = 1 

and c′(vn+1 ) = 0 , the pebble initially placed at vn+1 must eventually be removed. In order to remove a pebble from vn+1 , there must first be a move into vn+1 . Let 

k be the least index such that 

(uk, w k) = ( vn, v n+1 ),

and let ℓ be the least index such that 

(uℓ, w ℓ) = ( vn+1 , v n).

Then k < ℓ .For every i with k ≤ i < ℓ , no move removes a pebble from vn+1 , so ci(vn+1 ) = 2 .Hence, (7.1) implies that 

(ui, w i)̸ = ( vn, v n+1 ) (k ≤ i < ℓ ).

Thus, between the first move into vn+1 and the first move out of vn+1 there is no second move into vn+1 .14 T. CSERNÁK AND L. SOUKUP 

Step 4: partitioning the initial stack. We may write 

m · ev1 ⊕ evn+1 = c0 7 → cℓ = ( c′ 

> ℓ

⊕ evn+1 ) ⊕ evn+1 . (7.2) Since the move vn+1 ⇒ vn does not occur among the first ℓ steps, the extra pebble at vn+1 is untouched during this part of the sequence. Therefore, (7.2) yields 

m · ev1 7 → c′ 

> ℓ

⊕ evn+1 . (7.3) Now Lemma 4.3 splits the initial stack into two parts, one that produces c′ 

> ℓ

and one that produces the extra pebble at vn+1 . Thus, there exist integers m0, m 1 ≥ 0

with m0 + m1 = m such that 

m1ev1 7 → c′

> ℓ

, m0ev1 7 → evn+1 .

Since 

cℓ+1 = c′ 

> ℓ

⊕ evn ,

and cℓ+1 7 → c′, we obtain 

m1ev1 ⊕ evn 7 → c′. (7.4) Next apply the Homomorphism Lemma (Lemma 7.1) to the homomorphism φ

from Pn+1 onto Pn that sends vi to vi for 1 ≤ i ≤ n and sends vn+1 to vn−1.Let c′′ = c′ ↾ {v1, . . . , v n}. Then c′′ is stacked. Then from (7.4) we obtain 

cm1 

> n

7 → c′′ .

So 

cm1 

> n

is stackable. (7.5) 

Step 5: the induction contradiction. Because R is a valuation, 

m0 = wR(m0ev1 ) ≥ wR(evn+1 ) = 2 n.

Moreover, Pn+1 is bipartite, so the Imbalance Invariance Lemma applies. Since 

imb( m0ev1 ) = m0, imb( evn+1 ) = ( −1) n ≡ 2n (mod 3) ,

we obtain 

m0 ≡ 2n (mod 3) .

Since m ≡ 2n+1 ≡ − 2n (mod 3) and m0 ≡ 2n (mod 3) , we have 

m1 = m − m0 ≡ − 2n − 2n = −2n+1 ≡ 2n (mod 3) .

Now m1 ≤ m − m0 < 2n+1 − 2n = 2 n, so the inductive hypothesis applies to 

cm1 

> n

. Thus, cm1 

> n

is not stackable, contradicting (7.5). This contradiction completes the induction. □

Theorem 7.4. For every n ≥ 2,

stack( Pn) = 2 n − 1.

Proof. We first prove the lower bound, and then the upper bound by induction on 

n.

Lower bound. Let m = 2 n − 3. Then m < 2n, and clearly m ≡ 2n (mod 3) .Therefore, Theorem 7.3 applies to the configuration cmn , so this configuration is not stackable. Since 

∥cmn ∥ = m + 1 = 2 n − 2,

there exists a non-stackable configuration on Pn of size 2n − 2. Hence, 

stack( Pn) ≥ 2n − 1.

Upper bound. We prove by induction that stack( Pn) ≤ 2n − 1 for all n ≥ 2. The case n = 2 is immediate. STACKING AND CLEARING IN GRAPH PEBBLING 15 

Let c be a non-stacked configuration on Pn+1 with 

∥c∥ ≥ 2n+1 − 1,

and put 

k = c(vn+1 ).

We show that c can be transformed into a configuration c′ such that 

c′(vn+1 ) = 0 and ∥c′∥ ≥ 2n − 1.

Restricting c′ to the first n vertices and applying the induction hypothesis will then show that c is stackable. If k = 2 b is even, apply vn+1 ⇒ vn exactly b times. Then 

c′(vn+1 ) = 0 

and 

∥c′∥ = ∥c∥ − b ≥

 ∥c∥

2



≥ 2n.

If k = 2 b+1 ≥ 5 is odd, first apply vn+1 ⇒ vn exactly b times. Then vn+1 carries one pebble and vn has gained b ≥ 2 pebbles. Then perform the two additional moves 

vn ⇒ vn+1 , vn+1 ⇒ vn,

which clear vn+1 . Since c is not stacked, k ≤ 2n+1 − 3, and so b ≤ 2n − 2. We obtain a configuration c′ with 

c′(vn+1 ) = 0 and ∥c′∥ ≥ (2 n+1 − 1) − (b + 2) ≥ 2n+1 − 1 − 2n = 2 n − 1.

Thus, we can apply the induction hypothesis to c′, and conclude that c is stackable. Assume that k = 1 . Since π(Pn+1 ) = 2 n by Lemma 4.3 we can find configurations 

c0 and c1 such that 

c − evn+1 = c0 ⊕ c1, ∥c0∥ ≤ 2n, c0 7 → evn+1 .

Now consider the configuration c, carry out the pebble moves which transform 

c0 into evn+1 , and let c∗ be the resulting configuration. Then 

c∗ = evn+1 ⊕ c1, c∗(vn+1 ) = 2 , ∥c∗∥ = ∥c∥ − ∥ c0∥ + 1 ≥ 2n+1 − 1 − 2n + 1 = 2 n.

Now carry out a move vn+1 ⇒ vn, which produces a configuration c′ with 

c′(vn+1 ) = 0 and ∥c′∥ ≥ 2n − 1.

Finally, if k = 3 , either c(vn) ≥ 1 and the moves 

vn+1 ⇒ vn, vn ⇒ vn+1 , vn+1 ⇒ vn

clear vn+1 , or c(vn) = 0 and we first use π(Pn) = 2 n−1 to place a pebble on vn and then apply the same three moves. In both cases we obtain c′ with 

c′(vn+1 ) = 0 and ∥c′∥ ≥ 2n − 1.

Thus, in every case c reaches a configuration c′ such that c′(vn+1 ) = 0 and 

∥c′∥ ≥ 2n − 1. Restricting c′ to Pn and applying the induction hypothesis shows that c′ is stackable, and hence so is c. Therefore, 

stack( Pn+1 ) ≤ 2n+1 − 1.

□

We conclude with a simple application of the homomorphism lemma, which yields a lower bound for even cycles. 

Corollary 7.5. For each natural number n,

stack( C2n) ≥ 2n+1 − 1.16 T. CSERNÁK AND L. SOUKUP 

Proof. There is a graph homomorphism from C2n onto Pn+1 . Hence, by Lemma 7.1, we have stack( C2n) ≥ stack( Pn+1 ). Now Theorem 7.4 gives stack( Pn+1 ) = 2 n+1 −1,and the result follows. □

8. Complete multipartite graphs 

A graph G is complete k-partite , for some k ≥ 2, if its vertex set can be parti-tioned as 

V (G) = X1 ˙∪X2 ˙∪ · · · ˙∪Xk

so that G[Xi] is edgeless for each i, and every pair of vertices in distinct parts is adjacent. In this section we determine the stacking and clearing numbers for complete graphs, the stacking number for complete bipartite graphs, and both the stacking and clearing numbers for complete k-partite graphs with k ≥ 3. The proofs for bipartite and multipartite graphs follow a common pattern: we first reduce the norm of the configuration, and then reduce the independence number of its support. 

Theorem 8.1. If n ≥ 3, then 

clear( Kn) = stack( Kn) = n + 1 .

Proof. Clearly 

clear( Kn), stack( Kn) ≥ n + 1 ,

witnessed by the configuration in Conf n(Kn) that assigns one pebble to each vertex. Since every configuration in Conf n+1 (Kn) satisfies ∥c∥ > | supp( c)| and ∥c∥ ≥ 4,it suffices to prove the following claim. 

Claim. If c is a configuration on Kn such that 

∥c∥ > | supp( c)| and ∥c∥ ≥ 4, (8.1) then c is clearable. For ∥c∥ = 4 , up to isomorphism the possibilities are 

(4 , 0, 0, . . . ), (3 , 1, 0, . . . ), (2 , 2, 0, . . . ), (2 , 1, 1, 0, . . . ),

and all of them are clearable. Assume now that ∥c∥ > 4 and that the claim holds for all configurations of smaller norm satisfying the condition (8.1). If there are distinct vertices v, w such that c(v) ≥ 2 and c(w) ≥ 1, let c′ be the result of the pebbling step v ⇒ w. Then 

∥c′∥ = ∥c∥ − 1 ≥ 4 and | supp( c′)| < ∥c′∥.

Hence, c′ satisfies the assumptions of the claim, so c′ is clearable by the inductive assumption, and therefore c is clearable as well. Thus, no such pair v, w exists. Since ∥c∥ > | supp( c)|, there is a vertex w

with c(w) ≥ 2, and therefore w is the unique vertex in supp( c). In particular, 

c(w) = ∥c∥ > 4. Choose any v̸ = w, and let c′ be the result of the pebbling step 

w ⇒ v. Then 

∥c′∥ = ∥c∥ − 1 ≥ 4 and | supp( c′)| = 2 < ∥c′∥.

Again c′ satisfies the assumptions of the claim, so c′ is clearable by the inductive assumption, and hence c is clearable. This proves the claim. Therefore, 

clear( Kn) ≤ n + 1 .

Since every clearable configuration is stackable, we also have stack( Kn) ≤ clear( Kn),which completes the proof. □STACKING AND CLEARING IN GRAPH PEBBLING 17 

Theorem 8.2. If G is a complete bipartite graph with at least three vertices, then 

stack( G) = 3 · α(G) + 1 .

Theorem 8.3. If G is a complete k-partite graph for some k ≥ 3, then 

clear( G) = stack( G) = max  |V (G)| + 1 , 3 · α(G) + 1 .

The key technical statements are the following two propositions. 

Proposition 8.4. Let G be a complete bipartite graph and let c ∈ Conf ( G). If 

∥c∥ ≥ 3 · α(c) + 1 , or  ∥c∥ = 3 · α(c) and supp( c) contains an edge ,

then c is stackable. 

Proposition 8.5. Let G be a complete k-partite graph with k ≥ 3, and let c ∈

Conf ( G). Assume that the following two conditions hold: (1) ∥c∥ ≥ 3 · α(c) + 1 , or ( ∥c∥ = 3 · α(c) and supp( c) contains an edge), and (2) there exists v ∈ V (G) with c(v) ≥ 2 (i.e. ∥c∥ > | supp( c)|). Then c is clearable. Proof of Proposition 8.4. We proceed by induction on m = ∥c∥.If m ≤ 3, then α(c) = 1 and supp( c) contains an edge. Hence m = 3 , G[supp( c)] = 

K2, and c = (2 , 1) up to symmetry. Since (2 , 1) 7 → (0 , 2) is stacked, the claim holds. Assume that m ≥ 4 and that the proposition holds for all configurations of smaller norm. Let V (G) = V1 ˙∪V2 be the bipartition of G, and put 

S = supp( c), Si = S ∩ Vi (i = 1 , 2) .

Without loss of generality, assume |S1| ≥ | S2|. Then 

α(c) = |S1|.

If |S1| = 1 , then |S2| ≤ 1, so G[supp( c)] is either K1 or K2. Since stack( K2) = 3 ,the claim follows. Hence, we may assume that 

|S1| ≥ 2.

Step 1. Reducing the norm. Assume first that 

∥c∥ ≥ 3 · α(c) + 1 = 3 · | S1| + 1 . (8.2) If there is v ∈ S1 with c(v) ≥ 2, choose w ∈ V2, w ∈ S2 provided S2̸ = ∅, and perform v ⇒ w to obtain c′. Then w ∈ supp( c′) and supp( c′) ∩ V1 ⊃ S1 \ { v1}̸ = ∅,

so there is an edge in supp( c′). Moreover, ∥c′∥ = ∥c∥ − 1 ≥ 3 · α(c′). Thus, we can apply the induction hypothesis to c′, which is clearable, and hence c is also clearable. If c ↾ S1 = 1 , then |S2| ≤ | S1| and (8.2) imply that there is v ∈ S2 with 

c(v) ≥ 3. Pick w ∈ S1 and perform v ⇒ w to obtain c′. Then ∥c′∥ = ∥c∥ − 1,

supp( c) = supp( c′), and there is an edge in supp( c′). Thus, we can apply the induction hypothesis to c′, which is clearable, and hence c is also clearable. 

Step 2. Reducing the independence number. Assume now that 

∥c∥ = 3 · α(c) = 3 · | S1| and there is an edge in supp( c).

Then 

S2̸ = ∅.

Step 2.1. The case |S1| > |S2|. We show that one can eliminate at least one vertex from S1.Since S2̸ = ∅ and ∥c∥ = 3 · | S1|, there exists v1 ∈ S1 with c(v1) ≤ 2.18 T. CSERNÁK AND L. SOUKUP 

If c(v1) = 2 , choose v2 ∈ S2 and perform v1 ⇒ v2. Thus, 

supp( c′) = supp( c) \ { v1}, α(c′) = α(c) − 1, ∥c′∥ = ∥c∥ − 1.

If c(v1) = 1 and there exists v2 ∈ S2 with c(v2) ≥ 2, perform v2 ⇒ v1 and then 

v1 ⇒ v2 to obtain c′. Then 

c′(v1) = 0 , α(c′) = α(c) − 1, ∥c′∥ = ∥c∥ − 2.

In both cases supp( c′) contains an edge, so the induction hypothesis applies. Assume now that c(v1) = 1 and c ↾ S2 = 1 . Then there exists v′ 

> 1

∈ S1 with 

c(v′

> 1

) ≥ 3. Pick v2 ∈ S2. Apply the moves 

v′ 

> 1

⇒ v2, v2 ⇒ v1, v1 ⇒ v2

to obtain c′. Then c′(v′

> 1

) ≥ 3 − 2 = 1 , c′(v1) = 0 and c′(v2) = 1 . Thus, supp( c′)

contains an edge, and 

∥c′∥ = ∥c∥ − 3, supp( c′) = supp( c) \ { v1, v 2},

so the induction hypothesis applies again. 

Step 2.2. The case |S1| = |S2|. We show that one can eliminate at least one vertex from S1 and at least one vertex from S2.In this case there exist vertices vi ∈ Si with c(vi) ≤ 2 for i = 1 , 2. Choose vi ∈ Si

with c(vi) = 2 if possible, and pick wi ∈ Si \ { vi}.If c(v1) = c(v2) = 2 , perform 

v1 ⇒ w2, v2 ⇒ w1.

If exactly one of c(v1), c (v2) equals 2, say c(v2) = 2 , perform 

v2 ⇒ v1, v1 ⇒ w2.

If c(v1) = c(v2) = 1 , then there exists w ∈ S with c(w) ≥ 3. Assume w ∈ V1 and perform 

w ⇒ v2, v2 ⇒ v1, v1 ⇒ w2.

In each subcase we obtain a configuration c′ with 

α(c′) = α(c) − 1, ∥c′∥ ≥ ∥ c∥ − 3,

and supp( c′) meeting both parts. Thus, the induction hypothesis applies. □

Proof of Theorem 8.2. Theorem 5.1 gives stack( G) ≥ 3 · α(G) + 1 , while Proposi-tion 8.4 gives the reverse inequality. Hence, stack( G) = 3 · α(G) + 1 . □

Proof of Proposition 8.5. We proceed by induction on m = ∥c∥.If m = 1 or m = 2 , then condition (1) cannot hold, so the statement is trivial. If m = 3 , then α(c) = 1 and supp( c) contains an edge. Hence, G[supp( c)] = K2,and c = (2 , 1, 0) up to symmetry. Thus, the statement clearly holds: 

(2 , 1) → (0 , 2) → (1 , 0) .

Assume now that m ≥ 4 and that the proposition holds for all configurations of smaller norm. Let V (G) = ˙ ∪ki=1 Vi be the multipartition of G, and put 

S = supp( c), Si = S ∩ Vi (i = 1 , . . . , k ).

Without loss of generality assume 

|S1| ≥ | S2| ≥ · · · ≥ | Sk|.

Then 

α(c) = |S1|.STACKING AND CLEARING IN GRAPH PEBBLING 19 

If |S1| = 1 , then |Si| ≤ 1 for all i, so G[supp( c)] is a complete graph. Since 

clear( K| supp( c)|) = | supp( c)| + 1 , the claim follows. Hence we may assume that 

|S1| ≥ 2.

Step 1. Reducing the norm. Assume first that 

∥c∥ ≥ 3 · α(c) + 1 = 3 · | S1| + 1 .

If S = S1, then there exists v ∈ S with c(v) ≥ 4. Choose w ∈ V (G) \ V1 and perform v ⇒ w to obtain c′. Then 

c′(v) ≥ 2, supp( c′) = supp( c) ∪ { w},

so supp( c′) contains an edge, and 

∥c′∥ = ∥c∥ − 1 ≥ 3 · α(c′).

Thus, the induction hypothesis applies to c′.Hence we may assume that S̸ = S1 and so S contains an edge. If there exists v ∈ Si with c(v) ≥ 3 for some i, choose w ∈ S \ Si and perform 

v ⇒ w to obtain c′. Then 

c′(w) ≥ 2, supp( c′) = supp( c), α(c′) = α(c),

and so supp( c′) contains an edge, and 

∥c′∥ = ∥c∥ − 1 ≥ 3 · α(c′).

Thus, the induction hypothesis applies to c′.Hence we may assume that c(v) ≤ 2 for all v ∈ S.If there is exactly one vertex v with c(v) = 2 , then 

| supp( c)| ≥ 3 · | S1|,

and so |S \ Si| ≥ 23 |S|, so every vertex of G[supp( c)] has degree at least |S| 

> 2

. There-fore, G[supp( c)] has a Hamilton cycle by Dirac’s theorem, and starting from the vertex v we can clear all but one pebble along the Hamilton cycle. Assume now that there exist two vertices v1 and v2 with c(vi) = 2 . Let vi ∈ Sji .Since k ≥ 3, we can choose an index j different from j1 and j2, and pick w ∈ Vj ,taking w ∈ Sj if possible. Apply v1 ⇒ w to obtain c′. Then c′(v2) = 2 , c′(w) ≥ 1,and w is adjacent to v2, so the induction hypothesis applies to c′.

Step 2. Reducing the independence number. Assume now that 

∥c∥ = 3 · α(c) = 3 · | S1| and supp( c) contains an edge ,

that is S \ S1̸ = ∅. Then, by 8.5(2), 

|S1| + |S2| + |S3| < ∥c∥ = 3 · | S1|,

and therefore 

|S3| < |S1|.

We show that one can decrease α(c).

Step 2.1. The case |S1| > |S2|.

We show that one can eliminate at least one vertex from S1.Since S \S1̸ = ∅ and ∥c∥ = 3 ·| S1|, there exists v1 ∈ S1 with c(v1) ≤ 2. If possible choose v1 with c(v1) = 2 .If c(v1) = 2 , choose v2 ∈ S \ S1 and perform v1 ⇒ v2 to obtain c′. Then 

supp( c′) = supp( c) \ { v1} and so 

α(c′) = α(c) − 1 and ∥c′∥ = ∥c∥ − 1.20 T. CSERNÁK AND L. SOUKUP 

If c(v1) = 1 and so c ↾ S1 = 1 , then there exists v2 ∈ S \ S1 with c(v2) ≥ 2. In this case perform v2 ⇒ v1 and then v1 ⇒ v2. Then supp( c′) = supp( c) \ { v1} and so 

c′(v1) = 0 , α(c′) = α(c) − 1, ∥c′∥ = ∥c∥ − 2.

In both cases supp( c′) contains an edge, and so the induction hypothesis applies. 

Step 2.2. The case |S1| = |S2| > |S3|.

We show that one can eliminate at least one vertex from S1 and at least one vertex from S2.Since m = 3 · | S1| = 3 · | S2|, there exist vertices vi ∈ Si with c(vi) ≤ 2 for i = 1 , 2.Choose vi with c(vi) = 2 if possible, and choose wi ∈ Si \ { vi}; this is possible because |S1| = |S2| > 1.If c(v1) = c(v2) = 2 , perform 

v1 ⇒ w2, v2 ⇒ w1.

If exactly one of c(v1), c (v2) equals 2, say c(v2) = 2 , perform 

v2 ⇒ v1, v1 ⇒ w2.

If c(v1) = c(v2) = 1 , then there exists w ∈ Sj for some j with c(w) ≥ 3. If 

c(w) = 2 then j ≥ 3. If c(w) = 2 then we can assume that j̸ = 2 . Perform 

w ⇒ v2, v2 ⇒ v1, v1 ⇒ w2.

In each subcase we obtain a configuration c′ with supp( c′) = supp( c) \ { v1, v 2}

and so 

α(c′) = α(c) − 1, ∥c′∥ ≥ ∥ c∥ − 3,

and supp( c′) meeting both V1 and V2. Thus, the induction hypothesis applies. □

Proof of Theorem 8.3. By Theorem 5.1, stack( G) ≥ 3·α(G)+1 , and clearly stack( G) ≥|V (G)| + 1 . Proposition 8.5 gives clear( G) ≤ max  |V (G)| + 1 , 3 · α(G) + 1 . Hence 

clear( G) = stack( G) = max  |V (G)| + 1 , 3 · α(G) + 1 .

□

9. The Almost Stacked Hypothesis 

Motivated by Sjöstrand’s Cover Pebbling Theorem, we introduce the Almost Stacked Hypothesis . Let G be a graph. A configuration c on G is called almost stacked if there exists a vertex v0 ∈ V (G) such that 

c(u) ≤ 1 for all u ∈ V (G) \ { v0}.

We then say that c is almost stacked at v0. We refer to the following statement as the Almost Stacked Hypothesis , abbreviated ASH .

Hypothesis 9.1 (Almost Stacked Hypothesis) . Let G be a finite connected graph. (1) The stack( G) is the least integer t ≥ 2 such that every almost stacked configu-ration in Conf t(G) is stackable. (2) If G is non-bipartite, then clear( G) is the least integer t ≥ 2 such that every almost stacked configuration in Conf t(G) is clearable. 

The intended applications of ASH in this paper mainly concern cycles. Assuming ASH, we aim to prove 

stack( C2n) = 2 n+1 − 1, clear( C2n+1 ) = 3 · 2n − 2.

Without ASH, Corollaries 7.5 and 6.3 yield only the lower bounds 

stack( C2n) ≥ 2n+1 − 1, clear( C2n+1 ) ≥ 3 · 2n − 2.STACKING AND CLEARING IN GRAPH PEBBLING 21 

Pushing lemmas. In this subsection we prove two technical lemmas, the Pull-back Lemma and the Pushforward Lemma, that will be used later when we study 

clear( G) and stack( G). Assume that c is a configuration on a path Pk which is almost stacked at v1. The Pullback Lemma says that if c(v1) is large enough then all the pebbles can be moved to v1. The Pushforward Lemma says that if c(v1) is large enough then one can send an extra pebble into vk and clear all the vertices between v1 and vk.

Lemma 9.2 (Pullback Lemma) . Assume that k ≥ 2 and d : {v2, . . . , v k} → 2.Then there is a natural number 

p( d) ≤ 2k − 2 · | supp( d)| − 2

such that 

Pk : (p( d) · ev1 ⊕ d) 7 → ev1 .

Proof. Let J = {j : 2 ≤ j ≤ k : d(j) = 1 }, j = max( J ∪ { 1}) and 

p(d) = 2 j + X{2i−1 : 2 ≤ i < j ∧ i / ∈ J}. (9.1) First push one pebble to vi for 2 ≤ i < j with i / ∈ J using 2i pebbles from v1.Then put one pebble on vj using 2j pebbles from v1. After these steps we have a configuration c′ = (0 , (1) j−2, 2, (0) k−j ) and Pk : c′ 7 → e1. We need at most p(d)

pebbles in v1 and p(d) ≤ 2k − 2 · | supp( d)| − 2. □

Lemma 9.3 (Pushforward Lemma) . Assume that k ≥ 2 and d : {v2, . . . , v k−1} → 

2. Then there is a natural number 

s( d) ≤ 2k − 2 · | supp( d)|

such that 

Pk : (s( d) · ev1 ⊕ d) 7 → evk .

Proof. Let J = {j : 2 ≤ j ≤ k : d(j) = 1 }, and 

s(d) = 2 + X{2i−1 : 2 ≤ i < k ∧ i / ∈ J}. (9.2) Assume that we have s(d) pebbles on v1. First push one pebble to vi for 2 ≤ i < k 

with i /∈ J using 2i pebbles from v1. After this step we have a configuration 

c′ = (2 , (1) k−2, 0) and Pk : c′ 7 → evk □

Theorem 9.4. For every n ≥ 2, an almost stacked configuration c on C2n is stackable provided 

∥c∥ ≥ 2n+1 − 1.

Proof. Let G = C2n, and write 

V (G) = {v1, v 2, . . . , v 2n}

in cyclic order. By symmetry we may assume that c is almost stacked at v1. Put 

U = {v1, v 2, . . . , v n+1 }, W = {v1, v 2n, v 2n−1, . . . , v n+1 }.

and for H ∈ { U, W } take 

H′ = H \ { vn+1 }, H′′ = H \ { v1, v n+1 }.

Then G[U ] ∼= G[W ] ∼= Pn+1 , G[U ′] ∼= G[W ′] ∼= Pn and G[U ′′ ] ∼= G[W ′′ ] ∼= Pn−1.We distinguish two cases. 

Case 1: c(vn+1 ) = 0 .

Let H ∈ { U, W }. Since G[H′] ∼= Pn and c ↾ H′ is almost stacked at v1, we can apply the Pullback Lemma 9.2 to yield 

pH = p( c ↾ H′′ ) ≤ 2n − 2 · | supp( c ↾ H′′ )| − 2.22 T. CSERNÁK AND L. SOUKUP 

Set sH = | supp( c ↾ H′′ )|.

Because c(vn+1 ) = 0 and c is almost stacked at v1, every vertex in U ′′ ∪ W ′′ 

carries at most one pebble. Hence 

c(v1) = ∥c∥ − sU − sW ≥ 2n+1 − 1 − sU − sW .

On the other hand, 

pU + pW ≤ (2 n − 2 · sU − 2) + (2 n − 2 · sW − 2) = 2 n+1 − 2 · sU − 2 · sW − 4.

Therefore 

c(v1) ≥ pU + pW .

In particular, if we set 

ℓ = c(v1) − pU − pW ,

then ℓ ≥ 0.We now first perform the pebbling sequence on G[U ′] which witnesses 

G[U ′] : pU · ev1 ⊕ c ↾ U ′′ 7 → ev1 .

Then perform the pebbling sequence on G[W ′] which witnesses 

G[W ′] : pW · ev1 ⊕ c ↾ W ′′ 7 → ev1 .

These pebbling steps use pU + pW pebbles from v1 and put 2 pebbles on v1 and clear all the other vertices. So after that the obtained configuration c′ is stacked at 

v1. Therefore c is stackable. 

Case 2: c(vn+1 ) = 1 .

By the Pushforward Lemma 9.3, 

sU = s( c ↾ U ′′ ) ≤ 2n − supp( c ↾ U ′′ ) and G[U ] : sU · e1 ⊕ (c ↾ U ′′ ) 7 → en+1 .

That is, using sU pebbles from v1 we can clear all the pebbles on v2, . . . , v n and put an extra pebble on vn+1 . Carrying out these pebble steps in the original configuration c, we obtain a c′ ∈ Pebl ( c) such that (i) c′ ↾ W ′′ = c ↾ W ′′ ,(ii) c′(vn+1 ) = 2 ,(iii) c′(v1) = c(v1) − sU .Next, using the pebble move vn+1 ⇒ vn+2 we can clear the vertex vn+1 and we obtain the configuration c∗ from c′. Then, 

∥c∗∥ = ∥c′∥ − 1 = ( ∥c∥ − sU − | supp( c ↾ U ′′ )|) − 1 ≥ 2n+1 − 2n − 1 = 2 n − 1,

and supp( c∗) ⊂ W ′. So we can apply Theorem 7.4: since G[W ′] ≡ P n, every configuration of size at least 2n − 1 on G[W ′] is stackable. Therefore c∗, and hence 

c, is stackable. This completes the proof. □

Theorem 9.5. If n ≥ 1, then an almost stacked configuration c on C2n+1 is clear-able provided 

∥c∥ ≥ 3 · 2n − 2.

Proof. If n = 1 , then C3 ∼= K3, and Theorem 8.1 gives 

clear( C3) = 4 = 3 · 21 − 2.

So we may assume that n ≥ 2.Let G = C2n+1 with vertices 

V (G) = {v1, v 2, . . . , v 2n+1 }

in cyclic order, and let c be an almost stacked configuration on G with 

∥c∥ ≥ 3 · 2n − 2.STACKING AND CLEARING IN GRAPH PEBBLING 23 

By symmetry we may assume that c is almost stacked at v1, and after reversing the cyclic order if needed we may also assume 

c(vn+1 ) ≤ c(vn+2 ).

Let 

U = {v1, v 2, . . . , v n+1 }, W = {v1, v 2n+1 , v 2n, . . . , v n+2 },

and 

e = vn+1 vn+2 .

Then 

G[U ] ∼= G[W ] ∼= Pn+1 ,

and e is the unique edge of G not contained in either G[U ] or G[W ].Let 

IU = {vi ∈ U \ { v1} : c(vi) = 1 } and IW = {vi ∈ W \ { v1} : c(vi) = 1 }.

Define a valuation wR on Conf ( G) by 

R(v) = 2 d(v1,v ) (v ∈ V (G)) .

Its restrictions to G[U ] and G[W ] are the standard path valuations with root v1.Since every vertex different from v1 carries at most one pebble, we have 

∥c ↾ U ∥ ≥ 3 · 2n − 2 − | IW | and ∥c ↾ W ∥ ≥ 3 · 2n − 2 − | IU |. (9.3) Let 

X = {v ∈ V (G) : d(v1, v ) is even }, Y = V (G) \ X. 

that is, 

X = {v1, v 3, . . . } ∪ { v2n, v 2n−2, . . . } and Y = {v2, v 4, . . . } ∪ { v2n+1 , v 2n−1, . . . }.

Then G − e is bipartite with bipartition (X, Y ). Moreover, for every configuration 

d on G,

imb( d) = X 

> v∈V(G)

(−1) d(v1,v )d(v) ≡ X 

> v∈V(G)

2d(v1,v )d(v) = wR(d) (mod 3) , (9.4) because 2m ≡ (−1) m (mod 3) for every m. Hence every pebbling sequence in G − e

preserves wR modulo 3.Write 

vU = vn+1 and vW = vn+2 ,

and for H ∈ { U, W } let 

H′ = H \ { vH }, H′′ = H \ { v1, v H }, H∗ = H \ { v1},

Then 

G[H′] ∼= G[H∗] ∼= Pn and G[H′′ ] ∼= Pn−1

We should distinguish four cases. 

Case 1. c(vn+1 ) = 0 and wR(c)̸ ≡ 0 mod 3 .The plan is first to use the Pullback Lemma twice: first for G[U ′], then for G[W ].Let 

pU = p( c ↾ U ′′ ) and p∗ 

> W

= p( c ↾ W ∗).

By Lemma 9.2, 

pU ≤ 2n − 2 and G[U ′] : ( pU · e1 ⊕ c ↾ U ′′ ) 7 → ev1 (9.5) and 

p∗ 

> W

≤ 2n+1 − 2 and G[W ] : ( p∗ 

> W

· ev1 ⊕ c ↾ W ) 7 → e1. (9.6) That is, using pU pebbles from v1 we can clear all the pebbles on vertices in U ′′ 

and put an extra pebble on v1, and using p∗ 

> W

pebbles from v1 we can clear all the pebbles on vertices in W ∗ and put an extra pebble on v1,24 T. CSERNÁK AND L. SOUKUP 

Since pU + p∗ 

> W

≤ 3 · 2n − 2 ≤ c(v1), we have enough pebbles to carry out these pull-backs. Consider the final configuration c′ which is stacked at v1. Since we did not use the edge e,

c′(v1) = wR(c′) ≡ wR(c)̸ = 0 mod 3 .

So c′ is clearable. 

Case 2. c(vn+1 ) = 0 and wR(c) ≡ 0 mod 3 .We want to use the Pushforward Lemma 9.3 two or three times. For H ∈ { U, W }

let 

cH = c ↾ H′′ and sH = s( cH ).

Then 

sH ≤ 2n − 2 · supp( cH ) and G[H] : ( sH · e1 ⊕ cU ) 7 → evH (9.7) Observe that 

ℓ = c(v1) − (sU + sW ) ≥ 2n. (9.8) Indeed, if supp( c) = {v1}, then c(v1) = wR(c) ≡ 0 mod 3 , so c(v1) ≥ 3 · 2n. If 

supp( c)̸ = {v1}, then 

sU + sW ≤ (2 n − 2 · | supp( cU )|) + (2 n − 2 · | supp( cW )|) ≤ 2 · 2n − 2.

Hence (9.8) always holds. Since c = ( sU + sW + ℓ) · ev1 ⊕ cU ⊕ cW ⊕ c(vn+2 ) · evn+2 , by (9.7) we have 

0 ≡ wR(c) ≡ 2 · 2n + c(vn+2 ) · 2n + ℓ mod 3 . (9.9) 

Case 2.a. c(vn+2 ) = 1 .Then ℓ ≡ 0 mod 3 by (9.9). We do the following: first we remove ℓ pebbles from v1 using blocks of 3. After that we have a configuration c∗ such that 

c∗ = ( sU · ev1 ⊕ cU ) ⊕ (sW · ev1 ⊕ cW ) ⊕ evn+2 

Then 

G : c∗ 7 → 2 · evn+2 ⊕ evn+1 ,

and the configuration 2 · en+2 ⊕ en+1 is clearable. 

Case 2.b. c(vn+2 ) = 0 .

Let ℓ∗ = ℓ − 2n ≥ 0. Then 0 ≡ ℓ∗ mod 3 by (9.9) We do the following: first we remove ℓ∗ pebbles from v1 using blocks of 3. After that we have a configuration c∗ such that 

c∗ = ( sU · ev1 ⊕ cU ) ⊕ (sW · ev1 ⊕ cW ) ⊕ 2n · ev1 .

We can use 2n pebbles from v1 to push an extra pebble into vn+1 . Hence 

G : c∗ 7 → 2 · evn+2 ⊕ evn+1 ,

and 2 · evn+2 ⊕ evn+1 is clearable. So c is clearable as well. 

Case 3: c(vn+1 ) = c(vn+2 ) = 1 and wR(c)̸ ≡ 2n (mod 3) .

Let 

cW = c ↾ W ′′ , sW = s( cW ).

By the Pushforward Lemma 9.3, 

sW ≤ 2n − 2 · | supp( cW )| and G[W ] : sW · ev1 ⊕ cW 7 → evn+2 .

Thus, using only edges of G[W ], we can clear all pebbles from W ′′ and send one additional pebble to vn+2 . Since originally c(vn+2 ) = 1 , the vertex vn+2 now contains two pebbles, so we may perform the pebble move 

vn+2 ⇒ vn+1 .STACKING AND CLEARING IN GRAPH PEBBLING 25 

At that point vn+1 contains 2 pebbles. Now let j ∈ { 1, . . . , n } be minimal such that c(vi) > 0 for every i with j < i ≤ n. Then we can perform the pebble moves 

vi+1 ⇒ vi for i = n, n − 1, . . . , j. 

We obtain a configuration c∗ such that 

c∗(vi) = 



c(vi), if 2 ≤ i < j, 

1, if i = j and j > 1,

0, if j < i ≤ 2n + 1 .

Next we pull back the pebbles on v2, . . . , v j to v1. By the Pullback Lemma, this requires at most 

pU = p( c∗ ↾ U ′′ ) ≤ 2n − | supp( c∗ ↾ U ′′ )| − 2

pebbles from v1. Hence we obtain a configuration c′ that is stacked at v1.Since sW + pU ≤ 2n ≤ c(v1), we can carry out these pebbling moves. All pebbling moves above preserve the R-weight modulo 3, except for the move 

vn+2 ⇒ vn+1 . That move removes two pebbles of weight 2n and adds one pebble of the same weight, so it decreases the R-weight by 2n modulo 3. Therefore 

wR(c′) ≡ wR(c) − 2n (mod 3) .

Since wR(c)̸ ≡ 2n (mod 3) , it follows that 

c′(v1) = wR(c′)̸ ≡ 0 (mod 3) .

Hence the stacked configuration c′ is clearable. 

Case 4. c(vn+1 ) = c(vn+2 ) = 1 and wR(c) ≡ 2n (mod 3) .We use the Pushforward Lemma 9.3 three times. For H ∈ { U, W } let 

cH = c ↾ H′′ and sH = s( cH ).

Then 

sH ≤ 2n − 2 · supp( cH ) and G[H] : ( sH · e1 ⊕ cU ) 7 → evH (9.10) As in Case 2, if we set 

ℓ = c(v1) − (sU + sW ),

then 

ℓ ≥ 2n. (9.11) Now let 

ℓ∗ = ℓ − 2n ≥ 0.

Since c = ( cU + cW + 2 n + ℓ) · ev1 ⊕ cU ⊕ cW ⊕ evn+1 ⊕ evn+2 , using (9.10) we have 

2n ≡ wR(c) ≡ 3 · 2n + 2 n + ℓ∗, mod 3 (9.12) and so 

ℓ∗ ≡ 0 (mod 3) .

We first remove the ℓ∗ surplus pebbles from v1 in blocks of three. After that we obtain a configuration c∗ such that 

c∗ = ( sU · ev1 ⊕ cU ) ⊕ (sW · ev1 ⊕ cW ) ⊕ 2n · ev1 ⊕ evn+1 ⊕ evn+2 .

Now apply the Pushforward Lemma to the first two summands, and then use the remaining 2n pebbles at v1 to push one more pebble to vn+2 . This yields 

G : c∗ 7 → 3 · evn+2 ⊕ evn+2 .

The latter configuration is clearable: first apply vn+2 ⇒ vn+1 , then vn+1 ⇒ vn+2 ,and finally vn+2 ⇒ vn+1 . Hence c is clearable. □26 T. CSERNÁK AND L. SOUKUP 

Theorem 9.6. Assume ASH for even cycles. Then, for every n ≥ 2,

stack( C2n) = 2 n+1 − 1.

Proof. By Theorem 9.4, ASH implies stack( C2n) ≤ 2n+1 − 1. Corollary 7.5 gives the reverse inequality. Hence, stack( C2n) = 2 n+1 − 1. □

Theorem 9.7. Assume ASH for odd cycles. Then, for every n ≥ 1,

clear( C2n+1 ) = 3 · 2n − 2.

Proof. By Theorem 9.5, ASH implies clear( C2n+1 ) ≤ 3 · 2n − 2. Corollary 6.3 gives the reverse inequality. Hence, clear( C2n+1 ) = 3 · 2n − 2. □

10. Trees 

Let T be a connected tree and let r ∈ V (T ). Define leaf( r) to be the number of leaves of T different from r. Further, define 

σT (r) = X{deg( v) · 2d(r,v ) : v ∈ V (T ), v = r or deg( v) > 1} + 1 .

Finally, let 

estim( T ) = max {σT (r) + leaf( r) : r ∈ V (T )}.

If c is a configuration on T and r ∈ V (T ), let T (c, r ) denote the minimal subtree of T containing r and supp( c).

Theorem 10.1. Let T be a tree, let r ∈ V (T ), and let c be a configuration on T

that is almost stacked at r. If 

c(r) ≥ σT (c,r )(r),

then c is stackable at r.Proof. We argue by induction on | supp( c)|.If | supp( c)| = 1 , then there is nothing to prove. Assume now that | supp( c)| > 1, and that the statement holds for every config-uration d that is almost stacked at r and satisfies | supp( d)| < | supp( c)|.Write T ′ = T (c, r ). Choose s ∈ supp( c) \ { r} so that k = d(s, r ) is maximal, and let 

n = deg T ′ (s).

Then s is adjacent to n − 1 leaves of T ′. Let x be one of these leaves. First move one pebble to x, using the pebbles at r and along the path from r

to x. This requires at most 2k+1 pebbles from r. Then x contains two pebbles. During this process we may use all pebbles on the path from r to x, so afterwards no pebbles remain on the path between r and s. Now perform the move from x to 

s. Thus, s contains one pebble. If n = 2 , then we are done. Assume that n ≥ 3. Then s is adjacent to at least one further leaf of T ′, distinct from x. Let y be one of these leaves. Using the pebbles at r, we can move one pebble to s. This requires at most 2k pebbles. Then s contains two pebbles. Perform the move from s to y. Thus, y contains two pebbles. Now carry out the move from 

y back to s. Then s again contains one pebble, and y is cleared. Repeating this process for every other leaf of T ′ adjacent to s, we obtain a configuration c′.Altogether we use at most 

2k+1 + (deg( s) − 2)2 k = deg( s) · 2k

pebbles on t, and the resulting configuration c′ is supported on a smaller subtree. Hence, | supp( c′)| < | supp( c)|, and we may apply the induction hypothesis. □

The preceding theorem has the following immediate consequence. STACKING AND CLEARING IN GRAPH PEBBLING 27 

Theorem 10.2. Assume ASH. Then every tree T satisfies 

stack( T ) ≤ estim( T ).

Conjecture 10.3. If T is a finite connected tree, then 

stack( T ) = estim( T ).

Computational results support this conjecture for trees with at most 7 vertices; the corresponding data are available in [11]. 11. History, Motivation and Computational Background 

Our interest in graph pebbling was sparked by Hurlbert’s seminar talk [6]. As a first step, we computed the cover pebbling number and the stacking number for several small concrete graphs. These computational materials are available in the GitHub repository and the Zenodo archive [11], as follows. (1) The file special_graph_pebbling_report.pdf contains our first computa-tions for small concrete graphs. (2) atlas_tree_estimation_report.pdf contains the computations showing that Conjecture 10.3 holds for trees with at most 7 vertices, and (3) atlas_ash_report.pdf contains the computations showing that the Almost Stacked Hypothesis holds for graphs with at most 7 vertices. The experimental work reported in special_graph_pebbling_report.pdf then led us to the general results proved in the present paper. At present, we are unable to formulate a conjecture for stack( C2n+1 ). We have computed the values of stack( C2n+1 ) for the odd cycles C3, C5, C7, C9, and C11 ,obtaining the sequence 

4, 8, 17 , 37 , 77 .

However, these data do not yet suggest a convincing general pattern. The naive method to compute stack( G) would be to inspect, for each m ≥ 1, all configurations of size m, and declare c stackable if either c is already stacked or some pebbling step from c leads to a smaller stackable configuration. This becomes slow because the number of configurations of a fixed size grows rapidly. It is therefore better to work with the complementary family Nm of non-stackable configurations of size m. If d is a configuration of size m + 1 , call c a child of d if d 7 → c in one pebbling step, and call d a parent of c. Then a configuration d of size m + 1 is non-stackable if and only if it is not stacked and every child of d belongs to Nm. Hence, if Nm is known, we can generate Nm+1 by testing the parents of the configurations in Nm. This method is still exponential in the worst case, but it is much faster than the naive method. We used this approach to compute stack( G) and clear( G)

for some graphs. If G is a graph, v ∈ V (G) and c is a configuration with ∥c∥ = | supp( c)| + 1 = 

|V (G)| + 1 , then c is stackable if and only if G has a Hamiltonian path starting at v.However, we do not know if it is NP-complete to decide if stack( G) = |V (G)|+1 . We think that there is no efficient algorithm to compute stack( G) or clear( G) for large graphs. However, it would be interesting to find efficient algorithms for computing these parameters for special classes of graphs, such as trees. 

References        

> [1] Liliana Alcón and Glenn Hurlbert, Pebbling in powers of paths , Discrete Math. 346 (2023), no. 5, Paper No. 113315, 20, DOI 10.1016/j.disc.2023.113315. [2] Fan R. K. Chung, Pebbling in hypercubes , SIAM J. Discrete Math. 2(1989), no. 4, 467–472, DOI 10.1137/0402041. MR1018531 28 T. CSERNÁK AND L. SOUKUP

[3] Betsy Crull, Tammy Cundiff, Paul Feltman, Glenn H. Hurlbert, Lara Pudwell, Zsuzsanna Szaniszlo, and Zsolt Tuza, The cover pebbling number of graphs , Discrete Math. 296 (2005), no. 1, 15–23, DOI 10.1016/j.disc.2005.03.009. [4] Teresa W. Haynes and Rodney Keaton, Cover rubbling and stacking , Discrete Math. 343 

(2020), no. 11, 112080, 6, DOI 10.1016/j.disc.2020.112080. [5] Glenn Hurlbert, General graph pebbling , Discrete Appl. Math. 161 (2013), no. 9, 1221–1231, DOI 10.1016/j.dam.2012.03.010. [6] . Pebbling Problems and Paradigms. Talk at the Combinatorics Seminar, HUN-REN Alfréd Rényi Institute of Mathematics, Budapest and Zoom, October 26, 2023. [7] Glenn Hurlbert and Franklin Kenter, Graph pebbling: a blend of graph theory, number theory, and optimization , Notices Amer. Math. Soc. 68 (2021), no. 11, 1900–1913, DOI 10.1090/noti2379. [8] David Moews, Pebbling graphs , J. Combin. Theory Ser. B 55 (1992), no. 2, 244–252, DOI 10.1016/0095-8956(92)90043-W. [9] Lior Pachter, Hunter S. Snevily, and Bill Voxman, On pebbling graphs , Proceedings of the Twenty-sixth Southeastern International Conference on Combinatorics, Graph Theory and Computing (Boca Raton, FL, 1995), 1995, pp. 65–80. [10] Jonas Sjöstrand, The cover pebbling theorem , Electron. J. Combin. 12 (2005), Note 22, 5, DOI 10.37236/1989. MR2180807 [11] Lajos Soukup. Computational data and reports for graph pebbling, 2026. Includes the PDF computational reports. Available through the GitHub repository at https://github.com/ lajossoukup/pebbling/ , DOI 10.5281/zenodo.19114123. 

Pannon University, Hungary 

Email address : tamas@csernak.com 

HUN-REN Rényi Institute of Mathematics, Budapest, Hungary 

Email address : soukup@renyi.hu