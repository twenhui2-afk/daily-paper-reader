---
title: Small Language Model Helps Resolve Semantic Ambiguity of LLM Prompt
source: arxiv
title_zh: LLM Prompt的Small Language 模型 Helps Resolve Semantic Ambiguity
abstract_zh: "Prior works address this challenge by applying query editing during LLM inference process without ex。\n\n核心做法：To address this limitation，作者提出 pre-inference prompt optimization mechanism 通过 explicit prompt disam。\n\n主要结果：Natural prompts often do not follow proper syntactic rules，which creates ambiguous queries that yiel。\n\n值得关注的点在于：Our study promotes explicit prompt disambiguation as effective prompt optimization 方法 without distur。"
abstract_en: "Large language models (LLMs) are increasingly utilized in various complex reasoning tasks due to their excellent instruction following capability. However, the model's performance is highly dependent on the open-ended characteristics of the users' input prompt. Natural prompts often do not follow proper syntactic rules, which creates ambiguous queries that yield multiple interpretations. Such ambiguous prompts confuse the model in choosing the correct reasoning paths to answer questions. Prior works address this challenge by applying query editing during the LLM inference process without explicitly solving the root cause of the ambiguity. To address this limitation, we propose a pre-inference prompt optimization mechanism via explicit prompt disambiguation. Particularly, we identify semantic risks in the prompt, check their multi-perspective consistency, and resolve any semantic conflicts that arise. Finally, we organize the resolved ambiguities in a logically structured manner as a clean input to the LLM. By explicitly resolving semantic ambiguity, our method can produce a more focused attention distribution to the semantically essential tokens. We also leverage small language models (SLMs) as the main executor of prompt disambiguation to benefit from their efficient computation. Through comprehensive experiments on multiple benchmarks, we demonstrate that our method improves reasoning performance by 2.5 points at a cost of only \\$0.02. Our study promotes explicit prompt disambiguation as an effective prompt optimization method without disturbing the internal mechanism of LLM inference."
authors: "Zhenzhen Huang, Chaoning Zhang, Fachrina Dewi Puspitasari, Jiaquan Zhang, Yitian Zhou, Shuxu Chen, Yang Yang"
date: 2026-04-25
journal: cs.CL
source_link: "https://arxiv.org/pdf/2604.23263v1"
pdf: "https://arxiv.org/pdf/2604.23263v1"
score: 6.199999999999999
tldr: 围绕《LLM Prompt的Small Languag...》；To address this limitation，作者提出...；Natural prompts often do not follow...
tldr_cn: 围绕《LLM Prompt的Small Languag...》；To address this limitation，作者提出...；Natural prompts often do not follow...
tldr_en: "Large language models (LLMs) are increasingly utilized in various complex reasoning tasks due to their excellent instruction following capability. However, the model's performance is highly dependent on the open-ended characteristics of the users' input prompt."
selection_source: fresh_fetch
vocabulary: ["optimization | 优化 | CET6", "inference | 推理 | CET6"]
motivation: Prior works address this challenge by applying query editing during LLM inference process without ex。
method: 核心做法：To address this limitation，作者提出 pre-inference prompt optimization mechanism 通过 explicit prompt disam。
result: 主要结果：Natural prompts often do not follow proper syntactic rules，which creates ambiguous queries that yiel。
conclusion: 值得关注的点在于：Our study promotes explicit prompt disambiguation as effective prompt optimization 方法 without distur。
---

## 摘要
Prior works address this challenge by applying query editing during LLM inference process without ex。

核心做法：To address this limitation，作者提出 pre-inference prompt optimization mechanism 通过 explicit prompt disam。

主要结果：Natural prompts often do not follow proper syntactic rules，which creates ambiguous queries that yiel。

值得关注的点在于：Our study promotes explicit prompt disambiguation as effective prompt optimization 方法 without distur。

## Abstract
Large language models (LLMs) are increasingly utilized in various complex reasoning tasks due to their excellent instruction following capability. However, the model's performance is highly dependent on the open-ended characteristics of the users' input prompt. Natural prompts often do not follow proper syntactic rules, which creates ambiguous queries that yield multiple interpretations. Such ambiguous prompts confuse the model in choosing the correct reasoning paths to answer questions. Prior works address this challenge by applying query editing during the LLM inference process without explicitly solving the root cause of the ambiguity. To address this limitation, we propose a pre-inference prompt optimization mechanism via explicit prompt disambiguation. Particularly, we identify semantic risks in the prompt, check their multi-perspective consistency, and resolve any semantic conflicts that arise. Finally, we organize the resolved ambiguities in a logically structured manner as a clean input to the LLM. By explicitly resolving semantic ambiguity, our method can produce a more focused attention distribution to the semantically essential tokens. We also leverage small language models (SLMs) as the main executor of prompt disambiguation to benefit from their efficient computation. Through comprehensive experiments on multiple benchmarks, we demonstrate that our method improves reasoning performance by 2.5 points at a cost of only \$0.02. Our study promotes explicit prompt disambiguation as an effective prompt optimization method without disturbing the internal mechanism of LLM inference.

## 英语词汇
- **optimization**：优化（CET6）
- **inference**：推理（CET6）
