---
title: Stacking and clearing in graph pebbling
source: arxiv
title_zh: Stacking与clearing in graph pebbling
abstract_zh: "Suppose that pebbles are distributed on vertices 的 graph G。\n\n核心做法：作者引入 two new graph parameters：stack (G)：least integer t such that every configuration 结合 t pebbles c。\n\n主要结果：Finally，computational evidence leads us to conjectural closed formula 用于 stacking number 的 tree in t。\n\n值得关注的点在于：At present，we do not have conjecture 用于 exact value 的 stack (C_{2n+1})。"
abstract_en: "Suppose that pebbles are distributed on the vertices of a graph G. A pebbling step along an edge uv removes two pebbles from u and places one pebble on v.   We introduce two new graph parameters:   stack(G): the least integer t such that every configuration with t pebbles can be transformed, by a finite sequence of pebbling steps, into a configuration with all pebbles on a single vertex.   clear(G): defined analogously, but requiring that from every configuration with t pebbles, all but one pebble can be removed.   We prove that stack(G) is defined exactly for connected graphs, and that clear(G) is defined exactly for connected non-bipartite graphs. We also establish general upper bounds for these parameters; in particular,   stack(G), clear(G) <= 2 |V(G)| 2^diam(G),   where diam(G) denotes the diameter of G.   Among our exact results are the equalities   stack(K_n) = clear(K_n) = n + 1,   stack(K_{m,n}) = 3 max{m,n} + 1,   stack(P_n) = 2^n - 1.   We also establish general lower bounds in terms of the independence number and odd closed walks.   For cycles, the situation is more delicate. We prove the lower bounds   stack(C_{2n}) >= 2^{n+1} - 1,   clear(C_{2n+1}) >= 3 * 2^n - 2,   and formulate the Almost Stacked Hypothesis, motivated by Sjostrand's cover pebbling theorem. Assuming this hypothesis, we obtain   stack(C_{2n}) = 2^{n+1} - 1,   clear(C_{2n+1}) = 3 * 2^n - 2.   At present, we do not have a conjecture for the exact value of stack(C_{2n+1}). Finally, computational evidence leads us to a conjectural closed formula for the stacking number of a tree in terms of the distances and degrees of the vertices relative to a chosen root."
authors: "Tamás Csernák, Lajos Soukup"
date: 2026-04-24
journal: math.CO
source_link: "https://arxiv.org/pdf/2604.22341v1"
pdf: "https://arxiv.org/pdf/2604.22341v1"
score: 6.6
tldr: 围绕《Stacking与clearing in gra...》；作者引入 two new graph parameters：st...；Finally，computational evidence leads...
tldr_cn: 围绕《Stacking与clearing in gra...》；作者引入 two new graph parameters：st...；Finally，computational evidence leads...
tldr_en: Suppose that pebbles are distributed on the vertices of a graph G. A pebbling step along an edge uv removes two pebbles from u and places one pebble on v.
selection_source: fresh_fetch
motivation: Suppose that pebbles are distributed on vertices 的 graph G。
method: 核心做法：作者引入 two new graph parameters：stack (G)：least integer t such that every configuration 结合 t pebbles c。
result: 主要结果：Finally，computational evidence leads us to conjectural closed formula 用于 stacking number 的 tree in t。
conclusion: "值得关注的点在于：At present，we do not have conjecture 用于 exact value 的 stack (C_{2n+1})。"
---

## 摘要
Suppose that pebbles are distributed on vertices 的 graph G。

核心做法：作者引入 two new graph parameters：stack (G)：least integer t such that every configuration 结合 t pebbles c。

主要结果：Finally，computational evidence leads us to conjectural closed formula 用于 stacking number 的 tree in t。

值得关注的点在于：At present，we do not have conjecture 用于 exact value 的 stack (C_{2n+1})。

## Abstract
Suppose that pebbles are distributed on the vertices of a graph G. A pebbling step along an edge uv removes two pebbles from u and places one pebble on v.   We introduce two new graph parameters:   stack(G): the least integer t such that every configuration with t pebbles can be transformed, by a finite sequence of pebbling steps, into a configuration with all pebbles on a single vertex.   clear(G): defined analogously, but requiring that from every configuration with t pebbles, all but one pebble can be removed.   We prove that stack(G) is defined exactly for connected graphs, and that clear(G) is defined exactly for connected non-bipartite graphs. We also establish general upper bounds for these parameters; in particular,   stack(G), clear(G) <= 2 |V(G)| 2^diam(G),   where diam(G) denotes the diameter of G.   Among our exact results are the equalities   stack(K_n) = clear(K_n) = n + 1,   stack(K_{m,n}) = 3 max{m,n} + 1,   stack(P_n) = 2^n - 1.   We also establish general lower bounds in terms of the independence number and odd closed walks.   For cycles, the situation is more delicate. We prove the lower bounds   stack(C_{2n}) >= 2^{n+1} - 1,   clear(C_{2n+1}) >= 3 * 2^n - 2,   and formulate the Almost Stacked Hypothesis, motivated by Sjostrand's cover pebbling theorem. Assuming this hypothesis, we obtain   stack(C_{2n}) = 2^{n+1} - 1,   clear(C_{2n+1}) = 3 * 2^n - 2.   At present, we do not have a conjecture for the exact value of stack(C_{2n+1}). Finally, computational evidence leads us to a conjectural closed formula for the stacking number of a tree in terms of the distances and degrees of the vertices relative to a chosen root.
