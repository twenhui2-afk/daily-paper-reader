---
title: "Rad-JEPA 3D: Radiology Joint-Embedding Predictive Model for 3D Computed Tomography"
source: arxiv
title_zh: 用于3D Computed Tomography的Rad-JEPA 3D：Radiology Joint-Embedding Predictive 模型
abstract_zh: "Yet existing volumetric encoders often fail to preserve coarse spatial 与 geometric structure that do。\n\n核心做法：作者引入 Rad-JEPA 3D，joint-embedding predictive 框架 that learns volumetric CT representations by predicti。\n\n主要结果：This layer-wise regularization 得到 more consistent 与 discriminative volumetric representations，leadin。\n\n值得关注的点在于：Ablation studies confirm that hybrid block 与 HSOR contribute complementary gains，与 that induced spat。"
abstract_en: "Self-supervised pretraining is central to 3D medical image analysis, where unlabeled CT volumes are abundant but expert annotations are scarce. Yet existing volumetric encoders often fail to preserve the coarse spatial and geometric structure that downstream reasoning depends on, limiting their performance on organ disentanglement, abnormality detection, and spatial understanding when paired with language models. We introduce Rad-JEPA 3D, a joint-embedding predictive framework that learns volumetric CT representations by predicting the latent features of a complete scan from a masked view. At its core is a hybrid H-Mamba encoder that fuses a Mamba state-space branch, which models inter-slice continuity through sequential scanning, with a grouped-query attention branch, which captures cross-plane spatial context, combined through a lightweight per-token router. To improve the quality of intermediate representations, we further propose Hidden States Orthogonal Regularization (HSOR), which aligns student-teacher hidden states and reduces feature redundancy throughout the encoder. This layer-wise regularization produces more consistent and discriminative volumetric representations, leading to improved performance on organ recognition and spatial reasoning tasks. Pretrained on approximately 120,000 CT scans, Rad-JEPA 3D attains state-of-the-art results despite its compact size: with only 4.0B total parameters, it achieves competitive results with state-of-the-art on closed-ended VQA and the best average spatial-reasoning score on the Spatial-Med benchmark. Ablation studies confirm that the hybrid block and HSOR contribute complementary gains, and that the induced spatial structure can substitute for raw language-model scale on volumetric reasoning tasks."
authors: "Quoc-Huy Trinh, Minh-Van Nguyen, Ulas Bagci"
date: 2026-07-28
journal: cs.CV
source_link: "https://arxiv.org/pdf/2607.26196v1"
pdf: "https://arxiv.org/pdf/2607.26196v1"
score: 6.75
tldr: 围绕《用于3D Computed Tomography...》；作者引入 Rad-JEPA 3D，joint-embedding...；This layer-wise regularization 得到 mo...
tldr_cn: 围绕《用于3D Computed Tomography...》；作者引入 Rad-JEPA 3D，joint-embedding...；This layer-wise regularization 得到 mo...
tldr_en: "Self-supervised pretraining is central to 3D medical image analysis, where unlabeled CT volumes are abundant but expert annotations are scarce. Yet existing volumetric encoders often fail to preserve the coarse spatial and geometric structure that downstream reasoning depends on, limiting their performance on organ disentanglement, abnormality detection, and spatial understanding when paired with language models."
selection_source: fresh_fetch
vocabulary: ["discriminative | 判别式的 | IELTS", "pretraining | 预训练 | 学术高频", "downstream | 下游任务 | 学术高频", "volumetric | 体数据的 / 三维体积的 | 学术高频", "benchmark | 基准测试 | CET6/IELTS", "detection | 检测 | CET6", "embedding | 嵌入表示 | 学术高频"]
motivation: Yet existing volumetric encoders often fail to preserve coarse spatial 与 geometric structure that do。
method: 核心做法：作者引入 Rad-JEPA 3D，joint-embedding predictive 框架 that learns volumetric CT representations by predicti。
result: 主要结果：This layer-wise regularization 得到 more consistent 与 discriminative volumetric representations，leadin。
conclusion: 值得关注的点在于：Ablation studies confirm that hybrid block 与 HSOR contribute complementary gains，与 that induced spat。
---

## 摘要
Yet existing volumetric encoders often fail to preserve coarse spatial 与 geometric structure that do。

核心做法：作者引入 Rad-JEPA 3D，joint-embedding predictive 框架 that learns volumetric CT representations by predicti。

主要结果：This layer-wise regularization 得到 more consistent 与 discriminative volumetric representations，leadin。

值得关注的点在于：Ablation studies confirm that hybrid block 与 HSOR contribute complementary gains，与 that induced spat。

## Abstract
Self-supervised pretraining is central to 3D medical image analysis, where unlabeled CT volumes are abundant but expert annotations are scarce. Yet existing volumetric encoders often fail to preserve the coarse spatial and geometric structure that downstream reasoning depends on, limiting their performance on organ disentanglement, abnormality detection, and spatial understanding when paired with language models. We introduce Rad-JEPA 3D, a joint-embedding predictive framework that learns volumetric CT representations by predicting the latent features of a complete scan from a masked view. At its core is a hybrid H-Mamba encoder that fuses a Mamba state-space branch, which models inter-slice continuity through sequential scanning, with a grouped-query attention branch, which captures cross-plane spatial context, combined through a lightweight per-token router. To improve the quality of intermediate representations, we further propose Hidden States Orthogonal Regularization (HSOR), which aligns student-teacher hidden states and reduces feature redundancy throughout the encoder. This layer-wise regularization produces more consistent and discriminative volumetric representations, leading to improved performance on organ recognition and spatial reasoning tasks. Pretrained on approximately 120,000 CT scans, Rad-JEPA 3D attains state-of-the-art results despite its compact size: with only 4.0B total parameters, it achieves competitive results with state-of-the-art on closed-ended VQA and the best average spatial-reasoning score on the Spatial-Med benchmark. Ablation studies confirm that the hybrid block and HSOR contribute complementary gains, and that the induced spatial structure can substitute for raw language-model scale on volumetric reasoning tasks.

## 英语词汇
- **discriminative**：判别式的（IELTS）
- **pretraining**：预训练（学术高频）
- **downstream**：下游任务（学术高频）
- **volumetric**：体数据的 / 三维体积的（学术高频）
- **benchmark**：基准测试（CET6/IELTS）
- **detection**：检测（CET6）
- **embedding**：嵌入表示（学术高频）
