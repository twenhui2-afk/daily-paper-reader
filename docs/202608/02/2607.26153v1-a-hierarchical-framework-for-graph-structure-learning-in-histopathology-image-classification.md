---
title: A Hierarchical Framework for Graph Structure Learning in Histopathology Image Classification
source: arxiv
title_zh: 用于Graph Structure Learning in Histopathology Image Classification的Hierarchical 框架
abstract_zh: "spatial organization 的 cells 与 tissues provides important diagnostic cues in histopathology images。\n\n核心做法：In this work，作者提出 G_2^*-Net，optimized two-level graph learning 框架用于 classifying large-scale histopat。\n\n主要结果：Experimental validation on three distinct histopathology datasets 表明 effectiveness 的 our proposed 方法。\n\n值得关注的点在于：Here，G_2 denotes two-level hierarchical graph representation，与 superscript * indicates optimized ima。"
abstract_en: "The spatial organization of cells and tissues provides important diagnostic cues in histopathology images. Although graph-based approaches can model these relationships, many rely on fixed or heuristic graph structures that may not accurately represent tissue connectivity. In this work, we propose $G_2^*$-Net, an optimized two-level graph learning framework for classifying large-scale histopathology images, such as whole-slide images (WSIs) or large regions of interest (ROIs). Here, $G_2$ denotes the two-level hierarchical graph representation, and the superscript $*$ indicates the optimized image-level graph structure learned from the proposed framework. The method first divides each WSI or large ROI into image patches, constructs cell-level graphs within each patch to capture local tissue architecture, and then represents each patch as a node in a learnable image-level graph. $G_2^*$-Net formulates image-level graph structure learning as a second-order bilevel optimization problem, separating graph connectivity learning from classifier optimization while coupling them through validation-driven feedback. To make this formulation computationally practical, we adopt a DARTS-inspired one-step unrolled approximation for efficient hypergradient estimation. Experimental validation on three distinct histopathology datasets demonstrates the effectiveness of our proposed method."
authors: "Sudipta Paul, Amanda W. Lund, Bülent Yener"
date: 2026-07-28
journal: eess.IV
source_link: "https://arxiv.org/pdf/2607.26153v1"
pdf: "https://arxiv.org/pdf/2607.26153v1"
score: 6.699999999999999
tldr: "围绕《用于Graph Structure Learni...》；In this work，作者提出 G_2^*-Net，opti...；Experimental validation on three dis..."
tldr_cn: "围绕《用于Graph Structure Learni...》；In this work，作者提出 G_2^*-Net，opti...；Experimental validation on three dis..."
tldr_en: "The spatial organization of cells and tissues provides important diagnostic cues in histopathology images. Although graph-based approaches can model these relationships, many rely on fixed or heuristic graph structures that may not accurately represent tissue connectivity."
selection_source: fresh_fetch
vocabulary: ["classification | 分类 | CET6", "optimization | 优化 | CET6"]
motivation: spatial organization 的 cells 与 tissues provides important diagnostic cues in histopathology images。
method: "核心做法：In this work，作者提出 G_2^*-Net，optimized two-level graph learning 框架用于 classifying large-scale histopat。"
result: 主要结果：Experimental validation on three distinct histopathology datasets 表明 effectiveness 的 our proposed 方法。
conclusion: "值得关注的点在于：Here，G_2 denotes two-level hierarchical graph representation，与 superscript * indicates optimized ima。"
---

## 摘要
spatial organization 的 cells 与 tissues provides important diagnostic cues in histopathology images。

核心做法：In this work，作者提出 G_2^*-Net，optimized two-level graph learning 框架用于 classifying large-scale histopat。

主要结果：Experimental validation on three distinct histopathology datasets 表明 effectiveness 的 our proposed 方法。

值得关注的点在于：Here，G_2 denotes two-level hierarchical graph representation，与 superscript * indicates optimized ima。

## Abstract
The spatial organization of cells and tissues provides important diagnostic cues in histopathology images. Although graph-based approaches can model these relationships, many rely on fixed or heuristic graph structures that may not accurately represent tissue connectivity. In this work, we propose $G_2^*$-Net, an optimized two-level graph learning framework for classifying large-scale histopathology images, such as whole-slide images (WSIs) or large regions of interest (ROIs). Here, $G_2$ denotes the two-level hierarchical graph representation, and the superscript $*$ indicates the optimized image-level graph structure learned from the proposed framework. The method first divides each WSI or large ROI into image patches, constructs cell-level graphs within each patch to capture local tissue architecture, and then represents each patch as a node in a learnable image-level graph. $G_2^*$-Net formulates image-level graph structure learning as a second-order bilevel optimization problem, separating graph connectivity learning from classifier optimization while coupling them through validation-driven feedback. To make this formulation computationally practical, we adopt a DARTS-inspired one-step unrolled approximation for efficient hypergradient estimation. Experimental validation on three distinct histopathology datasets demonstrates the effectiveness of our proposed method.

## 英语词汇
- **classification**：分类（CET6）
- **optimization**：优化（CET6）
