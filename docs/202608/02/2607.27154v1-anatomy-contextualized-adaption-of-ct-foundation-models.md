---
title: Anatomy Contextualized Adaption of CT Foundation Models
source: arxiv
title_zh: CT Foundation 模型s的Anatomy Contextualized Adaption
abstract_zh: "CT vision-language foundation 模型s have 表明d promising 性能 across downstream tasks，but are typically tr。\n\n核心做法：作者引入 Anatomy Contextualized Adaptation (ACA)，lightweight 框架 that adapts frozen CT foundation 模型 repr。\n\n主要结果：Evaluated on Merlin 与 CT-RATE，ACA consistently 优于 both frozen foundation 模型 baselines 与 existing fin。\n\n值得关注的点在于：Altogether，these results support ACA as lightweight approach 用于 adapting CT foundation 模型s to anatom。"
abstract_en: "CT vision-language foundation models have demonstrated promising performance across downstream tasks, but are typically trained with whole-volume representations that dilute fine-grained anatomical signals. Fine-grained vision-language pre-training addresses this by aligning anatomy-level visual features with anatomy-specific text, but in doing so discards the global context that whole-volume models provide. Furthermore, existing fine-grained approaches train from scratch, making them computationally expensive. We introduce Anatomy Contextualized Adaptation (ACA), a lightweight framework that adapts frozen CT foundation model representations for anatomy-level vision-language alignment while enhancing global contextualization. ACA uses TotalSegmentator to decompose CT volumes into anatomy-level embeddings, which are refined via a transformer that captures cross-anatomy relationships, and aligned to both per-anatomy and scan-level text extracted from radiology reports. Evaluated on Merlin and CT-RATE, ACA consistently outperforms both the frozen foundation model baselines and existing fine-grained methods in zero-shot finding classification, while requiring less than one hour of training once embeddings are cached. The attention weights learned by ACA's inter-anatomy transformer additionally indicate plausible cross-anatomy context routing. Altogether, these results support ACA as a lightweight approach for adapting CT foundation models to anatomically grounded vision-language alignment while preserving and enhancing global anatomical context."
authors: "Roshan Kenia, Stephanie L McNamara, William Lotter"
date: 2026-07-29
journal: cs.CV
source_link: "https://arxiv.org/pdf/2607.27154v1"
pdf: "https://arxiv.org/pdf/2607.27154v1"
score: 6.5
tldr: 围绕《CT Foundation 模型s的Anatom...》；作者引入 Anatomy Contextualized Adap...；Evaluated on Merlin 与 CT-RATE，ACA co...
tldr_cn: 围绕《CT Foundation 模型s的Anatom...》；作者引入 Anatomy Contextualized Adap...；Evaluated on Merlin 与 CT-RATE，ACA co...
tldr_en: "CT vision-language foundation models have demonstrated promising performance across downstream tasks, but are typically trained with whole-volume representations that dilute fine-grained anatomical signals. Fine-grained vision-language pre-training addresses this by aligning anatomy-level visual features with anatomy-specific text, but in doing so discards the global context that whole-volume models provide."
selection_source: fresh_fetch
vocabulary: ["foundation model | 基础模型 | 学术高频", "classification | 分类 | CET6", "transformer | Transformer 架构 | 学术高频", "downstream | 下游任务 | 学术高频", "alignment | 对齐 | CET6"]
motivation: CT vision-language foundation 模型s have 表明d promising 性能 across downstream tasks，but are typically tr。
method: 核心做法：作者引入 Anatomy Contextualized Adaptation (ACA)，lightweight 框架 that adapts frozen CT foundation 模型 repr。
result: 主要结果：Evaluated on Merlin 与 CT-RATE，ACA consistently 优于 both frozen foundation 模型 baselines 与 existing fin。
conclusion: 值得关注的点在于：Altogether，these results support ACA as lightweight approach 用于 adapting CT foundation 模型s to anatom。
---

## 摘要
CT vision-language foundation 模型s have 表明d promising 性能 across downstream tasks，but are typically tr。

核心做法：作者引入 Anatomy Contextualized Adaptation (ACA)，lightweight 框架 that adapts frozen CT foundation 模型 repr。

主要结果：Evaluated on Merlin 与 CT-RATE，ACA consistently 优于 both frozen foundation 模型 baselines 与 existing fin。

值得关注的点在于：Altogether，these results support ACA as lightweight approach 用于 adapting CT foundation 模型s to anatom。

## Abstract
CT vision-language foundation models have demonstrated promising performance across downstream tasks, but are typically trained with whole-volume representations that dilute fine-grained anatomical signals. Fine-grained vision-language pre-training addresses this by aligning anatomy-level visual features with anatomy-specific text, but in doing so discards the global context that whole-volume models provide. Furthermore, existing fine-grained approaches train from scratch, making them computationally expensive. We introduce Anatomy Contextualized Adaptation (ACA), a lightweight framework that adapts frozen CT foundation model representations for anatomy-level vision-language alignment while enhancing global contextualization. ACA uses TotalSegmentator to decompose CT volumes into anatomy-level embeddings, which are refined via a transformer that captures cross-anatomy relationships, and aligned to both per-anatomy and scan-level text extracted from radiology reports. Evaluated on Merlin and CT-RATE, ACA consistently outperforms both the frozen foundation model baselines and existing fine-grained methods in zero-shot finding classification, while requiring less than one hour of training once embeddings are cached. The attention weights learned by ACA's inter-anatomy transformer additionally indicate plausible cross-anatomy context routing. Altogether, these results support ACA as a lightweight approach for adapting CT foundation models to anatomically grounded vision-language alignment while preserving and enhancing global anatomical context.

## 英语词汇
- **foundation model**：基础模型（学术高频）
- **classification**：分类（CET6）
- **transformer**：Transformer 架构（学术高频）
- **downstream**：下游任务（学术高频）
- **alignment**：对齐（CET6）
