---
title: Chebyshev Recurrence Structures for Reduced Spectral Functions of Cyclic Circulant Graphs
source: arxiv
title_zh: 用于Cyclic Circulant Graphs的Reduced Spectral Functions的Chebyshev Recurrence Structures
abstract_zh: "Let S be nonempty finite set 的 positive integers，let q=\\max S，与 let B_n (S)，n>2q，be normalized produ。\n\n核心做法：When G_n (S) is connected，B_n (S) is both normalized spanning-tree number 与 normalized special value。\n\n主要结果：Starting from these representations，we explicitly construct monic annihilating polynomial \\mathcal H。\n\n值得关注的点在于：用于 S=\\{1,2,3\\} 与 S=\\{1,3\\}，we explicitly derive corresponding ninth-degree annihilating polynomials。"
abstract_en: "Let $S$ be a nonempty finite set of positive integers, let $q=\\max S$, and let $B_n(S)$, $n>2q$, be the normalized product sequence associated with the Chebyshev-type polynomial of the cyclic circulant graph $G_n(S)$. When $G_n(S)$ is connected, $B_n(S)$ is both the normalized spanning-tree number and a normalized special value of the \\emph{reduced spectral function}, a determinant-type function constructed from the non-trivial adjacency spectrum. Chebyshev root representations and the existence of linear recurrences for fixed-step circulant spanning-tree sequences are known. Starting from these representations, we explicitly construct a monic annihilating polynomial $\\mathcal H_S(X)\\in\\mathbb Z[X]$ of degree $3^{q-1}$, which yields a general upper bound for the recurrence order of $B_n(S)$. By collecting coincident exponential bases and accounting for possible cancellations, we determine the minimal annihilating polynomial and give a sufficient condition under which the minimal recurrence order is exactly $3^{q-1}$. For $S=\\{1,2,3\\}$ and $S=\\{1,3\\}$, we explicitly derive the corresponding ninth-degree annihilating polynomials and prove their minimality."
authors: Shunya Tamura
date: 2026-07-31
journal: math.CO
source_link: "https://arxiv.org/pdf/2607.29321v1"
pdf: "https://arxiv.org/pdf/2607.29321v1"
score: 7.5
tldr: 围绕《用于Cyclic Circulant Graph...》；When G_n (S) is connected，B_n (S...；Starting from these representations...
tldr_cn: 围绕《用于Cyclic Circulant Graph...》；When G_n (S) is connected，B_n (S...；Starting from these representations...
tldr_en: "Let $S$ be a nonempty finite set of positive integers, let $q=\\max S$, and let $B_n(S)$, $n>2q$, be the normalized product sequence associated with the Chebyshev-type polynomial of the cyclic circulant graph $G_n(S)$. When $G_n(S)$ is connected, $B_n(S)$ is both the normalized spanning-tree number and a normalized special value of the \\emph{reduced spectral function}, a..."
selection_source: fresh_fetch
motivation: "Let S be nonempty finite set 的 positive integers，let q=\\max S，与 let B_n (S)，n>2q，be normalized produ。"
method: 核心做法：When G_n (S) is connected，B_n (S) is both normalized spanning-tree number 与 normalized special value。
result: 主要结果：Starting from these representations，we explicitly construct monic annihilating polynomial \mathcal H。
conclusion: "值得关注的点在于：用于 S=\\{1,2,3\\} 与 S=\\{1,3\\}，we explicitly derive corresponding ninth-degree annihilating polynomials。"
---

## 摘要
Let S be nonempty finite set 的 positive integers，let q=\max S，与 let B_n (S)，n>2q，be normalized produ。

核心做法：When G_n (S) is connected，B_n (S) is both normalized spanning-tree number 与 normalized special value。

主要结果：Starting from these representations，we explicitly construct monic annihilating polynomial \mathcal H。

值得关注的点在于：用于 S=\{1,2,3\} 与 S=\{1,3\}，we explicitly derive corresponding ninth-degree annihilating polynomials。

## Abstract
Let $S$ be a nonempty finite set of positive integers, let $q=\max S$, and let $B_n(S)$, $n>2q$, be the normalized product sequence associated with the Chebyshev-type polynomial of the cyclic circulant graph $G_n(S)$. When $G_n(S)$ is connected, $B_n(S)$ is both the normalized spanning-tree number and a normalized special value of the \emph{reduced spectral function}, a determinant-type function constructed from the non-trivial adjacency spectrum. Chebyshev root representations and the existence of linear recurrences for fixed-step circulant spanning-tree sequences are known. Starting from these representations, we explicitly construct a monic annihilating polynomial $\mathcal H_S(X)\in\mathbb Z[X]$ of degree $3^{q-1}$, which yields a general upper bound for the recurrence order of $B_n(S)$. By collecting coincident exponential bases and accounting for possible cancellations, we determine the minimal annihilating polynomial and give a sufficient condition under which the minimal recurrence order is exactly $3^{q-1}$. For $S=\{1,2,3\}$ and $S=\{1,3\}$, we explicitly derive the corresponding ninth-degree annihilating polynomials and prove their minimality.
