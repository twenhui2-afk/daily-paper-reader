Title: A Configuration-LP Framework for Connected k-Median Clustering

URL Source: https://arxiv.org/pdf/2608.28081v1

Published Time: Mon, 31 Aug 2026 00:43:29 GMT

Number of Pages: 23

Markdown Content:
# A Configuration-LP Framework for Connected k-Median Clustering 

Kushagra Chatterjee ∗ Rojin Rezvan † Ali Vakilian ‡

Abstract 

We study the connected k-median clustering problem, a clustering problem that augments the classical k-median objective with connectivity constraints. We focus on the overlapping 

variant of the problem, where clusters are allowed to share vertices. In addition to a metric space (V, d ), the input contains a connected graph G on the same vertex set V of size n.The goal is to select at most k centers C and assign vertices to them so as to minimize the k-median cost (i.e., P 

> v∈V

d(v, C )), subject to the constraint that each cluster induces a connected subgraph of G. Since the metric space and the connectivity graph are independent, the problem is significantly more challenging than standard clustering. Eube et al. [ELR +25] showed that even the assignment version is Ω(log n)-hard to approximate and gave approximation algorithms with guarantees depending polynomially on k.We develop a configuration-LP-based framework that combines covering LP techniques with a rooted minimum-density oracle. For the assignment version, we obtain an O(log 2 n)-approximation. For the general version, we develop a bicriteria framework that opens O(k log n)

centers while achieving an O(log 2 n)-approximation in cost. 

> ∗

Indian Statistical Institute, Kolkata, India. Email: kushagrachatterjee@gmail.com. This work was carried out when the author was a PhD student at the National University of Singapore 

> †

Virginia Tech, USA. Email: rojinrezvan@vt.edu 

> ‡

Virginia Tech, USA. Email: vakilian@ttic.edu arXiv:2608.28081v1 [cs.DS] 28 Aug 2026 Contents 

1 Introduction 1

1.1 Technical Overview . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 21.2 Related Work . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 3

2 Preliminaries and Problem Statement 43 Assignment Version 5

3.1 Configuration LP . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 63.2 The Oracle: Rooted Minimum-Density Connected Set . . . . . . . . . . . . . . . . . 73.3 Computing a ρ-Approximate Solution of the Assignment Version of the Configuration LP . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 73.4 Rounding the Configuration LP . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 10 3.5 Proof of Theorem 1 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 10 

4 General Version 10 

4.1 General Version of the Configuration LP . . . . . . . . . . . . . . . . . . . . . . . . . 11 4.2 Computing a ρ-Approximate Solution of the Configuration LP for the General Variant 11 4.3 Rounding the General Version of the Configuration LP . . . . . . . . . . . . . . . . . 13 4.4 Proof of Theorem 2 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 14 

5 Conclusion and Open Problems 14 A Proof of Lemma 3.1 18 B Proof of Lemma 3.2 18 C Proof of Theorem 4 19 1 Introduction 

Clustering is a central problem in algorithms and data analysis, with classical objectives such as 

k-center [HS85; KS00] and k-median [CGT +99; CG99; Che06] extensively studied over the past decades. In the k-median problem, we are given a vertex set V of size n together with a metric 

d : V × V → R≥0 and an integer k, and the goal is to select k centers so as to minimize the sum of distances of each vertex to its assigned center. While the vanilla k-median problem admits constant-factor approximation algorithms [CGT +99; CG99; AGK +01; CGH +22], many modern applications require incorporating additional structural constraints into the clustering process. In this work, we study the connected k-median problem, an extension of the classical k-median problem that incorporates connectivity constraints . In addition to the metric space (V, d ), the input includes an unweighted undirected graph G = ( V, E ), referred to as the connectivity graph . The goal is to select k centers and assign vertices to them so as to minimize the k-median objective, subject to the constraint that each cluster induces a connected subgraph in G. Importantly, the graph G is completely independent of the metric d, making the problem fundamentally different from classical clustering. We focus on the overlapping variant of the connected k-median problem, where a vertex may belong to more than one cluster. This is natural in applications such as community detection [BS16; FB17], where individuals can participate in multiple groups, and it also allows vertices such as articulation points to serve as connectors between several clusters. Theoretically, this distinction is crucial; Eube et al. [ELR +25] showed that the disjoint version is extremely hard to approximate, admitting an Ω( n1−ε) lower bound even for k = 2 .Connected clustering arises naturally in settings where both attribute data and relational struc-ture are present. For instance, in applications such as community detection in social networks [BS16; FB17] or spatial partitioning tasks like school redistricting [CBC +23], the input consists of data points equipped with features as well as an underlying graph capturing relationships such as social interactions or geographic adjacency. In such settings, it is desirable to form clusters that are not only metrically coherent but also respect the connectivity structure of the graph. For example, in school redistricting, geographical units must be grouped into contiguous regions, ensuring that each cluster forms a connected area, while still optimizing criteria such as proximity or compactness. This motivates the study of clustering models that integrate geometric objectives with graph-theoretic constraints. From an algorithmic perspective, connectivity constraints significantly increase the difficulty of clustering problems. Even in the assignment version of connected k-median, where the set of centers is fixed, and the goal is only to assign vertices to centers, Eube et al. [ELR +25] showed that the prob-lem is Ω(log n)-hard to approximate, and provided an O(k log n)-approximation algorithm. For the general version, where centers must also be selected, they obtained an O(k2 log n)-approximation. These results demonstrate a substantial gap between k-median clustering and its connected variant. 

Our contributions. Our main contribution is a configuration-LP framework for connected 

k-median clustering. Unlike the cut-based relaxation of Eube et al. [ELR +25], where connectivity is enforced indirectly through constraints, our LP uses variables indexed by connected sets rooted at centers, thereby encoding connectivity directly into the variables themselves. We combine this for-mulation with covering-LP techniques and rooted minimum-density oracles to obtain approximation guarantees for connected k-median. Our main results are summarized below. 1• Assignment version. We obtain an O(log 2 n)-approximation algorithm for general graphs, which improves Eube et al [ELR +25] assignment version result when k = ω(log n).• General version (bicriteria approximation). We develop a bicriteria framework that allows opening more than k centers. In particular, we compute a solution that opens at most O(k log n) centers while achieving an O(log 2 n)-approximation in cost, yielding an 

(O(log n), O (log 2 n)) bicriteria approximation. The bicriteria guarantee highlights a tradeoff between the number of opened centers and the approximation factor in cost. The algorithm of Eube et al. [ELR +25] gives a solution with k

centers with approximation factor of O(k2 log n) for the general version. In contrast, our bicriteria framework opens O(k log n) centers and achieves an O(log 2 n) cost approximation, eliminating the polynomial dependence on k in the objective guarantee. 

1.1 Technical Overview 

Our algorithms are based on a configuration-LP viewpoint. Instead of assigning vertices to centers directly, we introduce a variable for each pair (c, T ), where c is a center and T is a connected vertex set containing c. Choosing such a configuration corresponds to opening a connected cluster rooted at c. This formulation captures the connectivity requirement directly: every integral solution selects connected sets, and the covering constraints ensure that every vertex is contained in at least one selected cluster. It is useful to contrast this formulation with the cut-based LP used by Eube et al. [ELR +25]. In the cut-LP, the variables xcv represent fractional assignments of vertices to centers, and connectivity is enforced indirectly through cut constraints: if v is assigned to c, then every v–c

cut must contain enough mass assigned to c. This is a natural and compact relaxation, but it is still a vertex-assignment formulation; after rounding, one must still ensure that the assigned vertices form connected clusters. In contrast, the configuration LP works directly with connected sets. A variable yT,c corresponds to selecting an entire connected cluster T rooted at c, so connectivity is built into the variables themselves rather than enforced only through constraints. This global representation is particularly useful for rounding. Once a configuration (c, T ) is sam-pled, it already defines a connected set containing its center, and the union of sampled configurations with the same root remains connected. Thus, the randomized rounding step only needs to ensure coverage of vertices; connectivity is preserved automatically. Moreover, every configuration-LP so-lution induces a feasible solution to the cut-LP by setting xcv = PT ∈T c

> v∈T

yT,c , with the same objective value, so the configuration LP can be viewed as a more structured relaxation. The price of this stronger representation is that the LP contains exponentially many variables, so solving it exactly is not straightforwardly achievable in polynomial time. In fact, we can only solve this LP relaxation approximately, using column generation based on the rooted minimum-density connected-set oracle. For the assignment version, where the center set C is fixed, the configuration LP is a pure covering LP. We solve it approximately using the multiplicative weight update framework of Plotkin, Shmoys and Tarods [PST91] for covering linear programs. The key step is the pricing problem: given weights 

αv on the vertices that correspond to the covering constraints, we need to find a connected set T ∋ c

minimizing the ratio between its cost and the total weight of vertices it covers. This is exactly a rooted minimum-density connected-set problem: 

min   

> T|T∋c

P 

> v∈T

d(v, c )

P 

> v∈T

αv

.

2Thus, a ρ-approximate oracle for rooted minimum-density connected sets yields a ρ-approximate pricing oracle for the configuration LP. Combining this with the Plotkin–Shmoys–Tardos framework gives a polynomial-support fractional solution whose cost is within a factor O(ρ) of the optimum LP fractional value. We obtain such density oracles using connections to the node-weighted Steiner tree problem. The rounding step is simple and uses the standard independent randomized rounding. Given a fractional solution ˆy, we sample each configuration (c, T ) with probability proportional to ˆyT,c log n.For each center c, the final cluster is the union of all the sampled configurations rooted at c. This union remains connected because every sampled set contains the common root c. The logarithmic oversampling ensures, by a union bound over all vertices, that every vertex is covered with high probability. Linearity of expectation bounds the expected cost by an additional O(log n) factor. This yields an O(ρ log n)-approximation for the assignment version. For general graphs, the rooted minimum-density problem admits an O(log n)-approximation via known algorithms for the budgeted node-weighted Steiner tree problem. This yields an O(log 2 n)-approximation for the assignment version. For minor-closed graphs, the techniques of [DHK09; CEV21] suggest that the rooted minimum-density problem may admit an O(1) -approximation, which would improve the assignment-version guarantee to O(log n).For the general version, we provide a bicriteria approximation guarantee. We avoid the center-rounding step of Eube et al [ELR +25] and instead formulate a non-fixed-center configuration LP. This LP has variables for all roots c ∈ V and all connected sets T ∈ T c, together with a fractional center-budget constraint P

> c∈V

P  

> T∈T c

yT,c ≤ k. This LP is a mixed packing/covering program. Its pricing problem again reduces to a rooted minimum-density connected-set problem, except that the ratio includes both the connection cost of the configuration and the multiplier corresponding to the center-budget constraint. After computing a near-optimal fractional solution, we apply the same randomized rounding scheme. The covering constraints guarantee that all vertices are covered with high probability, while the budget constraint implies that the number of sampled configurations, and hence the number of opened centers, is at most O(k log n) with high probability. This gives an (O(log n), O (ρ log n)) bicriteria approximation, which becomes (O(log n), O (log 2 n)) for general graphs. 

1.2 Related Work 

Clustering problems such as k-center and k-median are among the most well-studied optimization problems in algorithm design. Both problems are known to be NP-hard. For the k-center prob-lem, a tight 2-approximation algorithm is known [Gon85; HS85], and this factor is optimal unless P=NP [HN79]. For metric k-median, the current best polynomial-time approximation guarantee is 

2+ ε, for any fixed ε > 0, achieved by the recent algorithm of Cohen-Addad et al. [CGL +25]. In con-trast, no polynomial-time algorithm can achieve an approximation ratio smaller than 1+2 /e ≈ 1.736 ,unless P = NP [JMS02]. 

Connected clustering. Clustering with connectivity constraints has been studied in several contexts. Ge et al. [GEG +08] introduced the connected k-center problem as a model for jointly clustering attribute data and relationship data: the metric captures similarity between attribute vectors, while the connectivity graph captures relational information. They gave an exact dy-namic program when the connectivity graph is a tree, and also studied general graphs, although without a known approximation guarantee. For general graphs with disjoint clusters, Drexler et 3al. [DEL +24] obtained an O(log 2 k)-approximation; they also gave constant-factor approximations for Euclidean metrics of constant dimension and metrics of constant doubling dimension, motivated by applications in geodesy and sea-level analysis. In contrast, when overlapping clusters are allowed, connected k-center admits a simple 2-approximation, highlighting an important distinction between the disjoint and overlapping models. For the k-median objective, the connected variant has been studied more recently. Validi et al. [VBL22] consider connected k-median in the context of districting, where geographical units must be grouped into contiguous regions while optimizing a proximity-based objective. They formulate the problem as an integer linear program, which can be solved optimally but requires exponential time in the worst case. Gupta et al. [GPS11] study the inapproximability of clustering with con-nectivity constraints and show that even when k = 2 and the connectivity graph is star-like, the problem is Ω(log n)-hard to approximate. They also provide an O(log n)-approximation algorithm for some restricted instances of the problem. 

Clustering with additional constraints. Beyond connectivity, a large body of work has focused on incorporating additional constraints into clustering problems motivated by real-world applications. Capacitated clustering introduces upper bounds on cluster sizes [Li16], while models with outliers allow for excluding a small fraction of points from the clustering [CKM +01]. More recently, fairness constraints have been extensively studied, requiring clusters to satisfy various fair-ness conditions [CKL +17; BCF +19; BIO +19; BGK +19; DMV22; JKL20; MV20; VY22; GSV21; ABV21; MV21; EBT +20; EBS +21; CFL +19]. Fairness constraints have also been studied for other types of clustering problems, like correlation clustering [AEK +20; AGS +20; AN23] and consensus clustering [CCD +25; CCD +26b; CCD +26a]. These variants highlight the increasing importance of structured clustering models, where combinatorial constraints are integrated with geometric objec-tives. 

# 2 Preliminaries and Problem Statement 

In this section, we introduce the definitions and notation used throughout the paper. We also formally state the problem and present our main results. 

Problem Statement. We are given a vertex set V , a metric d : V ×V → R≥0, and a connected 

connectivity graph G = ( V, E ), which is unrelated to d. We consider the problem in two settings. • Assignment Version: A set of centers C ⊆ V with |C| ≤ k, for some integer k is given. A feasible overlapping clustering solution outputs connected sets S := ( Sc)c∈C such that (i) 

c ∈ Sc for all c ∈ C, (ii) G[Sc] is connected for all c ∈ C, and (iii) S 

> c∈C

Sc = V .• General Version: An integer k is given, and the goal is to choose a set of centers C ⊆ V

with |C| ≤ k together with connected sets S := ( Sc)c∈C satisfying the same conditions as above. In both cases, the objective is to find a feasible solution S that minimizes 

cost( S) := X

> c∈C

X

> v∈Sc

d(v, c ),

4where a vertex assigned to multiple clusters contributes once per cluster. Here G[Sc] denotes the subgraph of G induced by the vertex set Sc.

Remark 1. Let G1, . . . , G ℓ be the connected components of G. In the assignment version, if some component contains at least one vertex but no center, then the instance is infeasible. Otherwise, each component can be solved independently, and the solutions can be combined. Hence, without loss of generality, we assume throughout that G is connected and every vertex can reach some center. 

Remark 2 (Bounded aspect ratio) . Throughout the paper we assume the metric d has polynomially bounded aspect ratio: if Dmax := max u,v ∈V d(u, v ) and δmin := min u̸=v∈V d(u, v ), we assume 

∆ := Dmax 

δmin 

= poly( n).

This is the standard bounded-spread condition used throughout the metric clustering and embedding literature. All stated polynomial running times in this paper are with respect to n, k, and 1/ε under this assumption. Otherwise, the runtime of our algorithm polynomially depends on log(∆) too, which is polynomial in the input size. 

Our Results. We now formally state our results. A feasible overlapping clustering solution for either version is referred to as a connected k-median clustering of the vertex set V . We say that a clustering S is an α-approximate connected k-median clustering if cost (S) ≤ α · cost (S∗), where 

S∗ denotes an optimal connected k-median clustering. For the assignment version, we obtain an improved approximation guarantee when k = ω(log n).In particular, we show the following. 

Theorem 1. Given a vertex set V , a metric d : V × V → R≥0, an undirected, unweighted connec-tivity graph G = ( V, E ), and a set of centers C ⊆ V with |C| ≤ k, there exists a polynomial-time algorithm that computes an O(log 2 n)-approximate connected k-median clustering. 

We now consider the general version of the problem, where the set of centers is not given and must be chosen as part of the solution. For the general version, we have obtained bicriteria approximation guarantees. For this, we need to define a bicriteria notion of approximation that allows the algorithm to open more than k centers. A clustering S is said to be a (β, α )-approximate connected k-median clustering if it uses at most βk centers and satisfies cost (S) ≤ α · cost (S∗),where S∗ is an optimal solution that uses at most k centers. We obtain the following bicriteria guarantees. 

Theorem 2. Given a vertex set V , a metric d : V × V → R≥0, an undirected, unweighted connec-tivity graph G = ( V, E ) and an integer k, there exists a polynomial-time algorithm that computes an 

(O(log n), O (log 2 n)) -approximate connected k-median clustering. 

# 3 Assignment Version 

In this section, we prove our results for the assignment version of the problem. In particular, we prove Theorem 1, which we restate below. 5Theorem 1. Given a vertex set V , a metric d : V × V → R≥0, an undirected, unweighted connec-tivity graph G = ( V, E ), and a set of centers C ⊆ V with |C| ≤ k, there exists a polynomial-time algorithm that computes an O(log 2 n)-approximate connected k-median clustering. 

To prove Theorem 1, we employ a configuration LP formulation. Since this LP has exponentially many variables, we compute a near-optimal fractional solution using a rooted minimum-density connected set oracle in conjunction with the Plotkin–Shmoys–Tardos [PST91] multiplicative weight update framework for covering LPs. We then round this fractional solution to obtain the desired approximation guarantee. The details are presented in the subsequent sections. 

3.1 Configuration LP 

We follow a configuration LP route: formulate an exponential-size covering LP over all connected clusters, solve it approximately via column generation/packing-covering with a minimum-density 

oracle, and round it in one shot via randomized rounding. This yields an O(ρ log n) approximation given a ρ-approximation oracle for the rooted minimum-density connected set problem. For each center c ∈ C, let Tc denote the family of all connected vertex sets T ⊆ V such that 

c ∈ T and G[T ] is connected. For T ∈ T c, define 

cost c(T ) := X

> v∈T

d(v, c ).

We introduce a variable yT,c ≥ 0 for each pair (c, T ) to represent the extent to which we choose configuration (c, T ). We define the primal and dual relaxation of the assignment problem as follows. 

Primal (Configuration) LP: 

min X

> c∈C

X 

> T∈T c

cost c(T ) yT,c (1) s.t. X

> c∈C

X 

> T∈T c
> v∈T

yT,c ≥ 1 ∀v ∈ V \ C, (2) 

yT,c ≥ 0 ∀c ∈ C, ∀T ∈ T c. (3) 

Dual LP: 

max X 

> v∈V\C

αv (4) s.t. X 

> v∈T\{ c}

αv ≤ cost c(T ) ∀c ∈ C, ∀T ∈ T c, (5) 

αv ≥ 0 ∀v ∈ V \ C. (6) 

Lemma 3.1. The configuration LP (1) –(3) is a valid relaxation of the assignment version of con-nected k-median with overlapping clusters. In particular, its optimal value is at most OPT .Proof. We provide the proof in the appendix. 63.2 The Oracle: Rooted Minimum-Density Connected Set 

Since the configuration LP has exponentially many variables (one for each connected set T ∈ T c and each center c ∈ C), it is not immediate how to solve it in polynomial time. We follow the standard oracle-based approach for large-scale packing/covering linear programs: rather than enumerating all configurations explicitly, we access them through an optimization subroutine that, for suitable dual weights, returns an approximately best column [PST91; You01]. In our setting, this subroutine is the rooted minimum-density connected-set problem defined below. 

Definition 1. Given a graph G = ( V, E ), a root c ∈ V , nonnegative node profits α : V → R≥0, and nonnegative node costs w : V → R≥0, the rooted minimum-density problem asks for a connected set 

T ∋ c minimizing 

dens( T ) := 

P 

> v∈T

w(v)

P 

> v∈T

αv

,

with the convention dens( T ) = + ∞ if P 

> v∈T

αv = 0 .

In our application, the dual variables are defined only on V \ C, so we set αc := 0 . Thus P 

> v∈T

αv = P  

> v∈T\{ c}

αv, which matches the left-hand side of the dual packing constraint (5). In particular, minimizing dens( T ) identifies the most violated dual constraint, and hence the most promising column for the primal configuration LP. 

Definition 2. A ρ-approximate density oracle returns a connected set T ∋ c such that dens( T ) ≤

ρ · min T ′∋c dens( T ′).

Lemma 3.2. There is a polynomial-time O(log n)-approximate density oracle. Proof. The proof uses a result by Bateni et al [BHL18]. We provide the full proof in the appendix. 

3.3 Computing a ρ-Approximate Solution of the Assignment Version of the Configuration LP 

We next explain how to approximately solve the configuration LP using the rooted minimum-density oracle from the previous subsection. We follow the standard oracle-based approach for large covering LPs: we view the configuration LP as a covering problem over an implicit column set and apply the framework of Plotkin–Shmoys–Tardos [PST91]; the key point is that the corresponding pricing subproblem is exactly a rooted minimum-density connected-set problem. Therefore, a ρ-approximate density oracle yields, in polynomial time, a (1 + ε)ρ-approximate fractional solution to the configuration LP. 

Theorem 3. Assume a ρ-approximate density oracle. Then for any ε ∈ (0 , 1) , one can compute in polynomial time a feasible fractional solution ˆy to the Configuration LP (1) –(3) satisfying 

X

> c∈C

X 

> T∈T c

cost c(T )ˆ yT,c ≤ (1 + ε)ρ · OPT CLP ≤ (1 + ε)ρ · OPT ,

with support size poly( n, ε −1, ρ ).

7Proof. We cast the configuration LP as a pure covering LP and apply a standard fractional pack-ing/covering approximation scheme (Plotkin–Shmoys–Tardos [PST91]), whose guarantee is that an approximately optimal fractional solution can be computed given oracle access to an approximate 

pricing subproblem. We discard all columns (c, T ) with T ∩ (V \ C) = ∅. Such columns cover no covering constraint and therefore play no role in the covering LP. After this restriction, every remaining column satisfies 

cost c(T ) > 0.

Step 1: Configuration LP as a covering LP. Let U0 := V \ C and n0 := |U0|. Index columns by pairs (c, T ) with c ∈ C and T ∈ T c. Define the incidence matrix A ∈ { 0, 1}n0×N and cost vector c ∈ RN>0 by 

Av,j := 1[v ∈ T ], cj := cost c(T ) = X

> u∈T

d(u, c ).

Since d is a metric with d(u, c ) > 0 for all u̸ = c, every column j = ( c, T ) with T̸ = {c} satisfies 

cj > 0, so there are no 0/0 anomalies in the pricing step. The configuration LP (1)–(3) is then 

min {c⊤y : Ay ≥ 1, y ≥ 0}, a non-negative covering LP with m := n0 constraints and N columns. 

Step 2: Pricing is (approximate) rooted minimum-density. In the Plotkin–Shmoys– Tardos framework for covering LPs, the algorithm maintains a non-negative weight vector p ∈ RU0

> ≥0

on constraints and, in each iteration, needs a column whose cost per covered weight is minimum (equivalently, whose covered weight per unit cost is maximum). For a column j = ( c, T ), the covered weight under p is ⟨p, A ·,j ⟩ = P  

> v∈T∩U0

pv, so the ratio is 

cj

⟨p, A ·,j ⟩ =

P 

> u∈T

d(u, c )

P  

> v∈T∩U0

pv

.

This is the rooted minimum-density objective (Definition 1) with root c, costs wc(u) := d(u, c ), and profits αv := pv (with αc := 0 for centers). A ρ-approximate density oracle therefore provides a 

ρ-approximate pricing oracle for the covering LP: calling it for each c ∈ C and returning the column of globally smallest density gives an ρ-approximate minimizer of the ratio over all columns. 

Step 3: Width and the upper bound q on OPT CLP . To apply Theorem 4, we need (i) an upper bound q ≥ OPT CLP and (ii) the associated width 

W ≥ q · max  

> v∈U0,j =( c,T )

Av,j 

cj

= q · max   

> j=( c,T ): T̸={c}

1cost c(T ) . (7) 

Upper bound q. Construct the following explicit feasible solution: for each v ∈ U0, let Pv be a shortest G-path from the nearest center cv to v, which exists by the assumption on connectivity of 

G, and set yV (Pv ),c v = 1 (with remaining variables 0). This is feasible (every v is covered) and has cost 

q := X

> v∈U0

cost cv (V (Pv)) ≤ X

> v∈U0

X  

> u∈V(Pv)

d(u, c v) ≤ n0 · n · Dmax ,

where Dmax := max u,v ∈V d(u, v ). Since the "Width W" step below normalizesd so that δmin = 1 ,then Dmax conincides with the aspect ratio ∆, which is poly( n) by Remark 2. Therefore, q =poly( n).

8Width W . After normalizing d by its minimum positive value so that δ := min u̸=v d(u, v ) = 1 

(this leaves all approximation ratios unchanged and ensures cj ≥ 1 for all non-trivial columns), the width in (7) becomes 

W = q · max 

> j

1

cj

≤ q · 1

δ = q = poly( n).

Step 4: Apply Theorem 4. With m = n0, the ρ-approximate pricing oracle from Step 2, the upper bound q ≥ OPT CLP from Step 3, and the width W ≤ q from Step 3, Theorem 4 yields a feasible ˆy with 

c⊤ ˆy ≤ ρ(1 + ε + ε2)OPT CLP ≤ (1 + ε)ρ · OPT CLP 

absorbing ε2 into ε by replacing ε with ε/ 2, and support size 

|supp(ˆ y)| = eO

 n0W ρ ε3



= eO

 n0qρ ε3



= poly( n).

Since OPT CLP ≤ OPT (Lemma 3.1), the stated bound c⊤ ˆy ≤ (1 + ε)ρ · OPT follows. 

Runtime. Each iteration of the Plotkin–Shmoys–Tardos framework calls the density oracle 

k := |C| times (once per center) and selects the column of globally smallest density. The total num-ber of iterations is eO(n0W ρ/ε 3) = poly( n). After all iterations, the restricted LP on the generated columns is solved exactly in polynomial time. Each oracle call is polynomial by Lemma 3.2, so the entire algorithm runs in polynomial time. The following theorem is a standard oracle-based reformulation of the fractional covering frame-work of Plotkin–Shmoys–Tardos; we cite Sharma [Sha20, Theorems 3 and 4] for a clean restatement with approximate pricing and support bounds. For completeness, we provide a self-contained proof of Theorem 4 in the appendix. 

Theorem 4 (Plotkin–Shmoys–Tardos [PST91]; cf. Sharma [Sha20, Theorems 3 and 4]) . Consider the covering linear program min {c⊤y : Ay ≥ 1, y ≥ 0}, where A ∈ Rm×N 

> ≥0

and c ∈ RN>0. Let 

OPT denote its optimal value, and let q ≥ OPT be a given upper bound on the optimal objective. Assume that for every weight vector w ∈ Rm

> ≥0

, there is a polynomial-time α-approximate pricing oracle returning a column j ∈ [N ] with 

cj

P 

> i

Aij wi

≤ α · min  

> j′∈[N]

cj′

P 

> i

Aij ′ wi

.

Let W ≥ q max i,j (Aij /c j ) be a width bound. Then for every ε ∈ (0 , 1] , there is a polynomial-time algorithm that computes a feasible ˆy with c⊤ ˆy ≤ α(1 + ε + ε2)OPT . Moreover, |supp(ˆ y)| =

eO(mW α/ε 3).

Remark 3. The approximation ratio α(1 + ε + ε2) in Theorem 4 depends only on the oracle quality 

α and the accuracy parameter ε; it is independent of the width W . The width W affects only the support size and iteration count, not the approximation guarantee. In our setting W ≤ q = poly( n),so both quantities are polynomial. 

93.4 Rounding the Configuration LP 

Theorem 5. Let ˆy be any feasible solution to (1) –(3) with polynomial support. There is a random-ized polynomial-time algorithm that, with high probability, outputs a feasible integral overlap solution 

(Sc)c ∈ C covering all of V \ C and its cost is O(log n) · P

> c∈C

P  

> T∈T c

cost c(T )ˆ yT,c .Proof. Fix λ ≥ 2. Independently for each pair (c, T ) in the support of ˆy (which is of polynomial size), sample T with probability pT,c := min 1, λ ln( n) · ˆyT,c . For each c ∈ C, let Fc be the (multi)set of the sampled configurations and define Sc := {c} ∪ S  

> T∈F c

T. Adding {c} ensures c ∈ Sc even if 

Fc = ∅; since d(c, c ) = 0 , this adds zero cost. Each sampled T is connected and contains c, so Sc is a union of connected sets sharing c, hence connected. 

Coverage. Fix v ∈ V \ C. Feasibility of ˆy gives P  

> c,T :v∈T

ˆyT,c ≥ 1. If any pT,c = 1 for a pair with 

v ∈ T , then v is certainly covered. Otherwise pT,c = λ ln( n)ˆ yT,c for all covering pairs, and 

Pr[ v uncovered ] = Y  

> c,T :v∈T

(1 − pT,c ) ≤ exp 



− X  

> c,T :v∈T

pT,c 



≤ exp( −λ ln n0) = n−λ 

> 0

.

Union bound over v ∈ V \ C (there are n0 such vertices): 

Pr[ any v uncovered ] ≤ n0 · n−λ 

> 0

≤ n−(λ−1) 0 ≤ 1/n 0 = 1 /Ω( n)

for λ ≥ 2.

Cost. For each c, since the cost of a union is at most the sum of costs: 

X

> v∈Sc

d(v, c ) ≤ X 

> T∈F c

X

> v∈T

d(v, c ) = X

> T

1[T sampled ] · cost c(T ).

Taking expectation and summing over c:

E[ALG] ≤ X

> c,T

cost c(T ) · pT,c ≤ λ ln( n) X

> c,T

cost c(T )ˆ yT,c = O(log n) X

> c,T

cost c(T )ˆ yT,c .

By Markov’s inequality, a single run has cost at most twice its expectation with constant proba-bility; therefore, by performing O(log n) independent runs and returning the minimum-cost feasible solution, we obtain an O(log n)-approximation with high probability. 

3.5 Proof of Theorem 1 

Now we are ready to complete the proof of Theorem 1. 

Proof of Theorem 1. By combining Theorem 3 and Theorem 5, we obtain that if there exists a ρ-approximate density oracle, then there is a polynomial-time algorithm that computes an O(ρ log n)-approximate connected k-median clustering. For general graphs, Lemma 3.2 provides an O(log n)-approximate density oracle. Substituting ρ = O(log n), we obtain an O(log 2 n)-approximation. 

# 4 General Version 

In this section, we establish bicriteria approximation guarantees for the general version of the connected k-median clustering problem. In particular, we prove Theorem 2, which we restate below. 10 Theorem 2. Given a vertex set V , a metric d : V × V → R≥0, an undirected, unweighted connec-tivity graph G = ( V, E ) and an integer k, there exists a polynomial-time algorithm that computes an 

(O(log n), O (log 2 n)) -approximate connected k-median clustering. 

To prove Theorem 2, we follow an approach similar to that used for the assignment version. We first formulate a configuration LP for the general case and compute a near-optimal fractional solution in polynomial time. We then round this solution to obtain the desired bicriteria guarantee. We provide the details in the subsequent sections. 

4.1 General Version of the Configuration LP 

Primal (Configuration) LP: 

min X

> c∈V

X 

> T∈T c

cost c(T ) yT,c (8) s.t. X

> c∈V

X 

> T∈T c
> v∈T

yT,c ≥ 1 ∀v ∈ V , (9) 

X

> c∈V

X 

> T∈T c

yT,c ≤ k (10) 

yT,c ≥ 0 ∀c ∈ V, ∀T ∈ T c. (11) 

4.2 Computing a ρ-Approximate Solution of the Configuration LP for the Gen-eral Variant 

First, we interpret the center-budget constraint as P

> c∈V

P  

> T∈T c

yT,c ≤ k, which bounds the total configuration mass. This serves as a fractional relaxation of the requirement that at most k centers are opened. In particular, the variables yT,c are now defined for all c ∈ V and all T ∈ T c. Let 

OPT GCLP denote the optimal value of this general configuration LP. 

Lemma 4.1. Assume a ρ-approximate rooted minimum-density oracle for arbitrary nonnegative node costs and profits. Then, for every ε ∈ (0 , 1) , one can compute in polynomial time a polynomial-support fractional solution ˆy satisfying 

X

> c∈V

X 

> T∈T c
> v∈T

ˆyT,c ≥ 1 ∀v ∈ V, (12) 

X

> c∈V

X 

> T∈T c

ˆyT,c ≤ (1 + ε)k, (13) 

X

> c∈V

X 

> T∈T c

cost c(T )ˆ yT,c ≤ (1 + ε)ρ · OPT GCLP . (14) 

Moreover, ˆy may be chosen with support size polynomial in the input size and 1/ε .Proof. Write the general configuration LP as a mixed packing/covering program with one packing constraint. The covering constraints are indexed by vertices v ∈ V , and the columns are indexed by pairs (c, T ) with c ∈ V and T ∈ T c. For such a column, define 

Av, (c,T ) := 1[v ∈ T ], b(c,T ) := 1 , q(c,T ) := cost c(T ).

11 Thus the LP has the form 

min X

> c,T

q(c,T )yT,c s.t. Ay ≥ 1, X

> c,T

b(c,T )yT,c ≤ k, y ≥ 0.

We apply the standard oracle-based framework for mixed packing and covering programs, as in Young [You01] and Plotkin–Shmoys–Tardos [PST91]. It suffices to identify the pricing problem required by this framework. For a guessed objective value τ , the feasibility version is 

Ay ≥ 1, X

> c,T

yT,c ≤ k, X

> c,T

cost c(T )yT,c ≤ τ, y ≥ 0.

In the mixed packing/covering framework, a pricing step is given nonnegative weights pv on the covering constraints and nonnegative multipliers η, θ for the two packing constraints. It asks for a column minimizing the ratio 

η · b(c,T ) + θ · q(c,T )

P 

> v∈V

pvAv, (c,T )

= η + θ cost c(T )

P 

> v∈T

pv

.

For a fixed root c, this is exactly a rooted minimum-density connected-set problem. Indeed, define node costs wη,θ c (u) := θ d (u, c ) + η · 1[u = c], and node profits αu := pu. Since every T ∈ T c contains 

c, we have X

> u∈T

wη,θ c (u) = θ X

> u∈T

d(u, c ) + η = θcost c(T ) + η. 

Therefore the pricing ratio is precisely     

> P
> u∈Twη,θ c(u)
> P
> v∈Tαv

. Calling the rooted minimum-density oracle once for each possible root c ∈ V and taking the best returned column gives a ρ-approximate pricing oracle for the mixed packing/covering instance. By the standard guarantee for mixed packing and covering with a ρ-approximate pricing oracle, binary search over τ and the above pricing routine compute, in polynomial time, a solution sat-isfying (12), (13), and (14). The factor (1 + ε) accounts for the usual accuracy loss in the mixed packing/covering feasibility procedure. Finally, the algorithm generates only polynomially many columns. Solving the restricted mixed LP over the generated columns and taking a basic feasible solution yields a polynomial-support solution ˆy with the same bounds. 

Corollary 1. For general connectivity graphs, one can compute in polynomial time a fractional solution ˆy satisfying (12) , (13) , and 

X

> c∈V

X 

> T∈T c

cost c(T )ˆ yT,c ≤ O(log n) · OPT GCLP .

Proof. By Lemma 3.2, the rooted minimum-density oracle has approximation ratio ρ = O(log n) on general graphs. Applying Lemma 4.1 with a constant ε gives the claim. 12 4.3 Rounding the General Version of the Configuration LP 

In this subsection, we round the fractional solution obtained from Lemma 4.1. 

Lemma 4.2. Suppose we are given a polynomial-support feasible solution ˆy to (8) –(11) of cost at most Γ. Then there is a randomized polynomial-time algorithm that outputs, with high probability, an overlapping connected clustering using at most O(k log n) centers of cost O(log n) · Γ.Proof. Fix a constant λ ≥ 2. Independently for every pair (c, T ) in the support of ˆy, sample the configuration T with probability pT,c := min {1, λ ln n · ˆyT,c }. For each c ∈ V , let Fc be the collection of the sampled configurations rooted at c. We open center c if Fc̸ = ∅, and define its cluster as 

Sc := S  

> T∈F c

T . Since every sampled set T ∈ T c contains c and is connected, the union Sc is connected whenever Fc̸ = ∅.We first prove coverage. Fix a vertex v ∈ V . By feasibility of ˆy, P

> c∈V

PT ∈T c

> v∈T

ˆyT,c ≥ 1. If there exists a pair (c, T ) with v ∈ T and pT,c = 1 , then v is covered with probability one. Otherwise, 

Pr[ v is uncovered ] = Y 

> c,T :v∈T

(1 − pT,c ) ≤ exp 



− X 

> c,T :v∈T

pT,c 



= exp 



− λ ln n X 

> c,T :v∈T

ˆyT,c 



≤ n−λ.

Taking a union bound over all vertices, the probability that some vertex is uncovered is at most 

n1−λ, which is at most 1/n for λ ≥ 2.Next, we bound the number of opened centers. Let N be the number of the sampled configura-tions. Since the number of opened centers is at most N , it suffices to bound N . We have 

E[N ] = X

> c∈V

X 

> T∈T c

pT,c ≤ λ ln n X

> c∈V

X 

> T∈T c

ˆyT,c ≤ λ(1 + ε)k ln n, 

where the last inequality follows from Constraint (13). By a Chernoff bound, N = O(k log n) with high probability. Therefore, the algorithm opens at most O(k log n) centers with high probability. Finally, we bound the cost. Since the cost of a union is at most the sum of the costs of the sampled configurations, cost ({Sc}) = X 

> c:Fc̸=∅

X

> v∈Sc

d(v, c ) ≤ X

> c∈V

X 

> T∈F c

cost c(T ).

Taking expectation, 

E[cost ({Sc})] ≤ X

> c∈V

X 

> T∈T c

pT,c cost c(T ) ≤ λ ln n X

> c∈V

X 

> T∈T c

cost c(T )ˆ yT,c = O(log n) · Γ.

By Markov’s inequality, a single run has cost at most twice its expectation with constant probability; therefore, by performing O(log n) independent runs and returning the minimum-cost feasible solu-tion, we obtain an O(log n)-approximation with high probability. This proves the claimed bicriteria guarantee. 13 4.4 Proof of Theorem 2 

Now, we are ready to complete the proof of Theorem 2. 

Proof of Theorem 2. Let ˆy be the fractional solution returned by Lemma 4.1 with a fixed constant 

ε ∈ (0 , 1) . Then ˆy satisfies the covering constraints and P

> c∈V

P  

> T∈T c

ˆyT,c ≤ (1 + ε)k, and its cost is at most (1 + ε)ρ · OPT GCLP ≤ (1 + ε)ρ · OPT , where the last inequality follows because the general configuration LP is a relaxation of the non-assignment overlapping connected k-median problem. Now apply Lemma 4.2 to ˆy with Γ := (1 + ε)ρ · OPT . The rounding opens at most O (1 + 

ε)k log n = O(k log n) centers with high probability, and its expected cost is at most O(log n) · Γ = 

O(ρ log n) · OPT . Thus we obtain an  O(log n), O (ρ log n) bicriteria approximation. For general graphs, Lemma 3.2 gives ρ = O(log n). Therefore, the bicriteria guarantee becomes  O(log n), O (log 2 n). This proves Theorem 2. 

# 5 Conclusion and Open Problems 

In this paper, we developed a configuration-LP framework for the connected k-median problem. Un-like previous cut-based relaxations, our formulation incorporates connectivity directly into the LP variables by working with connected configurations rooted at centers. We combined this viewpoint with covering-LP techniques and rooted minimum-density oracles to obtain approximation guaran-tees for both the assignment version and a bicriteria variant of the general version. In particular, we obtained an O(log 2 n)-approximation for the assignment version on general graphs, together with an (O(log n), O (log 2 n)) bicriteria approximation for the general version. More broadly, our results suggest that configuration LPs provide a natural framework for han-dling connectivity constraints in clustering problems. By separating the problem into a covering-LP layer and a graph-theoretic rooted density oracle, the framework makes it possible to incorporate stronger oracles for special graph families and related connectivity models. We believe this viewpoint may be useful beyond connected k-median, particularly for other constrained clustering objectives where connectivity plays a central role. Several interesting open problems remain. The most natural question is whether one can obtain polylogarithmic approximation guarantees for the general version containing exactly k centers. More generally, it would be interesting to better understand the power and limitations of configuration-LP formulations for connectivity-constrained clustering problems. Another promising direction is to design stronger rooted minimum-density oracles for restricted graph families, such as minor-closed graphs. Since the rooted minimum-density problem already admits an O(log n)-approximation in general graphs, improving this guarantee on structured graph families would immediately translate into stronger approximation guarantees within our framework. 14 References 

[ABV21] Mohsen Abbasi, Aditya Bhaskara, and Suresh Venkatasubramanian. Fair clustering via equitable group representations. In Proceedings of the 2021 ACM conference on fairness, accountability, and transparency , pages 504–514, 2021. [AEK +20] Sara Ahmadian, Alessandro Epasto, Ravi Kumar, and Mohammad Mahdian. Fair cor-relation clustering. In International conference on artificial intelligence and statistics ,pages 4195–4205. PMLR, 2020. [AGK +01] Vijay Arya, Naveen Garg, Rohit Khandekar, Adam Meyerson, Kamesh Munagala, and Vinayaka Pandit. Local search heuristic for k-median and facility location problems. In Proceedings of the thirty-third annual ACM symposium on Theory of computing ,pages 21–29, 2001. [AGS +20] Saba Ahmadi, Sainyam Galhotra, Barna Saha, and Roy Schwartz. Fair correlation clustering. arXiv preprint arXiv:2002.03508 , 2020. [AN23] Sara Ahmadian and Maryam Negahbani. Improved approximation for fair correla-tion clustering. In International Conference on Artificial Intelligence and Statistics ,pages 9499–9516. PMLR, 2023. [BCF +19] Suman Bera, Deeparnab Chakrabarty, Nicolas Flores, and Maryam Negahbani. Fair algorithms for clustering. In Advances in Neural Information Processing Systems , vol-ume 32, 2019. [BGK +19] Ioana O Bercea, Martin Groß, Samir Khuller, Aounon Kumar, Clemens Rösner, Daniel R Schmidt, and Melanie Schmidt. On the cost of essentially fair clusterings. In Ap-proximation, Randomization, and Combinatorial Optimization. Algorithms and Tech-niques (APPROX/RANDOM 2019) , pages 18–1. Schloss Dagstuhl–Leibniz-Zentrum für Informatik, 2019. [BHL18] MohammadHossein Bateni, MohammadTaghi Hajiaghayi, and Vahid Liaghat. Im-proved approximation algorithms for (budgeted) node-weighted steiner problems. 

SIAM Journal on Computing , 47(4):1513–1543, 2018. [BIO +19] Arturs Backurs, Piotr Indyk, Krzysztof Onak, Baruch Schieber, Ali Vakilian, and Tal Wagner. Scalable fair clustering. In International conference on machine learning ,pages 405–413. PMLR, 2019. [BS16] Punam Bedi and Chhavi Sharma. Community detection in social networks. Wiley interdisciplinary reviews: Data mining and knowledge discovery , 6(3):115–135, 2016. [CBC +23] Fanglan Chen, Subhodip Biswas, Zhiqian Chen, Shuo Lei, Naren Ramakrishnan, and Chang-Tien Lu. Exploring tradeoffs in automated school redistricting: computational and ethical perspectives. In Proceedings of the AAAI Conference on Artificial Intelli-gence , volume 37, pages 15912–15920, 2023. [CCD +25] Diptarka Chakraborty, Kushagra Chatterjee, Debarati Das, Tien Long Nguyen, and Romina Nobahari. Towards fair representation: clustering and consensus. arXiv preprint arXiv:2506.08673 , 2025. 15 [CCD +26a] Diptarka Chakraborty, Kushagra Chatterjee, Debarati Das, and Tien-Long Nguyen. A generic framework for fair consensus clustering in streams. arXiv preprint arXiv:2602.11500 , 2026. [CCD +26b] Diptarka Chakraborty, Kushagra Chatterjee, Debarati Das, and Tien-Long Nguyen. Generalizing fair clustering to multiple groups: algorithms and applications. In Pro-ceedings of the AAAI Conference on Artificial Intelligence , volume 40, pages 19934– 19942, 2026. [CEV21] Chandra Chekuri, Alina Ene, and Ali Vakilian. Node-weighted network design in pla-nar and minor-closed families of graphs. ACM Transactions on Algorithms (TALG) ,17(2):1–25, 2021. [CFL +19] Xingyu Chen, Brandon Fain, Liang Lyu, and Kamesh Munagala. Proportionally fair clustering. In International conference on machine learning , pages 1032–1041. PMLR, 2019. [CG99] Moses Charikar and Sudipto Guha. Improved combinatorial algorithms for the facil-ity location and k-median problems. In 40th Annual Symposium on Foundations of Computer Science (Cat. No. 99CB37039) , pages 378–388. IEEE, 1999. [CGH +22] Vincent Cohen-Addad, Anupam Gupta, Lunjia Hu, Hoon Oh, and David Saulpic. An improved local search algorithm for k-median. In Proceedings of the 2022 Annual ACM-SIAM Symposium on Discrete Algorithms (SODA) , pages 1556–1612. SIAM, 2022. [CGL +25] Vincent Cohen-Addad, Fabrizio Grandoni, Euiwoong Lee, Chris Schwiegelshohn, and Ola Svensson. A (2+ ε)-approximation algorithm for metric k-median. In Proceedings of the 57th Annual ACM Symposium on Theory of Computing , pages 615–624, 2025. [CGT +99] Moses Charikar, Sudipto Guha, Éva Tardos, and David B Shmoys. A constant-factor approximation algorithm for the k-median problem. In Proceedings of the thirty-first annual ACM symposium on Theory of computing , pages 1–10, 1999. [Che06] Ke Chen. On k-median clustering in high dimensions. In Proceedings of the seventeenth annual ACM-SIAM symposium on Discrete algorithm , pages 1177–1185, 2006. [CKL +17] Flavio Chierichetti, Ravi Kumar, Silvio Lattanzi, and Sergei Vassilvitskii. Fair clus-tering through fairlets. Advances in neural information processing systems , 30, 2017. [CKM +01] Moses Charikar, Samir Khuller, David M Mount, and Giri Narasimhan. Algorithms for facility location problems with outliers. In SODA , volume 1, pages 642–651, 2001. [DEL +24] Lukas Drexler, Jan Eube, Kelin Luo, Dorian Reineccius, Heiko Röglin, Melanie Schmidt, and Julian Wargalla. Connected k-center and k-diameter clustering. Algo-rithmica , 86(11):3425–3464, 2024. [DHK09] Erik D Demaine, MohammadTaghi Hajiaghayi, and Philip N Klein. Node-weighted steiner tree and group steiner tree in planar graphs. In International Colloquium on Automata, Languages, and Programming , pages 328–340. Springer, 2009. [DMV22] Zhen Dai, Yury Makarychev, and Ali Vakilian. Fair representation clustering with several protected classes. In Proceedings of the 2022 ACM Conference on Fairness, Accountability, and Transparency , pages 814–823, 2022. 16 [EBS +21] Seyed Esmaeili, Brian Brubach, Aravind Srinivasan, and John Dickerson. Fair clus-tering under a bounded cost. Advances in Neural Information Processing Systems ,34:14345–14357, 2021. [EBT +20] Seyed Esmaeili, Brian Brubach, Leonidas Tsepenekas, and John Dickerson. Proba-bilistic fair clustering. Advances in Neural Information Processing Systems , 33:12743– 12755, 2020. [ELR +25] Jan Eube, Kelin Luo, Dorian Reineccius, Heiko Röglin, and Melanie Schmidt. Con-nected k-median with disjoint and non-disjoint clusters. In Proceedings of the 33rd Annual European Symposium on Algorithms (ESA) , LIPIcs, 2025. doi : 10 . 4230 / LIPIcs.ESA.2025.63 .[FB17] Hossein Fani and Ebrahim Bagheri. Community detection in social networks. Ency-clopedia with semantic computing and robotic intelligence , 1(01):1630001, 2017. [GEG +08] Rong Ge, Martin Ester, Byron J Gao, Zengjian Hu, Binay Bhattacharya, and Boaz Ben-Moshe. Joint cluster analysis of attribute data and relationship data: the con-nected k-center problem, algorithms and applications. ACM Transactions on Knowl-edge Discovery from Data (TKDD) , 2(2):1–35, 2008. [Gon85] Teofilo F Gonzalez. Clustering to minimize the maximum intercluster distance. The-oretical computer science , 38:293–306, 1985. [GPS11] Neelima Gupta, Aditya Pancholi, and Yogish Sabharwal. Clustering with internal con-nectedness. In International Workshop on Algorithms and Computation , pages 158– 169. Springer, 2011. [GSV21] Mehrdad Ghadiri, Samira Samadi, and Santosh Vempala. Socially fair k-means clus-tering. In Proceedings of the 2021 ACM Conference on Fairness, Accountability, and Transparency , pages 438–448, 2021. [HN79] Wen-Lian Hsu and George L Nemhauser. Easy and hard bottleneck location problems. 

Discrete Applied Mathematics , 1(3):209–215, 1979. [HS85] Dorit S Hochbaum and David B Shmoys. A best possible heuristic for the k-center problem. Mathematics of operations research , 10(2):180–184, 1985. [JKL20] Christopher Jung, Sampath Kannan, and Neil Lutz. Service in your neighborhood: fairness in center location. Foundations of Responsible Computing (FORC) , 2020. [JMS02] Kamal Jain, Mohammad Mahdian, and Amin Saberi. A new greedy approach for facility location problems. In Proceedings of the thiry-fourth annual ACM symposium on Theory of computing , pages 731–740, 2002. [KS00] Samir Khuller and Yoram J Sussmann. The capacitated k-center problem. SIAM Jour-nal on Discrete Mathematics , 13(3):403–418, 2000. [Li16] Shi Li. Approximating capacitated k-median with k open facilities. In Proceedings of the twenty-seventh annual ACM-SIAM symposium on Discrete algorithms , pages 786– 796. SIAM, 2016. [MV20] Sepideh Mahabadi and Ali Vakilian. Individual fairness for k-clustering. In Interna-tional conference on machine learning , pages 6586–6596. PMLR, 2020. 17 [MV21] Yury Makarychev and Ali Vakilian. Approximation algorithms for socially fair clus-tering. In Conference on Learning Theory , pages 3246–3264. PMLR, 2021. [PST91] S. A. Plotkin, D. B. Shmoys, and É. Tardos. Fast approximation algorithms for frac-tional packing and covering problems. In Proceedings of the 32nd Annual Symposium on Foundations of Computer Science (FOCS) , 1991. doi : 10 . 1109 / SFCS . 1991 . 185411 .[Sha20] Eklavya Sharma. An approximation algorithm for covering linear programs and its application to bin-packing. arXiv preprint arXiv:2011.11268 , 2020. [VBL22] Hamidreza Validi, Austin Buchanan, and Eugene Lykhovyd. Imposing contiguity con-straints in political districting models. Operations Research , 70(2):867–892, 2022. [VY22] Ali Vakilian and Mustafa Yalciner. Improved approximation algorithms for individu-ally fair clustering. In International conference on artificial intelligence and statistics ,pages 8758–8779. PMLR, 2022. [You01] Neal E Young. Sequential and parallel algorithms for mixed packing and covering. In 

Proceedings 42nd IEEE symposium on foundations of computer science , pages 538– 546. IEEE, 2001. 

# A Proof of Lemma 3.1 

Proof. Fix a feasible overlapping solution (Sc)c∈C . By feasibility, for every c ∈ C, we have c ∈ Sc

and G[Sc] is connected. Hence Sc ∈ T c for every center c. Define a configuration-LP solution y by 

yT,c := 

(

1, if T = Sc,

0, otherwise .

We verify feasibility. First, for every v ∈ V \ C, the overlapping solution satisfies S 

> c∈C

Sc = V , so there exists at least one center c such that v ∈ Sc. Therefore, P

> c∈C

PT ∈T c

> v∈T

yT,c ≥ 1, and thus the covering constraints (2) hold for all v ∈ V \ C. Second, by construction, all variables satisfy 

yT,c ≥ 0, so (3) also holds. Finally, the objective value of this LP solution is 

X

> c∈C

X 

> T∈T c

cost c(T ) yT,c = X

> c∈C

cost c(Sc) = X

> c∈C

X

> v∈Sc

d(v, c ),

which is exactly the cost of the given integral solution. Since every feasible solution to the assignment variant of connected k-median with overlapping clusters induces a feasible solution to the configuration LP with the same cost, the optimal value of the configuration LP is at most OPT , the optimal cost of connected k-median with overlapping clusters for the prescribed center set C.

# B Proof of Lemma 3.2 

Proof. Write w(T ) := P 

> v∈T

w(v) and α(T ) := P 

> v∈T

αv. Let T ∗ be an optimal density solution with λ∗ := w(T ∗)/α (T ∗).18 We use the bicriteria guarantee of Bateni et al. [BHL18, Theorem 3] for the rooted budgeted node-weighted Steiner tree problem . In this problem, we are given a graph G = ( V, E ), a designated root c ∈ V , a non-negative node-cost function w : V → R≥0, a non-negative node-profit function 

α : V → R≥0, and a budget B ≥ 0. The goal is to find a connected tree T ⊆ G containing c whose total node cost is at most B and whose total profit α(T ) := P 

> v∈T

α(v) is as large as possible. Let 

α∗(B) := max {α(T ) : T ∋ c is connected and w(T ) ≤ B} where w(T ) := X

> v∈T

w(v).

Then, for any budget B and any ε > 0, there is a polynomial-time algorithm that returns a connected tree TB ∋ c such that w(TB ) ≤ (1 + ε)B and α(TB ) ≥ εO(log n) · α∗(B). In other words, the algorithm may violate the budget by a factor of at most 1+ ε, while guaranteeing an O(log n/ε )-approximation to the maximum achievable profit under budget B.Set Wmax := w(V ) and try the O(log Wmax ) budgets B ∈ { 1, 2, 4, . . . , 2⌈log 2 Wmax ⌉}. There exists 

B⋆ in this list with w(T ∗) ≤ B⋆ ≤ 2w(T ∗). Apply the guarantee of Bateni et al. [BHL18] with 

ε := 1 . Since w(T ∗) ≤ B⋆, we have α∗(B⋆) ≥ α(T ∗). The algorithm returns TB⋆ ∋ c with 

w(TB⋆ ) ≤ 2B⋆ ≤ 4w(T ∗) and α(TB⋆ ) ≥ α(T ∗)/O (log n). Therefore, 

dens( TB⋆ ) = w(TB⋆ )

α(TB⋆ ) ≤ 4 w(T ∗)

α(T ∗)/O (log n) = O(log n) · λ∗.

Returning arg min B dens( TB ) over all tried budgets yields an O(log n)-approximate density oracle in polynomial time. 

# C Proof of Theorem 4 

Proof. The covering LP min {c⊤y : Ay ≥ 1, y ≥ 0} has (in our usage) exponentially many columns, so we cannot solve it directly; we only have oracle access to an approximately-best column for any given constraint weighting. The proof has three parts. 1. We first observe that the α-approximate pricing oracle we are given is indeed also an η-weak 

index-finding oracle ( η = 1 /α ) for a natural quantity Dj (w), the “coverage per unit weight” of column j.2. We then show that an index-finding oracle over columns is the same thing as a point-finding oracle over a certain simplex-like polytope Pr (all vectors of cost exactly r). This is the step that lets us invoke a known black-box algorithm for fractional covering, which expects a point-finding oracle, not a column oracle. 3. The black-box algorithm, for a fixed guess r of the optimal cost, either certifies cost r suffices (up to a small violation) or fails. We do not know OPT in advance, so we do a binary search over r; two monotonicity facts about this success/failure signal pin down r ≈ OPT and let us convert the near-feasible solution found at the end into a genuinely feasible one, at a controlled extra cost. In what follows, we elaborate on each of these steps. 19 Part 1: from pricing to index-finding. For a column j and weight vector w ∈ Rm

> ≥0

, let 

Dj (w) := w⊤A·j 

> cj

=   

> P
> iAij wi
> cj

. Think of Dj (w) as how much weighted coverage column j buys you per unit of its cost. An oracle is an η-weak index-finding oracle if, given w ≥ 0, it returns some 

j with Dj (w) ≥ η max ℓ Dℓ(w), in other words a column that is within a factor η ≤ 1 of the best possible coverage-per-cost. The assumed pricing oracle minimizes the reciprocal quantity cj /(w⊤A·j ) = 1 /D j (w) to within a factor α: If cj 

> w⊤A·j

≤ α · min j′ 

> cj′
> w⊤A·j′

, then taking reciprocals (all quantities are positive since 

c > 0; the degenerate case max ℓ Dℓ(w) = 0 , i.e. when w is orthogonal to every column, makes the index-finding requirement vacuous and is set aside), then 

Dj (w) ≥ 1

α max  

> ℓ

Dℓ(w).

So the pricing oracle is an η-weak index-finding oracle with η := 1 /α .

Part 2: from index-finding to point-finding on Pr. Fix a target cost r ≥ 0 and consider the cost-r slice of the nonnegative orthant, Pr := {y ∈ RN 

> ≥0

: c⊤y = r}. We introduce this set because known fractional-covering algorithms are stated abstractly for a polytope P together with a point-finding oracle (return an approximately-best point of P for a given linear objective), and we want to feed our column oracle into one of them. The set Pr is exactly the right polytope: every 

y ∈ Pr can be written as y = PNj=1   

> cjyj
> r

· rcj ej , and the coefficients cj yj /r are nonnegative and sum to 1 (since P 

> j

cj yj = c⊤y = r), so y is a convex combination of the points {(r/c j )ej : j ∈ [N ]}.In other words, Pr is the simplex whose vertices are, for each column j, spend the entire budget r

on column j alone. Maximizing a linear function over a simplex is achieved at a vertex, so for any weight vector w ≥ 0,

max 

> y∈Pr

w⊤Ay = max   

> j∈[N]

w⊤A

 rcj

ej



= r · max  

> j

Dj (w).

This identity is the crux of this part: finding a good point of Pr for objective w⊤A(·) is literally the same problem as finding a good column for Dj (w), just rescaled by r. So our η-weak index-finding oracle, applied to w, returns a column j with Dj (w) ≥ η max ℓ Dℓ(w); setting y := ( r/c j )ej ∈ Pr

gives 

w⊤Ay = rD j (w) ≥ η · r max  

> ℓ

Dℓ(w) = η max 

> y′∈Pr

w⊤Ay ′,

In other words, y is an η-weak maximizer of w⊤Ay over Pr. We have therefore converted our column oracle into an η-weak point-finding oracle for Pr, for every r simultaneously (the construction above does not depend on r except through the final rescaling). We also record the width of this setup, since the black-box algorithm’s running time depends on it: by the same vertex computation, 

width( A, 1, P r) := max 

> y∈Pr

max  

> i

(Ay )i = max  

> i,j

Aij · rcj

= r max 

> i,j

Aij 

cj

.

Using any known bound q ≥ OPT and the given W ≥ q max i,j Aij /c j , we get, for every r ≤ q,

width( A, 1, P r) = r max 

> i,j

Aij 

cj

≤ q max 

> i,j

Aij 

cj

≤ W. 

20 Part 3: black-box covering, then binary search on r. We now invoke, as a black box, the standard guarantee for fractional covering with an approximate point-finding oracle (Plotkin– Shmoys–Tardos; we use the clean restatement of Sharma [Sha20], Theorems 3–4): given the η-weak point-finding oracle for Pr from Part 2 and the width bound W from Part 2, and setting μ := η 

> 1+ ϵ

,

the algorithm either correctly reports {y ∈ Pr : Ay ≥ 1} is empty or returns some y ∈ Pr with 

Ay ≥ μ1 (i.e. every covering constraint is satisfied up to a (1 − μ) shortfall) using a number of oracle calls that yields support size |supp( y)| = eO(mW/ (ηϵ 3)) when the oracle always returns a single vertex (which ours does, by construction in Part 2). Call this routine on input r and write 

g(r) = 1 if it succeeds (returns such a y) and g(r) = 0 if it reports infeasibility. The routine above only tells us, for a given r, whether spending exactly r can nearly cover everything. We do not know OPT , so we search for the smallest r at which g(r) flips from 0 to 1.The bisection argument has two parts: 

(i) r ≥ OPT =⇒ g(r) = 1 . Let y∗ be an optimal solution of the original covering LP, so 

c⊤y∗ = OPT and Ay ∗ ≥ 1. Rescaling, r 

> OPT

y∗ ∈ Pr (it has cost exactly r) and A  r 

> OPT

y∗ = 

> r
> OPT

Ay ∗ ≥ r 

> OPT

1 ≥ 1 since r ≥ OPT . So {y ∈ Pr : Ay ≥ 1} is genuinely nonempty, and since it is nonempty (and in particular Ay ≥ 1 ≥ μ1 trivially, as μ ≤ 1), the routine cannot correctly report infeasibility; hence g(r) = 1 .

(ii) r < μ · OPT = ⇒ g(r) = 0 . Suppose instead g(r) = 1 , i.e. the routine returns y ∈ Pr with 

Ay ≥ μ1. Rescale: ˆy := y/μ satisfies Aˆy = Ay/μ ≥ 1, so ˆy is feasible for the original covering LP, with cost c⊤ ˆy = ( c⊤y)/μ = r/μ < OPT (using r < μ · OPT ). This contradicts optimality of OPT .So g(r) = 1 is impossible, i.e. g(r) = 0 .Then, we initialize ℓ = 0 , u = q (both satisfy g(ℓ) = 0 , g (u) = 1 , using fact (ii) at ℓ = 0 whenever 

OPT > 0), and repeatedly query the midpoint r = ( ℓ + u)/2: if g(r) = 0 set ℓ := r, otherwise set 

u := r and record the returned vector. Stop once u ≤ (1 + δ)ℓ for δ := ε2/(1 + ε); this takes 

O(log( q/ (δ · (smallest gap )))) iterations, polynomial in the input size. Finally, at termination g(ℓ) = 0 forces ℓ < OPT (else fact (i) would give g(ℓ) = 1 ), so u ≤

(1 + δ)ℓ < (1 + δ) OPT . Let y(u) ∈ Pu be the vector recorded at the last successful call ( g(u) = 1 ), so Ay (u) ≥ μ1 and c⊤y(u) = u. Define ˆy := 1 

> μ

y(u). Exactly as in fact (ii)’s rescaling, Aˆy ≥ 1, so ˆy is feasible for the original covering LP. Its cost is c⊤ ˆy = uμ ≤ (1+ δ) 

> μ

OPT . Substituting μ = η/ (1 + ε)

and δ = ε2/(1 + ε):

1 + δμ =



1 + ε2

1 + ε



· 1 + εη = 1 + ε + ε2

η = α(1 + ε + ε2),

using η = 1 /α from Part 1. This gives the claimed cost bound c⊤ ˆy ≤ α(1 + ε + ε2)OPT . Rescaling by 1/μ does not change the support, so 

|support (ˆ y)| = |support (y(u))| = eO

 mW ηε 3



= eO

 mW α ε3



,

matching the statement of the theorem. 

Runtime. Each bisection step invokes the black-box covering routine once, which in turn calls our point-finding oracle (equivalently, the original pricing oracle, once per iteration of that routine) 

poly( m, W, 1/ε ) times; the number of bisection steps is poly(log( q/ OPT) , log(1 /δ )) . All oracle calls and arithmetic operations are polynomial whenever q, W , and the input encoding lengths are polynomially bounded, giving the stated overall polynomial running time. 21