---
title: Learning Spherical Occupancy Profiles for Multi-View 3D Reconstruction and Generation
source: arxiv
title_zh: 用于Multi-View 3D Reconstruction与Generation的Learning Spherical Occupancy Profiles
abstract_zh: "这篇论文研究 spherical occupancy profiles- ray-wise occupancy probability profiles P (r) = T (r) o (r) dis。\n\n核心做法：On 999-object subset 的 Google Scanned Objects 结合 48 turntable views each，we train (i) discriminative。\n\n主要结果：We further analyze morphology 的 predicted profiles：post-hoc power sharpening 与 learned sharpening ta。\n\n值得关注的点在于：Our results suggest that ray-wise occupancy profiles offer compact，learned，与 uncertainty-aware inter。"
abstract_en: "We study spherical occupancy profiles-the ray-wise occupancy probability profiles P(r) = T(r) o(r) distilled from multi-view 3D Gaussian reconstructions-as a unified intermediate representation for both discriminative and generative 3D reconstruction from images. On a 999-object subset of Google Scanned Objects with 48 turntable views each, we train (i) a discriminative per-ray decoder that injects global view-averaged and ray-specific image evidence into a FiLM-conditioned profile head, reaching median soft depth error 0.035 (normalized) on an independent 90-object test split, and (ii) a generative pipeline built on a profile VAE and a latent diffusion model, which supports unconditional sampling that matches the reconstruction manifold and image-conditioned multi-solution reconstruction whose per-object solution spread is quantifiable and tunable via classifier-free guidance. We further analyze the morphology of predicted profiles: post-hoc power sharpening and a learned sharpening target both recover ground-truth profile width without degrading depth, exposing a monotonic width-peak frontier in the L1-per-ray loss family and motivating a principled redefinition of morphology gates. Real-photo validation on two DTU scenes confirms the pipeline transfers to non-synthetic input. Our results suggest that ray-wise occupancy profiles offer a compact, learned, and uncertainty-aware interface between multi-view reconstruction and generative priors."
authors: YiHsuan Tsai
date: 2026-08-24
journal: cs.CV
source_link: "https://arxiv.org/pdf/2608.23206v1"
pdf: "https://arxiv.org/pdf/2608.23206v1"
score: 6.0
tldr: 围绕《用于Multi-View 3D Reconstr...》；On 999-object subset 的 Google Sc...；We further analyze morphology 的 pred...
tldr_cn: 围绕《用于Multi-View 3D Reconstr...》；On 999-object subset 的 Google Sc...；We further analyze morphology 的 pred...
tldr_en: "We study spherical occupancy profiles-the ray-wise occupancy probability profiles P(r) = T(r) o(r) distilled from multi-view 3D Gaussian reconstructions-as a unified intermediate representation for both discriminative and generative 3D reconstruction from images. On a 999-object subset of Google Scanned Objects with 48 turntable views each, we train (i) a discriminative per-ray decoder that injects global..."
selection_source: fresh_fetch
vocabulary: ["discriminative | 判别式的 | IELTS", "reconstruction | 重建 | CET6", "uncertainty | 不确定性 | IELTS", "generative | 生成式的 | IELTS", "diffusion | 扩散 | 学术高频"]
motivation: 这篇论文研究 spherical occupancy profiles- ray-wise occupancy probability profiles P (r) = T (r) o (r) dis。
method: 核心做法：On 999-object subset 的 Google Scanned Objects 结合 48 turntable views each，we train (i) discriminative。
result: 主要结果：We further analyze morphology 的 predicted profiles：post-hoc power sharpening 与 learned sharpening ta。
conclusion: 值得关注的点在于：Our results suggest that ray-wise occupancy profiles offer compact，learned，与 uncertainty-aware inter。
---

## 摘要
这篇论文研究 spherical occupancy profiles- ray-wise occupancy probability profiles P (r) = T (r) o (r) dis。

核心做法：On 999-object subset 的 Google Scanned Objects 结合 48 turntable views each，we train (i) discriminative。

主要结果：We further analyze morphology 的 predicted profiles：post-hoc power sharpening 与 learned sharpening ta。

值得关注的点在于：Our results suggest that ray-wise occupancy profiles offer compact，learned，与 uncertainty-aware inter。

## Abstract
We study spherical occupancy profiles-the ray-wise occupancy probability profiles P(r) = T(r) o(r) distilled from multi-view 3D Gaussian reconstructions-as a unified intermediate representation for both discriminative and generative 3D reconstruction from images. On a 999-object subset of Google Scanned Objects with 48 turntable views each, we train (i) a discriminative per-ray decoder that injects global view-averaged and ray-specific image evidence into a FiLM-conditioned profile head, reaching median soft depth error 0.035 (normalized) on an independent 90-object test split, and (ii) a generative pipeline built on a profile VAE and a latent diffusion model, which supports unconditional sampling that matches the reconstruction manifold and image-conditioned multi-solution reconstruction whose per-object solution spread is quantifiable and tunable via classifier-free guidance. We further analyze the morphology of predicted profiles: post-hoc power sharpening and a learned sharpening target both recover ground-truth profile width without degrading depth, exposing a monotonic width-peak frontier in the L1-per-ray loss family and motivating a principled redefinition of morphology gates. Real-photo validation on two DTU scenes confirms the pipeline transfers to non-synthetic input. Our results suggest that ray-wise occupancy profiles offer a compact, learned, and uncertainty-aware interface between multi-view reconstruction and generative priors.

## 英语词汇
- **discriminative**：判别式的（IELTS）
- **reconstruction**：重建（CET6）
- **uncertainty**：不确定性（IELTS）
- **generative**：生成式的（IELTS）
- **diffusion**：扩散（学术高频）
