---
title: Deterministic Spectral Sparsification in Almost-Linear Time for Dense Graphs
source: arxiv
title_zh: 用于Dense Graphs的Deterministic Spectral Sparsification in Almost-Linear Time
abstract_zh: "spectral sparsifier 的 weighted graph is reweighted subgraph whose Laplacian quadratic form approxima。\n\n核心做法：Let G be positively weighted n-vertex，m-edge multigraph，let 0<\\varepsilon\\le1/2。\n\n主要结果：We evaluate resulting conditional-expectation scores in two ways to produce two algorithms：使用 dense。\n\n值得关注的点在于：After O (\\log n) levels，exponent is 2+O (1/\\log n)，yielding m^{1+o (1)}+\\widetilde O_{\\varepsilon}(n。"
abstract_en: "A spectral sparsifier of a weighted graph is a reweighted subgraph whose Laplacian quadratic form approximates that of the original graph. Let $G$ be a positively weighted $n$-vertex, $m$-edge multigraph, let $0<\\varepsilon\\le1/2$. Assuming $m,\\varepsilon^{-1}\\le n^{O(1)}$ and the ratio of maximum to minimum weight is polynomially bounded, we deterministically construct a $(1\\pm\\varepsilon)$-spectral sparsifier with \\[   O\\!\\left(n\\varepsilon^{-2}\\log^{24+o(1)}n\\right) \\] edges in \\[   m^{1+o(1)}+O\\!\\left(n^2\\varepsilon^{-9/2}\\log^{113/2+o(1)}n\\right) \\] time.   The construction has two main ingredients. First, we sparsify an approximately regular expander by partitioning its edges into few matchings and viewing their normalized Laplacians as an isotropic family of positive semidefinite matrices. Rather than sample from this family and apply matrix Chernoff, we select matchings deterministically using a pessimistic estimator. We evaluate the resulting conditional-expectation scores in two ways to produce two algorithms: using dense matrix multiplication and sparsely using polynomial approximations to the inverse square root and matrix exponential. Deterministic expander decomposition, along with replacing vertices by fixed expander graphs to achieve approximate regularity, extends these algorithms to general graphs. Second, a recursive blocking scheme applies the dense algorithm to smaller subgraphs and the sparse algorithm to their union, balancing their costs. Reusing the resulting algorithm as the dense algorithm gives $α_{r+1}=3-1/(α_r-1)$, starting from $α_0=ω$. After $O(\\log n)$ levels, the exponent is $2+O(1/\\log n)$, yielding $m^{1+o(1)}+\\widetilde O_{\\varepsilon}(n^2)$ time."
authors: "Jason Li, Trevor Vaughn"
date: 2026-08-14
journal: cs.DS
source_link: "https://arxiv.org/pdf/2608.13910v1"
pdf: "https://arxiv.org/pdf/2608.13910v1"
score: 7.85
tldr: 围绕《用于Dense Graphs的Determini...》；Let G be positively weighted n-v...；We evaluate resulting conditional-ex...
tldr_cn: 围绕《用于Dense Graphs的Determini...》；Let G be positively weighted n-v...；We evaluate resulting conditional-ex...
tldr_en: "A spectral sparsifier of a weighted graph is a reweighted subgraph whose Laplacian quadratic form approximates that of the original graph. Let $G$ be a positively weighted $n$-vertex, $m$-edge multigraph, let $0<\\varepsilon\\le1/2$."
selection_source: fresh_fetch
motivation: spectral sparsifier 的 weighted graph is reweighted subgraph whose Laplacian quadratic form approxima。
method: 核心做法：Let G be positively weighted n-vertex，m-edge multigraph，let 0<\varepsilon\le1/2。
result: 主要结果：We evaluate resulting conditional-expectation scores in two ways to produce two algorithms：使用 dense。
conclusion: "值得关注的点在于：After O (\\log n) levels，exponent is 2+O (1/\\log n)，yielding m^{1+o (1)}+\\widetilde O_{\\varepsilon}(n。"
---

## 摘要
spectral sparsifier 的 weighted graph is reweighted subgraph whose Laplacian quadratic form approxima。

核心做法：Let G be positively weighted n-vertex，m-edge multigraph，let 0<\varepsilon\le1/2。

主要结果：We evaluate resulting conditional-expectation scores in two ways to produce two algorithms：使用 dense。

值得关注的点在于：After O (\log n) levels，exponent is 2+O (1/\log n)，yielding m^{1+o (1)}+\widetilde O_{\varepsilon}(n。

## Abstract
A spectral sparsifier of a weighted graph is a reweighted subgraph whose Laplacian quadratic form approximates that of the original graph. Let $G$ be a positively weighted $n$-vertex, $m$-edge multigraph, let $0<\varepsilon\le1/2$. Assuming $m,\varepsilon^{-1}\le n^{O(1)}$ and the ratio of maximum to minimum weight is polynomially bounded, we deterministically construct a $(1\pm\varepsilon)$-spectral sparsifier with \[   O\!\left(n\varepsilon^{-2}\log^{24+o(1)}n\right) \] edges in \[   m^{1+o(1)}+O\!\left(n^2\varepsilon^{-9/2}\log^{113/2+o(1)}n\right) \] time.   The construction has two main ingredients. First, we sparsify an approximately regular expander by partitioning its edges into few matchings and viewing their normalized Laplacians as an isotropic family of positive semidefinite matrices. Rather than sample from this family and apply matrix Chernoff, we select matchings deterministically using a pessimistic estimator. We evaluate the resulting conditional-expectation scores in two ways to produce two algorithms: using dense matrix multiplication and sparsely using polynomial approximations to the inverse square root and matrix exponential. Deterministic expander decomposition, along with replacing vertices by fixed expander graphs to achieve approximate regularity, extends these algorithms to general graphs. Second, a recursive blocking scheme applies the dense algorithm to smaller subgraphs and the sparse algorithm to their union, balancing their costs. Reusing the resulting algorithm as the dense algorithm gives $α_{r+1}=3-1/(α_r-1)$, starting from $α_0=ω$. After $O(\log n)$ levels, the exponent is $2+O(1/\log n)$, yielding $m^{1+o(1)}+\widetilde O_{\varepsilon}(n^2)$ time.
