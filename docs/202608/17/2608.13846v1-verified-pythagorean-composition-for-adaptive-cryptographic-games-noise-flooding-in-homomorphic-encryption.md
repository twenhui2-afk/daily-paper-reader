---
title: "Verified Pythagorean Composition for Adaptive Cryptographic Games: Noise Flooding in Homomorphic Encryption"
source: arxiv
title_zh: 用于Adaptive Cryptographic Games：Noise Flooding in Homomorphic Encryption的Verified Pythagorean Composition
abstract_zh: "Noise flooding is standard defense against decryption attacks on approximate homomorphic encryption。\n\n核心做法：Replacing each 的 q adaptive decryption answers 结合 statistically close simulation 与 applying ordinary。\n\n主要结果：cryptographic proof instead accumulates conditional Kullback-Leibler (KL) costs 与 converts to statis。\n\n值得关注的点在于：Its Pythagorean judgment composes conditional KL budgets without converting them to statistical dist。"
abstract_en: "Noise flooding is a standard defense against decryption attacks on approximate homomorphic encryption, but its security proof is unusually sensitive to composition. Replacing each of $q$ adaptive decryption answers with a statistically close simulation and applying an ordinary hybrid argument loses linearly in $q$. The cryptographic proof instead accumulates conditional Kullback-Leibler (KL) costs and converts to statistical distance once, giving the parameter-critical square-root loss.   We machine-check this argument using Rocq and SSProve. Given any fully homomorphic encryption scheme that is approximately correct and IND-CPA secure, we formalize a reduction for every $q$-query IND-CPAD adversary and prove \\[   \\Pr[\\mathsf{IND\\text{-}CPAD}_{\\mathsf{NF}}^{\\mathcal A}=1]   \\leq β_{\\mathsf{CPA}}(\\mathcal B_{\\mathcal A,q})   + \\frac{\\sqrt{qn}}{2γ}. \\] where $n$ is the plaintext dimension and $γ$ is the flooding-width multiplier. Our proof constructs a new relational program logic over SSProve semantics. Its Pythagorean judgment composes conditional KL budgets without converting them to statistical distance, and a verified trace compiler lifts a local oracle rule to arbitrary adaptive programs with a single final conversion."
authors: "Yi Lee, Alexandru Cojocaru, Junyi Liu, Xiaodi Wu"
date: 2026-08-14
journal: cs.CR
source_link: "https://arxiv.org/pdf/2608.13846v1"
pdf: "https://arxiv.org/pdf/2608.13846v1"
score: 8.299999999999999
tldr: 围绕《用于Adaptive Cryptographic...》；Replacing each 的 q adaptive decr...；cryptographic proof instead accumula...
tldr_cn: 围绕《用于Adaptive Cryptographic...》；Replacing each 的 q adaptive decr...；cryptographic proof instead accumula...
tldr_en: "Noise flooding is a standard defense against decryption attacks on approximate homomorphic encryption, but its security proof is unusually sensitive to composition. Replacing each of $q$ adaptive decryption answers with a statistically close simulation and applying an ordinary hybrid argument loses linearly in $q$."
selection_source: fresh_fetch
motivation: Noise flooding is standard defense against decryption attacks on approximate homomorphic encryption。
method: 核心做法：Replacing each 的 q adaptive decryption answers 结合 statistically close simulation 与 applying ordinary。
result: 主要结果：cryptographic proof instead accumulates conditional Kullback-Leibler (KL) costs 与 converts to statis。
conclusion: 值得关注的点在于：Its Pythagorean judgment composes conditional KL budgets without converting them to statistical dist。
---

## 摘要
Noise flooding is standard defense against decryption attacks on approximate homomorphic encryption。

核心做法：Replacing each 的 q adaptive decryption answers 结合 statistically close simulation 与 applying ordinary。

主要结果：cryptographic proof instead accumulates conditional Kullback-Leibler (KL) costs 与 converts to statis。

值得关注的点在于：Its Pythagorean judgment composes conditional KL budgets without converting them to statistical dist。

## Abstract
Noise flooding is a standard defense against decryption attacks on approximate homomorphic encryption, but its security proof is unusually sensitive to composition. Replacing each of $q$ adaptive decryption answers with a statistically close simulation and applying an ordinary hybrid argument loses linearly in $q$. The cryptographic proof instead accumulates conditional Kullback-Leibler (KL) costs and converts to statistical distance once, giving the parameter-critical square-root loss.   We machine-check this argument using Rocq and SSProve. Given any fully homomorphic encryption scheme that is approximately correct and IND-CPA secure, we formalize a reduction for every $q$-query IND-CPAD adversary and prove \[   \Pr[\mathsf{IND\text{-}CPAD}_{\mathsf{NF}}^{\mathcal A}=1]   \leq β_{\mathsf{CPA}}(\mathcal B_{\mathcal A,q})   + \frac{\sqrt{qn}}{2γ}. \] where $n$ is the plaintext dimension and $γ$ is the flooding-width multiplier. Our proof constructs a new relational program logic over SSProve semantics. Its Pythagorean judgment composes conditional KL budgets without converting them to statistical distance, and a verified trace compiler lifts a local oracle rule to arbitrary adaptive programs with a single final conversion.
