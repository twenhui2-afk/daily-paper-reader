---
title: Space-Efficient Hierholzer for Undirected Graphs
source: arxiv
title_zh: 用于Undirected Graphs的Space-Efficient Hierholzer
abstract_zh: "作者提出 simple linear-time algorithm that outputs Eulerian tour 的 undirected multigraph 结合 n vertices 与。\n\n核心做法：input is given as read-only adjacency lists，与 output is written to append-only stream in traversal o。\n\n主要结果：Our algorithm first finds sparse spanning circuit ( skeleton)，then traverses circuit step-by-step，re。\n\n值得关注的点在于：Our result completes picture in read-only 模型用于 space-efficient output 的 Eulerian tours。"
abstract_en: "We present a simple linear-time algorithm that outputs an Eulerian tour of an undirected multigraph with $n$ vertices and $m$ edges, if one exists, in $O(m)$ time and using $O(n)$ words of working memory. The input is given as read-only adjacency lists, and the output is written to an append-only stream in traversal order. Our algorithm first finds a sparse spanning circuit (a skeleton), then traverses the circuit step-by-step, repeatedly outputting further circuits rooted at the current vertex. This solves a problem left open by Ismaili Alaoui, Plump, and Wild (SOSA 2026): their space-efficient variant of Hierholzer's algorithm handles general directed multigraphs, but it is unclear how to generalize it to general undirected multigraphs. Our result completes the picture in the read-only model for space-efficient output of Eulerian tours."
authors: "Elena Grigorescu, Ziad Ismaili Alaoui, Tamio-Vesa Nakajima, Shayan Shirazi Mofrad, Sebastian Wild"
date: 2026-08-19
journal: cs.DS
source_link: "https://arxiv.org/pdf/2608.19081v1"
pdf: "https://arxiv.org/pdf/2608.19081v1"
score: 6.699999999999999
tldr: 围绕《用于Undirected Graphs的Spac...》；input is given as read-only adja...；Our algorithm first finds sparse spa...
tldr_cn: 围绕《用于Undirected Graphs的Spac...》；input is given as read-only adja...；Our algorithm first finds sparse spa...
tldr_en: "We present a simple linear-time algorithm that outputs an Eulerian tour of an undirected multigraph with $n$ vertices and $m$ edges, if one exists, in $O(m)$ time and using $O(n)$ words of working memory. The input is given as read-only adjacency lists, and the output is written to an append-only stream in traversal order."
selection_source: fresh_fetch
motivation: 作者提出 simple linear-time algorithm that outputs Eulerian tour 的 undirected multigraph 结合 n vertices 与。
method: 核心做法：input is given as read-only adjacency lists，与 output is written to append-only stream in traversal o。
result: 主要结果：Our algorithm first finds sparse spanning circuit ( skeleton)，then traverses circuit step-by-step，re。
conclusion: 值得关注的点在于：Our result completes picture in read-only 模型用于 space-efficient output 的 Eulerian tours。
---

## 摘要
作者提出 simple linear-time algorithm that outputs Eulerian tour 的 undirected multigraph 结合 n vertices 与。

核心做法：input is given as read-only adjacency lists，与 output is written to append-only stream in traversal o。

主要结果：Our algorithm first finds sparse spanning circuit ( skeleton)，then traverses circuit step-by-step，re。

值得关注的点在于：Our result completes picture in read-only 模型用于 space-efficient output 的 Eulerian tours。

## Abstract
We present a simple linear-time algorithm that outputs an Eulerian tour of an undirected multigraph with $n$ vertices and $m$ edges, if one exists, in $O(m)$ time and using $O(n)$ words of working memory. The input is given as read-only adjacency lists, and the output is written to an append-only stream in traversal order. Our algorithm first finds a sparse spanning circuit (a skeleton), then traverses the circuit step-by-step, repeatedly outputting further circuits rooted at the current vertex. This solves a problem left open by Ismaili Alaoui, Plump, and Wild (SOSA 2026): their space-efficient variant of Hierholzer's algorithm handles general directed multigraphs, but it is unclear how to generalize it to general undirected multigraphs. Our result completes the picture in the read-only model for space-efficient output of Eulerian tours.
