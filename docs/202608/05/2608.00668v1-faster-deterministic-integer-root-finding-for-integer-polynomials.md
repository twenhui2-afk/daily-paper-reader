---
title: Faster Deterministic Integer Root Finding for Integer Polynomials
source: arxiv
title_zh: 用于Integer Polynomials的Faster Deterministic Integer Root Finding
abstract_zh: "We give deterministic algorithm 用于 finding all integer roots 的 square-free polynomial f\\in\\mathbb Z[。\n\n核心做法：algorithm follows classical p-adic 框架：find roots modulo prime p，lift them modulo high power 的 p，与 ve。\n\n主要结果：Together，these ingredients give first deterministic subquadratic-in-n improvement 用于 integer root fi。\n\n值得关注的点在于：We also give faster deterministic candidate-verification algorithm：given n candidate integers smalle。"
abstract_en: "We give a deterministic algorithm for finding all integer roots of a square-free polynomial $f\\in\\mathbb Z[x]$ of degree $n$ with $\\lVert f\\rVert_\\infty<2^b$. The running time is $$ \\tilde{O}(n^{3/2}b), $$ improving the $\\tilde{O}(n^2b)$ bound of Harvey and Hittmeir (Research in Number Theory, 2022).   The algorithm follows the classical $p$-adic framework: find roots modulo a prime $p$, lift them modulo a high power of $p$, and verify the lifted candidates. The main new idea is to avoid searching for a prime for which $f\\bmod p$ is square-free. Instead, we find a prime for which the total multiplicity of repeated roots modulo $p$ is small. This requires lifting repeated roots, which we handle using a weighted lifting tree. We also give a faster deterministic candidate-verification algorithm: given $n$ candidate integers smaller in absolute value than $2^b$, we decide which are roots of $f$ in $$ \\tilde{O}(nb+\\min(n^2,nb^2)) $$ bit operations. Together, these ingredients give the first deterministic subquadratic-in-$n$ improvement for integer root finding in the square-free case."
authors: Itamar Nir
date: 2026-08-01
journal: math.NT
source_link: "https://arxiv.org/pdf/2608.00668v1"
pdf: "https://arxiv.org/pdf/2608.00668v1"
score: 8.6
tldr: 围绕《用于Integer Polynomials的Fa...》；algorithm follows classical p-ad...；Together，these ingredients give firs...
tldr_cn: 围绕《用于Integer Polynomials的Fa...》；algorithm follows classical p-ad...；Together，these ingredients give firs...
tldr_en: "We give a deterministic algorithm for finding all integer roots of a square-free polynomial $f\\in\\mathbb Z[x]$ of degree $n$ with $\\lVert f\\rVert_\\infty<2^b$. The running time is $$ \\tilde{O}(n^{3/2}b), $$ improving the $\\tilde{O}(n^2b)$ bound of Harvey and Hittmeir (Research in Number Theory, 2022)."
selection_source: fresh_fetch
motivation: "We give deterministic algorithm 用于 finding all integer roots 的 square-free polynomial f\\in\\mathbb Z[。"
method: 核心做法：algorithm follows classical p-adic 框架：find roots modulo prime p，lift them modulo high power 的 p，与 ve。
result: 主要结果：Together，these ingredients give first deterministic subquadratic-in-n improvement 用于 integer root fi。
conclusion: 值得关注的点在于：We also give faster deterministic candidate-verification algorithm：given n candidate integers smalle。
---

## 摘要
We give deterministic algorithm 用于 finding all integer roots 的 square-free polynomial f\in\mathbb Z[。

核心做法：algorithm follows classical p-adic 框架：find roots modulo prime p，lift them modulo high power 的 p，与 ve。

主要结果：Together，these ingredients give first deterministic subquadratic-in-n improvement 用于 integer root fi。

值得关注的点在于：We also give faster deterministic candidate-verification algorithm：given n candidate integers smalle。

## Abstract
We give a deterministic algorithm for finding all integer roots of a square-free polynomial $f\in\mathbb Z[x]$ of degree $n$ with $\lVert f\rVert_\infty<2^b$. The running time is $$ \tilde{O}(n^{3/2}b), $$ improving the $\tilde{O}(n^2b)$ bound of Harvey and Hittmeir (Research in Number Theory, 2022).   The algorithm follows the classical $p$-adic framework: find roots modulo a prime $p$, lift them modulo a high power of $p$, and verify the lifted candidates. The main new idea is to avoid searching for a prime for which $f\bmod p$ is square-free. Instead, we find a prime for which the total multiplicity of repeated roots modulo $p$ is small. This requires lifting repeated roots, which we handle using a weighted lifting tree. We also give a faster deterministic candidate-verification algorithm: given $n$ candidate integers smaller in absolute value than $2^b$, we decide which are roots of $f$ in $$ \tilde{O}(nb+\min(n^2,nb^2)) $$ bit operations. Together, these ingredients give the first deterministic subquadratic-in-$n$ improvement for integer root finding in the square-free case.
