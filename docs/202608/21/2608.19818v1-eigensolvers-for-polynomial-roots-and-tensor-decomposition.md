---
title: Eigensolvers for polynomial roots and tensor decomposition
source: arxiv
title_zh: 用于polynomial roots与tensor decomposition的Eigensolvers
abstract_zh: "Computing eigenvalues 与 eigen向量 is at heart 的 solution 的 many non-linear problems。\n\n核心做法：用于 instance，finding roots 的 polynomial systems reduces to computing joint eigen向量的 operators 的 multi。\n\n主要结果：Similarly，tensor decomposition can be performed 通过 joint diagonalization 的 submatrices 的 Catalectica。\n\n值得关注的点在于：We describe 与 illustrate symbolic-numeric 方法s 用于 computing solutions 的 these algebraic problems from。"
abstract_en: "Computing eigenvalues and eigenvectors is at the heart of the solution of many non-linear problems. For instance, finding the roots of polynomial systems reduces to computing joint eigenvectors of operators of multiplication. Similarly, tensor decomposition can be performed via the joint diagonalization of submatrices of the Catalecticant of the tensor. We describe and illustrate symbolic-numeric methods for computing the solutions of these algebraic problems from the computation of joint eigenvectors of commuting operators, and for analysing their multiplicity structure, as well as their implementation in the package AlgebraicSolvers.jl."
authors: "Enrica Barrilli, Bernard Mourrain"
date: 2026-08-20
journal: cs.MS
source_link: "https://arxiv.org/pdf/2608.19818v1"
pdf: "https://arxiv.org/pdf/2608.19818v1"
score: 8.25
tldr: 围绕《用于polynomial roots与tenso...》；用于 instance，finding roots 的 poly...；Similarly，tensor decomposition can b...
tldr_cn: 围绕《用于polynomial roots与tenso...》；用于 instance，finding roots 的 poly...；Similarly，tensor decomposition can b...
tldr_en: "Computing eigenvalues and eigenvectors is at the heart of the solution of many non-linear problems. For instance, finding the roots of polynomial systems reduces to computing joint eigenvectors of operators of multiplication."
selection_source: fresh_fetch
motivation: Computing eigenvalues 与 eigen向量 is at heart 的 solution 的 many non-linear problems。
method: 核心做法：用于 instance，finding roots 的 polynomial systems reduces to computing joint eigen向量的 operators 的 multi。
result: 主要结果：Similarly，tensor decomposition can be performed 通过 joint diagonalization 的 submatrices 的 Catalectica。
conclusion: 值得关注的点在于：We describe 与 illustrate symbolic-numeric 方法s 用于 computing solutions 的 these algebraic problems from。
---

## 摘要
Computing eigenvalues 与 eigen向量 is at heart 的 solution 的 many non-linear problems。

核心做法：用于 instance，finding roots 的 polynomial systems reduces to computing joint eigen向量的 operators 的 multi。

主要结果：Similarly，tensor decomposition can be performed 通过 joint diagonalization 的 submatrices 的 Catalectica。

值得关注的点在于：We describe 与 illustrate symbolic-numeric 方法s 用于 computing solutions 的 these algebraic problems from。

## Abstract
Computing eigenvalues and eigenvectors is at the heart of the solution of many non-linear problems. For instance, finding the roots of polynomial systems reduces to computing joint eigenvectors of operators of multiplication. Similarly, tensor decomposition can be performed via the joint diagonalization of submatrices of the Catalecticant of the tensor. We describe and illustrate symbolic-numeric methods for computing the solutions of these algebraic problems from the computation of joint eigenvectors of commuting operators, and for analysing their multiplicity structure, as well as their implementation in the package AlgebraicSolvers.jl.
