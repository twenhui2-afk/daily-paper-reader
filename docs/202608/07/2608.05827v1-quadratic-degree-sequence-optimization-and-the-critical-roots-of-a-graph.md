---
title: Quadratic Degree Sequence Optimization and the Critical Roots of a Graph
source: arxiv
title_zh: Graph的Quadratic Degree Sequence Optimization与Critical Roots
abstract_zh: "Here 这篇论文研究 this problem 与 its complexity 用于 quadratic functions。\n\n核心做法：In particular，作者引入 critical roots 的 graph，与 show they define intervals over which optimal value 的 pr。\n\n主要结果：degree sequence optimization problem is to find subgraph 的 given graph which maximizes sum over all。\n\n值得关注的点在于：这篇文章的价值在于把《Graph的Quadratic Degree Sequence Optimization与Critical Roots》这。"
abstract_en: "The degree sequence optimization problem is to find a subgraph of a given graph which maximizes the sum over all vertices of a given function evaluated at the subgraph degree of that vertex. Here we study this problem and its complexity for quadratic functions. In particular, we introduce the critical roots of a graph, and show they define intervals over which the optimal value of the problem, as the quadratic root varies, is convex piecewise affine."
authors: "Frédéric Meunier, Shmuel Onn"
date: 2026-08-06
journal: math.OC
source_link: "https://arxiv.org/pdf/2608.05827v1"
pdf: "https://arxiv.org/pdf/2608.05827v1"
score: 8.1
tldr: 围绕《Graph的Quadratic Degree S...》；In particular，作者引入 critical root...；degree sequence optimization problem...
tldr_cn: 围绕《Graph的Quadratic Degree S...》；In particular，作者引入 critical root...；degree sequence optimization problem...
tldr_en: The degree sequence optimization problem is to find a subgraph of a given graph which maximizes the sum over all vertices of a given function evaluated at the subgraph degree of that vertex. Here we study this problem and its complexity for quadratic functions.
selection_source: fresh_fetch
vocabulary: ["optimization | 优化 | CET6"]
motivation: Here 这篇论文研究 this problem 与 its complexity 用于 quadratic functions。
method: 核心做法：In particular，作者引入 critical roots 的 graph，与 show they define intervals over which optimal value 的 pr。
result: 主要结果：degree sequence optimization problem is to find subgraph 的 given graph which maximizes sum over all。
conclusion: 值得关注的点在于：这篇文章的价值在于把《Graph的Quadratic Degree Sequence Optimization与Critical Roots》这。
---

## 摘要
Here 这篇论文研究 this problem 与 its complexity 用于 quadratic functions。

核心做法：In particular，作者引入 critical roots 的 graph，与 show they define intervals over which optimal value 的 pr。

主要结果：degree sequence optimization problem is to find subgraph 的 given graph which maximizes sum over all。

值得关注的点在于：这篇文章的价值在于把《Graph的Quadratic Degree Sequence Optimization与Critical Roots》这。

## Abstract
The degree sequence optimization problem is to find a subgraph of a given graph which maximizes the sum over all vertices of a given function evaluated at the subgraph degree of that vertex. Here we study this problem and its complexity for quadratic functions. In particular, we introduce the critical roots of a graph, and show they define intervals over which the optimal value of the problem, as the quadratic root varies, is convex piecewise affine.

## 英语词汇
- **optimization**：优化（CET6）
