Title: Achieving Rate-Concurrency Balance for Underwater Concurrent Random Access

URL Source: https://arxiv.org/pdf/2608.01254v1

Published Time: Tue, 04 Aug 2026 01:23:03 GMT

Number of Pages: 16

Markdown Content:
# Achieving Rate-Concurrency Balance for Underwater Concurrent Random Access 

Enqi Zhang, Yuxuan Guo, Weining Li, Linpeng Chen, Yuetong Chen, Deqing Wang, Lizhao You and Liqun Fu 

School of Informatics, Xiamen University, Xiamen, China Corresponding emails: {deqing, lizhaoyou }@xmu.edu.cn 

Abstract —Underwater acoustic networks face a fundamental rate–concurrency tradeoff: high-rate waveforms (e.g., OFDM, OTFS) are designed for point-to-point links and rely on orthog-onal MAC protocols (e.g., TDMA) to avoid collisions, sacrific-ing concurrency; conversely, collision-resilient waveforms (e.g., CDMA, ZCMod) support uncoordinated access but are inher-ently rate-limited by spreading or sparse index modulation. We present A QUA ZC, a cross-layer concurrent random-access system that combines two new components: (i) EZCDM, an equidistant ZC division-multiplexing waveform that activates multiple cyclic shifts of a ZC root as parallel sub-channels with a tunable rate– robustness tradeoff, and an intra-symbol differential receiver that eliminates the shared multipath channel response without explicit CIR estimation; and (ii) a cross-layer link adaptation (LA) framework featuring beacon-framed random access, user-specific closed-loop power control, and overlap- and CIR-aware common-MS selection. Channel-trace- and signal-trace-driven physical-layer experiments combined with PHY-in-the-loop net-work simulations demonstrate that A QUA ZC achieves significant BER and throughput gains over conventional waveforms and MAC protocols by converting traditionally destructive collisions into decodable concurrent streams. 

Index Terms —Underwater acoustic communication, underwa-ter acoustic network, random access, concurrent access, Zadoff-Chu sequences. 

I. I NTRODUCTION 

Emerging underwater operations increasingly involve mul-tiple mobile users, including teams of divers and autonomous underwater vehicles (AUVs) [1]–[8]. Such operations naturally favor an uplink-dominated star topology, in which resource-constrained mobile users report data to a resource-rich central gateway. In practice, the gateway faces both time-varying traffic demands and rapidly changing acoustic channels caused by user mobility and environmental fluctuations. Supporting these deployments therefore requires efficient multi-user ac-cess that can adapt to both traffic and channel dynamics while sustaining useful data rates. Existing underwater acoustic (UWA) MAC protocols rely on either coordinated resource allocation or contention-based access, but neither class efficiently accommodates dynamic workloads. Fixed TDMA or FDMA allocations [9] reserve resources in advance, leaving idle slots for inactive users while preventing active users from exploiting them as traffic de-mands shift. Contention-based schemes such as S-FAMA [10], CSMA/CA [11], and reservation-free ALOHA variants [12]– [14] avoid preallocation, but suffer from long propagation delays or throughput saturation under overlapping transmis-sions with conventional single-packet reception. Together, these limitations call for on-demand uplink access without per-packet coordination, supported by a PHY capable of separating asynchronous overlapping transmissions at high data rates. At the PHY layer, existing UWA waveforms face a funda-mental rate–concurrency dilemma. High-rate waveforms such as OFDM [15], [16], OCDM [17], and OTFS [18] are designed for point-to-point links, where synchronization and accurate channel estimation are needed but possible. However, for synchronous multi-user access, accurate time synchronization and protocol coordination is needed. Maintaining inter-user orthogonality under asynchronous access requires costly multi-user detection or successive interference cancellation, which is vulnerable to errors in rapidly varying UWA multipath channels. Conversely, conventional underwater CDMA accom-modates asynchronous users through spreading sequences, but is at the cost of low spectral efficiency and substantial channel-estimation overhead [19]. Recent Zadoff–Chu (ZC)-based waveforms offer a promis-ing direction for narrowing this gap by using the correlation structure of ZC sequences for payload modulation in terrestrial wireless and UWA systems [20]–[22]. Their key idea is to encode payloads through cyclic-shift indices within a ZC root: a transmitter activates one or a small subset of shifts from a predefined set, and the receiver identifies the corresponding correlation-peak locations. In particular, ZCMod [22] activates one cyclic shift per symbol and decodes its index by matching the received multipath pattern against a channel-dependent template, but its payload unit remains the index of the single activated shift; the other cyclic shifts do not simultaneously carry independently modulated payload symbols. Because information is carried by sparse activation patterns rather than by independently modulating all available shifts, this index-modulation paradigm does not exploit the full sequence space for parallel transmission. Thus, although these designs improve support for uncoordinated access, their sparse index signaling leaves the rate–concurrency gap only partially closed. We take a different approach: within each symbol, it maps independently modulated values onto multiple cyclic shifts of the same ZC root and superimposes the resulting sequences in the time domain . Under an ideal channel, the zero cyclic autocorrelation between distinct shifts makes these superim-posed components separable after cyclic correlation, thereby arXiv:2608.01254v1 [cs.NI] 2 Aug 2026 turning the cyclic-shift domain into a bank of parallel sequence sub-channels rather than a sparse index alphabet. Deploying the new waveform in underwater environments, however, remains challenging due to severe multipath and rapid channel variation. Moreover, the optimal design parameters depend on both the instantaneous channel condition and the prevailing interference level, so a static configuration cannot perform well across the diverse operating regimes encountered in practice. To address these challenges, we design A QUA ZC, a cross-layer UWA system comprising two new components: 

a) Equidistant ZC Division Multiplexing (EZCDM) wave-form (§III): We design EZCDM to activate cyclic shifts at an equidistant spacing τ , leaving a guard region between adjacent sub-channels in the cyclic-correlation spectrum. Meanwhile, increasing τ reduces the number of active sub-channels but better separates their multipath responses, exposing a direct rate–robustness tradeoff. To tolerate rapid channel variation without explicit CIR estimation, EZCDM differentially en-codes data across sub-channels within each symbol (i.e., intra-symbol differential modulation). Since these sub-channels share the same channel response, their relative phases remain decodable even when the response changes across symbols. At the receiver, the periodic peak structure induced by τ en-ables lightweight estimation of relative path weights from the cyclic-correlation spectrum, after which weighted differential decisions combine energy across paths. Then, a hard or soft demodulator converts multipath-weighted symbol probabilities into bits or bit LLRs for channel decoding. 

b) Concurrent MAC and Link Adaptation for EZCDM (§IV): For concurrent uplinks, A QUA ZC assigns each user a distinct ZC root, allowing the gateway to process overlapping transmissions with user-specific correlators without global tim-ing coordination. A beacon-framed, continuously randomized access procedure admits on-demand transmissions without per-packet grants, while user-specific closed-loop power con-trol mitigates the near–far effect. The preamble–midamble– postamble frame lets the gateway measure per-user received power, packet overlap, and intra-packet CIR similarity. It combines these observations into an effective SINR and selects a common modulation scheme that jointly configures the differential alphabet and τ for the next superframe. 

Implementation and Evaluation. We implement a custom acoustic SDR prototype and evaluate A QUA ZC using channel-trace and signal-trace data in lake and diving-pool environ-ments, together with PHY-in-the-loop network simulations of a 12-node star topology. At the physical layer, under low-to-moderate channel variability, EZCDM achieves orders-of-magnitude lower uncoded BER than DSSS, CSS, and OFDM-DQPSK, with at least a 5 dB Eb/N 0 gain over CSS at a BER of 0.02 ; under high variability the gain increases to 

10 dB, confirming EZCDM’s robustness under severe channel dynamics. In diving-pool signal-trace experiments, MS 1 and MS 2 achieve near error-free transmission in quasi-static channels and an order-of-magnitude BER reduction under highly dynamic conditions. At the network level, A QUA ZC delivers substantial through-put gains: compared to TDMA, S-ALOHA, and ZCMod (state-of-the-art concurrent system), it achieves 7.13 ×, 2.40 ×, and 2.02 × improvements at 0.16 packets/slot, with peak gains of 10.6 ×, 2.71 ×, and 2.92 ×, respectively. The advantage decomposes into contributions from the concurrent decoding with designed on-demand access (vs. TDMA), the concurrent decoding (vs. S-ALOHA), and EZCDM’s higher-rate robust waveform (vs. ZCMod), together converting traditionally de-structive collisions into decodable concurrent streams. Our main technical contributions are as follows:  

> •

We introduce EZCDM, a dense sequence-domain wave-form that multiplexes independently modulated values over equidistant cyclic shifts of a ZC root, converting the shift domain from a sparse index alphabet into paral-lel payload sub-channels with a tunable rate–multipath-robustness tradeoff.  

> •

We design an intra-symbol differential receiver that avoids explicit CIR estimation and equalization under rapidly varying channels, and a lightweight multipath-aware demodulator that estimates relative path weights directly from the cyclic-correlation spectrum to improve hard and soft decisions.  

> •

We build A QUA ZC, a cross-layer concurrent-access sys-tem that combines root-separated asynchronous uplinks, user-specific receive-power control, and overlap- and CIR-aware closed-loop link adaptation, and validate it through pool experiments, real-channel replay, and link-and network-level simulations. II. B ACKGROUND AND MOTIVATION 

A. System Model 

We consider a star network consisting of one half-duplex gateway and K half-duplex registered users. Users initiate packet transmissions without per-packet scheduling or global uplink timing coordination. Let U(t) ⊆ { 1, . . . , K } denote the set of users whose packets are present at the gateway at time 

t. Their transmissions may partially or fully overlap, and the gateway seeks to identify and decode each active user. 

Underwater Acoustic Channel Model. Let fc be the carrier frequency and P be the number of multipath components. The UWA channel is a time-varying, frequency-selective medium modeled by the time-varying baseband channel impulse re-sponse (CIR) [23], [24] 

h(t, τ ) = 

> P

X

> p=1

Gp(t)e−j2πf cτp(t)δ(τ − τp(t)) , (1) where h(t, τ ) is the response at observation time t to an impulse transmitted at time t − τ , and Gp(t) ∈ C and τp(t)

denote the time-varying complex path gain and instantaneous propagation delay of the p-th path, respectively. Unlike narrowband RF systems where the bandwidth is negligible relative to the carrier, in UWA links the signal bandwidth can be a sizable fraction of fc, so motion-induced distortion is better captured by time scaling than by a fre-quency shift alone. Over a short observation window, the 5 10 15 20 25               

> Delay time (ms)
> 246810
> Time (s) (a) 510 15 20 25
> Delay time (ms)
> 246810
> Time (s) (b) 510 15 20 25
> Delay time (ms)
> 246810
> Time (s)
> (c) 510 15 20 25
> Delay time (ms)
> 246810
> Time (s) (d) Fig. 1. Underwater channel impulse responses measured in a real-world diving pool under dynamic motions: (a) hovering; (b) and (c) horizontal motions in different directions; and (d) vertical ascent and descent.

instantaneous delay of the p-th path is approximated as τp(t) ≈

τp −apt, where τp is the delay at the beginning of the window and ap = vp/c is the path-dependent Doppler scale ( vp is the closing velocity along the p-th path, defined as positive when the path length decreases). The scale ap represents the total uncompensated propagation-induced Doppler scaling. Substituting this into the CIR yields: 

h(t, τ ) = 

> P

X

> p=1

Gp(t)e−j2πf c(τp−apt)δ(τ − (τp − apt)) . (2) Eq. (2) captures two distinct effects: the term ej2πa pfct corre-sponds to conventional Doppler-induced phase rotation, while the argument of the Dirac delta, δ(τ −(τp −apt)) , dictates that physical delay taps continuously drift across the observation window, causing intra-symbol time-scaling. Fig. 1 presents empirically measured underwater channels in h(t, τ ) format ( x-axis: delay τ , y-axis: time t), collected in a diving pool under dynamic motions (experimental setup in §V-A). Hovering motion (Fig. 1(a)) exhibits pronounced multipath with minimal Doppler spread. Horizontal motions at 

≈ 0.5m/s (Fig. 1(b,c)) exhibit strong Doppler effects. Vertical motion (Fig. 1(d)) shows a mixture of static and dynamic paths. These measurements highlight that multipath isolation and temporal robustness must be addressed simultaneously. 

Transmitter and Receiver Model. Let x(t) be the complex-baseband packet transmit waveform. Based on the wideband CIR in Eq. (2), the received baseband signal in a single-user scenario is                 

> y(t) = ej2πν cfo t
> Z
> h(t, τ )x(t−τ)dτ +w(t)=ej2πν cfo tPX
> p=1
> Gp(t)e−j2πf c(τp−apt)·x (1 + ap)t−τp
> +w(t).

(3) Here, νcfo is the carrier-frequency offset (CFO) and w(t) de-notes residual complex Gaussian noise after bandpass filtering. In an uncoordinated multi-user random access setting, each active user u ∈ U (t) transmits xu(t), arriving with an arbitrary access delay ∆u ∈ R. Designating user 1 as the target user (∆1 = 0 ), the superimposed received signal is 

y(t) = X

> u∈U (t)

ej2πν cfo  

> utPu

X

> p=1

Gu,p (t)e−j2πf c(τu,p −au,p t)

· xu((1 + au,p )t − ∆u − τu,p ) + w(t),

(4) 

where Pu, Gu,p (t), τu,p , and au,p characterize the user-specific multipath channel, and each user has an independent residual CFO νcfo  

> u

common across its multipath components. In summary, the UWA channel combines severe multipath delay spread with highly dynamic time variation, while the multi-user setting adds three coupled impairments: (1) each active user generates multiple time-scaled replicas that in-terleave with those of other users; (2) the path-dependent Doppler scales preclude a single per-user frequency correction from stabilizing all paths; and (3) the arbitrary access delays, combined with user-specific multipath, make the instantaneous interference pattern unpredictable across packets. Together, these challenges demand a practical waveform that preserves separability under multipath, avoids continuous coherent chan-nel reconstruction, and tolerates asynchronous inter-user inter-ference without sacrificing data rates. 

B. Limitation of Existing Approaches 

Existing ZC payload waveforms remain single-shift and index-centric. ZC sequences are widely used for random-access timing in cellular systems [25], [26] and for pream-ble detection and Doppler estimation in UWA communica-tions [27], [28]. Recent designs further use ZC sequences for payload modulation [20]–[22]. In particular, ZCMod [22] activates one cyclic shift per symbol and decodes its index by matching the received multipath correlation pattern against a channel-dependent template. Its payload unit, however, re-mains the index of the single activated shift; the other cyclic shifts do not simultaneously carry independently modulated payload symbols. 

Single-shift pattern matching does not recover multi-shift payloads. After ZC correlation, one activated shift produces a translated replica of the physical CIR, allowing single-shift methods to infer the shift index by locating a channel-shaped template. When multiple cyclic shifts are transmitted concurrently, the correlation output becomes a superposition of translated replicas of the same physical channel, each weighted by a payload-dependent complex coefficient. The receiver must separate these shift-domain components and decode the information carried in their complex relationships under an unknown shared channel response—a challenge that single-shift pattern matching does not address. The remaining task is therefore to structure shift-domain overlap and exploit the shared channel response so that multiple cyclic shifts can carry parallel payloads without continuous CIR reconstruction. Post-hoc Collision Resolution. Collision-resolution algo-rithms for chirp spread spectrum systems (e.g., LoRa) typically track sharp spectral, correlation, or time–frequency features to separate overlapping packets [29]–[42]. Long-delay-spread multipath broadens and overlaps these features, while path-dependent Doppler and arbitrary access delays make their locations and shapes vary across paths and users. Successive interference cancellation (SIC) [43] requires accurate, up-to-date channel estimates; otherwise residual errors propagate through cancellation stages. These assumptions are difficult to maintain in dynamic UWA channels, illustrating the need for a waveform that preserves separability by construction rather than relying on post-hoc collision resolution. Together, these limitations call for a proactive waveform that preserves separability under asynchronous collisions by construction and eliminates the shared unknown channel re-sponse directly in the shift domain, as developed in Sec. III. III. P HYSICAL -LAYER DESIGN 

A. Generic ZC-Shift Division Multiplexing 

We first introduce a generic division multiplexing primitive based on cyclic shifts of a Zadoff–Chu (ZC) sequence. Let 

sr ∈ CN be a length-N ZC sequence with root r, where r is coprime to N . For the odd lengths used in our design, 

sr [n] = exp( −jπrn (n + 1) /N ) , n = 0 , . . . , N − 1. (5) The sequence has constant amplitude. Let Π denote a one-sample cyclic-shift operator, and collect all shifts of the same root as the columns of 

Cr = [ sr , Πs r , . . . , ΠN −1sr ]. (6) The zero cyclic autocorrelation of a ZC sequence gives 

CHr Cr = N IN . Thus, the columns of Cr form orthogonal ZC-shift dimensions; they are logical multiplexing dimensions rather than independent physical propagation subchannels. Distinct roots have bounded cross-correlation and are assigned to different users for random access, as described in Sec. IV. Appendix A validates that multi-user cross-root interference is effectively whitened under asynchronous access. For a coefficient vector x ∈ CN , the transmitted time-domain block before cyclic-prefix insertion is 

xtx ≜ u = Cr x = 

> N−1

X

> q=0

x[q]Πq sr . (7) Its support A ≜ supp( x) ⊆ { 0, . . . , N − 1} determines the signaling mode. A single-active vector x = aeq , where eq

is the q-th standard basis vector, corresponds to traditional index modulation when q is selected by payload bits. A fully loaded vector satisfies A = {0, . . . , N −1}, while intermediate sparsity levels realize the equidistant design developed below. The circulant structure of Cr provides an efficient FFT-domain realization u = F−1 

> N

[ FN (sr ) ⊙ F N (x) ] , where FN

denotes the N -point DFT. Since FN (sr ) is fixed and can be precomputed, the cost is reduced to O(N log N ).Block 1 Block 2 Block n              

> …
> Source bits
> xs(τ) (0) xs(τ) (0) …xs(τ) (𝑵
> 𝝉 −𝟏 )ZP ZP ZP
> τ-1τ-1τ-1
> insertion operation with τ-1
> Separate into blocks
> ℱN(x)
> FFT
> Zadoff -Chu
> sequence
> FFT diag (λ)

* IFFT            

> xtx
> xs(τ) (0)
> ×
> xs(τ) (1)
> ×
> 0
> ×
> 0
> ×
> Modulated on sub -channel 0
> Modulated on sub -channel 1
> Modulated on sub -channel n
> 0
> 0
> … … ……  …
> Σ
> Add CP
> Modulated
> sequence
> Equivalent time
> domain operation xs(τ) (𝑁
> 𝜏 −1)
> ×
> Fig. 2. EZCDM modulator structure: payload bits are mapped to coefficients
> xwith equidistant sparse support, then transformed to the time-domain block
> uvia the ZC shift matrix Cr.

Fig. 2 illustrates the EZCDM modulator structure. Payload bits are mapped to a coefficient vector x with structured sparse support (the equidistant design is developed in §III-B), where each active coefficient is differential-PSK modulated. The coefficient vector is then multiplied by the precomputed ZC shift matrix Cr , producing the time-domain block u.Equivalently, this step can be implemented via the FFT-domain realization in the preceding paragraph. Given a received block y ∈ CN , the receiver projects it onto all N cyclic shifts. We define the resulting vector as the 

ZC-shift-domain spectrum :

z ≜ 1

N CHr y = 1

N F−1 

> N

[FN (sr )∗ ⊙ F N (y)] , (8) which can be computed in O(N log N ) via one FFT, one element-wise multiplication, and one IFFT. Its q-th bin is the matched cyclic-correlation coefficient for the q-sample shift of sr and serves as the common front-end for all subsequent EZCDM demodulation. 

B. Equidistant ZCDM over Multipath UWA Channels 1) CP-Aided Transmission: UWA channels exhibit long multipath delay spreads, causing inter-symbol interference between consecutive waveform blocks. We therefore first prepend a cyclic prefix (CP) to each block. Let h =[h[0] , . . . , h [Lh − 1]] T ∈ CLh denote the sampled CIR of length Lh ≤ N with maximum excess delay Dh = Lh − 1.For each length-N block u, the transmitter prepends a CP of length Ncp ≥ Dh. After timing alignment and CP removal, the retained N samples follow a circular-convolution model. Appendix B analyzes the robustness of this model to residual receive-window displacement. Define the zero-padded channel vector eh = [ hT , 0TN −Lh ]T

and let Hc be the circulant matrix with eh as its first column. The received block is y = HcCr x + v. Since circulant ma-trices commute, applying Eq. (8) gives the received spectrum 

z = Hcx + vz = eh ⊛ x + vz , (9) where vz ≜ 1 

> N

CHr v and ⊛ denotes length-N circular convo-lution. Thus, every active coefficient in x produces a shifted replica of the CIR in the ZC-shift-domain spectrum. Eq. (9) CIR of  one  sub -channe l(a) Single-active ( τ = N = 257 )50 100 150 200 250  

> Bin Index
> 00.2 0.4 0.6 0.8 1
> Cyclic Correlation Value

(b) Full-load ( τ = 1 )50 100 150 200 250  

> Bin Index
> 00.2 0.4 0.6 0.8 1
> Cyclic Correlation Value

(c) Equidistant ( τ = 17 )00.2 0.4 0.6 0.8 1   

> Normalization Value
> 510 15
> Bin Index
> Extracted CIR Ground-truth CIR

(d) Folded path profile for (c) Fig. 3. Normalized ZC-shift-domain spectra for N = 257 after replay through a representative measured UWA channel. A single active coefficient produces one CIR replica; full loading causes severe overlap; equidistant activation separates the dominant replicas; folding those replicas estimates the channel profile. 

establishes the shift-domain observation model used by the remainder of the receiver. 

2) Equidistant Activation: EZCDM chooses the sparse sup-port at equidistant indices. For spacing τ ∈ { 1, . . . , N }, we use 

K =

 Nτ



, qℓ = ℓτ, ℓ = 0 , . . . , K − 1, (10) and construct the coefficient vector as 

x[q] = 

(

Aτ a[ℓ], q = qℓ, ℓ = 0 , . . . , K − 1,

0, otherwise , (11) where a[ℓ] ∈ C is a unit-modulus coefficient and Aτ is a common amplitude-normalization factor. Since x has K

nonzero entries of magnitude Aτ , the useful block energy is 

Eu = ∥u∥2 = N ∥x∥2 = N KA 2 

> τ

, giving Aτ = pEu/(N K ).Let Dϵ be the span containing the significant taps of the CIR under an energy threshold ϵ. A sufficient no-overlap condition for the replicas is 

Dϵ ≤ min {τ, N − (K − 1) τ } . (12) The second term accounts for the circular gap between the last and first active indices. In practice this is a design target: increasing τ suppresses replica overlap and increases the energy per active dimension, but decreases K and hence the carried bits per block. Fig. 3 illustrates this progression on a measured channel trace with N = 257 . At τ = N (i.e., [22]), the shift-domain spectrum exposes one shifted CIR replica. At τ = 1 , the replicas are densely superimposed and are irresolvable. The intermediate choice τ = 17 separates the dominant paths while retaining multiple active ZC-shift dimensions. 

3) Intra-Symbol Differential Modulation: For a significant path offset d and nonoverlapping replicas, Eq. (9) reduces to 

z[( qℓ +d) mod N ] ≈ Aτ h[d]a[ℓ]+ vz [( qℓ +d) mod N ]. (13) Hence, for a fixed d, all active dimensions within one waveform block share approximately the same complex path gain. This motivates intra-symbol differential encoding, which avoids explicit estimation of the phase of h[d].Let M be a power-of-two PSK order and B = log 2 M . For the B bits {bℓ,b }B−1 

> b=0

assigned to active dimension ℓ, define 

uℓ =

> B−1

X

> b=0

2bbℓ,b , c[ℓ] = exp 



j 2πu ℓ

M



, (14) 0 5 10 15 

> Sub-Channel Index
> -2 024
> Residual Phase (in Rad)

(a) τ = 17 , 1st block 0 5 10 15  

> Sub-Channel Index
> -4 -2 02
> Residual Phase (in Rad)

(b) τ = 17 , 2nd block 0 5 10 15  

> Sub-Channel Index
> -4 -2 024
> Residual Phase (in Rad)

(c) τ = 17 , 15th block 0 20 40 60 

> Sub-Channel Index
> -20 -15 -10 -5 05
> Residual Phase (in Rad)

(d) τ = 4 , 1st block 0 20 40 60  

> Sub-Channel Index
> -20 -10 010
> Residual Phase (in Rad)

(e) τ = 4 , 2nd block 0 20 40 60  

> Sub-Channel Index
> -10 010 20
> Residual Phase (in Rad)

(f) τ = 4 , 15th block Fig. 4. ZC-shift-domain phases at representative strong path offsets for waveform blocks with N = 257 replayed through measured UWA channels. Darker curves denote stronger paths. With τ = 17 , each fixed path offset has a nearly common phase across the active dimensions within a block; the smaller spacing τ = 4 exhibits stronger replica interference. 

for ℓ = 1 , . . . , K − 1. The transmitted coefficients are 

a[0] = 1 , a[ℓ] = a[ℓ − 1] c[ℓ], ℓ = 1 , . . . , K − 1. (15) The first active dimension is a differential reference; one waveform block carries (K − 1) B payload bits. From two adjacent observations on the same path, the receiver forms 

vℓ,d ≜ z[( qℓ + d) mod N ]z∗[( qℓ−1 + d) mod N ]

≈ A2 

> τ

|h[d]|2c[ℓ] + ηℓ,d , (16) where ηℓ,d collects noise and residual replica interference. The unknown path phase is eliminated without assuming that different physical paths share the same phase. Fig. 4 empirically checks the common-path approximation in Eq. (13). For τ = 17 , the phase at each selected path offset is stable across active dimensions within successive blocks, although it changes between blocks. At τ = 4 , stronger replica overlap produces visibly less regular observations. 

4) Multipath-Aware Demodulation: Using only the strongest path discards energy carried by other replicas. EZCDM instead folds the ZC-shift-domain energy modulo τ

to estimate a path profile: 

β[d] = 

> K−1

X

> ℓ=0

|z[( qℓ + d) mod N ]|2 , d = 0 , . . . , τ − 1. (17) This accumulation over ℓ estimates blockwise path reliability. Significant path offsets are retained as 

bD =



d : β[d] ≥ ρ max  

> d′

β[d′]



, 0 < ρ ≤ 1. (18) For each differential-symbol index ℓ = 1 , . . . , K − 1 and path offset d, we normalize the differential statistic to reduce path-amplitude bias, 

¯vℓ,d = vℓ,d 

|z[( qℓ + d) mod N ]|| z[( qℓ−1 + d) mod N ]| . (19) For each ℓ and differential modulation candidate cm =exp( j2πm/M ), the multipath-combined metric is 

Λm[ℓ] = − X 

> d∈bD

wd |¯vℓ,d − cm|2 , wd = β[d]

P  

> d′∈bD

β[d′] . (20) The hard output is bmℓ = arg max m∈{ 0,...,M −1} Λm[ℓ], and the same metrics can be converted to bit LLRs for soft decoding (see Appendix C). IV. L INK -LAYER DESIGN 

A. Design Overview 

The multiuser reception capability of EZCDM creates three link-layer challenges, which we address while preserving asynchronous random access: 

▷ Near–far effect. Different ZC roots make concurrent packets separable but do not eliminate the power disparity caused by heterogeneous path losses. We therefore use user-specific closed-loop power control : the gateway measures each user’s uplink power from its assigned-root known symbols and returns a coarse adjustment command. 

▷ Half-duplex constraint. Returning power commands re-quires a predictable downlink opportunity, yet a half-duplex gateway cannot broadcast control while receiving. We there-fore use beacon-framed random access : each beacon is fol-lowed by an uplink interval in which users independently apply continuous random backoffs, protecting control without per-packet grants, slots, or precise synchronization. 

▷ Dynamic operating regime. Channel evolution and traffic variation change the required robustness and interference level. We therefore use overlap- and channel-aware link adaptation 

to select a common modulation scheme (MS) from recent uplink observations. The overall design is conservative: a worst-case SINR estimate is chosen to satisfy the target PER on every observed link. We present a beacon-framed, half-duplex random-access protocol exampled in Fig. 5, which uses two frame types. The beacon, transmitted via the ZCMod frame with a dedicated root, carries the common MS mr and one power-control com-mand per registered user pu,r , with no per-packet grant, ACK, or retry state. The uplink data packet contains a preamble, two EZCDM data regions separated by a midamble, and a postamble; for user u, the preamble, midamble, and postamble are known ZC symbols (root ru + 1 ), while data regions use EZCDM (root ru). These known symbols jointly support user identification, packet-boundary detection, receive-power measurement, and CIR-similarity estimation. In one round, the gateway broadcasts a beacon carrying the common MS and per-user power commands, then switches to receive mode. Eligible users apply their command and the advertised mode, take a queue snapshot, draw a continuous random backoff, and transmit at most one packet. The gateway runs per-user ZC correlators and EZCDM decoders in parallel, recording arrival intervals and extracting per-user power, CIR-similarity, and effective SINR estimates. During the reserved tail interval, it updates the power-control EWMAs, computes the per-user commands for the next beacon, and applies the rules to select the common MS for the following superframe. The following subsections detail these designs. 

B. Beacon-Framed Random Access 

The gateway beacon is transmitted using ZCMod—a single-shift ZC index modulation—with a dedicated root known to all registered users. It provides a coarse timing anchor and a protected opportunity to broadcast common control. Eligible users independently choose continuous backoffs within a fixed window; their packets may still overlap asynchronously. The superframe bounds arrivals before the next beacon without slots, grants, timing advance, or precise synchronization. 

1) Superframe Timing: Let tr be the gateway beacon start time and Tb its airtime. The single-trip propagation delay to user u is τu. After receiving the beacon, user u requires a local turnaround du for processing, RX–TX switching, and a fixed holdoff. Its maximal timing error is Tclk .We define an RTT-inclusive pre-budget 

Gpre ≥ max  

> u∈U

(2 τu + du) + Tclk , (21) and a tail budget for computation and RX–TX switching, 

Gtail = T GW ctrl + T GW ,RX →TX sw . (22) Let W be the continuous-backoff window, M the set of supported MSs, and Tm the airtime of one uplink packet under mode m. Reserving Tmax = max m∈M Tm yields the fixed superframe duration 

TSF = Tb + Gpre + W + Tmax + Gtail , tr+1 = tr + TSF .

(23) Suppose an eligible user draws Xu,r ∈ [0 , W ]. Its packet-arrival start at the gateway is 

au,r = tr + Tb + 2 τu + du + Xu,r , (24) with end bu,r = au,r + Tmr . From Eqs. (21)–(23), bu,r ≤

tr+1 − Gtail < t r+1 . Hence every legal packet reaches the gateway before the next beacon; propagation-delay differences naturally contribute to the arrival skew without timing advance. (a) Beacon-framed Rounds (b) Frame Formats                                                                               

> Round rRound r+1
> Gateway
> (half-duplex)
> Time
> Beacon
> r
> Concurrent reception (A&B) Beacon
> r+1
> Concurrent reception (A&B)
> mr+1 ,qu,r+1
> delayed beacon RX (propagation delay)
> 1) apply mr,qu,r 2) draw backoff XA,r 3) RX/TX turnaround dA
> dA+XA,r
> ..
> dB+XB,r
> ...
> Xu,r ∼Uniform(0, W);
> Beacon —broadcast, common to all registered users
> Protocol /ID Common MS mr
> Per-user
> power control commands qu,r
> CRC
> Uplink packet of user u—at most one per round
> ZC preamble root ru+1
> EZCDM data root ru
> ZC midamble
> root ru+1
> EZCDM data root ru
> ZC postamble root ru+1
> Control update at the end of each round
> Per-user measurement (P u[r], γeff,u ,ξu)
> Compute MS mode and power control command
> Next-round Beacon carries
> {m r+1 ,qu,r+1 }
> Gpre Gtail Gpre Gtail
> User A
> (half-duplex)
> User B
> (half-duplex)

Fig. 5. Example of beacon-framed random access and adopted frame formats in A QUA ZC. 

2) Beacon-Triggered Randomized Transmission: Let tB

> u,r

be the local time at which user u completely receives and verifies beacon r. Eligible contenders are 

Ar = u ∈ U : Qu

 tB

> u,r

 > 0 , (25) where Qu(t) is the queue length at time t, so the instantaneous transmitter count varies across superframes. After verifying beacon r, user u applies its power-control command, and installs the advertised common mode mr .Then, if the queue is empty, it remains silent. Otherwise, it locks one head-of-line packet and draws Xu,r ∼ Uniform(0 , W ).After the mandatory turnaround, it waits Xu,r and transmits once. Packets arriving after the snapshot wait for a subsequent beacon, limiting each user to one attempt per superframe. Because backoffs are continuously distributed, different users may overlap partially or almost completely; the gateway separates user-specific observations using the assigned-root receiver in Sec. III. A user that fails to verify the beacon remains silent, avoiding stale timing or an obsolete MS. After transmission, the packet leaves the link-layer queue regardless of the decode outcome—no implicit ARQ is created. 

C. Per-User Power Control 

The gateway closes one low-rate power-control loop per user, estimating received power from assigned-root known symbols, smoothing observations, and comparing against a common target. The next beacon returns one quantized UP ,

DOWN , or HOLD command per user. 

1) User-Specific Receive-Power Estimation: The preamble, midamble, and postamble of user u use root ru + 1 and are separable from the data root ru. For known symbol 

s ∈ S u,r detected in round r, let zu,s be its ZC-shift-domain spectrum and bDu,s the significant CIR offsets from Eq. (18). We estimate the user-specific received signal power as 

bPu,s =

h X  

> d∈bDu,s

|zu,s [d]|2 − | bDu,s |bσ2

> u,s

i

> +

, (26) where bσ2 

> u,s

is estimated from off-profile bins. Summing sig-nificant path energies makes the estimate insensitive to which path dominates, while noise-floor subtraction prevents weak users from appearing artificially strong. The gateway aggregates multiple valid known symbols in the logarithmic domain: 

bpu,r = 1

|S val  

> u,r

|

X

> s∈S val
> u,r

10 log 10 

  bPu,s /P ref 

, (27) where Sval  

> u,r

contains valid known-symbol observations. The gateway maintains the exponentially weighted state 

¯pu,r = (1 − αp)¯ pu,r − + αp bpu,r , 0 < α p ≤ 1, (28) whenever a valid observation is available, where r− is the most recent preceding round in which user u was observed. Silent rounds preserve the estimate unchanged; before the first valid observation, the state is unavailable. 

2) Quantized Closed-Loop Control: Let p⋆ be the desired received power in dB and δp > 0 a deadband. For a user validly observed in round r, the gateway generates 

qu,r +1 =



UP , ¯pu,r < p ⋆ − δp,

DOWN , ¯pu,r > p ⋆ + δp,

HOLD , otherwise .

(29) If no valid observation exists, qu,r +1 = HOLD . The command vector is inserted into beacon r+1 , making the one-superframe feedback delay explicit. Let gu,r denote user u’s transmit gain in dB and ∆p the control step. After receiving beacon r + 1 , the user applies 

gu,r +1 =



[gu,r + ∆ p]gmax  

> gmin

, qu,r +1 = UP ,

[gu,r − ∆p]gmax  

> gmin

, qu,r +1 = DOWN ,gu,r , qu,r +1 = HOLD .

(30) The updated gain applies to the entire next uplink packet. A user missing the beacon retains its current gain. 

D. Overlap- and Channel-Aware Link Adaptation 

The gateway summarizes each observed link by its worst-case effective SINR and within-packet CIR similarity, queries an offline PER LUT, and feeds back one common MS with conservative fallback and asymmetric hysteresis. 1) Modulation Schemes: We expose four modulation schemes: MS 1= (DBPSK , τ = 17) , MS 2= (DBPSK , τ = 9) ,MS 3= (DQPSK , τ = 17) , and MS 4= (DQPSK , τ = 9) .Larger spacing reduces ZC-shift-domain replica overlap and allocates more energy per active coefficient but lowers the number of subchannels. DBPSK trades one bit per differential symbol for greater phase-variation robustness. The best choice depends jointly on interference and channel variation. 

2) CIR Similarity: The preamble, midamble, and postamble provide three user-specific CIR snapshots per detected packet. For two snapshots separated by ∆t, we use the magnitude of their normalized complex inner product, 

ξu(t, t +∆ t) = 

PL−1 

> i=0

bhu(t, i )bh∗

> u

(t + ∆ t, i )

qPL−1 

> i=0

|bhu(t, i )|2

qPL−1 

> i=0

|bhu(t + ∆ t, i )|2

,

(31) where bhu(t, i ) is tap i of user u’s estimated CIR over length L.Let tpre  

> u,r

, tmid  

> u,r

, tpost  

> u,r

denote the three reference-symbol times. We summarize within-packet variation as 

bξu[r] = 12

 ξpre ,mid  

> u

[r] + ξmid ,post  

> u

[r], (32) evaluated only when all three reference symbols pass root-assignment and CIR-quality checks. A smaller value indicates faster channel evolution or stronger multipath-profile variation; the metric does not require a successful payload CRC. 

3) Worst-Case SINR Estimation: Let Or be the set of users with valid interval and power estimates in round r.For each detected user u, the gateway obtains its arrival interval [au,r , b u,r ] from reference symbols. All other users in Or whose intervals overlap with [au,r , b u,r ] are treated as interferers: 

Iu, max [r] = v̸ = u | [av,r , b v,r ] ∩ [au,r , b u,r ]̸ = ∅ . (33) Since multi-user interference dominates noise in the concurrent-access regime, the effective per-user SINR is ap-proximated by the power ratio against the set of all overlapping interferers: 

bγeff ,u [r] ≈ bPu[r]

P 

> v∈I u, max [r]

bPv [r] , (34) where bPu[r] is from Sec. IV-C. This formulation is deliberately conservative: it replaces the time-varying interference pattern with the worst case where all overlapping interferers are active simultaneously. The subsequent common-MS selection further protects the user with the smallest bγeff ,u [r], yielding a doubly conservative design. 

4) Common-MS Selection and Feedback: Offline, we su-perimpose EZCDM packets over an SINR grid for channels spanning the observed CIR-similarity range and build the MS– SINR–CIR-Similarity–PER look-up table [PER( m, γ, ξ ) (see Appendix D for construction details). For a candidate mode and observation pair, predicted goodput is 

G(m; γ, ξ ) = Rm

1 − [PER( m, γ, ξ ), (35) Diver  

> Diving Pool
> Transmitter
> Transmitter
> Receiver
> Diver
> Receiver
> hydrophone
> Fig. 6. Diving pool environment for channel and signal profiling.

where Rm is the coded PHY rate. All packets in superframe r use the common mode mr

announced in beacon r. After reception, define the valid adaptation set 

Vr = u : bγeff ,u [r] and bξu[r] are both valid . (36) When Vr̸ = ∅, the gateway first forms the feasible modes satisfying the target PER on every valid link, 

Fr =

n

m ∈ M : max 

> u∈V r

[PER  m, bγeff ,u [r], bξu[r] ≤ εPER 

o

,

(37) with εPER = 0 .1. If Fr̸ = ∅, the candidate maximizes the worst-link goodput: 

m⋆r ∈ arg max 

> m∈F r

min 

> u∈V r

G m; bγeff ,u [r], bξu[r], (38) with ties broken by smaller worst-link PER, then by smaller PHY rate. If Fr = ∅, the gateway selects the mode minimiz-ing the maximum predicted PER. V. P ERFORMANCE EVALUATION 

Our evaluation aims to answer the following questions: (1) Can EZCDM sustain reliable high-rate communication over dynamic UWA channels without explicit CIR estimation or equalization? (2) Can the closed-loop power-control mecha-nism reduce near–far imbalance in a two-user pool experi-ment? and (3) Can the complete A QUA ZC system convert EZCDM’s PHY rate and concurrent-decoding capability into higher network throughput than both non-concurrent protocols and representative concurrent-access schemes? 

A. Evaluation Setup 1) Methodology.: We adopt a layered evaluation method-ology. At the physical layer, we employ both channel-trace-driven and signal-trace-driven approaches: the former replays measured CIRs via the replay-filter method [44], while the latter replays recorded passband signals directly. Both operate in a single-user setting, which is representative because multi-user cross-root interference is effectively whitened. At the network layer, we conduct system-level simulations using a PHY-in-the-loop simulator built on Matlab and the Bellhop channel model [45]. This simulator captures realistic UWA channel dynamics, including multipath propagation, Doppler shifts, and time-varying SNRs. Node Motion Range  

> Gateway Fig. 7. Lake environment for channel profiling.

2) Experimental setup.: To gather real-world UWA chan-nels, we conduct empirical experiments in two distinct en-vironments: a diving pool capturing authentic diver mobility, and a lake utilizing emulated motions. Appendix E provides detailed channel measurement methodology. 

Pool Setup. As illustrated in Fig. 6, our pool experiments are conducted in a 25 × 25 × 6m diving pool, with acoustic nodes deployed at a depth of approximately 1.5m. To comprehen-sively evaluate the system under varying mobility profiles, we collect data across four distinct motion patterns relative to a stationary receiver: strictly stationary, forward-backward, left-right, and vertical (up-down) movements. For these tests, we developed a half-duplex acoustic SDR based on an STM32MP157 SoC running embedded Linux. As shown in Fig. 6, the acoustic front-end uses a piezoelectric transducer as both transmitter and hydrophone. The platform operates at a 50kHz carrier, 20kHz bandwidth (40–60kHz), and 200kHz AD/DA sampling rate. 

Lake Setup. To validate our system in a heterogeneous environment, we conduct experiments in an artificial lake measuring 288 ×182 m (Fig. 7), with devices deployed at a 2m depth. Notably, the lakebed is densely covered with decorative stones, which severely exacerbates multipath scattering. The lake experiments use National Instruments (NI) data acquisition boards for continuous passband sampling. The transmitter consists of a transducer driven by a power am-plifier, while the receiver uses a dedicated hydrophone. The setup operates at 120kHz sampling, 25kHz carrier, and 6kHz bandwidth (22–28kHz). To emulate the high-dynamic mobility of a scuba diver, the transmitter is continuously towed within a designated trajectory region (marked yellow in Fig. 7). 

B. Physical-layer Performance 

We compare EZCDM against DQPSK, DSSS [46], M-ary CSS [47], and OFDM-DQPSK [48], all configured with the same symbol duration and bandwidth as EZCDM. The uncoded PHY rates of EZCDM modes and baseline waveforms are derived in Appendix F. Among the EZCDM modes, we report MS 2, MS 3 and MS 4, corresponding to 27, 28 and 54 uncoded bits per symbol ( N = 257 ). All results use a single-user setting, since 1) the benchmark waveforms do not support multi-user random access; and 2) multi-user cross-root interference is effectively whitened, this remains representative of concurrent-access performance. 0 5 10 15 20 25                

> Relative Eb/N0 (dB)
> 10 -3
> 10 -2
> 10 -1
> 10 0
> Uncoded Bit Error Rate
> EZCDM-MS2
> EZCDM-MS3
> EZCDM-MS4
> EZCDM-MS4-VTRM
> DQPSK
> DSSS-DQPSK
> CSS
> OFDM-DQPSK
> (a) ξ∗mean
> CIR ≥0.25 (low/moderate variability) 0510 15 20 25
> Relative Eb/N0 (dB)
> 10 -2
> 10 -1
> 10 0
> Uncoded Bit Error Rate
> EZCDM-MS2
> EZCDM-MS3
> EZCDM-MS4
> EZCDM-MS4-VTRM
> DQPSK
> DSSS-DQPSK
> CSS
> OFDM-DQPSK
> (b) ξ∗mean
> CIR <0.25 (high variability) Fig. 8. Channel-trace-driven evaluation: uncoded BER vs. Eb/N 0of EZCDM (MS 2, MS 3, MS 4, MS4-VTRM), DSSS, CSS, and OFDM.

1) Real-world channel-based evaluation.: We use both the measured channels and the open-source channels obtained from the Watermark dataset [49]. We refer to channels with 

ξ∗mean  

> CIR

> 0.5 are defined as low variability channels, those with 

0.25 < ξ ∗mean  

> CIR

≤ 0.5 as moderate variability channels, and channels with ξ∗mean  

> CIR

≤ 0.25 are referred to as high variability channels. The replay filter method is adopted to apply real-world channel responses to different waveforms, incorporating both instantaneous Doppler shifts and multipath effects. We consider uncoded BER as a function of Eb/N 0, which normalizes the energy differences caused by the distinct num-bers of information bits carried per waveform block. Because the replay-filter method replays CIRs extracted from real-world measurements, the recorded signals already contain residual measurement noise; the Eb/N 0 values shown here therefore represent the ratio of the added signal energy to this existing noise floor, rather than an absolute noise-free reference. As shown in Figs. 8(a)(b), EZCDM outperforms the baseline waveforms under both channel conditions, with the advantage being more pronounced in highly dynamic channels. Under ξ∗mean  

> CIR

≥ 0.25 , MS 3 achieves orders-of-magnitude lower BER than other waveforms at relative Eb/N 0 ≥ 20 dB. At a BER of 0.02 , EZCDM achieves at least a 5 dB Eb/N 0

gain over CSS, the best-performing baseline. Under ξ∗mean  

> CIR

<

0.25 , the gain increases to 10 dB at a BER of 0.01 , confirming EZCDM’s robustness under severe channel dynamics. We also compare MS 4 against a virtual time-reversal mirror (VTRM) baseline, a recent non-coherent demodulator that correlates the received signal with the conjugate of the preamble and takes the peak of the resulting profile—without EZCDM-MS1 EZCDM-MS2 EZCDM-MS3 EZCDM-MS4 DQPSK CSS SF=8 OFDM-DQPSK  

> Bit Error Rate
> 00.1 0.2 0.3 0.4 0.5 Quasi-Static Moving Fig. 9. Signal-trace-driven evaluation: BER of EZCDM and baseline wave-forms decoded from recorded pool-environment passband signals.

exploiting the multipath structure as our detector does. MS 4 consistently outperforms this VTRM counterpart across both channel regimes, confirming the benefit of multipath-weighted combining over single-peak detection. 

2) Real-world signal-based evaluation.: As a complemen-tary signal-driven validation, we transmit EZCDM and the benchmark waveforms through the pool testbed and directly decode the recorded passband signals without channel es-timation or equalization. We use 1/2 LDPC coding for all waveforms, and the per-sample SNR is set to around 8 dB. Fig. 9 reports the per-mode BER under the four representative motion patterns whose CIRs are shown in Fig. 1(a)–(d). In the quasi-static regime (Fig. 1(a)), MS 1 and MS 2 achieve near-error-free transmission, whereas all baselines exhibit BER above 0.2. Under highly dynamic conditions (Fig. 1(b)–(d)), the BER of MS 1 and MS 3 outperforms other waveforms by approximately an order of magnitude. Although MS 2 and MS 4 suffer degraded performance in the most severe dynamic scenarios, 42% and 63% of their packets, respectively, are decoded without error—far exceeding the sub-10% error-free rate of all baseline waveforms. 

C. Power Control Performance 

To validate the closed-loop power control mechanism de-scribed in §IV-C, we conduct a two-user experiment in the pool environment using the developed prototype (see Fig. 6). The near user is placed close to the gateway (2m) while the far user is positioned at the pool edge (6m), creating an initial received-power imbalance of approximately 6 dB. δp is set to 25% of the target power, and ∆p is set to 10% of the maximal gain. Across consecutive update rounds, the normalized preamble-correlation magnitudes of both users converge: after 5 rounds, both reach within 0.14 dB of the target level, and the residual inter-user power imbalance is reduced to less than 0.27 dB (averaged over ten repeated trials). These results confirm that the proposed closed-loop mechanism can effectively mitigate near–far imbalance in a practical deployment. 

D. Network-layer Performance 

Setup. We simulated a 12-node star-topology network (1 gateway, 11 users) uniformly deployed within a 70 m radius (corresponding to a 47 ms propagation delay). The system bandwidth is 20 kHz and the carrier frequency is 50 kHz. The frame/slot duration is fixed at T = 378 ms, consisting of a 23-symbol packet (3 preamble symbols, N = 257 samples per Packet generation rate 66 (packets/slot)     

> 0.05 0.1 0.15 0.2
> Aggregate goodput (bit/s)
> 0
> 200
> 400
> 600
> 800
> 1000
> AquaZC-ELA
> AquaZC-CLA
> S-Aloha-EZCDM
> TDMA-EZCDM
> CDMA
> ZCMod
> Fig. 10. Network throughput of different MACs.

symbol, 10 /11 CP ratio, 325 ms) and a 53 ms guard interval that accommodates the maximum propagation delay plus the timing uncertainties of the proposed MAC protocol ( 6 ms). We use a PHY-in-the-loop simulator built on Matlab and the Bellhop channel model [45]. Matlab manages MAC logic and passes each transmission to an external PHY block, which maps signals to Bellhop channels, performs waveform-specific synchronization, soft demodulation, LDPC decoding, and CRC verification. Channel dynamics follow a latent Ornstein-Uhlenbeck (OU) process augmented with a compound Pois-son jump component, with time-varying intensity coupled to ξ∗mean  

> CIR

; the measured SINR variation is [−13 , dB , 10 , dB ].Packets at each node are generated according to a Poisson process with mean arrival rate λ ∈ [0 .04 , 0.22] packets/slot. Each simulation run lasts over 1000 s, and results are averaged over 10 independent runs. The following protocols are considered: 

▷ AQUA ZC-(CLA/ELA). The proposed conservative LA (CLA, Sec. IV-D) selects a common MS from the precomputed MS–SINR–CIR-Similarity–PER LUT. For comparison, an ex-haustive LA benchmark (ELA) iterates through all available modes after each round and chooses the one that maximized goodput on the just-observed channel; this mode is then used for the next round, which is equivalent to having perfect knowledge of the last-round channel state to select the MS for the upcoming transmission. Note that the ELA scheme is infeasible in practice due to the exhaustive search and perfect channel knowledge. 

▷ S-ALOHA-EZCDM. Each user independently transmits with probability inversely proportional to the number of users per round; overlapping packets are treated as a collision, and the CLA scheme selects the MS. 

▷ TDMA-EZCDM. Each user is assigned a fixed time slot and transmits with the EZCDM waveform, using the same CLA look-up based on last-round channel information. 

▷ CDMA and ZCMod. Users transmit in each slot. Both use the same distinct-sequence per-user as EZCDM, differing only in the demodulation stage: CDMA uses peak-based matching, while ZCMod employs single-shift template matching [22]. 

Results. As shown in Fig. 10, A QUA ZC-CLA achieves sub-stantial throughput gains across the entire spectrum of packet generation rates. At a workload of 0.16 packets/slot, it deliv-ers improvements of 7.13 ×, 3.34 ×, 2.40 ×, and 2.02 × over TDMA-EZCDM, CDMA, S-ALOHA-EZCDM and ZCMod respectively, while the peak throughput is enhanced by 10.6 ×,4.56 ×, 2.71 ×, and 2.92 ×, respectively. We analyze the gains case by case: 

▷ vs. TDMA-EZCDM. AQUA ZC benefits from both con-current decoding and on-demand access that avoids idle slots; TDMA could in principle adopt higher-rate OFDM, but OFDM performs poor in dynamic channel (§V-B). 

▷ vs. S-ALOHA-EZCDM. Holding the waveform and LA fixed, A QUA ZC mainly benefits from concurrent decoding. 

▷ vs. CDMA and ZCMod. All use distinct-sequence concur-rent transmission, so the gain is from the waveform design: EZCDM’s higher uncoded rate and intra-symbol differential receiver deliver more decodable bits per concurrent symbol. 

▷ vs. A QUA ZC-ELA. Holding the waveform and MAC fixed, CLA and ELA exhibit comparable performance while ELA is infeasible in practice, confirming that the conservative strategy is effective under different packet rates. VI. R ELATED WORK 

Underwater acoustic communication and network-ing. Single-carrier modulations (M-FSK [50], M-PSK [51], CSS [47], [52]–[54], DSSS [46], CDMA [19]) have been widely studied alongside multi-carrier schemes (OFDM [55], OTFS [56], OCDM [17]); more recently, smartphone- and smartwatch-based UAC systems have been explored for under-watermessaging and SOS signal detection [5], [16], [57]. On the MAC side, common designs include random access [12], handshake-based [10], and TDMA-based [9] protocols. Both layers share the same limitation: conventional PHY waveforms either offer low rates or require accurate channel estimation, while MAC protocols treat overlapping transmissions as de-structive collisions. By designing a waveform that achieves both high rate and concurrent-access capability, our work redefines the role of the MAC layer. 

Link adaptation in UAN. Prior LA schemes tune mod-ulation based on post-equalization SNR in point-to-point links [58], [59] or leverage sparse channel estimation and pre-trained classifiers [60], [61]. In contrast, our LA jointly adapts the differential alphabet and τ based on both CIR similarity and worst-case SINR in a multi-user concurrent-access setting. VII. C ONCLUSION 

This paper presented AQUA ZC, a high-rate cross-layer concurrent-access system that combines EZCDM—an equidis-tant ZC division multiplexing waveform with a lightweight intra-symbol differential receiver—with root-separated asyn-chronous uplinks, closed-loop power control, and overlap-and CIR-aware link adaptation. Experimental evaluations us-ing custom SDR prototypes, replay of measured and open-source UWA channels, and PHY-in-the-loop network simula-tions confirm that A QUA ZC substantially outperforms both non-coherent and coherent baseline waveforms as well as conventional MAC protocols. Future work includes real-time SoC implementation and channel access control that adapts to traffic load and multi-user reception capability. APPENDIX AVALIDATION OF WHITENED CROSS -U SER INTERFERENCE 

ZC sequences with distinct roots exhibit bounded and con-sistently low cyclic cross-correlation (Sec. III-A). In a multi-user random-access setting, assigning a different root to each user transforms concurrent access into an equivalent single-user problem—each receiver sees the target signal plus resid-ual cross-root interference that superimposes as an elevated noise floor. Section IV exploits this property to treat multi-user interference as additive white noise. This appendix provides the quantitative justification: we measure the spectral flatness of the residual cross-root interference under asynchronous random access and compare it directly against the AWGN benchmark. We examine three interference regimes through simulation: (i) Full Concurrent : all users’ frame starts align within the CP window, producing complete temporal overlap of the ZC-shift-domain blocks; (ii) Partial Concurrent : users’ arrival times are independently randomized according to the beacon-framed access in Sec. IV, creating arbitrary partial overlaps; and (iii) AWGN as the reference. The goal is to statistically compare the spectral characteristics of the first two regimes against the white-noise benchmark. Recall from Eq. (8) that the ZC-shift-domain spectrum z

is the matched cyclic-correlation output. Its absolute value 

|z|, which we denote by the vector r, is proportional to the cyclic correlation magnitude spectrum [62]. In the absence of a signal, r reduces to the post-correlation noise spectrum. To quantify spectrum whiteness, we introduce the spectral Flatness Measure (SFM) [63], [64], a compact statistical descriptor defined as 

SFM = exp 

 1

> N

PNi=1 ln r[i]



> 1
> N

PNi=1 r[i] . (39) By construction, SFM ∈ [0 , 1] : a value close to 1 indicates a perfectly flat (white) spectrum, whereas a value close to 0 indicates a highly structured, tonally concentrated spectrum. This validation normalizes the interference and focuses on the intrinsic distribution pattern, independent of signal magnitude. Since each frame is treated as statistically inde-pendent, local anomalies are preserved rather than smoothed out, ensuring a faithful characterization of spectrum behavior. Fig. 11(a) illustrates examples of cyclic correlation under different levels of concurrency. As the number of concurrent users increases, the background noise floor rises systemati-cally, but the overall spectral envelope remains unchanged— the shape is preserved, only the level shifts. This separation of level and shape is the signature of whitened interference. Fig. 11(b) shows that both Full Concurrent and Partial Con-current interference scenarios yield SFM values around 0.85, closely aligning with those of AWGN. Quantitatively, the mean SFM difference between either concurrent regime and AWGN is below 0.05 across the tested concurrency levels, confirming a strong tendency toward spectral whiteness in both interference types. 00.5 1     

> No Interference
> 00.5 1
> Cyclic Correlation Value
> Double Concurrency
> 050 100 150 200 250
> Bin Index
> 00.5 1
> Triple Concurrency

(a) 0 200 400 600 800 1000       

> Frame Index
> 00.2 0.4 0.6 0.8 1
> SFM
> Partial Concurrent Full Concurrent White Noise
> 100 200 300 400 500 600 0.8 0.85 0.9

(b) Fig. 11. (a) Influence of equal-power multi-user interference on cyclic correlation spectrum; (b) SFM of cyclic correlation spectra under different types of interference. 

In summary, this validation establishes that multi-user ZC cross-root interference under asynchronous access is effec-tively whitened. This property provides the analytical founda-tion for two simplifications used throughout the paper: (i) the worst-case effective SINR in Eq. (34) needs only power-level information, not phase or structural interference models; and (ii) single-user PER results obtained through replay or Bellhop channels remain representative of multi-user performance in system-level simulation. APPENDIX BCP W INDOW -O FFSET ROBUSTNESS 

The CP model in Sec. III-B assumes that the receiver retains an N -sample window after timing alignment. This appendix makes explicit how much residual window displacement that model tolerates. Using the notation of Eq. (7), the CP-extended version of u can be written as 

ucp [n] = u([ n]N ) , n = −Ncp , . . . , N − 1, (40) where [·]N denotes reduction modulo N . For the channel in Sec. III-B, whose nonzero taps lie at delays 0, . . . , D h, let the receive window begin ∆ samples earlier than the nominal block boundary. Its n-th sample is 

y∆[n] = 

> Dh

X

> d=0

h[d]ucp [n − ∆ − d] + v∆[n], n = 0 , . . . , N − 1.

(41) If 

0 ≤ ∆ ≤ Ncp − Dh, (42) all required samples remain inside the CP-protected interval, and Eq. (41) is still a circular convolution. Define the length-

N circular-shift operator S∆ by [S∆a]n = a([ n − ∆] N ). The retained block and its ZC-shift-domain spectrum are then 

y∆ = S∆Hcu + v∆,

z∆ = 1

N CHr y∆ = S∆Hcx + vz, ∆. (43) The second equality follows because S∆, Hc, and Cr are circulant and commute, while CHr Cr = N IN . Hence, an admissible window displacement cyclically translates the en-tire channel-broadened ZC-shift profile; it does not change its shape or the relative spacing among the activated coefficients. 0 50 100 150 200 250   

> Bin Index
> 00.2 0.4 0.6 0.8 1
> Cyclic Correlation Value
> No Shift 5 Samples Shifted
> 150 200 250 00.1 0.2

(a) CP padding 0 50 100 150 200 250    

> Bin Index
> 00.2 0.4 0.6 0.8 1
> Cyclic Correlation Value
> No Shift 5 Samples Shifted
> 150 200 250 00.05 0.1 0.15

(b) Zero padding Fig. 12. Effect of a five-sample receive-window displacement on the ZC-shift-domain spectrum with (a) cyclic-prefix padding and (b) zero padding. 

The front end can absorb this common translation into the estimated path offsets. This is a bounded robustness result rather than a claim that an arbitrary timing error is harmless: displacement beyond Eq. (42) can introduce samples from an adjacent block and invalidate the circular model. Fig. 12(a) illustrates this behavior for a five-sample receive-window displacement: the CP case preserves the ZC-shift-domain profile up to a common cyclic translation. With zero padding (ZP), an early window instead contains zeros while omitting samples from the other end of the block. The resulting truncation cannot be represented by a circular-shift operator and causes leakage across the ZC-shift bins, as shown in Fig. 12(b). This preservation of the receiver model is why we use CP rather than ZP. APPENDIX CLLR C ONVERSION FOR CHANNEL DECODING 

In the system-level simulator, the LDPC decoder consumes bit log-likelihood ratios (LLRs) derived from the candidate metrics in Eq. (20). The folded path profile and its weights 

{wd} are shared across the block, but the metric Λm[ℓ] is computed separately for every differential index ℓ by com-bining only its observations across the selected path offsets 

d ∈ bD; metrics from different values of ℓ are not combined. For the B-bit binary label of candidate m, let bp(m) ∈ { 0, 1}

denote its p-th bit, where p = 0 , . . . , B − 1. We first convert each candidate metric into a normalized symbol probability, 

Pm[ℓ] = exp(Λ m[ℓ]/T Λ)

PM −1 

> n=0

exp(Λ n[ℓ]/T Λ) , (44) for m = 0 , . . . , M − 1 and ℓ = 1 , . . . , K − 1. Here, TΛ > 0 is the likelihood-scale parameter, and we currently set TΛ = 1 .The bit LLR supplied to the LDPC decoder is then 

Lp[ℓ] = log 

P 

> m:bp(m)=0

Pm[ℓ]

P 

> m:bp(m)=1

Pm[ℓ]= log 

P 

> m:bp(m)=0

exp(Λ m[ℓ]/T Λ)

P 

> m:bp(m)=1

exp(Λ m[ℓ]/T Λ) . (45) Thus, one waveform block produces (K − 1) B LLRs. In implementation, the two log-sum-exp terms in Eq. (45) are evaluated with the standard max-subtraction stabilization to avoid overflow and underflow. APPENDIX DLUT FOR SYSTEM -LEVEL SIMULATION 

We build the look-up table off-line as follows. First, we assemble a channel set spanning the CIR-similarity range observed in our measurements (Sec. V-A). For each target sim-ilarity interval, Bellhop [45] is used to generate independent UWA channel realizations with the corresponding multipath dynamics. Each candidate MS is then transmitted through randomly selected channels from this set. At each (SINR, CIR-similarity) bin, signal-level decoding trials produce the CRC packet-error rate [PER( m, γ, ξ ). Fig. 13 shows the resulting MS–SINR–CIR-Similarity–PER surfaces. Their staircase tran-sitions reflect the distinct robustness–rate operating regions of the four MSs. At run time, the selector first requires a predicted PER no larger than 0.1 for every valid observed link. Among those feasible modes, it maximizes the worst-link predicted goodput 

Rm[1 − [PER( m, γ, ξ )] , as defined in Eq. (38). If no mode satisfies the PER target, it falls back to the mode with the smallest worst-link predicted PER. APPENDIX ECHANNEL MEASUREMENT METHODOLOGY 

Across both the pool and lake environments (see Sec-tion V-A for the hardware and deployment details), we adopt a standardized sounding approach: a linear chirp followed by a pseudo-random binary sequence (PRBS) serves as the probe frame. The receiver uses the chirp for coarse packet synchronization and then performs cross-correlation with the PRBS to extract the Channel Impulse Response (CIR) at high resolution. Although utilizing the identical pool environment and SDR platform, the background measurements were exclusively de-signed for channel sounding to profile UWA dynamics, rather than transmitting EZCDM communication payloads. Specif-ically, the transmitter generated a specialized probe frame operating at a 50 kHz carrier frequency with a 200 kHz sam-pling rate. The frame consists of two parts: 1) A 0.3-second Linear Frequency Modulation (LFM) chirp used for precise frame synchronization. 2) A continuous pseudo-random binary sequence (PRBS) for channel probing. The PRBS is generated using a 511-length m-sequence with a chip duration of 0.05 ms (yielding a 20 kHz bandwidth) and is modulated via BPSK. At the stationary receiver, the offline processing first utilizes cross-correlation with the reference chirp to locate the tempo-ral start of the frame. Subsequently, a sliding matched filter correlates the received passband signal with the local PRBS replica across the delay domain. This process precisely extracts the high-resolution, time-varying delay profiles ( h(t, τ )). During these sounding sessions, the transmitting node was maneuvered through specific kinematic patterns—hovering, horizontal motion (at approximately 0.5 m/s), and vertical dis-placement. These isolated empirical measurements accurately capture the severe Doppler scaling and multipath evolution characteristic of diver mobility, which directly motivates the core physical-layer innovations of A QUA ZC. -15 -10 -5 0 5 10 

SINR (dB) 

MS1 MS2 MS3 MS4 0.2 0.4 0.6 0.8 1    

> (a) PER performance in UWA channels with ξ∗mean
> CIR >0.5(low variability)

-15 -10 -5 0 5 10 

SINR (dB) 

MS1 MS2 MS3 MS4 0.2 0.4 0.6 0.8 1     

> (b) PER performance in UWA channels with 0.25 < ξ ∗mean
> CIR ≤0.5(moderate variability)

-15 -10 -5 0 5 10 

SINR (dB) 

MS1 MS2 MS3 MS4 0.2 0.4 0.6 0.8 1     

> (c) PER performance in UWA channels with ξ∗mean
> CIR ≤0.25 (high variability) Fig. 13. MS–SINR–CIR-Similarity–PER look-up table under UWA channels with varying CIR similarity.

APPENDIX FPHY R ATES OF DIFFERENT WAVEFORMS 

This appendix derives the uncoded data-only PHY rates of EZCDM and the baseline waveforms used in the physical-layer evaluation (§V-B). Rates include the cyclic-prefix (CP) dura-tion but exclude packet-level overhead (preamble, midamble, postamble, and guard intervals). All waveforms are assumed to operate at the same bandwidth W and the same symbol duration Tsym for fair comparison. 

EZCDM Modes. For mode m with ZC length N = 257 ,equidistant spacing τm, and differential alphabet size Mm, the number of active shift dimensions is Km = ⌊N/τ m⌋. The first active dimension serves as a differential reference, so each EZCDM symbol carries Lm = ( Km − 1) log 2 Mm uncoded TABLE I UNCODED PHY RATES OF EZCDM MODES AND BASELINE WAVEFORMS .ALL RATES INCLUDE THE CP ( ρ = Ncp /N ) AND ASSUME IDENTICAL BANDWIDTH W AND SYMBOL DURATION Tsym = N (1 + ρ)/W , WITH 

N = 257 .

Waveform bits/sym. R/W (bits/s/Hz) Rel. to MS 1 

EZCDM modes 

MS 1 (DBPSK, τ =17 ) 14 14 /[257(1 + ρ)] 1.000 ×

MS 2 (DBPSK, τ =9 ) 27 27 /[257(1 + ρ)] 1.929 ×

MS 3 (DQPSK, τ =17 ) 28 28 /[257(1 + ρ)] 2.000 ×

MS 4 (DQPSK, τ =9 ) 54 54 /[257(1 + ρ)] 3.857 ×

Baseline waveforms 

DQPSK 2 2/[257(1 + ρ)] 0.143 ×

DSSS-BPSK 1 1/[257(1 + ρ)] 0.071 ×

CSS ( SF =8 ) 8 8/[257(1 + ρ)] 0.571 ×

OFDM-DQPSK 512 512 /[257(1 + ρ)] 36 .571 ×

payload bits. With CP ratio ρ = Ncp /N , the symbol duration is Tsym = N (1 + ρ)/W . The uncoded PHY rate is therefore 

REZCDM 

m = Lm

Tsym 

= (Km − 1) log 2 Mm

N (1 + ρ) W. (46) Table I summarizes the resulting per-mode payloads and normalized rates. 

Baseline Waveforms. DSSS-BPSK uses a 256-chip spreading sequence with BPSK modulation, delivering 1 uncoded bit per symbol: RDSSS = 1/T sym = W/ [N (1 + ρ)] . CSS 

(SF = 8 ) uses chirp spreading with 2SF = 256 samples per chirp, delivering SF = 8 uncoded bits per symbol: 

RCSS = 8 /T sym = 8 W/ [N (1 + ρ)] . OFDM-DQPSK uses 256 subcarriers with differential QPSK (2 bits per subcarrier), delivering 256 × 2 = 512 uncoded bits per OFDM symbol: 

ROFDM = 512 /T sym = 512 W/ [N (1 + ρ)] . DQPSK is a single-carrier differential QPSK baseline carrying 2 bits per symbol: RDQPSK = 2 /T sym = 2 W/ [N (1 + ρ)] . VTRM uses the same EZCDM mode-4 parameters ( τ = 9 , DQPSK) and thus has the same payload (54 bits/symbol) and rate as MS 4, differing only in the demodulation algorithm. For the EZCDM family, the rate spans a factor of 3.86 ×

from MS 1 to MS 4. MS 4 carries twice the payload of MS 2 (54 vs. 27 bits/symbol) at the same spacing τ = 9 , isolating the effect of the differential alphabet size. MS 2 and MS 3 have nearly equal payloads ( 27 vs. 28 bits/symbol) but employ dif-ferent spacing–alphabet combinations, enabling approximately rate-matched comparisons. Among the baselines, OFDM-DQPSK achieves the highest nominal rate ( 512 bits/symbol) because all subcarriers carry independent payload, but its BER performance degrades substantially under time-varying UWA channels without accurate channel estimation (§V-B). APPENDIX GNOTATION TABLE II SUMMARY OF KEY NOTATION .

System Model (Sec. II) 

fc Carrier frequency 

h(t, τ ) Time-varying baseband channel impulse response (CIR) 

ap = vp/c Path-dependent Doppler scale 

x(t) Complex-baseband packet transmit waveform 

y(t) Received baseband signal 

νcfo Carrier-frequency offset (CFO) 

U(t) Set of active users at time t

PHY Design (Sec. III) 

N ZC sequence length 

r ZC root index (coprime to N )

sr Length-N ZC sequence with root r

Cr ZC shift matrix [sr , Πs r , . . . ]

x Coefficient vector (payload mapped to shift domain) 

u Transmitted time-domain block (before CP) 

z ZC-shift-domain spectrum 

h Sampled CIR vector 

τ Equidistant shift spacing 

K = ⌊N/τ ⌋ Number of active shift dimensions 

qℓ = ℓτ Active shift index ( ℓ = 0 , . . . , K − 1)

a[ℓ] Unit-modulus coefficient at active dimension ℓc[ℓ] Differential symbol at dimension ℓβ[d] Folded path-profile energy at offset d

bD Set of significant path offsets 

Λm[ℓ] Multipath-combined metric for candidate mwd Path weight for offset d

LLC Design (Sec. IV) 

U Set of registered users {1, . . . , K }

tr Start time of beacon rTb Beacon airtime 

τu Single-trip propagation delay to user uGpre RTT-inclusive pre-budget 

W Continuous-backoff window length 

Tm Uplink packet airtime under mode mTSF Superframe duration 

Xu,r Random backoff drawn by user u in round rau,r , b u,r Packet arrival start/end at the gateway 

mr Common modulation scheme for superframe rpu,r Power-control command for user u in round r

¯pu,r Exponentially weighted (EWMA) power state 

qu,r Power-control command ( UP/DOWN/HOLD )

ξu CIR similarity (magnitude of normalized inner product) 

Iu, max [r] Set of interferers overlapping user u in round r

bγeff ,u [r] Worst-case effective SINR 

PER( m, γ, ξ ) Packet-error rate LUT 

m⋆r Candidate common MS for round r + 1 REFERENCES [1] G. V. Research, “Diving tourism market size, share & growth report, 2030,” 2023, accessed: 2023-06-20. [Online]. Available: https://www. grandviewresearch.com/industry-analysis/diving-tourism-market-report [2] R. Cattaneo-Vietti, “The essential role of diving in marine biology,” 2021, accessed: 2025-06-20. [Online]. Available: https://riviste.unige.it/ bels/article/view/1279 [3] J. Colarossi and J. Ricciardi, “Scuba diving safely for marine biology research,” Jul. 2024, accessed: 2025-06-20. [Online]. Available: https://www.bu.edu/articles/2024/ scuba-diving-safely-for-marine-biology-research/ [4] T. Qiu, Y. Li, and X. Feng, “Optimal broadcast scheduling algorithm for a multi-AUV acoustic communication network,” IEEE/ACM Trans-actions on Networking , vol. 31, no. 5, pp. 2058–2069, 2023. [5] Q. Yang and Y. Zheng, “Neural enhanced underwater SOS detection,” in IEEE INFOCOM , 2024, pp. 971–980. [6] H. Hou, B. Zheng, S. Cheng, X. Zhao, P. Wu, L. He, Y. Guo, G. Xing, and Z. Yan, “Aquascan: A sonar-based underwater sensing system for human activity monitoring,” in ACM MobiCom , 2025, pp. 438–452. [7] B. Tian, L. Zhao, B. Chen, M. Wu, H. Zheng, D. Vasisht, F. Y. Yan, and K. Nahrstedt, “AquaScope: Reliable underwater image transmission on mobile devices,” in ACM MobiSys , 2026, pp. 607–621. [8] B. Tian, L. Zhao, B. Chen, H. Zheng, J. Yang, M. Wu, D. Vasisht, and K. Nahrstedt, “Aquavlm: Improving underwater situation awareness with mobile vision language models,” arXiv preprint arXiv:2510.21722 ,2025. [9] K. Kredo II, P. Djukic, and P. Mohapatra, “Stump: Exploiting position diversity in the staggered TDMA underwater MAC protocol,” in IEEE INFOCOM 2009 , 2009, pp. 2961–2965. [10] M. Molins and M. Stojanovic, “Slotted FAMA: a MAC protocol for underwater acoustic networks,” in OCEANS 2006-Asia Pacific . IEEE, 2006, pp. 1–7. [11] H. Hwang and H.-S. Cho, “Throughput and delay analysis of an under-water CSMA/CA protocol with multi-RTS and multi-DATA receptions,” 

International Journal of Distributed Sensor Networks , vol. 12, 2016. [12] N. Chirdchoo, W.-S. Soh, and K. C. Chua, “ALOHA-based MAC protocols with collision avoidance for underwater acoustic networks,” in IEEE INFOCOM 2007-26th IEEE International Conference on Com-puter Communications . IEEE, 2007, pp. 2271–2275. [13] J. Ahn, A. Syed, B. Krishnamachari, and J. Heidemann, “Design and analysis of a propagation delay tolerant ALOHA protocol for underwater networks,” Ad Hoc Networks , vol. 9, no. 5, pp. 752–766, 2011. [14] P. Mandal, S. De, and S. S. Chakraborty, “A receiver synchronized slotted ALOHA for underwater wireless networks with imprecise prop-agation delay information,” Ad Hoc Networks , vol. 11, no. 4, pp. 1443– 1455, 2013. [15] S. Zhou and Z. Wang, OFDM for underwater acoustic communications .John Wiley & Sons, 2014. [16] T. Chen, J. Chan, and S. Gollakota, “Underwater messaging using mobile devices,” in ACM SIGCOMM , 2022, pp. 545–559. [17] X. Ouyang and J. Zhao, “Orthogonal chirp division multiplexing,” IEEE Transactions on Communications , vol. 64, no. 9, pp. 3946–3957, 2016. [18] X. Feng, J. Wang, M. Zhou, H. Sun, and Y. Gu, “Underwater acoustic communications based on OTFS,” in 2020 15th IEEE International Conference on Signal Processing (ICSP) , 2021. [19] T. Yang, “Spatially multiplexed CDMA multiuser underwater acoustic communications,” IEEE Journal of Oceanic Engineering , vol. 41, no. 1, pp. 217–231, 2015. [20] Z. Zhang, R. Rathi, S. Perez, J. Bukhari, and Y. Zhong, “ZCNET: Achieving high capacity in low power wide area networks,” IEEE/ACM Transactions on Networking , vol. 30, no. 5, pp. 2032–2045, 2022. [21] L. Vangelista, B. Jechoux, J.-X. Canonici, and M. Zorzi, “Golden modulation: A new and effective waveform for massive IoT,” IEEE Transactions on Communications , vol. 72, no. 4, pp. 1938–1948, 2023. [22] E. Zhang, L. Liang, L. You, Z. Wang, D. Wang, and L. Fu, “High-rate uncoordinated concurrent random access in underwater acoustic networks,” IEEE Transactions on Mobile Computing , 2025. [23] D. Tse and P. Viswanath, Fundamentals of wireless communication .Cambridge University Press, 2005. [24] P. Bello, “Characterization of randomly time-variant linear channels,” 

IEEE transactions on Communications Systems , vol. 11, no. 4, pp. 360– 393, 1963. [25] E. Dahlman, S. Parkvall, and J. Skold, 4G: LTE/LTE-advanced for mobile broadband . Academic Press, 2013. [26] ——, 5G NR: The next generation wireless access technology . Aca-demic Press, 2020. [27] Y. Li, Y. Wang, and X. Guan, “Joint synchronization and doppler scale estimation using Zadoff–Chu sequences for underwater acoustic communications,” in OCEANS 2017-Anchorage . IEEE, 2017, pp. 1–5. [28] Q. Tan, Y. Wang, and X. Ma, “Preamble detection based on cyclic fea-tures of zadoff–chu sequences for underwater acoustic communications,” 

IEEE Signal Processing Letters , vol. 26, no. 8, pp. 1192–1196, 2019. [29] X. Wang, L. Kong, L. He, and G. Chen, “mLoRa: A multi-packet reception protocol in LoRa networks,” in IEEE ICNP , 2019, pp. 1–11. [30] X. Xia, Y. Zheng, and T. Gu, “FTrack: Parallel decoding for LoRa transmissions,” in ACM SenSys , 2019, pp. 192–204. [31] Z. Wang, L. Kong, K. Xu, L. He, K. Wu, and G. Chen, “Online concurrent transmissions at LoRa gateway,” in IEEE INFOCOM , 2020, pp. 2331–2340. [32] S. Tong, Z. Xu, and J. Wang, “CoLoRa: Enabling multi-packet reception in LoRa,” in IEEE INFOCOM , 2020, pp. 2303–2311. [33] S. Tong, J. Wang, and Y. Liu, “Combating packet collisions using non-stationary signal scaling in LPWANs,” in ACM MobiSys , 2020, pp. 234– 246. [34] Z. Xu, S. Tong, P. Xie, and J. Wang, “FlipLora: Resolving collisions with up-down quasi-orthogonality,” in IEEE SECON , 2020, pp. 1–9. [35] B. Hu, Z. Yin, S. Wang, Z. Xu, and T. He, “SCLoRa: Leveraging multi-dimensionality in decoding collided LoRa transmissions,” in IEEE ICNP , 2020, pp. 1–11. [36] Z. Xu, P. Xie, and J. Wang, “Pyramid: Real-time LoRa collision decoding with peak tracking,” in IEEE INFOCOM , 2021, pp. 1–9. [37] X. Xia, N. Hou, Y. Zheng, and T. Gu, “PCube: Scaling LoRa concurrent transmissions with reception diversities,” in ACM MobiCom , 2021, pp. 1–25. [38] M. O. Shahid, M. Philipose, K. Chintalapudi, S. Banerjee, and B. Krish-naswamy, “Concurrent interference cancellation: Decoding multi-packet collisions in LoRa,” in ACM SIGCOMM , 2021, pp. 503–515. [39] Q. Chen and J. Wang, “AlignTrack: Push the limit of LoRa collision decoding,” in IEEE ICNP , 2021, pp. 1–11. [40] C. Li, X. Guo, L. Shangguan, Z. Cao, and K. Jamieson, “CurvingLoRa to boost LoRa network throughput via concurrent transmission,” in 

USENIX NSDI , 2022, pp. 879–895. [41] R. Rathi and Z. Zhang, “Tnb: resolving collisions in LoRa based on the peak matching cost and block error correction,” in ACM CoNEXT ,2022, pp. 401–416. [42] Y. Wang, F. Zhang, X. Zheng, L. Liu, and H. Ma, “Decoding LoRa col-lisions via parallel alignment,” ACM Transactions on Sensor Networks ,pp. 1–25, 2022. [43] D. Halperin, T. Anderson, and D. Wetherall, “Taking the sting out of carrier sense: interference cancellation for wireless lans,” in ACM MobiCom , 2008, pp. 339–350. [44] R. Otnes, P. A. van Walree, and T. Jenserud, “Validation of replay-based underwater acoustic communication channel simulation,” IEEE Journal of Oceanic Engineering , vol. 38, no. 4, pp. 689–700, 2013. [45] M. B. Porter, “The bellhop manual and user’s guide: Preliminary draft,” 

Heat, Light, and Sound Research, Inc., La Jolla, CA, USA, Tech. Rep ,vol. 260, p. 70, 2011. [46] K. Pelekanakis and L. Cazzanti, “On adaptive modulation for low SNR underwater acoustic communications,” in IEEE Oceans , 2018, pp. 1–6. [47] F. Steinmetz and B.-C. Renner, “Taking LoRa for a dive: CSS for low-power acoustic underwater communication,” in IEEE UComms , 2022, pp. 1–5. [48] Y. Xie, X. Hu, J. Xiao, D. Wang, and W. Lei, “Implementation of timing synchronization for OFDM underwater communication system on FPGA,” in 2009 3rd International Conference on Anti-counterfeiting, Security, and Identification in Communication . IEEE, 2009, pp. 568– 570. [49] P. A. van Walree, F.-X. Socheleau, R. Otnes, and T. Jenserud, “The watermark benchmark for underwater acoustic modulation schemes,” 

IEEE Journal of Oceanic Engineering , vol. 42, no. 4, pp. 1007–1018, 2017. [50] D. Wax, “MFSK–the basis for robust acoustical communications,” in 

IEEE OCEANS , 1981, pp. 61–66. [51] M. Stojanovic, J. A. Catipovic, and J. G. Proakis, “Phase-coherent digital communications for underwater acoustic channels,” IEEE Journal of Oceanic Engineering , vol. 19, no. 1, pp. 100–111, 1994. [52] W. Lei, D. Wang, Y. Xie, B. Chen, X. Hu, and H. Chen, “Implementation of a high reliable chirp underwater acoustic modem,” in IEEE Oceans ,2012, pp. 1–5. [53] Z. Jia, W. Zheng, and F. Yuan, “A two-dimensional chirp-MFCSK modulation method for underwater LoRa system,” IEEE Internet of Things Journal , vol. 9, no. 23, pp. 24 388–24 397, 2022. [54] A. Petroni, G. Scarano, R. Cusani, and M. Biagit, “Feasibility and performance analysis of underwater acoustic LoRa modulation,” in IEEE ICC Workshops , 2023, pp. 709–714. [55] B. Li, S. Zhou, M. Stojanovic, L. Freitag, and P. Willett, “Multicarrier communication over underwater acoustic channels with nonuniform doppler shifts,” IEEE Journal of Oceanic Engineering , vol. 33, no. 2, pp. 198–209, 2008. [56] S. K. Mohammed, R. Hadani, A. Chockalingam, and R. Calderbank, “OTFS—a mathematical foundation for communication and radar sens-ing in the delay-doppler domain,” IEEE BITS Inf. Theory , vol. 2, no. 2, pp. 36–55, 2022. [57] Q. Yang and Y. Zheng, “AquaHelper: Underwater SOS transmission and detection in swimming pools,” in ACM SenSys , 2023, pp. 294–307. [58] S. Mani, T. M. Duman, and P. Hursky, “Adaptive coding-modulation for shallow-water UWA communications,” Journal of the Acoustical Society of America , vol. 123, no. 5, p. 3749, 2008. [59] L. Wan, H. Zhou, X. Xu, Y. Huang, S. Zhou, Z. Shi, and J.-H. Cui, “Adaptive modulation and coding for underwater acoustic OFDM,” IEEE Journal of Oceanic Engineering , vol. 40, no. 2, pp. 327–336, 2014. [60] A. Radosevic, R. Ahmed, T. M. Duman, J. G. Proakis, and M. Sto-janovic, “Adaptive OFDM modulation for underwater acoustic commu-nications: Design considerations and experimental results,” IEEE journal of oceanic engineering , vol. 39, no. 2, pp. 357–370, 2013. [61] J. Huang and R. Diamant, “Adaptive modulation for long-range under-water acoustic communication,” IEEE Transactions on Wireless Com-munications , vol. 19, no. 10, pp. 6844–6857, 2020. [62] W. A. Gardner, Cyclostationarity in Communications and Signal Pro-cessing . New York: IEEE Press, 1994. [63] S. Dubnov, “Generalization of spectral flatness measure for non-gaussian linear processes,” IEEE Signal Processing Letters , vol. 11, no. 8, pp. 698–701, 2004. [64] G. Peeters, “A large set of audio features for sound description (similarity and classification) in the cuidado project,” IRCAM, Tech. Rep., 2004. [Online]. Available: https://recherche.ircam.fr/equipes/pcm/ peeters/Peeters04 ISMIR.pdf