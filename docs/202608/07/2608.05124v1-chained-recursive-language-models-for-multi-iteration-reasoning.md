---
title: Chained Recursive Language Models for Multi-Iteration Reasoning
source: arxiv
title_zh: 用于Multi-Iteration Reasoning的Chained Recursive Language 模型s
abstract_zh: "这篇论文研究 when fresh-context artifact continuation gives measurable gain in accuracy over direct LLM an。\n\n核心做法：In this work，作者提出 Chained Recursive Language 模型s (Chained RLM)，inference-time 架构，in which same under。\n\n主要结果：Long context reasoning in large language 模型s (LLMs) is usually constrained by fact that single infer。\n\n值得关注的点在于：Each root receives original problem 与 context，but does not inherit full conversational history。"
abstract_en: "Long context reasoning in large language models (LLMs) is usually constrained by the fact that a single inference trajectory has to simultaneously explore the context, store intermediate state, verify evidence, and produce the final answer. This becomes particularly difficult in tasks that require extraction, counting, ordering, or multi-hop reasoning, where an early mistake can propagate until the final response. In this work, we propose Chained Recursive Language Models (Chained RLM), an inference-time architecture, in which the same underlying model is called repeatedly as a sequence of fresh reasoning roots. Each root receives the original problem and context, but does not inherit the full conversational history. Instead, it receives a compact plain-text summary, a plain-text blackboard, and some durable task-specific artifacts written by predecessor roots. The motivation is to manage the context by chopping into partial tasks rather than one large inference response; in each staged computation, intermediate artifacts can be inspected, corrected, and extended by a later fresh inference by the same model. We describe the system model, handoff mechanism, artifact workspace, and evaluation protocol for this system. We study when fresh-context artifact continuation gives a measurable gain in accuracy over direct LLM answering even with recursive tool-calling."
authors: "Purbesh Mitra, Sennur Ulukus"
date: 2026-08-05
journal: cs.CL
source_link: "https://arxiv.org/pdf/2608.05124v1"
pdf: "https://arxiv.org/pdf/2608.05124v1"
score: 8.4
tldr: 围绕《用于Multi-Iteration Reason...》；In this work，作者提出 Chained Recurs...；Long context reasoning in large lang...
tldr_cn: 围绕《用于Multi-Iteration Reason...》；In this work，作者提出 Chained Recurs...；Long context reasoning in large lang...
tldr_en: "Long context reasoning in large language models (LLMs) is usually constrained by the fact that a single inference trajectory has to simultaneously explore the context, store intermediate state, verify evidence, and produce the final answer. This becomes particularly difficult in tasks that require extraction, counting, ordering, or multi-hop reasoning, where an early mistake can propagate..."
selection_source: fresh_fetch
vocabulary: ["inference | 推理 | CET6"]
motivation: 这篇论文研究 when fresh-context artifact continuation gives measurable gain in accuracy over direct LLM an。
method: 核心做法：In this work，作者提出 Chained Recursive Language 模型s (Chained RLM)，inference-time 架构，in which same under。
result: 主要结果：Long context reasoning in large language 模型s (LLMs) is usually constrained by fact that single infer。
conclusion: 值得关注的点在于：Each root receives original problem 与 context，but does not inherit full conversational history。
---

## 摘要
这篇论文研究 when fresh-context artifact continuation gives measurable gain in accuracy over direct LLM an。

核心做法：In this work，作者提出 Chained Recursive Language 模型s (Chained RLM)，inference-time 架构，in which same under。

主要结果：Long context reasoning in large language 模型s (LLMs) is usually constrained by fact that single infer。

值得关注的点在于：Each root receives original problem 与 context，but does not inherit full conversational history。

## Abstract
Long context reasoning in large language models (LLMs) is usually constrained by the fact that a single inference trajectory has to simultaneously explore the context, store intermediate state, verify evidence, and produce the final answer. This becomes particularly difficult in tasks that require extraction, counting, ordering, or multi-hop reasoning, where an early mistake can propagate until the final response. In this work, we propose Chained Recursive Language Models (Chained RLM), an inference-time architecture, in which the same underlying model is called repeatedly as a sequence of fresh reasoning roots. Each root receives the original problem and context, but does not inherit the full conversational history. Instead, it receives a compact plain-text summary, a plain-text blackboard, and some durable task-specific artifacts written by predecessor roots. The motivation is to manage the context by chopping into partial tasks rather than one large inference response; in each staged computation, intermediate artifacts can be inspected, corrected, and extended by a later fresh inference by the same model. We describe the system model, handoff mechanism, artifact workspace, and evaluation protocol for this system. We study when fresh-context artifact continuation gives a measurable gain in accuracy over direct LLM answering even with recursive tool-calling.

## 英语词汇
- **inference**：推理（CET6）
