---
title: "GSToken: Geometry-Structured Gaussian Tokens for Compact 3D Medical Image Representation"
source: arxiv
title_zh: 用于Compact 3D Medical Image Representation的GSToken：Geometry-Structured Gaussian Tokens
abstract_zh: "Effective segmentation 的 multi-modal MRI is central to improving neural 网络 accuracy in brain tumor r。\n\n核心做法：In this paper，作者引入 Gaussian tokens to multi-modal brain tumor segmentation 用于 first time：each token。\n\n主要结果：Multi-seed paired statistical testing shows that GSToken consistently 与 substantially 优于 capacity-ma。\n\n值得关注的点在于：These results 表明 that explicitly encoding spatial geometry within tokens significantly improves info。"
abstract_en: "Effective segmentation of multi-modal MRI is central to improving neural network accuracy in brain tumor recognition. Existing methods typically compress 3D volumes into token sequences via fixed patch encoding or learned attention pooling (e.g., TokenLearner). However, these compression schemes discard explicit spatial shape information; the resulting tokens convey no notion of lesion morphology or spatial extent. Meanwhile, end-to-end evaluation entangles a tokenizer's information retention with the reconstruction capacity of the downstream decoder, and the lack of a unified capacity contract across methods makes performance differences difficult to attribute. In this paper, we introduce Gaussian tokens to multi-modal brain tumor segmentation for the first time: each token carries not only a semantic feature but also a learned 3D center, anisotropic scale, and orientation, endowing the representation with explicit geometric support at negligible parameter cost. We further propose a frozen-token utility evaluation protocol: the trained tokenizer is frozen, its output is cast into a fixed-capacity serialized contract, and a shared lightweight Transformer probe independently measures each tokenizer's retained information under strictly matched conditions. Multi-seed paired statistical testing shows that GSToken consistently and substantially outperforms capacity-matched adaptive baselines under frozen probing, with uniform advantages across all tumor sub-regions, surface, and distance metrics. These results demonstrate that explicitly encoding spatial geometry within tokens significantly improves the information density of volumetric representations, offering a new design principle for compact 3D medical image representation and downstream reading."
authors: "Xiaoduo Li, Quan Gu"
date: 2026-08-18
journal: cs.CV
source_link: "https://arxiv.org/pdf/2608.17425v1"
pdf: "https://arxiv.org/pdf/2608.17425v1"
score: 6.6499999999999995
tldr: 围绕《用于Compact 3D Medical Ima...》；In this paper，作者引入 Gaussian toke...；Multi-seed paired statistical testin...
tldr_cn: 围绕《用于Compact 3D Medical Ima...》；In this paper，作者引入 Gaussian toke...；Multi-seed paired statistical testin...
tldr_en: "Effective segmentation of multi-modal MRI is central to improving neural network accuracy in brain tumor recognition. Existing methods typically compress 3D volumes into token sequences via fixed patch encoding or learned attention pooling (e.g., TokenLearner)."
selection_source: fresh_fetch
vocabulary: ["reconstruction | 重建 | CET6", "segmentation | 分割 | CET6/IELTS", "transformer | Transformer 架构 | 学术高频", "downstream | 下游任务 | 学术高频", "volumetric | 体数据的 / 三维体积的 | 学术高频", "lesion | 病损 | IELTS"]
motivation: Effective segmentation 的 multi-modal MRI is central to improving neural 网络 accuracy in brain tumor r。
method: 核心做法：In this paper，作者引入 Gaussian tokens to multi-modal brain tumor segmentation 用于 first time：each token。
result: 主要结果：Multi-seed paired statistical testing shows that GSToken consistently 与 substantially 优于 capacity-ma。
conclusion: 值得关注的点在于：These results 表明 that explicitly encoding spatial geometry within tokens significantly improves info。
---

## 摘要
Effective segmentation 的 multi-modal MRI is central to improving neural 网络 accuracy in brain tumor r。

核心做法：In this paper，作者引入 Gaussian tokens to multi-modal brain tumor segmentation 用于 first time：each token。

主要结果：Multi-seed paired statistical testing shows that GSToken consistently 与 substantially 优于 capacity-ma。

值得关注的点在于：These results 表明 that explicitly encoding spatial geometry within tokens significantly improves info。

## Abstract
Effective segmentation of multi-modal MRI is central to improving neural network accuracy in brain tumor recognition. Existing methods typically compress 3D volumes into token sequences via fixed patch encoding or learned attention pooling (e.g., TokenLearner). However, these compression schemes discard explicit spatial shape information; the resulting tokens convey no notion of lesion morphology or spatial extent. Meanwhile, end-to-end evaluation entangles a tokenizer's information retention with the reconstruction capacity of the downstream decoder, and the lack of a unified capacity contract across methods makes performance differences difficult to attribute. In this paper, we introduce Gaussian tokens to multi-modal brain tumor segmentation for the first time: each token carries not only a semantic feature but also a learned 3D center, anisotropic scale, and orientation, endowing the representation with explicit geometric support at negligible parameter cost. We further propose a frozen-token utility evaluation protocol: the trained tokenizer is frozen, its output is cast into a fixed-capacity serialized contract, and a shared lightweight Transformer probe independently measures each tokenizer's retained information under strictly matched conditions. Multi-seed paired statistical testing shows that GSToken consistently and substantially outperforms capacity-matched adaptive baselines under frozen probing, with uniform advantages across all tumor sub-regions, surface, and distance metrics. These results demonstrate that explicitly encoding spatial geometry within tokens significantly improves the information density of volumetric representations, offering a new design principle for compact 3D medical image representation and downstream reading.

## 英语词汇
- **reconstruction**：重建（CET6）
- **segmentation**：分割（CET6/IELTS）
- **transformer**：Transformer 架构（学术高频）
- **downstream**：下游任务（学术高频）
- **volumetric**：体数据的 / 三维体积的（学术高频）
- **lesion**：病损（IELTS）
