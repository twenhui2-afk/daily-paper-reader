---
title: Learning Dense 2D-3D Correspondence for X-ray-to-CT Registration of Knee Bones
source: arxiv
title_zh: 用于Knee Bones的X-ray-to-CT Registration的Learning Dense 2D-3D Correspondence
abstract_zh: "Recovering 6-DoF pose 的 knee bones from plain radiograph，given patient's segmented pre-operative CT。\n\n核心做法：Classic solutions align rendered bone silhouette to image edges；recent alternatives refine pose by b。\n\n主要结果：Both operate one patient at time 与 are fragile under single view。\n\n值得关注的点在于：On large single-institution cohort 模型 generalizes well to held-out patients。"
abstract_en: "Recovering the 6-DoF pose of the knee bones from a plain radiograph, given the patient's segmented pre-operative CT, turns a routine low-dose image into a quantitative measurement of joint geometry, without the added dose of a repeat CT or a fixed biplanar rig. Classic solutions align a rendered bone silhouette to image edges; recent alternatives refine pose by backpropagating an image-similarity loss through a differentiable X-ray renderer. Both operate one patient at a time and are fragile under a single view. Silhouettes are depth-ambiguous, and differentiable-rendering refinement has a narrow capture range at substantial per-iteration cost. We instead learn an amortized, subject-agnostic dense 2D-3D correspondence, supervised solely by projection geometry. One shared-weight model per bone, trained across 758 patients, registers patients unseen during training. The pose then follows in closed form from a global, initialization-free, render-free PnP+RANSAC solve. Because X-ray formation is transmissive, our correspondence target is transmission-aware rather than tied to a single surface. Though trained only to register, the representation is anatomically semantic: a simple classifier reads a landmark's anatomical region from its embedding across held-out patients, and the same features separate the knee's bones into a 2D-3D-consistent identity learned without any bone label. On a large single-institution cohort the model generalizes well to held-out patients."
authors: "Rembert Daems, Jonas Grammens, Caro Roten, Andrew Meyer, Thomas Luyckx, Matthias Verstraete"
date: 2026-07-24
journal: eess.IV
source_link: "https://arxiv.org/pdf/2607.22803v1"
pdf: "https://arxiv.org/pdf/2607.22803v1"
score: 6.0
tldr: 围绕《用于Knee Bones的X-ray-to-CT...》；Classic solutions align rendered...；Both operate one patient at time 与 a...
tldr_cn: 围绕《用于Knee Bones的X-ray-to-CT...》；Classic solutions align rendered...；Both operate one patient at time 与 a...
tldr_en: "Recovering the 6-DoF pose of the knee bones from a plain radiograph, given the patient's segmented pre-operative CT, turns a routine low-dose image into a quantitative measurement of joint geometry, without the added dose of a repeat CT or a fixed biplanar rig. Classic solutions align a rendered bone silhouette to image edges; recent alternatives..."
selection_source: fresh_fetch
vocabulary: ["radiograph | X 光影像 | IELTS", "embedding | 嵌入表示 | 学术高频"]
motivation: "Recovering 6-DoF pose 的 knee bones from plain radiograph，given patient's segmented pre-operative CT。"
method: 核心做法：Classic solutions align rendered bone silhouette to image edges；recent alternatives refine pose by b。
result: 主要结果：Both operate one patient at time 与 are fragile under single view。
conclusion: 值得关注的点在于：On large single-institution cohort 模型 generalizes well to held-out patients。
---

## 摘要
Recovering 6-DoF pose 的 knee bones from plain radiograph，given patient's segmented pre-operative CT。

核心做法：Classic solutions align rendered bone silhouette to image edges；recent alternatives refine pose by b。

主要结果：Both operate one patient at time 与 are fragile under single view。

值得关注的点在于：On large single-institution cohort 模型 generalizes well to held-out patients。

## Abstract
Recovering the 6-DoF pose of the knee bones from a plain radiograph, given the patient's segmented pre-operative CT, turns a routine low-dose image into a quantitative measurement of joint geometry, without the added dose of a repeat CT or a fixed biplanar rig. Classic solutions align a rendered bone silhouette to image edges; recent alternatives refine pose by backpropagating an image-similarity loss through a differentiable X-ray renderer. Both operate one patient at a time and are fragile under a single view. Silhouettes are depth-ambiguous, and differentiable-rendering refinement has a narrow capture range at substantial per-iteration cost. We instead learn an amortized, subject-agnostic dense 2D-3D correspondence, supervised solely by projection geometry. One shared-weight model per bone, trained across 758 patients, registers patients unseen during training. The pose then follows in closed form from a global, initialization-free, render-free PnP+RANSAC solve. Because X-ray formation is transmissive, our correspondence target is transmission-aware rather than tied to a single surface. Though trained only to register, the representation is anatomically semantic: a simple classifier reads a landmark's anatomical region from its embedding across held-out patients, and the same features separate the knee's bones into a 2D-3D-consistent identity learned without any bone label. On a large single-institution cohort the model generalizes well to held-out patients.

## 英语词汇
- **radiograph**：X 光影像（IELTS）
- **embedding**：嵌入表示（学术高频）
