---
title: "VIDS-Seg: Towards Reliable Uncertainty Quantification in Pediatric Cardiac Ultrasound Segmentation"
source: arxiv
title_zh: VIDS-Seg：Towards Reliable Uncertainty Quantification in Pediatric Cardiac Ultrasound Segmentation
abstract_zh: "Reliable clinical deployment 的 machine learning requires 模型s that know when they are likely to fail。\n\n核心做法：Building on VIDS (Variational Inference under Distribution Shifts) 框架，作者引入 VIDS-Seg，which applies am。\n\n主要结果：Downstream，it yields more accurate 与 stable ejection fraction estimates 与 more reliable detection 的。\n\n值得关注的点在于：Our results indicate that OOD-aware uncertainty quantification can serve as practical safety layer 用。"
abstract_en: "Reliable clinical deployment of machine learning requires models that know when they are likely to fail, particularly for subgroups underrepresented in training data. A common case is pediatric care, where models trained on adult cohorts can silently under-perform on children with no indication that something has gone wrong. As retraining with labeled pediatric data is often infeasible, detecting such failures at inference time is a critical clinical need. Building on the VIDS (Variational Inference under Distribution Shifts) framework, we introduce VIDS-Seg, which applies amortized variational inference over a lightweight prediction head to make this adaptive, OOD-aware prior tractable for dense image segmentation. We evaluate VIDS-Seg on left ventricular segmentation in echocardiography, a setting where pediatric anatomy differs systematically from the adult population most segmentation models are trained on, training on an adult cohort (EchoNet-Dynamic) and evaluating zero-shot on a pediatric cohort (EchoNet-Pediatric). Across all age strata, VIDS-Seg matches competitive baselines in segmentation accuracy while producing substantially higher spatial correspondence between predicted uncertainty and segmentation error, an advantage that persists even after applying temperature scaling to all baselines. Downstream, it yields more accurate and stable ejection fraction estimates and more reliable detection of cardiac malfunction in the infant subgroup. Our results indicate that OOD-aware uncertainty quantification can serve as a practical safety layer for deployed segmentation models, enabling detection of silent failures in underrepresented subgroups without retraining or additional labeled data."
authors: "Paul Fischer, Ece Ozkan"
date: 2026-08-11
journal: cs.CV
source_link: "https://arxiv.org/pdf/2608.10903v1"
pdf: "https://arxiv.org/pdf/2608.10903v1"
score: 6.299999999999999
tldr: 围绕《VIDS-Seg：Towards Reliabl...》；Building on VIDS (Variational In...；Downstream，it yields more accurate 与...
tldr_cn: 围绕《VIDS-Seg：Towards Reliabl...》；Building on VIDS (Variational In...；Downstream，it yields more accurate 与...
tldr_en: "Reliable clinical deployment of machine learning requires models that know when they are likely to fail, particularly for subgroups underrepresented in training data. A common case is pediatric care, where models trained on adult cohorts can silently under-perform on children with no indication that something has gone wrong."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "uncertainty | 不确定性 | IELTS", "downstream | 下游任务 | 学术高频", "detection | 检测 | CET6", "inference | 推理 | CET6", "clinical | 临床的 | CET6"]
motivation: Reliable clinical deployment 的 machine learning requires 模型s that know when they are likely to fail。
method: 核心做法：Building on VIDS (Variational Inference under Distribution Shifts) 框架，作者引入 VIDS-Seg，which applies am。
result: 主要结果：Downstream，it yields more accurate 与 stable ejection fraction estimates 与 more reliable detection 的。
conclusion: 值得关注的点在于：Our results indicate that OOD-aware uncertainty quantification can serve as practical safety layer 用。
---

## 摘要
Reliable clinical deployment 的 machine learning requires 模型s that know when they are likely to fail。

核心做法：Building on VIDS (Variational Inference under Distribution Shifts) 框架，作者引入 VIDS-Seg，which applies am。

主要结果：Downstream，it yields more accurate 与 stable ejection fraction estimates 与 more reliable detection 的。

值得关注的点在于：Our results indicate that OOD-aware uncertainty quantification can serve as practical safety layer 用。

## Abstract
Reliable clinical deployment of machine learning requires models that know when they are likely to fail, particularly for subgroups underrepresented in training data. A common case is pediatric care, where models trained on adult cohorts can silently under-perform on children with no indication that something has gone wrong. As retraining with labeled pediatric data is often infeasible, detecting such failures at inference time is a critical clinical need. Building on the VIDS (Variational Inference under Distribution Shifts) framework, we introduce VIDS-Seg, which applies amortized variational inference over a lightweight prediction head to make this adaptive, OOD-aware prior tractable for dense image segmentation. We evaluate VIDS-Seg on left ventricular segmentation in echocardiography, a setting where pediatric anatomy differs systematically from the adult population most segmentation models are trained on, training on an adult cohort (EchoNet-Dynamic) and evaluating zero-shot on a pediatric cohort (EchoNet-Pediatric). Across all age strata, VIDS-Seg matches competitive baselines in segmentation accuracy while producing substantially higher spatial correspondence between predicted uncertainty and segmentation error, an advantage that persists even after applying temperature scaling to all baselines. Downstream, it yields more accurate and stable ejection fraction estimates and more reliable detection of cardiac malfunction in the infant subgroup. Our results indicate that OOD-aware uncertainty quantification can serve as a practical safety layer for deployed segmentation models, enabling detection of silent failures in underrepresented subgroups without retraining or additional labeled data.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **uncertainty**：不确定性（IELTS）
- **downstream**：下游任务（学术高频）
- **detection**：检测（CET6）
- **inference**：推理（CET6）
- **clinical**：临床的（CET6）
