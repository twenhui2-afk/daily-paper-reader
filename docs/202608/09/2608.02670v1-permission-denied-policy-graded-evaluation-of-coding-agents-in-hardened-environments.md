---
title: "Permission Denied: Policy-Graded Evaluation of Coding Agents in Hardened Environments"
source: arxiv
title_zh: Coding Agents in Hardened Environments的Permission Denied：Policy-Graded Evaluation
abstract_zh: "Coding agents increasingly run inside organizations whose security controls (scoped credentials，rest。\n\n核心做法：Existing benchmarks，however，evaluate agents almost exclusively in permissive sandboxes，so it is unkn。\n\n主要结果：In this work，we evaluate 12 coding agents on Terminal-Bench 2.1 across nested security policy levels。\n\n值得关注的点在于：We release Boundary-Bench，open-source hardening plugin enabling policy-constrained evaluation 的 codi。"
abstract_en: "Coding agents increasingly run inside organizations whose security controls (scoped credentials, restricted egress, read-only filesystems, non-root execution) constrain them like any other software. Existing benchmarks, however, evaluate agents almost exclusively in permissive sandboxes, so it is unknown how performance changes when policy is enforced. In this work, we evaluate 12 coding agents on Terminal-Bench 2.1 across nested security policy levels derived from common real-world enterprise restrictions. Hardening is never free but far from uniform: under the strictest policy, success losses reach 18.3 points and cost inflation 167.3\\%, and the two axes disagree; the model that best preserves success is also the one that loses the most efficiency, so model choice is policy-dependent. Beyond aggregate scores, we characterize how agents behave when policy blocks their actions and decompose the failures hardening induces: runs grind into timeouts or wrong solutions rather than stopping early, in a mix that differs by model. To ground comparisons, we verify task solvability under the strictest policy, separating model failures from tasks the policy forecloses. We release Boundary-Bench, an open-source hardening plugin enabling policy-constrained evaluation of coding agents on Terminal-Bench and compatible benchmarks."
authors: "Dotan Davidovich, Yair Amar, Hai Rozencwajg, Or Hiltch"
date: 2026-08-02
journal: cs.CR
source_link: "https://arxiv.org/pdf/2608.02670v1"
pdf: "https://arxiv.org/pdf/2608.02670v1"
score: 6.8999999999999995
tldr: 围绕《Coding Agents in Hardene...》；Existing benchmarks，however，eval...；In this work，we evaluate 12 coding a...
tldr_cn: 围绕《Coding Agents in Hardene...》；Existing benchmarks，however，eval...；In this work，we evaluate 12 coding a...
tldr_en: "Coding agents increasingly run inside organizations whose security controls (scoped credentials, restricted egress, read-only filesystems, non-root execution) constrain them like any other software. Existing benchmarks, however, evaluate agents almost exclusively in permissive sandboxes, so it is unknown how performance changes when policy is enforced."
selection_source: fresh_fetch
motivation: Coding agents increasingly run inside organizations whose security controls (scoped credentials，rest。
method: 核心做法：Existing benchmarks，however，evaluate agents almost exclusively in permissive sandboxes，so it is unkn。
result: 主要结果：In this work，we evaluate 12 coding agents on Terminal-Bench 2.1 across nested security policy levels。
conclusion: 值得关注的点在于：We release Boundary-Bench，open-source hardening plugin enabling policy-constrained evaluation 的 codi。
---

## 摘要
Coding agents increasingly run inside organizations whose security controls (scoped credentials，rest。

核心做法：Existing benchmarks，however，evaluate agents almost exclusively in permissive sandboxes，so it is unkn。

主要结果：In this work，we evaluate 12 coding agents on Terminal-Bench 2.1 across nested security policy levels。

值得关注的点在于：We release Boundary-Bench，open-source hardening plugin enabling policy-constrained evaluation 的 codi。

## Abstract
Coding agents increasingly run inside organizations whose security controls (scoped credentials, restricted egress, read-only filesystems, non-root execution) constrain them like any other software. Existing benchmarks, however, evaluate agents almost exclusively in permissive sandboxes, so it is unknown how performance changes when policy is enforced. In this work, we evaluate 12 coding agents on Terminal-Bench 2.1 across nested security policy levels derived from common real-world enterprise restrictions. Hardening is never free but far from uniform: under the strictest policy, success losses reach 18.3 points and cost inflation 167.3\%, and the two axes disagree; the model that best preserves success is also the one that loses the most efficiency, so model choice is policy-dependent. Beyond aggregate scores, we characterize how agents behave when policy blocks their actions and decompose the failures hardening induces: runs grind into timeouts or wrong solutions rather than stopping early, in a mix that differs by model. To ground comparisons, we verify task solvability under the strictest policy, separating model failures from tasks the policy forecloses. We release Boundary-Bench, an open-source hardening plugin enabling policy-constrained evaluation of coding agents on Terminal-Bench and compatible benchmarks.
