---
title: "Context Matters: Support Set Selection and Failure Detection for In-Context Medical Image Segmentation"
source: arxiv
title_zh: 用于In-Context Medical Image Segmentation的Context Matters：Support Set Selection与Failure Detection
abstract_zh: "In-context learning (ICL) adapts medical image segmentation 模型s to unseen structures 与 modalities wi。\n\n核心做法：Because this support set is 模型's only task-specific signal，its composition directly influences segme。\n\n主要结果：使用 MultiverSeg 结合 DINOv3 embeddings across four benchmarks 与 three imaging modalities，we show that s。\n\n值得关注的点在于：In this work，we investigate support set as controllable determinant 的 ICL reliability。"
abstract_en: "In-context learning (ICL) adapts medical image segmentation models to unseen structures and modalities without retraining by conditioning on a task-specific support set of image-mask exemplars. Because this support set is the model's only task-specific signal, its composition directly influences segmentation performance. In this work, we investigate the support set as a controllable determinant of ICL reliability. First, we compare random sampling against similarity-based selection, where exemplars are retrieved based on their visual similarity to the query image. Second, we train a transformer-based classifier to predict, from the query and support images alone, whether a segmentation will fall below a specified Intersection-over-Union (IoU) threshold. Using MultiverSeg with DINOv3 embeddings across four benchmarks and three imaging modalities, we show that similarity-based selection consistently matches or outperforms random sampling, with the largest gains at the smallest support set sizes. Furthermore, our classifier predicts segmentation failure above chance on all four benchmarks. Ultimately, these results demonstrate that the reliability of in-context segmentation can be both improved via informed support selection and anticipated before use, providing practical mechanisms for safer clinical deployment."
authors: "Youssef Gehad, Emmanuel Zerefa, Krish Kabra, Guha Balakrishnan"
date: 2026-08-05
journal: cs.CV
source_link: "https://arxiv.org/pdf/2608.05333v1"
pdf: "https://arxiv.org/pdf/2608.05333v1"
score: 6.6
tldr: "围绕《用于In-Context Medical Ima...》；Because this support set is 模型's...；使用 MultiverSeg 结合 DINOv3 embeddings..."
tldr_cn: "围绕《用于In-Context Medical Ima...》；Because this support set is 模型's...；使用 MultiverSeg 结合 DINOv3 embeddings..."
tldr_en: "In-context learning (ICL) adapts medical image segmentation models to unseen structures and modalities without retraining by conditioning on a task-specific support set of image-mask exemplars. Because this support set is the model's only task-specific signal, its composition directly influences segmentation performance."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "transformer | Transformer 架构 | 学术高频", "detection | 检测 | CET6", "clinical | 临床的 | CET6"]
motivation: In-context learning (ICL) adapts medical image segmentation 模型s to unseen structures 与 modalities wi。
method: "核心做法：Because this support set is 模型's only task-specific signal，its composition directly influences segme。"
result: 主要结果：使用 MultiverSeg 结合 DINOv3 embeddings across four benchmarks 与 three imaging modalities，we show that s。
conclusion: 值得关注的点在于：In this work，we investigate support set as controllable determinant 的 ICL reliability。
---

## 摘要
In-context learning (ICL) adapts medical image segmentation 模型s to unseen structures 与 modalities wi。

核心做法：Because this support set is 模型's only task-specific signal，its composition directly influences segme。

主要结果：使用 MultiverSeg 结合 DINOv3 embeddings across four benchmarks 与 three imaging modalities，we show that s。

值得关注的点在于：In this work，we investigate support set as controllable determinant 的 ICL reliability。

## Abstract
In-context learning (ICL) adapts medical image segmentation models to unseen structures and modalities without retraining by conditioning on a task-specific support set of image-mask exemplars. Because this support set is the model's only task-specific signal, its composition directly influences segmentation performance. In this work, we investigate the support set as a controllable determinant of ICL reliability. First, we compare random sampling against similarity-based selection, where exemplars are retrieved based on their visual similarity to the query image. Second, we train a transformer-based classifier to predict, from the query and support images alone, whether a segmentation will fall below a specified Intersection-over-Union (IoU) threshold. Using MultiverSeg with DINOv3 embeddings across four benchmarks and three imaging modalities, we show that similarity-based selection consistently matches or outperforms random sampling, with the largest gains at the smallest support set sizes. Furthermore, our classifier predicts segmentation failure above chance on all four benchmarks. Ultimately, these results demonstrate that the reliability of in-context segmentation can be both improved via informed support selection and anticipated before use, providing practical mechanisms for safer clinical deployment.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **transformer**：Transformer 架构（学术高频）
- **detection**：检测（CET6）
- **clinical**：临床的（CET6）
