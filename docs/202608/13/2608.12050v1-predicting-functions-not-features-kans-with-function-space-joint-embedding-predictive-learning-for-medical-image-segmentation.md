---
title: "Predicting Functions, Not Features: KANs with Function-Space Joint-Embedding Predictive Learning for Medical Image Segmentation"
source: arxiv
title_zh: 用于Medical Image Segmentation的Predicting Functions，Not Features：KANs 结合 Function-Space Joint-Embedding Predictive Learning
abstract_zh: "To address this limitation，作者提出 Function-Space Joint-Embedding Predictive Learning (FS-JEPA) 用于 medi。\n\n核心做法：Kolmogorov--Arnold 网络s (KANs) introduce explicit functional representations by parameterizing each 网。\n\n主要结果：Experiments on five medical image segmentation benchmarks show that our FS-JEPA 达到 best average Dice。\n\n值得关注的点在于：Our FS-JEPA 框架 moves predictive learning 为 pre-aggregation function space 的 KANs。"
abstract_en: "Kolmogorov--Arnold Networks (KANs) introduce explicit functional representations by parameterizing each network edge as a learnable univariate function. However, existing KAN-based segmentation models optimize edge functions only through objectives defined after edge aggregation, leaving individual functions without an explicit pre-aggregation learning target. To address this limitation, we propose Function-Space Joint-Embedding Predictive Learning (FS-JEPA) for medical image segmentation. Our FS-JEPA framework moves predictive learning into the pre-aggregation function space of KANs. A masked online branch predicts structured signatures of sampled KAN edge functions generated by a full-context exponential moving average target branch, while shared edge indices preserve correspondence between predictions and targets. Rather than predicting an isolated edge response, we represent each sampled edge function using a multi-radius signature composed of function evaluations around its input anchor. This structured representation captures local functional variations that cannot be characterized by a single response and provides a more informative predictive target. The function-space objective is jointly optimized with the segmentation loss during training, while the predictive branch is removed at inference. Experiments on five medical image segmentation benchmarks show that our FS-JEPA achieves the best average Dice and outperforms the strongest competing KAN-based method by +2.25 percentage points."
authors: "Yungeng Liu, Xuanzi Fang, Yuge Zhang, Shuqi Ren, Haijin Zeng, Yongyong Chen"
date: 2026-08-12
journal: cs.CV
source_link: "https://arxiv.org/pdf/2608.12050v1"
pdf: "https://arxiv.org/pdf/2608.12050v1"
score: 6.75
tldr: 围绕《用于Medical Image Segmenta...》；Kolmogorov--Arnold 网络s (KANs) in...；Experiments on five medical image se...
tldr_cn: 围绕《用于Medical Image Segmenta...》；Kolmogorov--Arnold 网络s (KANs) in...；Experiments on five medical image se...
tldr_en: "Kolmogorov--Arnold Networks (KANs) introduce explicit functional representations by parameterizing each network edge as a learnable univariate function. However, existing KAN-based segmentation models optimize edge functions only through objectives defined after edge aggregation, leaving individual functions without an explicit pre-aggregation learning target."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "embedding | 嵌入表示 | 学术高频", "inference | 推理 | CET6"]
motivation: To address this limitation，作者提出 Function-Space Joint-Embedding Predictive Learning (FS-JEPA) 用于 medi。
method: 核心做法：Kolmogorov--Arnold 网络s (KANs) introduce explicit functional representations by parameterizing each 网。
result: 主要结果：Experiments on five medical image segmentation benchmarks show that our FS-JEPA 达到 best average Dice。
conclusion: 值得关注的点在于：Our FS-JEPA 框架 moves predictive learning 为 pre-aggregation function space 的 KANs。
---

## 摘要
To address this limitation，作者提出 Function-Space Joint-Embedding Predictive Learning (FS-JEPA) 用于 medi。

核心做法：Kolmogorov--Arnold 网络s (KANs) introduce explicit functional representations by parameterizing each 网。

主要结果：Experiments on five medical image segmentation benchmarks show that our FS-JEPA 达到 best average Dice。

值得关注的点在于：Our FS-JEPA 框架 moves predictive learning 为 pre-aggregation function space 的 KANs。

## Abstract
Kolmogorov--Arnold Networks (KANs) introduce explicit functional representations by parameterizing each network edge as a learnable univariate function. However, existing KAN-based segmentation models optimize edge functions only through objectives defined after edge aggregation, leaving individual functions without an explicit pre-aggregation learning target. To address this limitation, we propose Function-Space Joint-Embedding Predictive Learning (FS-JEPA) for medical image segmentation. Our FS-JEPA framework moves predictive learning into the pre-aggregation function space of KANs. A masked online branch predicts structured signatures of sampled KAN edge functions generated by a full-context exponential moving average target branch, while shared edge indices preserve correspondence between predictions and targets. Rather than predicting an isolated edge response, we represent each sampled edge function using a multi-radius signature composed of function evaluations around its input anchor. This structured representation captures local functional variations that cannot be characterized by a single response and provides a more informative predictive target. The function-space objective is jointly optimized with the segmentation loss during training, while the predictive branch is removed at inference. Experiments on five medical image segmentation benchmarks show that our FS-JEPA achieves the best average Dice and outperforms the strongest competing KAN-based method by +2.25 percentage points.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **embedding**：嵌入表示（学术高频）
- **inference**：推理（CET6）
