---
title: Fidelity-Constrained Anchoring for Black-Box Denoisers
source: arxiv
title_zh: 用于Black-Box Denoisers的Fidelity-Constrained Anchoring
abstract_zh: "作者提出 fidelity-constrained 框架 that anchors output 的 black-box denoiser to its input without retrainin。\n\n核心做法：方法 linearly blends denoised image 结合 input 与 selects maximum blending factor that satisfies prescrib。\n\n主要结果：Experiments on DIV2K images 结合 synthetic Gaussian noise 与 outputs from Real-ESRGAN 与 non-local means。\n\n值得关注的点在于：In particular，SSIM-based anchoring yields more consistent behavior across noise levels 相比 PSNR-based。"
abstract_en: "We propose a fidelity-constrained framework that anchors the output of a black-box denoiser to its input without retraining and with little additional computation. The method linearly blends the denoised image with the input and selects the maximum blending factor that satisfies a prescribed local fidelity constraint using Peak Signal-to-Noise Ratio (PSNR) or Structural Similarity Index (SSIM). For PSNR control, a closed-form solution is obtained under a local constant-blending assumption. For SSIM control, we derive a tractable formulation based on inverse SSIM under the same assumption and solve it efficiently using iterative root finding. Experiments on DIV2K images with synthetic Gaussian noise and outputs from Real-ESRGAN and a non-local means denoiser show that the proposed anchoring strategy provides effective fidelity control while balancing denoising performance and statistical naturalness, as measured by the excess kurtosis of residual noise. In particular, SSIM-based anchoring yields more consistent behavior across noise levels than PSNR-based anchoring."
authors: Masaki Satoh
date: 2026-08-13
journal: cs.CV
source_link: "https://arxiv.org/pdf/2608.13194v1"
pdf: "https://arxiv.org/pdf/2608.13194v1"
score: 7.05
tldr: 围绕《用于Black-Box Denoisers的Fi...》；方法 linearly blends denoised imag...；Experiments on DIV2K images 结合 synth...
tldr_cn: 围绕《用于Black-Box Denoisers的Fi...》；方法 linearly blends denoised imag...；Experiments on DIV2K images 结合 synth...
tldr_en: We propose a fidelity-constrained framework that anchors the output of a black-box denoiser to its input without retraining and with little additional computation. The method linearly blends the denoised image with the input and selects the maximum blending factor that satisfies a prescribed local fidelity constraint using Peak Signal-to-Noise Ratio (PSNR) or Structural Similarity Index...
selection_source: fresh_fetch
vocabulary: ["denoising | 去噪 | 学术高频"]
motivation: 作者提出 fidelity-constrained 框架 that anchors output 的 black-box denoiser to its input without retrainin。
method: 核心做法：方法 linearly blends denoised image 结合 input 与 selects maximum blending factor that satisfies prescrib。
result: 主要结果：Experiments on DIV2K images 结合 synthetic Gaussian noise 与 outputs from Real-ESRGAN 与 non-local means。
conclusion: 值得关注的点在于：In particular，SSIM-based anchoring yields more consistent behavior across noise levels 相比 PSNR-based。
---

## 摘要
作者提出 fidelity-constrained 框架 that anchors output 的 black-box denoiser to its input without retrainin。

核心做法：方法 linearly blends denoised image 结合 input 与 selects maximum blending factor that satisfies prescrib。

主要结果：Experiments on DIV2K images 结合 synthetic Gaussian noise 与 outputs from Real-ESRGAN 与 non-local means。

值得关注的点在于：In particular，SSIM-based anchoring yields more consistent behavior across noise levels 相比 PSNR-based。

## Abstract
We propose a fidelity-constrained framework that anchors the output of a black-box denoiser to its input without retraining and with little additional computation. The method linearly blends the denoised image with the input and selects the maximum blending factor that satisfies a prescribed local fidelity constraint using Peak Signal-to-Noise Ratio (PSNR) or Structural Similarity Index (SSIM). For PSNR control, a closed-form solution is obtained under a local constant-blending assumption. For SSIM control, we derive a tractable formulation based on inverse SSIM under the same assumption and solve it efficiently using iterative root finding. Experiments on DIV2K images with synthetic Gaussian noise and outputs from Real-ESRGAN and a non-local means denoiser show that the proposed anchoring strategy provides effective fidelity control while balancing denoising performance and statistical naturalness, as measured by the excess kurtosis of residual noise. In particular, SSIM-based anchoring yields more consistent behavior across noise levels than PSNR-based anchoring.

## 英语词汇
- **denoising**：去噪（学术高频）
