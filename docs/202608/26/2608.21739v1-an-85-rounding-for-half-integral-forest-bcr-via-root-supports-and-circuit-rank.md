---
title: An 8/5 Rounding for Half-Integral Forest-BCR via Root Supports and Circuit Rank
source: arxiv
title_zh: 用于Half-Integral Forest-BCR 通过 Root Supports与Circuit Rank的8/5 Rounding
abstract_zh: "这篇论文研究 rounding 的 supplied half-integral feasible solution 的 root-assignment bidirected cut relaxati。\n\n核心做法：Byrka，Grandoni，与 Traub [IPCO 2025] proved 16/9 guarantee 用于 recursive 框架 that normalizes LP point，se。\n\n主要结果：用于 every connected component C 的 split-root graph this yields β_C\\ \\ge\\ \\frac{L_C}{2}，where β_C is c。\n\n值得关注的点在于：In simple 投影，cut constraints at terminal 的 degree two determine root-assignment 向量的 every demand inc。"
abstract_en: "We study the rounding of a supplied half-integral feasible solution of the root-assignment bidirected cut relaxation for Steiner Forest (Forest-BCR). Byrka, Grandoni, and Traub [IPCO 2025] proved a $16/9$ guarantee for a recursive framework that normalizes the LP point, selects a vertex set of maximum projected LP density, buys a minimum spanning tree on that set, contracts it, and recurses. We prove that the same framework has guarantee $8/5$.   The new analysis keeps the orientation and the root label of each projected half-unit of LP mass. In a simple projection, the cut constraints at a terminal of degree two determine the root-assignment vector of every demand incident with it, and half-integrality leaves only two possibilities: a unit assignment to one root, which forces excess outdegree inside that root's support, or a split assignment to two roots, which forces overlap between their supports. For every connected component $C$ of the split-root graph this yields $ β_C\\ \\ge\\ \\frac{L_C}{2}$, where $β_C$ is the circuit rank of the union of the root supports in $C$ and $L_C$ is the number of its vertices of degree two in the full projection. Balancing the density certificate obtained from this inequality against the ordinary degree sum gives a vertex set of density at least $5/8$, and the inherited contraction lemma turns that into the $8/5$ rounding.   For every $q\\ge3$ we also construct a normalized half-integral point whose maximum projected density is exactly $5q/[2(4q-1)]$, so the universal projected-density bound is asymptotically tight."
authors: Morteza Alimi
date: 2026-08-22
journal: cs.DS
source_link: "https://arxiv.org/pdf/2608.21739v1"
pdf: "https://arxiv.org/pdf/2608.21739v1"
score: 8.6
tldr: "围绕《用于Half-Integral Forest-B...》；Byrka，Grandoni，与 Traub [IPCO 202...；用于 every connected component C 的 spl..."
tldr_cn: "围绕《用于Half-Integral Forest-B...》；Byrka，Grandoni，与 Traub [IPCO 202...；用于 every connected component C 的 spl..."
tldr_en: "We study the rounding of a supplied half-integral feasible solution of the root-assignment bidirected cut relaxation for Steiner Forest (Forest-BCR). Byrka, Grandoni, and Traub [IPCO 2025] proved a $16/9$ guarantee for a recursive framework that normalizes the LP point, selects a vertex set of maximum projected LP density, buys a minimum spanning tree on that..."
selection_source: fresh_fetch
motivation: 这篇论文研究 rounding 的 supplied half-integral feasible solution 的 root-assignment bidirected cut relaxati。
method: "核心做法：Byrka，Grandoni，与 Traub [IPCO 2025] proved 16/9 guarantee 用于 recursive 框架 that normalizes LP point，se。"
result: "主要结果：用于 every connected component C 的 split-root graph this yields β_C\\ \\ge\\ \\frac{L_C}{2}，where β_C is c。"
conclusion: 值得关注的点在于：In simple 投影，cut constraints at terminal 的 degree two determine root-assignment 向量的 every demand inc。
---

## 摘要
这篇论文研究 rounding 的 supplied half-integral feasible solution 的 root-assignment bidirected cut relaxati。

核心做法：Byrka，Grandoni，与 Traub [IPCO 2025] proved 16/9 guarantee 用于 recursive 框架 that normalizes LP point，se。

主要结果：用于 every connected component C 的 split-root graph this yields β_C\ \ge\ \frac{L_C}{2}，where β_C is c。

值得关注的点在于：In simple 投影，cut constraints at terminal 的 degree two determine root-assignment 向量的 every demand inc。

## Abstract
We study the rounding of a supplied half-integral feasible solution of the root-assignment bidirected cut relaxation for Steiner Forest (Forest-BCR). Byrka, Grandoni, and Traub [IPCO 2025] proved a $16/9$ guarantee for a recursive framework that normalizes the LP point, selects a vertex set of maximum projected LP density, buys a minimum spanning tree on that set, contracts it, and recurses. We prove that the same framework has guarantee $8/5$.   The new analysis keeps the orientation and the root label of each projected half-unit of LP mass. In a simple projection, the cut constraints at a terminal of degree two determine the root-assignment vector of every demand incident with it, and half-integrality leaves only two possibilities: a unit assignment to one root, which forces excess outdegree inside that root's support, or a split assignment to two roots, which forces overlap between their supports. For every connected component $C$ of the split-root graph this yields $ β_C\ \ge\ \frac{L_C}{2}$, where $β_C$ is the circuit rank of the union of the root supports in $C$ and $L_C$ is the number of its vertices of degree two in the full projection. Balancing the density certificate obtained from this inequality against the ordinary degree sum gives a vertex set of density at least $5/8$, and the inherited contraction lemma turns that into the $8/5$ rounding.   For every $q\ge3$ we also construct a normalized half-integral point whose maximum projected density is exactly $5q/[2(4q-1)]$, so the universal projected-density bound is asymptotically tight.
