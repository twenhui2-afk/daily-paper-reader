Title: Parameterized Complexity of Power Network Design: Coordinating Cable Placement is Hard

URL Source: https://arxiv.org/pdf/2606.25859v1

Published Time: Thu, 25 Jun 2026 01:13:14 GMT

Number of Pages: 55

Markdown Content:
# Parameterized Complexity of Power Network Design: Coordinating Cable Placement is Hard 

## Thekla Hamm #

Eindhoven University of Technology, Eindhoven, the Netherlands 

## Bart M. P. Jansen #

Eindhoven University of Technology, Eindhoven, the Netherlands 

## Faezeh Motiei #

Eindhoven University of Technology, Eindhoven, the Netherlands 

Abstract 

We study several generalizations of the Steiner Tree problem that are motivated by the design of power networks. While Steiner Tree asks for a single minimum-cost tree that connects a given set of terminal vertices, a power network typically consists of multiple trees . Each tree connects to a subset of the terminals, to avoid electrical overloads. The cost of installing a power network is therefore determined by two factors: the total length of the cables in the network and the cost of digging underground trenches into which the cables are placed. Since the digging costs can be substantial, to minimize the total cost of the network it might be necessary to place multiple cables into the same trench. These characteristics lead to variations of Steiner Tree in which the goal is to compute a minimum-cost set of Steiner trees, all with a common root, that together connect a given terminal set while balancing the power demand of the terminals in each tree. Two important variations arise depending on whether the network is intended for low-voltage or high-voltage power. In the low-voltage setting, there is substantial power loss across the cables which effectively means that the maximum depth of any tree in the solution has to be bounded. No such depth bound applies to the high-voltage setting. We investigate the parameterized complexity of several power network design problems, using the number of terminals as the parameter. While this parameterization of the standard Steiner Tree 

problem is fixed-parameter tractable, many of our variants are W[1]-hard. For low-voltage networks (bounded-depth trees), we present an XP -algorithm for planar inputs, which exploits a nontrivial bound on the treewidth of solution subgraphs. We provide an intricate reduction from Grid Tiling 

to establish that the resulting algorithm is tight under the Exponential Time Hypothesis. The 

XP -algorithm extends to the high-voltage setting and to general graphs, albeit at a cost in the running time. For high-voltage networks, we prove that the problem remains W[1]-hard on planar graphs. Finally, we explore a variation of the cost model for sharing digging costs in which both problems become fixed-parameter tractable. 

2012 ACM Subject Classification Theory of computation → Graph algorithms analysis; Theory of computation → Fixed parameter tractability 

Keywords and phrases Steiner Tree, Network Design, Parameterized Complexity, ETH-tight Al-gorithm 

> arXiv:2606.25859v1 [cs.DS] 24 Jun 2026

2 Parameterized Complexity of Power Network Design 

## 1 Introduction 

Steiner Tree is a classic NP-complete problem [ 20 , 26 ] in network design with numerous applications [ 5, 12 , 28 , 34 ]. Given a graph G and a subset of its vertices called terminals ,the goal is to compute a minimum-weight subtree of G that contains all terminals. The study of Steiner Tree has led to a wide range of algorithmic techniques in parameterized complexity [ 4, 10 , 14 , 17 , 19 , 21 , 24 , 29 , 32 , 33 , 35 ]. In this work, we investigate the parameterized complexity of several generalizations of Steiner Tree concerning the design of power networks. They arose from a cooperation with an industrial partner that develops underground infrastructure in the form of low-voltage electrical power networks. 

Background and Motivation A low-voltage power network distributes electricity from a transformer station to an urban neighborhood. A transformer station has a fixed number (typically less than a dozen) of power outputs that each feed one aluminum or copper power cable. Such a cable supplies power to multiple households and can fork off into a tree layout to do so. The maximum amount of power that can be drawn from any single output on a transformer station is limited, which is why the households have to be balanced over the power cables based on their demand. Due to power losses during transport, the depth (maximum length of a root-to-leaf path) of the tree-structure formed by a power cable determines its maximum load. Hence, the design of a low-voltage power network involves deciding which households are connected to a common cable, as well as finding a suitable low-depth tree structure for each cable. A high-voltage network has a similar structure, but does not require a depth bound on the trees since losses on high-voltage connections are negligible. The cost of installing a network depends on two factors. Naturally, there is a cost for the material of the cables that scales linearly with their total length. A less obvious factor, which can be even more important in practice, is the cost of installing the network, i.e., digging trenches in the ground in which the cables are placed. The cost of digging a unit-length trench depends heavily on the terrain: it is cheap when the surface is covered by grass or small pavement tiles that can temporarily be set aside, but it is expensive when the surface is covered by an asphalt road that has to be sliced open and repaired. When multiple cables are placed in the same trench, the costs for digging the trench can be shared among all the cables placed inside it. Hence, it may be advantageous to route cables over slightly longer trajectories if this avoids digging through asphalt or allows cables to share a trench. As the energy transition necessitates the development of better power networks that support a society that increasingly relies on electricity to operate cars, heat pumps, and cooking appliances [3, 15], designing power networks has important applications. 

Problem Statement We model the task of designing a power network as a graph problem. Each edge e of the input graph G represents a trench that can be dug to place cables underground. Each edge has a length ℓ(e) ∈ N which determines the material costs for placing a cable in the corresponding trench, and a separate cost dig (e) ∈ N for digging the trench. The transformer station is represented by a vertex r in G, which serves as the root of the tree-structured cables. The power consumers to be connected are represented by 

terminal vertices T , which are dead ends in the network and have degree one in G. Since power consumption differs per consumer, each terminal vertex u ∈ T has an associated power demand d(u) ∈ N. Finally, the input specifies the number of power outputs t on the transformer station, the depth constraint λ of each tree-structured cable (in the low-voltage setting), and the maximum power demand α that can be supplied by a single cable. T. Hamm, B.M.P. Jansen and F. Motiei 3

To turn the corresponding algorithmic task into a decision problem, there is a threshold value β for the total cost. An input of the problem then asks whether there is a feasible network of cost at most β. In the cost function, we pay the digging cost dig (e) once for every edge e in the union of the solution trees, and for each occurrence of an edge e in a solution tree, we additionally pay ℓ(e) in cable costs. This leads to the following formalization, in which we employ the number of trees t and the number of terminals |T | as the parameters since they are typically much smaller than the total size of the network of potential trenches. 

Low-voltage Network Design (LVND) Parameter: t + |T |

Instance: An undirected graph G with edge lengths ℓ : E(G) → N0 and digging costs 

dig : E(G) → N0, a root vertex r ∈ V (G), a set of degree-1 terminal vertices T ⊆ V (G)

with demands d : T → N, and integers α, β, λ, and t, encoded in unary. 

Question: Does G contain t tree subgraphs H1, . . . , H t, each rooted at r, such that: 

T ⊆ ⋃ti=1 V (Hi) [covering constraint : each terminal is covered], each root-to-leaf path P in each tree Hi satisfies ∑  

> e∈E(P)

ℓ(e) ≤ λ [depth constraint], 

∑ 

> u∈V(Hi)∩T

d(u) ≤ α for each 1 ≤ i ≤ t, [ demand constraint: the total demand of terminals in each tree Hi is at most α], and cost ({H1, . . . , H t}) := 

( t∑

> i=1

ℓ(Hi)

)

+ dig( ⋃ti=1 Hi) ≤ β? [ cost constraint ]The high-voltage variation of the problem arises from the above by setting λ = ∞, so that the depth constraint plays no role. We refer to it as High-voltage Network Design 

(HVND ). The assumption that integers are encoded in unary means we focus our analysis on how the size of the graph and the number of terminals affect the complexity of solving the problem. With input integers encoded in binary, LVND remains NP-complete for t = 1 

and |T | = 1 since it generalizes Shortest Weight-Constrained Path [20, ND 30]. 

Our Contribution We initiate the parameterized-complexity analysis of power network design, focusing on the parameterization by t + |T |. While Steiner Tree [14 ] is fixed-parameter tractable ( FPT ) for the parameter |T |, which easily extends to generalizations such as (Prize-Collecting) Steiner Forest , our problems turns out to be significantly harder. We prove that LVND and HVND are W[1]-hard parameterized by t + |T |, even when restricted to planar graphs. Planar inputs are particularly relevant since the graph models potential locations for digging trenches on the surface of the earth. As in previous W[1]-hardness proofs for (planar) network design problems [ 2, 7 , 8], our reductions start from 

Grid Tiling 1 ([ 30 ], cf. [ 9 , Sec. 14.4.1]) and produce a grid-like graph in which the solution has to route a horizontal part and a vertical part , which have to interact in a prescribed way to meet the cost requirement. For LVND (Theorem 4.1), we only utilize two trees ( t = 2) . By a suitable setting of the demand values for the terminals, we can ensure that the first solution tree H1 must       

> 1Grid Tiling receives as input a k×kmatrix, each cell of which contains a set of pairs of integers. The task is to decide whether a pair can be selected from each cell such that the first entries of selected pairs are equal in cells of the same row, while the second entries are equal for cells of the same column.

4 Parameterized Complexity of Power Network Design 

contain a prescribed subset T1 of terminals, which are attached to the top row of the grid, whereas the other tree H2 contains the remainder, connected to the rightmost column. By a careful choice of depth constraint, we then ensure that the main part of tree H1 consists of horizontal paths through the grid, and the tree H2 consists of paths that are vertical apart from using some horizontal edges. To obtain a solution whose cost meets the desired threshold, each horizontal edge used by H2 must lie on a horizontal path in H1 so that some digging cost is shared among H1 and H2. By setting the cost of such edges based on feasible combinations of values in the input of Grid Tiling , we can ensure the correctness of the reduction. It shows that coordinating the cable placement for the two trees H1, H 2 is hard. Our hardness reduction for HVND (Theorem 4.9) is significantly more involved. Since solutions to the high-voltage problem are not bounded in depth, we have to insert additional terminals inside the grid to enforce the solution to consist of separate horizontal and vertical parts. Embedding these terminals in a planar way while still allowing a solution sufficient degrees of freedom to model Grid Tiling is difficult. We overcome this obstacle by encoding the vertical and horizontal choices using two trees each, leading to t = 4 .By known lower bounds [ 9, Theorem 14.28] on the running time for solving Grid Tiling ,our first reduction implies that under the Exponential Time Hypothesis (ETH) LVND 

on planar input graphs cannot be solved in time f (|T |, t ) · (|V (G)| · λf ′(t))o(|T |) for any computable functions f and f ′. We give an algorithm for planar inputs whose running time matches this bound, thereby showing that the square-root phenomenon [ 31 ] does not manifest itself for this problem. The algorithm works by enumerating a bounded-size set of choices for a certain abstract structure A(S) of the desired solution S = {H1, . . . , H t} and encoding the search for the best solution matching that structure as a Valued Binary Constraint Satisfaction Problem (CSP). We give an exchange argument to prove that if there is a solution, there is one whose abstract structure has O(|T |2) vertices. When the input graph G

is planar, then the abstract structure (which is a minor of G) must be as well, and therefore its treewidth is O(|T |). The treewidth of the abstract structure governs the treewidth of the primal graph of the CSP, which yields the claimed running time using known CSP algorithms. A similar approach also yields an XP -algorithm if the input graph G is not planar, and for the high-voltage problem, albeit at a cost in the running time. Our lower bounds show that even when the parameterization of number of terminals and solution trees is bounded, power network design is intractable due to the difficulties of coordinating cable placement. These difficulties effectively arise when two solution trees leave the root node in different directions, but later converge to place some of their cables in a common trench. As our last contribution, we present an alternative model for the cost computation in which digging costs can only be shared along prefixes of the solution trees. We develop an FPT -algorithm for the resulting Prefix-Sharing Low-voltage Network Design problem parameterized by |T | + t by adapting Dreyfus-Wagner [ 14 ], thereby shedding further light on which aspect of the cost sharing model leads to intractability. 

Related Work In Strongly Connected Steiner Subgraph , the goal is to find a minimum-weight subgraph of a directed graph that is strongly connected and contains all terminals. This problem, and several variations, are known to be W[1]-hard parameterized by the number of terminals [ 16 , 36 ]. However, the direction of the edges plays a crucial role in this hardness. Common variations of Steiner Tree on undirected graphs tend to be FPT 

parameterized by |T |. We are not aware of earlier work on the parameterized complexity of network design problems in which costs can be shared. But, in the operations research community, there has been research [ 1] on the Minimum-Cost Shared Arborescence T. Hamm, B.M.P. Jansen and F. Motiei 5

problem, which also incorporates this cost-sharing aspect. It lacks other features of Low-voltage Network Design though: most importantly, the depth constraint. When it comes to network design problems involving costs and depth-constraints, there has been some work on the Shallow-Light Steiner Tree problem, which aims to compute a low-diameter Steiner tree of minimum cost [ 6, 22 , 27 ]. This problem formulation, therefore, incorporates the depth constraint, but not the cost-sharing feature. 

Organization After presenting basic preliminaries in Section 2, we provide our algorithmic results in Section 3. The complementary hardness proofs are given in Section 4. We conclude in Section 5. 

## 2 Preliminaries 

For i ∈ N, we denote by [i] the set {1, . . . , i }. For sets A, B we denote by 2A the powerset of A,and by A × B the set {(a, b ) | a ∈ A ∧ b ∈ B}. Given a total function f : A → B and a subset 

A′ ⊆ A, we define f (A′) = {f (a) | a ∈ A′}. We assume familiarity with graph theory [ 11 ]and parameterized complexity theory [ 9] and use standard terminology and notation from both. Unless stated otherwise, we consider undirected simple graphs and denote an edge between vertices u and v by uv = vu . The interior of a path P is the subgraph induced by its non-endpoint vertices. For a rooted tree (T, r ) with edge weights w : E(T ) → N, its w-depth 

is max t∈V (T )

∑ 

> e∈E(Pr,t )

w(e), where Pr,t is the unique path from r to t in T . When w is clear from context, we simply refer to this quantity as depth. For brevity, for w : E(G) → N and 

H ⊆ G, we denote w(H) = ∑ 

> e∈E(H)

w(e). For a set of subgraphs S = {H1, . . . , H t} and a subgraph G′, the cost of S in G′ is defined as cost G′ (H1, . . . , H t) = cost (H1 ∩G′, . . . , H t ∩G′).We denote the treewidth of a graph G by tw( G).For our algorithms, we use two results from the literature as subroutines; one for certain path computations, and one for solving a certain constraint satisfaction problem (CSP) .

▶ Theorem 2.1 ([ 25 ]) . Given a directed graph G, two functions f, g : E(G) → N, two vertices 

s, t ∈ V (G), and a threshold τ ∈ N, there is an algorithm that computes a path P from s to t

that satisfies ∑  

> e∈E(P)

f (e) ≤ τ for which ∑  

> e∈E(P)

g(e) is minimized, if one exists, in time 

O(τ · | V (G)| · | E(G)|). If no such path exists, the algorithm reports failure. 

A valued binary CSP is a tuple (V, D, C) where V and D are two finite sets called the set of variables and the domain respectively. The third component C is a finite set of value constraints which are tuples (x, y, c ) with x, y ∈ V and c : D × D → N. Given an assignment 

ϕ : V → D , its value is ∑ 

> (x,y,c )∈C

c(ϕ(x), ϕ (y)) . The primal graph GP (V, D, C) of a valued binary CSP (V, D, C) is the graph (V, {xy | (x, y, c ) ∈ C for some c : D × D → N}).

▶ Theorem 2.2 (folklore, e.g. [ 18 ]) . Given a valued binary CSP (V, D, C), an assignment of minimum value can be computed in time |V| · |D| O(tw( GP (V,D,C)) .

The following lemma can be used to show that for a given partition P of terminals, one can define a demand function and a value α such that P is the unique partition into sets of total demand at most α. This will be used later, in Section 4.2, to prove that both 

Low-voltage Network Design and High-voltage Network Design are W[1]-hard. 

▶ Lemma 2.3. There exists a polynomial-time algorithm that, given a set of items U and a partition P of U into t sets P1, . . . , Pt, computes a function f : U → [|U |t+2 ] such that P is the unique partition of U into t sets that satisfies ∑

> x∈P i

f (x) ≤ 1

> t

∑

> x∈U

f (x) for each i ∈ [t].6 Parameterized Complexity of Power Network Design 

Proof. For each i ∈ [t], let ϵi = |U |i−1 and Ωi = |U |t+2 − (|P i| − 1) · ϵi. Set f as Ωi for one arbitrarily chosen item of Pi and set f as ϵi for the rest of Pi. Note that f (Pi) = |U |t+2 

for each i ∈ [t], so that 1

> t

∑ 

> x∈U

f (x) = |U |t+2 . We now prove that this choice of f has the claimed properties. Let P′ be a partition of U into t sets P′

> 1

, . . . , P′ 

> t

such that ∑

> x∈P ′
> i

f (x) ≤ 1

> t

∑ 

> x∈U

f (x)

for each i ∈ [t], which can only happen if f (P′ 

> i

) = |U |t+2 for each i ∈ [t]. If two items with 

f -values Ωi and Ωj are in the same part P′ 

> z

of P′, then 

∑

> x∈P ′
> z

f (x) ≥ Ωi + Ω j = |U |t+2 − (|P i| − 1) · ϵi + |U |t+2 − (|P j | − 1) · ϵj

≥ 2|U |t+2 − | U |t − | U |t−1 > |U |t+2 .

Thus, no part contains two such items, and w.l.o.g., we can assume that the item with f -value 

Ωi is in P′ 

> i

. Now, we prove the rest of the items in P′ 

> i

each have f -value ϵi by induction on i.For i = 1 , we know that P′ 

> 1

contains the item of value Ω1 and satisfies f (P′

> 1

) = |U |t+2 .Hence the other items in P′ 

> 1

have total value |U |t+2 − Ω1 = ( |P 1| − 1) · ϵ1 < |U | · | U |0 = |U |.Since ϵi > |U | for all i > 1, the only other items in P1 have value less than ϵ2, and are therefore the |P 1| − 1 items of value ϵ1. Hence P′ 

> 1

= P1.Now, assume that each part P′ 

> i′

∈ {P ′

> 1

, . . . , P′

> i−1

} consists of one item with f -value Ωi′

and |P ′ 

> i

| − 1 items with f -value ϵi′ . We show that P′ 

> i

consists of an item with f -value Ωi and 

|P i| − 1 items with f -value ϵi, so that P′ 

> i

= Pi. We proved that an item in P′ 

> i

has f -value 

Ωi. The total f -value of the rest of the items in P′ 

> i

must be at most (|P i| − 1) · ϵi, which is strictly less than ϵj for j > i . Hence the rest of the items in Pi must have f -value exactly equal to ϵi. Therefore, each part P′ 

> i

must consist of an item with f -value Ωi and |P i| − 1

items with f -value ϵi, and so, P = P′. Hence, this partition is unique and f is the desired function of this lemma. ◀

## 3 Algorithms 3.1 Algorithms for LVND and HVND 

In this section, we present our algorithmic results for LVND and HVND . These are based on first analyzing the structure of solutions and then exploiting this structure to be able to compute their cost using Theorem 2.1 and Theorem 2.2. In particular, we show that a minimum-cost solution {H1, . . . , H t} can be decomposed into pairwise disjoint paths between the vertices of degree three and higher in H := ⋃ti=1 Hi, with the guarantee that the digging cost of each path is minimum among all paths whose sum of ℓ-values is the same. (If a path does not have minimum cost, then it can be replaced by a cheaper one to obtain a cheaper overall solution.) Such paths can be computed with Theorem 2.1 as soon as we know their endpoints and length-thresholds. As these are interdependent among each other, we formulate this problem as a valued binary CSP with a variable for each endpoint of such a path. Hence, the structure of this CSP can be captured by a graph with a vertex for each endpoint of such a path and an edge for each such path. This is the graph we investigate in Section 3.1.1; ultimately arriving at a bound on the number of its vertices. This will actually allow us to solve a CSP with the rough structure described above using Theorem 2.2. We carry out the details of setting up the CSP appropriately and show that this indeed yields algorithms for LVND and HVND with the claimed running times in Section 3.1.2. T. Hamm, B.M.P. Jansen and F. Motiei 7

## 3.1.1 Solution structure 

We will now analyze the structure of a solution viewed as a set of minimum-cost bounded-length paths between certain endpoints. In particular, we want to bound the number of such endpoints that we need to consider. For this, we introduce the abstract structure of a solution whose vertices correspond to such endpoints and whose edges correspond to minimum-cost bounded-length paths between them. 

▶ Definition 3.1. For a solution S = {H1, . . . , H t} of LVND or HVND , the abstract structure A(S) is a graph H with a color from {cS | ∅̸ = S ⊆ 2[t]} associated to each of its edges, constructed as follows. Initially, let H = ⋃ti=1 Hi and color each e ∈ E(H) by cS , where S = {i ∈ [t] | e ∈ E(Hi)}.While a vertex v̸ = r has degree 2 in H and its incident edges have the same color c, we remove v and connect the neighbors of v with an edge of color c.The subgraph of A(S) corresponding to Hi, denoted by Ai(S), is the subgraph of A(S)

consisting of all edges with a color cS for which i ∈ S.

Considering the color of the edges of A({H1, . . . , H t}), we can find Ai(S) in polynomial time for each i ∈ [t]. Clearly, Ai(S) is obtained from Hi by dissolving some degree-2 vertices. The following lemma guarantees the existence of a solution for which the number of vertices in its abstract structure is bounded quadratically in |T |. This bound enables us to enumerate all possible such structures and, accordingly, to search for a solution. 

▶ Lemma 3.2. For every yes-instance (G, ℓ, dig , r, T, d, α, β, λ, t ) to LVND and HVND ,there exists a solution S = {H1, . . . , H t} such that |V (A(S)) | ≤ 1 + 2 |T | + 8 |T |2.

Proof. To prove this lemma, we define an ordering on solutions to the instance, and then prove that a maximal element in this ordering has an abstract structure whose number of vertices is suitably small. For an arbitrary solution S = {H1, . . . , H t}, for each edge e ∈ E(G),we set xe(S) to be the number of trees in S that contain e. Then, we define the coloring sequence of S as a tuple T (S) := ( y1, . . . , y |E(G)|) where y1, . . . , y |E(G)| is the decreasingly sorted sequence of values xe(S) for e ∈ E(G). Let S = {H1, . . . , H t} be a solution where the value of cost (S) is minimized and which, among all solutions that minimize cost (S), has a maximum tuple T (S) with respect to the lexicographic ordering. To bound the number of vertices in the abstract structure A(S), we exploit the conditions under which Definition 3.1 removes a degree-2 vertex. These conditions can be expressed using the following concept. For a tree Hi, we define its special vertices to be the root r

of the instance together with the vertices of Hi whose degree in Hi is unequal to 2. We define a plain path of Hi as a path P in Hi whose endpoints are special but whose internal vertices are not. Since degree-2 non-root vertices of ⋃ 

> i

Hi are dissolved when both incident edges have the same color, while the edge colors encode which trees contain the edge, it follows that the vertices of ⋃ 

> i

Hi that remain in A(S) are (1) the root vertex r, (2) each non-root vertex that has degree unequal to 2 in at least one tree Hi, and (3) vertices v that lie on plain paths Pi, P j of two distinct trees Hi, H j , such that v has degree more than 2 in the union Hi ∪ Hj , i.e., places where plain paths of distinct trees converge or diverge (see Figure 1). The root vertex r is the only vertex that is of type 1. To bound the number of vertices in the abstract structure, we bound the number of vertices of each of the two other types. First, we bound the the number of vertices of type 2. Let Ai be the set of non-root vertices of Hi whose degree is not 2 and fi be the number of non-root leaves of Hi. By cost-minimality, 8 Parameterized Complexity of Power Network Design 

r

v1 v2

v3 v4 v5

v6

v7 v8

v10 

v11 

v9

> (a)

r

v1 v2

v3 v4 v5

v6

v7 v8

v10 

v9

> (b)

r

v1 v2

v3 v4

v6

v7 v8

v10 

> (c)

Figure 1 (a) An instance of LVND /HVND . Each edge has length 1 and digging cost 100 . Squares represent terminals. (b) A solution S. (c) A(S), with vertices of the same type in the same color. Type 2 vertices are shown in orange and type 3 vertices are shown in purple. The vertex v11 is not in any solution trees and so not in A(S). The vertices v5 and v9 are in the solution trees but not in the abstract structure as they have degree 2 in the union of the trees. One can see that the vertices 

v4 and v6 are shared between the path from r to v7 in (c) and the path from v2 to v8 in (d) causing a degree more than 2 for them in the abstract structure. The path from r to v3 in (b) and the path from r to v7 in (c) also intersect in v1 which makes v1 to be of degree more than 2 in the abstract structure. So, all of these vertices remain in the abstract structure though they have degree 2 in the mentioned trees. 

we can assume that the non-root leaves of each tree are terminals. Additionally, since each terminal has degree-1 in the input graph G by definition of LVND , it also follows from cost-minimality that each terminal appears in exactly one tree. So, we have ∑ti=1 fi = |T |.Recalling that the number of vertices of degree unequal to 2 in a tree is at most twice the number of its leaves minus 1, we can provide an upper bound for |Ai| as follows. If root r is a leaf of Hi, then the number of leaves is fi + 1 and so, |Ai ∪ { v}| ≤ 2( fi + 1) − 1 = 2 fi + 1 .Otherwise, if the root r is not a leaf, we have |Ai| ≤ 2fi − 1 ≤ 2fi. Since in both cases we have |Ai| ≤ 2fi, we can conclude that the total number of vertices of type 2 is at most 

> t

∑

> i=1

|Ai| ≤ t∑

> i=1

2fi ≤ 2|T |.Now, it remains to analyze vertices of type (3). The following claim will be instrumental to do so. 

▷ Claim 3.3. For plain paths Pi, P j of two distinct trees Hi, H j , the graph Pi ∪ Pj has at most two vertices of degree more than 2.

Proof. Since both Pi and Pj are paths, a vertex of Pi ∪ Pj of degree more than 2 is a place where the two paths either converge or diverge. Hence if there are three or more such vertices, the graph Pi ∪ Pj has a cycle. A minimal such cycle can be decomposed into two parts where one part is a subpath of Pi and the other is a subpath of Pj . We call these sub-paths P ′ 

> i

and 

P ′ 

> j

, respectively. Using P ′ 

> i

and P ′ 

> j

, we now derive a contradiction by building a minimum-cost solution S′ whose coloring sequence is larger than that of S.Since all internal vertices of P ′ 

> i

and P ′ 

> j

are plain, they have degree 2 and cannot be a terminal or the root. Hence we can replace P ′ 

> i

in Hi by P ′ 

> j

to obtain a tree H′ 

> i

containing the root that covers the same terminals as Hi. Similarly, we can replace P ′ 

> j

in Hj by P ′ 

> i

to obtain a tree H′ 

> j

. If ℓ(P ′ 

> i

)̸ = ℓ(P ′ 

> j

), then replacing the longer by the shorter segment yields a T. Hamm, B.M.P. Jansen and F. Motiei 9

solution whose cost is strictly smaller: the digging costs do not increase (as all edges involved in the replacement were dug on account of the other tree anyway), the total depth does not increase, and the total length of the edges in the replaced tree strictly decreases. Since we started from a cost-minimal solution, we must therefore have ℓ(P ′ 

> i

) = ℓ(P ′ 

> j

). We now derive a contradiction by defining a minimum-cost solution that is lexicographically larger. Let x′ 

> i

be the maximum value of xe(S) over all e ∈ E(P ′ 

> i

) and x′ 

> j

be the maximum value of xe(S) over all e ∈ E(P ′ 

> j

). Without loss of generality, assume that x′ 

> i

≥ x′ 

> j

. Let S′ be the solution obtained from S by replacing Hj with H′ 

> j

. We argue the coloring sequence of S′

is larger than that of S in the lexicographical ordering. The edge of P ′ 

> i

∪ P ′ 

> j

that was used most in solution S (which appears on the most relevant position in the sorted sequence) appears at least one additional time in solution S′ on account of replacing Hj (which does not contain that edge) by H′ 

> j

(which does). This contradiction proves the claim. ◁

Claim 3.3 leads to an upper bound of 8|T |2 on the number of vertices of A(S) of type (3) by bounding the total number of plain paths in the solution, as follows. Note that the plain paths of a tree Hi are in 1-to-1 correspondence with the edges of the tree H′ 

> i

that results by dissolving all non-special vertices of Hi. Since any tree has strictly fewer edges than vertices, and noting that each tree has at most |Ai| + 1 special vertices, r together with all non-degree 2 vertices, there are |Ai| edges in H′ 

> i

. Thus, the total number of edges over all trees H′

> 1

, . . . , H ′ 

> t

(and therefore the total number of plain paths) is at most ∑

> i∈[t]

|Ai| ≤ 2|T |.Claim 3.3 shows that each pair of plain paths adds at most 2 vertices of type (3) to A(S).Hence, the total number of vertices in A(S) is 1 + 2 |T | + 2 · (2 |T |)2 = 1 + 2 |T | + 8 |T |2. ◀

## 3.1.2 XP -algorithms for Power Network Design 

Using bounds on the abstract structure we present XP -algorithms for power network design. 

▶ Theorem 3.4. The Low-voltage Network Design problem can be solved in time 

f (t, |T |) · (|V (G)| · λt)O(|T |2) for any arbitrary input instance (G, ℓ, dig , r, T, d, α, β, λ, t ).

Proof. First, we provide an algorithm that solves this problem. We then discuss its correctness and running time analysis. Given an input instance (G, ℓ, dig , r, T, d, α, β, λ, t ), we proceed in the following steps. 1. For each graph H with at most 1 + 2 |T | + 8 |T |2 vertices whose edges are colored with 

2t − 1 colors, each color corresponding to a non-empty subset of [t], we fix an arbitrary orientation of the edges of H and do the following. (We consider H as a guess for the abstract structure of a solution, up to isomorphism.) a. Let H′ 

> i

be the subgraph of H induced by all edges with a color cS where i ∈ S. Let 

Vi be the set of vertices of H′ 

> i

. Check if each subgraph H′ 

> i

for i ∈ [t] is a tree; if not, continue with the next guess for H.b. For each injective assignment φ : T ∪{ r} → V (H) that satisfies the following conditions: the demand constraint holds for all trees, i.e., the total demand of the preimages of 

φ of the terminals in each tree Hi is at most α, and vertex φ(r) is contained in each tree H′ 

> i

,we do the following: i. Root tree H′ 

> i

at φ(r) and direct its edges away from the root. ii. Define a valued binary CSP-instance (V, D, C) as follows. 

V = V (H), so each vertex of H has an associated variable. 10 Parameterized Complexity of Power Network Design 

D = {(v, x 1, . . . , x t) | v ∈ V (G) ∧ x1, . . . , x t ∈ { 0} ∪ [λ]}.

C = C′ ∪ C ′′ , so the set of constraints consists of two parts that are defined below. (The first part, C′, effectively acts on individual variables and forces those vertices of H that correspond to a terminal or the root under φ, to be mapped to the corresponding terminal or root. The second part, C′′ , tracks the cost of realizing an edge of the abstract structure by a bounded-depth path in G.) 

C′ = {(u, u, c u) | u ∈ V} where 

cu(( v, x 1, . . . , x t), (v′, y 1, . . . , y t)) = 



β + 1 if v̸ = v′ ∨ ∃ i ∈ [t] xi̸ = yi

∨(v ∈ T ∪ { r} ∧ u̸ = φ(v)) 

∨(u ∈ φ(T ∪ { r}) ∧ φ−1(u)̸ = v)0 otherwise. Note that a feasible solution to the problem has cost at most β, so that the value β + 1 in the first case effectively forbids such behavior from happening. 

C′′ = {(u, v, c uv ) | uv ∈ E(H)} where cuv (( u′, x 1, . . . , x t), (v′, y 1, . . . , y t)) is defined as follows for each (( u′, x 1, . . . , x t), (v′, y 1, . . . , y t)) ∈ D × D . Let S ⊆ [t]

be the set encoded by the color of edge uv . For all i ∈ [t] for which uv ∈ E(H′ 

> i

),let zi be 1 if the direction of edge uv is from u to v, and zi be −1 otherwise. Let λ′ be the minimum value of zi · (yi − xi) taken over all i ∈ [t] for which 

uv ∈ E(H′ 

> i

). Set cuv (( u′, x 1, . . . , x t), (v′, y 1, . . . , y t)) to the minimum value of 

|S|· ℓ(P ′)+ dig (P ′) over all paths P ′ between u′ and v′ in G with ℓ(P ′) ≤ λ′. This can be computed using Theorem 2.1 considering the input graph as a copy G′ of 

G where all edges are duplicated in two different directions, defining f (e′) = ℓ(e)

and g(e′) = |S| · ℓ(e) + dig (e) for all e′ ∈ E(G′) where e′ is a copy of e ∈ E(G),and clearly considering s = u′, t = v′ and λ′ as the threshold. If no such path exists, set the cost to β + 1 .iii. Solve CSP-instance (V, D, C) using Theorem 2.2. iv. Check if the computed cost is at most β and return ‘yes’ if so. 2. If the algorithm reaches this step, then return ‘no’. 

Correctness We show that the given algorithm outputs ‘yes’ if and only if its input 

(G, ℓ, dig , r, T, d, α, β, λ, t ) is a ‘yes’-instance to LVND . To prove that the algorithm outputs ‘yes’ if there exists some solution to the given input instance, we show that the properties of at least one solution lie in all of our branching steps leading up to solving the CSP-instance, and so, we show that at most the cost of that solution should be computed during the CSP subroutine. On the other hand, for the other direction, we show that if the algorithm returns ‘yes’, we can construct a solution from the implied solution returned by the CSP subroutine. The validity of the properties required to be a solution for the LVND -instance solution can be proved based on the properties considered in the branching steps. We prove these directions, respectively, through the following claims. 

▷ Claim 3.5. If there exists a solution for the LVND -instance (G, ℓ, dig , r, T, d, α, β, λ, t ),the algorithm returns ‘yes’. 

Proof. We discuss the algorithm step by step to show that, considering a proper solution, each step of the algorithm is executed successfully, resulting in a return of ‘yes’. By Lemma 3.2, there exists at least one solution S = {H1, . . . , H t} such that A(S) has at most 1 + 2 |T | + 8 |T |2 vertices. This means that in at least one of the cases of step 1, we T. Hamm, B.M.P. Jansen and F. Motiei 11 

are considering H ≃ A (S) with this isomorphism witnessed by ι : V (H) → V (A(S)) . As the algorithm tries all ways to assign colors from the range [2 t − 1] to the edges of H, in some iteration it achieves the same coloring as the abstract structure A(S). This means that each H′ 

> i

as defined in the algorithm is equal to Ai(S), which is obtained by dissolving degree-2 vertices of the tree Hi and therefore a tree. Therefore, the algorithm proceeds to step 1.a. Next we argue that, in some branch of step 2.b, we consider φ with φ(v) = ι−1(v) for each v ∈ T ∪ { r}. Without loss of generality, we can assume that S is a solution that has the minimum cost among all solutions for which A(S) has at most 1 + 2 |T | + 8 |T |2 vertices. This minimality assumption guarantees that all terminals are leaves of trees, each terminal is covered by exactly one tree, and each leaf is either a terminal or the root. Thus, the assignment of terminals implied by φ satisfies the demand constraint, and r is assigned to a vertex contained in each H′ 

> i

. Hence, indeed φ = ι−1 is considered in step 2.b. Then, as we discussed, we know that H′ 

> i

in H is isomorphic to Ai(S). Hence directions are defined in the same way on both subgraphs, noting that both φ(r) and r take the role of the root, respectively. We define a solution for CSP as follows. For each variable in V,corresponding to some vertex v in H, we set the vertex value of the corresponding tuple to ι(v) and set xi to be the depth of this vertex in Hi if Hi contains it; if not, we set xi

to 0. Then, the cost of each constraint in C′, of the form (u, u, c u) for u ∈ V , is 0 since the vertices are assigned correctly. Moreover, each constraint in C′′ , of the form (u, v, c uv ),has a cost cuv = |S| · ℓ(e) + dig (e) by definition. This is precisely the cost of the path in S

corresponding to uv in H: since the path appears in exactly the trees of S indexed by S,its length is counted |S| times, while its digging cost, being shared among all of them, is counted once. Thus, the cost of this CSP solution is equal to the cost of S. Since we know that the CSP has a solution of cost at most β, Theorem 2.2 returns a solution of cost at most β, and thus the algorithm returns ‘yes’. ◀▷ Claim 3.6. If the algorithm returns ‘yes’ for the input instance (G, ℓ, dig , r, T, d, α, β, λ, t ),

LVND has a solution for this input instance. 

Proof. Assume that H is the guess for A(S) that ends in a ‘yes’ output. Since the algorithm returns ‘yes’, in some iteration the CSP-subroutine in step 2.b.ii finds an assignment of the variables (which by construction correspond to vertices of H) whose cost is at most β. All further references to steps in this claim’s proof refer to the iteration in which step 2.b.ii returns ‘yes’. We construct a subgraph H′′  

> i

of G for all i ∈ [t] as follows. In H′ 

> i

from step 1.a replace the vertices with the vertices of G in the first entry of their corresponding variable’s CSP-assignment, and replace each edge of H′ 

> i

with the path in G between the vertices assigned to its endpoints in this way, which can be found by the algorithm of Theorem 2.1. By the definition of the cost of an assignment for the CSP and the fact that no constraint realizes a value of β + 1 , cost ({H′′  

> 1

, . . . , H ′′  

> t

}) ≤ β. Since we do not have control over finding pairwise disjoint paths for each pair, there is a possibility that H′′  

> i

is not a tree for some i ∈ [t]. We will resolve this later and first show that apart from this {H′′  

> 1

, . . . , H ′′  

> t

} satisfies the covering, demand, and depth constraints of being a LVND -solution. Because constraints C′ are included, the assignment ensures that T ∪ { r} appears in V (H)

according to φ from step 1.a. Hence, by the conditions on φ from step 1.a, the covering and demand constraints hold for all H′′  

> i

where i ∈ [t].For the depth constraint, consider an arbitrary i ∈ [t] and z ∈ V (H′′  

> i

) ∩ T . By the construction of C′′ in step 1.a.i, and since the cost of the CSP-assignment is at most β, there 12 Parameterized Complexity of Power Network Design 

is some value xvi ∈ { 0} ∪ [λ] for each vertex v on the φ(r)-φ(z) path P in Hi such that the path we include into H′′  

> i

for the edge between two consecutive vertices u, v on P has length at most xvi − xui . This means that overall ℓ(P ) ≤ ∑  

> uv ∈E(P)

xvi − xui which is a telescoping sum equal to xφ(z) 

> i

− xφ(u) 

> i

, which is at most λ − 0 since the domain D only allows integers from {0} ∪ [λ] on the integer-valued coordinates. So apart from being trees, the subgraphs 

H′′  

> 1

, . . . , H ′′  

> t

satisfy all the conditions of being a solution for LVND .For each i ∈ [t], we get rid of cycles in H′′  

> i

and construct ˆHi ⊆ H′′  

> i

, which is a tree in the following way. Initialize ˆHi as a copy of H′′  

> i

. As long as ˆHi contains some cycle C proceed as follows. Let v be a vertex in C that has the maximum ℓ-distance to r. Let u and u′

be neighbors of v in C. If the removal of the edge vu increases the distance ℓ of a vertex from r, the edge vu must be on the unique shortest path from r to v. In this case, since the shortest path p from r to v must be unique, the removal of u′v does not change the distance of any vertices from r; otherwise, it belongs to a shortest path from r to v that is not p, contradicting that p is unique. Therefore, we can remove one of the edges of C that is incident on v without changing the distance of the vertices from r.This procedure maintains in ˆHi as in H′′  

> i

the same connectivity and hence connects the same terminals, which is considered over all i ∈ [t] ensures the covering and demand constraints. Moreover, by the arguments above, the depth constraint is also maintained. Finally, since each ˆHi is a subgraph of H′′  

> i

, the cost constraint is also trivially maintained. ◀

Putting these two directions together, we can conclude that LVND has a solution for the input instance (G, ℓ, dig , r, T, d, α, β, λ, t ) if and only if the algorithm returns ‘yes’. 

Running Time Analysis Step 1 goes through all graphs with at most 1+2 |T |+8 |T |2 vertices with edges of 2t − 1 colors. Considering all graphs with at most 1 + 2 |T | + 8 |T |2 vertices, 

(1+2 |T |+8 |T |2)2 edges, and 2t −1 colors on its edges, the number of cases is at most 2O(t·| T |4).Then, steps 1.a and 1.a.i can be processed in linear time using a simple graph traversal algorithm such as BFS on the edges belonging to each tree, and so each takes O(|T |2) and in total O(t · | T |2). The number of ways to assign terminals and the root vertex to the vertices of H is at most O(|T ||T |2

), which indicates an upper bound on the cases in step 2.b. The demand constraint can be checked trivially in linear time O(|T |). Finally, we discuss the time needed to solve the defined CSP-instance in step 2.b.ii. By Theorem 2.1, we can compute the cost function for two values of variables in a constraint in time O(λ · | V (G)| · | E(G)|)

since λ′ ≤ λ and the number of vertices and edges of G and G′ are asymptotically equal. To solve the described CSP, we use Theorem 2.2. So, the CSP-instance can be solved in time 

|V| · |D| O(tw( GP (V,D,C)) . Since there are at most 8|T |2 + 2 |T | + 1 variables and so, at most 

8|T |2 + 2 |T | + 1 vertices in the primal graph, the treewidth is at most O(|T |2), this results in a running time of (8 |T |2 + 2 |T | + 1)( |V (G)| · λt)O(|T |2) for solving the CPS-instance. Each of the remaining steps can be followed in constant time. Putting all of this together results in a running time of f (t, |T |) · (|V (G)| · λt)O(|T |2). ◀

The main bottleneck of the algorithm presented in the proof of Theorem 3.4 lies in solving the CSP-instance. A straightforward alternative is to replace the call to the CSP algorithm from Theorem 2.2 with an exhaustive branching approach: iterating over all possible assignments of domain values D to variables V and checking in polynomial time which of these satisfy all constraints to find a valid solution for the given input instance. In this approach, the running time remains f (t, |T |) · (|V (G)| · λt)O(|T |2), but the algorithm requires only polynomial space and avoids the usage of the powerful CSP algorithm. However, it is important to note that this asymptotic running-time equivalence arises from the analysis, T. Hamm, B.M.P. Jansen and F. Motiei 13 

not from the actual behavior of the CSP-based algorithm. In the CSP approach, the running time depends on the treewidth of the primal graph of the CSP-instance, which is conservatively replaced with a crude upper bound in the analysis. The following theorem refines this by considering the case where the input graph is planar, allowing for a tighter bound on the treewidth. In Section 4.1, we further show that in this planar setting, the algorithm achieves the best possible running time under standard complexity assumptions. 

▶ Theorem 3.7. Low-voltage Network Design can be solved in time f (t, |T |) · (|V (G)| · 

λt)O(|T |) for any input instance (G, ℓ, dig , r, T, d, α, β, λ, t ) where G is planar. 

Proof. The algorithm is a small variation of that of Theorem 3.4. Since the abstract structure A(S) of a solution S = {H1, . . . , H t} is obtained by dissolving degree-2 vertices of a subgraph of G, for a planar input graph G it follows that A(S) is also planar. Hence Lemma 3.2 implies that there exists a solution S where A(S) is a planar graph on O(|T |2)

vertices. As an n-vertex planar graph has treewidth O(√n) (see, e.g., [ 9 , Corollary 7.24]), the treewidth of such A(S) is O(√|V (A(S)) |) = O(|T |).This means that in step 1 of the algorithm it suffices to look only through branches where 

H is planar (which can be tested in linear time [ 23 ]) and therefore has treewidth O(|T |).Furthermore, in these branches, since the treewidth of the primal graph is equal to the treewidth of H, we can present a tighter upper bound on the running time of step 2.b.ii, solving CSP. The CSP subroutine invoked in this step takes time |D| O tw( GP (V,D,C)) . Thus, each call to the subroutine can be handled in time (|V (G)| · λt)O(|T |). The remaining overhead is the same as in Theorem 3.4. Hence the overall running time for planar instances is f (t, |T |) · (|V (G)| · λt)O(|T |) for some computable function f . ◀

Finally, we address the version of the problem without the depth constraint. The next theorem presents an algorithm for solving this variant. 

▶ Theorem 3.8. The High-voltage Network Design problem can be solved in time 

f (t, |T |) · | V (G)|O(|T |2) for any arbitrary input instance (G, ℓ, dig , r, T, d, α, β, t ).

Proof. Consider the algorithm presented in the proof of Theorem 3.4, where step 1.a.i is removed and step 2.b.ii is replaced by the following step. 1.b.ii Define a valued binary CSP-instance (V, D, C) as follows. 

V = V (H).

D = V (G), so an element of the domain no longer encodes a tuple of depth values. 

C = C′ ∪ C ′′ , with the two parts of the constraint set as defined below. 

C′ = {(u, u, c u) | u ∈ V} where 

cu(v, v ′) = 



β + 1 if v̸ = v′

∨(v ∈ T ∪ { r} ∧ u̸ = φ(v)) 

∨(u ∈ φ(T ∪ { r}) ∧ φ−1(u)̸ = v)0 otherwise. 

C′′ = {(u, v, c uv ) | uv ∈ E(H)} where cuv (u′, v ′) is defined as follows for each 

(u′, v ′) ∈ D × D . Let S ⊆ [t] be the set encoded by the color of edge uv . Set 

cuv (u′, v ′) to the minimum value of |S| · ℓ(P ′) + dig (P ′) over all paths P ′ between 

u′ and v′. This can computed using Dijkstra’s algorithm for finding a shortest path between u′ and v′ in a weighting of graph G where for each edge e ∈ E(G),its weight is defined as |S| · ℓ(e) + dig (e). If no such path exists, set the cost to 

β + 1 .14 Parameterized Complexity of Power Network Design 

Correctness of the algorithm follows analogously to the proof of Theorem 3.4, with the key difference that the depth and its associated arguments are no longer relevant. The running time analysis is also similar to Theorem 3.4. However, the running time of the CSP solving subroutine, which is |D| O(tw( GP (V,D,C)) , reduces due to the smaller domain D of size |V (G)|. Solving one instance of the newly defined CSP therefore takes time |V (G)|O(|T |2).Thus, the total running time of this algorithm is f (t, |T |) · | V (G)|O(|T |2). ◀

## 3.2 FPT-Algorithms for Prefix-Sharing Low-voltage Network Design 

In an attempt to understand how sharing digging costs causes intractability, we define a variation of the power network design problem whose cost function is easier to optimize. While the cost of a solution still arises as a digging term plus the sum of the cable lengths, we change the definition of the digging term. In the definition of Section 1, whenever multiple trees contain the same edge the digging costs of the corresponding trench only have to be paid once. The variation we introduce effectively means that when multiple trees contain the same edge, the digging costs can only be shared when the path from the root to this edge is the same in both trees. This is formalized through a technical definition of the different prefixes leading to an edge. The digging costs are then paid once for each different prefix leading to that trench. We show that under this cost model, the problem is FPT parameterized by |T | + t via dynamic programming. We now formally define and analyze the resulting Prefix-Sharing Low-voltage Network Design problem. For the definition we need the concept of a walk in a graph, which is an alternating sequence of vertices and edges (v0, e 1, . . . , v k) such that edge ei is incident on vi−1 and vi for all i ∈ [k]. A walk is simple (sometimes called trail in the literature) when no edge occurs more than once. Note that a path is a simple walk with no repeated vertex. If the direction of the walk is irrelevant, we can also consider a simple walk as a set of edges. 

▶ Definition 3.9. Let G be a graph and let W = ( v0, e 1, . . . , v k) be a simple walk in G starting at r. For each edge ei ∈ E(W ) we define the prefix of ei in W , denoted by pfx (W, r, e i), as the subwalk of W from r to vi; in other words, pfx (W, r, e i) = ( v0, e 1, . . . , e i, v i).

This definition can be used to define the prefix set of an edge in a given set of simple walks. 

▶ Definition 3.10. Let G be a graph and let F = {W1, . . . , W x} be a set of simple walks in G starting at r ∈ V (G). For each e ∈ E(G), we define the prefix set of e in F, denoted by pfx (F, r, e ), as the set of all prefixes for e among the walks in F that contain e. More formally: pfx (F, r, e ) := {pfx (Pi, r, e ) | i ∈ [x] ∧ e ∈ E(Pi)}.

If there is no walk in F that contains e, then pfx (F, r, e ) = ∅.

The main idea behind our definition of the prefix-sharing version of the problem is as follows. We want to modify the cost function so that if H1, . . . , H t is a set of trees that forms a solution to a power network design problem, the number of times we pay the cost of digging a trench at a particular edge e ∈ E(G) is equal to the number of different routes by which a tree Hi can arrive at edge e. Intuitively, this captures the idea that if the power network is installed starting at the root vertex, then digging costs are shared among different trees that place a cable in the same trench, as long as the work crews arrive at this edge by T. Hamm, B.M.P. Jansen and F. Motiei 15 

the same path from the root. This can be formalized as saying that the number of times the digging cost dig (e) has to be paid is equal to the size of the prefix set pfx (F, r, e ), when taking F to be the following family of paths: for each tree Hi that contains edge e, the set F

contains the path in Hi connecting e to the root r.When incorporating this concept into the definition of a power network design problem described in Section 1, a surprising behavior arises. To minimize the resulting cost function it might be beneficial for a single power cable Hi to be placed in a network of trenches that does not form a tree, but admits cycles among its trenches: the (seemingly superfluous) additional edges that form cycles can lead to a reduction in the number of different prefixes formed in combination with other power cables Hj (see Figure 2). In our formulation of the problem, we therefore allow cycles in the connected subgraph Hi that represents the trenches into which a single power cable is placed. To avoid short-circuits, the route by which electricity is transferred from the root to a terminal is still a simple path, but different terminals fed by the same power cable can use different paths through the (possibly cyclic) network of trenches that is dug. Using this intuition and the definitions above, the Prefix-Sharing Low-voltage Network Design problem can now be defined as follows. 

Prefix-Sharing Low-voltage Network Design (PLVND) Parameter: t + |T |

Instance: An undirected graph G with edge lengths ℓ : E(G) → N and digging costs 

dig : E(G) → N, a root vertex r ∈ V (G), a set of degree-1 terminal vertices T ⊆ V (G)

with demands d : T → N, and integers α, β, λ, and t, encoded in unary. 

Question: Does there exist a partition P of T into t sets T1, . . . , T t and a function 

f : T → 2E(G) that maps each terminal u ∈ T to a path in G from r to u such that: for all u ∈ T , the path P = f (u) satisfies ∑  

> e∈E(P)

ℓ(e) ≤ λ [depth constraint], 

∑

> u∈Ti

d(u) ≤ α for all i ∈ [t] [demand constraint], and cost PLV  

> r

(P, f ) ≤ β?[ cost constraint] The value cost PLV  

> r

(P, f ), referred to as PLV-cost, is defined as follows: cost PLV  

> r

(P, f ) := ∑ 

> T′∈P
> e∈E(G)

(ℓ(e) · | pfx (f (T ′), r, e )|) + ∑

> e∈E(G)

(

dig( e) ·

∣∣∣∣⋃ 

> T′∈P

pfx (f (T ′), r, e )

∣∣∣∣)

.

Contrasting the W[1]-hardness of Low-voltage Network Design and High-voltage Network Design , which we prove in Section 4, the Prefix-Sharing Low-voltage Network Design problem is in FPT. Theorem 3.11 discusses this algorithm. In fact we can employ the Dreyfus-Wagner dynamic-programming approach that solves the Steiner Tree 

problem to solve PLVND . For this algorithm, instead of guessing the abstract structure and using CSP for the realization of this abstract structure, which are the main bottlenecks of the previous algorithms, we branch on and fix the partition of terminals and use a dynamic-programming approach to check the existence of a desired solution. 

▶ Theorem 3.11. Prefix-Sharing Low-voltage Network Design can be solved in time 2|T |·O (log t) · | V (G)|2 · λ for any arbitrary input instance (G, ℓ, dig , r, T, d, α, β, λ, t ).

Proof. We find the solution to (G, ℓ, dig , r, T, d, α, β, λ, t ) using a dynamic-programming approach. As a preparatory step, we first branch out and fix the partition P of T into sets 16 Parameterized Complexity of Power Network Design 

r

v1 v2

v3

v4

v5 v6

t1

t2

t3 t4 t5 t6

t8

t7

(a) An instance of PLVND . The lengths of edges v1v3, v2v3 are 10 , and the lengths of rv 1,rv 2

are 20 . The digging cost of edges v4v5 and v4v6 is 1000 . The remaining lengths and digging costs are 1. The depth restriction λ is set to 33 . The demand values d are chosen via Lemma 2.3 such that the only possible partition P of terminals is {t1, t 3}, {t5, t 7}, {t2, t 4, t 6, t 8}.

r

v1 v2

v3

v4

v5 v6

t1

t2

t3 t4 t5 t6

t8

t7

(b) An acyclic solution for (a). In this solution, we pay for the digging cost of each edge other than v1v3 once, which gives a total digging cost of 2013 . We pay for the length of rv 1 twice, for rv 2, v2v3, v3v4 three times (once for each color), for v4v5 and v4v6 twice, and for the edges incident on terminals once. So, the total length cost is 2 × 20 + 3 × (20 + 10 + 1) + 2 × 2 + 8 = 145 .Thus, the total cost is 2013 + 145 = 2158 .

r

v1 v2

v3

v4

v5 v6

t1

t2

t3 t4 t5 t6

t8

t7

(c) A cyclic solution to (a) of smaller cost. Note that the red subgraph contains the cycle 

(r, v 1, v 3, v 2). In this solution, we pay the dig-ging cost of all edges except v3v4 once, and those of v3v4 twice. So, the total digging cost of this solution is 2014 . We pay for the length of the edges around the cycle rv 1v3v2 twice, for v3v4

four times consisting of 2 reds and once for blue and green, for v4v5 and v4v6 twice, and for the edges incident on terminals once. So, the total length is 2 × 60 + 4 × 1 + 2 × 2 + 8 × 1 = 136 

which yields a total cost of 2150 .

Figure 2 Illustration of the PLVND problem and the possibility of cyclic solution structures. (a) An instance of PLVND . (b),(c) an acyclic solution, and optimal and cyclic solution respectively. In these solutions, edges of the same color form solution paths to terminals belonging to the same part of P. The edge styles distinguish the different root-terminal paths. One can argue that for the instance in (a), all optimal solutions are such that the union of the paths to t4 and t6 contain a cycle, as follows. Due to the depth restriction, the paths for t1, t 2, t 7, t 8 are unique. If the subgraph that covers t2, t 4, t 6, t 8 is acyclic, then at least one of v1v3 and v2v3 is excluded from the corresponding paths and at least one of them must be used for connectivity to t4 and t6. So, exactly one of these edges belongs to the paths to t4 and t6. If the paths to t3, t 4, t 5, t 6 do not pass through the same edge among {v1v3, v 2v3}, the solution will have a digging cost larger than 3000 . Otherwise, all must pass through the same edge which results in a suboptimal solution similar to (b). T. Hamm, B.M.P. Jansen and F. Motiei 17 

T1, . . . , T t such that the demand constraint is satisfied, i.e. ∑

> u∈Ti

d(u) ≤ α for all i ∈ [t]. Then in the next step, the goal is to check the existence of a function f : T → 2E(G) that maps each terminal u ∈ T to a path in G from r to u that has ℓ-length at most λ and cost PLV  

> r

(P, f ) ≤ β.This existence is checked through a dynamic-programming approach. For any subset T ′ ⊆ T ,we denote { Ti ∩ T ′ | Ti ∈ P} \ {∅} by P ⋒ T ′. For each subset T ′ ⊆ T , v ∈ V (G), and each integer value λ′ such that 0 ≤ λ′ ≤ λ, let dP [T ′, v, λ ′] be defined as the minimum value of cost PLV  

> v

(P ⋒ T ′, f ′) among all possible functions f ′ : T ′ → 2E(G) that map each terminal 

u ∈ T ′ to a path in G from v to u with ℓ-depth at most λ′. If no such set exists, then dP [T ′, v, λ ′] = + ∞. To avoid several case distinctions, we also define dP [T ′, v, λ ′] as +∞

for every λ′ < 0. Before discussing how we compute dP [T ′, v, λ ′], we present a claim that provides a structural tool. This tool helps us to combine the solutions to subproblems to construct a solution for d P [T ′, v, λ ′].

▷ Claim 3.12. Let λ′ be a positive integer. For a given vertex v ∈ V (G) and any arbitrary function f : T ′ → 2E(G) that maps each terminal u ∈ T ′ to a simple walk in G from v to 

u such that ℓ(f (u)) ≤ λ′, there exists a function f ′ : T ′ → 2E(G) that maps each terminal 

u ∈ T ′ to a path in G from v to u such that ℓ(f (u)) ≤ λ′ and cost PLV  

> v

(P, f ′) ≤ cost PLV  

> v

(P, f ).

Proof. Let f ′ = f , initially. While there exists a terminal u ∈ T ′ such that f ′(u) contains a cycle C, do the following. For all u′ ∈ T ′ (including u), if f ′(u′) contains C, remove C from 

f ′(u′). Clearly, the remaining part of the walk is still a walk from v to u′.Now, we verify that the conditions of the claim are maintained after each such step. Let f ′ 

> prev

be the function f ′ before one such step and f ′ 

> new

be the function f ′ after that step. The depth constraint holds since for each u′ ∈ T ′, we only removed some edges from f ′

> prev

(u′) to reach f ′

> new

(u′) and so ℓ(f ′

> new

(u′)) ≤ ℓ(f ′

> prev

(u′)) . To show that cost PLV  

> v

(P, f ′

> new

) ≤ cost PLV  

> v

(P, f ′

> prev

), we show that for each e ∈ E(G) and T ′′ ∈ P ⋒ T ′,we have |pfx (f ′

> new

(T ′′ ), v, e )| ≤ | pfx (f ′

> prev

(T ′′ ), v, e )| and 

∣∣∣∣⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> new

(T ′′ ), v, e )

∣∣∣∣ ≤

∣∣∣∣⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> prev

(T ′′ ), v, e )

∣∣∣∣.For the latter part, we present an injective function g from ⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> new

(T ′′ ), v, e )

to ⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> prev

(T ′′ ), v, e ). For each sequence S ∈ ⋃ 

> T′′ ∈P

pfx (f ′

> new

(T ′′ ), v, e ), if S is also in ⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> prev

(T ′′ ), v, e ), then g(S) = S. Otherwise, consider a terminal u ∈ T ′ such that pfx (f ′

> new

(u), v, e ) = S. Let g(S) = f ′

> prev

(u). Since S / ∈ ⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> prev

(T ′′ ), v, e ),we have (pfx (f ′

> prev

(u), v, e ) = S′̸ = S. Thus, f ′

> prev

(u) is a path that contains the cycle C

and this cycle also belongs to pfx (f ′

> prev

(u), v, e ). On the other hand, since no sequence in ⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> prev

(T ′′ ), v, e ) contains C, for all sequences that g(S) = S, we know that 

S does not contain C and so, g(S) is distinct from the value of g for sequences not in 

⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> prev

(T ′′ ), v, e ). Therefore, to show that g is injective, it is sufficient to show that g(S) is distinct for all sequences like S that are not in ⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> prev

(T ′′ ), v, e ).This is true since by the construction, for each such sequence S, the sequence S should be g(S) where C is removed from it. Since g(S) is a walk, removing C from g(S) is unique and cannot result in two different sequences. Therefore, g is injective and so, ∣∣∣∣⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> new

(T ′′ ), v, e )

∣∣∣∣ ≤

∣∣∣∣⋃  

> T′′ ∈P ⋒T′

pfx (f ′

> prev

(T ′′ ), v, e )

∣∣∣∣.We can define an injective function gT ′′ from pfx (f ′

> new

(T ′′ ), v, e ) to pfx (f ′

> prev

(T ′′ ), v, e )18 Parameterized Complexity of Power Network Design 

for each T ′′ ∈ P ⋒ T ′ and show that it is injective in the same way. This yields that 

|pfx (f ′

> new

(T ′′ ), v, e )| ≤ | pfx (f ′

> prev

(T ′′ ), v, e )| for each T ′ ∈ P . Therefore, we have cost PLV  

> v

(P, f ′

> new

) ≤

cost PLV  

> v

(P, f ′

> prev

).Since in each step the total number of edges in the walks is decreased, after at most 

|E(G)| · | T | steps, we get rid of all cycles and so repeated vertices in all walks and obtain a function that maps the terminals to paths and satisfies the conditions of the claim. ◀

We compute d P [T ′, v, λ ′] as follows. 

▷ Claim 3.13. For a fixed partition P of T into sets T1, . . . , T t, the values of dP [T ′, v, λ ′]

satisfy the following recurrence. dP [T ′, v, λ ′] = 



0, if T ′ ⊆ { v},

min   

> T′′ ⊂T′
> v′∈NG(v)

(

dP [T ′ \ T ′′ , v, λ ′] + dP [T ′′ , v ′, λ ′ − ℓ(vv ′)] + ℓ(vv ′) · ∣∣P ⋒ T ′′ ∣∣ + dig( vv ′)

)

,

otherwise .

Proof. We discuss this claim for each part of the recurrence separately. 

T ′ = ∅. Since T ′ = 0 , the domain of f ′ is empty and so, the PLV-cost is 0.

T ′ = {v}. In this case, if f ′ : T ′ → 2E(G) is the corresponding function to a solution of minimum PLV-cost, f ′(v) is the single vertex v which results in a PLV-cost of 0 which is best-possible. 

Otherwise. We show the correctness of this case by showing it in two directions: The left-hand side is greater than or equal to the right-hand side, and vice versa. 

Left ≤ Right: Consider an arbitrary choice of T ′′ ⊂ T ′ and v′ ∈ NG(v). Let g : T ′′ →

2E(G) be a function such that dP [T ′′ , v ′, λ ′ − ℓ(vv ′)] = cost PLV  

> v′

(P ⋒ T ′′ , g ) and g′ :

T ′ \ T ′′ → 2E(G) be a function such that dP [T ′ \ T ′′ , v, λ ′] = cost PLV  

> v

(P ⋒ (T ′ \ T ′′ ), g ′).Let g′′ : T ′ → 2E(G) be defined as follows for each u ∈ T ′.If u ∈ T ′′ and the next vertex after v′ in g(u) is not v, let g′′ (u) = g(u) ∪ { vv ′}.If u ∈ T ′′ and the next vertex after v′ in g(u) is v, let g′′ (u) be the subpath of g(u)

from v to u.If u ∈ T ′ \ T ′′ , let g′′ (u) = g′(u).For the third case, clearly g′′ (u) is a path in G from v to u of length at most λ′ as g′ is a solution to dP [T ′ \ T ′′ , v, λ ′]. This is also true for the second case as we used a subpath of g(u). Furthermore, for the first case, g(u) is a path from v′ to u of length at most 

λ′ − ℓ(vv ′) since g is a solution to dP [T ′′ , v ′, λ ′ − ℓ(vv ′)] . Since v′ is an endpoint of this path and v′ ∈ NG(v), by adding edge vv ′, we are extending this path to obtain a walk from v to u of length at most λ′ in G. Note that this walk is simple as g(u) is a path and does not contain any repeated edges and does not contain the edge vv ′ as otherwise 

v is the neighbor of v′ in g(u). Therefore, g′′ is a function that maps each terminal u

of T ′ to a simple walk of length at most λ′ in G that is from v to u. We can rewrite cost PLV  

> v

(P ⋒ T ′, g ′′ ) as 

∑ 

> S∈P ⋒T′
> e∈E(G)

(ℓ(e) · | pfx (g′′ (S), v, e )|) + ∑

> e∈E(G)

(

dig( e) ·

∣∣∣∣∣⋃ 

> S∈P ⋒T′

pfx (g′′ (S), v, e )

∣∣∣∣∣)

.T. Hamm, B.M.P. Jansen and F. Motiei 19 

This yields that cost PLV  

> v

(P ⋒ T ′, g ′′ ) is at most 

∑ 

> S∈P ⋒T′
> e∈E(G)

ℓ(e) · (|pfx (g′′ (S ∩ T ′′ ), v, e )| + |pfx (g′′ (S ∩ (T ′ \ T ′′ )) , v, e )|) (1) 

+ ∑

> e∈E(G)

dig( e) ·

∣∣∣∣∣⋃ 

> S∈P ⋒T′′

pfx (g′′ (S), v, e )

∣∣∣∣∣ +

∣∣∣∣∣∣⋃  

> S∈P ⋒(T′\T′′ )

pfx (g′′ (S), v, e )) 

∣∣∣∣∣∣ .

Recalling that for each S ∈ P ⋒T ′ and each edge e ∈ E(G), if S ∩T ′′ ̸ = ∅, the simple walks in pfx (g′′ (S ∩T ′′ ), v, e ) consist of either the single edge vv ′ or a path in pfx (g(S ∩T ′′ ), v ′, e )

extended by vv ′, we can rewrite |pfx (g′′ (S∩T ′′ ), v, e )| as |pfx (g(S∩T ′′ ), v ′, e )| if e̸ = vv ′ and as |pfx (g(S ∩ T ′′ ), v ′, e )| + 1 if e = vv ′. Clearly, if S ∩ T ′′ = ∅, then pfx (g′′ (S ∩ T ′′ ), v, e ) = 

pfx (g(S ∩ T ′′ ), v ′, e ) = ∅. Moreover, for each S ∈ P ⋒ T ′ and each edge e ∈ E(G), we have pfx (g′′ (S ∩ (T ′ \ T ′′ )) , v, e ) = pfx (g′(S ∩ (T ′ \ T ′′ )) , v, e ) since g and g′ have the same value for all terminals in T ′ \ T ′′ . On the other hand, for each edge e ∈ E(G), the walks in ⋃ 

> S∈P ⋒T′′

pfx (g′′ (S), v, e ) are consisting of either the single edge vv ′ or a path in 

⋃ 

> S∈P ⋒T′′

pfx (g(S), v ′, e ) extended by vv ′ and hence 

∣∣∣∣∣⋃ 

> S∈P ⋒T′′

pfx (g′′ (S), v, e )

∣∣∣∣∣ =

∣∣∣∣⋃ 

> S∈P ⋒T′′

pfx (g(S), v ′, e )

∣∣∣∣ , e̸ = vv ′,

∣∣∣∣⋃ 

> S∈P ⋒T′′

pfx (g(S), v ′, e )

∣∣∣∣ + 1 , otherwise .

Also, for each edge e ∈ E(G), we have 

⋃  

> S∈P ⋒(T′\T′′ )

pfx (g′′ (S), v, e )) = ⋃  

> S∈P ⋒(T′\T′′ )

pfx (g′(S), v, e )) .

Note that we need to pay once more for the digging cost of vv ′ as it is added to the beginning of some paths. Putting all the above together, we can conclude that (1) is equal to 

∑ 

> S∈P ⋒T′′
> e∈E(G)

(ℓ(e) · | pfx (g(S), v ′, e )|) + ∑  

> S∈P ⋒(T′\T′′ )
> e∈E(G)

(ℓ(e) · | pfx (g′(S), v, e )|)+ ℓ(vv ′) · |P ⋒ T ′′ |

+ ∑

> e∈E(G)

dig( e) ·

∣∣∣∣∣⋃ 

> S∈P ⋒T′′

pfx (g(S), v ′, e )

∣∣∣∣∣ +

∣∣∣∣∣∣⋃  

> S∈P ⋒(T′\T′′ )

pfx (g′(S), v, e )) 

∣∣∣∣∣∣

+ dig( vv ′)= cost PLV  

> v′

(P ⋒ T ′′ , g ) + cost PLV  

> v

(P ⋒ (T ′ \ T ′′ ), g ′) + ℓ(vv ′) · |P ⋒ T ′′ | + dig( vv ′)= dP [T ′′ , v ′, λ ′ − ℓ(vv ′)] + dP [T ′ \ T ′′ , v, λ ′] + ℓ(vv ′) · |P ⋒ T ′′ | + dig( vv ′).

By Claim 3.12, we can find a function g′′′ that maps paths to the terminals and is a candidate for a solution to dP [T ′, v, λ ′] and hence we have dP [T ′, v, λ ′] ≤ cost PLV  

> v

(P ⋒

T ′, g ′′′ ) ≤ cost PLV  

> v

(P ⋒ T ′, g ′′ ). Thus, we have dP [T ′, v, λ ′] ≤ cost PLV  

> v

(P ⋒ T ′, g ′′ )

≤ dP [T ′′ , v ′, λ ′ − ℓ(vv ′)] + dP [T ′ \ T ′′ , v, λ ′] + ℓ(vv ′) · |P ⋒ T ′′ | + dig( vv ′),20 Parameterized Complexity of Power Network Design 

and since T ′′ ⊂ T ′ and v′ ∈ NG(v) are chosen arbitrarily, we can conclude the first inequality. 

Left ≥ Right: Let g : T ′ → 2E(G) be a function such that dP [T ′, v, λ ′] = cost PLV  

> v

(P ⋒

T ′, g ). Consider an arbitrary terminal u ∈ T ′ and let v′ be the neighbor of v in g(u). We define Tv′ as the set of all terminals u′ ∈ T ′ such that vv ′ appears as the first edge in 

g(u′). We define g′ : Tv′ → 2E(G) and g′′ : (T ′ \ Tv′ ) → 2E(G) as follows. For each u′ ∈ Tv′ ,let g′(u′) be the path g(u′) where the edge vv ′ is removed; Thus, it is a path from v′ to 

u′ with length at most λ′ − ℓ(vv ′). Additionally, for all u′′ ∈ T ′ \ Tv′ , let g′′ (u′) = g(u′′ )

which is a path from v to u′ of length at most λ′. Since the paths of g(Tv′ ) start with 

vv ′ and the other paths of g start with other edges, the prefixes arising from the ones in g(Tv′ ) and the ones not in g(Tv′ ) for all edges are distinct. Thus, we can rewrite dP [T ′, v, λ ′] = cost PLV  

> v

(P ⋒ T ′, g ) as 

∑  

> T′′ ∈P ⋒T′
> e∈E(G)

(ℓ(e) · | pfx (g(T ′′ ), v, e )|) + ∑

> e∈E(G)

(

dig( e) ·

∣∣∣∣∣⋃ 

> T′′ ∈P

pfx (g(T ′′ ), v, e )

∣∣∣∣∣)

= ∑ 

> T′′ ∈P ⋒Tv′
> e∈E(G)

(ℓ(e) · | pfx (g(T ′′ ), v, e )|)+ ∑   

> T′′ ∈P ⋒(T′\Tv′)
> e∈E(G)

(ℓ(e) · | pfx (g(T ′′ ), v, e )|)+ ∑

> e∈E(G)

dig( e) ·

∣∣∣∣∣∣⋃ 

> T′′ ∈P ⋒Tv′

pfx (g(T ′′ ), v, e )

∣∣∣∣∣∣

+ ∑

> e∈E(G)

dig( e) ·

∣∣∣∣∣∣⋃   

> T′′ ∈P ⋒(T′\Tv′)

pfx (g(T ′′ ), v, e )

∣∣∣∣∣∣ .

If e = vv ′, then pfx (g(Tv′ ∩ T ′′ ), v, e ) consists of pfx (g′(Tv′ ∩ T ′′ ), v ′, e ) where each is extended by vv ′ and the single edge vv ′ and otherwise, pfx (g(Tv′ ∩ T ′′ ), v, e ) is equal to pfx (g′′ (Tv′ ∩ T ′′ ), v, e ) where each is extended by vv ′. Therefore, we have 

∑ 

> T′′ ∈P ⋒Tv′
> e∈E(G)

(ℓ(e) · | pfx (g(T ′′ ), v, e )|) + ∑   

> T′′ ∈P ⋒(T′\Tv′)
> e∈E(G)

(ℓ(e) · | pfx (g(T ′′ ), v, e )|)= ∑ 

> T′′ ∈P ⋒Tv′
> e∈E(G)

(ℓ(e) · | pfx (g′(T ′′ ), v ′, e )|) + ℓ(vv ′) · |P ⋒ Tv′ |

+ ∑   

> T′′ ∈P ⋒(T′\Tv′)
> e∈E(G)

(ℓ(e) · | pfx (g′′ (T ′′ ), v, e )|) ,T. Hamm, B.M.P. Jansen and F. Motiei 21 

and 

∑

> e∈E(G)

dig( e) ·

∣∣∣∣∣∣⋃ 

> T′′ ∈P ⋒Tv′

pfx (g(T ′′ ), v, e )

∣∣∣∣∣∣

+ ∑

> e∈E(G)

dig( e) ·

∣∣∣∣∣∣⋃   

> T′′ ∈P ⋒(T′\Tv′)

pfx (g(T ′′ ), v, e )

∣∣∣∣∣∣

= ∑

> e∈E(G)

dig( e) ·

∣∣∣∣∣∣⋃ 

> T′′ ∈P ⋒Tv′

pfx (g′(T ′′ ), v ′, e )

∣∣∣∣∣∣ + dig( vv ′)+ ∑

> e∈E(G)

dig( e) ·

∣∣∣∣∣∣⋃   

> T′′ ∈P ⋒(T′\Tv′)

pfx (g′′ (T ′′ ), v, e )

∣∣∣∣∣∣ .

This implies that cost PLV  

> v

(P ⋒ T ′, g ) can be written as cost PLV  

> v′

(P ⋒ Tv′ , g ′) + cost PLV  

> v

(P ⋒ (T ′ \ Tv′ ), g ′′ ) + ℓ(vv ′) · |P ⋒ Tv′ | + dig( vv ′).

Clearly, g′ is a candidate for a solution to dP [Tv′ , v ′, λ ′ − ℓ(vv ′)] and g′′ is a candidate for a solution to d P [T ′ \ Tv′ , v, λ ′]. Therefore, we have cost PLV  

> v′

(P ⋒ Tv′ , g ′) + cost PLV  

> v

(P ⋒ (T ′ \ Tv′ ), g ′′ ) + ℓ(vv ′) · |P ⋒ Tv′ | + dig( vv ′)

≥ dP [Tv′ , v ′, λ ′ − ℓ(vv ′)] + dP [T ′ \ Tv′ , v, λ ′] + ℓ(vv ′) · |P ⋒ Tv′ | + dig( vv ′).

Considering that Tv′ is a possible value of T ′′ , we can conclude that dP [T ′, v, λ ′] = cost PLV  

> v

(P ⋒ T ′, g )

≥ dP [Tv′ , v ′, λ ′ − ℓ(vv ′)] + dP [T ′ \ Tv′ , v, λ ′] + ℓ(vv ′) · |P ⋒ Tv′ | + dig( vv ′)

≥ min   

> T′′ ⊂T′
> v′∈NG(v)

(

dP [T ′ \ T ′′ , v, λ ′] + dP [T ′′ , v ′, λ ′ − ℓ(vv ′)] + ℓ(vv ′) · ∣∣P ⋒ T ′′ ∣∣ + dig( vv ′)

)

.

This implies the other direction and hence the claim. ◀

The algorithm can be described as follows. 1. For each partition P of T into t sets T1, . . . , T t,a. If for some i ∈ [t], ∑

> u∈Ti

d(u) > α , skip this branch. b. Compute d P [T, r, λ ] using Claim 3.13. If d P [T, r, λ ] ≤ β, then return ‘yes’. 2. If none of the cases resulted in a ‘yes’-output, output ‘no’. We show the correctness of this algorithm and afterward, we its running time analysis. 

Correctness. 

Let P′ and f ′ be a solution for (G, ℓ, dig , r, T, d, α, β, λ, t ) that has the minimum PLV-cost. Clearly, if cost PLV  

> r

(P′, f ′) ≤ β, the answer to this input instance is ‘yes’ and otherwise, the answer is ‘no’. First, assume that the answer is ‘yes’. Since we got through all possible partitions in line 1, at some point the algorithm takes P = P′. Then, since P′ captures the demand 22 Parameterized Complexity of Power Network Design 

constraint it passes the check of line 1.a. Since this is a solution with minimum PLV-cost, the value of dP′ [T, r, λ ] should be cost PLV  

> r

(P′, f ′) by definition. Therefore, line 2.b returns ‘yes’. Conversely, if the answer is ‘no’, we know that for all partitions P and all functions f ,we have cost PLV  

> r

(P, f ) > β . Therefore, for each choice of P, the value of dP′ [T, r, λ ] is more than β by the definition of dP′ [T, r, λ ]. Therefore, the algorithm does not return in line 2.b and proceeds with the rest, reaching line 2 which returns ‘no’. 

Running Time Analysis. 

There are t|T | possible options for P in step 1. Then, checking the step 1.a of the algorithm can be done in O(|T |). The computation of dP [T ′, v, λ ′] in step 2.b takes |T | · | V (G)| · λ ·

2|T | · | V (G)| · | T |2. Step 2 can be done in constant time. Therefore, the total running time is 

2|T |·O (log t) · | V (G)|2 · λ. ◀

## 4 Hardness results 

For our hardness results, we provide reductions from Grid Tiling , which is defined as follows. 

Grid Tiling Parameter: k

Instance: A k × k matrix where each cell (i, j ) ∈ [k]2 contains a set of pairs Si,j ⊆ [n] × [n].

Question: Can we choose a pair si,j ∈ Si,j for each cell such that if si,j = ( a, b ) and si,j ′ = ( a′, b ′), then a = a′; and if si,j = ( a, b ) and si′,j = ( a′, b ′), then b = b′?

Grid Tiling cannot be solved in time f (k) · no(k) under the ETH and is W[1]-hard [9]. 

## 4.1 W[1]-hardness of Low-voltage Network Design on planar graphs 

In our first reduction, we show that the algorithm from Theorem 3.7 is ETH-tight. 

▶ Theorem 4.1. The Low-voltage Network Design problem is W[1]-hard for t = 2 and under the ETH admits no f (|T |, t ) · (|V (G)| · λ)f ′(t)·o(|T |)-time algorithm for any computable functions f and f ′, even when restricted to planar input graphs. 

Proof. From a Grid Tiling instance (Si,j )(i,j )∈[k]2 , construct an LVND -instance as follows. 

G, r and T

Let G0 be a grid graph with N := k · n rows and M := k · (k + 1) · n columns. For notational convenience, for i ∈ [N ] and j ∈ [M ], we denote the vertex in the i-th row and j-th column of G0 by v[i, j ]. For i ∈ [N ] and j ∈ [M − 1] , we denote the edge v[i, j ]v[i, j + 1] by e[i, j ].We define G by adding the following vertices to G0:A vertex rcons which we call row consistency root and make adjacent to all v[i, 1] with 

i ∈ [N ] and to a new terminal p(rcons ) as pendant. A vertex rselect which we call selection root and make adjacent to all v[1 , (j − 1)( k + 1) + 1] 

with j ∈ [k · n] and to a new terminal p(rselect ) as pendant. T. Hamm, B.M.P. Jansen and F. Motiei 23 

The root vertex r of the LVND -instance which we make adjacent to rcons and rselect .For each i ∈ [k],a vertex ti called a row consistency preterminal (for value of row i) and connect it to 

v[i′, M ] for all i′ ∈ { (i − 1) · n + 1 , . . . , i · n} and to a new terminal p(ti) as pendant. a vertex t′ 

> i

called a selection preterminal (for value of column i) and make adjacent to 

v[N, (i − 1) n(k + 1) + i′(k + 1)] for all i′ ∈ [n] and to a new terminal p(t′

> i

) as pendant. Up to the definition of lengths, digging costs and demands this concludes the description of all parts of the input pertaining to G. By construction G is planar; an embedding is indicated in Figure 3.                   

> C1,1C1,2. . . C1,n C2,1. . . C2,n n·k
> k+ 1
> Ck, 1. . . Ck,n
> R1
> ...
> Rk
> rcons
> rselect r
> t1
> tk
> v[N, M ]
> v[1 , n (k+ 1) + 1]
> t′
> 1t′
> 2
> v[N, (n+ 1)( k+ 1)]
> t′
> k

Figure 3 Illustration of the graph G in the proof of Theorem 4.1. Terminals are shown as squares; only selected vertices of G0 and their indices are indicated explicitly. The hatched rectangle represents G0, with v[1 , 1] at the bottom left and v[N, M ] at the top right corner. A more detailed view of a part of the construction using an explicit example is given in Figure 5. 

t, d and α. We fix t = 2 , i.e. a hypothetical solution for the LVND -instance under construc-tion consists of two trees H1 and H2. Consider the following partition P of terminals into two parts: 1) p(rcons ) along with all row consistency terminals, and 2) p(rselect ) along with all selec-tion preterminals; more formally, P = {{ p(rcons ), p (t1), . . . , p (tk)}, {p(rselect ), p (t′

> 1

), . . . , p (t′

> k

)}} .By Lemma 2.3, there exists a function f : T → [|U |4] such that P is the unique partition of terminals into at most two parts for which the summation of f -values in each part is at most 

∑  

> u∈Tf(u)2

. Since the demand values are integers, by setting the demand function d = f and 

α = ⌊ ∑ 

> u∈Td(u)2

⌋, the aim is to find two trees H1, H 2 where H1 contains the first part and H2

contains the second part of the terminals. 

Reduction overview 

Before fixing the parts of the LVND -instance that still need to be specified, we provide some intuition about what we will try to achieve, along with some useful notation for specific parts of the construction. The lengths of the edges, in particular between r and rcons and r and rselect , and β will be chosen in such a way that each terminal-root-path in H1 passes through rcons and each terminal-root-path in H2 passes through rselect .

λ will be set in such a way that these paths in H2 will have to be of the minimum possible 24 Parameterized Complexity of Power Network Design 

length. This will constrain each t′ 

> j

-rselect -path to be contained in exactly one of the n subgrids (see Figure 3) of the form 

Cj,j ′′ := G0[{v[i, j ′] | i ∈ [N ] ∧ ∃ z ∈ [k + 1] j′ = ( j − 1)( k + 1) n + ( j′′ − 1)( k + 1) + z}]

for some j′′ ∈ [n] and contain precisely k horizontal edges. This allows us to encode a choice of the second entry for the j-th column of the Grid Tiling instance through j′′ such that the rselect -t′ 

> j

-path in H2 is contained in Cj,j ′′ . Similarly, we will be able to assume that each ti-rcons -path traverses exactly one of the n rows in the subgrid Ri := G0[{vi′,j | i′ ∈{(i − 1) n + 1 , . . . , i · n, j ∈ [M ]}]. This allows us to encode a choice of the first entry of the pairs selected for the i-th row of the Grid Tiling instance through the index of the row whose vertices are on the rcons -ti-path in H1.We need to ensure that the implied encoding of a pair (a, b ) for each cell (i, j ) ∈ [k]2 satisfies that (a, b ) ∈ Si,j . For this we set the digging costs in a way that each ti-rcons -path in H1

shares a (by the assumed structure of paths in H1 necessarily horizontal) edge with each 

t′ 

> j

-rselect -path in H2 and we set the lengths of a horizontal edge prohibitively large for the depth requirements of H2 whenever it is between the j′ and (j′ + 1) -th column in Cj,j ′′ ,on the i′-th row in Ri, and (i′, j ′′ ) /∈ Si,j – thereby disallowing pairs that are not in the appropriate set Si,j – or j′̸ = i – thereby ensuring that we check the compatibility of the selected second entry with the selected row for each i ∈ [k]. We now continue with the formal construction of the LVND -instance to implement the described intended behavior. 

ℓ, dig , λ and β To specify the edge lengths, we distinguish some horizontal edges. In particular, we will set the lengths of some horizontal edges to be higher ( k + 1 ) than that of others ( 1) and refer to the longer edges (see Figure 4a) as blocking :For (j, j ′′ ) ∈ ([ k] × [n]) \ { (k, n )}, we call horizontal edges that connect Cj,j ′′ to Cj,j ′′ +1 

if j′′ < n and to Cj+1 ,1 otherwise column inconsistency blocking . I.e. the set of column inconsistency blocking edges is given by 

{e ∈ e[i, (j − 1) n(k + 1) + j′′ (k + 1)] | i ∈ [N ], j ∈ [k], j ′′ ∈ [n]}.

For j, j ′ ∈ [k], and j′′ ∈ [n], we call edges between vertices in the j′-th and (j′ + 1) -th column in Cj,j ′′ invalid selection blocking if they are not in Rj′ or they are in the i′-th row of Rj′ for some i′ ∈ [n] for which (i′, j ′′ ) /∈ Sj′,j . I.e. the set of invalid selection blocking edges is given by 

{e = eCj,j ′′ [i∗, j ′] |j, j ′ ∈ [k], j ′′ ∈ [n], i ∗ ∈ [N ]∧∃i ∈ [k]∃i′ ∈ [n]∃j∗ ∈ [M ] e = eRi [i′, j ∗] ∧ (( i′, j ′′ ) /∈ Si,j ∨ j′̸ = i)}.

Horizontal edges of G0 that are not blocking, i.e. edges between vertices in the j′-th and 

(j′ + 1) -th column in Cj,j ′′ for some j, j ′ ∈ [k], and j′′ ∈ [n] for which ∃i′ ∈ [n]∃j∗ ∈ [M ] e =

eRj′ [i′, j ∗] ∧ (i′, j ′′ ) ∈ Sj′,j , receive length 1. There are at most kn such edges per row. See Figure 5 for a specific example of blocking edges and an indication of their intended role. The edges with precisely one endpoint in V (G0) receive length X := ∑ 

> e∈E(G0)

2ℓ(e) + dig (e) + 1 each. (While we have not specified all lengths and the digging costs of edges in 

E(G0) yet, they will not depend on X.) The edges between r and rcons and between r and 

rselect receive length X∗ each, where X∗ denotes the sum of the length and digging costs of all edges other than rr select , rr cons , and edges incident to terminals. The edges rcons p(rcons ) and 

rselect p(rselect ) receive length λ − X∗, with λ as defined below. For i ∈ [k], the edge t′

> i

p(t′

> i

)T. Hamm, B.M.P. Jansen and F. Motiei 25 

rcons 

rselect r

t2

t1

t′ 

> 1

t′

> 2

(a) The LVND -instance corresponding to a Grid Tiling instance with n = 3 , k = 2 , S1,1 =

{(2 , 1) , (3 , 2) }, S1,2 = {(2 , 3) , (3 , 1) }, S2,1 = {(1 , 1) }, and S2,2 = {(1 , 3) , (2 , 2) }. Equal colors and edge styles indicate equal lengths. Dotted and dashed edges represent column inconsistency and invalid selections and are too long to appear in an rselect -t′

> 1

-path in H2. The purple edges are the only length-1 edges. These are the only horizontal edges that can appear in such a path. 

rcons 

rselect r

t2

t1

t′ 

> 1

t′

> 2

(b) A solution for the instance in (a). Red and blue denote the two trees; purple edges are shared and indicate the selected cell pairs s1,1 = (2 , 1) , s1,2 = (2 , 3) , s2,1 = (1 , 1) , and s2,2 = (1 , 3) .

rcons 

rselect r

t2

t1

t′ 

> 1

t′

> 2

(c) Illustrating why the construction fails for HVND when λ = + ∞. The red and blue trees share purple edges and achieve lower cost than the solution in Figure 4b. 

Figure 4 

receives length 2 and the edge tip(ti) receives length λ − X∗ − 2X − (M − 1)( k + 1) . The length of all vertical edges is set to 2k2n(k + 1) + 1 .The digging cost of horizontal edges is nk 2(k + 1) , and the digging cost of all other edges is 0. Finally, we set λ = X∗ + 2 X + 2 + ( N − 1)(2 k2n(k + 1) + 1) + k. This will constrain each root-terminal path in H2 to be of minimum length. The total cost threshold β is the sum of k-times this minimum length minus X∗ (one for each path to a selection terminal), 

k-times λ minus X∗ (one for each path to a row consistency terminal), two times λ to connect 

p(rcons ) via rcons and p(rselect ) via rselect , and lastly the digging cost necessarily incurred by 

k horizontal rows of G0. An alternative way of expressing β is as the sum of: 26 Parameterized Complexity of Power Network Design 

rselect 

t′

> 1

Figure 5 Example of G[{rselect , t ′

> 1

} ∪ V (C1,1) ∪ V (C1,2) ∪ V (C1,3)] for n = 3 , k = 2 , S1,1 =

{(1 , 1) , (2 , 3) }, S2,1 = {(1 , 1) , (2 , 1) }, i.e. the part of G encoding a selection of pairs in the first column of the Grid Tiling instance. Equal colors and drawing styles of edges indicate equal lengths. Column inconsistency and invalid selection blocking edges are dotted and dashed, respectively. Their lengths will be too high to be included in an rselect -t′

> 1

-path in H2. The bold purple edges are the only horizontal edges that can be included in such a path. We can see that in this example this leaves us with two possible rselect -t′

> 1

-paths; one corresponding to selecting (1 , 1) ∈ S1,1 and (1 , 1) ∈ S2,1 and the other corresponding to (1 , 1) ∈ S1,1 and (2 , 1) ∈ S2,1.

2λ for the cumulative length of rr select ,rselect p(rselect ), rr cons and rcons p(rcons ).

k(λ − X∗) for the paths to each row consistency terminal without their shared first edge. 

2kX + 2 k corresponding to the cumulative length of the edges between each selection pre-terminal and its terminal pendant, one length-X edge between each selection preterminal and G0, an one length-X edge between rselect and G0 for each selection preterminal. 

k · (( N − 1)(2 k2n(k + 1) + 1) + k) corresponding to the minimum length of k edge-disjoint paths that each traverse all rows of G0 while each using exactly k horizontal edges of length one. (Note that summing this item with the previous gives k(λ − X∗).) 

k · (M − 1)( nk 2(k + 1)) corresponding to the digging cost of one horizontal edge per pair of adjacent columns in G0 per consistency preterminal. This completes the LVND -instance. We will show that (Si,j )(i,j )∈[k]2 and I := ( G, ℓ, dig , r, T, d, α, β, λ, t ) are equivalent. 

▷ Claim 4.2. If (Si,j )(i,j )∈[k]2 is a yes-instance then I is a yes-instance. 

Proof. Fix a solution (( ai, b j )) (i,j )∈[k]2 for (Si,j )(i,j )∈[k]2 .We will show that H1 and H2 defined as follows are a solution for I. Let H1 be the tree induced on the endpoints of edges rr cons , rcons p(rcons ), for each i ∈ [k] the edges in the 

ai-th row in Ri, and tip(ti). Let H2 be the tree induced on the endpoints of edges rr select ,

rselect p(rselect ), for each j ∈ [k] the edges on the unique path in Cj,b j from a neighbor of rselect 

to a neighbor of t′ 

> j

whose only horizontal edges are those that go from column i to i + 1 in 

Cj,b j and are on the ai-th row of Ri, and t′ 

> j

p(t′ 

> j

).It is straightforward to verify that H1 and H2 are indeed trees that cover all of T .Moreover, by construction each of H1 and H2 contains terminals with demand precisely α.When considering the depth of H1, the root-distance of p(rcons ) is precisely λ, and for each i ∈ [k], the r-p(ti)-path has length at most λ − (M − 1)( k + 1) + ( M − 1 − k)( k + 1) + k,because the ai-th row of Ri contains at least k edges which are of length 1, namely those that are between the i and (i + 1) -th column of Cj,b j for each j ∈ [k]. This is smaller than λ.T. Hamm, B.M.P. Jansen and F. Motiei 27 

When considering the depth of H2, the root-distance of p(rselect ) is precisely λ, and for each j ∈ [k] the r-p(t′ 

> j

)-path has length 2 + 2 X + X∗ + ( N − 1)(2 k2n(k + 1) + 1) + k, because the selection of the horizontal edges on this path is such that they are each of length 1. This is what we chose λ as. Finally, one may verify that the cost of H1 and H2 does not exceed β by using the previous length bounds of the paths which only intersect in rr cons , rcons p(rcons ), rr select and 

rselect p(rselect ) and the fact that all horizontal edges are contained in H1 and we can hence bound the overall digging costs by k · (N − 1)( nk 2(k + 1)) . ◁

For the backward direction, assume that {H1, H 2} is a solution for I. To derive a solution for Grid Tiling , it is useful to analyze the behavior of H1 and H2. As announced, by our construction, paths to terminals all use rcons and rselect respectively. 

▷ Claim 4.3. Every path from r to a terminal in H1 contains rr cons , and every path from r

to a terminal in H2 contains rr select .

Proof. Assume for contradiction that this is not the case. Then, because {rr cons , rr select }

separates r from all terminals, this means that rr cons ∈ E(H2) or rr select ∈ E(H1). Note that both edges have length X∗.By the choice of λ and ℓ, rr cons ∈ E(H1) because λ is equal to the length of the unique shortest path between r and p(rcons ) and this path contains rr cons . Similarly, by the choice of λ and ℓ, rr select ∈ E(H2) because λ is equal to the length of the unique shortest path between r and p(rselect ) and this path contains rr select . Hence from the edges incident to r, at least three edges of length X∗ contribute to the cost of solution {H1, H 2}. But the definition of X∗ implies 3X∗ > β , hence the cost of solution {H1, H 2} exceeds β; a contradiction. ◁

By construction, for each j ∈ [k], the path from rselect to p(t′ 

> j

) in H2 contains t′ 

> j

. The implied paths from rselect to t′ 

> j

in H2 turn out to have a very specific structure. 

▷ Claim 4.4. For each j ∈ [k] the interior of the path in H2 from rselect to t′ 

> j

is contained in 

Cj,j ′′ for some j′′ ∈ [n] and consists of exactly N − 1 vertical and exactly k horizontal edges, of which the latter have length 1.

Proof. Consider an arbitrary j ∈ [k]. By Claim 4.3, the path from r to p(t′ 

> j

) in H2 contains the edge rr select of length X∗, a length-X edge into G0, and a length-X edge from G0 to t′ 

> j

.Our choice of X∗, X and the depth threshold λ then ensure that no further length-X edge can be included in the path. Hence, the interior P of the rselect -t′ 

> j

-path in H2 has to be contained in G0. Such a path necessarily uses at least N − 1 vertical edges. Let us calculate an upper bound on the length of P implied by λ. In particular, its length can be at most 

λ − ℓ(p(t′ 

> j

)t′ 

> j

) − ℓ(rr select ) − 2X = ( N − 1)(2 k2n(k + 1) + 1) + k. This means the total length of all horizontal edges in P can be at most k. In particular, no horizontal edge with length higher than k can be contained in P which restricts P to be entirely in Cj,j ′′ 

for some j′′ ∈ [n] by the length choice for column inconsistency blocking edges. Within the interior of any rselect -t′ 

> j

-path in Cj,j ′′ there must be at least k horizontal edges by the construction of G: edges from rselect to G0 arrive in G0 at offset +1 , while edges from G0

to t′ 

> j

exit G0 from offset +( k + 1) . Each of these at least k horizontal edges has length at least 1 (because all edges in our construction have length at least 1), which means that the length (N − 1)(2 k2n(k + 1) + 1) + k of P can only be attained by a path P that has exactly 

k horizontal and exactly N − 1 vertical edges. ◁

Similarly, for each i ∈ [k], the path from rcons to p(ti) in H1 contains ti. The implied paths from rcons to ti in H1 can also be shown to have a specific structure. 28 Parameterized Complexity of Power Network Design 

▷ Claim 4.5. For each i ∈ [k], the interior of the rcons -ti-path in H1 is one row of Ri.

Proof. We have argued in Claim 4.3 that rr cons is necessarily included in H1. Further, the 

rcons -ti-path in H1 necessarily includes an edge of length X from ti to V (G0) and an edge of length X from rcons to V (G0). This means that any further length-X edge that could potentially be used to leave and reenter G0, is too long to be included in each r-p(ti)-path in 

H1. Hence the interior P of each rcons -ti-path in H1 has to be contained in G0.Let us calculate an upper bound on the length of P implied by λ. In particular, its length can be at most λ − ℓ(p(tj )tj ) − ℓ(rr cons ) − 2X = ( M − 1)( k + 1) .

P must contain kn − 1 column inconsistency blocking edges which means that the other edges of P have cumulative length at most (M − 1 − (kn − 1))( k + 1) = k2n(k + 1) . This is strictly smaller than 2k2n(k + 1) + 1 which means that P cannot contain a vertical edge and hence must be a row of G0. ◁

Our choice of β and dig prohibits horizontal edges that are not in H1 to be dug for H2.

▷ Claim 4.6. Every horizontal edge in H2 is also in H1.

Proof. We consider the contribution of ℓ(H1) and ℓ(H2) to the cost of solution {H1, H 2},using the structure of H1, H 2 inferred in the previous claims. By Claim 4.5, for each row consistency terminal j ∈ [k] the path from rcons to p(tj ) in H1

contains an entire row of G0. As tj is only adjacent to the rows of Rj , these paths and the length-X edges connecting them to rcons and tj are distinct for all j ∈ [k]. Let us give a lower bound on the total length of any row of G0, as follows. There are M − 1 edges in a row. Of these edges, all but kn are blocking edges of length k + 1 and the remaining edges have length at least 1. Hence the length of any row is at least (M − 1 − kn )( k + 1) + kn . We use this information to lower-bound ℓ(H1) + ℓ(H2).By Claim 4.3, Claim 4.4, Claim 4.5 and the definition of ℓ:the total length ℓ(H1) of edges in H1 is at least λ + k(2 X + λ − X∗ − 2X − (M − 1)( k +1) + ( M − 1 − nk )( k + 1) + nk ) = λ + k(λ − X∗ − nk (k + 1) + nk ) = λ + k(λ − X∗ − nk 2).the total length ℓ(H2) of edges in H2 is at least λ + k(λ − X∗).Further, dig (H1) is already k · (M − 1)( nk 2(k + 1)) . Compared to the total cost threshold β,this leaves at most nk 3 of slack for dig (E(H2) \ E(H1)) . This is smaller than the digging cost of any horizontal edge. Hence every horizontal edge in H2 is also in H1. ◁

We can use the above analysis of arbitrary solutions for I to show the reverse implication to Claim 4.2. 

▷ Claim 4.7. If I is a yes-instance then (Si,j )(i,j )∈[k]2 is a yes-instance. 

Proof. Let {H1, H 2} be a solution set for I. We show that selecting for each (i, j ) ∈ [k]2, the pair (a, b ) such that the i-th horizontal edge of the rselect -t′ 

> j

-path in H2 is in the a-th row of 

Ri and in Cj,b , yields a solution to the Grid Tiling instance from which we started the construction. By Claim 4.4, the horizontal edges in rselect -t′ 

> j

-paths in H2 have length 1. Fix j ∈ [k]

and let P be the rselect -t′ 

> j

-path in H2. Because of which horizontal edges receive length 1, all horizontal edges of P are contained in pairwise distinct Ri meaning that we indeed select a pair for each (i, j ) ∈ [k]2. Also by the definition of ℓ, the i-th horizontal edge of P (i.e. the one between the i-th and (i + 1) -th column of Cj,b ) having length one implies that it is in the b-th row in Ri, and (a, b ) ∈ Si,j .T. Hamm, B.M.P. Jansen and F. Motiei 29 

Moreover, as by Claim 4.4, P is entirely contained in Cj,b , this choice implies that all pairs selected for cells in column j have the same second entry. Lastly, horizontal edges of H2

have to be contained in H1 by Claim 4.6. By Claim 4.5, there is precisely one row of each Ri

contained in H1, implying that all selected pairs in the same row share their first entry. ◁

Overall, we have shown that a Grid Tiling -solution for (Si,j )(i,j )∈[k]2 implies a LVND -solution for I. This concludes the proof of correctness of our reduction. Notice that the number of terminals in the LVND -instance is linear in k from the Grid Tiling instance, that t = 2 is constant, and that |V (G)|, λ ∈ nO(1) . Hence if there were f, f ′ such that LVND 

can be solved in time f (|T |, t ) · (|V (G)| · λf ′(t))o(|T |) for any instance I then we could solve the original Grid Tiling instance in time ˜f (k) · no(k), contradicting the ETH. ◀

## 4.2 W[1]-hardness of High-voltage Network Design 

Our hardness result from the previous section (Theorem 4.1) crucially relies on the depth constraint in LVND . In particular, the depth constraint allowed us to severely restrict which paths could be used to connect terminals to r in the solution. However, to prove that HVND 

is also W[1]-hard, if we use the same reduction structure and set λ = + ∞, then instead of having a path from rcons (rselect ) to ti (t′

> i

) for each i ∈ [k], a solution of lower cost has a fork-like structure consisting of a single row (column) that then branches out on the last column (row) to reach all terminals on the other side of the grid (see Figure 4c). I.e. parts of the solution that should correspond to choices in separate rows or columns collapse into one. To prevent this issue and show that HVND is W[1]-hard, we introduce gadgets, called 

connectors , that contain further terminals throughout the inner parts of the grid. Concretely, the grid is divided into k × k subgrids of size N × M , called cell gadgets . Whenever two cell gadgets are adjacent in the original grid, their corresponding boundary vertices are no longer directly connected; instead, they are each linked via a connector which contains terminals that force the part of the solution that must cover them to connect through it and prevent it from collapsing. For our reduction it will also be more convenient for each valid row choice to have the same cost. To achieve this, we give a reduction from a variant of Grid Tiling called 

Regular Grid Tiling where for each x ∈ [n] and each i, j ∈ [k], the number of pairs of the form (x, y ) in Si,j is equal. We refer to this number by the regularity parameter of a 

Regular Grid Tiling instance. 

▶ Theorem 4.8. The Regular Grid Tiling problem is W[1]-hard parameterized by k, and under the ETH admits no f (k) · no(k)-time algorithm for any computable function f .

Proof. The k-clique problem is W[1]-hard even when restricted to regular graphs [ 13 ]. Using the reduction provided by Marx as Theorem 14.27 in [ 13 ] for reducing k-clique to 

Grid Tiling , any regular input of the k-clique problem can be reduced to an instance 

(Si,j )(i,j )∈[k]2 of Grid Tiling such that for each a, a ′ ∈ [n] and each i, i ′, j, j ′ ∈ [k], the number of pairs with first entry equal to a in Si,j is equal to the number of pairs with first entry equal to a′ in Si′,j ′ . Since k-clique is W[1]-hard and cannot be solved in time 

f (k) · no(k) for any computable function f , Regular Grid Tiling is also W[1]-hard and there is no f (k) · no(k)-time algorithm that solves this problem for any computable function 

f . ◀

Knowing that Regular Grid Tiling is W[1]-hard, we provide a reduction from this problem to prove that High-voltage Network Design is W[1]-hard in the following theorem. 30 Parameterized Complexity of Power Network Design 

▶ Theorem 4.9. The High-voltage Network Design problem is W[1]-hard even for 

t = 4 and under the ETH admits no f (|T |, t ) · | V (G)|o(√|T |)·f ′(t)-time algorithm for any computable functions f and f ′, even when restricted to planar input graphs. 

Proof. By Theorem 4.8, Regular Grid Tiling is W[1]-hard parameterized by k and there is no f (k) · no(k)-time algorithm for any computable function f that solves it. We show that HVND is also W[1]-hard and cannot be solved in time f (|T |, t ) · | V (G)|o(√|T |)·f ′(t) for any computable functions f and f ′ by providing a reduction from Regular Grid Tiling .Since the input graph constructed for the input instance through this reduction is planar, this shows that this statement persists even when restricted to planar input graphs. Let 

I = (Si,j )(i,j )∈[k]2 be an instance of Regular Grid Tiling with a regularity parameter δ

whose pairs are drawn from [n] × [n] where n ≥ 2. Let c = 6 , L = 40( c + 1)( n + 1)( n(k + 1) + 1)( ℓH + dH + ℓV ), L′ = 2 L, L′′ = 5 L, and X = 40 L3 where the values of dH , ℓH , and ℓV

will be defined later. We construct an equivalent instance (G, ℓ, dig , r, T, d, α, β, t ) of HVND 

as follows. 

Instance structure 

The main part of the graph G consists of two types of gadgets, namely cell gadgets and 

connectors .A connector for a sequence of pairs of vertices P = (( p1,1, p 1,2), (p2,1, p 2,2), . . . , (p|P| ,1, p (|P| ,2) )) 

consists of a set of paths of length c + 1 , one for each x ∈ [|P| ], from px, 1 to px, 2, which are called linking paths, and another set of c paths, referred to as supporting paths, where the 

q-th path connects two new terminals by passing through the q-th vertex of the x-th linking path for x ∈ [|P| ] respectively. Each of the edges of the linking paths has length L and digging cost L 

> 4( c+1)

and each of the edges of the supporting paths has length L

> 4( |P| +1)( c+1)

and digging cost 0. Note that in all connectors P is chosen such that L is always divisible by 

(|P| + 1)( c + 1) , so the resulting length value is an integer. For q ∈ [c + 1] , we call the set of edges consisting of the q-th edge of each linking path the q-th level of the connector. In other words, if the q-th linking path consists of vertices pq, 1 = uq

> 1

, u q

> 2

, . . . , u qc+1 = pq, 2, respectively, then the edge set {uxq uxq+1 | x ∈ [|P| ]} is the q-th level of the connector. Moreover, we call all vertices of a connector except the endpoints of the linking paths the internal vertices of this connector. A schematic view of a connector is shown in Figure 6a. A schematic overview of this graph is depicted in Figure 8. Using these definitions, we construct the graph as follows. 

Cell gadgets. There are k × k cell gadgets where cell gadget Gi,j corresponds to the cell 

Si,j from the Regular Grid Tiling input instance. Each cell gadget is a grid graph with N = n rows and M = n(k + 1) columns. For ease of notation, for x ∈ [N ] and 

y ∈ [M ], we denote the vertex in the x-th row and y-th column of a cell gadget Gi,j by 

ui,j x,y . The lengths and digging costs of the edges of the grid are defined as follows. 

Horizontal edges. For each choice of x ∈ [N ], y ∈ [k], and y′ ∈ [n] there is a unique horizontal edge ui,j x, (k+1)( y′−1)+ y ui,j x, (k+1)( y′−1)+ y+1 in the grid. The digging cost of this edge is dH = n(k + 1) and the length of this edge depends on whether (x, y ′) ∈ Si,j or not, which is defined as follows. 

∗ If (x, y ′) ∈ Si,j and i = y then the length of this edge is 1.

∗ Otherwise, it has length ℓH = k + 1 .So, by definition of the Regular Grid Tiling problem, each row contains exactly δ

number of edges of length 1 and the rest are of length ℓH .T. Hamm, B.M.P. Jansen and F. Motiei 31 

p|P |,1 p|P |,2

p|P |− 1,1 p|P |− 1,2

p2,1 p2,2

p1,1 p1,2

... ...

(a) Schematic view of a connector. The third level of the connector consists of the orange edges. 

hi,j  

> 1,◁

hi,j  

> 2,◁

hi,j  

> 3,◁

hi,j  

> 4,◁

hi,j  

> 5,◁

hi,j 

> 6,◁

ui,j N,M ui,j +1 

> N, 1

ui,j N −1,M ui,j +1  

> N−1,1

ui,j  

> 2,M

ui,j +1 2,1

ui,j  

> 1,M

ui,j +1 1,1

hi,j  

> 1,▷

hi,j  

> 2,▷

hi,j  

> 3,▷

hi,j  

> 4,▷

hi,j  

> 5,▷

hi,j 

> 6,▷

... ...

(b) An instance of a row connector Ci,j R and an indication of the part of a minimum solution in this connector. The purple part is shared between two trees that intersect this connector, and the red and blue edges distinguish between these two trees. In this solution, n − 1 is chosen as the first value of pairs of i-th row cells. 

Figure 6 Schematic view of a connector and an instance of it. The terminals are colored gray. The vertical edges have digging cost 0 and are of length ⌈ L 

> 4( |P| +1)( c+1)

⌉ where |P| = N in the provided instance in (b). The horizontal edges are of length L and have digging cost ⌈ Lc+1 ⌉. The linking paths are made up of horizontal edges and the supporting paths consist of vertical edges. 

Vertical edges. For each vertical edge, we set its length to ℓV = n(k + 1) and its digging cost to 0.For each y ∈ [M ], we call the path that passes through vertices ui,j  

> 1,y

, u i,j  

> 2,y

, . . . , u i,j N,y the 

y-th column path of this gadget. Similarly, for each x ∈ [N ], we call the path that passes through vertices ui,j x, 1, u i,j x, 2, . . . , u i,j x,M the x-th row path of this gadget. An example of this gadget is shown in Figure 7a. Note that our choice of L ensures that that the total cost (including diggin cost) of the edges in cell gadget is at most L 

> 40

. Thus, the total cost of a column path is at most L 

> 40 M

< LM and the total cost of a row path is at most L 

> 40 N

< LN .

Row connectors. For each i ∈ [k] and j ∈ [k − 1] , there is a connector between cell gadgets Gi,j and Gi,j +1 on the sequence of pairs (( ui,j  

> 1,M

, u i,j +1 1,1 ), . . . , (ui,j N,M , u i,j +1  

> N, 1

)) ,which is called a row connector and is denoted Ci,j R . Note that by definition of L and N ,we have ⌈ L  

> 4( N+1)( c+1)

⌉ = L  

> 4( N+1)( c+1)

which is the length of the edges of the supporting paths in these connectors. For each q ∈ [c], let hi,j q, 1, . . . , h i,j q,N denote the interior of the 

q-th supporting path, ordered according to the linking paths: hi,j q, 1 lies on the first linking path, hi,j q, 2 lies on the second linking path, and so on, with hi,j q,N lying on the last linking path. We denote by hi,j q,▷ the terminal adjacent to hi,j q, 1, and by hi,j q,◁ the terminal adjacent to hi,j q,N . A schematic view of an instance of these connectors is shown in Figure 6b. 

Column connectors. For each i ∈ [k − 1] and j ∈ [k], there is a connector between cell gadgets Gi,j and Gi+1 ,j on the sequence pairs (( ui,j N, 1, u i,j +1 1,1 ), . . . , (ui,j N,M , u i,j +1 1,M )) , which is called a column connector and is denoted Ci,j C . Note that by definition of L and M ,32 Parameterized Complexity of Power Network Design 

we have ⌈ L  

> 4( M+1)( c+1)

⌉ = L  

> 4( M+1)( c+1)

which is the length of the edges of the supporting paths in these connectors. For each q ∈ [c], let vi,j q, 1, . . . , v i,j q,N denote the interior of the 

q-th supporting path, ordered according to the linking paths: vi,j q, 1 lies on the first linking path, vi,j q, 2 lies on the second linking path, and so on, with vi,j q,N lying on the last linking path. We denote by vi,j q,▷ the terminal adjacent to vi,j q, 1, and by vi,j q,◁ the terminal adjacent to vi,j q,N .

u2,14,1

u2,13,1

u2,12,1

u2,11,1

u2,11,0+1 u2,11,0+2 u2,11,0+3 u2,11,3+1 u2,11,3+2 u2,11,3+3 u2,11,6+1 u2,11,6+2 u2,11,6+3 u2,11,9+1 u2,11,9+2 u2,11,9+3 

(a) The cell gadget G2,1 which corresponds to S2,1, where red edges are of length 1 and the other horizontal edges are of length ℓH . The vertical edges are of length ℓV . The digging cost of horizontal edges is dH

and the digging cost of vertical edges is 0.

v2,14,1

v2,13,1

v2,12,1

v2,11,1

v2,11,0+1 v2,11,0+2 v2,11,0+3 v2,11,3+1 v2,11,3+2 v2,11,3+3 v2,11,6+1 v2,11,6+2 v2,11,6+3 v2,11,9+1 v2,11,9+2 v2,11,9+3 

(b) The part of a minimum solution in cell gadget G2,1 which can be transformed to a solution for 

Regular Grid Tiling . The first value of all chosen pairs from the second row of Regular Grid Tiling 

instance is 2 and the second value of all chosen pairs of the first column of this instance is 3. The yellow edges belong to HR,▷ and HR,◁ , blue edges belong to HC,▷ and HC,◁ , and green edges belong to all trees. 

Figure 7 Let n = 4 , k = 2 , and S2,1 = {(1 , 2) , (1 , 4) , (2 , 1) , (2 , 3) , (3 , 2) , (3 , 4) , (4 , 1) , (4 , 3) }.

Root part. To complete the description of the graph, additional sets of edges, vertices, and terminals are defined as follows. We refer to this additional part of the graph as its root part .The root part can be divided into three parts, which are defined as follows. 

Row root part. 

There is a set of vertices bi,z for i ∈ [k] and z ∈ {→ , ←} , each with a pair of terminals 

p▷(bi,z ) and p◁(bi,z ) as pendants, where bi, → is connected to vertices ui, 1 

> x, 1

for all x ∈ [N ]

and bi, ← is connected to vertices ui,k x,M for all x ∈ [N ]. The length of each of the edges between these vertices and their pendant terminals is X, and the length of each of the rest of these edges is L′. The digging cost of all of them is 0.There is also a vertex called rcons connected to the root r with an edge of length 

X, which is connected to the vertices bi, → for all i ∈ [k] with edges of length L′′ 

and digging cost 0 and is connected to two new terminals p▷(rcons ) and p◁(rcons ) as pendants. Similarly, the length of the edges between these vertices and their pendant terminals is X, and their digging cost is 0.T. Hamm, B.M.P. Jansen and F. Motiei 33 

We refer to the subgraph consisting of the above as row root part .

Column root part. 

There is another set of vertices bz,j for z ∈ {↑ , ↓} and j ∈ [k], referred to as column root part , each with a pair of terminals p▷(bz,j ) and p◁(bz,j ) as pendants, where b↑,j 

is connected to vertices ui,j  

> 1,y

with y = y′ · (k + 1) + 1 for all 0 ≤ y′ < n , and b↓,j is connected to vertices ui,j N,y with y = y′ · (k + 1) for all 1 ≤ y′ ≤ n. The length of the edges between these vertices and their pendant terminals is X, and the length of the rest of these edges is L′. The digging cost of all of them is 0.There is also a vertex called rselect connected to the root r with an edge of length X,which is connected to the vertices b↑,j for all j ∈ [k] with edges of length L′′ and digging cost 0 and is connected to two new terminals p▷(rselect ) and p◁(rselect ) as pendants. The length of the edges between these vertices and their pendant terminals is X, and their digging cost is 0.We refer to the subgraph consisting of the above as column root part .                  

> Gk, 1Gk, 2Gk,k −1Gk,k
> Gk−1,1Gk−1,2Gk−1,k −1Gk−1,k
> G1,1G1,2G1,k −1G1,k
> Ck,k −1
> H
> Ck−1,1
> RCk−1,k −1
> R
> C1,1
> RC1,k −1
> R
> Ck−1,1
> CCk−1,2
> CCk−1,k −1
> CCk−1,k C
> ............
> · · ·
> · · ·
> · · ·
> bk, →
> ...
> bk−1,→
> ...
> b1,→
> ...
> bk, ←
> ...
> bk−1,←
> ...
> b1,←
> ...
> rcons ...
> b↑,1
> . . .
> b↑,2
> . . .
> b↑,k −1
> . . .
> b↑,k
> . . .
> rselect
> . . .
> b↓,1
> . . .
> b↓,2
> . . .
> b↓,k −1
> . . .
> b↓,k
> . . .
> r

Figure 8 Schematic view of the reduction in Theorem 4.9. Connectors are indicated in yellow and cell gadgets in gray. The orange edges are of weight L′,the green edges are of weight L′′ , and the blue edges are of length X. An instance of a connector is shown for Ck, 1 

> R

.

Reduction overview. 

Before finalizing the unspecified parts of the HVND -instance, we provide some intuition for the structural design we aim to achieve. Similarly to the reduction provided for Theorem 4.1, to encode a choice of pairs of numbers for each cell of the Regular Grid Tiling instance, our HVND -solution should consist of two parts, one selecting k rows and one selecting k columns, such that the intersection of a selected row and a selected column consists of a single shared horizontal edge, which needs to be dug only once. 34 Parameterized Complexity of Power Network Design         

> ............
> (a) Purple edges are of trees, each passing through a connector and covering all terminals. Left: the tree intersects only one row. Right: the tree intersects/switches between two rows. Both trees have equal cost.
> ............
> (b) Purple edges are shared by pairs of trees with red resp. blue private edges, each passing through a connector and covering all terminals. Left: trees intersects one row. Right: the pair of trees switches between two rows and has higher cost due to counting the vertical purple edge twice.
> Figure 9

We will use Lemma 2.3 to find suitable demand values on the terminals that ensure the following distribution of terminals: the row-selection trees cover the terminals belonging to the row connectors and the terminals of the row root part, while the column selection trees only contain the terminals belonging to the column connectors and the terminals of the column root part. The digging costs and the edge lengths within the connector gadgets are defined so that these connectors are used only when necessary to establish connections between cell gadgets and cover the terminals. More specifically, in a minimum-cost solution, this prevents a tree encoding row choices to intersect column connectors and a tree encoding column choices to intersect row connectors. To make the selection of linking paths within each connector independent of their distance from terminals, which affects the selection of row and column values, while preserving planarity, each connector includes supporting paths each with two terminals as its endpoints. In a cost-optimal solution, two trees should intersect each connector, each covering terminals on one side of all supporting paths, but together covering all supporting paths and their terminals; see Figure 6b. To ensure that within a row (column) connector the solution does not switch between rows (columns), our construction enforces a solution consisting of four (two for row and two for column choices) trees rather than two (one for row and one for column choices); see Figure 9. The edge lengths, particularly those between r and rcons and between r and rselect , and 

β are chosen so that every root-terminal-path in the row-selection part passes through rcons ,while every root-terminal-path in the column selection part passes through rselect .By construction and the cost budget, we will make, all edges of the two trees encoding row (column) choices are shared, except for those corresponding to the supporting paths inside the connectors and those that are only for the connection to the terminals of that tree. T. Hamm, B.M.P. Jansen and F. Motiei 35 

This structure also constrains each rcons -bi, ← path to pass through bi, → and the interior of the path from bi, ← to bi, → lies entirely within the subgraph ⋃kj=1 Gi,j ∪ ⋃k−1 

> j=1

Ci,j R , and using vertical edges from the cell gadgets is impossible since it would incur a prohibitively large cost. This design allows us to encode the first entry of the pair selected for the i-th row of the Grid Tiling -instance through the index of the row whose vertices appear on the 

rcons -bi, ← path in the trees encoding row choices. Similarly, for each j ∈ [k], the rselect -b↓,j path must pass through b↑,j and the interior of the path from b↓,j to b↑,j lies within exactly one of the n subgraphs of the form ⋃ki=1 Gi,j y ∪

⋃k−1 

> i=1

Ci,j C , for some y ∈ [n], where Gi,j y denotes the union of the (y − 1)( k + 1) -th through 

(y(k + 1) − 1) -th column paths of Gi,j together with all horizontal edges connecting their vertices. Each such path contains exactly k horizontal edges in total from cell gadgets. This allows us to encode the second entry of the selected pair for the j-th column of the 

Grid Tiling -instance. Finally, we must ensure that the encoded pair (a, b ) ∈ [n] × [n] for each cell (i, j ) ∈ [k] × [k]

satisfies (a, b ) ∈ Si,j . To enforce this, we assign digging costs so that, for each j ∈ [k], every 

rcons -bi, ← path in row-selection trees necessarily shares a (horizontal) edge with each rselect -

b↓,j path in column-selection trees. We then make the horizontal edges prohibitively long whenever they connect the y(k + 1) -th and (y(k + 1) + 1) -th column paths on the x-th row path of Gi,j if either (x, y ) /∈ Si,j – thereby excluding invalid pairs – or y̸ = i – thereby ensuring that the selected column entry is compatible with the selected row entry for each 

i ∈ [k].We now proceed with the formal construction of the HVND -instance to realize the intended behavior described above. 

d, t, α, and β

We set t = 4 ; so a solution consists of four trees HR,▷ , H R,◁ , H C,▷ , H C,◁ . Additionally, we set 

β = (8 + 8 k)X + 8 k · L′ + 4 k · L′′ 

︸ ︷︷ ︸

> root part

+k2(2( M − 1 − δ)ℓH + 2 δ + 2( N − 1) ℓV + 2 + ( M − 1) dH )

︸ ︷︷ ︸

> cell gadgets

+2 k(k − 1) · (2 c + 3 + c

4( c + 1) ) · L

︸ ︷︷ ︸

> connectors

,

so that the existence of a solution for HVND is guaranteed if and only if the given Regular Grid Tiling instance with the regularity parameter δ has a solution. Consider a partition 

P of terminals into four parts as follows. 

TR,▷ = {p▷(rcons )} ∪ { p▷(bi,j ) | i ∈ [k], j ∈ {→ , ←}} ∪ { hi,j q,▷ | i ∈ [k], j ∈ [k − 1] , q ∈ [c]},TR,◁ = {p◁(rcons )} ∪ { p◁(bi,j ) | i ∈ [k], j ∈ {→ , ←}} ∪ { hi,j q,◁ | i ∈ [k], j ∈ [k − 1] , q ∈ [c]},TC,▷ = {p▷(rselect )} ∪ { p▷(bi,j ) | i ∈ {↑ , ↓} , j ∈ [k]} ∪ { vi,j q,▷ | i ∈ [k − 1] , j ∈ [k], q ∈ [c]},TC,◁ = {p◁(rselect ) ∪ { p◁(bi,j ) | i ∈ {↑ , ↓} , j ∈ [k]} ∪ { vi,j q,◁ | i ∈ [k − 1] , j ∈ [k], q ∈ [c]}.

The first two parts cover all terminals in the row connectors together with the terminals of the row root part. The main difference of these two sets is in the group of terminals that they contain from each row connector. In more detail, one covers the terminals on the top of each row connector and the other one covers the ones on the bottom (see Figure 6b). The 36 Parameterized Complexity of Power Network Design 

two other parts cover all terminals in the column connector as well as the terminals of the column root part. Similarly, the main difference is in covering the terminals of each side of the column connectors: right and left. By Lemma 2.3, there exists a function f : T → [|T |6]

such that P is the only way of partitioning the terminals into at most four parts where the summation of f -values of each part is at most ∑  

> u∈Tf(u)4

. Since the demand values are integers, by setting the demand function d = f and α = ⌊ ∑ 

> u∈Td(u)4

⌋, the goal becomes finding four trees that each covers exactly one part of P.So, from now on, we can assume that a solution to this HVND -instance consists of four trees where Ha,a ′ covers Ta,a ′ for a ∈ { R, C } and a′ ∈ { ▷, ◁ }. Note that this guarantees the validity of the covering constraint and the demand constraint. This concludes the description of the HVND -instance. In the remainder of the proof, we establish that it is equivalent to the instance of Regular Grid Tiling that we started from. 

If (Si,j )(i,j )∈[k]2 is a yes-instance then (G, ℓ, dig , r, T, d, α, β, t ) is a yes-instance. 

Fix a solution (( ai, b j )) (i,j )∈[k]×[k] for (Si,j )(i,j )∈[k]2 . We transform this into a solution for 

HVND on the input instance (G, ℓ, dig , r, T, d, α, β, λ, t ) by specifying the four trees HR,▷ ,

HR,◁ , HC,▷ and HC,◁ , two passing horizontally through rows and two passing vertically through columns. Since each pair of trees with the same orientation has a large overlap, we define their shared parts first. For each part of the solution, we bound sum of the length values occurring in that subgraph. Afterwards, we bound the digging costs for the entire solution. We define HR,▷ ∩ HR,◁ as the union of the following parts. 1. The ai-th row path from each cell gadget Gi,j for each i, j ∈ [k] where each has length 

(M − 1 − δ)ℓH + δ.2. The ai-th linking path of the row connector Ci,j R for each i ∈ [k] and j ∈ [k − 1] , where each has length (c + 1) · L.3. The edge rr cons which has length X along with the following edges. For each i ∈ [k],the edges rcons bi, → where each has length L′′ ,the edges bi, →ui, 1

> ai,1

, and ui,k ai,M bi, ← where each has length L′.These edges have a total length X + k · L′′ + 2 k · L′.So, the total length of HR,▷ ∩ HR,◁ is 

k2(( M − 1 − δ)ℓH + δ) + k(k − 1)( c + 1) · L + X + k · L′′ + 2 k · L′.

Additionally, for each a ∈ { ▷, ◁ }, the tree HR,a contains the following parts. 1. For each i ∈ [k], the edges connecting rcons , bi, →, and bi, ← to their corresponding pendant terminals pa(rcons ), pa(bi, →), and pa(bi, ←); recall that each of these edges has length X.2. For each i ∈ [k] and each j ∈ [k − 1] , for all q ∈ [c],the hi,j q,▷ -hi,j q,a i -subpath from the q-th supporting path of the row connector Ci,j R if a = ▷,the hi,j q,◁ -hi,j q,a i -subpath from the q-th supporting path of the row connector Ci,j R if a = ◁.Note that these subpaths does not share any edges and their union consists of all supporting paths of the corresponding row connector. Since each supporting path has length L 

> 4( c+1)

, the total length of all supporting paths in a connector is L 

> 4( c+1)

· c.T. Hamm, B.M.P. Jansen and F. Motiei 37 

So, the total length of the non-shared parts of trees HR,▷ and HR,◁ is 

(4 + 4 k)X + k(k − 1)( cc + 1 ) · L. 

For each j ∈ [k], there should be a path Pj appearing in both HC,▷ and HC,◁ that corresponds to the chosen value bj . This path from u1,j   

> 1,(bj−1)( k+1)+1

to uk,j N,b j (k+1) which lies within G1,j ∪ C 1,j C ∪ G2,j ∪ . . . ∪ C k−1,j C ∪ Gk,j . The path Pj consists of exactly k horizontal edges, one in each of the cell gadgets G1,j , . . . , G k,j where the one in Gi,j belongs to the x-th row path if si,j = ( x, y ) and is from ui,j   

> (bj−1)( k+1)+ i

to ui,j   

> (bj−1)( k+1)+( i+1)

(see Figure 7b). The rest of the edges of Pj are vertical edges and the edges of linking paths. We formally define 

HC,▷ ∩ HC,◁ as the union of the following parts. The path Pj can be achieved through 1 and 2. 1. For each i, j ∈ [k], a path from ui,j   

> 1,(bj−1)( k+1)+ i

which lies on the first row path of Gi,j to 

ui,j N, (bj −1)( k+1)+ i+1 which belongs to the last row path of Gi,j consisting of the following parts. The subpath of the (( bj − 1)( k + 1) + i)-th column path of each cell gadget Gi,j that is from ui,j   

> 1,(bj−1)( k+1)+ i

to ui,j ai,(bj −1)( k+1)+ i.The subpath of the (( bj − 1)( k + 1) + i + 1) -th column path of each cell gadget Gi,j 

that is from ui,j ai,(bj −1)( k+1)+ i+1 to ui,j N, (bj −1)( k+1)+ i+1 .The edge ui,j ai,(bj −1)( k+1)+ iui,j ai,(bj −1)( k+1)+ i+1 

These subpaths have N − 1 vertical edges of length ℓV . The horizontal edge corresponds to the choice of (ai, b j ) which has length 1. So, each of these paths has a total length 

(N − 1) ℓV + 1 .2. The bj -th linking path of the column connector Ci,j C for each i ∈ [k − 1] and j ∈ [k]. Each of these paths has length (c + 1) · L.3. The edge rr select , which has length X, along with the following edges. For all j ∈ [k],

rselect b↑,j where each has length L′′ ,the edges b↑,j u1,j   

> 1,(bj−1)( k+1)+1

and uk,j n,b j ·(k+1) b↓,j where each has length L′.These edges have a total length X + k · L′′ + 2 k · L′.Note that the total length of edges other than horizontal edges in each mentioned path Pj is the fixed value k(N −1) ℓV +( k −1)( c+1) ·L regardless of where the side steps are taken. Since the sidesteps are taken in edges with minimum length (the ones that correspond to the choice of si,j which has length 1), each of these paths has length k(( N − 1) ℓV + 1) + ( k − 1)( c + 1) · L

which is the minimum possible value for such a path. Thus, the total length of HC,▷ ∩ HC,◁ is 

k2(( N − 1) ℓV + 1) + k(k − 1)( c + 1) · L + X + k · L′′ + 2 k · L′.

Additionally, for each a ∈ { ▷, ◁ }, the tree HC,a contains the following parts. 1. The edge rr select along with the edges connecting rselect , b↑,j , and b↓,j to their correspond-ing pendant terminals pa(rselect ), pa(b↑,j ), and pa(b↓,j ) for each j ∈ [k]; recall that each of these edges has length X and so, they have a total length (2 + 2 k)X.2. For each i ∈ [k − 1] and j ∈ [k], for all q ∈ [c],the vi,j q,▷ -hi,j q,b j -subpath from q-th supporting paths of the column connector Ci,j C if a = ▷,the vi,j q,◁ -hi,j q,b j -subpath from q-th supporting path of the column connector Ci,j C if a = ◁.38 Parameterized Complexity of Power Network Design 

Note that these subpaths does not share any edges and their union consists of all supporting paths of the corresponding column connector. Since each supporting path has length L 

> 4( c+1)

, the total length of all supporting paths in a connector is L 

> 4( c+1)

· c.Hence, the total length of the non-shared parts of trees HC,▷ and HC,◁ is 

(4 + 4 k)X + k(k − 1) · c

4( c + 1) · L. 

Since the edge with a non-zero digging cost only belong to cell gadgets and connectors, the digging cost can be computed as follows. 1. The digging cost arising within each connector is L since the edges on supporting paths have digging cost 0, while the solution uses all c + 1 edges of exactly one linking path and each such edge has a digging cost Lc+1 .2. The digging cost arising within each cell gadget is (M − 1) dH since the vertical edges have digging cost 0 and the solution uses all M − 1 edges of exactly one row path and each such edge has a digging cost dH .Hence, the total digging cost is 

2k(k − 1) · L + k2(M − 1) dH .

Therefore, we can conclude that the total cost of this solution cost (HC1,, H C2 , H R1 , H R2 )

is at most 

2k2(( M − 1 − δ)ℓH + δ) + 2 k(k − 1)( c + 1) · L + 2 X + 2 k · L′′ + 4 k · L′

︸ ︷︷ ︸   

> length of two copies of HR,▷ ∩HR,◁

+ (4 + 4 k)X + k(k − 1)( c

4( c + 1) ) · L

︸ ︷︷ ︸   

> length of non-shared parts of HR,▷ and HR,◁

2k2(( N − 1) ℓV + 1) + 2 k(k − 1)( c + 1) · L + 2 X + 2 k · L′′ + 4 k · L′

︸ ︷︷ ︸   

> length of two copies of HC,▷ ∩HC,◁

+ (4 + 4 k)X + k(k − 1)( c

4( c + 1) ) · L

︸ ︷︷ ︸   

> length of non-shared parts of HC,▷ and HC,◁

+ 2 k(k − 1) · L + k2(M − 1) dH

︸ ︷︷ ︸

> total digging cost

= β, 

and so, the cost constraint holds. One can easily see that, by construction, the shared part of HR,▷ and HR,◁ in ⋃kj=1 Gi,j ∪

⋃k−1 

> j=1

Ci,j R , for each i ∈ [k] is a path where these paths are disjoint and become connected through a subtree that is defined in the row root part while non-shared parts are paths that has exactly one endpoint in the shared part which keeps the subgraph connected and acyclic. A similar argument holds for HC,▷ and HC,◁ . Therefore, these subgraphs are trees. Moreover, since HR,▷ , HR,◁ , HC,▷ and HC,◁ cover TR,▷ , TR,◁ , TC,▷ , and TC,◁ , respectively, the rest of constraints also hold for this solution, thus, this is a valid solution for this HVND -instance. T. Hamm, B.M.P. Jansen and F. Motiei 39 If (G, ℓ, dig , r, T, d, α, β, t ) is a yes-instance then (Si,j )(i,j )∈[k]2 is a yes-instance. 

Let S = {HR,▷ , H R,◁ , H C,▷ , H C,◁ } be the trees defined by a solution with minimum cost that witnesses that (G, ℓ, dig , r, T, d, α, β, t ) is a yes-instance. Without loss of generality, assume that HR,▷ and HR,◁ cover TR,▷ and TR,◁ respectively and HC,▷ and HC,◁ cover TC,▷ and 

TC,◁ respectively. First, we provide some necessary structural properties of a minimum solution in Claim 4.12, Claim 4.13, and Claim 4.14. Then, considering these properties, we provide a lower bound for the cost of different parts of a minimum solution, namely the parts that lie within each cell gadget and the parts that lie within each connector, respectively in Claim 4.16 and Claim 4.15. Finally, we show that these lower bounds are tight in Claim 4.17 and discuss how we can extract a solution to the Regular Grid Tiling instance from this minimum solution S in the rest of the proof. We start with two properties regarding the locations of the trees of a minimum-cost solution are that each tree only intersects: a specific subset of connectors (Claim 4.12) and a specific subset of vertices in the root part of G (Claim 4.13). To establish these properties, we show that if this is not true, we can provide a new solution S′ to the considered High-voltage Network Design -instance, by replacing a part of S by another subgraph where cost (S′) < cost (S), contradicting the cost-minimality assumption on S. Claim 4.11 provides a tool for this replacement that will be used in the proofs of Claim 4.12 and Claim 4.13. The following claim facilitates the discussion on the effect of a replacement that will be done in the proof of Claim 4.11 . 

▷ Claim 4.10. If H is a subgraph of G that, for some a ∈ { R, C } and a′ ∈ { ▷, ◁ },

Ta,a ′ ∪ { r} ⊆ V (H), and has at most two connected components, each of which contains at least two vertices, then for each connector C of G that is defined on a sequence of pairs of vertices P where 

C is a row connector if a = R and is a column connector if a = C, we have ℓ(H ∩ C ) ≥

L · (c − 1) + L 

> 4( |P| +1)( c+1)

· c.

Proof. We assume that C is a row connector Ci,j R . The proof for the column connectors is symmetric, since we only use this assumption to identify the indices. We now show that from at least c − 1 out of the c + 1 levels of Ci,j R , at least one edge appears in H. Suppose, for the sake of contradiction, that this is not the case. Then, there exist at least three levels of Ci,j R whose edges do not appear in H. Let q1 < q 2 < q 3 be the indices of these levels. Because a connector has a path-like structure with respect to the linear order of the levels (see Figure 6b), removing the edges of levels q1, q 2, q 3 from a connector splits the connector into at least four connected components, just as removing three edges from a path yields four components F1, F 2, F 3, F 4. The two middle components, 

F2 and F3, do not contain endpoints of linking paths. When connectors are placed into the graph G, the only connections from the outside to the inside of a connector are through the endpoints of the linking paths. Hence there can be a path in G between vertices of 

F1 and F4 that avoids levels q1, q 2, q 3, but no vertex of F2 or F3 can connect through the rest of the graph without using an edge of layers q1, q 2, or q3. Consequently, in the graph obtained from H by removing the edges of levels q1, q 2, q 3 (which is identical to H itself, by assumption), there are at least three connected components: two corresponding to F2 and 

F3, and one additional component containing the root. This contradicts the assumption about the number of connected components of H. Thus, H contains an edge from at least 40 Parameterized Complexity of Power Network Design 

c − 1 levels of Ci,j R and hence the total length of the edges of H that lie on the linking paths is at least (c − 1) · L.On the other hand, the only way to connect to each terminal is via the unique edge incident to it. Since each connected component of H contains at least two vertices, all terminals of H are connected to some other vertices, and so all these edges are in H. Recalling that Ta,a ′ contains all c terminals of Ci,j R that are on one side of it (bottom or top) and each edge that is incident on those terminals has length L 

> 4( |P| +1)( c+1)

, the total length of these c edges is L 

> 4( |P| +1)( c+1)

· c. This is a lower bound for the total length of the edges of H that are on the supporting paths of Ci,j R . This implies that ℓ(H ∩ C i,j R ) is at least 

(c − 1) · L + L 

> 4( |P| +1)( c+1)

· c. ◀

Next, we present the tool required to prove Claim 4.12 and Claim 4.13. This tool shows that any disconnected subgraph that contains a subset of the terminals Ta,a ′ for some 

a ∈ { R, C } and a′ ∈ { ▷, ◁ } can be transformed into a connected one while increasing the cost by only a bounded amount. We can consider the operation of replacing H by H′ as a two-step process: first removing H′ \ H, which decreases the cost by at least ℓ(H \ H′), and then inserting H H ′, which increases the cost by at most ℓ(H′ \ H) + dig( H′ \ H).

▷ Claim 4.11. If H is a subgraph of G that, for some a ∈ { R, C } and a′ ∈ { ▷, ◁ },contains all vertices in Ta,a ′ ∪ { r}, and has exactly two connected components, each of which contains at least two vertices, then there exists a tree H′ ⊆ G such that Ta,a ′ ∪ { r} ⊆ V (H′) and dig (H′ \ H) + ℓ(H′ \

H) − ℓ(H \ H′) ≤ 5L.

Proof. We assume that a = R; the proof for the other case is symmetric. Let Cr be the connected component of H that contains r, and let ¯Cr denote the other connected component. For any two subgraphs Hx and Hy of G, we define ∆( Hx, H y ) = dig (Hy \ Hx) + ℓ(Hy \ Hx) −

ℓ(Hx \ Hy ). Since every terminal in V (H) ∩ T belongs to either Cr or ¯Cr , we now perform a case distinction based on the structure of H. For each case, we construct a connected subgraph H′ that contains Ta,a ′ ∪ { r} and satisfies dig (H′ \ H) + ℓ(H′ \ H) − ℓ(H \ H′) ≤ 5L.Finally, in each case, if the constructed subgraph is not acyclic, we replace it with a spanning tree of it. Clearly, this replacement does not increase the value of ∆( H, H ′) and yields a tree that satisfies all required conditions. I. For some i ∈ [k] and j ∈ [k − 1] , there exists at least one terminal in Ci,j R that belongs to Cr and one terminal in Ci,j R that belongs to ¯Cr . In this case, H′ will be obtained from a copy of H via the following modification steps: 1. We remove all edges of H ∩ C i,j R along with all internal vertices of Ci,j R that are in H;Note that the total length of the removed edges is at least L · (c − 1) + L  

> 4( N+1)( c+1)

· c

by Claim 4.10 since a = R and Ci,j R is a row connector. 2. We insert the last column path of Gi,j , the first column path of Gi,j +1 , the linking path that contains all unique neighbors of terminals in Ta,a ′ ∩ C i,j R , and all edges that are incident on these terminals; recall that each column path has length (N − 1) · ℓV and digging cost 0, each linking path has length L · (c + 1) and digging cost Lc+1 · (c + 1) = L,and each unique edge that is connected to a terminal of Ci,j R has length L 

> 4( N+1)( c+1)

and digging cost 0.The set of edges that are removed in step 1 consists of the set E(H\H′) and E(H∩H′∩C i,j R ),and so the total length of the removed edges in step 1 is ℓ(H \ H′) + ℓ(H ∩ H′ ∩ C i,j R ).T. Hamm, B.M.P. Jansen and F. Motiei 41 

Furthermore, the set of edges that are added in step 2 consists of the set E(H′ \ H) and 

E(H ∩H′ ∩C i,j R ). So, the total length of added edges in step 2 is ℓ(H′ \H)+ ℓ(H ∩H′ ∩C i,j R ).Therefore, recalling that there are c terminals in V (Ci,j R ) ∩ Ta,a ′ , we have 

ℓ(H′ \ H) − ℓ(H \ H′) = ℓ(H′ \ H) + ℓ(H ∩ H′ ∩ C i,j R ) − ℓ(H \ H′) − ℓ(H ∩ H′ ∩ C i,j R )

≤ 2( N − 1) · ℓV + L · (c + 1) + L

4( N + 1)( c + 1) · c

− L · (c − 1) − L

4( N + 1)( c + 1) · c

≤ 2( N − 1) · ℓV + 2 L

Since we have (N − 1) · ℓV < M ·N ·ℓV 

> M

< LM , we can conclude that ℓ(H′ \ H) − ℓ(H \ H′) ≤

2 LM + 2 L. Therefore, we have ∆( H, H ′) = dig (H′ \ H) + ℓ(H′ \ H) − ℓ(H \ H′) ≤

L + 2 LM + 2 L ≤ 5L.We now show H′ is connected and contains all terminals in Ta,a ′ and r. Let VC be the vertices in the last column path of Gi,j together with the vertices in the first column path of Gi,j +1 . All connected components that remain after step 1 should contain at least one vertex from VC since they do not belong to the internal vertices of Ci,j R and there is a path from their vertices to a terminal in Ci,j R in the corresponding connected component of H. Noting that the edges and vertices of these connected components are also in H′

and the added part in step 2 connects all vertices of VC , hence H′ is connected. Since r

belongs to one the remaining connected components after step 1 and each terminal of 

Ta,a ′ is either in the added part or in one of these connected components, therefore, H′

covers Ta,a ′ ∪ { r}.II. For each row connector CR, all terminals of Ta,a ′ contained in CR lie in the same connected component of H. We have following subcases for this case. a. For some i ∈ [k], j ∈ [k − 1] , the terminals of Ta,a ′ that lie in Ci,j R belong to a different connected component of H than those in Ci,j +1  

> R

. Let u be the unique neighbor of the terminal hi,j c,a ′ and v be the unique neighbor of hi,j +1 1,a ′ . Let x = 1 

if a = ▷ and x = N if a = ◁. Then, we know that u lies on the x-th linking path of 

Ci,j R and v lies on the x-th linking path of Ci,j +1  

> R

. We set H′ to be a copy of H union the following parts: The first and last column paths and the first row path of Gi,j . Recall that each column path has a total cost at most LM and each row path has a total cost at most  

> LN

.Edges ui,j x,M u and ui,j +1  

> x, 1

v, i.e. the edges connecting u and v to their neighbor in the cell gadget Gi,j . Each of these edges has length L and digging cost Lc+1 .Noting that all edges of H are still in H′, we have ℓ(H \ H′) = 0 . Therefore, we have 

∆( H, H ′) = ℓ(H′ \ H) + dig( H′ \ H) and so, we can rewrite ∆( H, H ′) as 

∆( H, H ′) ≤2 · LM + LN + 2 · (L + Lc + 1 )

≤3 · LN + 2 · (L + Lc + 1 ) (M > N )

≤5L. (N ≥ 2 ∧ c = 6) 

Noting that V (H) ⊆ V (H′) and Ta,a ′ ∪ { r} ⊆ V (H), we have Ta,a ′ ∪ { r} ⊆ V (H′) as well. Now we show that H′ is connected. Since no connected component of H consists of a single vertex by the precondition, both hi,j c,a ′ and hi,j +1 1,a ′ are connected to at least 42 Parameterized Complexity of Power Network Design 

one other vertex in H. Since u and v are their unique neighbors, the edges hi,j c,a ′ u

and hi,j +1 1,a ′ v are in E(H) and so in E(H′). Since hi,j c,a ′ and hi,j +1 1,a ′ belong to different connected components of H, u and v are also in different connected components of 

H. Noting that u and v are connected through the added part H′ \ H, the connected components of H, which are also in H′ before adding the new part H′ \ H, must become connected through the added part in H′.b. For some i ∈ [k], the terminals of Ci, 1 

> R

belong to a different connected component of H than pa′ (bi, →). Let u be the unique neighbor of hi, 11,a ′ and let x

be the index of the linking path of Ci, 1 

> R

that contains u. We set H′ to be a copy of H

union the following parts: the last column path and the first row path of the cell gadget Gi, 1. Recall that each column path has a total cost at most LM and each row path has a total cost at most  

> LN

.the edges bi, →ui, 11,1 and uu i, 1 

> x,M

, i.e. the edge connecting bi, → to one of its neighbors in the cell gadget Gi,j and the edge that connects u to its unique neighbor in the cell gadget Gi,j . These edges have lengths L′ and L and digging costs 0 and Lc+1 ,respectively. Since E(H) ⊆ E(H′), we have ℓ(H \ H′) = 0 . Thus, we can conclude that ∆( H, H ′) = 

ℓ(H′ \ H) + dig( H′ \ H) and so, we can rewrite ∆( H, H ′) as 

∆( H, H ′) ≤ LM + LN + L′ + L + Lc + 1 

≤2 · LN + L′ + L + Lc + 1 (M > N )

≤5L (N ≥ 2 ∧ L′ = 2 L ∧ c = 6) .We now show that H′ is connected. Since pa′ (bi, →) and hi, 11,a ′ are connected to some other vertices in H, the edges bi, →pa′ (bi, →) and uh i, 11,a ′ should be in H as well as these are the only edges that are incident on pa′ (bi, →) and hi, 11,a ′ respectively. Since pa′ (bi, →)

and hi, 11,a ′ are in different connected components of H, the vertices u and bi, → also must belong to different connected components of H. These connected components are in H′ as well before adding the new part. Then, since the added part connects u

and bi, →, these connected components become connected and form a single connected component in H′. Moreover, noting that V (H) ⊆ V (H′), we can conclude that H′

covers Ta,a ′ ∪ { r}.c. For some i ∈ [k], the vertex pa′ (bi, ←) belongs to a different connected component than the terminals in Ci,k R . This is symmetric to the previous case: Instead of the unique neighbor of hi, 11,a ′ , the unique neighbor of hi,k −1 

> c,a ′

is considered; Instead of the first row path and the last column path of the cell gadget Gi, 1, the first row path and the first column path of Gi,k are added; Instead of the edge bi, →ui, 11,1 and the edge between the unique neighbor of hi, 11,a ′

and its neighbor in the last column path of Gi, 1, the edge bi, ←ui, 11,M and the edge between the unique neighbor of hi,k −1 

> c,a ′

and its neighbor in the first column path of 

Gi,k are added. d. None of the previous subcases hold. In this case, for each i ∈ [k], we know that the terminals Ta,a ′ ∩

( ⋃

> j∈[k−1]

Ci,j R

)

belong to the same connected component of H as T. Hamm, B.M.P. Jansen and F. Motiei 43 

pa′ (bi, →) and pa′ (bi, ←). Since rcons is the unique neighbor of pa′ (rcons ), it is either in 

Cr or ¯Cr . We have three subcases for this scenario. i. For some i ∈ [k],pa′ (bi, →) belongs to a connected component other than the one that rcons belongs to. Let H′ be a copy of H, together with the edge bi, →rcons . Since this edge has length L′′ and digging cost 0, we have 

∆( H, H ′) = L′′ where L′′ = 5 L. We prove that the constructed H′ is connected. Since each connected component has at least two vertices, the vertex bi, → is in the same connected component as pa′ (bi, →) in H. Hence the inserted edge bi, →rcons 

connects the two connected components of H, resulting in a connected subgraph. Furthermore, since V (H) ⊆ V (H′), H′ includes Ta,a ′ ∪ { r}.ii. All terminals of H belong to Cr . In this case, Cr contains all terminals of H as well as r. We consider H′ as a copy of Cr . Clearly, it is a connected graph that covers Ta,a ′ ∪ { r} where ∆( H, H ′) ≤ 0 since E(H′) ⊆ E(H) and so 

ℓ(H′ \ H) + dig( H′ \ H) = 0 .iii. All terminals of H belong to ¯Cr . Let H′ be a copy of ¯Cr along with the edge rr cons . Recall that this edge has length X and digging cost 0. Hence ℓ(H′ \

H) + dig (H′ \ H) ≤ X. On the other hand, considering that rcons is the unique neighbor of pa′ (rcons ) ∈ Ta,a ′ , it should belong to ¯Cr , and so, the edge rr cons is not in H. Since Cr contains at least two vertices, the edge rr select must be in 

Cr since r must pass through this edge to reach other vertices. Thus, we have 

ℓ(H \ H′) ≥ X as the edges of Cr are in H but not in H′. Therefore, we can conclude that ∆( H, H ′) ≤ X − X = 0 . Clearly, H′ is connected as ¯Cr is connected and contains rcons , and since ¯Cr contain all terminals and r is also added to H′

through the edge rr cons , it contains Ta,a ′ ∪ { r}.Since the case distinction is exhaustive, this completes the proof for Claim 4.11 in the 

a = R case. We can show a similar argument for a = C as follows. Firstly, we swap the role of row and column connectors, the role of the row and columns of each cell gadget, and the role of rcons and rselect in the proof. We also note the following facts: The cost of adding a row or a column path of a cell gadget is at most LN ;The cost of adding and removing an edge from a linking or supporting path from a connector, and the cost of connections to rselect , b↑,1, . . . , b ↑,k and b↓,1, . . . , b ↓,k compared to the cost of connections to rcons , b1,→, . . . , b k, → and b1,←, . . . , b k, ← would remain the same. This completes the proof of this claim. ◀

We are not ready to prove Claim 4.12 – that each tree only intersects a specific subset of connectors. In more detail, we show that HR,▷ ∪ HR,◁ does not intersect column connectors and HC,▷ ∪ HC,◁ does not intersect row connectors. 

▷ Claim 4.12. For each a ∈ { ▷, ◁ }, we have 1. E(HR,a ) ∩ E(Ci,j C ) = ∅ for each i ∈ [k], j ∈ [k − 1] , and 2. E(HC,a ) ∩ E(Ci,j R ) = ∅ for each i ∈ [k − 1] , j ∈ [k].

Proof. Assume that for some i, j ∈ [k] and a ∈ { ▷, ◁ }, we have E(HR,a ) ∩ E(Ci,j C )̸ = ∅. We construct H′ 

> R,a

as follows. Initially, let H′ 

> R,a

= HR,a .1. If HR,a contains a path from one of the vertices of the last row path of Gi,j to one of the vertices of the first row path of Gi+1 ,j that lies completely in Ci,j C :44 Parameterized Complexity of Power Network Design 

a. We remove all edges of Ci,j C from H′

> R,a

. Since there is at least one path from one of the vertices the vertices of the last row path of Gi,j to one of the vertices of the first row path of Gi+1 ,j that lies completely in Ci,j C , after removing these edges, we reduce the cost by at least L · (c + 1) .b. Add the last row path of Gi,j and the first row path of Gi+1 ,j to H′

> R,a

. Recall that each of these paths has a total cost LN as shown before. After this, H′ 

> R,a

has at most two connected components, since we only lost connection between the vertices of the last row path of Gi,j and the vertices of the first row path of 

Gi+1 ,j and the vertices within each path are still connected. 

If H′ 

> R,a

has exactly one connected component: In this case, the cost increases by at most 2LN − 2 · L < 0, which contradicts the cost-minimality of the solution. 

If H′ 

> R,a

contains exactly two connected components. Since H′ 

> R,a

is a subgraph that contains all vertices TR,a ∪ { r} and has exactly two connected components, each of which has at least two vertices, by Claim 4.11, there exists a tree H′′  

> R,a

where 

dig (H′′  

> R,a

\ H′

> R,a

) + ℓ(H′′  

> R,a

\ H′

> R,a

) − ℓ(H′ 

> R,a

\ H′′ 

> R,a

) ≤ 5L. Therefore, by replacing 

HR,a in the considered minimum solution with H′′ 

> R,a

, we can construct a solution where the cost increases by at most 2LN + 5 L − L · (c + 1) < 0. This contradicts the cost-minimality assumption on the considered solution. 2. If HR,a does not contain any paths from the vertices of the last row path of 

Gi,j to the vertices of the first row path of Gi+1 ,j that lies completely in Ci,j C :

We modify H′ 

> R,a

as follows. a. We remove all edges of Ci,j C from H′

> R,a

.b. If there is a path between two vertices among the vertices of the last row path of Gi,j 

in HR,a , we add the the last row path of Gi,j to H′

> R,a

.c. Similarly, if there is a path between two vertices among the vertices of the first row path of Gi+1 ,j in HR,a , we add the the first row path of Gi+1 ,j to H′

> R,a

.In this case, the part of paths in HR,a that intersect the interior of Ci,j C should be either between two vertices among the vertices of the last row path of Gi,j or between two vertices among the vertices of the first row path of Gi+1 ,j . Thus, H′ 

> R,a

must be still connected after these modification steps. On the other hand, since no terminal in Ci,j C

belongs to TR,a and we are considering the minimum-cost solution, there is at least one path in HR,a that is either between two vertices among the vertices of the last row path of Gi,j or between two vertices among the vertices of the first row path of Gi+1 ,j in HR,a 

and lies completely in Ci,j C . Thus, there are at least two edges of length L in Ci,j C that belong to HR,a and so, after step 1.a, the cost decreases by at least 2 · L. Recall that each row path has a total cost LN . Hence, after these modifications, the cost increases by at most 2 · LN − 2 · L < 0, which contradicts the cost-minimality of the solution. This completes the proof for the first case of Claim 4.12. We can show similarly that 

E(HC,a ) ∩ E(Ci,j R ) = ∅ by swapping the role of row and column connectors, the role of the rows and columns of each cell gadget, and the role of rcons and rselect in the proof and noting that the cost of adding a row or a column path of a cell gadget costs at most LN , and the cost of adding or removing the supporting paths from connectors is at most L  

> 4( N+1)( c+1)

,and T. Hamm, B.M.P. Jansen and F. Motiei 45 

the cost of adding and removing an edge from a linking or supporting path from a connector, and the cost of connections to rselect , b↑,1, . . . , b ↑,k and b↓,1, . . . , b ↓,k compared to the cost of connections to rcons , b1,→, . . . , b k, → and b1,←, . . . , b k, ← would remain the same. This proves the claim. ◀

The other property that we mentioned for a minimum solution is that each tree only intersects a specific subset of the vertices of the root part of G. More elaborately, HR,▷ ∪ HR,◁ 

does not contain any vertices from the column root part, and HC,▷ ∪ HC,◁ does not contain any vertices from the row root part of G.

▷ Claim 4.13. For a ∈ { ▷, ◁ }, we have 1. V (HR,a ) ∩ { bi,j | i ∈ {↑ , ↓} ∧ j ∈ [k]} = ∅, and 2. V (HC,a ) ∩ { bi,j | i ∈ [k] ∧ j ∈ {→ , ←}} = ∅.

Proof. Firstly, we show that V (HR,a ) ∩ { bi,j | i ∈ {↑ , ↓} ∧ j ∈ [k]} = ∅. The proof for showing that V (HC,a ) ∩ { bi,j | i ∈ [k] ∧ j ∈ {→ , ←}} = ∅ can be obtained similarly, where instead of considering the cell gadgets of the first and last rows, we consider the cell gadgets of the first and last columns. Assume towards a contradiction that for some j ∈ [k], the vertex b↑,j is in HR,a ; we will treat b↓,j later. We prove the claim by making a case distinction on whether rr select belongs to HR,a or not. 

rr select ∈ E(HR,a ). We construct a subgraph H′ 

> R,a

from HR,a by modifying it as follows. We remove all edges that are incident on the vertices {b↑,j ′

| j′ ∈ [k]} as well as the edge rr select . Recall that the edge rr select has length X and hence, the removed part has length at least X.For each j′ ∈ [k], we add the first row path of G1,j ′

. Recall that the cost of each row path is at most LN .For each j′ ∈ [k−1] , we add the first linking path of C1,j ′ 

> R

along with all of its supporting paths. Recall that the cost of a linking path of a connector is (c + 2) · L, and the cost of each of c supporting paths of a connector is L 

> 4( c+1)

.We add the edges 

∗ rcons b1,→ which has cost L′′ ,

∗ b1,→u1,11,1 and u1,k  

> 1,M

b1,← where each costs L′.This procedure increases the cost by at most 

L′′ + 2 L′ + k · LN + ( k − 1) · (( c + 2) · L + L

4( c + 1) · c) − X < 0.

We claim that this subgraph is connected and includes all terminals covered by HR,a as well as r. Since we did not remove any edges that are incident on a terminal of HR,a ,it is clear that H′ 

> R,a

contains all those terminals. Furthermore, by Claim 4.12, we have 

E(HR,a ) ∩ E(C1,j ′ 

> C

) = ∅ for all j′ ∈ [k − 1] which shows that removal edges in step 4.2 can only disconnect a part of the terminals of TR,a that lies within the first row, i.e. the terminals of the row connectors C1,1 

> R

, . . . , C1,k −1 

> R

along with pa(b1,→) and pa(b1,←),from the rest of the terminals. The added part connects all the terminals in the first row to r. On the other hand, rcons should be in the same connected component as r46 Parameterized Complexity of Power Network Design 

and so the same connected component as the terminals that does not lie within the first row. Thus, all terminals should be connected to r in H′ 

> R,a

which means that H′ 

> R,a

is connected and contains all the terminals in HR,a and r. So, replacing HR,a with H′ 

> R,a

in the solution results in a smaller cost, which contradicts the cost-minimality assumption on the considered solution. 

rr select /∈ E(HR,a ). Since the pendant vertices of b↑,j are not in HR,a , neither b↑,j nor its pendant vertices can be a leaf of HR,a due to cost-minimality. Therefore, there should be some y ∈ [M ] such that the edge b↑,j u1,j  

> 1,y

is in HR,a . We construct a subgraph H′

> R,a

as follows. Let H′ 

> R,a

initially be the same as HR,a which we modify it as follows, step by step. 1. All edges between b↑,j and the vertices of G1,j are removed. Recall that each of these edges has length L′.2. All edges of the first row path of G1,j are added. Recall a row path has length LN .3. If the connected component that contains b↑,j does not intersect TR,a ∪ { r}, remove it from H′

> R,a

.This increases the cost by at most LN − L′.If H′ 

> R,a

is connected after this modification, then replacing HR,a by H′ 

> R,a

results in a solution with smaller cost since L′ = 2 L. This contradicts the cost-minimality assumption on the solution. So, there should be exactly two connected components in H′ 

> R,a

that arise from losing connectivity of the vertices of the first row path in G1,j and the vertex b↑,j . In this case, we continue the modification as follows. Noting that b↑,j is connected to at least one terminal in TR,a or r in H′

> R,a

, the connected component that contains b↑,j must cover the edge rselect b↑,j since this is the only edge that is incident on b↑,j that is not removed from H′

> R,a

. This shows that b↑,j is a leaf of H′

> R,a

. Since this vertex is not a terminal, we remove it from H′ 

> R,a

along with its edge rselect b↑,j , which reduces the cost by at least L′′ .Note that the connected component that contains rselect still has a terminal of HR,a or r

as well. Thus, this procedure results in a subgraph that still covers TR,a ∪ { r} and has exactly two connected components, each having at least two vertices. Hence by Claim 4.11, there exists a tree H′′  

> R,a

where dig (H′′  

> R,a

\ H′

> R,a

) + ℓ(H′′  

> R,a

\ H′

> R,a

) − ℓ(H′ 

> R,a

\ H′′ 

> R,a

) ≤ 5L.Therefore, by replacing HR,a in the minimum solution considered with H′′ 

> R,a

, we can construct a solution where the cost increases by at most LN + 5 L − L′ − L′′ < 0 recalling that L′ = 2 L and L′′ = 5 L. This contradicts the cost-minimality assumption on the considered solution. Now we show that for all j ∈ [k], the vertex b↓,j is not in HR,a ; the argument for b↑,j 

is symmetric. Assume towards a contradiction that this is not the case and so, for some 

j ∈ [k], b↓,j is in HR,a . Then, since no terminal of TR,a is connected to b↓,j and b↓,j is only connected to the vertices of the last row path of Gk,j , we can remove b↓,j along with all incident edges of HR,a and add the last row path of Gk,j to construct a subgraph H′

> R,a

.Clearly, this new subgraph is connected and still covers all the terminals in TR,a . However, through this modification, the cost is increased by at most LN − L′ < 0, contradicting the cost-minimality of the solution. ◀

The following claim restricts the number of edges that appear in the solution from each level of a connector. This restriction is useful in providing a lower bound for the cost that arises from each connector. In more detail, this restriction not only provides a precise value for the cost arising from the linking paths of the connectors, but forces the solution to include all supporting paths, resulting in the lower bound presented in Claim 4.15. From now on, we refer to the union of the solution trees HR,▷ ∪ HR,◁ ∪ HC,▷ ∪ HC,◁ by HS .T. Hamm, B.M.P. Jansen and F. Motiei 47 

▷ Claim 4.14. For each q ∈ [c + 1] and each connector C, the union of solution trees HS

contains exactly one edge from the q-th level of C.

Proof. By Claim 4.12, there is no edge of HR,▷ or HR,◁ in a column connector, and there is no edge of HC,▷ or HC,◁ in a row connector. Therefore, to explore the edges of HS =

HR,▷ ∪ HR,◁ ∪ HC,▷ ∪ HC,◁ in a column connector, it suffices to consider the edges of HR,▷ 

and HR,◁ . Similarly, to explore the edges of HS in a row connector, it suffices to consider the edges of HC,▷ and HC,◁ . On the other hand, considering both Claim 4.12 and Claim 4.13 and noting that there should be a path from bi, → to bi, ← for each i ∈ [k] in each of the trees 

HR,▷ and HR,◁ , each of these trees must contain at least one edge in each level of each row connector, and noting that there should be a path from b↑,j to b↓,j for each j ∈ [k] in each of trees HC,▷ and HC,◁ , each of these trees must contain at least one edge from each level of each column connector. To complete the proof, We demonstrate that there can be no more than one edge from each level of each connector. Assume for contradiction that for a connector C and a value of 

q ∈ [c + 1] , the subgraph HS contains more than one edge of the q-th level of this connector. First, we discuss the case where C is a row connector. I.e. let C = Ci,j R for some i ∈ [k] and 

j ∈ [k − 1] . We prove the claim considering three subcases. 1. q ∈ { 2, . . . , c }. Construct two subgraphs H′ 

> R,▷

and H′ 

> R,◁

as follows. Let them initially be the same as HR,▷ and HR,◁ , respectively. We modify them as follows. a. We remove all edges of the q-th level of C from each of these subgraphs. Noting that only HR,▷ and HR,◁ intersect this connector and recall that each of these edges has a cost L + Lc+1 , this step decreases the cost by at least 2( L + Lc+1 ).b. We add one of the edges of the q-th level to both of subgraphs. This edge is of length 

L and digging cost Lc+1 .c. We add the interior of the q-th and the interior of the (q + 1) -th supporting paths of C to both H′ 

> R,▷

and H′

> R,◁

. Recall that each edge of a supporting path is of cost   

> L
> 4( c+1)( N+1)

and the interior contains N + 1 − 2 such edges. Since each edge in the q-th level of C makes a connection between a vertex of the interior of the q-th supporting path and a vertex of the interior of the (q + 1) -th supporting path, both H′ 

> R,▷

and H′ 

> R,◁

are connected after this procedure. If either of H′ 

> R,▷

and 

H′ 

> R,◁

contains a cycle, we consider a spanning tree of that subgraph. Now, if we replace 

HR,▷ and HR,◁ by H′ 

> R,▷

and H′ 

> R,◁

in the initial minimum-cost solution, the cost increases by at most 2L + Lc+1 + 4( N − 1) · L  

> 4( c+1)( N+1)

− 2( L + Lc+1 ) < 0 which contradicts the cost-minimality assumption on the initial solution. 2. q = 1 . From the previous case, we know that for each q ∈ { 2, . . . , k − 1}, subgraph 

HR,▷ ∪ HR,◁ contains at most one edge from the q-th level of C. Let hi,j 

> 1,x

hi,j  

> 2,x

be the edge from the second level that is in HR,▷ ∪ HR,◁ where x ∈ [N ] which we know to be unique from the previous case. We construct H′ 

> R,▷

and H′ 

> R,◁

from HR,▷ and HR,◁ with following modifications. a. We remove all edges of the first level of C from both of these subgraphs, where each of these edges has length L and digging cost Lc+1 and b. We add ui,j y,M hi,j  

> 1,x

which is of length L and digging cost Lc+1 ,c. We add the last column path of Gi,j and the interior of the first supporting path of the connector to both; recall that the length of each column path is at most L 

> 40 M

, each edge of a supporting path is of cost L  

> 4( c+1)( N+1)

, and the interior of a supporting path contains N + 1 − 2 such edges. 48 Parameterized Complexity of Power Network Design 

Since the edges in the first level make a connection between the vertices of the last column path of Gi,j and the vertices of the first supporting path of C, and considering that the first supporting path is already in HR,▷ ∪ HR,◁ and hence also in H′ 

> R,▷

∪ H′

> R,◁

, the added edges connect all the vertices of the last column path of Gi,j to hi,j  

> 1,x

in both H′ 

> R,▷

and 

H′

> R,◁

. This implies the connectivity of both of these subgraphs. Moreover, clearly, H′

> R,▷

still covers all terminals covered by HR,▷ and H′ 

> R,◁

still covers all terminals covered by 

HR,◁ . If we replace HR,▷ and HR,◁ by H′ 

> R,▷

and H′

> R,◁

, this modification increases the cost of the solution by at most 2L + Lc+1 + 2 · L 

> 40 M

+ 2( N − 1) L  

> 4( N+1)( c+1)

− 2( L + Lc+1 ) < 0

which contradicts the cost-minimality assumption on the considered solution. 3. q = c + 1 . This is symmetric to the previous case. Using a similar argument considering the last supporting path of C and the first column path of Gi,j +1 , we can show that in the last level of C, there cannot be more than one edge of HR,▷ ∪ HR,◁ .In each of these three cases, we have proved that there is at most one edge of HR,▷ ∪ HR,◁ in each level of C.The proof to show that HC,▷ ∪ HC,◁ contains at most one edge from each level of a column connector is similar to the analogue statement for row connectors; instead of column paths, we consider the row paths of adjacent cell gadgets. Recalling that HR,▷ and HR,◁ contain at least one edge from each level of each row connector and HC,▷ and HC,◁ contain at least one edge from each level of each column connector, we can conclude that HS contains exactly one edge from each level of each connector. ◀

Knowing which connectors and root parts each tree in a minimum-cost solution intersects, and that exactly one edge from each level of a connector appears in HS , we can now obtain a lower bound for the cost that arises from the part of a solution within each connector. Claim 4.15 presents this lower bound and a property that must hold in the case that this lower bound is tight, i.e. the cost that arises from the part of a solution in a connector is precisely the lower bound. 

▷ Claim 4.15. For each connector C, the value of cost C (S) is at least (2 c + 3 + c 

> 4( c+1)

) · L. If cost C (S) = (2 c + 3 + c 

> 4( c+1)

) · L, then 1. C is a row connector Ci,j R and HR,▷ ∩ HR,◁ ∩ C i,j R is a single linking path of Ci,j R , or 2. C is a column connector Ci,j C and HC,▷ ∩ HC,◁ ∩ C i,j C is a single linking path of Ci,j C .

Proof. Recall that by Claim 4.12 there is no edge of HC,▷ and HC,◁ in any row connector and there is no edge of HR,▷ and HR,◁ in any column connector. By Claim 4.14, there is exactly one edge from each level of a row connector in HR,▷ ∪ HR,◁ and exactly one edge from each level of a column connector in HC,▷ ∪ HC,◁ . Because, by construction, there must be a path from bi, → to bi, ← for each i ∈ [k] in each of the trees HR,▷ and HR,◁ , both trees must contain the single edge of HS from each level of a row connector. With a similar argument, we can conclude that the single edge of HS in each level of a column connector must appear in both HC,▷ and HC,◁ . Hence, to cover all the terminals of C, all supporting paths of C should be in HS ∩ C . Therefore, the total length cost arising from S in C is at least 2( c + 1) · L + c · L 

> 4( c+1)

= (2 c + 2 + c 

> 4( c+1)

) · L and the total digging cost is at least L

arising from single edges of each level of C. This implies that (2 c + 3 + c 

> 4( c+1)

) · L is a lower bound for cost C (S).This lower bound is tight if each edge of the supporting paths is used exactly once, which implies that the single edges of different levels must be from the same linking path of C and thus, if C is a row connector Ci,j R , HR,▷ ∩ HR,◁ ∩ C i,j R is a single linking path of Ci,j R , and if C is a column connector Ci,j R , HC,▷ ∩ HC,◁ ∩ C i,j C is a single linking path of the connector Ci,j C . ◀T. Hamm, B.M.P. Jansen and F. Motiei 49 

We now turn our attention from the behavior of minimum-cost solutions in connectors to that in cell gadgets. In our analysis the following notation will be useful: For a subgraph 

H of G, we denote the set of horizontal edges of Gi,j that are in H by Ei,j H (H) and the set of vertical edges of Gi,j that are also in this subgraph by Ei,j V (H). In addition, we define 

EH (H) = ⋃ 

> i,j ∈[k]

Ei,j H (H) and EV (H) = ⋃ 

> i,j ∈[k]

Ei,j V (H) as the horizontal and vertical edges of H.Knowing that HR,▷ and HR,◁ only intersect row connectors and HC,▷ and HC,◁ only intersect column connectors, we can observe that by the graph structure, cell gadgets play an important role in the connectivity of each tree in S. With this information, the following claim demonstrates a lower bound for the cost of the part of HS in each cell gadget. Additionally, if this lower bound is tight, some structural properties are presented in this claim for the part of each tree that lies within a cell gadget. 

▷ Claim 4.16. For each i, j ∈ [k], let Li,j = ℓ(Ei,j H (HC,▷ )) + ℓ(Ei,j H (HC,◁ )) . The value of cost Gi,j (S) is at least 

2( M − 1 − δ)ℓH + 2 δ + Li,j 

︸ ︷︷ ︸

> length of horizontal edges

+ (M − 1) dH

︸ ︷︷ ︸

> digging cost of horizontal edges

+ 2( N − 1) ℓV

︸ ︷︷ ︸

> length of vertical edges

.

If equality is attained, then, 1. HR,▷ ∩ Gi,j is equal to HR,◁ ∩ Gi,j and is a row path of Gi,j , and 2. HC,◁ ∩ Gi,j is equal to HC,◁ ∩ Gi,j ,3. all the edges in Ei,j H (HC,▷ ) are also in HR,▷ , and 4. there are exactly n − 1 edges in Ei,j V (HC,▷ ) where for each x ∈ [n − 1] , there is exactly one edge of Ei,j V (HC,▷ ) that connects a vertex of x-th row path to a vertex of (x + 1) -th row path. 

Proof. Considering Claim 4.12 and Claim 4.13 and noting that there should be a path from 

bi, → to bi, ← for each i ∈ [k] in each of trees HR,▷ and HR,◁ , each of these trees must contain a path P from one of the vertices of the first column path of Gi,j to a vertex of the last column path of Gi,j . So, for each y ∈ [M − 1] , the path P contains at least one edge among the horizontal edges between the vertices of y-th and (y + 1) -th column paths of Gi,j . For each y ∈ [M − 1] if the edge of P between the vertices of y-th and (y + 1) -th column paths and the edge of P between the vertices of (y + 1) -th and (y + 2) -th column paths of Gi,j are from different row paths, there should be at least one edge of (y + 1) -th column path of Gi,j 

in P . Noting that by construction, each row path contains δ edges of length 1, if P contains horizontal edges from x different row paths, ℓ(P ) is at least 

x · δ + ( M − 1 − x · δ)ℓH + ( x − 1) ℓV

= ( M − 1) ℓH + x · (δ − δ · ℓH + ℓV ) − ℓV

≥ (M − 1) ℓH + ( δ − δ · ℓH + ℓV ) − ℓV

= ( M − 1 − δ)ℓH + δ

since 

δ + ℓV − δ · ℓH ≥ δ + ℓV − n · ℓH

= δ + M − M = δ > 0.

As there are two trees, namely, HR,▷ and HR,◁ that contain such a path, this adds a value of at least 2( M − 1 − δ)ℓH + 2 δ to the cost. Again, considering Claim 4.12 and Claim 4.13 and 50 Parameterized Complexity of Power Network Design 

noting that there should be a path from b↑,j to b↓,j for each j ∈ [k] in each of trees HC,▷ 

and HC,◁ , each of these trees must contain a path P ′ from one of the vertices of the first row path of Gi,j to a vertex of the last row path of Gi,j . Thus, for each y ∈ [M − 1] , the path P ′ contains at least one edge among the vertical edges between the vertices of y-th and 

(y + 1) -th row paths of Gi,j and hence, ℓ(Ei,j V (P ′)) is at least (N − 1) ℓV . Since such a path appears in both HC,▷ and HC,◁ , this adds a value of at least 2( N − 1) ℓV + Li,j to the cost. To compute the total digging cost of S in Gi,j , assume that we first pay for the digging cost of (HR,▷ ∪ HR,◁ ) and then pay the additional cost for HC,▷ ∪ HC,◁ . Since P contains at least M − 1 horizontal edges, the total digging cost of the edges of P is at least (M − 1) dH .So, the total digging cost of of S in Gi,j is also at least (M − 1) dH . Therefore, the value of cost Gi,j (S), which is the summation of total length and total digging cost of S in Gi,j is at least 

2( M − 1 − δ)ℓH + 2 δ

︸ ︷︷ ︸ 

> length of two copies of P

+ 2( N − 1) ℓV + Li,j 

︸ ︷︷ ︸  

> length of two copies of P′

+ ( M − 1) dH

︸ ︷︷ ︸

> digging cost

.

Clearly, if HS ∩ Gi,j contains any edges other than the edges of P and P ′, the cost must be more than 2( M − 1 − δ)ℓH + 2 δ + 2( N − 1) ℓV + Li,j + ( M − 1) dH . So, we can conclude that if the total cost is 2( M − 1 − δ)ℓH + 2 δ + 2( N − 1) ℓV + Li,j + ( M − 1) dH , we must have HR,▷ ∩ Gi,j = HR,◁ ∩ Gi,j = P H C,▷ ∩ Gi,j = HC,◁ ∩ Gi,j = P ′ which implies a part of property 1, property 2, and property 4 of the claim. On the other hand, ℓ(P ) is at least 

x · δ + ( M − 1 − x · δ)ℓH + ( x − 1) ℓV which must be exactly (M − 1 − δ)ℓH + δ and so x = 1 .This means that all edges of P are from a single row path of C and property 1 of the claim holds. Moreover, since we only computed the digging cost of P , to achieve this total cost, the digging cost of P ′ should be 0. Since the digging cost of none of the horizontal edges is 

0, all horizontal edges of P ′ must be shared with P and thus, property 3 holds as well. So, if the value 2( M − 1 − δ)ℓH + 2 δ + 2( N − 1) ℓV + Li,j + ( M − 1) dH is tight, all three mentioned properties hold. ◀

To cover terminals connected to b↓,j and considering Claim 4.12 and Claim 4.13, for each j ∈ [k], there must be a path Pj from b↑,j to b↓,j in trees HC,▷ and HC,◁ . Taking this into account, Claim 4.17 explores a minimum solution and proves that the lower bounds provided in Claim 4.15 and Claim 4.16 must be tight in all connectors and cell gadgets in a minimum-cost solution. Then, using these claims, it provides some additional properties of a minimum solution that help us to construct a solution for the given Regular Grid Tiling 

instance. Specifically, they allow us to fix the second value of the chosen pair from each cell 

Si,j .

▷ Claim 4.17. If S is a solution with minimum cost, then we have following facts. 1. For each i, j ∈ [k], let Li,j = ℓ(Ei,j H (HC,▷ )) + ℓ(Ei,j H (HC,◁ )) . The value of cost Gi,j (S) is 

2( M − 1 − δ)ℓH + 2 δ + 2( N − 1) ℓV + ( M − 1) dH + Li,j ,

2. For each connector C, the value of cost C (S) is (2 c + 3 + c 

> 4( c+1)

) · L,3. For each j ∈ [k], there exists some value yj such that the edges b↑,j u1,j   

> 1,(yj−1)( k+1)+1

and 

uk,j n,y j (k+1) b↓,j are in both HC,▷ and HC,◁ , and 4. For each j ∈ [k], ⋃ki=1 Ei,j H (HC,▷ ) = ⋃ki=1 Ei,j H (HC,◁ ) is a set of k edges of length 1.

Proof. If we exclude the first and last edges of Pj , what remains is a path from u1,j 

> 1,y ·(k+1)+1

to uk,j n,y ′·(k+1) for some 0 ≤ y < n and 1 ≤ y′ ≤ n. If y̸ = y′, then Pj must have at least 

k horizontal edges within cell gadgets and the edge sets of supporting paths in column T. Hamm, B.M.P. Jansen and F. Motiei 51 

connectors. This implies that the total length of horizontal edges is at least k since all such edges have length at least 1. If y = y′, it must contain at least one edge among the horizontal edges between the vertices of (y(k + 1)) -th column path and the vertices of (y(k + 1) + 1) -th column path of a cell gadget and the edges of supporting paths in column connectors. Such an edge has length at least ℓH since the horizontal edges between the vertices of (y(k + 1)) -th column path and the vertices of (y(k + 1) + 1) -th column path of a cell gadget are of length 

ℓH and the edges of supporting paths are of length L  

> 4( c+1)( M+1)

> ℓ H . Therefore, the total length of horizontal edges in each of HC,▷ and HC,◁ is at least k · k. Therefore, by Claim 4.16, the total cost arising from all cell gadgets is at least 

Ccells =2 k2(( M − 1 − δ)ℓH + δ + ( N − 1) ℓV + ( M − 1) dH ) + ∑

> i,j ∈[k]

Li,j 

= 2 k2(( M − 1 − δ)ℓH + δ + ( N − 1) ℓV + ( M − 1) dH ) + 2 k2

= 2 k2(( M − 1 − δ)ℓH + δ + ( N − 1) ℓV + ( M − 1) dH + 1) .

Furthermore, by Claim 4.13, for each i ∈ [k], the only way to connect the root r to the terminal bi, ← is through rcons bi, →, an edge between bi, → and the vertices of the first column path of Gi, 1, and an edge between the vertices of the last column path of Gi,k and bi, ←.Also, for each j ∈ [k], the only way to connect the root r to the terminal b↓,j is through the edge rselect b↑,j , an edge between b↑,j and the vertices of the first row path of G1,j , and an edge between the vertices of the last row path of Gk,j and b↓,j . Since each of these edges must appear in two trees, this costs in total 8k · L′ + 4 k · L′′ . Moreover, for these connections we need to have rr cons in both HR,▷ and HR,◁ and rr select in both HC,▷ and 

HC,◁ . In addition, for connection to the pendant terminals of rcons , bi, →, bi, ←, rselect , b↑,j ,and b↓,j we need a length edge X in each tree. So, the total cost arising from the root part is at least Croot part = 8 k · L′ + 4 k · L′′ + (8 + 8 k)X.So, the total cost within the connectors must be at most β − Ccells − Croot part =2k(k − 1) · (2 c + 3 + c 

> 4( c+1)

) · L. Since we have 2k(k − 1) connectors and considering Claim 4.15, the cost within each connector must be exactly (2 c + 3 + c 

> 4( c+1)

) · L. This shows that the total cost within the connectors must be exactly 2k(k − 1) · (2 c + 3 + c 

> 4( c+1)

) · L. As a result, the lower bound Ccells for the total cost of parts in the cell gadgets is also tight, which implies that the total cost of the horizontal edges in each of HC,▷ and HC,◁ must be exactly k2 and so the total cost of the edges in ⋃ki=1 Ei,j H (HC,▷ ) = ⋃ki=1 Ei,j H (HC,◁ ) for each j ∈ [k] must be precisely k. As we explained, this is possible only if there are exactly k edges of length 1

and so yj = y′ = y + 1 since if y + 1 < y ′, by graph construction, there would be at least one edge between the vertices of the (( y + 1)( k + 1)) -th column path and the vertices of the 

(( y + 1)( k + 1) + 1) -th column path of a cell gadget that appears in the horizontal edges of 

Pj which has length k + 1 . ◀

Note that the value provided in property 1 of Claim 4.17 is equal to the minimum cost of a cell gadget presented in Claim 4.16 which shows that this cost is tight for each cell gadget in a minimum-cost solution. Moreover, the value provided in property 2 is equal to the minimum cost of a connector presented in Claim 4.15 yielding that this cost is tight for each connector in a minimum-cost solution. To cover the terminals connected to bi, ← and considering Claim 4.12 and Claim 4.13, for each i ∈ [k], there must be a path P ′ 

> i

from bi, → to bi, ← in trees HR,▷ and HR,◁ . Taking into account this and knowing that the lower bounds provided by Claim 4.15 and Claim 4.16 are tight, we introduce some new properties for the parts of the solution that lie within a cell gadget and the parts of the solution that lie within a connector, which guide us to fix the 52 Parameterized Complexity of Power Network Design 

first value of the pairs chosen from each cell Si,j for a solution to the given Regular Grid Tiling instance. 

▷ Claim 4.18. For each i ∈ [k], there exists a value xi such that 1. for each j ∈ [k], the subgraph HR,▷ ∩ Gi,j is the xi-th row path of Gi,j , and 2. for each j ∈ [k − 1] , the subgraph HR,▷ ∩ HR,◁ ∩ C i,j R is the xi-th linking path of Ci,j R .

Proof. Since by Claim 4.17, the lower bound for the cost arising from each cell gadget must be tight to achieve the total cost β and considering Claim 4.16, HR,▷ ∩Gi,j = HR,◁ ∩Gi,j must be a single row path which is part of P ′ 

> i

. Furthermore, for each row connector CRi, j , as we explained, the cost must be exactly (2 c + 3 + c 

> 4( c+1)

) · L, so by Claim 4.15, HR,▷ ∩ HR,◁ ∩ Gi,j 

must be a single linking path which is part of P ′ 

> i

. Recalling that the cost of solution within each cell gadget and the cost solution within each row connector is tight, the union of these single paths from the cell gadgets and connectors must be equal to P ′ 

> i

without its first and last edge. Thus, the union of these single paths from the cell gadgets and the connectors must be equal to the interior of P ′ 

> i

. Thus, for each cell gadget Gi,j , if this single path is the xi,j -th row path, due to the connectivity of P ′ 

> i

, the single path from Ci,j R and the single path from Ci,j −1 

> R

(if j > 1) must also be the xi,j -th linking path of them. Therefore, we have 

xi,j = xi,j ′ for all j, j ′ ∈ [k], and so, for xi = xi, 1, the conditions of the claim hold. ◀

Claim 4.19 states the last necessary property of a minimum solution, which we need to prove the existence of a pair with the first and second chosen values in each Si,j of the given 

Regular Grid Tiling instance. 

▷ Claim 4.19. For each i, j ∈ [k], there is exactly one horizontal edge in (HC,▷ ∪ HC,◁ ) ∩ Gi,j 

and this edge has length 1.

Proof. By property 3 of Claim 4.17, we have HC,▷ ∩ Gi,j = HC,◁ ∩ Gi,j and this is equal to 

(HC,▷ ∪ HC,◁ ) ∩ Gi,j . By property 4 of the same Claim, ⋃ki=1 Ei,j H (HC,▷ ) = ⋃ki=1 Ei,j H (HC,◁ )

is a set of edges of length 1. By Claim 4.16, these edges must be shared with HR,▷ . Taking into account Claim 4.18, for each i, j ∈ [k], subgraph HR,▷ ∩ Gi,j is exactly one row path, the xi-th row path of Gi,j . So, Ei,j H (HC,▷ ) must also belong to this row path. Taking into account property 4 of Claim 4.16, there are exactly n − 1 vertical edges of HC,▷ ∩ Gi,j where for each x′ ∈ [n − 1] , there is exactly one edge of HC,▷ ∩ Gi,j that connects a vertex of the 

x′-th row path to a vertex of the (x′ + 1) -th row path. Thus, we can conclude that Ei,j H (HC,▷ )

should be a subset of the edges of the xi-th row path that appear consecutively in this path. Since by graph construction, the length of the edge appears after each edge of length 1 is 

ℓH = k + 1 and we know that all the edges in Ei,j H (HC,▷ ) have length 1, there cannot be more than one horizontal edge in HC,▷ ∩ Gi,j = HC,◁ ∩ Gi,j = ( HC,▷ ∪ HC,◁ ) ∩ Gi,j . On the other hand, since ⋃ki=1 Ei,j H (HC,▷ ) is a set of k edges and noting that there cannot be more than one of them in each Gi,j , there must be exactly one of them in each of these cell gadgets and hence the Claim. ◀

Now, with the properties shown for a solution to the HVND -instance, we construct the solution to the Regular Grid Tiling instance as follows. For each i, j ∈ [k], we choose the pair (xi, y j ) from Si,j where xi is the value found by Claim 4.18 and yj is the value found by property 3 of Claim 4.17. We prove that such a pair exists in Si,j by showing that the edge 

ui,j xi,(k+1)( yj −1)+ iui,j xi,(k+1)( yj −1)+ i+1 must have length 1. Considering that costs are tight due to Claim 4.17, by Claim 4.16 and Claim 4.15, HC,▷ and HC,◁ share all edges in connectors and cell gadgets except for the edges of the supporting paths in the connectors which are T. Hamm, B.M.P. Jansen and F. Motiei 53 

completely disjoint, Pj ∩ Gi,j must be equal to HC,▷ ∩ Gi,j = HC,◁ ∩ Gi,j and Pj ∩ C i,j C must be equal to HC,▷ ∩ HC,◁ ∩ C i,j C . By property 3 of Claim 4.17, if we exclude the first and last edges of Pj , it is a path from u1,j   

> 1,(yj−1)( k+1)+1

to uk,j n,y j (k+1) . If we go through this path, the first vertex of the cell gadget Gi,j that we reach should be the vertex ui,j  

> 1,(yj−1)( k+1)+ i

.This is because we pass through exactly i − 1 horizontal edges in total in the previous cell gadgets, and by Claim 4.15, the parts in the column connectors are straight paths. Since by Claim 4.19, there is exactly one horizontal edge in Pj ∩ Gi,j that by Claim 4.15 is shared with HR,▷ and so, by Claim 4.18, it belongs to the xi-th row path of Gi,j , this edge must be ui,j xi,(yj −1)( k+1)+ iui,j xi,(yj −1)( k+1)+ i+1 . Finally, by Claim 4.17, we know that this edge must have length 1 and so, by graph construction, (xi, y j ) must be in Si,j . The part of the solution in a cell gadget is shown in Figure 7b for an instance of Si,j .We have proven that a Regular Grid Tiling -solution for (Si,j )(i,j )∈[k]2 induces a 

HVND -solution for I. This concludes the proof of the correctness of our reduction. Recall that the number of terminals in our constructed HVND -instance is O(k2) and that t = 4 

is constant. Hence if there were f, f ′ such that HVND can be solved in time f (|T |, t ) ·|V (G)|o(√|T |)·f ′(t) for any instance I then we could solve the original Regular Grid Tiling 

instance in time ˜f (k) · no(k) which by Theorem 4.8 contradicts the ETH. ◀

## 5 Conclusion 

In this paper, we introduced several power network design problems that arose from real-world applications. Despite their similarities to Steiner Tree , we showed that the parameteriza-tion by the number of terminals and solution trees is W[1]-hard. We leave the exploration of other parameterizations, such as treewidth, to future work. A distinguishing feature of our power network design problems, which are shared by 

Minimum-Cost Shared Arborescence [ 1], is that a solution consists of multiple parts and a suitable sharing of resources among the parts leads to a lower overall cost. It would be interesting to introduce this setting to other problems. For example, consider the Subset Traveling Salesperson Problem in which the goal is to find a minimum-cost closed walk that visits all terminals. One could investigate the version in which we look for a set of closed walks that together visit all terminals, with the number of terminals visited by a single walk upper-bounded by a given threshold, while minimizing a weighted sum of the total length of the individual walks and the visiting costs of the edges in the union of the walks. Which algorithmic approaches for solving Subset TSP carry over to this setting? 

References 

1 Eduardo Álvarez-Miranda, Ivana Ljubic, Martin Luipersbeck, and Markus Sinnl. Solving minimum-cost shared arborescence problems. Eur. J. Oper. Res. , 258(3):887–901, 2017. 

doi:10.1016/J.EJOR.2016.11.004 .

2 Matthias Bentert, André Nichterlein, Malte Renken, and Philipp Zschoche. Using a geometric lens to find k disjoint shortest paths. In Nikhil Bansal, Emanuela Merelli, and James Worrell, editors, 48th International Colloquium on Automata, Languages, and Programming, ICALP 2021 , volume 198 of LIPIcs , pages 26:1–26:14. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2021. doi:10.4230/LIPICS.ICALP.2021.26 .

3 Paolo Bertoldi, Javier López-Lorente, and Nicola Labanca. Energy consumption and energy efficiency trends in the eu-28 2000-2014. Jt. Res. Cent. Ispra Italy , 1:1–175, 2016. 54 Parameterized Complexity of Power Network Design 

4 Hans L. Bodlaender, Marek Cygan, Stefan Kratsch, and Jesper Nederlof. Deterministic single exponential time algorithms for connectivity problems parameterized by treewidth. 

Information and Computation , 243:86–111, 2015. doi:10.1016/j.ic.2014.12.008 .

5 Xiu Zhen Cheng and Ding-Zhu Du, editors. Steiner Tree Problems in VLSI Layout Designs ,pages 101–173. Springer US, Boston, MA, 2001. doi:10.1007/978-1-4613-0255-1_4 .

6 Markus Chimani and Joachim Spoerhase. Network design problems with bounded distances via shallow-light steiner trees. In Ernst W. Mayr and Nicolas Ollinger, editors, 32nd International Symposium on Theoretical Aspects of Computer Science, STACS 2015, March 4-7, 2015, Garching, Germany , volume 30 of LIPIcs , pages 238–248. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2015. doi:10.4230/LIPICS.STACS.2015.238 .

7 Rajesh Chitnis. A tight lower bound for edge-disjoint paths on planar dags. SIAM J. Discret. Math. , 37(2):556–572, 2023. doi:10.1137/21M1395089 .

8 Rajesh Chitnis, Samuel Thomas, and Anthony Wirth. Lower bounds for approximate (& exact) k-disjoint-shortest-paths. In Marcin Bienkowski and Matthias Englert, editors, Approximation and Online Algorithms - 22nd International Workshop, WAOA 2024 , volume 15269 of Lecture Notes in Computer Science , pages 46–60. Springer, 2024. doi:10.1007/978-3-031-81396-2\ _4 .

9 Marek Cygan, Fedor V. Fomin, Lukasz Kowalik, Daniel Lokshtanov, Dániel Marx, Marcin Pilipczuk, Michal Pilipczuk, and Saket Saurabh. Parameterized Algorithms . Springer, 2015. 

doi:10.1007/978-3-319-21275-3 .

10 Marek Cygan, Jesper Nederlof, Marcin Pilipczuk, Michał Pilipczuk, Johan M.M. Van Rooij, and Jakub Onufry Wojtaszczyk. Solving connectivity problems parameterized by treewidth in single exponential time. ACM Transactions on Algorithms (TALG) , 18(2):1–31, 2022. 

doi:10.1145/3506707 .

11 Reinhard Diestel. Graph Theory, 4th Edition , volume 173 of Graduate texts in mathematics .Springer, 2012. 

12 11th DIMACS Implementation Challenge, 2014. Accessed 21 Nov 2023. URL: https:// dimacs11.zib.de/ .

13 Rodney G. Downey and Michael R. Fellows. Parameterized Complexity . Monographs in Computer Science. Springer, 1999. 

14 Stuart E. Dreyfus and Robert A. Wagner. The Steiner problem in graphs. Networks , 1(3):195– 207, 1971. doi:10.1002/net.3230010302 .

15 European Parliament. Report on electricity grids: the backbone of the eu energy system (2024/2226(ini)), 2025. Document A-10-0091/2025, Rapporteur: Anna Stürgkh. URL: https: //www.europarl.europa.eu/doceo/document/A-10-2025-0091_EN.html .

16 Andreas Emil Feldmann and Dániel Marx. The complexity landscape of fixed-parameter directed steiner network problems. ACM Trans. Comput. Theory , 15(3-4):4:1–4:28, 2023. 

doi:10.1145/3580376 .

17 Fedor V Fomin, Petteri Kaski, Daniel Lokshtanov, Fahad Panolan, and Saket Saurabh. Parameterized single-exponential time polynomial space algorithm for Steiner tree. SIAM Journal on Discrete Mathematics , 33(1):327–345, 2019. doi:10.1137/17M1140030 .

18 Eugene C. Freuder. Complexity of k-tree structured constraint satisfaction problems. In 

Proceedings of the Eighth National Conference on Artificial Intelligence - Volume 1 , AAAI’90, page 4–9. AAAI Press, 1990. 

19 Bernhard Fuchs, Walter Kern, D Molle, Stefan Richter, Peter Rossmanith, and Xinhui Wang. Dynamic programming for minimum Steiner trees. Theory of Computing Systems , 41:493–500, 2007. doi:10.1007/S00224-007-1324-4 .

20 M. R. Garey and David S. Johnson. The rectilinear steiner tree problem is NP complete. 

SIAM Journal of Applied Mathematics , 32:826–834, 1977. 

21 Carla Groenland, Jesper Nederlof, and Tomohiro Koana. A polynomial time algorithm for steiner tree when terminals avoid a rooted K4-minor. In Édouard Bonnet and Pawel Rzazewski, editors, 19th International Symposium on Parameterized and Exact Computation, IPEC 2024, T. Hamm, B.M.P. Jansen and F. Motiei 55 

September 4-6, 2024, Royal Holloway, University of London, Egham, United Kingdom , volume 321 of LIPIcs , pages 12:1–12:17. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2024. URL: 

https://doi.org/10.4230/LIPIcs.IPEC.2024.12 , doi:10.4230/LIPICS.IPEC.2024.12 .

22 Mohammad Taghi Hajiaghayi, Guy Kortsarz, and Mohammad R. Salavatipour. Approximating buy-at-bulk and shallow-light k-steiner trees. Algorithmica , 53(1):89–103, 2009. doi:10.1007/ S00453-007-9013-X .

23 John E. Hopcroft and Robert Endre Tarjan. Efficient planarity testing. J. ACM , 21(4):549–568, 1974. doi:10.1145/321850.321852 .

24 Bart M. P. Jansen and Céline M. F. Swennenhuis. Steiner tree parameterized by multiway cut and even less. In Timothy M. Chan, Johannes Fischer, John Iacono, and Grzegorz Herman, editors, 32nd Annual European Symposium on Algorithms, ESA 2024, September 2-4, 2024, Royal Holloway, London, United Kingdom , volume 308 of LIPIcs , pages 76:1–76:16. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2024. doi:10.4230/LIPICS.ESA.2024.76 .

25 H.C Joksch. The shortest route problem with constraints. Journal of Mathematical Analysis and Applications , 14(2):191–197, 1966. URL: https://www.sciencedirect.com/science/ article/pii/0022247X66900205 , doi:10.1016/0022-247X(66)90020-5 .

26 Richard M. Karp. Reducibility among combinatorial problems. In Proceedings of a symposium on the Complexity of Computer Computations , The IBM Research Symposia Series, pages 85–103. Plenum Press, New York, 1972. doi:10.1007/978-1-4684-2001-2_9 .

27 Guy Kortsarz and David Peleg. Approximating shallow-light trees (extended abstract). In Michael E. Saks, editor, Proceedings of the Eighth Annual ACM-SIAM Symposium on Discrete Algorithms, 5-7 January 1997, New Orleans, Louisiana, USA , pages 103–110. ACM/SIAM, 1997. URL: http://dl.acm.org/citation.cfm?id=314161.314191 .

28 Ivana Ljubić. Solving Steiner trees: Recent advances, challenges, and perspectives. Networks ,77(2):177–204, 2021. doi:10.1002/net.22005 .

29 Daniel Lokshtanov and Jesper Nederlof. Saving space by algebraization. In Proc. 42nd STOC ,pages 321–330. ACM, 2010. doi:10.1145/1806689.1806735 .

30 Dániel Marx. On the optimality of planar and geometric approximation schemes. In 48th Annual IEEE Symposium on Foundations of Computer Science (FOCS 2007), October 20-23, 2007, Providence, RI, USA, Proceedings , pages 338–348. IEEE Computer Society, 2007. 

doi:10.1109/FOCS.2007.50 .

31 Dániel Marx. The square root phenomenon in planar graphs. In Michael R. Fellows, Xuehou Tan, and Binhai Zhu, editors, Frontiers in Algorithmics and Algorithmic Aspects in Information and Management, Third Joint International Conference, FAW-AAIM 2013 , Lecture Notes in Computer Science. Springer, 2013. doi:10.1007/978-3-642-38756-2_1 .

32 Dániel Marx, Marcin Pilipczuk, and Michal Pilipczuk. On subexponential parameterized algorithms for steiner tree and directed subset TSP on planar graphs. In Mikkel Thorup, editor, 59th IEEE Annual Symposium on Foundations of Computer Science, FOCS 2018, Paris, France, October 7-9, 2018 , pages 474–484. IEEE Computer Society, 2018. doi:10. 1109/FOCS.2018.00052 .

33 Jesper Nederlof. Fast polynomial-space algorithms using inclusion-exclusion. Algorithmica ,65(4):868–884, 2013. doi:10.1007/S00453-012-9630-X .

34 PACE 2018, 2018. Accessed 21 Nov 2023. URL: https://pacechallenge.org/2018/ .

35 Marcin Pilipczuk, Michal Pilipczuk, Piotr Sankowski, and Erik Jan van Leeuwen. Net-work sparsification for steiner problems on planar and bounded-genus graphs. ACM Trans. Algorithms , 14(4):53:1–53:73, 2018. doi:10.1145/3239560 .

36 Ondrej Suchý. On directed steiner trees with multiple roots. In Pinar Heggernes, editor, 

Graph-Theoretic Concepts in Computer Science - 42nd International Workshop, WG 2016, Istanbul, Turkey, June 22-24, 2016, Revised Selected Papers , volume 9941 of Lecture Notes in Computer Science , pages 257–268, 2016. doi:10.1007/978-3-662-53536-3\_22 .