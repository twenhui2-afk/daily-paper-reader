---
title: "MHOT: Height-Optimized Authenticated Data Structure for Blockchain State Commitment"
source: arxiv
title_zh: 用于Blockchain State Commitment的MHOT：Height-Optimized Authenticated Data Structure
abstract_zh: "State root computation dominates (78%) blockchain block processing time。\n\n核心做法：Prior work mitigates this trade-off 使用向量 commitments，at cost 的 trusted setup or expensive verificati。\n\n主要结果：Unlike MPT's fixed-prefix indexing，which couples span 与 fanout exponentially，\\textsc{Mhot} indexes b。\n\n值得关注的点在于：Our results，somewhat surprisingly，show that height optimality (not new crypto primitives!) is key ab。"
abstract_en: "State root computation dominates (78%) blockchain block processing time. Ethereum's canonical authenticated data structure, i.e., Merkle Patricia Trie (MPT), suffers from severe tree-height growth and is vulnerable to \\textit{Nurgle attacks} (SP'24), where adversaries inflate path depth via hash collisions and degrade system performance at negligible cost. Existing defenses increase node fanout (span) to bound tree height, but higher span inflates proof size exponentially. Prior work mitigates this trade-off using vector commitments, at the cost of trusted setup or expensive verification.   We present \\textsc{Mhot}, a height-optimal authenticated data structure for blockchain state commitment that preserves standard hash-based verification without trusted setup. Unlike MPT's fixed-prefix indexing, which couples span and fanout exponentially, \\textsc{Mhot} indexes by discriminative bits that actually distinguish keys, achieving adaptive span with linear fanout coupling and provably minimal height. To prevent high fanout from inflating proofs, we introduce hierarchical proofs, a two-layer Merkle construction that reduces per-node proof overhead from O(k) to O(log k).   On Ethereum mainnet workloads, \\textsc{Mhot} achieves up to 9X higher write throughput, 4X lower write amplification, and 2X smaller proofs than MPT. Under Nurgle attacks, even when the adversary consumes an entire block's gas budget, \\textsc{Mhot} maintains a 0% attack success rate (v.s., 99.97% for MPT). Our results, somewhat surprisingly, show that height optimality (not new crypto primitives!) is the key abstraction for scalable and attack-resilient blockchain state commitment."
authors: "Sipeng Xie, Qianhong Wu, Minghang Li, Qiyuan Gao, Bo Qin, Qin Wang"
date: 2026-06-10
journal: cs.CR
source_link: "https://arxiv.org/pdf/2606.11736v1"
pdf: "https://arxiv.org/pdf/2606.11736v1"
score: 7.35
tldr: "围绕《用于Blockchain State Commi...》；Prior work mitigates this trade-...；Unlike MPT's fixed-prefix indexing，w..."
tldr_cn: "围绕《用于Blockchain State Commi...》；Prior work mitigates this trade-...；Unlike MPT's fixed-prefix indexing，w..."
tldr_en: "State root computation dominates (78%) blockchain block processing time. Ethereum's canonical authenticated data structure, i.e., Merkle Patricia Trie (MPT), suffers from severe tree-height growth and is vulnerable to \\textit{Nurgle attacks} (SP'24), where adversaries inflate path depth via hash collisions and degrade system performance at negligible cost."
selection_source: fresh_fetch
vocabulary: ["discriminative | 判别式的 | IELTS"]
motivation: "State root computation dominates (78%) blockchain block processing time。"
method: 核心做法：Prior work mitigates this trade-off 使用向量 commitments，at cost 的 trusted setup or expensive verificati。
result: "主要结果：Unlike MPT's fixed-prefix indexing，which couples span 与 fanout exponentially，\\textsc{Mhot} indexes b。"
conclusion: "值得关注的点在于：Our results，somewhat surprisingly，show that height optimality (not new crypto primitives!) is key ab。"
---

## 摘要
State root computation dominates (78%) blockchain block processing time。

核心做法：Prior work mitigates this trade-off 使用向量 commitments，at cost 的 trusted setup or expensive verificati。

主要结果：Unlike MPT's fixed-prefix indexing，which couples span 与 fanout exponentially，\textsc{Mhot} indexes b。

值得关注的点在于：Our results，somewhat surprisingly，show that height optimality (not new crypto primitives!) is key ab。

## Abstract
State root computation dominates (78%) blockchain block processing time. Ethereum's canonical authenticated data structure, i.e., Merkle Patricia Trie (MPT), suffers from severe tree-height growth and is vulnerable to \textit{Nurgle attacks} (SP'24), where adversaries inflate path depth via hash collisions and degrade system performance at negligible cost. Existing defenses increase node fanout (span) to bound tree height, but higher span inflates proof size exponentially. Prior work mitigates this trade-off using vector commitments, at the cost of trusted setup or expensive verification.   We present \textsc{Mhot}, a height-optimal authenticated data structure for blockchain state commitment that preserves standard hash-based verification without trusted setup. Unlike MPT's fixed-prefix indexing, which couples span and fanout exponentially, \textsc{Mhot} indexes by discriminative bits that actually distinguish keys, achieving adaptive span with linear fanout coupling and provably minimal height. To prevent high fanout from inflating proofs, we introduce hierarchical proofs, a two-layer Merkle construction that reduces per-node proof overhead from O(k) to O(log k).   On Ethereum mainnet workloads, \textsc{Mhot} achieves up to 9X higher write throughput, 4X lower write amplification, and 2X smaller proofs than MPT. Under Nurgle attacks, even when the adversary consumes an entire block's gas budget, \textsc{Mhot} maintains a 0% attack success rate (v.s., 99.97% for MPT). Our results, somewhat surprisingly, show that height optimality (not new crypto primitives!) is the key abstraction for scalable and attack-resilient blockchain state commitment.

## 英语词汇
- **discriminative**：判别式的（IELTS）
