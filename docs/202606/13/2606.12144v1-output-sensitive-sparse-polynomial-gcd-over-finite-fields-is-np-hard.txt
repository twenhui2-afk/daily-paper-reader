Title: Output-sensitive Sparse Polynomial GCD over Finite Fields is NP-hard

URL Source: https://arxiv.org/pdf/2606.12144v1

Published Time: Thu, 11 Jun 2026 01:22:33 GMT

Number of Pages: 17

Markdown Content:
Key Laboratory of Mathematics Mechanization MechMath Agent Team 

# Output-sensitive Sparse Polynomial GCD over Finite Fields is NP-hard 

Ruichen Qiu 1,* , Yichuan Cao 1,* , Qiao-Long Huang 2, Ruyong Feng 1, Xiao-Shan Gao 1,+ 

# Abstract 

In this paper, we prove that output-sensitive sparse polynomial GCD computation over finite fields is NP-hard under BPP many-one reduction. More precisely, for two sparse univariate polynomials f , g with finite field coefficients, there exists no randomized algo-rithm to compute gcd ( f , g), which is polynomial-time in the sizes of f , g, gcd ( f , g) under the standard complexity assumption NP ⊈ BPP. This settles the open problem posed as Challenge 5 in [7] in the finite field setting. Furthermore, we show that the Roots of Unity Detection problem over finite fields is NP-hard; that is, determining whether the GCD of a sparse univariate polynomial and xn − 1 has nonzero degree is NP-hard. 

# 1 Introduction 

The greatest common divisor (GCD) for polynomials is one of the core and most extensively studied prob-lems in computer algebra [26]. For two dense degree-D univariate polynomials over a field, the fast Eu-clidean algorithm can be used to compute their GCD using O(D log 2 D log log D) field operations [26, Chapter 11]. In practical computations and in computer algebra systems like Maple and Mathematica, polynomials are typically represented in a sparse form SparsityChallenges,Roche2018, meaning that only the nonzero coef-ficients and their corresponding exponents are stored in lists. Consider a sparse univariate polynomial f

of degree D, containing T nonzero terms, and whose coefficients have a maximum absolute value of H. Its size is tightly bounded by Size ( f ) = O(T(log D + log H)) = O(T log (DH )) .So a sparse polynomial-time algorithm should have bit complexity that is polynomial in T, log D, and log H.There exist no sparse polynomial-time GCD algorithms, because the GCD of two sparse polynomials can have a size that is exponential in the sizes of the input polynomials, as shown below. 

Example 1 ([22]) . Let p, q be distinct primes and f = xpq − 1, g = xp+q − xp − xq + 1 = ( xp − 1)( xq − 1).Then we have the following irreducible decompositions: x p − 1 = ( x − 1)Φp, x q − 1 = ( x − 1)Φq, x pq − 1 =(x − 1)ΦpΦqΦpq , where Φn is the n-th cyclotomic polynomial. Thus 

gcd ( f , g) = ( x − 1)ΦpΦq = ( xp − 1)Φq = xpΦq − Φq

which contains 2q terms, assuming q < p. 

This implies that the strongest guaranty we can provide is output-sensitive complexity; in other words, the algorithm’s running time must explicitly depend on the size of the GCD. There exist many breakthrough algorithms for GCD, both output-sensitive and output-insensitive, includ-ing methods based on sub-resultant computation [4, 5, 10], methods based on interpolations [11, 12, 15, 25, 27], and modular methods based on Hensel lifting [19]. Recent research has continued to advance GCD 

> 1

State Key Laboratory of Mathematical Sciences, Academy of Mathematics and Systems Science, Chinese Academy of Sciences; University of Chinese Academy of Sciences 

> ∗

Equal contribution. 

> 2

Shandong University 

> +

Corresponding author. 

1

> arXiv:2606.12144v1 [cs.SC] 10 Jun 2026

Key Laboratory of Mathematics Mechanization MechMath Agent Team 

computation, focusing on improvements such as the half-GCD algorithm [24]. Additionally, constant-time variants of the GCD algorithm have been developed, which are particularly valuable in cryptographic ap-plications where resistance to timing attacks is essential [1]. In many cases, these algorithms perform quite effectively, as demonstrated by their use in popular software systems such as Maple and Mathematica. However, the complexity of all these methods depends on the degrees of the input polynomials; therefore, they are not polynomial-time algorithms. The following example shows that, even for output-sensitive complexity, the procedure to compute the GCD might be exponential. 

Example 2. Let p > q be two distinct primes, and f = xp − 1, g = xq − 1. Then their GCD can be uniquely written as 

gcd ( f , g) = x − 1 = A f + Bg .

We can show that T B > p − q; that is, even if T gcd ( f ,g) is small, computing gcd ( f , g) might require handling polynomials A and B that are exponential in the sizes of f , g, gcd ( f , g).

Determining whether two sparse univariate polynomials over Q possess a non-trivial GCD is NP-hard [20, Theorem 3.3]; this result has also been generalized to polynomials over finite fields [9, 14]. This brings us directly to the following problem [7, Challenge 5] about the output-sensitive sparse polynomial GCD computation. 

Open Problem 1. Find an algorithm for computing gcd ( f , g) that is polynomial-time in 

∥ f ∥0, ∥g∥0, and ∥ gcd ( f , g)∥0.For finite fields, we make Open Problem 1 more explicit as follows. 

Open Problem 2. For f , g ∈ Fq[x], if there exists an algorithm for computing gcd ( f , g) that is polynomial in T, log D, and log q, where 

D = max (deg ( f ), deg (g)) , T = max (∥ f ∥0, ∥g∥0, ∥ gcd ( f , g)∥0).Here, GCD is assumed to be monic to ensure its uniqueness. 

1.1 Main Results 

In this paper, we settle Open Problem 2 by proving that sparse polynomial GCD computation is NP-hard under bounded-error probabilistic polynomial-time (BPP) reduction. Precisely, we have the following con-ditional obstruction. 

Theorem 3. If there were an algorithm that, for every input pair f , g ∈ Fq[x] satisfying 

deg ( f ), deg (g) ≤ D, | supp ( f )|, | supp (g)|, | supp (gcd ( f , g)) | ≤ T,

computes the sparse GCD in bit complexity that is polynomial in T, log D, and log q, then NP ⊆ BPP . Thus, under the standard complexity assumption NP ⊈ BPP , no such algorithm exists. 

Open Problem 1 remains unsolved in the settings of integers. Even for the finite field setting, we may further ask whether the problem is NP-hard? or PSPACE-hard? Or is the decision of whether a sparse polynomial is the GCD of two sparse polynomials NP-complete? Furthermore, we show that the Roots of Unity Detection problem is NP-hard; that is, given a sparse univariate polynomial f of degree bounded by D, determining whether f vanishes at a root of unity whose order is at most D is NP-hard. Equivalently, deciding whether deg gcd ( f , g) > 0 is NP-hard for g = xn − 1 and a sparse polynomial f .

Theorem 4. If there were an algorithm that, for f ∈ Fq[x] and g = xn − 1 satisfying 

deg f ≤ D n ≤ D | supp ( f )| ≤ T,

decides whether deg gcd ( f , g) > 0 in bit complexity that is polynomial in T, log D, and log q, then NP ⊆ BPP .Thus, under the standard complexity assumption NP ⊈ BPP , no such algorithm exists. 

2Key Laboratory of Mathematics Mechanization MechMath Agent Team 

Theorem 4 strengthens the previously known result that deciding whether two sparse polynomials are co-prime is NP-hard [20, Theorem 3.3]. Note that Theorem 4 is not output-sensitive, so it cannot be deduced from Theorem 3, since gcd (xn − 1, f ) could have exponential size, as shown in Example 1. Theorem 4 states that determining whether there exists a unit root that is a zero of f is NP-hard, whereas [20, Theorem 4.1] shows that deciding whether there exists a unit root that is not a zero of f is NP-hard. Therefore, Theorem 4 cannot be inferred from [20, Theorem 4.1]. The main results presented in this paper are obtained through an interactive collaboration between the authors and an artificial intelligence agent system, MechMath Agent Team (MMAT) [18]. The authors assume full responsibility for the paper’s content. MMAT is a large language model driven agent designed to prove mathematical theorems expressed in both natural language and formal language in Lean. 

1.2 Related Work 

Most of the classic NP-hardness results concerning GCD computation are due to Plaisted [20, 21, 27]. These include the NP-hardness of determining whether the GCD of sparse integer polynomials is nontrivial [20, Theorem 3.3], as well as the NP-completeness of deciding whether xN − 1 fails to divide a given sparse polynomial [20, Theorem 4.1]. Non-trivial GCD is shown to be NP-hard over finite fields [9]. Deciding the square-freeness of a sparse polynomial in Z[x] is NP-hard [16]. The Co-prime test of sparse polynomials over large finite fields is CoNP-hard [14]. For univariate polynomials represented by straight-line programs, an algorithm that runs in time polyno-mial in Size ( f , g) and D is provided in [13]. Filaseta, Granville, and Schinzel give a sparse GCD algorithm that is linear in log D when one of the polynomials does not have cyclotomic-factors [8]. Multivariate polynomial GCD computation is central to algebraic and symbolic computation. The main-stream approach reduces the multivariate case to univariate computations via modular methods or Hensel lifting, followed by interpolation or lifting to recover the true GCD. Representative contributions include Brown’s interpolation-based algorithm for dense polynomials [3], the EZ-GCD algorithm using Hensel lift-ing for sparse polynomials by Moses and Yun [19], Zippel’s probabilistic sparse GCD algorithm based on sparse interpolation and the Schwartz–Zippel lemma [27]. Additionally, Kaltofen’s algorithm for polynomi-als given by straight-line programs [13], extends naturally to the multivariate case. Likewise, the black-box GCD algorithm of Kaltofen and Trager [15] also applies to multivariate polynomials. The parallel sparse GCD algorithm of Hu and Monagan combining Kronecker substitution with Ben-Or–Tiwari sparse interpo-lation [11]. These methods typically run in polynomial time, but often rely on randomization or additional assumptions to achieve deterministic complexity or explicit bit complexity bounds. An algorithm of bit complexity eO(nDT G (Tf + Tg) log 2 q log 2 log 2 1/ δ) is given over Fq in [12]. The rest of the paper is organized as follows. In Section 2, the proof for Theorem 3 is presented. In Section 3, the proof for Theorem 4 is presented. In Section 4, conclusions are presented. 

# 2 Output-sensitive Sparse Polynomial GCD over Finite Field is NP-hard 

In this section, we present the proof for Theorem 3. The proof consists of five steps given in the five subsec-tions, respectively. We outline the five steps of the proof below. The first step in Section 2.1 isolates the SAT instance, reducing it to a bounded-clause formula that is either unsatisfiable or, with inverse-polynomial probability, uniquely satisfiable. The second step in Section 2.2 constructs a suitable prime field and a system of roots of unity whose residue classes encode Boolean assignments. The third step in Section 2.3 builds sparse polynomials whose common roots among these roots of unity are exactly the satisfying assignments of the isolated formula. The fourth step in Section 2.4 compresses the whole list of constraint polynomials into one random sparse polynomial while preserving the relevant common roots with high probability. The final step in Section 2.5 checks the sparse size and degree bounds of the constructed instance and then amplifies the randomized reduction to obtain the claimed BPP consequence. We use the sparse bit model over variable finite fields: a polynomial over Fq is represented by its nonzero coefficient–exponent pairs, the exponents are written in binary, and the field size is part of the input. The construction below uses only prime fields. 

3Key Laboratory of Mathematics Mechanization MechMath Agent Team 

2.1 Boolean Isolation Definition 5 (Boolean variable and literal) . A Boolean variable x takes values in {0, 1 } (false/true). A literal is either a variable x (positive literal) or its negation ¬x (negative literal). 

Definition 6 (Clause and CNF) . A clause is a disjunction (logical OR) of literals. A formula is in conjunctive normal form (CNF) if it is a conjunction (logical AND) of clauses. 

Definition 7 (Satisfiability (SAT)) . Given a CNF formula F on n Boolean variables, the SAT problem asks: does there exist an assignment to the variables such that F evaluates to true? Such an assignment, if it exists, is called a 

satisfying assignment .

The Cook–Levin theorem states that SAT is NP-complete. The result was proved independently by Cook [6] and Levin [17]. 

Theorem 8 (Cook–Levin Theorem) . SAT is NP-complete: every problem in NP can be reduced to SAT in polyno-mial time, and SAT itself belongs to NP. 

Definition 9 (Parsimonious reduction) . A reduction from SAT to a problem P is parsimonious if it preserves the exact number of satisfying assignments — not only whether a satisfying assignment exists, but also the count. 

Definition 10 (Unique-3SAT-style promise problem) . A CNF formula with clause size at most three is called a Unique-3SAT-style instance under the promise that it has either zero or exactly one satisfying assignment. The promise problem asks to distinguish the two cases. 

Formulas with more than one satisfying assignment may arise from failed randomized trials and lie outside the promise. For an exact three-literal display, repeated-literal padding is allowed: 

(λ) becomes (λ ∨ λ ∨ λ), (λ ∨ μ) becomes (λ ∨ μ ∨ μ),which preserves the number of satisfying assignments. We use the notion of randomized polynomial-time reduction as defined by Valiant and Vazirani in their seminal paper [23]. This notion allows a one-sided error: no-instances are always mapped to no-instances, while yes-instances are mapped to yes-instances with at least inverse-polynomial probability. 

Definition 11 (Randomized polynomial-time reduction [23]) . A problem A is randomized polynomial-time reducible to a problem B if there exists a randomized Turing machine T running in polynomial time and a polynomial P such that: 1. (No-instance correctness): If x /∈ A, then for all coPin flips, T (x) /∈ B. 2. (Yes-instance correctness): If x ∈ A, then P[T(x) ∈ B] ≥ 1/ P(|x|).Here |x| denotes the bit-length of the encoding of the input instance x, i.e., the input size. 

Based on Theorem 1.1 of Valiant and Vazirani [23] and their notion of randomized polynomial-time reduc-tion (see Definition 11), we obtain the following concrete isolation result, which serves as the core lemma in our reduction. 

Lemma 12 (Valiant–Vazirani isolation theorem [23]) . For a CNF formula F on n ≥ 1 Boolean variables, the randomized Valiant–Vazirani procedure adds random homogeneous linear equations over F2 (equivalently, affine constraints after translating the selected cell), encoded by polynomial-size CNF gadgets. The satisfying assignments of the output are exactly those of F that lie in the selected affine subspace. Thus an unsatisfiable input remains unsat-isfiable for every random choice. If F is satisfiable, the output has exactly one satisfying assignment with probability at least 1/ P(|F|), where P is a polynomial (depending only on the reduction) and |F| denotes the encoding length of F. 

Lemma 13 (Randomized reduction to Unique-3SAT-style) . There exists a randomized polynomial-time reduction from SAT to the Unique-3SAT-style promise family with the following properties: 

• Unsatisfiable inputs always map to unsatisfiable formulas. 

• Satisfiable inputs map, with probability at least 1/ P(n) (inverse polynomial in the input length), to formulas having exactly one satisfying assignment. 

4Key Laboratory of Mathematics Mechanization MechMath Agent Team 

Proof. First convert the input formula to a parsimonious CNF form. For each gate, introduce an auxiliary variable and add clauses that force it to equal the gate value: 

y ↔ (a ∨ b) : (¬y ∨ a ∨ b), (y ∨ ¬ a), (y ∨ ¬ b);

y ↔ (a ∧ b) : (¬y ∨ a), (¬y ∨ b), (y ∨ ¬ a ∨ ¬ b);

y ↔ ¬ a : (¬y ∨ ¬ a), (y ∨ a).For any assignment to original variables, the auxiliary variables are uniquely forced, and the output gate is set to true. Hence the number of satisfying assignments is preserved. The zero-variable case is handled directly by outputting a fixed unsatisfiable or uniquely satisfiable formula. Now apply the Valiant–Vazirani isolation theorem. The randomized procedure adds random affine parity constraints over F2, encoded as a polynomial-size CNF, so that the satisfying assignments of the output are exactly those of the input lying in the chosen affine subspace. Therefore: • An unsatisfiable input remains unsatisfiable for every random choice. • If the input is satisfiable, the output has exactly one satisfying assignment with probability at least 1/ P(|F|).

Example. Let F(x1, x2) = ( x1 ∨ x2), with satisfying assignments (1, 0 ), (0, 1 ), (1, 1 ). Choose the parity con-straint x1 = 0 over F2 (i.e., (x1, x2) · (1, 0 ) = 0), encoded by the unit clause (¬x1). The isolated formula is 

G(x1, x2) = ( x1 ∨ x2) ∧ (¬x1),which retains only (0, 1 ). Thus G has exactly one satisfying assignment. Under the exact-three-literal con-vention this becomes 

(x1 ∨ x2 ∨ x2) ∧ (¬x1 ∨ ¬ x1 ∨ ¬ x1).Finally, convert the output to 3CNF without changing the solution count. For each clause ℓ1 ∨ · · · ∨ ℓm with 

m ≥ 4, introduce fresh variables p2, . . . , pm−1. Add clauses: 

(¬p2 ∨ ℓ1 ∨ ℓ2), (p2 ∨ ¬ ℓ1), (p2 ∨ ¬ ℓ2)

to force p2 = ℓ1 ∨ ℓ2. For 3 ≤ j ≤ m − 1, add clauses forcing pj = pj−1 ∨ ℓj. Finally add (pm−1 ∨ ℓm). The auxiliary variables are uniquely determined when the original clause is true, and no extension exists when it is false. Clauses of length one or two are left as is, or padded to length three if required. Using disjoint auxiliary variables for different clauses yields a bijection between satisfying assignments before and after conversion. Composing the Valiant–Vazirani isolation with this parsimonious 3CNF conversion gives the required ran-domized reduction. 

2.2 Prime Field and Roots of Unity 

The following lemma provides the prime numbers and modulus needed for our construction. It is derived from the BCR/AIRR prime-construction procedure, which runs in randomized polynomial time and pro-duces distinct primes ℓ1, . . . , ℓN together with a prime p. These parameters will be used in the subsequent reduction. 

Lemma 14 (BCR/AIRR prime-construction procedure [2]) . For any integer N ≥ 4, applying the Las Vegas randomized construction from Theorem 1.11 of Bi–Cheng–Rojas to the dummy N-variable 3CNF formula BN =

> N^
> j=1

(uj ∨ uj ∨ uj)

yields, in randomized polynomial time in N, distinct primes ℓ1, . . . , ℓN , an integer c ≥ 11 , and a prime p = 1 + cM , M =

> N

# ∏

> j=1

ℓj,

with log p = poly (N).

5Key Laboratory of Mathematics Mechanization MechMath Agent Team 

Let Φ be a Unique-3SAT instance produced by the preceding reduction, with variables y1, . . . , yn, and let 

ε > 0 be any inverse-polynomial error target. |Φ| denotes the bit-length of the encoding of the formula Φ,i.e., the input size. 

Lemma 15 (Prime field and roots of unity construction) . Let ε = 1/ poly (|Φ|) be an inverse-polynomial error target. In randomized polynomial time (in |Φ|), one can construct: 

• a prime field Fp;

• distinct primes ℓ1, . . . , ℓn;

• their product D = ∏ni=1 ℓi;

• a primitive D-th root of unity ω ∈ Fp;such that D | (p − 1), log p = poly (|Φ|), Dp < ε.

Moreover, the constants z i = ωD/ℓi , each of order ℓi, can be computed in randomized polynomial bit complexity. Proof. Choose a number r of dummy primes so that 2 r > 1/ ε, increasing r if necessary to make N =

n + r ≥ 4. Since ε is inverse-polynomial, N is polynomial in the Boolean input size. Apply the BCR/AIRR prime-construction procedure just stated to the dummy N-variable 3CNF formula BN . It gives, in Las Vegas randomized polynomial time, distinct primes ℓ1, . . . , ℓN , an integer c ≥ 11, and a prime 

p = 1 + cM , M =

> N

# ∏

> j=1

ℓj,with log p = poly (N).Only the first n primes are assigned to Boolean variables. Put 

D =

> n

# ∏

> i=1

ℓi, Pdum =

> N

# ∏

> j=n+1

ℓj.Then M = DP dum , so D | p − 1. Also 

Dp = D

1 + cDP dum 

< 1

cP dum 

≤ 1

Pdum 

< ε,because every dummy prime is at least 2 and Pdum ≥ 2r. The dummy primes do not create Boolean coordinates later: all later encoding polynomials use only the product D and the exponents D/ℓi for 1 ≤

i ≤ n.It remains to construct ω. If D = 1, take ω = 1. Otherwise sample a ∈ F∗ 

> p

uniformly and set 

ω = a(p−1)/D.Since F∗ 

> p

is cyclic of order p − 1, this samples uniformly from the subgroup of D-th roots. Using the known squarefree factorization D = ∏i ℓi, test whether 

ωD/ℓi̸ = 1 for every i.The test is equivalent to ω having exact order D. A uniform element of the subgroup has exact order D

with probability 

φ(D)

D =

> n

# ∏

> i=1



1 − 1

ℓi



.Since the ℓi are distinct primes, after ordering them increasingly ℓi ≥ i + 1, and the elementary estimate log (1 − x) ≥ − 2x for 0 ≤ x ≤ 1/2 gives 

φ(D)

D ≥ (n + 1)−2.

6Key Laboratory of Mathematics Mechanization MechMath Agent Team 

Thus polynomially many trials find a primitive D-th root with high probability, or Las Vegas expected polynomial time if we repeat until success. All exponentiations use exponents of bit length at most log p,so their bit cost is polynomial in n and log p. Finally, compute 

zi = ωD/ℓi .Because D is squarefree and ω has order D, each zi has order ℓi.

2.3 Sparse Polynomial Encoding of Unique-3SAT 

Before stating the lemma, we briefly describe the reduction from Unique-3SAT-style to a common-root problem for sparse polynomials. Using the prime field Fp, the primes ℓi, the product D, and the root of unity 

ω obtained from Lemma 15, we construct polynomials R, Bi, and PC such that the satisfying assignments of the Unique-3SAT instance Φ correspond bijectively to the common roots of these polynomials in Fp. The polynomials are sparse: R has at most 2 terms, each Bi has at most 3 terms, and each clause polynomial PC

has at most 8 terms. All degrees are bounded by 2 D. This yields a randomized polynomial-time reduction from Unique-3SAT to the problem of deciding whether a given set of sparse polynomials shares a common root (or nontrivial common factor) over Fp.

Lemma 16 (Sparse polynomial encoding of Unique-3SAT) . Let Fp, ℓ1, . . . , ℓn, D = ∏i ℓi, and ω be as con-structed in Lemma 15. For the Unique-3SAT instance Φ on variables y 1, . . . , yn, one can construct in randomized polynomial time: R = xD − 1, Bi = ( xD/ℓi − 1)( xD/ℓi − zi), zi = ωD/ℓi ,

and clause polynomials P C such that the common roots of R, all B i, and all P C in Fp are in bijection with the satisfying assignments of Φ. Moreover, 

| supp (R)| ≤ 2, | supp (Bi)| ≤ 3, | supp (PC )| ≤ 8, 

and all degrees are at most 2D. Proof. For a positive literal yi, put 

Li,1 (x) = xD/ℓi − zi,and for a negative literal ¬yi, put 

Li,0 (x) = xD/ℓi − 1. For a clause C = λ1 ∨ λ2 ∨ λ3, define 

PC (x) = Lλ1 (x)Lλ2 (x)Lλ3 (x),where Lλ is the corresponding literal factor. The roots of R in Fp are exactly the elements ωm, indexed by m ∈ Z/DZ. Indeed, ω has order D, so these 

D elements are distinct roots of xD − 1, and a nonzero polynomial of degree D over a field has at most D

roots. For x = ωm, we have 

xD/ℓi = ( ωD/ℓi )m = zmi .Since zi has order ℓi,

Bi(ωm) = ( zmi − 1)( zmi − zi) = 0if and only if m ≡ 0 or 1 (mod ℓi). Therefore the common roots of R, B1, . . . , Bn are exactly the roots ωm

whose residues (m mod ℓi)i lie in {0, 1 }n. By the Chinese remainder theorem, these roots are in bijection with Boolean assignments, using residue 1 for yi = true and residue 0 for yi = false. On such a Boolean-domain root, Li,1 vanishes exactly at residue 1, hence exactly when the positive literal 

yi is true. Similarly, Li,0 vanishes exactly at residue 0, hence exactly when the negative literal ¬yi is true. Since Fp has no zero divisors, PC vanishes exactly when at least one of its three literal factors vanishes, i.e. exactly when the clause C is satisfied. Adding all equations PC = 0 therefore cuts out exactly the satisfying assignments. The support and degree estimates are immediate from the displayed forms. The polynomial R has at most two terms. Expanding 

Bi = x2D/ℓi − (1 + zi)xD/ℓi + zi

7Key Laboratory of Mathematics Mechanization MechMath Agent Team 

shows at most three terms, with degree at most 2 D/ℓi ≤ D. Each clause polynomial is a product of three binomials, so before collection it has at most 2 3 = 8 monomial products; collection and cancellation cannot increase support. Its degree is at most 

D

ℓi1

+ D

ℓi2

+ D

ℓi3

≤ 3D

2 ≤ 2D,because all ℓi ≥ 2. 

2.4 Random Pair Compression and Sparse GCD 

We now show that the common-root problem for the collection of sparse polynomials R, F1, . . . , Fm (where 

Fj ranges over all Bi and PC from Lemma 16) can be reduced to the problem of finding the common root of just two polynomials: R and a random linear combination H = ∑mj=1 uj Fj. By choosing the coefficients 

uj ∈ Fp independently and uniformly at random, we obtain with high probability that the zero set of H

together with R coincides with the common zero set of all polynomials. Moreover, the resulting polynomial 

H remains sparse with support size polynomial in |Φ|, and the construction runs in randomized polynomial time. 

Lemma 17 (Random linear combination preserves common roots) . Let F 1, . . . , Fm be the list of all polynomials Bi and P C from Lemma 16. Choose independent uniform coefficients u j ∈ Fp and define H =

> m

# ∑

> j=1

uj Fj.

Then, with probability at least 1 − D/p, Z(R, H) = Z(R, F1, . . . , Fm)

as subsets of Fp, where Z (·) denotes the set of common roots in Fp. On this event: 

• If the Unique-3SAT instance Φ has no satisfying assignment, then gcd (R, H) = 1.

• If Φ has a unique satisfying assignment corresponding to α ∈ Fp, then gcd (R, H) = x − α.In particular, the GCD has at most two terms (i.e., is a constant or a linear binomial). Moreover, H is a sparse polynomial with | supp (H)| ≤ ∑mj=1 | supp (Fj)| = poly (|Φ|), and the construction runs in randomized polynomial time. Proof. Fix a root α of R which is not a common root of all constraints Fj. Then the value vector 

(F1(α), . . . , Fm(α)) 

is nonzero. Choose s with Fs(α)̸ = 0. After all coefficients uj with j̸ = s are fixed, the equation H(α) = 0 is a single linear equation 

us Fs(α) + c = 0in the uniform variable us, and it has exactly one solution in Fp. Hence 

P[H(α) = 0] = 1/ p.Let S = Z(R, F1, . . . , Fm) and TR = Z(R). Always S ⊆ Z(R, H). The reverse inclusion fails only if some 

α ∈ TR \ S also satisfies H(α) = 0. Since R has degree D, |TR| ≤ D, and the union bound gives 

P[Z(R, H)̸ = S] ≤ D/p.Next R = xD − 1 is squarefree over Fp. Since D | p − 1, the characteristic p does not divide D. Therefore 

R′(x) = Dx D−1

is nonzero at every root of R, because every root of xD − 1 is nonzero. The primitive D-th root ω shows that 

R splits into the distinct linear factors x − β for β ∈ TR. Consequently gcd (R, H) = ∏ 

> β∈TR,H(β)= 0

(x − β)

8Key Laboratory of Mathematics Mechanization MechMath Agent Team 

as a monic polynomial. On the successful compression event this product is indexed by β ∈ Z(R, F1, . . . , Fm). By Lemma 16, that zero set is in bijection with satisfying assignments. Hence in the zero-satisfying-assignment case the GCD is the empty product 1. In the unique-satisfying-assignment case the zero set is {α}, so the GCD is x − α.Since αD = 1, α̸ = 0, and x − α has exactly the two exponents 1 and 0. Finally, | supp (R)| ≤ 2. The sum defining H has at most 3 n + 8k monomial contributions before collection, because each Bi has at most three terms and each PC has at most eight. Collection and cancellation cannot create new exponents, so 

| supp (H)| ≤ 3n + 8k.Every Fj has degree at most 2 D, so deg H ≤ 2D. Thus the final pair (R, H) has degree bound ∆ = 2D, input term bound max (2, 3 n + 8k), and GCD support at most 2 in the promised zero-or-one cases. 

2.5 A Promised Sparse-GCD Solver Implies NP ⊆ BPP 

Theorem 18. Assume there is a total algorithm A gcd which runs in bit time polynomial in T, log ∆, and log q, and which is correct on every promised input pair over Fq satisfying 

deg f , deg g ≤ ∆, | supp ( f )|, | supp (g)|, | supp (gcd ( f , g)) | ≤ T.

Then SAT is in BPP, hence NP ⊆ BPP .Proof. If Agcd is randomized, amplify it on promised calls so that the binary test “does the returned sparse GCD have positive degree?” has error at most γ, using O(log (1/ γ)) independent runs and majority vote. For deterministic Agcd , take γ = 0. The algorithm is total, so even off-promise calls halt in the same polynomial time, though their outputs are not trusted. Let Φ be a SAT instance of length s. By Lemma 13, for some polynomial A(s), one Valiant–Vazirani trial maps a satisfiable Φ to a Unique-3SAT-style formula with exactly one satisfying assignment with probabil-ity at least 1/ A(s), while an unsatisfiable Φ always maps to an unsatisfiable formula. Set 

r = ⌈8A(s)⌉, ε = γ = 1100 r .Run r independent trials. In one trial, apply Lemma 13 to get a formula Ψ. Apply Lemma 15 to Ψ with error target ε, giving Fp, D, ω with D/p ≤ ε and log p polynomial in s. This field-construction call is exactly the BCR/AIRR prime-construction procedure stated in Lemma 14, with dummy primes used only to force 

D/p < ε. Construct R and the constraints as in Lemma 16, form the random compressed polynomial H as in Lemma 17, and call Agcd on (R, H) with 

∆ = 2D, T = max (2, 3 n + 8k),where n, k are the numbers of variables and clauses of Ψ. Accept the trial exactly when the amplified output-degree test is positive. Accept Φ if any trial accepts. This is polynomial time. The Valiant–Vazirani output has polynomial size, so n and k are polynomial in s.The BCR/AIRR procedure used in Lemma 14 gives log p = poly (N), and Lemma 14 gives log D polynomial in s; hence log ∆ is polynomial. Lemma 16 gives 

| supp (R)| ≤ 2, | supp (H)| ≤ 3n + 8k, deg R, deg H ≤ 2D,so constructing the sparse pair and reading the solver’s sparse output both take polynomial bit time. The assumed solver runs in polynomial time in these parameters. If Φ is unsatisfiable, every Ψ is unsatisfiable. On a trial where the compression event of Lemma 17 succeeds, gcd (R, H) = 1, and the call is a promised call with negative degree answer. The degree test errs with probability at most γ. If compression fails, the solver call may be off promise and we charge the trial as a possible false positive. Since P[compression failure ] ≤ D/p ≤ ε, one trial accepts with probability at most 

ε + γ. A union bound over r trials gives false acceptance probability at most 

r(ε + γ) = 1/50 < 1/3. 

9Key Laboratory of Mathematics Mechanization MechMath Agent Team 

If Φ is satisfiable, one trial produces a uniquely satisfiable Ψ with probability at least 1/ A(s). Conditional on that event, Lemma 17 succeeds with probability at least 1 − ε, and then gcd (R, H) = x − α for the corresponding root α. The promised degree test is positive, except with probability at most γ. Allowing also the bounded probability that a randomized field-construction subroutine has to skip a trial, the conditional acceptance probability is at least 1 − 2ε − γ ≥ 97/100 ≥ 1/2. Thus one trial accepts with probability at least 1/ (2A(s)) . Independence across trials gives rejection prob-ability at most 

1 − 12A(s)

r

≤ exp (−r/(2A(s))) ≤ e−4 < 1/3. Therefore SAT has a BPP algorithm. Since every language in NP reduces deterministically in polynomial time to SAT by the Cook–Levin theorem (Theorem 8) [6, 17], NP ⊆ BPP. 

# 3 Roots of Unity Detection Is NP-hard over Finite Field 

By Roots of Unity Detection , we refer to the following problem: given a sparse polynomial f ∈ Fq[x] of degree D, determine whether f (x) has, as a root, a root of unity whose order is at most D. In this section, we present the proof for Theorem 4, that is, Roots of Unity Detection over finite field is NP-hard. We first present the main result and then the lemmas. 

Theorem 19. The following decision problem is hard in the randomized-reduction/BPP-consequence sense: given a sparse polynomial f ∈ Fq[x], an integer n, the polynomial g = xn − 1, and a degree bound D with 

deg f ≤ D, n ≤ D,

decide whether f and g are coprime. Equivalently, decide whether deg gcd ( f , g) > 0. More precisely, a polynomial-time algorithm for this problem would imply SAT ∈ BPP , and therefore NP ⊆ BPP .Proof. The reduction is given by Lemmas 21–26. For each randomized trial, Lemma 21 produces a bounded-clause formula whose unsatisfiability is zero-preserved and whose satisfiable inputs are isolated with inverse-polynomial probability. Lemmas 22 and 23 construct, over a prime field, a polynomial system whose common roots over the root set of xE − 1 are precisely the satisfying assignments of the isolated for-mula. Lemma 24 replaces that system by one sparse polynomial H with inverse-polynomially small failure probability and identifies positive GCD degree with the presence of a satisfying assignment on the suc-cessful compression event. Lemma 25 checks that the constructed instance has the required restricted form 

g = xn − 1, with n = E and D = 2E, and is represented in polynomial sparse bit size. Finally, Lemma 26 repeats the trial polynomially many times and shows that a polynomial-time solver for the restricted prob-lem gives a BPP algorithm for SAT. Since every language in NP has a polynomial-time many-one reduction to SAT, this implies NP ⊆ BPP. 

Remark 20 (Scope) . The theorem is stated in the exact scope used by the reduction: variable prime fields, randomized reductions with a BPP consequence, target polynomial g = xn − 1, and target degree bound D = 2n. It does not assert deterministic Karp hardness, fixed-field hardness, or the stronger side condition deg f ≤ n. 

We now present the necessary lemmas along with their proofs. Observe that the proof may be arranged in the following sequence:    

> Lemma 21 Lemma 22 Lemma 23
> Lemma 24
> Lemma 25
> Lemma 26 Theorem 19

Lemma 21 (Isolation and parsimonious bounded-clause source) . There is a randomized polynomial-time pro-cedure which, given a SAT instance Φ of bit length s, outputs a CNF formula 

Ψ(y1, . . . , ym) = C1 ∧ · · · ∧ Ck

with clause size at most three, where m , k = poly (s), such that: 1. if Φ is unsatisfiable, then every possible output Ψ is unsatisfiable; 

10 Key Laboratory of Mathematics Mechanization MechMath Agent Team 

2. if Φ is satisfiable, then with probability at least 1/ A(s), for a polynomial A, the output Ψ has exactly one satisfying assignment; 3. the bounded-clause conversion is parsimonious, so it preserves the exact number of satisfying assignments. The isolation input is the zero-preserving Valiant–Vazirani isolation theorem together with polynomial-size CNF encodings of affine parity constraints over F2, followed by the standard parsimonious conversion to clauses of size at most three. Proof. Apply the zero-preserving Valiant–Vazirani isolation procedure to the SAT instance. Its zero-preserving property gives the first item: if the original formula has no satisfying assignment, then no output produced by the procedure has a satisfying assignment. If the original formula is satisfiable, the isolation guarantee gives, with probability at least 1/ A(s) for a polynomial A, a formula with exactly one satisfying assignment, after the affine parity constraints have been encoded by polynomial-size CNF formulas over Boolean variables. It remains to keep the bounded-clause conversion parsimonious. Consider a long clause 

λ1 ∨ · · · ∨ λt.Introduce auxiliary variables p2, . . . , pt−1, impose 

p2 = λ1 ∨ λ2, pj = pj−1 ∨ λj (3 ≤ j ≤ t − 1),encode each displayed OR equivalence by its three-clause CNF encoding, and add the final clause pt−1 ∨

λt. For fixed values of the old variables, the auxiliary variables are uniquely forced by the recurrence above. The final clause is satisfied exactly when the original long clause is satisfied. Thus every satisfying assignment of the old formula extends to exactly one satisfying assignment of the converted formula, and no non-satisfying assignment extends to a satisfying one. Clauses of size one or two may be retained, or padded by repeated literals, without changing the truth condition. Hence the conversion preserves the exact number of satisfying assignments and yields clauses of size at most three. 

Lemma 22 (Prime-field package and Boolean root domain) . Let Ψ(y1, . . . , ym) be as in Lemma 21, and let ε > 0

be inverse-polynomially small. Choose r dum with 

2rdum > 1/ ε, N = max (m + rdum , 4 ).

There is a Las Vegas randomized polynomial-time construction which outputs distinct primes 

ℓ1, . . . , ℓN ,

an integer c ≥ 11 , and a prime p = 1 + cM , M =

> N

# ∏

> j=1

ℓj,

with log p = poly (N). Define E =

> m

# ∏

> i=1

ℓi, Pdum =

> N

# ∏

> j=m+1

ℓj.

Then E | p − 1, Ep < ε, log E ≤ log p = poly (s).

Moreover one can construct, in Las Vegas expected polynomial time, an element ω ∈ F∗ 

> p

of exact order E. For zi = ωE/ℓi (1 ≤ i ≤ m),

the element z i has exact order ℓi.Proof. Apply the prime-field construction to the dummy N-variable formula 

> N^
> j=1

(uj ∨ uj ∨ uj).

11 Key Laboratory of Mathematics Mechanization MechMath Agent Team 

It returns distinct primes ℓ1, . . . , ℓN , an integer c ≥ 11, and a prime p = 1 + cM , where M = ∏Nj=1 ℓj, with log p = poly (N). Since 

p − 1 = cM = cEP dum ,we have E | p − 1. Also 

Ep < Ep − 1 = 1

cP dum 

≤ 1

Pdum 

.The dummy primes are distinct and at least 2, so 

Pdum =

> N

# ∏

> j=m+1

ℓj ≥ 2N−m ≥ 2rdum > 1/ ε.Therefore E/p < ε. The inequality log E ≤ log p follows from E < p, and log p = poly (N) = poly (s)

because N = m + rdum up to the harmless lower bound 4, with m = poly (s) and rdum chosen inverse-polynomially in ε.Since p is prime, F∗ 

> p

is cyclic. Sample a ∈ F∗

> p

, put 

ω = a(p−1)/E,and test 

ωE/ℓi̸ = 1 (1 ≤ i ≤ m).Because E = ∏mi=1 ℓi is squarefree, these tests hold exactly when ω has order E. The accepted prime-field package gives inverse-polynomial success probability for this sampling step, so repetition gives a Las Vegas expected polynomial-time construction. Finally, if ω has order E, then 

zi = ωE/ℓi

has order ℓi, since raising an element of order E to the E/ℓi-th power gives order E/ gcd (E, E/ℓi) = ℓi.

Lemma 23 (Polynomial encoding of assignments and clauses) . Assume the data of Lemma 22. Put R(x) = xE − 1. 

For 1 ≤ i ≤ m, define Bi(x) = ( xE/ℓi − 1)( xE/ℓi − zi),

and define literal polynomials Li,1 (x) = xE/ℓi − zi, Li,0 (x) = xE/ℓi − 1, 

where L i,1 corresponds to y i and L i,0 corresponds to ¬yi. If C = λ1 ∨ λ2 ∨ λ3

is a clause, with repeated literals allowed, define PC (x) = Lλ1 (x)Lλ2 (x)Lλ3 (x).

Let F 1, . . . , Ft be the list consisting of all B i and all clause polynomials P C for clauses of Ψ. Then Z(R, F1, . . . , Ft) ⊆ Fp

is in bijection with the satisfying assignments of Ψ.More explicitly, the roots of R are ωa, a ∈ Z/EZ, and the common roots of R , B1, . . . , Bm correspond exactly to residues satisfying a mod ℓi ∈ { 0, 1 } (1 ≤ i ≤ m).

Under the convention yi = true ⇐⇒ a ≡ 1 (mod ℓi), yi = false ⇐⇒ a ≡ 0 (mod ℓi),

the polynomial P C vanishes on such a root if and only if the corresponding assignment satisfies the clause C. 

12 Key Laboratory of Mathematics Mechanization MechMath Agent Team 

Proof. Since ω has exact order E, the roots of R = xE − 1 in Fp are precisely 

ωa, a ∈ Z/EZ.For x = ωa, one has 

xE/ℓi = ( ωE/ℓi )a = zai .The element zi has order ℓi, so 

Bi(ωa) = 0 ⇐⇒ (zai − 1)( zai − zi) = 0 ⇐⇒ a mod ℓi ∈ { 0, 1 }.The primes ℓi are distinct, and therefore the Chinese remainder isomorphism 

Z/EZ ≃

> m

# ∏

> i=1

Z/ℓiZ

identifies the common roots of R, B1, . . . , Bm with choices of residues a mod ℓi ∈ { 0, 1 }. Under the stated convention, these choices are exactly Boolean assignments to y1, . . . , ym.On a Boolean-domain root, Li,1 vanishes exactly when a ≡ 1 (mod ℓi), namely when yi is true, while Li,0 

vanishes exactly when a ≡ 0 (mod ℓi), namely when ¬yi is true. For a clause C = λ1 ∨ λ2 ∨ λ3, the polynomial 

PC = Lλ1 Lλ2 Lλ3

vanishes over the field Fp exactly when at least one factor vanishes, because Fp is an integral domain. This is exactly the condition that at least one literal of C is true. Thus imposing all the PC’s cuts out precisely the satisfying assignments of Ψ inside the Boolean root domain, proving the asserted bijection. 

Lemma 24 (Random compression and GCD correctness) . Choose independent uniform coefficients u1, . . . , ut ∈ Fp

and set H(x) = 

> t

# ∑

> j=1

uj Fj(x).

Let S = Z(R, F1, . . . , Ft), TR = Z(R).

Then S ⊆ Z(R, H) always, and 

P[Z(R, H)̸ = S] ≤ Ep < ε.

On the complementary event, H has exactly the same roots on Z (R) as the full constraint list F 1, . . . , Ft.The polynomial R = xE − 1 is squarefree over Fp. Hence, for every H ∈ Fp[x],

gcd (R, H) = ∏ 

> β∈Z(R),H(β)= 0

(x − β),

where the GCD is monic and the empty product is 1. Consequently, on the successful compression event: 1. if Ψ is unsatisfiable, then gcd (R, H) = 1;2. if Ψ has exactly one satisfying assignment, then gcd (R, H) = x − α for the corresponding Boolean-domain root α, and in particular deg gcd (R, H) = 1 > 0.Proof. If β ∈ S, then every Fj(β) is zero, and hence 

H(β) = 

> t

# ∑

> j=1

uj Fj(β) = 0. Thus S ⊆ Z(R, H) for every choice of the coefficients. 

13 Key Laboratory of Mathematics Mechanization MechMath Agent Team 

Now fix α ∈ TR \ S. By definition of S, at least one Fj(α) is nonzero. Condition on all coefficients except such a coefficient uj. The equation H(α) = 0 is then a nontrivial affine linear equation in the uniform variable 

uj ∈ Fp, so it holds with probability 1/ p. Taking the union bound over the E points of TR gives 

P[Z(R, H)̸ = S] ≤ | TR|/p = E/p < ε.On the complementary event, no point of TR \ S is added as a root of H, while all points of S remain roots, so H and the full constraint list have the same roots on Z(R).It remains to relate these roots to the GCD. Since E | p − 1, the characteristic p does not divide E. Therefore 

R′(x) = Ex E−1

does not vanish at a root of R = xE − 1, and R is squarefree. All roots of R in Fp are the elements of TR, so the monic GCD of R and H is exactly the product of the linear factors corresponding to the common roots: gcd (R, H) = ∏ 

> β∈Z(R),H(β)= 0

(x − β).If Ψ is unsatisfiable, Lemma 23 gives S = ∅; on the successful compression event the product is empty and the GCD is 1. If Ψ has exactly one satisfying assignment, Lemma 23 gives S = {α}; on the same event the product is x − α, whose degree is 1. 

Lemma 25 (Constructed instance satisfies the sparse target bounds) . For one trial of the reduction, set f = H, g = R = xE − 1, n = E, D = 2E.

Then g = xn − 1, n = E ≤ D = 2E.

The polynomial R has two terms. Each B i has at most three terms after collecting like terms, and each clause polyno-mial P C has at most eight monomial contributions before collection. Moreover 

deg Bi ≤ E, deg PC ≤ 2E.

Therefore, after listing terms, collecting like exponents, and deleting zero coefficients, 

| supp (H)| ≤ 3m + 8k, deg H ≤ 2Ewhen H ̸ = 0, while the zero polynomial has empty sparse support and vacuous degree bound. All exponents and coefficients in the instance have polynomial bit length in s, and the sparse representation of H is computable in polynomial bit time. Proof. The identities g = xn − 1 and n ≤ D follow immediately from the definitions g = R = xE − 1, n = E,and D = 2E. The polynomial R has the two displayed monomials xE and −1. For each variable polynomial, 

Bi(x) = x2E/ℓi − (1 + zi)xE/ℓi + zi,so Bi has at most three terms after collecting like terms. Since ℓi ≥ 2, deg Bi = 2E/ℓi ≤ E.Each clause polynomial PC is a product of three binomials, hence has at most 2 3 = 8 monomial contributions before collection. If the three literals involve indices a, b, c, with repetitions allowed, then deg PC ≤ E/ℓa + E/ℓb + E/ℓc ≤ 3E/2 ≤ 2E,because every ℓi ≥ 2. Repeated literals do not increase this bound. The polynomial H = ∑j uj Fj is a linear combination of the m polynomials Bi and the k clause polynomials. Listing all their monomial contributions gives at most 3 m + 8k terms before collecting. Collecting equal exponents and deleting zero coefficients can only decrease the support size, so 

| supp (H)| ≤ 3m + 8k.The same degree estimates give deg H ≤ 2E whenever H̸ = 0; if H = 0, the sparse support is empty and the degree condition is vacuous. The exponents are bounded by 2 E, so their binary lengths are O(log E). Lemma 22 gives log E ≤ log p =

poly (s), and all coefficients lie in Fp. Since m, k = poly (s), the list of monomial contributions has polyno-mial size, and the arithmetic for collecting equal exponents and removing zero coefficients takes polynomial bit time. 

14 Key Laboratory of Mathematics Mechanization MechMath Agent Team 

Lemma 26 (Amplification gives a BPP SAT algorithm) . Assume that there is a polynomial-time solver for the restricted sparse coprimality problem of Theorem 19, or equivalently for deciding whether deg gcd ( f , xn − 1) > 0,on the constructed instances. If the solver is randomized with bounded error, repeat it independently so that each query has error at most γ.Let 1/ A(s) be the isolation success lower bound from Lemma 21. Choose r = ⌈8A(s)⌉, ε = γ = 1100 r .

Run r independent trials. In each trial, perform the isolation step, construct the prime-field data and the sparse instance 

( f , g, n, D) = ( H, xE − 1, E, 2 E),

query the amplified restricted solver, and accept the SAT input Φ if some trial reports “not coprime”, equivalently 

deg gcd ( f , g) > 0.If Φ is unsatisfiable, then the false acceptance probability is at most r(ε + γ) = 1/50 < 1/3. 

If Φ is satisfiable, then a trial accepts with probability at least 

12A(s) ,

and hence the probability that all r trials reject is at most 



1 − 12A(s)

r

≤ exp (−r/(2A(s))) ≤ e−4 < 1/3. 

For strict worst-case BPP time, the Las Vegas field-construction subroutine may be given a polynomial cutoff, with cutoff failure counted as an additional per-trial bad event and the constants above decreased accordingly. Therefore a polynomial-time solver for the restricted sparse coprimality problem would imply SAT ∈ BPP , and hence NP ⊆ BPP .Proof. Run the stated r independent trials. In an unsatisfiable input, the zero-preserving property from Lemma 21 makes every isolated formula Ψ unsatisfiable. By Lemma 24, after a successful compression event the constructed pair is coprime. Thus a false accepting trial can occur only if compression fails or the amplified solver answers incorrectly. The per-trial probability is at most ε + γ, and the union bound over all r trials gives 

r(ε + γ) = r

 1100 r + 1100 r



= 150 < 13 .Now suppose that Φ is satisfiable. In one trial, Lemma 21 produces a uniquely satisfiable Ψ with probability at least 1/ A(s). Conditional on this event, the compression is successful and the amplified solver is correct with probability at least 1 − ε − γ > 1/2. On that joint event, Lemma 24 gives deg gcd (H, xE − 1) = 1 > 0, and the solver reports “not coprime.” Therefore a single trial accepts with probability at least 1/ (2A(s)) . The probability that all r = ⌈8A(s)⌉

trials reject is at most 

1 − 12A(s)

r

≤ exp (−r/(2A(s))) ≤ e−4 < 1/3. The construction in each trial has polynomial sparse bit size by Lemma 25, and the number of trials is polynomial in the input length. If the field-construction subroutine is used in Las Vegas expected time, this provides the expected-time accounting described above. For a strict worst-case BPP algorithm, impose a polynomial cutoff on that Las Vegas subroutine and count a cutoff as another bad per-trial event; reducing the constants in the choices of ε and γ keeps the total error below 1/3. Hence, the assumed solver yields a BPP algorithm for SAT, and consequently NP ⊆ BPP. 

15 Key Laboratory of Mathematics Mechanization MechMath Agent Team 

# 4 Conclusion 

In this paper, we show that sparse polynomial GCD computation over a finite field is NP-hard under BPP reduction. Taking the contrapositive, for sparse polynomials f , g ∈ Fq[x], under the standard complexity assumption NP ⊈ BPP, there is no algorithm which computes gcd ( f , g) for every finite-field input pair satisfying deg f , deg g ≤ D, | supp ( f )|, | supp (g)|, | supp (gcd ( f , g)) | ≤ T

with bit complexity polynomial in T, log D, and log q. Equivalently, a positive answer to the question in this bit-complexity model would imply NP ⊆ BPP. We also show that the Roots of Unity Detection over finite fields is NP-hard under BPP reduction. Equiva-lently, determining whether a sparse polynomial f (x) is co-prime to xn − 1 is NP-hard under BPP reduction. 

# Acknowledgment 

This paper is supported by the Strategic Priority Research Program of CAS Grants XDA0480502 and XDA0480503, NSFC Grants 12288201 and 92270001. 

# References 

[1] Daniel J Bernstein and Bo-Yin Yang. Fast constant-time gcd computation and modular inversion. IACR transactions on cryptographic hardware and embedded systems , pp. 340–398, 2019. [2] Jingguo Bi, Qi Cheng, and J. Maurice Rojas. Sub-linear root detection, and new hardness results, for sparse polynomials over finite fields. In Proceedings of the 38th International Symposium on Symbolic and Algebraic Computation , ISSAC ’13, pp. 61–68, New York, NY, USA, 2013. [3] W. S. Brown. On euclid’s algorithm and the computation of polynomial greatest common divisors. 

Journal of the ACM , 18(4):478–504, 1971. [4] W. S. Brown and J. F. Traub. On euclid’s algorithm and the theory of subresuhants. Journal of the ACM ,18(4):505–514, 1971. [5] George E. Collins. Subresultants and reduced polynomial remainder sequences. J. ACM , 14(1):128–142, January 1967. ISSN 0004-5411. doi: 10.1145/321371.321381. URL https://doi.org/10.1145/321371. 321381 .[6] Stephen A. Cook. The complexity of theorem-proving procedures. In Logic, Automata, and Computa-tional Complexity: The Works of Stephen A. Cook , pp. 143–152, New York, NY, USA, 2023. Association for Computing Machinery. [7] James Harold Davenport and Jacques Carette. The sparsity challenges. In 2009 11th International Symposium on Symbolic and Numeric Algorithms for Scientific Computing , pp. 3–7, 2009. doi: 10.1109/ SYNASC.2009.62. [8] Michael Filaseta, Andrew Granville, and Andrzej Schinzel. Irreducibility and greatest common divisor algorithms for sparse polynomials. In James McKee and ChrisEditors Smyth (eds.), Number Theory and Polynomials , London Mathematical Society Lecture Note Series, pp. 155–176. Cambridge University Press, 2008. [9] Joachim von zur Gathen, Marek Karpinski, and Igor Shparlinski. Counting curves and their projec-tions. In Proceedings of the twenty-fifth annual ACM symposium on Theory of Computing , STOC’93, pp. 805–812. ACM, 1993. [10] Hoon Hong and Jing Yang. Computing greatest common divisor of several parametric univariate poly-nomials via generalized subresultant polynomials, 2024. URL https://arxiv.org/abs/2401.00408 .[11] Jiaxiong Hu and Michael Monagan. A fast parallel sparse polynomial gcd algorithm. In Proceedings of the 2016 ACM International Symposium on Symbolic and Algebraic Computation , ISSAC ’16, pp. 271–278, New York, NY, USA, 2016. Association for Computing Machinery. 

16 Key Laboratory of Mathematics Mechanization MechMath Agent Team 

[12] Qiao-Long Huang and Xiao-Shan Gao. Bit complexity of polynomial gcd on sparse representation. 

Mathematics of Computation , 95(357):389–413, 2025. [13] Erich Kaltofen. Greatest common divisors of polynomials given by straight-line programs. J. ACM , 35 (1):231–264, 1988. [14] Erich Kaltofen and Pascal Koiran. On the complexity of factoring bivariate supersparse (lacunary) polynomials. In Proceedings of the 2005 International Symposium on Symbolic and Algebraic Computation (ISSAC ’05) , pp. 208–215. ACM, 2005. [15] Erich Kaltofen and Barry M. Trager. Computing with polynomials given by black boxes for their evaluations: Greatest common divisors, factorization, separation of numerators and denominators. 

Journal of Symbolic Computation , 9(3):301–320, 1990. [16] Marek Karpinski and Igor Shparlinski. On the computational hardness of testing square-freeness of sparse polynomials. In Proc. AAECC-13 (Heidelberg, Germany, 1999) , Vol. 1719 of LNCS, pp. 492–497. Springer Verlag, 1999. [17] L. A. Levin. Universal sequential search problems. Probl. Peredachi Inf. , 9(3):115–116, 1973. Translation in Problems Inform. Transmission 9 (3), 265–266 (1973). [18] MechMath Team. Mechmath agent team. Academy of Mathematics and Systems Science, Chinese Academy of Sciences, 2026. URL https://eonmath.github.io/mechmath .[19] Joel Moses and David Y. Y. Yun. The ez gcd algorithm. In Proceedings of the ACM Annual Conference ,ACM ’73, pp. 159–166, New York, NY, USA, 1973. Association for Computing Machinery. [20] David A. Plaisted. New np-hard and np-complete polynomial and integer divisibility problems. The-oretical Computer Science , 31(1):125–138, 1984. [21] David Alan Plaisted. Sparse complex polynomials and polynomial reducibility. Journal of Computer and System Sciences , 14(2):210–221, 1977. [22] A. Schinzel. On the greatest common divisor of two univariate polynomials, i. In GisbertEditor Wüstholz (ed.), A Panorama of Number Theory or The View from Baker’s Garden , pp. 337–352. Cambridge University Press, 2002. [23] L.G. Valiant and V.V. Vazirani. Np is as easy as detecting unique solutions. In Proceedings of the Seven-teenth Annual ACM Symposium on Theory of Computing , STOC ’85, pp. 458–463, 1985. [24] Joris van der Hoeven. Optimizing the half-gcd algorithm. Applicable Algebra in Engineering, Communi-cation and Computing , May 2025. URL https://doi.org/10.1007/s00200-025-00690-w .[25] Joris van der Hoeven and Grégoire Lecerf. On sparse interpolation of rational functions and gcds. 

ACM Commun. Comput. Algebra , 55(1):1–12, 2021. [26] Joachim von zur Gathen and Jürgen Gerhard. Modern Computer Algebra . Cambridge University Press, 2013. [27] Richard Zippel. Probabilistic algorithms for sparse polynomials. In Edward W. Ng (ed.), Symbolic and Algebraic Computation, EUROSAM ’79, An International Symposium on Symbolic and Algebraic Computa-tion , volume 72 of Lecture Notes in Computer Science , pp. 216–226. Springer, 1979. 

17