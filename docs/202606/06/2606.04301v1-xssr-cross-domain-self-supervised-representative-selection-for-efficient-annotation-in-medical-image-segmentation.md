---
title: "XSSR: Cross-Domain Self-Supervised Representative Selection for Efficient Annotation in Medical Image Segmentation"
source: arxiv
title_zh: 用于Efficient Annotation in Medical Image Segmentation的XSSR：Cross-Domain Self-Supervised Representative Selection
abstract_zh: "Acquiring labeled medical image data is resource-intensive 与 challenge further exacerbated in cross-。\n\n核心做法：This study introduces XSSR (Cross-Domain Self-Supervised Representative Selection)，框架 designed to mi。\n\n主要结果：XSSR 达到 99.3% 的 full-data 性能 on Chest X-ray 使用 only 22 labeled samples，surpasses random selection by。\n\n值得关注的点在于：Ablation studies indicate that diversity is most influential scoring component，与 per-site analysis s。"
abstract_en: "Acquiring labeled medical image data is resource-intensive and a challenge further exacerbated in cross-domain scenarios where source and target datasets differ in imaging equipment, population, or clinical site. This study introduces XSSR (Cross-Domain Self-Supervised Representative Selection), a framework designed to minimize annotation effort in the target domain while maintaining robust segmentation performance. XSSR comprises three stages: first, a Masked Autoencoder (MAE) is trained on unlabeled source data to establish a shared embedding space without requiring target labels; second, a greedy selection algorithm scores unlabeled target samples based on a composite density, novelty, and diversity criterion; and third, a U-Net segmentation model is trained exclusively on the selected subset. The novelty-diversity trade-off parameter, alpha, is automatically calibrated by minimizing embedding-space coverage, eliminating manual tuning. We evaluate XSSR on three public benchmarks: Chest X-ray, RIGA+ retinal fundus imaging, and multi-site Prostate MRI, each under a fixed 5% annotation budget. XSSR achieves 99.3% of full-data performance on Chest X-ray using only 22 labeled samples, surpasses random selection by up to 2.5 Dice points on Prostate MRI, and consistently outperforms the CoreSet baseline by 0.4 to 1.2 Dice points across all datasets. Ablation studies indicate that diversity is the most influential scoring component, and per-site analysis shows that performance correlates with scanner similarity to the source domain."
authors: "Byunghyun Ko, Aleksei Anisimov, Kobe Ke, Suhas Bharthepude, Jeongkyu Lee"
date: 2026-06-03
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.04301v1"
pdf: "https://arxiv.org/pdf/2606.04301v1"
score: 7.0
tldr: "围绕《用于Efficient Annotation i...》；This study introduces XSSR (Cros...；XSSR 达到 99.3% 的 full-data 性能 on Ches..."
tldr_cn: "围绕《用于Efficient Annotation i...》；This study introduces XSSR (Cros...；XSSR 达到 99.3% 的 full-data 性能 on Ches..."
tldr_en: "Acquiring labeled medical image data is resource-intensive and a challenge further exacerbated in cross-domain scenarios where source and target datasets differ in imaging equipment, population, or clinical site. This study introduces XSSR (Cross-Domain Self-Supervised Representative Selection), a framework designed to minimize annotation effort in the target domain while maintaining robust segmentation performance."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "annotation | 标注 | CET6", "embedding | 嵌入表示 | 学术高频", "clinical | 临床的 | CET6"]
motivation: Acquiring labeled medical image data is resource-intensive 与 challenge further exacerbated in cross-。
method: 核心做法：This study introduces XSSR (Cross-Domain Self-Supervised Representative Selection)，框架 designed to mi。
result: "主要结果：XSSR 达到 99.3% 的 full-data 性能 on Chest X-ray 使用 only 22 labeled samples，surpasses random selection by。"
conclusion: 值得关注的点在于：Ablation studies indicate that diversity is most influential scoring component，与 per-site analysis s。
---

## 摘要
Acquiring labeled medical image data is resource-intensive 与 challenge further exacerbated in cross-。

核心做法：This study introduces XSSR (Cross-Domain Self-Supervised Representative Selection)，框架 designed to mi。

主要结果：XSSR 达到 99.3% 的 full-data 性能 on Chest X-ray 使用 only 22 labeled samples，surpasses random selection by。

值得关注的点在于：Ablation studies indicate that diversity is most influential scoring component，与 per-site analysis s。

## Abstract
Acquiring labeled medical image data is resource-intensive and a challenge further exacerbated in cross-domain scenarios where source and target datasets differ in imaging equipment, population, or clinical site. This study introduces XSSR (Cross-Domain Self-Supervised Representative Selection), a framework designed to minimize annotation effort in the target domain while maintaining robust segmentation performance. XSSR comprises three stages: first, a Masked Autoencoder (MAE) is trained on unlabeled source data to establish a shared embedding space without requiring target labels; second, a greedy selection algorithm scores unlabeled target samples based on a composite density, novelty, and diversity criterion; and third, a U-Net segmentation model is trained exclusively on the selected subset. The novelty-diversity trade-off parameter, alpha, is automatically calibrated by minimizing embedding-space coverage, eliminating manual tuning. We evaluate XSSR on three public benchmarks: Chest X-ray, RIGA+ retinal fundus imaging, and multi-site Prostate MRI, each under a fixed 5% annotation budget. XSSR achieves 99.3% of full-data performance on Chest X-ray using only 22 labeled samples, surpasses random selection by up to 2.5 Dice points on Prostate MRI, and consistently outperforms the CoreSet baseline by 0.4 to 1.2 Dice points across all datasets. Ablation studies indicate that diversity is the most influential scoring component, and per-site analysis shows that performance correlates with scanner similarity to the source domain.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **annotation**：标注（CET6）
- **embedding**：嵌入表示（学术高频）
- **clinical**：临床的（CET6）
