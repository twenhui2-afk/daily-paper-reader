---
title: Process-Verified Reinforcement Learning for Theorem Proving via Lean
source: arxiv
title_zh: 用于Theorem Proving 通过 Lean的Process-Verified Reinforcement Learning
abstract_zh: "We incorporate these structured rewards 为 GRPO-style reinforcement learning objective 结合 first-error。\n\n核心做法：This gap between structured processes 与 unstructured rewards highlights importance 的 feedback that i。\n\n主要结果：In this work，we 表明 that Lean proof assistant itself can serve as symbolic process oracle，supplying b。\n\n值得关注的点在于：This opens path toward reinforcement learning 框架s that combine scalability 的 language 模型s 结合 reliabi。"
abstract_en: "While reinforcement learning from verifiable rewards (RLVR) typically has relied on a single binary verification signal, symbolic proof assistants in formal reasoning offer rich, fine-grained structured feedback. This gap between structured processes and unstructured rewards highlights the importance of feedback that is both dense and sound. In this work, we demonstrate that the Lean proof assistant itself can serve as a symbolic process oracle, supplying both outcome-level and fine-grained tactic-level verified feedback during training. Proof attempts are parsed into tactic sequences, and Lean's elaboration marks both locally sound steps and the earliest failing step, yielding dense, verifier-grounded credit signals rooted in type theory. We incorporate these structured rewards into a GRPO-style reinforcement learning objective with first-error propagation and first-token credit methods that balances outcome- and process-level advantages. Experiments with STP-Lean and DeepSeek-Prover-V1.5 show that tactic-level supervision outperforms outcome-only baselines in most settings, delivering improvements on benchmarks such as MiniF2F and ProofNet. Beyond empirical gains, our study highlights a broader perspective: symbolic proof assistants are not only verifiers at evaluation time, but can also act as process-level reward oracles during training. This opens a path toward reinforcement learning frameworks that combine the scalability of language models with the reliability of symbolic verification for formal reasoning."
authors: "Minsu Kim, Se-Young Yun"
date: 2026-06-18
journal: cs.AI
source_link: "https://arxiv.org/pdf/2606.20068v1"
pdf: "https://arxiv.org/pdf/2606.20068v1"
score: 7.3
tldr: 围绕《用于Theorem Proving 通过 Lea...》；This gap between structured proc...；In this work，we 表明 that Lean proof a...
tldr_cn: 围绕《用于Theorem Proving 通过 Lea...》；This gap between structured proc...；In this work，we 表明 that Lean proof a...
tldr_en: "While reinforcement learning from verifiable rewards (RLVR) typically has relied on a single binary verification signal, symbolic proof assistants in formal reasoning offer rich, fine-grained structured feedback. This gap between structured processes and unstructured rewards highlights the importance of feedback that is both dense and sound."
selection_source: fresh_fetch
motivation: We incorporate these structured rewards 为 GRPO-style reinforcement learning objective 结合 first-error。
method: 核心做法：This gap between structured processes 与 unstructured rewards highlights importance 的 feedback that i。
result: 主要结果：In this work，we 表明 that Lean proof assistant itself can serve as symbolic process oracle，supplying b。
conclusion: 值得关注的点在于：This opens path toward reinforcement learning 框架s that combine scalability 的 language 模型s 结合 reliabi。
---

## 摘要
We incorporate these structured rewards 为 GRPO-style reinforcement learning objective 结合 first-error。

核心做法：This gap between structured processes 与 unstructured rewards highlights importance 的 feedback that i。

主要结果：In this work，we 表明 that Lean proof assistant itself can serve as symbolic process oracle，supplying b。

值得关注的点在于：This opens path toward reinforcement learning 框架s that combine scalability 的 language 模型s 结合 reliabi。

## Abstract
While reinforcement learning from verifiable rewards (RLVR) typically has relied on a single binary verification signal, symbolic proof assistants in formal reasoning offer rich, fine-grained structured feedback. This gap between structured processes and unstructured rewards highlights the importance of feedback that is both dense and sound. In this work, we demonstrate that the Lean proof assistant itself can serve as a symbolic process oracle, supplying both outcome-level and fine-grained tactic-level verified feedback during training. Proof attempts are parsed into tactic sequences, and Lean's elaboration marks both locally sound steps and the earliest failing step, yielding dense, verifier-grounded credit signals rooted in type theory. We incorporate these structured rewards into a GRPO-style reinforcement learning objective with first-error propagation and first-token credit methods that balances outcome- and process-level advantages. Experiments with STP-Lean and DeepSeek-Prover-V1.5 show that tactic-level supervision outperforms outcome-only baselines in most settings, delivering improvements on benchmarks such as MiniF2F and ProofNet. Beyond empirical gains, our study highlights a broader perspective: symbolic proof assistants are not only verifiers at evaluation time, but can also act as process-level reward oracles during training. This opens a path toward reinforcement learning frameworks that combine the scalability of language models with the reliability of symbolic verification for formal reasoning.
