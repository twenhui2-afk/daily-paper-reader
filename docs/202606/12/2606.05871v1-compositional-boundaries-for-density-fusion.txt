Title: Compositional Boundaries for Density Fusion

URL Source: https://arxiv.org/pdf/2606.05871v1

Published Time: Fri, 05 Jun 2026 00:52:16 GMT

Number of Pages: 22

Markdown Content:
COMPOSITIONAL BOUNDARIES FOR DENSITY FUSION 

RATAN BAHADUR THAPA, ALI DARIJANI, JÜRGEN BEYERER, AND STEFFEN STAAB 

Abstract. Distributed uncertainty-management systems often combine local probabilistic models along aggregation trees chosen by communication, privacy, or scheduling constraints. The final density should depend on the weighted sources, not on the particular order in which intermediate nodes combine them. We study this requirement as an algebraic compositionality problem for binary fusion of weighted probability densities. The central question is when a local fusion rule can be executed hierarchically while remaining order-invariant. We establish a compositional boundary for local segment-valued fusion rules. Within the class of continuous binary rules with additive output weights and weight-only coefficients, order-invariant hierarchical execution characterizes normalized weighted linear pooling; norm-induced segment balancing realizes the corresponding coefficient. Smooth endpoint-to-candidate f -divergence balancing has a different local geometry: its quadratic expansion induces square-root effective weights, showing why pairwise solvability alone is insufficient for schedule-independent fusion. We show that this obstruction is local to endpoint-to-candidate binary balancing, whereas global divergence barycenters retain additive-weight local limits. Finally, Gaussian mixtures show how the same issue appears in finite model classes: exact fusion is compositional, whereas stepwise compression is compositional only under a congruence condition on unnormalized component measures. These results distinguish exact schedule-independent fusion from global aggregation objectives and local approximation heuristics. 

1. Introduction 

Scalable uncertainty management often requires probabilistic predictions to be combined across distributed sources. Edge devices estimate local densities, hospitals train models under privacy constraints, and federated systems exchange summaries rather than raw data. This makes aggregation more than probability averaging: it requires fusion of probabilistic evidence with source-dependent weights. We study weighted probability densities (w, p ),where p is a density and w > 0 represents sample size, reliability, confidence, or priority. The central semantic requirement is that the final density depend only on the weighted sources, not on the aggregation tree. If the same weighted sources are combined along a left-deep tree, a balanced tree, or a dynamically scheduled aggregation tree, the final density should not change merely because the communication pattern changed. For states x1, x 2, x 3,the computations (x1 ⊕ x2) ⊕ x3 and x1 ⊕ (x2 ⊕ x3) should have the same meaning; with permuted arrivals, x1 ⊕ x2 and x2 ⊕ x1 should agree as well. Thus, the binary fusion operation should form a commutative monoid on weighted density states. This requirement is stronger 

Date : June 5, 2026. 2020 Mathematics Subject Classification. 68T37, 94A15, 62B11. 

Key words and phrases. Uncertainty fusion, Opinion pooling, f -divergences, Gaussian mixtures, Distributed aggregation. Funded by the Deutsche Forschungsgemeinschaft (DFG) - Project number 471687386. 

> 1
> arXiv:2606.05871v1 [cs.IT] 4 Jun 2026

than pairwise solvability and stronger than closure of a model class under convex mixtures [9]. A natural local protocol chooses a dissimilarity, solves a balanced binary fusion problem at each internal node, and repeats this step through the tree. Pairwise solvability is not enough: Kullback–Leibler (KL) segment balancing [12] already gives different Bernoulli densities for the two parenthesizations of three equal-weight sources. The classical schedule-independent candidate is weighted linear pooling [19, 8, 10], (1.1) ppool =

Pmi=1 wipi

Pmi=1 wi

,

which is standard in opinion aggregation and expert probability assessment. We do not study weighted linear pooling as a one-shot aggregate. Instead, we ask when a binary fusion rule can be executed at arbitrary internal nodes of an aggregation tree without making the final result schedule-dependent. Our results therefore concern algebraic consistency of distributed execution, not statistical optimality for a particular data-generating model. Our main contribution is a compositional boundary result for local segment-based fusion protocols. Norm-induced segment balancing realizes weighted linear pooling. Conversely, any continuous segment-valued binary rule that propagates additive weights, chooses its mixing coefficient only from the two input weights, and is commutative and associative over all convex density classes with three affinely independent densities must use the normalized weight coefficient. Therefore, within this operational class, order-invariant hierarchical execution forces weighted linear pooling. Associative alternatives exist only after changing the additive quantity carried by the state, for example by propagating transformed weights. The main boundary concerns divergences. Under the local bounded-ratio assumptions used in Section 4, endpoint-to-candidate balancing for every twice continuously differentiable 

f -divergence with positive curvature at equality is locally quadratic along mixture segments, so its coefficient converges to √wb/(√wa + √wb), not to wb/(wa + wb). We identify a first-order square-root local repair and contrast it with global f -barycenter objectives, whose local quadratic limiting target is additive-weighted. Thus, the obstruction does not rule out global divergence barycenters, logarithmic pooling, Wasserstein barycenters, or endpoint-dependent optimization rules [12, 3, 5]. Gaussian mixtures give a finite model-class instance of the same issue. Exact weighted fusion stays inside the class of all finite Gaussian mixtures by concatenating components, while fixed-budget subclasses are generally not closed. Stepwise compression is order-invariant exactly when the idempotent compression map is compatible with unnormalized mixture addition, equivalently when it induces a monoid congruence. In Sections 2–5, we develop these claims from local balance to algebraic order-invariance, divergence barriers, and compressed Gaussian-mixture fusion. In Section 6, we compare the results with opinion pooling, evidence fusion [ 17 ], barycenters [ 2], and mixture reduction [ 7]. 2. Segment Balance 

Let (Ω , F, μ ) be a measure space. A probability density function, or PDF, is a measurable function p : Ω → R≥0 with R 

> Ω

p dμ = 1 , and PDFs are identified up to almost-everywhere equality. A PDF class P is a nonempty convex set of PDFs in an ambient real vector space of measurable functions modulo almost-everywhere equality. For p, q ∈ P and t ∈ [0 , 1] , we 

> 2

write (2.1) [p, q ]t = (1 − t)p + tq. 

A weighted PDF state is a pair (w, p ) with w ∈ R>0 and p ∈ P . We also use a formal identity element e and set bP = ( R>0 × P ) ∪ { e}; the element e is not a zero-weight density. When a norm-induced distance is used, we assume that all differences p − q, for p, q ∈ P ,belong to a normed ambient vector space and have finite norm. A norm-induced distance on 

P is a dissimilarity ∆( p, q ) = ∥p − q∥. A divergence on P is a map D : P × P → R≥0 ∪ {∞} 

such that (2.2) D(p, q ) = 0 if and only if p = q almost everywhere .

We do not require divergences to be symmetric or to satisfy the triangle inequality. 

Definition 2.1. Let P be a PDF class, let ∆ : P ×P → R≥0 ∪{∞} , and let (wa, p a), (wb, p b) ∈

R>0 × P . A parameter t∗ ∈ [0 , 1] with finite endpoint dissimilarities is a segment-balancing point for these two weighted states with respect to ∆ if 

(2.3) wa∆( pa, [pa, p b]t∗ ) = wb∆( pb, [pa, p b]t∗ ).

The associated fused PDF is [pa, p b]t∗ .

The definition restricts the binary output to the mixture segment and balances the weighted dissimilarities to the two endpoints. For norm-induced distances, this local condition already determines the mixing coefficient. 

Theorem 2.2. Let ∆( p, q ) = ∥p − q∥ be norm-induced. If pa̸ = pb, then the unique segment-balancing point is 

(2.4) t∗ = wb

wa + wb

, [pa, p b]t∗ = wa

wa + wb

pa + wb

wa + wb

pb.

If pa = pb, the balancing parameter need not be unique, but the fused density is uniquely pa.Proof. Let pt = [ pa, p b]t. Then, pt − pa = t(pb − pa) and pt − pb = (1 − t)( pa − pb), so absolute homogeneity gives ∥pt − pa∥ = t∥pb − pa∥ and ∥pt − pb∥ = (1 − t)∥pb − pa∥. If pa̸ = pb,cancellation of the positive common factor in the balance equation yields wat = wb(1 − t),thus (2.4) ; uniqueness follows because the equation is linear in t. If pa = pb, all segment points are the same density. □

Hence all ambiguity in the degenerate case is parametrization-only, not density-level ambiguity. The result depends on using a norm rather than its square. If ∆( p, q ) = ∥p − q∥2,the same calculation gives wat2 = wb(1 − t)2 and hence t∗ = √wb/(√wa + √wb). The same square-root coefficient reappears for smooth divergences in Section 4. The norm-induced rule has the required compositional structure. On bP, let ⊕ be the operation with e ⊕ x = x ⊕ e = x and, for positive states, (2.5) (wa, p a) ⊕ (wb, p b) = 



wa + wb, wapa + wbpb

wa + wb



.

Proposition 2.3. Let η be the map given by 

η(e) = (0 , 0) , η(w, p ) = ( w, wp )

> 3

for positive states (w, p ). Then, η represents ⊕ by addition: 

(2.6) η(( wa, p a) ⊕ (wb, p b)) = η(wa, p a) + η(wb, p b).

Consequently, ( bP, ⊕, e) is a commutative monoid, and every nonempty finite family (wi, p i)mi=1 

of positive weighted states evaluates, independently of order and parenthesization, to 

(2.7) 

> m

X

> i=1

wi,

Pmi=1 wipi

Pmi=1 wi

!

.

Proof. For positive states, applying η to (2.5) gives (wa + wb, w apa + wbpb) = η(wa, p a) + 

η(wb, p b). The identity laws hold by definition, closure follows from convexity of P, and commutativity and associativity follow from addition in the represented coordinates. Iteration gives (2.7). □

Proposition 2.3 is the formal reason why exact weighted fusion can be implemented locally. Each intermediate state carries only an accumulated weight and an accumulated weighted density; it does not need to record the aggregation tree that produced it. 3. Algebraic Characterization 

We prove a converse for the operational class introduced above. For binary segment-valued rules with additive output weights and weight-only coefficients, commutative and associative execution determines the mixing coefficient uniquely. Consider a binary operation ⊙ on weighted PDF states of the form (3.1) (wa, p a) ⊙ (wb, p b) = ( wa + wb, (1 − α(wa, w b)) pa + α(wa, w b)pb) ,

where α : R2 

> >0

→ (0 , 1) is continuous and depends only on the two weights. The assumption that coefficients depend only on weights excludes endpoint-adaptive barycenters and other global optimization rules; those belong to a different design space. Affine independence means that no one of the three densities lies in the affine span of the other two; it is used below only to identify barycentric coefficients. We use the following standard continuous cocycle representation for the additive semi-group [1]. 

Lemma 3.1. Let m : R2 

> >0

→ (0 , 1) be continuous and satisfy 

m(a, b )m(a + b, c ) = m(a, b + c)

for all a, b, c > 0. Then, there is a positive continuous function F : R>0 → R>0, unique up to multiplication by a positive constant, such that 

m(a, b ) = F (a)

F (a + b).

Proof. Let h(a, b ) = − log m(a, b ). Then h is continuous and satisfies h(a, b ) + h(a + b, c ) = 

h(a, b + c). For fixed a, c > 0, this identity implies h(a, b ) = h(a, b + c) − h(a + b, c ), and hence h(a, b ) → 0 as b ↓ 0. Choose r > 0. Set H(r) = 0 , set H(x) = h(r, x − r) for 

x > r , and set H(x) = −h(x, r − x) for 0 < x < r . The preceding boundary limit makes H

continuous at r. The cocycle identity then gives, in the three cases a > r , a + b < r , and 

a < r < a + b, the representation h(a, b ) = H(a + b) − H(a); the boundary cases follow by continuity. With F = exp H we obtain m(a, b ) = F (a)/F (a + b). If eF is another positive 

> 4

continuous representative, then F (a)/ eF (a) = F (a + b)/ eF (a + b) for all a, b > 0, so this ratio is constant on R>0 and F is unique up to a positive factor. □

Theorem 3.2. If ⊙ is commutative and associative for all choices of PDFs in every convex PDF class containing at least three affinely independent densities, then 

(3.2) α(wa, w b) = wb

wa + wb

for all wa, w b > 0.Proof. Let m(a, b ) = 1 − α(a, b ). Expanding (( a, p a) ⊙ (b, p b)) ⊙ (c, p c) gives the coefficient of 

pa as m(a, b )m(a + b, c ), whereas expanding (a, p a) ⊙ (( b, p b) ⊙ (c, p c)) gives the coefficient of pa as m(a, b + c). Since the three densities are affinely independent, equality of the two parenthesizations implies m(a, b )m(a + b, c ) = m(a, b + c). Lemma 3.1 yields m(a, b ) = 

F (a)/F (a + b) for a positive continuous F . Commutativity gives m(a, b ) + m(b, a ) = 1 , hence 

F (a + b) = F (a) + F (b); continuity gives F (w) = cw on R>0. Therefore m(a, b ) = a/ (a + b)

and α(a, b ) = b/ (a + b). □

The quantification over convex PDF classes with three affinely independent densities is used only to identify barycentric coefficients in the associativity argument. Theorem 3.2 is not a uniqueness theorem for all probabilistic aggregation rules. It applies only to continu-ous segment-valued binary rules with additive output weights and weight-only coefficients. Non-segment-valued rules, logarithmic pooling, endpoint-dependent barycenters, and global optimization principles fall outside these assumptions. The theorem identifies the point at which hierarchical execution itself forces the normalized linear-pooling coefficient. The additive-weight assumption is sharp: supplied weights may first be transformed into effective weights, which are then propagated additively. 

Proposition 3.3. Let g : R>0 → R>0 be a continuous, strictly increasing bijection. On bP,let ⊕g be the operation with e ⊕g x = x ⊕g e = x for all x ∈ bP and, for positive states, 

(3.3) (a, p ) ⊕g (b, q ) = 



g−1(g(a) + g(b)) , g(a)p + g(b)qg(a) + g(b)



.

Then, ( bP, ⊕g, e) is a commutative monoid, and every finite family (wi, p i)mi=1 of positive weighted states evaluates to 

(3.4) g−1

> m

X

> i=1

g(wi)

!

,

Pmi=1 g(wi)pi

Pmi=1 g(wi)

!

.

Proof. The map ηg(w, p ) = ( g(w), g (w)p), with ηg(e) = (0 , 0) , represents ⊕g by addition. Since 

g is a bijection, a represented pair (s, r ) with s > 0 recovers the state as (g−1(s), r/s ). Thus, commutativity, associativity, identity, and the finite formula (3.4) follow from addition. □

With g(w) = w, Proposition 3.3 recovers ordinary weighted linear pooling. With g(w) = √w, the effective contribution of a source is proportional to the square root of its supplied weight, and the accumulated output weight is (P

> i

√wi)2. In Proposition 3.3, the coefficient is weight-only, but it is normalized in the transformed weights g(a), g (b), not in the additively propagated supplied masses a, b . Thus, different associative segment rules are possible only after changing which numerical quantity the distributed computation treats as additive. 

> 5

When weights represent sample masses, weighted linear pooling has the usual source-mixture interpretation: if Pr (S = i) = wi/ P 

> j

wj and X | S = i has density pi, then the marginal density of X is P 

> i

wipi/ P 

> i

wi. Reliability or priority weights require domain-specific calibration, but the algebraic condition for order-invariant execution is unchanged. 4. Divergence Barriers 

We next show why smooth information divergences change the compositional behavior. Endpoint-to-candidate segment balancing compares D(pa∥pt) with D(pb∥pt) for a segment point pt = [ pa, p b]t. Such equations can be solvable pairwise, but they do not generally provide an associative binary operation under additive source weights. Let D be a divergence satisfying (2.2). For two weighted states (wa, p a) and (wb, p b), let (4.1) Φ( t) = waD(pa, [pa, p b]t) − wbD(pb, [pa, p b]t).

If pa̸ = pb and, for p ∈ { pa, p b}, each scalar map t 7 → D(p, [pa, p b]t) is finite and continuous on 

[0 , 1] , then Φ(0) < 0 and Φ(1) > 0. The intermediate value theorem gives a root in (0 , 1) .This observation proves pairwise existence only; it does not give uniqueness, a closed form, or associativity. We now prove the obstruction for f -divergences. Let f : (0 , ∞) → R be convex with 

f (1) = 0 . The associated f -divergence is (4.2) Df (p∥q) = 

Z

> Ω

q f 

pq



dμ, 

whenever the integral is defined, with the usual extended-value conventions at points where 

q = 0 . When we use Df as a divergence in the sense of (2.2) , we restrict to choices of f

and PDF classes for which Df (p∥q) = 0 implies p = q almost everywhere. The local results below require only the stated bounded-ratio assumptions and the strict quadratic behavior given by f ′′ (1) > 0. Adding a term proportional to u − 1 to f (u) does not change (4.2) , since R q(p/q − 1) d μ = R p dμ − R q dμ = 0 ; hence, after replacing f (u) by f (u) − f ′(1)( u − 1) , we may assume f ′(1) = 0 in the local argument. Under the local bounded-ratio assumptions used below, the argument applies to the standard f -divergence generators for KL, reverse KL, Hellinger-type divergences, Pearson and Neyman chi-square divergences, and Jensen–Shannon divergence [12, 3, 5, 13]. 

Theorem 4.1. Let f be twice continuously differentiable in a neighborhood of 1, with 

f (1) = f ′(1) = 0 and f ′′ (1) > 0. Let p be a strictly positive almost-everywhere PDF, and let h be measurable with R h dμ = 0 , R h2/p dμ ∈ (0 , ∞), and h/p essentially bounded. For sufficiently small ε > 0, let 

qε = p + εh, pε,t = [ p, q ε]t = p + tεh. 

Then, the balance equation 

(4.3) aD f (p∥pε,t ) = bD f (qε∥pε,t )

has a solution t ∈ (0 , 1) for all sufficiently small ε. If tε ∈ [0 , 1] is any selected solution, then 

(4.4) tε −→ √b

√a + √b (ε ↓ 0) .

> 6

Proof. Let u = h/p and I = R u2p dμ ∈ (0 , ∞). The bounded-ratio assumptions keep all likelihood ratios entering f in a compact neighborhood of 1 for small ε, so the endpoint maps are continuous. Since I > 0 and f ′′ (1) > 0, the endpoint divergences are positive for all sufficiently small ε, and the balance function changes sign; hence a balancing root exists by the intermediate value theorem. Uniform Taylor expansion at 1, with remainders uniform in 

t ∈ [0 , 1] , gives 

Df (p∥pε,t ) = f ′′ (1) 2 t2ε2I + o(ε2), Df (qε∥pε,t ) = f ′′ (1) 2 (1 − t)2ε2I + o(ε2).

Dividing the balance equation by (f ′′ (1) /2) ε2I and passing to subsequential limits gives 

aτ 2 = b(1 − τ )2, whose unique solution in [0 , 1] is (4.4). □

Theorem 4.1 gives the endpoint-to-candidate divergence barrier. Norm-induced balance gives b/ (a + b) because endpoint distance grows linearly along the segment. Smooth f -divergence balance gives √b/ (√a + √b) locally because the divergence grows quadratically. Thus, all such divergences agree with the transformed-weight monoid of Proposition 3.3 for 

g(w) = √w, not with additive supplied weights. The same observation yields a positive first-order result when the propagated mass follows the square-root law. For a binary aggregation tree T with leaves labeled by weighted states 

(wi, p εi ), we use the following square-root propagated Df -balancing protocol. At each internal node, let (WA, r εA) and (WB , r εB ) be the two child states. Choose a root of the endpoint-to-candidate Df -balance equation for rεA and rεB . The output density is the corresponding segment point, and the output weight is 

WA∪B = ( pWA + pWB )2.

Corollary 4.2. Let f satisfy the assumptions of Theorem 4.1, and let p be a strictly positive almost-everywhere PDF. For i = 1 , . . . , m , let pεi = p + εh i, where R hi dμ = 0 , hi/p is essentially bounded, and pεi is a PDF for all sufficiently small ε > 0. Let w1, . . . , w m > 0.For every binary aggregation tree T with leaves labeled by (wi, p εi ), the square-root propagated 

Df -balancing protocol has roots at all internal nodes for sufficiently small ε, and its output density satisfies 

(4.5) pε 

> out

= p + ε

Pmi=1 

√wi hi

Pmi=1 

√wi

+ o(ε),

where the remainder is understood in the finite-dimensional perturbation space span {h1, . . . , h m}

with norm ∥r∥p, ∞ = ∥r/p ∥∞. Thus the first-order term is independent of the aggregation tree. Proof. For a subtree with leaf set A, let GA = P

> i∈A

√wi and HA = G−1

> A

P

> i∈A

√wihi.Induction on the tree shows that the subtree has propagated weight G2 

> A

and density p + εH A +

o(ε). All o(ε) terms are taken in the finite-dimensional span generated by the hi; equivalently, after division by p, the corresponding remainders are uniformly bounded and converge to zero in that span. Since intermediate densities are convex combinations of the leaf densities, the bounded-ratio assumptions persist, so the local balance equation has a root for sufficiently small ε. At a parent with child leaf sets A, B , the two-perturbation Taylor expansion of the local balance equation gives segment coefficient GB /(GA + GB ) + o(1) unless HA = HB , in which case every coefficient gives the same first-order term. The parent density is therefore 

> 7

p + ε(GAHA + GB HB )/(GA + GB ) + o(ε) = p + εH A∪B + o(ε), and the propagated weight is 

(GA + GB )2 = G2 

> A∪B

. At the root this gives (4.5) , independently of the aggregation tree. □

Corollary 4.2 does not restore order-invariance for additive source weights. It identifies a first-order local repair: under square-root propagated weights, smooth f -divergence seg-ment balancing is tree-independent to first order in the local perturbation parameter, and exact associativity still requires propagating the transformed effective weight required by Proposition 3.3. The next result shows that this is not a criticism of divergence objectives in general. For the forward objective considered below, global barycenter objectives have a different local limit. 

Proposition 4.3. Let f and p satisfy the assumptions of Theorem 4.1. For i = 1 , . . . , m ,let pεi = p + εh i, where R hi dμ = 0 , hi/p is essentially bounded, and R h2 

> i

/p dμ < ∞. Let 

qεk = p + εk , where k has the same zero-integral, bounded-ratio, and square-integrability properties, and assume qεk is positive for sufficiently small ε. Then, 

(4.6) 

> m

X

> i=1

wiDf (pεi ∥qεk) = f ′′ (1) 2 ε2

> m

X

> i=1

wi

Z

> Ω

(hi − k)2

p dμ + o(ε2).

Consequently, within the local linear perturbation space, the quadratic limiting objective is minimized by 

(4.7) ¯h =

Pmi=1 wihi

Pmi=1 wi

.

Proof. For ui = hi/p and v = k/p , the bounded-ratio assumptions give 

pεi

qεk

= 1 + εu i

1 + εv = 1 + ε(ui − v) + O(ε2).

Uniform Taylor expansion at 1 yields (4.6) . The limiting quadratic objective equals a constant plus (P 

> i

wi) R (k − ¯h)2/p dμ, because P 

> i

wi(hi − ¯h) = 0 ; hence ¯h minimizes it within the local linear perturbation space. □

Proposition 4.3 separates two uses of divergences. Global f -barycenter objectives have the usual additive-weight target in their local quadratic limit; the proposition does not assert exact equality to linear pooling away from the local model. The square-root law is specific to endpoint-to-candidate binary balancing. With additive output weights, the following incompatibility remains. 

Corollary 4.4. No endpoint-to-candidate segment-balancing rule generated by a smooth f -divergence as in Theorem 4.1 can, on all sufficiently close endpoint pairs satisfying the stated local assumptions, simultaneously satisfy all of the following requirements: additive output weight a + b, continuous coefficient depending only on a and b, commutativity, associativity over all density classes with three affinely independent densities, and divergence balance. Proof. Theorem 3.2 forces coefficient b/ (a + b) under the algebraic assumptions. For a̸ = b,this value differs from √b/ (√a + √b). Theorem 4.1 supplies arbitrarily close endpoint pairs whose selected f -divergence balancing coefficients converge to the latter value, so the listed requirements cannot all hold. □

> 8

KL balancing gives a finite witness of the same obstruction. For the numerical cases below, let ⊕KL denote the binary rule that assigns output weight wa + wb and chooses the unique segment point satisfying the KL balancing equation. Let px be the Bernoulli density assigning probability x to outcome 1. For endpoints x < y , a segment point has Bernoulli parameter 

z = (1 − t)x + ty , equivalently t = ( z − x)/(y − x), and the KL balancing equation is 

wa KL( px∥pz ) = wb KL( py∥pz ).

For Bernoulli parameters x < y , the derivative of wa KL (px∥pz ) − wb KL (py∥pz ) with respect to z is (wa − wb)z − wax + wbyz(1 − z) .

Example 4.5. Consider the three equal-weight Bernoulli inputs (1 , p 0.1), (1 , p 0.6) and (1 , p 0.9).For the balancing equations used in this example, the derivative above is strictly positive on the relevant intervals, so each root is unique. One-dimensional root solving gives 

(1 , p 0.1) ⊕KL (1 , p 0.6) = (2 , p 0.332731 ) and (1 , p 0.6) ⊕KL (1 , p 0.9) = (2 , p 0.761290 ).

The two parenthesizations then give ((1 , p 0.1) ⊕KL (1 , p 0.6)) ⊕KL (1 , p 0.9) = (3 , p 0.581166 ), whereas 

(1 , p 0.1) ⊕KL ((1 , p 0.6) ⊕KL (1 , p 0.9)) = (3 , p 0.478333 ). The final Bernoulli parameters differ by approximately 0.102834 , so repeated local KL balancing is not associative. 

5. Compression Congruences for Gaussian Mixtures 

We now instantiate the algebraic results in a finite model class. Gaussian mixtures are closed under exact weighted linear fusion, but exact closure may increase the number of displayed components. Stepwise compression preserves order-invariance only under an algebraic compatibility condition. A finite Gaussian-mixture density on Rd has the form (5.1) p(x) = 

> m

X

> r=1

αrN (x; μr, Σr), αr ≥ 0,

> m

X

> r=1

αr = 1 ,

where each covariance matrix Σr is positive definite. A displayed component is one Gaussian term in a chosen finite representation; it is a representation-level object, not a unique density-level component. Zero-weight displayed components may be removed, and identical displayed components may be merged. The closure property is elementary but important: exact weighted fusion is linear pooling at the density level. 

Proposition 5.1. Let pi(x) = Pmi 

> r=1

αir N (x; μir , Σir ) be finite Gaussian mixtures and let 

wi > 0. The exact weighted fusion 

(5.2) pfused (x) = X

> imi

X

> r=1

wiαir 

P 

> j

wj

N (x; μir , Σir )

is a Gaussian mixture with at most P 

> i

mi displayed components. If, in the concatenated list, all displayed component weights are positive and no two displayed components have the same mean and covariance, the concatenated representation has exactly P 

> i

mi nonzero displayed components. 9Proof. Substituting each mixture expansion into P 

> i

wipi/ P 

> j

wj gives (5.2) . The displayed coefficients are nonnegative and sum to one. Duplicate or zero-weight displayed components may be removed. If all displayed weights are positive and no duplicates occur, the concatenated representation has exactly P 

> i

mi nonzero displayed components. □

Proposition 5.1 describes a density-level aggregate of already trained local models. It does not infer latent component correspondences, merge nearby components, or optimize likelihood under a prescribed component budget. For example, exact post-hoc fusion of two one-dimensional Gaussians N (−1, σ 2) and N (1 , σ 2) with equal weights gives a two-component mixture, whereas the population maximum-likelihood one-Gaussian approximation has mean 

0 and variance σ2 + 1 [7, 14]. To study compression, associate with a weighted Gaussian-mixture state (w, p ), where 

p = P 

> r

αrN (·; μr, Σr), the unnormalized component measure M(w,p ) = P 

> r

βrδ(μr ,Σr ) with 

βr = wα r. Exact weighted fusion corresponds to addition of these unnormalized measures followed by normalization by total mass. We therefore use unnormalized component measures (5.3) M =

> m

X

> r=1

βrδθr , θr = ( μr, Σr), βr ≥ 0,

including the zero measure. This unnormalized representation is different from the formal identity e for positive PDF states: here 0 is an actual zero component measure. Exact fusion before final normalization is addition in the commutative monoid M of such measures. Let 

C : M → M be an idempotent compression map with C(0) = 0 . When C is used to compress weighted PDF states, we additionally require total-mass preservation W (C(M )) = W (M )

unless an explicit weight renormalization is intended. Stepwise compressed fusion is (5.4) M ⊙C N = C(M + N ).

The question is when evaluating (5.4) at every internal node gives the same result as exact summation followed by one final compression. 

Theorem 5.2. Let C : M → M be idempotent with C(0) = 0 . The following conditions are equivalent. (1) For every finite family M1, . . . , M n ∈ M , n ≥ 2, every binary-tree evaluation using 

⊙C returns C(P 

> i

Mi).(2) C is incrementally compatible with addition: 

(5.5) C(C(M ) + N ) = C(M + N )

for all M, N ∈ M .(3) The equivalence relation M ∼ N iff C(M ) = C(N ) is a congruence for addition: 

M ∼ N implies M + R ∼ N + R for every R ∈ M .Proof. Assume (5.5) . Induction over binary trees shows that every nontrivial subtree with leaf set I evaluates to C(P 

> i∈I

Mi); the induction step uses (5.5) once or twice depending on whether the children are leaves. Conversely, the tree condition applied to the allowed three-leaf family M, 0, N gives C(C(M ) + N ) = C(M + N ). Finally, idempotence gives 

M ∼ C(M ), so congruence implies incremental compatibility; the reverse follows from 

C(M + R) = C(C(M ) + R) = C(C(N ) + R) = C(N + R) whenever C(M ) = C(N ). □

> 10

The theorem reduces compression-compatible fusion to a monoid-congruence condition. Identity compression is trivially safe. Moment compression to one Gaussian is also safe for positive total mass W (M ) > 0, with C(0) = 0 by convention, when the state stores total mass W (M ) = P 

> r

βr, first moment m(M ) = P 

> r

βrμr, and second raw moment 

S(M ) = P 

> r

βr(Σ r + μrμ⊤ 

> r

); these quantities are additive and preserved by the summary. Since the component covariances are positive definite and W (M ) > 0, the moment-matched covariance is positive definite. Fixed-dictionary compression is safe for the same reason provided the dictionary assignment is fixed, componentwise, independent of the current batch or aggregation tree, and assigns dictionary atoms to themselves so that C is idempotent: it stores additive mass assigned to each dictionary element. For weighted Gaussian states, let ⊕C mean exact weighted fusion followed by the com-pression C on the associated unnormalized component measure. Common local reduction heuristics require more care. Gaussian-mixture reduction by pruning or pairwise merging is a standard approximation problem [ 16 ], but such reduction does not automatically define a schedule-independent fusion rule. 

Example 5.3. Let C retain the displayed Gaussian component with largest normalized coefficient, equivalently largest unnormalized component mass within a fixed total weight. The total state weight is preserved, and ties are broken by choosing the smaller mean. Consider 

x = (1 , N (0 , 1)) , y = (1 , N (1 , 1)) , z = (1 , N (2 , 1)) .

Then C(x ⊕ y) = (2 , N (0 , 1)) and C(y ⊕ z) = (2 , N (1 , 1)) . Therefore, 

(x ⊕C y) ⊕C z = (3 , N (0 , 1)) , x ⊕C (y ⊕C z) = (3 , N (1 , 1)) .

The two final states differ, so local pruning is not associative. 

Lossy compression is therefore not an implementation detail unless it satisfies the congruence condition or carries an explicit approximation guarantee. Example 5.3 separates exact semantic protocols from local reduction heuristics: exact weighted fusion and additive summaries are tree-independent, while local pruning is not. 6. Related Work and Discussion 

Linear and logarithmic opinion pools are standard rules for aggregating probability dis-tributions and expert forecasts [ 19 , 8, 10 ]. Our question is not which pooling formula to choose, but when a binary pooling rule can be used as a schedule-independent distributed protocol. Within continuous segment-valued rules with additive weights and weight-only coefficients, Theorem 3.2 shows that order-invariant iteration forces normalized linear pooling. This places Theorem 3.2 close to representation results for associative means and functional equations [1], but our setting is weighted density states and hierarchical execution. Dempster–Shafer theory, the transferable belief model, and distances between bodies of evi-dence provide broader frameworks for uncertain evidence fusion [ 6, 17 , 18 , 11 ]. Distributional averages based on f -divergences, Bregman divergences, or Wasserstein barycenters define global aggregation principles [ 3, 5, 4, 15 , 2]. These principles answer a different question: a global barycenter may be well posed, but this does not imply that repeatedly solving binary barycenter or balancing subproblems along an arbitrary tree is associative. 

> 11

Gaussian-mixture learning and Gaussian-mixture reduction are adjacent but distinct [ 7, 14 ,16 , 20 ]. Distributed learning of Gaussian mixtures estimates mixture parameters from parti-tioned data or sufficient statistics. Mixture-reduction algorithms compress a given mixture under an approximation criterion. We instead assume already trained local densities and ask which exact or compressed post-hoc fusion procedures have a tree-independent semantics. The divergence and compression results are compositional boundary results: pairwise solvability and model-size reduction are not enough for schedule-independent uncertainty fusion. 7. Conclusion 

We have studied weighted density fusion as a compositionality problem for scalable uncertainty management. Our results give exact algebraic conditions for schedule-independent binary fusion in the segment-valued additive class. In this class, norm-induced segment balancing yields weighted linear pooling, and commutative associative hierarchical execution forces the normalized-weight coefficient. Transformed-weight rules avoid this conclusion only by changing the quantity propagated additively by the state. Smooth endpoint-to-candidate f -divergence balancing does not satisfy the same additive-weight compositional requirement. Under the bounded-ratio local model of Theorem 4.1, its quadratic geometry induces square-root effective weights rather than additive source weights, yielding only first-order local tree-independence under square-root propagation; the Bernoulli KL example gives a finite associativity failure. Global f -barycenter objectives behave differently: their local quadratic limit retains additive-weight targets. For Gaussian mixtures, exact fusion is schedule-independent but may increase the number of displayed components, while stepwise compression is safe exactly when idempotent compression is compatible with unnormalized component addition. Future work should quantify how endpoint-dependent divergence rules and approximate compression maps deviate from associativity, for example through explicit associator or tree-dependent error bounds. 

References                              

> 1. János Aczél and Hansjorg Oser, Lectures on functional equations and their applications , Courier Corpora-tion, 2006. 2. Martial Agueh and Guillaume Carlier, Barycenters in the Wasserstein space , SIAM Journal on Mathe-matical Analysis 43 (2011), no. 2, 904–924. 3. S. M. Ali and Samuel D. Silvey, A general class of coefficients of divergence of one distribution from another , Journal of the Royal Statistical Society: Series B (Methodological) 28 (1966), no. 1, 131–142. 4. Arindam Banerjee, Srujana Merugu, Inderjit S Dhillon, and Joydeep Ghosh, Clustering with Bregman divergences , Journal of Machine Learning Research 6(2005), no. Oct, 1705–1749. 5. Imre Csiszár, On information-type measure of difference of probability distributions and indirect observa-tions , Studia Sci. Math. Hungar. 2(1967), 299–318. 6. Arthur P Dempster, Upper and lower probabilities induced by a multivalued mapping , Classic Works of the Dempster–Shafer Theory of Belief Functions, Springer, 2008, pp. 57–72. 7. Arthur P Dempster, Nan M Laird, and Donald B Rubin, Maximum likelihood from incomplete data via the EM algorithm , Journal of the royal statistical society: series B (methodological) 39 (1977), no. 1, 1–22. 8. Christian Genest and James V Zidek, Combining probability distributions: A critique and an annotated bibliography , Statistical Science 1(1986), no. 1, 114–135. 9. Michel Grabisch, Jean-Luc Marichal, Radko Mesiar, and Endre Pap, Aggregation functions , vol. 127, Cambridge University Press, 2009.
> 12

10. Robert A Jacobs, Methods for combining experts’ probability assessments , Neural computation 7 (1995), no. 5, 867–888. 11. Anne-Laure Jousselme, Dominic Grenier, and Éloi Bossé, A new distance between two bodies of evidence ,Information fusion 2 (2001), no. 2, 91–101. 12. Solomon Kullback and Richard A Leibler, On information and sufficiency , The annals of mathematical statistics 22 (1951), no. 1, 79–86. 13. Jianhua Lin, Divergence measures based on the shannon entropy , IEEE Transactions on Information theory 37 (1991), no. 1, 145–151. 14. Geoffrey McLachlan and David Peel, Finite mixture models. hoboken , NJ: John Wiley & Sons. doi 10 

(2000), 0471721182. 15. Frank Nielsen and Richard Nock, Sided and symmetrized Bregman centroids , IEEE Transactions on Information Theory 55 (2009), no. 6, 2882–2904. 16. Andrew R. Runnalls, Kullback–leibler approach to Gaussian mixture reduction , IEEE Transactions on Aerospace and Electronic Systems 43 (2007), no. 3, 989–999. 17. Glenn Shafer, A mathematical theory of evidence , vol. 42, Princeton University Press, 1976. 18. Philippe Smets and Robert Kennes, The transferable belief model , Artificial intelligence 66 (1994), no. 2, 191–234. 19. Mervyn Stone, The opinion pool , The Annals of Mathematical Statistics 32 (1961), no. 4, 1339–1342. 20. Qiong Zhang and Jiahua Chen, Distributed learning of finite gaussian mixtures , Journal of Machine Learning Research 23 (2022), no. 99, 1–40. 

Appendix A. Extended Proofs 

Notations are the same as in Sections 2–5. 

Proof of Theorem 2.2. Let pt = [ pa, p b]t = (1 − t)pa + tp b. Then, 

pt − pa = t(pb − pa), pt − pb = (1 − t)( pa − pb).

Since ∆( p, q ) = ∥p − q∥ is induced by a norm, absolute homogeneity gives 

∆( pa, p t) = ∥pa − pt∥ = t∥pb − pa∥

and 

∆( pb, p t) = ∥pb − pt∥ = (1 − t)∥pb − pa∥.

If pa̸ = pb in the almost-everywhere quotient, then ∥pb − pa∥ > 0. The balance equation is therefore equivalent to 

wat∥pb − pa∥ = wb(1 − t)∥pb − pa∥,

and cancellation of the positive common factor yields wat = wb(1 − t). Solving gives 

t∗ = wb

wa + wb

.

Substitution into [pa, p b]t gives the stated fused density. The scalar equation in t is linear with nonzero coefficient wa + wb, so the balancing point is unique. If pa = pb almost everywhere, then [pa, p b]t = pa for all t ∈ [0 , 1] . The parameter may not be unique, but the density is unique. 13 Proof of Proposition 2.3. For positive states, the operation in (2.5) gives 

(wa, p a) ⊕ (wb, p b) = 



wa + wb, wapa + wbpb

wa + wb



.

Applying η gives 

η(( wa, p a) ⊕ (wb, p b)) = 



wa + wb, (wa + wb)wapa + wbpb

wa + wb



= ( wa + wb, w apa + wbpb)= ( wa, w apa) + ( wb, w bpb)= η(wa, p a) + η(wb, p b).

The element e satisfies e⊕x = x⊕e = x by definition, and η(e) = (0 , 0) is the additive identity in represented coordinates. Closure follows from convexity of P: the second coordinate of the fused state is a convex combination of pa and pb, because wa, w b > 0 and the coefficients sum to one. Since addition of represented pairs is commutative and associative, the original operation is commutative and associative as well. For a finite nonempty family, repeated use of the additive representation yields 

η(x1 ⊕ · · · ⊕ xm) = 

> m

X

> i=1

η(wi, p i) = 

> m

X

> i=1

wi,

> m

X

> i=1

wipi

!

.

Recovering the weighted state from the represented pair gives (2.7) . Commutativity and associativity make the result independent of the parenthesization and order. 

Proof of Lemma 3.1. The assumptions give a continuous function m : R2 

> >0

→ (0 , 1) 

satisfying 

m(a, b )m(a + b, c ) = m(a, b + c).

Since m is positive, the function h(a, b ) = − log m(a, b ) is continuous and satisfies (A.1) h(a, b ) + h(a + b, c ) = h(a, b + c)

for all a, b, c > 0. We construct a potential H with h(a, b ) = H(a + b) − H(a). Choose 

r > 0. We first justify the boundary value at r. For fixed a, c > 0, equation (A.1) gives 

h(a, b ) + h(a + b, c ) = h(a, b + c) for every b > 0. Letting b ↓ 0 and using continuity of h on the positive domain gives lim b↓0 h(a, b ) = 0 . Hence h(r, x − r) → 0 as x ↓ r and h(x, r − x) → 0 as 

x ↑ r. On (r, ∞), set H(x) = h(r, x − r), set H(r) = 0 , and on (0 , r ) set H(x) = −h(x, r − x).The preceding limit shows that H is continuous at r.We verify the representation. If a > r , then a + b > r , and (A.1) with first argument r,second argument a − r, and third argument b gives 

h(r, a − r) + h(a, b ) = h(r, a + b − r).

Thus, 

h(a, b ) = H(a + b) − H(a).

If a + b < r , then a < r . Applying (A.1) with first argument a, second argument b, and third argument r − a − b gives 

h(a, b ) + h(a + b, r − a − b) = h(a, r − a),

> 14

so 

h(a, b ) = −h(a + b, r − a − b) + h(a, r − a) = H(a + b) − H(a).

If a < r < a + b, let c = r − a > 0 and d = a + b − r > 0. Applying (A.1) to (a, c, d ) gives 

h(a, r − a) + h(r, a + b − r) = h(a, b ).

By the definition of H, this is again h(a, b ) = H(a + b) − H(a). The boundary cases follow by continuity. Hence the representation holds for all positive a, b .Let F (x) = exp H(x). Then, F is positive and continuous, and 

m(a, b ) = exp( −h(a, b )) = F (a)

F (a + b).

If another positive continuous eF gives the same representation, then F (a)/ eF (a) = F (a +

b)/ eF (a + b) for all a, b > 0. For any x < y , choose b = y − x and obtain the same ratio at 

x and y; hence the ratio is constant on R>0. Thus, F is unique up to multiplication by a positive constant. 

Proof of Theorem 3.2. Let m(a, b ) = 1 − α(a, b ). Then m is continuous and takes values in (0 , 1) . For positive weights a, b , the operation has the form 

(a, p ) ⊙ (b, q ) = ( a + b, m (a, b )p + (1 − m(a, b )) q).

Choose a convex PDF class containing three affinely independent densities pa, p b, p c. Associa-tivity says that the density component of 

(( a, p a) ⊙ (b, p b)) ⊙ (c, p c)

coincides with the density component of 

(a, p a) ⊙ (( b, p b) ⊙ (c, p c)) .

Expanding the left parenthesization gives the coefficient of pa as m(a, b )m(a+b, c ). Expanding the right parenthesization gives the coefficient of pa as m(a, b + c). Since pa, p b, p c are affinely independent, equality of the two convex combinations implies equality of the corresponding barycentric coefficients. Therefore, 

m(a, b )m(a + b, c ) = m(a, b + c)

for all a, b, c > 0.Lemma 3.1 gives a positive continuous function F with 

m(a, b ) = F (a)

F (a + b).

Commutativity of ⊙ means that the density produced from (a, p ) and (b, q ) is the same as the density produced from (b, q ) and (a, p ). Comparing the coefficients of p and q gives 

m(a, b ) = 1 − m(b, a ),

or equivalently m(a, b ) + m(b, a ) = 1 . Substituting the representation gives 

F (a)

F (a + b) + F (b)

F (a + b) = 1 .

Thus, 

F (a + b) = F (a) + F (b)

> 15

for all positive a, b . Since F is continuous and positive on R>0, the Cauchy equation on the positive reals has the form F (w) = cw for some constant c > 0. Hence 

m(a, b ) = aa + b, α(a, b ) = 1 − m(a, b ) = ba + b.

Proof of Proposition 3.3. Let 

ηg(w, p ) = ( g(w), g (w)p), ηg(e) = (0 , 0) .

For positive states, the operation in (3.3) gives 

ηg(( a, p ) ⊕g (b, q )) = 



g(a) + g(b), (g(a) + g(b)) g(a)p + g(b)qg(a) + g(b)



= ( g(a) + g(b), g (a)p + g(b)q)= ηg(a, p ) + ηg(b, q ).

The first represented coordinate is positive for every positive state, and the original state is recovered from (s, r ) with s > 0 as (g−1(s), r/s ). The formal element e corresponds to the additive zero. Therefore, addition of represented pairs transfers closure, commutativity, associativity, and the identity laws to ⊕g. Iteration gives 

ηg(x1 ⊕g · · · ⊕ g xm) = 

> m

X

> i=1

g(wi),

> m

X

> i=1

g(wi)pi

!

,

which recovers (3.4). 

Proof of Theorem 4.1. Let u = h/p . The assumptions give R up dμ = 0 , I = R u2p dμ ∈

(0 , ∞), and ∥u∥∞ < ∞. For sufficiently small ε > 0, the quantities 1 + εu and 1 + tεu are bounded away from zero uniformly in t ∈ [0 , 1] . Hence qε = p(1 + εu ) and pε,t = p(1 + tεu )

are nonnegative densities, and all ratios entering f lie in a compact interval contained in the neighborhood where f is twice continuously differentiable. The endpoint maps are finite and continuous; the expansions below show that the balance function is negative at t = 0 and positive at t = 1 for sufficiently small ε, so at least one root exists. For the left endpoint divergence, the ratio is 

ppε,t 

= 11 + tεu .

Uniformly in t ∈ [0 , 1] ,

11 + tεu = 1 − tεu + t2ε2u2 + O(ε3),

where the remainder is dominated by a constant multiple of ε3 because u is essentially bounded. Since f (1) = f ′(1) = 0 ,

f (1 + s) = f ′′ (1) 2 s2 + o(s2)

> 16

uniformly for s in a sufficiently small compact neighborhood of zero. Multiplying by pε,t =

p(1 + tεu ), integrating, and using bounded domination gives 

Df (p∥pε,t ) = 

Z

p(1 + tεu )f

 11 + tεu 



dμ

= f ′′ (1) 2 t2ε2

Z

u2p dμ + o(ε2)= f ′′ (1) 2 t2ε2I + o(ε2),

with the o(ε2) term uniform over t ∈ [0 , 1] .For the right endpoint divergence, 

qε

pε,t 

= 1 + εu 

1 + tεu = 1 + (1 − t)εu + O(ε2)

uniformly in t. The same Taylor argument gives 

Df (qε∥pε,t ) = f ′′ (1) 2 (1 − t)2ε2I + o(ε2),

uniformly in t ∈ [0 , 1] .Let tε be any selected solution of (4.3) . The interval [0 , 1] is compact, so every sequence 

εk ↓ 0 has a subsequence along which tεk → τ ∈ [0 , 1] . Along that subsequence, divide the balance equation by (f ′′ (1) /2) ε2

> k

I. The two uniform expansions yield 

aτ 2 = b(1 − τ )2.

Both sides are nonnegative. Taking square roots gives √a τ = √b (1 − τ ), and therefore 

τ =

√b

√a + √b.

Every convergent subsequence has the same limit, so the whole family tε converges to the displayed value. 

Proof of Corollary 4.2. For a set A of leaves, let 

GA = X

> i∈A

√wi, HA = 1

GA

X

> i∈A

√wi hi.

Let V = span {h1, . . . , h m} and equip V with the norm ∥r∥p, ∞ := ∥r/p ∥∞. This norm is finite on V because each hi/p is essentially bounded. We prove by induction on subtrees that the state computed for every subtree with leaf set A has propagated weight G2 

> A

and density 

p + εH A + rεA, ∥rεA∥p, ∞ = o(ε).

Every intermediate density is a convex combination of the leaf densities, hence has the form 

p + εH with H in the compact convex hull generated by the hi. Therefore, the bounded-ratio assumptions persist uniformly at internal nodes. The assertion is immediate for a leaf, with 

rεA = 0 .Assume the assertion for two child subtrees with leaf sets A and B. Their densities have the form 

p + εH εA, p + εH εB ,

> 17

where HεA → HA and HεB → HB in ∥ · ∥ p, ∞ within the finite-dimensional span generated by the hi. Let θε be a balancing coefficient at the parent. A candidate point on the segment is 

p + ε (1 − θε)HεA + θεHεB

.

The same uniform Taylor expansion as in Theorem 4.1, applied over the compact bounded-ratio set containing the two perturbations HεA and HεB , gives 

Df

 p + εH εA∥p + ε((1 − θ)HεA + θH εB ) = f ′′ (1) 2 ε2θ2Jε + o(ε2),Df

 p + εH εB ∥p + ε((1 − θ)HεA + θH εB ) = f ′′ (1) 2 ε2(1 − θ)2Jε + o(ε2),

where 

Jε =

Z

> Ω

(HεB − HεA)2

p dμ. 

If HA̸ = HB , then Jε converges to a positive limit. Dividing the balance equation by 

(f ′′ (1) /2) ε2Jε gives 

G2

> A

θ2 

> ε

= G2 

> B

(1 − θε)2 + o(1) ,

so every accumulation point of θε is GB /(GA + GB ). Hence θε = GB /(GA + GB ) + o(1) . If 

HA = HB , then both endpoints have the same first-order perturbation; hence every segment coefficient gives the same first-order perturbation, and no coefficient limit is needed in the induction step. In both cases the parent density is 

p + εGAHA + GB HB

GA + GB

+ o(ε) = p + ε

P

> i∈A∪B

√wihi

GA + GB

+ o(ε)= p + εH A∪B + o(ε).

The propagated-weight rule gives 

(

q

G2 

> A

+

q

G2 

> B

)2 = ( GA + GB )2 = G2 

> A∪B

.

The induction proves the root claim, and the root is independent of the tree because both 

G{1,...,m } and H{1,...,m } are determined only by the leaves. 

Proof of Proposition 4.3. Let ui = hi/p and v = k/p . The assumptions imply that all ui

and v are essentially bounded and square-integrable with respect to the measure p dμ, and 

k belongs to the local linear perturbation class specified in Proposition 4.3. For sufficiently small ε, both pεi = p(1 + εu i) and qεk = p(1 + εv ) are densities, and the ratios remain in a compact neighborhood of 1.For each i,

pεi

qεk

= 1 + εu i

1 + εv = 1 + ε(ui − v) + O(ε2),

> 18

where the remainder is dominated by a constant times ε2 on the bounded local class. Taylor expansion at 1 gives 

Df (pεi ∥qεk) = 

Z

p(1 + εv )f

1 + εu i

1 + εv 



dμ

= f ′′ (1) 2 ε2

Z

(ui − v)2p dμ + o(ε2)= f ′′ (1) 2 ε2

Z

> Ω

(hi − k)2

p dμ + o(ε2).

Summing over i with weights wi gives (4.6). For the limiting objective 

Q(k) = 

> m

X

> i=1

wi

Z

> Ω

(hi − k)2

p dμ, 

let ¯h = ( P 

> i

wihi)/(P 

> i

wi). Expanding around ¯h gives 

Q(k) = X

> i

wi

Z (hi − ¯h + ¯h − k)2

p dμ

= X

> i

wi

Z (hi − ¯h)2

p dμ + X

> i

wi

! Z (k − ¯h)2

p dμ, 

where the cross term vanishes because P 

> i

wi(hi − ¯h) = 0 . The second term is nonnegative and equals zero exactly at k = ¯h within the local linear perturbation space. Hence ¯h is the minimizer there. 

Proof of Corollary 4.4. Assume that a rule satisfies the algebraic requirements listed in the corollary. Theorem 3.2 applies, because the rule has additive output weight, a continuous coefficient depending only on the two weights, and commutative associative execution over all convex density classes containing three affinely independent densities. Therefore, the segment coefficient must be 

ba + b.

Choose a̸ = b. The equality 

ba + b =

√b

√a + √b

would imply b(√a+√b) = ( a+b)√b, hence b√a = a√b, and therefore √a = √b, contradicting 

a̸ = b. Theorem 4.1 gives sufficiently close endpoint pairs for which every selected f -divergence balancing coefficient converges to the square-root value. For small enough ε, that coefficient cannot equal the normalized additive-weight coefficient required by Theorem 3.2. Hence the listed requirements cannot all hold. 19 Verification of the KL witness. For a Bernoulli density px on {0, 1}, the KL divergence to pz is 

KL( px∥pz ) = x log xz + (1 − x) log 1 − x

1 − z .

For fixed endpoints x < y and positive weights wa, w b, the scalar balance function is 

F (z) = wa KL( px∥pz ) − wb KL( py∥pz ), z ∈ (x, y ).

Differentiating gives 

F ′(z) = wa



−xz + 1 − x

1 − z



− wb



−yz + 1 − y

1 − z



= (wa − wb)z − wax + wbyz(1 − z) .

On each numerical interval used in the main text, the numerator is positive, and the denominator is positive because z ∈ (0 , 1) . Thus, the root is unique. Solving F (z) = 0 by one-dimensional bisection or Newton iteration gives 

(1 , p 0.1) ⊕KL (1 , p 0.6) = (2 , p 0.332731 )

and 

(1 , p 0.6) ⊕KL (1 , p 0.9) = (2 , p 0.761290 ).

The second fusion step then gives 

((1 , p 0.1) ⊕KL (1 , p 0.6)) ⊕KL (1 , p 0.9) = (3 , p 0.581166 )

and 

(1 , p 0.1) ⊕KL ((1 , p 0.6) ⊕KL (1 , p 0.9)) = (3 , p 0.478333 ).

The final parameters differ by approximately 0.102833 , so the induced binary operation is not associative. 

Proof of Proposition 5.1. For each input mixture, 

pi(x) = 

> mi

X

> r=1

αir N (x; μir , Σir ), αir ≥ 0,

> mi

X

> r=1

αir = 1 .

The exact weighted fusion is 

P 

> i

wipi(x)

P 

> j

wj

= 1

P 

> j

wj

X

> i

wimiX

> r=1

αir N (x; μir , Σir )= X

> imi

X

> r=1

wiαir 

P 

> j

wj

N (x; μir , Σir ).

All displayed coefficients are nonnegative. Their sum is 

X

> imi

X

> r=1

wiαir 

P 

> j

wj

=

P 

> i

wi

P 

> r

αir 

P 

> j

wj

= 1 .

Thus, the result is a finite Gaussian mixture with at most P 

> i

mi displayed components. If every displayed component weight is positive and no two displayed components have the same mean and covariance, no component is removed by zero coefficient deletion or duplicate 

> 20

merging. The concatenated representation therefore has exactly P 

> i

mi nonzero displayed components. 

Proof of Theorem 5.2. We prove the equivalence of the three statements. Assume first that (5.5) holds. We show by induction on a binary aggregation tree that every nontrivial subtree with leaf set I evaluates to 

C X

> i∈I

Mi

!

.

For two leaves i, j , the value is Mi ⊙C Mj = C(Mi + Mj ), so the claim holds. Consider a larger subtree with child leaf sets I and J. If both children are nontrivial, the induction hypothesis gives the value 

C C X

> i∈I

Mi

!

+ C X

> j∈J

Mj

!! 

.

Applying (5.5) once with M = P 

> i∈I

Mi and N = C(P 

> j∈J

Mj ) gives 

C X

> i∈I

Mi + C X

> j∈J

Mj

!! 

.

By commutativity of addition and another use of (5.5) , now with M = P 

> j∈J

Mj and 

N = P 

> i∈I

Mi, this equals 

C X

> i∈I

Mi + X

> j∈J

Mj

!

.

If one child is a leaf, the same argument uses (5.5) once. Thus, every binary tree evaluates to final compression of the exact sum. Next assume the tree condition. Since 0 is the zero component measure in M, it is an allowed leaf. Apply the tree condition to the three-leaf family M, 0, N . In the tree that first combines M and 0, the first internal node gives 

M ⊙C 0 = C(M + 0) = C(M ),

since 0 is the additive zero measure. Combining with N gives C(C(M ) + N ). The tree condition says that every binary tree has the same value as final compression of the exact sum, namely C(M + 0 + N ) = C(M + N ). Hence (5.5) holds. It remains to show the equivalence of incremental compatibility and congruence. Suppose first that ∼ is a congruence. Since C is idempotent, C(C(M )) = C(M ), so M ∼ C(M ).Congruence under addition gives M + N ∼ C(M ) + N , which means 

C(M + N ) = C(C(M ) + N ).

Thus, incremental compatibility holds. Conversely, assume incremental compatibility and let 

M ∼ N . Then C(M ) = C(N ). For any R ∈ M ,

C(M + R) = C(C(M ) + R)= C(C(N ) + R)= C(N + R),

> 21

where the first and last equalities use incremental compatibility. Hence M + R ∼ N + R.Therefore, ∼ is a congruence for addition. 

Verification of the pruning example. Let C retain the displayed component with largest normalized coefficient, equivalently largest unnormalized component mass within a fixed total weight, preserve the total state weight, and break coefficient ties by choosing the smaller mean. For 

x = (1 , N (0 , 1)) , y = (1 , N (1 , 1)) , z = (1 , N (2 , 1)) ,

exact fusion of x and y gives a two-component mixture with total weight 2 and equal component coefficients. The tie rule keeps the smaller mean, so C(x ⊕ y) = (2 , N (0 , 1)) .Similarly, C(y ⊕z) = (2 , N (1 , 1)) . Fusing the first compressed state with z gives unnormalized component masses 2 for N (0 , 1) and 1 for N (2 , 1) , so the largest-mass rule keeps N (0 , 1) and 

(x ⊕C y) ⊕C z = (3 , N (0 , 1)) .

Fusing x with the second compressed state gives unnormalized component masses 1 for 

N (0 , 1) and 2 for N (1 , 1) , so the largest-mass rule keeps N (1 , 1) and 

x ⊕C (y ⊕C z) = (3 , N (1 , 1)) .

The two final states differ, so local pruning is not associative. The example is only a witness for tree dependence; it is not a claim about the statistical quality of pruning.