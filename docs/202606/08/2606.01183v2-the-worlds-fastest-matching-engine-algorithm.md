---
title: "The World's Fastest Matching Engine Algorithm"
source: arxiv
title_zh: "World's Fastest Matching Engine Algorithm"
abstract_zh: "single CPU core sustains 32 million order messages per second at sub-microsecond median wire-to-wire。\n\n核心做法：作者提出 two data-structure contributions that eliminate them。\n\n主要结果：Under micro-bursts these costs produce tail-latency spikes that degrade market quality precisely whe。\n\n值得关注的点在于：Neighbor-aware insertion 与 deletion use known neighbor references to attach or remove node 结合 O (1)。"
abstract_en: "A single CPU core sustains 32 million order messages per second at sub-microsecond median wire-to-wire latency, up to 11 times faster than the best open-source matching engines on identical hardware. Scaled out, a single 96-core commodity server (~$1,630/month) sustains ~640 million messages per second across 10,000 symbols, over 20 times the provisioned capacity of the U.S. consolidated quote feed. We reach these numbers by attacking the storage layer that sets matching latency. The dominant order-book implementation, linked lists chained through a balanced tree, imposes two costs on every operation: pointer-chased traversal to the insertion point, and root-to-leaf search to locate the target price level. Under micro-bursts these costs produce tail-latency spikes that degrade market quality precisely when liquidity is most needed. We present two data-structure contributions that eliminate them. The first is the Priority-Indicated Node (PIN), a priority queue in which entries occupy fixed-capacity, contiguously addressable slots, with indicators encoding each entry's global priority status. Unlike heaps, which require O(log n) comparisons per operation, the PIN resolves insertion position directly from the indicators without comparing entries; indicator updates are O(1), independent of queue size. A depth-aware capacity model sizes each PIN so hot entries fit within L1 residency. The second targets a broader inefficiency: balanced search trees search from root to leaf on every insertion and deletion, even when the caller already knows the key's in-order neighbors, which in ordered event streams and electronic trading are available at zero cost. Neighbor-aware insertion and deletion use known neighbor references to attach or remove a node with O(1) reference writes, followed by single-path rebalancing, across red-black, AVL, and B+-tree variants."
authors: Jake Yoon
date: 2026-05-31
journal: cs.DC
source_link: "https://arxiv.org/pdf/2606.01183v2"
pdf: "https://arxiv.org/pdf/2606.01183v2"
score: 8.549999999999999
tldr: "围绕《World's Fastest Matching...》；作者提出 two data-structure contribu...；Under micro-bursts these costs produ..."
tldr_cn: "围绕《World's Fastest Matching...》；作者提出 two data-structure contribu...；Under micro-bursts these costs produ..."
tldr_en: "A single CPU core sustains 32 million order messages per second at sub-microsecond median wire-to-wire latency, up to 11 times faster than the best open-source matching engines on identical hardware. Scaled out, a single 96-core commodity server (~$1,630/month) sustains ~640 million messages per second across 10,000 symbols, over 20 times the provisioned capacity of the..."
selection_source: fresh_fetch
motivation: single CPU core sustains 32 million order messages per second at sub-microsecond median wire-to-wire。
method: 核心做法：作者提出 two data-structure contributions that eliminate them。
result: 主要结果：Under micro-bursts these costs produce tail-latency spikes that degrade market quality precisely whe。
conclusion: 值得关注的点在于：Neighbor-aware insertion 与 deletion use known neighbor references to attach or remove node 结合 O (1)。
---

## 摘要
single CPU core sustains 32 million order messages per second at sub-microsecond median wire-to-wire。

核心做法：作者提出 two data-structure contributions that eliminate them。

主要结果：Under micro-bursts these costs produce tail-latency spikes that degrade market quality precisely whe。

值得关注的点在于：Neighbor-aware insertion 与 deletion use known neighbor references to attach or remove node 结合 O (1)。

## Abstract
A single CPU core sustains 32 million order messages per second at sub-microsecond median wire-to-wire latency, up to 11 times faster than the best open-source matching engines on identical hardware. Scaled out, a single 96-core commodity server (~$1,630/month) sustains ~640 million messages per second across 10,000 symbols, over 20 times the provisioned capacity of the U.S. consolidated quote feed. We reach these numbers by attacking the storage layer that sets matching latency. The dominant order-book implementation, linked lists chained through a balanced tree, imposes two costs on every operation: pointer-chased traversal to the insertion point, and root-to-leaf search to locate the target price level. Under micro-bursts these costs produce tail-latency spikes that degrade market quality precisely when liquidity is most needed. We present two data-structure contributions that eliminate them. The first is the Priority-Indicated Node (PIN), a priority queue in which entries occupy fixed-capacity, contiguously addressable slots, with indicators encoding each entry's global priority status. Unlike heaps, which require O(log n) comparisons per operation, the PIN resolves insertion position directly from the indicators without comparing entries; indicator updates are O(1), independent of queue size. A depth-aware capacity model sizes each PIN so hot entries fit within L1 residency. The second targets a broader inefficiency: balanced search trees search from root to leaf on every insertion and deletion, even when the caller already knows the key's in-order neighbors, which in ordered event streams and electronic trading are available at zero cost. Neighbor-aware insertion and deletion use known neighbor references to attach or remove a node with O(1) reference writes, followed by single-path rebalancing, across red-black, AVL, and B+-tree variants.
