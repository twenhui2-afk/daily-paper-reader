---
title: Note on the Maximum Number of Trees Displayed by a Tree-Child Network
source: arxiv
title_zh: Trees Displayed by Tree-Child Network的Note on Maximum Number
abstract_zh: "In this note，we show that，用于 all n\\ge 2，number 的 distinct rooted binary phylogenetic X-trees display。\n\n核心做法：Furthermore，if \\mathcal{N} displays exactly 2^{n-1}-1 such trees，then exactly one rooted binary phyl。\n\n主要结果：摘要给出了正向结果。\n\n值得关注的点在于：这篇文章的价值在于把《Trees Displayed by Tree-Child Network的Note on Maximum Number》。"
abstract_en: "In this note, we show that, for all $n\\ge 2$, the number of distinct rooted binary phylogenetic $X$-trees displayed by a binary tree-child network $\\mathcal{N}$ on $X$ with $n$ leaves is at most $2^{n-1}-1$ and that this upper bound is sharp. Furthermore, if $\\mathcal{N}$ displays exactly $2^{n-1}-1$ such trees, then exactly one rooted binary phylogenetic $X$-tree is displayed twice, and this tree can be canonically found by iteratively replacing a reticulated cherry with a cherry."
authors: "Yukihiro Murakami, Charles Semple"
date: 2026-06-12
journal: math.CO
source_link: "https://arxiv.org/pdf/2606.14464v1"
pdf: "https://arxiv.org/pdf/2606.14464v1"
score: 7.55
tldr: "围绕《Trees Displayed by Tree-...》；Furthermore，if \\mathcal{N} displ...；摘要给出了正向结果。"
tldr_cn: "围绕《Trees Displayed by Tree-...》；Furthermore，if \\mathcal{N} displ...；摘要给出了正向结果。"
tldr_en: "In this note, we show that, for all $n\\ge 2$, the number of distinct rooted binary phylogenetic $X$-trees displayed by a binary tree-child network $\\mathcal{N}$ on $X$ with $n$ leaves is at most $2^{n-1}-1$ and that this upper bound is sharp. Furthermore, if $\\mathcal{N}$ displays exactly $2^{n-1}-1$ such trees, then exactly one rooted binary phylogenetic..."
selection_source: fresh_fetch
motivation: In this note，we show that，用于 all n\ge 2，number 的 distinct rooted binary phylogenetic X-trees display。
method: "核心做法：Furthermore，if \\mathcal{N} displays exactly 2^{n-1}-1 such trees，then exactly one rooted binary phyl。"
result: 主要结果：摘要给出了正向结果。
conclusion: 值得关注的点在于：这篇文章的价值在于把《Trees Displayed by Tree-Child Network的Note on Maximum Number》。
---

## 摘要
In this note，we show that，用于 all n\ge 2，number 的 distinct rooted binary phylogenetic X-trees display。

核心做法：Furthermore，if \mathcal{N} displays exactly 2^{n-1}-1 such trees，then exactly one rooted binary phyl。

主要结果：摘要给出了正向结果。

值得关注的点在于：这篇文章的价值在于把《Trees Displayed by Tree-Child Network的Note on Maximum Number》。

## Abstract
In this note, we show that, for all $n\ge 2$, the number of distinct rooted binary phylogenetic $X$-trees displayed by a binary tree-child network $\mathcal{N}$ on $X$ with $n$ leaves is at most $2^{n-1}-1$ and that this upper bound is sharp. Furthermore, if $\mathcal{N}$ displays exactly $2^{n-1}-1$ such trees, then exactly one rooted binary phylogenetic $X$-tree is displayed twice, and this tree can be canonically found by iteratively replacing a reticulated cherry with a cherry.
