---
title: Temporal Backtracking Search for Test-time Generative Video Reasoning
source: arxiv
title_zh: 用于Test-time Generative Video Reasoning的Temporal Backtracking Search
abstract_zh: "While test-time scaling has revolutionized reasoning in large language 模型s，generative video reasonin。\n\n核心做法：To unlock effective test-time scaling 用于 video 模型s，作者引入 Temporal Backtracking Search (TBS)，which shi。\n\n主要结果：We 表明 that searching over denoising steps cannot rescue logically flawed rollouts because spatial tr。\n\n值得关注的点在于：Ultimately，TBS reveals that local reasoning competence 的 video 模型s far exceeds what single-shot roll。"
abstract_en: "While test-time scaling has revolutionized reasoning in large language models, generative video reasoning remains bottlenecked by a single-shot paradigm. We demonstrate that searching over denoising steps cannot rescue logically flawed rollouts because spatial trajectories commit early in the diffusion process. Root-level Best-of-N (BoN) sampling is similarly inefficient: reasoning errors cluster early in the temporal axis, and resampling blindly discards verified upstream progress. To unlock effective test-time scaling for video models, we introduce Temporal Backtracking Search (TBS), which shifts the search space to the temporal axis. TBS transforms video generation into an iterative generate-verify-restart loop via three core mechanisms: (1) variable-K conditioning to resume generation from arbitrary clean prefixes; (2) temporal process verification to localize failures and extract valid restart anchors; and (3) prefix-based search to reallocate compute toward extending correct trajectories rather than root resampling. Across algorithmic, navigation, and robotics domains, TBS Pareto-dominates matched-budget BoN. In a strict out-of-distribution setting where one-shot generation collapses (0.7% for BoN), TBS achieves 22.7%, with every solved episode stemming from a restarted branch. Ultimately, TBS reveals that the local reasoning competence of video models far exceeds what single-shot rollouts indicate, providing a scalable test-time framework to unlock it."
authors: "Sejoon Jun, Zheng Ding, Huangyuan Su, Weirui Ye, Yilun Du"
date: 2026-06-11
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.13861v1"
pdf: "https://arxiv.org/pdf/2606.13861v1"
score: 7.8
tldr: 围绕《用于Test-time Generative V...》；To unlock effective test-time sc...；We 表明 that searching over denoising...
tldr_cn: 围绕《用于Test-time Generative V...》；To unlock effective test-time sc...；We 表明 that searching over denoising...
tldr_en: "While test-time scaling has revolutionized reasoning in large language models, generative video reasoning remains bottlenecked by a single-shot paradigm. We demonstrate that searching over denoising steps cannot rescue logically flawed rollouts because spatial trajectories commit early in the diffusion process."
selection_source: fresh_fetch
vocabulary: ["generative | 生成式的 | IELTS", "denoising | 去噪 | 学术高频", "diffusion | 扩散 | 学术高频"]
motivation: While test-time scaling has revolutionized reasoning in large language 模型s，generative video reasonin。
method: 核心做法：To unlock effective test-time scaling 用于 video 模型s，作者引入 Temporal Backtracking Search (TBS)，which shi。
result: 主要结果：We 表明 that searching over denoising steps cannot rescue logically flawed rollouts because spatial tr。
conclusion: 值得关注的点在于：Ultimately，TBS reveals that local reasoning competence 的 video 模型s far exceeds what single-shot roll。
---

## 摘要
While test-time scaling has revolutionized reasoning in large language 模型s，generative video reasonin。

核心做法：To unlock effective test-time scaling 用于 video 模型s，作者引入 Temporal Backtracking Search (TBS)，which shi。

主要结果：We 表明 that searching over denoising steps cannot rescue logically flawed rollouts because spatial tr。

值得关注的点在于：Ultimately，TBS reveals that local reasoning competence 的 video 模型s far exceeds what single-shot roll。

## Abstract
While test-time scaling has revolutionized reasoning in large language models, generative video reasoning remains bottlenecked by a single-shot paradigm. We demonstrate that searching over denoising steps cannot rescue logically flawed rollouts because spatial trajectories commit early in the diffusion process. Root-level Best-of-N (BoN) sampling is similarly inefficient: reasoning errors cluster early in the temporal axis, and resampling blindly discards verified upstream progress. To unlock effective test-time scaling for video models, we introduce Temporal Backtracking Search (TBS), which shifts the search space to the temporal axis. TBS transforms video generation into an iterative generate-verify-restart loop via three core mechanisms: (1) variable-K conditioning to resume generation from arbitrary clean prefixes; (2) temporal process verification to localize failures and extract valid restart anchors; and (3) prefix-based search to reallocate compute toward extending correct trajectories rather than root resampling. Across algorithmic, navigation, and robotics domains, TBS Pareto-dominates matched-budget BoN. In a strict out-of-distribution setting where one-shot generation collapses (0.7% for BoN), TBS achieves 22.7%, with every solved episode stemming from a restarted branch. Ultimately, TBS reveals that the local reasoning competence of video models far exceeds what single-shot rollouts indicate, providing a scalable test-time framework to unlock it.

## 英语词汇
- **generative**：生成式的（IELTS）
- **denoising**：去噪（学术高频）
- **diffusion**：扩散（学术高频）
