---
title: Alternate loss functions and regression models that achieve robustness to outliers by modulating the learning rate
source: arxiv
title_zh: Alternate loss functions与regression 模型s that achieve robustness to outliers by modulating learning rate
abstract_zh: "Most real-world datasets used 用于 training supervised learning 模型s are contaminated 结合 noisy data 与 o。\n\n核心做法：This paper proposes new approach 用于 achieving robustness where learning rate is modulated by factor。\n\n主要结果：In this approach reduction 的 learning rate is shown to be 达到d by 使用 alternate loss functions that ar。\n\n值得关注的点在于：Highly 向量ized robust parameter update formulae that take advantage 的 modern GPUs 用于 both stochastic。"
abstract_en: "Most real-world datasets used for training supervised learning models are contaminated with noisy data and outliers leading to large prediction errors. This paper proposes a new approach for achieving robustness where the learning rate is modulated by a factor that is sensitive to outliers. In this approach a reduction of the learning rate is shown to be achieved by using alternate loss functions that are infinitely differentiable, strictly convex or quasiconvex and more closely approximate the absolute error than Huber and log-cosh losses. A comparison of the performance of regression models trained with different loss functions on a wide variety of benchmarks and datasets is presented to demonstrate the superior performance of the Square Root Loss (SRL) and Smooth Mean Absolute Error (SMAE) losses proposed in this paper. Two new robust linear regression models are presented. Highly vectorized robust parameter update formulae that take advantage of modern GPUs for both stochastic and batch gradient descent are presented."
authors: "Mathew Mithra Noel, Arindam Banerjee, Yug D. Oswal, Geraldine Bessie Amali D, Venkataraman Muthiah-Nakarajan"
date: 2026-06-20
journal: cs.LG
source_link: "https://arxiv.org/pdf/2606.22068v2"
pdf: "https://arxiv.org/pdf/2606.22068v2"
score: 7.699999999999999
tldr: 围绕《Alternate loss functions...》；This paper proposes new approach...；In this approach reduction 的 learnin...
tldr_cn: 围绕《Alternate loss functions...》；This paper proposes new approach...；In this approach reduction 的 learnin...
tldr_en: Most real-world datasets used for training supervised learning models are contaminated with noisy data and outliers leading to large prediction errors. This paper proposes a new approach for achieving robustness where the learning rate is modulated by a factor that is sensitive to outliers.
selection_source: fresh_fetch
vocabulary: ["robustness | 鲁棒性 | IELTS"]
motivation: Most real-world datasets used 用于 training supervised learning 模型s are contaminated 结合 noisy data 与 o。
method: 核心做法：This paper proposes new approach 用于 achieving robustness where learning rate is modulated by factor。
result: 主要结果：In this approach reduction 的 learning rate is shown to be 达到d by 使用 alternate loss functions that ar。
conclusion: 值得关注的点在于：Highly 向量ized robust parameter update formulae that take advantage 的 modern GPUs 用于 both stochastic。
---

## 摘要
Most real-world datasets used 用于 training supervised learning 模型s are contaminated 结合 noisy data 与 o。

核心做法：This paper proposes new approach 用于 achieving robustness where learning rate is modulated by factor。

主要结果：In this approach reduction 的 learning rate is shown to be 达到d by 使用 alternate loss functions that ar。

值得关注的点在于：Highly 向量ized robust parameter update formulae that take advantage 的 modern GPUs 用于 both stochastic。

## Abstract
Most real-world datasets used for training supervised learning models are contaminated with noisy data and outliers leading to large prediction errors. This paper proposes a new approach for achieving robustness where the learning rate is modulated by a factor that is sensitive to outliers. In this approach a reduction of the learning rate is shown to be achieved by using alternate loss functions that are infinitely differentiable, strictly convex or quasiconvex and more closely approximate the absolute error than Huber and log-cosh losses. A comparison of the performance of regression models trained with different loss functions on a wide variety of benchmarks and datasets is presented to demonstrate the superior performance of the Square Root Loss (SRL) and Smooth Mean Absolute Error (SMAE) losses proposed in this paper. Two new robust linear regression models are presented. Highly vectorized robust parameter update formulae that take advantage of modern GPUs for both stochastic and batch gradient descent are presented.

## 英语词汇
- **robustness**：鲁棒性（IELTS）
