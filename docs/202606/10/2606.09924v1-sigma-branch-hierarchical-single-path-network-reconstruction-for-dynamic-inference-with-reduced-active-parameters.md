---
title: "Sigma-Branch: Hierarchical Single-Path Network Reconstruction for Dynamic Inference with Reduced Active Parameters"
source: arxiv
title_zh: 用于Dynamic Inference 结合 Reduced Active Parameters的Sigma-Branch：Hierarchical Single-Path Network Reconstruction
abstract_zh: "Deploying deep neural 网络s on memory-constrained edge accelerators is bottlenecked by per-inference o。\n\n核心做法：作者提出 Sigma-Branch (SigmaB)，框架 that restructures pretrained dense 网络为 hierarchical binary tree compos。\n\n主要结果：cross-modal evaluation，spanning 2D vision 与 3D point-cloud backbones，substantiates 框架-level claim th。\n\n值得关注的点在于：Pretrained weights are distributed across tree 通过 activation-based spherical k-means clustering，whic。"
abstract_en: "Deploying deep neural networks on memory-constrained edge accelerators is bottlenecked by per-inference off-chip weight transfer rather than computation: the dense network cannot be retained on-chip, and every parameter must be loaded for every input. Existing model compression reduces this transfer only at the cost of permanent capacity loss. We propose Sigma-Branch (SigmaB), a framework that restructures a pretrained dense network into a hierarchical binary tree composed of a shared backbone, hierarchical routers, and specialized leaves. Pretrained weights are distributed across the tree via activation-based spherical k-means clustering, which jointly initializes router weights and per-branch channel allocations; soft-routing fine-tuning then aligns each leaf with its routed input subset. At inference, the resulting network executes only a single root-to-leaf path, reducing the active-parameter footprint while storing the complete dense parameter set in memory. Across CIFAR-100 / ResNet-50, ImageNet-1K / ResNet-50, and ModelNet40 / PointNet++, SigmaB-Net reduces per-inference active parameters by 58-60% while remaining within 1.72 percentage points (pp) of the dense baseline Top-1. At comparable ImageNet-1K Top-1, the active-parameter reduction exceeds static structured pruning (FPGM, HRank) by 14-23 pp. The cross-modal evaluation, spanning 2D vision and 3D point-cloud backbones, substantiates a framework-level claim that decouples per-inference memory traffic from the total parameter count."
authors: "Kohga Tanaka, Hiroaki Nishi"
date: 2026-06-07
journal: cs.LG
source_link: "https://arxiv.org/pdf/2606.09924v1"
pdf: "https://arxiv.org/pdf/2606.09924v1"
score: 8.0
tldr: 围绕《用于Dynamic Inference 结合 R...》；作者提出 Sigma-Branch (SigmaB)，框架 th...；cross-modal evaluation，spanning 2D v...
tldr_cn: 围绕《用于Dynamic Inference 结合 R...》；作者提出 Sigma-Branch (SigmaB)，框架 th...；cross-modal evaluation，spanning 2D v...
tldr_en: "Deploying deep neural networks on memory-constrained edge accelerators is bottlenecked by per-inference off-chip weight transfer rather than computation: the dense network cannot be retained on-chip, and every parameter must be loaded for every input. Existing model compression reduces this transfer only at the cost of permanent capacity loss."
selection_source: fresh_fetch
vocabulary: ["reconstruction | 重建 | CET6", "fine-tuning | 微调 | 学术高频", "inference | 推理 | CET6"]
motivation: Deploying deep neural 网络s on memory-constrained edge accelerators is bottlenecked by per-inference o。
method: 核心做法：作者提出 Sigma-Branch (SigmaB)，框架 that restructures pretrained dense 网络为 hierarchical binary tree compos。
result: 主要结果：cross-modal evaluation，spanning 2D vision 与 3D point-cloud backbones，substantiates 框架-level claim th。
conclusion: 值得关注的点在于：Pretrained weights are distributed across tree 通过 activation-based spherical k-means clustering，whic。
---

## 摘要
Deploying deep neural 网络s on memory-constrained edge accelerators is bottlenecked by per-inference o。

核心做法：作者提出 Sigma-Branch (SigmaB)，框架 that restructures pretrained dense 网络为 hierarchical binary tree compos。

主要结果：cross-modal evaluation，spanning 2D vision 与 3D point-cloud backbones，substantiates 框架-level claim th。

值得关注的点在于：Pretrained weights are distributed across tree 通过 activation-based spherical k-means clustering，whic。

## Abstract
Deploying deep neural networks on memory-constrained edge accelerators is bottlenecked by per-inference off-chip weight transfer rather than computation: the dense network cannot be retained on-chip, and every parameter must be loaded for every input. Existing model compression reduces this transfer only at the cost of permanent capacity loss. We propose Sigma-Branch (SigmaB), a framework that restructures a pretrained dense network into a hierarchical binary tree composed of a shared backbone, hierarchical routers, and specialized leaves. Pretrained weights are distributed across the tree via activation-based spherical k-means clustering, which jointly initializes router weights and per-branch channel allocations; soft-routing fine-tuning then aligns each leaf with its routed input subset. At inference, the resulting network executes only a single root-to-leaf path, reducing the active-parameter footprint while storing the complete dense parameter set in memory. Across CIFAR-100 / ResNet-50, ImageNet-1K / ResNet-50, and ModelNet40 / PointNet++, SigmaB-Net reduces per-inference active parameters by 58-60% while remaining within 1.72 percentage points (pp) of the dense baseline Top-1. At comparable ImageNet-1K Top-1, the active-parameter reduction exceeds static structured pruning (FPGM, HRank) by 14-23 pp. The cross-modal evaluation, spanning 2D vision and 3D point-cloud backbones, substantiates a framework-level claim that decouples per-inference memory traffic from the total parameter count.

## 英语词汇
- **reconstruction**：重建（CET6）
- **fine-tuning**：微调（学术高频）
- **inference**：推理（CET6）
