---
title: Formalizing and Mitigating Structural Distortion in LLM Attention for Graph Reasoning
source: arxiv
title_zh: 用于Graph Reasoning的Formalizing与Mitigating Structural Distortion in LLM Attention
abstract_zh: "Large Language 模型s (LLMs) have shown promise 用于 reasoning over Text-Attributed Graphs (TAGs)。\n\n核心做法：Motivated by this analysis，作者提出 \\textbf{G}raph-\\textbf{ }ligned \\textbf{L}anguage \\textbf{ }ttention。\n\n主要结果：In this work，we show \\textit{how} rotary positional embeddings turn graph linearization 为 bandwidth-。\n\n值得关注的点在于：Across TAG benchmarks，GaLA improves 性能结合 negligible overhead，demonstrating that distortion is correc。"
abstract_en: "Large Language Models (LLMs) have shown promise for reasoning over Text-Attributed Graphs (TAGs). However, applying LLMs to graphs requires linearizing their structure into sequences, introducing distortion rooted in the graph bandwidth problem. While this distortion has been shown to degrade performance, it is often attributed to prompt design or model scale, leaving the underlying mechanism unclear. In this work, we show \\textit{how} rotary positional embeddings turn graph linearization into bandwidth-dependent attention decay, suppressing attention between graph-adjacent nodes that are forced far apart in the serialized sequence. This shifts the focus of LLM-based graph reasoning from prompt engineering and scaling toward correcting attention misalignment. Motivated by this analysis, we propose \\textbf{G}raph-\\textbf{a}ligned \\textbf{L}anguage \\textbf{A}ttention (\\textbf{GaLA}), a lightweight, inference-time modification for LLMs. GaLA biases attention toward graph-adjacent nodes while preserving the LLM's sequential inductive biases. Across TAG benchmarks, GaLA improves performance with negligible overhead, demonstrating that distortion is a correctable bottleneck in LLM-based graph reasoning."
authors: "Donald Loveland, Puja Trivedi, Ari Weinstein, Edward W Huang, Danai Koutra"
date: 2026-06-14
journal: cs.LG
source_link: "https://arxiv.org/pdf/2606.15633v2"
pdf: "https://arxiv.org/pdf/2606.15633v2"
score: 7.8
tldr: "围绕《用于Graph Reasoning的Formal...》；Motivated by this analysis，作者提出...；In this work，we show \\textit{how} ro..."
tldr_cn: "围绕《用于Graph Reasoning的Formal...》；Motivated by this analysis，作者提出...；In this work，we show \\textit{how} ro..."
tldr_en: "Large Language Models (LLMs) have shown promise for reasoning over Text-Attributed Graphs (TAGs). However, applying LLMs to graphs requires linearizing their structure into sequences, introducing distortion rooted in the graph bandwidth problem."
selection_source: fresh_fetch
vocabulary: ["inference | 推理 | CET6"]
motivation: Large Language 模型s (LLMs) have shown promise 用于 reasoning over Text-Attributed Graphs (TAGs)。
method: "核心做法：Motivated by this analysis，作者提出 \\textbf{G}raph-\\textbf{ }ligned \\textbf{L}anguage \\textbf{ }ttention。"
result: "主要结果：In this work，we show \\textit{how} rotary positional embeddings turn graph linearization 为 bandwidth-。"
conclusion: 值得关注的点在于：Across TAG benchmarks，GaLA improves 性能结合 negligible overhead，demonstrating that distortion is correc。
---

## 摘要
Large Language 模型s (LLMs) have shown promise 用于 reasoning over Text-Attributed Graphs (TAGs)。

核心做法：Motivated by this analysis，作者提出 \textbf{G}raph-\textbf{ }ligned \textbf{L}anguage \textbf{ }ttention。

主要结果：In this work，we show \textit{how} rotary positional embeddings turn graph linearization 为 bandwidth-。

值得关注的点在于：Across TAG benchmarks，GaLA improves 性能结合 negligible overhead，demonstrating that distortion is correc。

## Abstract
Large Language Models (LLMs) have shown promise for reasoning over Text-Attributed Graphs (TAGs). However, applying LLMs to graphs requires linearizing their structure into sequences, introducing distortion rooted in the graph bandwidth problem. While this distortion has been shown to degrade performance, it is often attributed to prompt design or model scale, leaving the underlying mechanism unclear. In this work, we show \textit{how} rotary positional embeddings turn graph linearization into bandwidth-dependent attention decay, suppressing attention between graph-adjacent nodes that are forced far apart in the serialized sequence. This shifts the focus of LLM-based graph reasoning from prompt engineering and scaling toward correcting attention misalignment. Motivated by this analysis, we propose \textbf{G}raph-\textbf{a}ligned \textbf{L}anguage \textbf{A}ttention (\textbf{GaLA}), a lightweight, inference-time modification for LLMs. GaLA biases attention toward graph-adjacent nodes while preserving the LLM's sequential inductive biases. Across TAG benchmarks, GaLA improves performance with negligible overhead, demonstrating that distortion is a correctable bottleneck in LLM-based graph reasoning.

## 英语词汇
- **inference**：推理（CET6）
