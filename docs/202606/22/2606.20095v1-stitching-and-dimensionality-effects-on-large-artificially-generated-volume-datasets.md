---
title: Stitching and dimensionality effects on large artificially generated volume datasets
source: arxiv
title_zh: Stitching与dimensionality effects on large artificially generated volume 数据集s
abstract_zh: "Generating large images 通过 deep learning requires patching input data to accommodate hardware memory。\n\n核心做法：While these artifacts are known to affect segmentation tasks，their impact on generative 模型s 用于 style。\n\n主要结果：Our key findings reveal that：(1) FID scores fail to detect subtle stitching artifacts that significa。\n\n值得关注的点在于：These results 表明 that maximizing generative 模型性能 on large scientific datasets requires careful consi。"
abstract_en: "Generating large images via deep learning requires patching input data to accommodate hardware memory limitations, then assembling output patches, a process that can introduce stitching artifacts when neighboring patches do not align at borders. While these artifacts are known to affect segmentation tasks, their impact on generative models for style-transfer remains poorly understood. We investigated three stitching approaches and two patch dimensionalities (2D vs 3D) using cycleGAN models trained on cryo-electron microscopy datasets. We evaluated both perceptual quality and performance on downstream mitochondria segmentation. Our key findings reveal that: (1) FID scores fail to detect subtle stitching artifacts that significantly impact downstream segmentation performance, (2) 3D models with artifact-free stitching marginally outperform 2D models on downstream tasks, though the improvement barely justifies the computational cost, and (3) 2D models train more stably due to larger batch sizes. Additionally, we demonstrate that ensembling predictions from three orthogonal directions can improve low-quality volumes but provides no benefit for high-quality outputs. These results demonstrate that maximizing generative model performance on large scientific datasets requires careful consideration and mitigation of stitching artifacts, and that perceptual metrics alone are insufficient for evaluating domain adaptation quality in biomedical imaging."
authors: "Lucas von Chamier, Jan Philipp Albrecht, Dagmar Kainmüller"
date: 2026-06-18
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.20095v1"
pdf: "https://arxiv.org/pdf/2606.20095v1"
score: 6.1499999999999995
tldr: 围绕《Stitching与dimensionality...》；While these artifacts are known...；Our key findings reveal that：(1) FID...
tldr_cn: 围绕《Stitching与dimensionality...》；While these artifacts are known...；Our key findings reveal that：(1) FID...
tldr_en: "Generating large images via deep learning requires patching input data to accommodate hardware memory limitations, then assembling output patches, a process that can introduce stitching artifacts when neighboring patches do not align at borders. While these artifacts are known to affect segmentation tasks, their impact on generative models for style-transfer remains poorly understood."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "downstream | 下游任务 | 学术高频", "generative | 生成式的 | IELTS"]
motivation: Generating large images 通过 deep learning requires patching input data to accommodate hardware memory。
method: 核心做法：While these artifacts are known to affect segmentation tasks，their impact on generative 模型s 用于 style。
result: 主要结果：Our key findings reveal that：(1) FID scores fail to detect subtle stitching artifacts that significa。
conclusion: 值得关注的点在于：These results 表明 that maximizing generative 模型性能 on large scientific datasets requires careful consi。
---

## 摘要
Generating large images 通过 deep learning requires patching input data to accommodate hardware memory。

核心做法：While these artifacts are known to affect segmentation tasks，their impact on generative 模型s 用于 style。

主要结果：Our key findings reveal that：(1) FID scores fail to detect subtle stitching artifacts that significa。

值得关注的点在于：These results 表明 that maximizing generative 模型性能 on large scientific datasets requires careful consi。

## Abstract
Generating large images via deep learning requires patching input data to accommodate hardware memory limitations, then assembling output patches, a process that can introduce stitching artifacts when neighboring patches do not align at borders. While these artifacts are known to affect segmentation tasks, their impact on generative models for style-transfer remains poorly understood. We investigated three stitching approaches and two patch dimensionalities (2D vs 3D) using cycleGAN models trained on cryo-electron microscopy datasets. We evaluated both perceptual quality and performance on downstream mitochondria segmentation. Our key findings reveal that: (1) FID scores fail to detect subtle stitching artifacts that significantly impact downstream segmentation performance, (2) 3D models with artifact-free stitching marginally outperform 2D models on downstream tasks, though the improvement barely justifies the computational cost, and (3) 2D models train more stably due to larger batch sizes. Additionally, we demonstrate that ensembling predictions from three orthogonal directions can improve low-quality volumes but provides no benefit for high-quality outputs. These results demonstrate that maximizing generative model performance on large scientific datasets requires careful consideration and mitigation of stitching artifacts, and that perceptual metrics alone are insufficient for evaluating domain adaptation quality in biomedical imaging.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **downstream**：下游任务（学术高频）
- **generative**：生成式的（IELTS）
