---
title: "Threshold Minimum Cut with Terminal Quotas: Logarithmic and Planar Approximation Algorithms"
source: arxiv
title_zh: 结合Terminal Quotas：Logarithmic与Planar Approximation Algorithms的Threshold Minimum Cut
abstract_zh: "这篇论文研究 threshold minimum cut problems 结合 distinguished root vertex，set 的 terminals，与 quota。\n\n核心做法：In threshold minimum edge cut problem (\\TMEC)，goal is to find minimum-cost edge cut that disconnects。\n\n主要结果：In threshold minimum node cut problem (\\TMNC)，goal is to delete minimum-cost set 的 nonterminal，nonro。\n\n值得关注的点在于：results separate exact-quota guarantees from bicriteria small-set-expansion-type guarantees 与 identi。"
abstract_en: "We study threshold minimum cut problems with a distinguished root vertex, a set of terminals, and a quota. In the threshold minimum edge cut problem (\\TMEC), the goal is to find a minimum-cost edge cut that disconnects at least $k$ terminals from the root. In the threshold minimum node cut problem (\\TMNC), the goal is to delete a minimum-cost set of nonterminal, nonroot vertices so that at least $k$ terminals become disconnected from the root. We prove three approximation guarantees. First, undirected general-graph \\TMEC{} admits a randomized polynomial-time expected $O(\\log n)$ approximation via a Räcke-style cut-dominating tree decomposition and an exact dynamic program on trees. A standard repetition argument gives the same asymptotic ratio with high probability. Second, planar \\TMEC{} admits a factor-$2$ approximation by reducing the threshold condition to planar weighted balanced cut. Third, bounded-degree planar \\TMNC{} admits a $2Δ$-approximation, where $Δ$ is the maximum degree of a deletable vertex, by reducing the node-cost problem to the planar edge-cut problem on the same graph. The results separate exact-quota guarantees from bicriteria small-set-expansion-type guarantees and identify the unbounded-degree planar node-cut case as the main remaining obstacle."
authors: Qi Duan
date: 2026-06-13
journal: cs.DS
source_link: "https://arxiv.org/pdf/2606.15324v1"
pdf: "https://arxiv.org/pdf/2606.15324v1"
score: 8.549999999999999
tldr: 围绕《结合Terminal Quotas：Logari...》；In threshold minimum edge cut pr...；In threshold minimum node cut proble...
tldr_cn: 围绕《结合Terminal Quotas：Logari...》；In threshold minimum edge cut pr...；In threshold minimum node cut proble...
tldr_en: "We study threshold minimum cut problems with a distinguished root vertex, a set of terminals, and a quota. In the threshold minimum edge cut problem (\\TMEC), the goal is to find a minimum-cost edge cut that disconnects at least $k$ terminals from the root."
selection_source: fresh_fetch
motivation: 这篇论文研究 threshold minimum cut problems 结合 distinguished root vertex，set 的 terminals，与 quota。
method: 核心做法：In threshold minimum edge cut problem (\TMEC)，goal is to find minimum-cost edge cut that disconnects。
result: 主要结果：In threshold minimum node cut problem (\TMNC)，goal is to delete minimum-cost set 的 nonterminal，nonro。
conclusion: 值得关注的点在于：results separate exact-quota guarantees from bicriteria small-set-expansion-type guarantees 与 identi。
---

## 摘要
这篇论文研究 threshold minimum cut problems 结合 distinguished root vertex，set 的 terminals，与 quota。

核心做法：In threshold minimum edge cut problem (\TMEC)，goal is to find minimum-cost edge cut that disconnects。

主要结果：In threshold minimum node cut problem (\TMNC)，goal is to delete minimum-cost set 的 nonterminal，nonro。

值得关注的点在于：results separate exact-quota guarantees from bicriteria small-set-expansion-type guarantees 与 identi。

## Abstract
We study threshold minimum cut problems with a distinguished root vertex, a set of terminals, and a quota. In the threshold minimum edge cut problem (\TMEC), the goal is to find a minimum-cost edge cut that disconnects at least $k$ terminals from the root. In the threshold minimum node cut problem (\TMNC), the goal is to delete a minimum-cost set of nonterminal, nonroot vertices so that at least $k$ terminals become disconnected from the root. We prove three approximation guarantees. First, undirected general-graph \TMEC{} admits a randomized polynomial-time expected $O(\log n)$ approximation via a Räcke-style cut-dominating tree decomposition and an exact dynamic program on trees. A standard repetition argument gives the same asymptotic ratio with high probability. Second, planar \TMEC{} admits a factor-$2$ approximation by reducing the threshold condition to planar weighted balanced cut. Third, bounded-degree planar \TMNC{} admits a $2Δ$-approximation, where $Δ$ is the maximum degree of a deletable vertex, by reducing the node-cost problem to the planar edge-cut problem on the same graph. The results separate exact-quota guarantees from bicriteria small-set-expansion-type guarantees and identify the unbounded-degree planar node-cut case as the main remaining obstacle.
