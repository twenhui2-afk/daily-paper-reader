Title: Finite-Dimensional Type I von Neumann Algebras in PyTorch: A GPU-Accelerated Framework for Random Block-Diagonal Operators

URL Source: https://arxiv.org/pdf/2606.15882v1

Published Time: Tue, 16 Jun 2026 01:41:07 GMT

Number of Pages: 18

Markdown Content:
# Finite-Dimensional Type I von Neumann Algebras in PyTorch: A GPU-Accelerated Framework for Random Block-Diagonal Operators 

Irina Nikolaeva 1 Andrej Novikov 2

June 16, 2026 

Abstract 

We present torch vn algebra , an open-source Python library built on PyTorch for numerical experiments with finite-dimensional Type I von Neumann algebras (direct sums of matrix algebras). The library provides: 

• a compact batched tensor representation ( B, C, k max , k max ) that handles both Monte Carlo samples and multiple direct summands; 

• lazy evaluation of operators to avoid unnecessary memory allocation; 

• generation of random operators with arbitrary eigenvalue distributions (user-provided samplers) and various unitary ensembles (Haar, SU( n), COE, CSE, diagonal phases); 

• functional calculus via SVD (absolute value, square root, inverse, entropy) and a hybrid method for extreme eigenvalues (exact diagonalisation for kmax ≤ 256, otherwise power iteration); 

• three trace functionals (blunt, normalised subspace trace, and the von Neu-mann tracial state); 

• GPU-accelerated batched linear algebra for moderate-scale Monte Carlo stud-ies (e.g., 2 × 10 4 samples of 100 × 100 operators). The library is validated against analytical expectations (Haar moments, trace prop-erties). Performance benchmarks on a Tesla P100 GPU are presented and discussed. Limitations and future work are outlined. The code is open-source. 

# Contents 

1 Introduction 22 Mathematical background 23 Implementation 34 Performance benchmarks and validation 3

4.1 Validation . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 34.2 Benchmark setup and speedup results . . . . . . . . . . . . . . . . . . . . . 41

> arXiv:2606.15882v1 [cs.MS] 14 Jun 2026

5 Monte Carlo experiments on trace inequalities 4

5.1 Experiment 1 (positive X, positive Y , with random orthogonal U ) . . . . . 55.2 Experiment 2 (non-Hermitian X, non-Hermitian Y ) . . . . . . . . . . . . . 65.3 Experiment 3 (self-adjoint X, positive Y ) . . . . . . . . . . . . . . . . . . . 8

6 Limitations and future work 11 7 Conclusion 11 A Complete set of speedup heatmaps 14 

# 1 Introduction 

Finite-dimensional Type I von Neumann algebras, 

M =

> C

M

> c=1

Mnc (C),

naturally appear in quantum mechanics with superselection rules, decoherence, and in random matrix theory when symmetries or constraints force operators into block-diagonal form. Monte Carlo simulations of such systems – averaging over many random operators with controlled spectral properties – require efficient numerical tools that can exploit modern hardware. Existing libraries (NumPy/SciPy, QuTiP, Qiskit) lack native support for block-diagonal (direct-sum) structures, do not allow arbitrary eigenvalue distributions, and rarely provide GPU parallelism. 

torch vn algebra fills this gap by combining a compact tensor representation that batches both Monte Carlo samples and direct summands with padding to a common 

kmax ; lazy evaluation (operators are built from generators and materialised only when needed); flexible random operators (any eigenvalue distribution via a user callable) with several unitary ensembles (Haar, SU( n), COE, CSE, random diagonal phases); batched functional calculus with hybrid eigenvalue extraction (exact diagonalisation for kc ≤ 256, power iteration for larger); three trace functionals Tr blunt , Tr norm and the von Neumann tracial state τvN ; and full integration with PyTorch’s GPU backend. The library builds upon the theoretical framework developed in [11, 12], where the Michelson contrast was generalised to operators in von Neumann algebras. The present work provides a scalable GPU implementation of these concepts, enabling Monte Carlo studies that were previously infeasible. The paper is organised as follows. Section 2 recalls necessary definitions. Section 3 describes the implementation. Section 4 presents performance benchmarks and validation. Section 5 gives Monte Carlo experiments. Section 6 discusses limitations and future work. Section 7 concludes. 

# 2 Mathematical background 

A finite-dimensional Type I von Neumann algebra is M = LCc=1 Mnc (C) acting on 

H = LCc=1 Ckc with kc ≤ nc. Operators are block-diagonal: A = L 

> c

Ac. Three trace 2functionals are relevant: Tr blunt (A) = 

> C

X

> c=1

Tr( Ac), Tr norm (A) = 

> C

X

> c=1

1

kc

Tr( A(kc) 

> c

), τvN (A) = 1

C

> C

X

> c=1

1

kc

Tr( A(kc) 

> c

),

where A(kc) 

> c

is the restriction of Ac to the kc-dimensional subspace. τvN is a faithful normal tracial state. For a density matrix ρ (ρ ≥ 0, Tr ρ = 1), the von Neumann entropy is S(ρ) = − Tr( ρ log ρ). For a positive operator A, define its Michelson contrast as ∆( A) = λmax (A) − λmin (A)

λmax (A) + λmin (A) .

The theory of noncommutative integration associates to a von Neumann algebra M

equipped with a faithful normal trace τ a family of noncommutative Lp-spaces. In the finite-dimensional Type I case, the L1-space is simply the space of trace-class operators with norm ∥A∥1 = τ (|A|). For a positive operator X affiliated with M , one can define its 

L1-norm as τ (X) when finite [7]. 

# 3 Implementation 

Operators are stored as a 4-D tensor ( B, C, k max , k max ) with active block of size kc × kc in the top-left corner. The Operator class uses lazy generation: the matrix is materialised only when accessed. The auxiliary class HilbertSpace provides inner product, norm, and orthonormal bases (standard, random, Haar), supporting embedding and restriction of vectors/operators. Random operator generation is performed via operator from eigenvalues . The user supplies a callable eigenvalue sampler(dim) . For each channel, eigenvalues are sampled, a random unitary (from chosen ensemble) is generated, and Ac = U diag( λ)U ∗ is formed. Properties (self-adjointness, positivity, etc.) are inferred; forced properties trigger valida-tion. For self-adjoint operators, λmax and λmin are obtained via exact diagonalisation ( torch.linalg.eigva 

for kc ≤ 256; for larger dimensions a power iteration with shift is used (by default 100 iterations with early stopping at 10 −8; the user may increase the number of iterations for poorly conditioned spectra). For positive operators, |A|, √A, A−1 and entropy are computed via batched SVD. The three traces are implemented as Tr blunt , Tr norm 

(normalised by kc), and tau vN .

# 4 Performance benchmarks and validation 

4.1 Validation 

We validate the library against analytical expectations. For random unitary matrices generated with haar , the expected value E[|U11 |2] = 1 /n was tested for n = 2 , 4, 8, 16 , 32 using 10 000 samples. Table 1 shows the relative errors; all errors are below 3 .2%, with the largest deviation at n = 8. For a diagonal matrix with eigenvalues [1 .0, 0.99 , 0.98] (gap 0 .01), the power iteration required 491 iterations to reach 10 −8 accuracy, compared to 30 iterations for a well-separated spectrum [2 , 1, 0.5]. This demonstrates expected sensitivity to the spectral gap. 3Table 1: Relative error for E[|U11 |2] vs 1 /n (10 000 samples). 

n Expected Relative error 2 0.5 1.73 × 10 −4

4 0.25 6.53 × 10 −3

8 0.125 1.58 × 10 −2

16 0.0625 1.49 × 10 −2

32 0.03125 5.52 × 10 −3

We generated 1000 random 100 × 100 positive matrices (eigenvalues uniform in [0 , 1]). For each, we computed B = √A via SVD and verified ∥B2 − A∥F /∥A∥F . The mean relative error was 4 .54 × 10 −8, with maximum 4 .94 × 10 −8, confirming the accuracy of the SVD implementation. 

4.2 Benchmark setup and speedup results 

All benchmarks were run on a GPU (NVIDIA Tesla P100, 16 GB) and a CPU (Intel Xeon E5-2650 v4 @ 2.2 GHz, 12 cores). CPU code used PyTorch with torch.set num threads(1) 

to ensure single-thread execution. Batch size was fixed at 1000 for configurations where memory allowed; for ( kmax , C ) with k2max · C · 1000 · 4 > 12 GB, batch size was reduced adaptively (see Table 2). Speedup is defined as tCPU /t GPU .Table 2: Actual batch sizes used for inverse operation benchmark. 

kmax C Batch size 2–512 1–1024 1000 (except 1024x1024: 10) 1024 1024 10 Table 3 shows speedup for the inverse operation. For large kmax and many channels, GPU outperforms CPU by factors up to 30. Table 3: Mean speedup (CPU/GPU) for inverse operation. 

kmax C Speedup 16 64 14.4 32 64 32.0 128 64 9.5 256 64 7.4 512 64 5.8 1024 64 9.6 

# 5 Monte Carlo experiments on trace inequalities 

We perform three Monte Carlo experiments that directly mirror the original NumPy code and illustrate the library’s capabilities. All experiments are run on an NVIDIA Tesla 4P100 GPU using the experiment.py script. For each matrix dimension kmax ∈ { 2, 16 }

and number of channels C ∈ { 1, 2, 16 , 32 } we generate 20 000 random operator pairs, with batch size automatically adapted to GPU memory. Operators are constructed via the spectral theorem with eigenvalues drawn from samplers that produce a uniform Michelson contrast in [0 , 1], and then conjugated with independent Haar-random unitary matrices, yielding Hermitian (or positive) operators. The figures below show scatter plots (2D projections and 3D visualisations) of z versus the relevant contrasts. All generated plots are available in the repository; a selection is presented here. 

5.1 Experiment 1 (positive X, positive Y , with random orthog-onal U )

Both X and Y are positive operators. A random orthogonal matrix U is generated independently for each channel and batch element. The quantity of interest is 

z = | Tr( XU Y )| − Tr( XY ).

For positive operators one expects z ≤ 0 (a consequence of the inequality | Tr( XU Y )| ≤ 

Tr( XY )). The contrasts ∆( X) and ∆( Y ) are computed directly from the eigenvalues of 

X and Y . In all generated samples we observe z ≤ 0 within numerical precision. Listing 1: Experiment 1 implementation  

> 1

def experiment1 ( alg , batch_size , num_batches , device ) :  

> 2

pos_sampler = positive_sampler ( batch_size , device )  

> 3

deltaX_list , deltaY_list , z_list = [] , [] , []  

> 4

for _ in range ( num_batches ) :  

> 5

X = alg . operator_from_eigenvalues ( pos_sampler , batch_size = batch_size ,  

> 6

force_positive = True , force_self_adjoint = True )  

> 7

Y = alg . operator_from_eigenvalues ( pos_sampler , batch_size = batch_size ,  

> 8

force_positive = True , force_self_adjoint = True )  

> 9

lmaxX , lminX = X . lambda_max , X . lambda_min  

> 10

deltaX = ( lmaxX - lminX ) / ( lmaxX + lminX )  

> 11

lmaxY , lminY = Y . lambda_max , Y . lambda_min  

> 12

deltaY = ( lmaxY - lminY ) / ( lmaxY + lminY )  

> 13

X_mat , Y_mat = X . matrix . clone () , Y . matrix . clone ()  

> 14

batch_z = torch . zeros ( batch_size , device = device )  

> 15

for c in range ( alg . C ) :  

> 16

k_c = alg . k_factors [ c ]  

> 17

if k_c == 0: continue  

> 18

Xc = X_mat [: , c , : k_c , : k_c ]  

> 19

Yc = Y_mat [: , c , : k_c , : k_c ]  

> 20

U = torch . stack ([ alg . random_unitary ( k_c , measure = ’ haar ’)  

> 21

for _ in range ( batch_size ) ]) . to ( device )  

> 22

XY = Xc @ U @ Yc  

> 23

term1 = torch . abs ( torch . diagonal ( XY , dim1 = -2 , dim2 = -1) . sum ( -1) )  

> 24

term2 = torch . diagonal ( Xc @ Yc , dim1 = -2 , dim2 = -1) . sum ( -1)  

> 25

batch_z += ( term1 - term2 )  

> 26

z = batch_z / alg . C 

527 deltaX_list . append ( deltaX . cpu () . numpy () )  

> 28

deltaY_list . append ( deltaY . cpu () . numpy () )  

> 29

z_list . append ( z . cpu () . numpy () )  

> 30

return ( np . concatenate ( deltaX_list ) , np . concatenate ( deltaY_list ) , np . concatenate ( z_list ) ) 

(a) kmax = 2 , C = 1 

(b) kmax = 2 , C = 2 

(c) kmax = 2 , C = 16 

Figure 1: Experiment 1: small dimension ( kmax = 2). 

5.2 Experiment 2 (non-Hermitian X, non-Hermitian Y )

Both X and Y are made non-Hermitian by multiplying positive Hermitian matrices with random orthogonal matrices. The quantity of interest is 

z = Tr( |XY |) − Tr( |X|| Y |).

The contrasts are computed from |X| and |Y |, i.e., from the singular values of X and Y .In our experiments z is always non-positive within numerical accuracy. Listing 2: Experiment 2 implementation  

> 1

def experiment2 ( alg , batch_size , num_batches , device ) : 

6(a) kmax = 16 , C = 1 

(b) kmax = 16 , C = 16 

(c) kmax = 16 , C = 32 

Figure 2: Experiment 1: intermediate dimension ( kmax = 16). 72 pos_sampler = positive_sampler ( batch_size , device )  

> 3

deltaX_list , deltaY_list , z_list = [] , [] , []  

> 4

for _ in range ( num_batches ) :  

> 5

X0 = alg . operator_from_eigenvalues ( pos_sampler , batch_size = batch_size ,  

> 6

force_positive = True , force_self_adjoint = True )  

> 7

Y0 = alg . operator_from_eigenvalues ( pos_sampler , batch_size = batch_size ,  

> 8

force_positive = True , force_self_adjoint = True )  

> 9

lmaxX0 , lminX0 = X0 . lambda_max , X0 . lambda_min  

> 10

deltaX = ( lmaxX0 - lminX0 ) / ( lmaxX0 + lminX0 )  

> 11

lmaxY0 , lminY0 = Y0 . lambda_max , Y0 . lambda_min  

> 12

deltaY = ( lmaxY0 - lminY0 ) / ( lmaxY0 + lminY0 )  

> 13

X_mat = apply_random_orthogonal ( X0 . matrix . clone () , alg )  

> 14

Y_mat = apply_random_orthogonal ( Y0 . matrix . clone () , alg )  

> 15

# compute | X | , | Y | , | XY |  

> 16

absX_mat = torch . zeros_like ( X_mat )  

> 17

absY_mat = torch . zeros_like ( Y_mat )  

> 18

for c in range ( alg . C ) :  

> 19

k_c = alg . k_factors [ c ]  

> 20

if k_c == 0: continue  

> 21

Xc = X_mat [: , c , : k_c , : k_c ]  

> 22

Yc = Y_mat [: , c , : k_c , : k_c ]  

> 23

Ux , Sx , Vx = torch . linalg . svd ( Xc )  

> 24

Uy , Sy , Vy = torch . linalg . svd ( Yc )  

> 25

absX_mat [: , c , : k_c , : k_c ] = Ux @ torch . diag_embed ( Sx ) @ Ux . conj () . transpose ( -2 , -1)  

> 26

absY_mat [: , c , : k_c , : k_c ] = Uy @ torch . diag_embed ( Sy ) @ Uy . conj () . transpose ( -2 , -1)  

> 27

XY_mat = X_mat @ Y_mat  

> 28

absXY_mat = torch . zeros_like ( XY_mat )  

> 29

for c in range ( alg . C ) :  

> 30

k_c = alg . k_factors [ c ]  

> 31

if k_c == 0: continue  

> 32

U , S , V = torch . linalg . svd ( XY_mat [: , c , : k_c , : k_c ])  

> 33

absXY_mat [: , c , : k_c , : k_c ] = U @ torch . diag_embed ( S ) @ U . conj () . transpose ( -2 , -1)  

> 34

def trace_op ( t ) :  

> 35

return torch . diagonal (t , dim1 = -2 , dim2 = -1) . sum ( -1) . sum ( -1)  

> 36

z = trace_op ( absXY_mat ) - trace_op ( absX_mat @ absY_mat )  

> 37

deltaX_list . append ( deltaX . cpu () . numpy () )  

> 38

deltaY_list . append ( deltaY . cpu () . numpy () )  

> 39

z_list . append ( z . cpu () . numpy () )  

> 40

return ( np . concatenate ( deltaX_list ) , np . concatenate ( deltaY_list ) , np . concatenate ( z_list ) ) 

5.3 Experiment 3 (self-adjoint X, positive Y )

Here X is self-adjoint (may have negative eigenvalues) and Y is positive. The quantity of interest is 

z = Tr( Y |X|Y ) − Tr( |Y XY |).

For central elements one expects z ≥ 0; violations correspond to z < 0. This inequality is known to characterise central elements [6], building upon the theory of noncommutative 8(a) kmax = 2 , C = 1 

(b) kmax = 2 , C = 2 

(c) kmax = 2 , C = 16 

Figure 3: Experiment 2: small dimension. 9(a) kmax = 16 , C = 1 

(b) kmax = 16 , C = 16 

(c) kmax = 16 , C = 32 

Figure 4: Experiment 2: intermediate dimension. 10 L1-spaces for positive operators [7]. Our numerical results for random non-central X

produce both positive and negative z values, with the histogram centred around zero. Listing 3: Experiment 3 implementation  

> 1

def experiment3 ( alg , batch_size , num_batches , device ) :  

> 2

sa_sampler = selfadjoint_sampler ( batch_size , device )  

> 3

pos_sampler = positive_sampler ( batch_size , device )  

> 4

deltaX_list , deltaY_list , z_list = [] , [] , []  

> 5

for _ in range ( num_batches ) :  

> 6

X = alg . operator_from_eigenvalues ( sa_sampler , batch_size = batch_size , force_self_adjoint = True )  

> 7

Y = alg . operator_from_eigenvalues ( pos_sampler , batch_size = batch_size ,  

> 8

force_positive = True , force_self_adjoint = True )  

> 9

absX = X . abs ()  

> 10

lmaxX , lminX = absX . lambda_max , absX . lambda_min  

> 11

deltaX = ( lmaxX - lminX ) / ( lmaxX + lminX )  

> 12

lmaxY , lminY = Y . lambda_max , Y . lambda_min  

> 13

deltaY = ( lmaxY - lminY ) / ( lmaxY + lminY )  

> 14

Y_absX_Y = Y @ absX @ Y 

> 15

abs_YXY = ( Y @ X @ Y ) . abs ()  

> 16

z = Y_absX_Y . trace - abs_YXY . trace  

> 17

deltaX_list . append ( deltaX . cpu () . numpy () )  

> 18

deltaY_list . append ( deltaY . cpu () . numpy () )  

> 19

z_list . append ( z . cpu () . numpy () )  

> 20

return ( np . concatenate ( deltaX_list ) , np . concatenate ( deltaY_list ) , np . concatenate ( z_list ) ) 

Full data sets (CSV files) and all generated plots are available in the repository. 

# 6 Limitations and future work 

Current limitations include: SVD becomes a bottleneck for kmax > 200 with large batch size; power iteration converges linearly; no automatic differentiation; only Type I algebras (direct sums) are supported; the library is limited to finite dimensions. Future work includes support for tensor products, Cauchy-type functional calculus, distributed GPU computing, and mixed-precision SVD. The code is open to contributions. 

# 7 Conclusion 

We have presented torch vn algebra , a PyTorch library for finite-dimensional Type I von Neumann algebras that combines a compact tensor representation, lazy evaluation, flex-ible random operator generation, and GPU-accelerated functional calculus. The library is validated and benchmarked, and is open-source at https://gitlab.com/a.hobukob/ von_neumann_type_i/ .

# Data availability 

All code and reproduction scripts are at the above repository. 11 (a) kmax = 2 , C = 1 

(b) kmax = 2 , C = 2 

(c) kmax = 2 , C = 16 

Figure 5: Experiment 3: small dimension. 12 (a) kmax = 16 , C = 1 

(b) kmax = 16 , C = 16 

(c) kmax = 16 , C = 32 

Figure 6: Experiment 3: intermediate dimension. 13 References 

[1] M. Takesaki, Theory of Operator Algebras , Springer, 2003. [2] B. Blackadar, Operator Algebras , Springer, 2006. [3] M.L. Mehta, Random Matrices , Elsevier, 2004. [4] M.A. Nielsen, I.L. Chuang, Quantum Computation and Quantum Information , CUP, 2010. [5] L.T. Gardner, “An inequality characterizes the trace”, Canad. J. Math. 31 (1979), 1322–1328. [6] A. Novikov, O. Tikhonov, “Characterization of central elements of operator algebras by inequalities”, arXiv:1502.01267 (2015). [7] A. Novikov, “ L1-space for a positive operator affiliated with von Neumann algebra”, 

Positivity 21 (2017), 359–375. [8] D. Petz, J. Zem´ anek, “Characterizations of the trace”, Linear Algebra Appl. 111 

(1998), 43–52. [9] D. Virosztek, “Connections between centrality and local monotonicity of certain func-tions”, J. Math. Anal. Appl. 453 (2017), 221–226. [10] A. Michelson, Studies in Optics , University of Chicago Press, 1927. [11] S.A. Abed, I.A. Nikolaeva, A.A. Novikov, “Generalisation of Michelson contrast for operators and its properties”, Lobachevskii J. Math. 45 (2024), 3835–3848. [12] A. Novikov, S.A. Abed, I. Nikolaeva, “Generalisation of Michelson contrast for oper-ators”, arXiv:2010.10130 (2020). [13] A. Paszke et al., “PyTorch”, NeurIPS 2019. [14] C.R. Harris et al., “Array programming with NumPy”, Nature 585 (2020), 357–362. [15] P. Virtanen et al., “SciPy 1.0”, Nat. Methods 17 (2020), 261–272. [16] J.R. Johansson, P.D. Nation, F. Nori, “QuTiP 2”, Comput. Phys. Commun. 184 

(2013), 1234–1240. [17] M.D. H. et al., “Qiskit”, 2019, https://qiskit.org .

# A Complete set of speedup heatmaps 

The benchmark script benchmark.py generates a heatmap for every operation. Figures 7 to 16 show the GPU/CPU speedup as a function of matrix dimension kmax (vertical axis) and number of channels C (horizontal axis). Red colours indicate speedup > 1 (GPU faster), blue colours indicate speedup < 1 (CPU faster). All heatmaps are saved in the repository as PNG files. 14 Figure 7: Heatmap for the trace operation. 

Figure 8: Heatmap for the addition operation. 

Figure 9: Heatmap for the multiplication operation. 15 Figure 10: Heatmap for the lambda max operation (power iteration). 

Figure 11: Heatmap for the svd abs operation (mean absolute singular value). 

Figure 12: Heatmap for the inverse operation. 16 Figure 13: Heatmap for the frobenius norm operation. 

Figure 14: Heatmap for the trace norm operation (nuclear norm). 

Figure 15: Heatmap for the entropy operation (von Neumann entropy). 17 Figure 16: Heatmap for the michelson contrast operation. 18