Title: Threshold Minimum Cut with Terminal Quotas: Logarithmic and Planar Approximation Algorithms

URL Source: https://arxiv.org/pdf/2606.15324v1

Published Time: Tue, 16 Jun 2026 00:42:13 GMT

Number of Pages: 10

Markdown Content:
# Threshold Minimum Cut with Terminal Quotas: Logarithmic and Planar Approximation Algorithms 

## Qi Duan, Carnegie Mellon University 

Abstract 

We study threshold minimum cut problems with a distinguished root vertex, a set of terminals, and a quota. In the threshold minimum edge cut problem ( TMEC ), the goal is to find a minimum-cost edge cut that disconnects at least k terminals from the root. In the threshold minimum node cut problem ( TMNC ), the goal is to delete a minimum-cost set of nonterminal, nonroot vertices so that at least k terminals become disconnected from the root. We prove three approximation guarantees. First, undirected general-graph TMEC admits a randomized polynomial-time expected O(log n) approximation via a R¨ acke-style cut-dominating tree decomposition and an exact dynamic program on trees. A standard repetition argument gives the same asymptotic ratio with high probability. Second, planar TMEC 

admits a factor-2 approximation by reducing the threshold condition to planar weighted balanced cut. Third, bounded-degree planar TMNC admits a 2∆-approximation, where ∆ is the maximum degree of a deletable vertex, by reducing the node-cost problem to the planar edge-cut problem on the same graph. The results separate exact-quota guarantees from bicriteria small-set-expansion-type guarantees and identify the unbounded-degree planar node-cut case as the main remaining obstacle. 

# 1 Introduction 

Cut problems are among the central objects in graph algorithms and network optimization. Classical mini-mum s–t cut asks for the cheapest edge or vertex set separating one specified terminal from another. Many applications, however, require a more aggregated form of separation: one wants to isolate not a single target but a sufficiently large number of targets from a protected root. This motivates threshold minimum cut problems. In this paper we study rooted threshold cut problems. The input consists of an undirected graph G =(V, E ), a root vertex s, a terminal set T ⊆ V \ { s}, and a quota k with 1 ≤ k ≤ | T |. A feasible solution must disconnect at least k terminals from the root. We consider both edge and node deletion. In the edge version, the solution is an edge boundary δ(X) of a set X containing at least k terminals and not containing s. In the node version, the solution is an outer vertex boundary N (X), and s and all terminals are undeletable. These problems are natural in several settings. In network reliability, one may ask for the minimum intervention that isolates a threshold number of vulnerable hosts from a protected source. In image segmen-tation, planar grid-like graphs often encode pixels or superpixels, and quota constraints can express the need to separate a sufficiently large region. In cyber-security attack graphs, a node cut corresponds to blocking or mitigating actions represented by attack states, and a quota can model disconnecting a sufficient number of harmful terminal states from an initial compromise state. 

Our results. We prove the following approximation guarantees. 1. For undirected general graphs, TMEC admits a randomized polynomial-time expected O(log n) approxi-mation. The algorithm samples a cut-dominating decomposition tree, solves the threshold problem exactly on the tree by dynamic programming, and maps the resulting tree cut back to the original graph. 2. For planar graphs, TMEC admits a factor-2 approximation. The reduction assigns weight 2 |T | to the root, weight 1 to every terminal, and weight 0 to all other vertices. A b-balanced cut with b = k/ (3 |T |)is then exactly strong enough to force at least k terminals outside the root component. 1

> arXiv:2606.15324v1 [cs.DS] 13 Jun 2026

3. For planar graphs in which every deletable vertex has degree at most ∆, TMNC admits a 2∆-approximation. The proof assigns each edge {u, v } the cost min {c(u), c (v)}, applies the planar TMEC approximation, and converts the returned edge cut back into a vertex separator by covering every crossing edge with a cheapest deletable endpoint. The edge-cut results preserve the quota exactly: the returned solution always disconnects at least k

terminals. This distinction is important because small-set-expansion-type algorithms often give stronger-looking bounds only in a bicriteria sense, for example by returning a cut that separates Ω( k) terminals rather than at least k terminals. 

# 2 Related Work 

Balanced separator and sparsest-cut approximations are classical tools in graph partitioning. Leighton and Rao gave logarithmic approximations based on multicommodity flow and metric embeddings [4]. Arora, Rao, and Vazirani improved the approximation ratio for sparsest cut and balanced separator to O(√log n)using semidefinite programming and geometric rounding [1]. These results are closely related in spirit but do not directly enforce the exact rooted terminal quota considered here. R¨ acke introduced hierarchical decompositions for congestion minimization and oblivious routing, giving logarithmic-loss tree representations of general graphs [5]. We use a standard cut-dominating formulation of this line of work as a black box: each sampled tree dominates every graph cut, and the expected tree value of every graph cut is within an O(log n) factor. This makes it possible to reduce exact-quota edge-cut objectives on general graphs to exact dynamic programming on trees. For planar graphs, Garg, Saran, and Vazirani gave a factor-2 approximation for minimum-cost b-balanced edge cut when b ≤ 1/3, assuming unary vertex weights; with binary weights they give a pseudoapproximation algorithm [3]. Our planar TMEC result is a direct quota-preserving reduction to this theorem. Small-set expansion and min–max graph partitioning provide another family of methods for small un-balanced cuts. Bansal et al. give bicriteria algorithms with guarantees of the form O(plog n log(1 /ρ )) for small-set expansion and related partitioning problems [2]. These tools suggest possible bicriteria improve-ments for threshold cuts, but the distinction between separating Ω( k) terminals and separating at least k

terminals is essential for the exact-feasibility results in this paper. 

# 3 Preliminaries 

Let G = ( V, E ) be an undirected graph. For a set X ⊆ V , its edge boundary is 

δG(X) = {{ u, v } ∈ E : u ∈ X, v / ∈ X}.

If the graph is clear from context, we write δ(X) instead of δG(X). If edges have nonnegative costs c : E →

R≥0, then 

cG(δG(X)) = X

> e∈δG(X)

c(e).

For vertex-deletion problems, the outer vertex boundary of X is 

NG(X) = {v ∈ V \ X : there exists u ∈ X with {u, v } ∈ E}.

Again, we write N (X) when the graph is clear. 

Definition 1 (Threshold minimum edge cut) . Given an undirected graph G = ( V, E ) with nonnegative edge costs c : E → R≥0, a root s ∈ V , a terminal set T ⊆ V \ { s}, and a quota k with 1 ≤ k ≤ | T |, the threshold minimum edge cut problem is 

OPT E (G, s, T, k ) = min {cG(δG(X)) : X ⊆ V, s / ∈ X, |X ∩ T | ≥ k} .

We abbreviate this problem as TMEC .

2Definition 2 (Threshold minimum node cut) . Given an undirected graph G = ( V, E ) with nonnegative vertex deletion costs c : V → R≥0 ∪ { +∞} , a root s ∈ V , a terminal set T ⊆ V \ { s}, and a quota k with 

1 ≤ k ≤ | T |, the threshold minimum node cut problem is 

OPT N (G, s, T, k ) = min {c(NG(X)) : X ⊆ V, s / ∈ X, |X ∩ T | ≥ k} ,

where 

c(NG(X)) = X

> v∈NG(X)

c(v).

The root s and terminals T are undeletable, which we model by assigning c(s) = + ∞ and c(t) = + ∞ for every t ∈ T . We abbreviate this problem as TMNC .

# 4 General-Graph Threshold Minimum Edge Cut 

In this section we prove that TMEC admits an O(log n) approximation on undirected general graphs. The algorithm uses a cut-dominating tree decomposition and an exact dynamic program on trees. If G is disconnected, then terminals already outside the connected component of s are disconnected at zero cost. Thus one may first count those terminals. If at least k terminals are already disconnected from 

s, the empty cut is optimal. Otherwise, we restrict to the connected component of s and reduce the quota accordingly. Hence, in the sequel, we assume without loss of generality that G is connected. 

## 4.1 Cut-Dominating Tree Decomposition 

We use the following cut-dominating tree-decomposition theorem as a black box. It is a standard formulation of R¨ acke-style cut-based hierarchical decompositions. 

Theorem 3 (Cut-dominating tree decomposition) . For every undirected edge-weighted graph G = ( V, E, c )

with n = |V |, there is a randomized polynomial-time construction of a weighted tree H whose leaves are in bijection with the vertices of G such that the following holds. For every set A ⊆ V , let λH (A) denote the minimum cost of a set of tree edges whose removal separates the leaves corresponding to A from the leaves corresponding to V \ A. Then, for every A ⊆ V ,

cG(δG(A)) ≤ λH (A),

and 

EH [λH (A)] ≤ α(n)cG(δG(A)) ,

where 

α(n) = O(log n).

The first inequality is the domination property: every tree cut dominates the corresponding graph cut. The second inequality says that the expected tree overestimate of every graph cut is only logarithmic. 

## 4.2 Exact TMEC on a Tree 

We next show that TMEC can be solved exactly on a weighted tree whose leaves represent the original graph vertices. Let H be a weighted tree whose leaves are in bijection with the original vertices V . We root H at the leaf corresponding to s. For a node u of H, let Hu denote the rooted subtree of H below u, and let L(u) ⊆ V

be the set of original vertices whose leaves lie in Hu. Define 

τ (u) = |L(u) ∩ T |.

All terminal counts are truncated at k.For every node u and every j ∈ { 0, 1, . . . , k }, define DP [u, j ] to be the minimum cost of cutting tree edges strictly inside Hu so that exactly j terminals in L(u) ∩ T are disconnected from u, under the condition that u remains connected to its parent. The value j = k means “at least k” after truncation. 3For a rooted leaf u, no edge lies strictly inside Hu, so 

DP [u, 0] = 0 ,

and 

DP [u, j ] = + ∞ for all j > 0.

The root of the oriented tree is the original leaf corresponding to s; it is treated by the same recurrence using its children. Thus, although it is a leaf of the unrooted decomposition tree, it is not a rooted leaf unless H

has one node. Now consider an internal rooted node u with children 

u1, u 2, . . . , u d.

For a child ui, there are two choices. 

Keep the edge (u, u i). If the edge is kept, then the number of disconnected terminals contributed by the child subtree is determined by DP [ui, ·]. 

Cut the edge (u, u i). If the edge ( u, u i) is cut, then all terminals in L(ui) ∩ T are disconnected from u.This contributes min {τ (ui), k }

terminals and costs 

cH (u, u i).

Thus define the child contribution table Bi by initially setting 

Bi[j] = DP [ui, j ] for j = 0 , 1, . . . , k, 

and then updating 

Bi[min {τ (ui), k }] = min {Bi[min {τ (ui), k }], c H (u, u i)} .

The table DP [u, ·] is obtained by knapsack convolution over the children. Initialize 

Q0[0] = 0 and 

Q0[j] = + ∞ for j > 0.

For i = 1 , . . . , d , define 

Qi[j] = min 

> a,b ∈{ 0,1,...,k }
> min {a+b,k }=j

{Qi−1[a] + Bi[b]} .

Finally set 

DP [u, j ] = Qd[j] for j = 0 , 1, . . . , k. 

At the root r, which corresponds to the original root vertex s, the optimum tree value is OPT H = DP [r, k ].

Lemma 4 (Exact tree algorithm) . TMEC on a weighted tree whose leaves are the original vertices can be solved exactly in polynomial time. A straightforward implementation runs in 

O(|V (H)|k2)

time. Proof. For every child subtree, an optimal tree solution has exactly two possibilities: either it cuts the edge to the child, thereby disconnecting all terminals in that child subtree, or it keeps the edge and recursively disconnects some terminals inside the child subtree. Since a tree has no cycles, choices in different child subtrees are independent once the state at the parent is fixed. The recurrence enumerates all such possibilities and combines them by knapsack convolution. By induc-tion from the rooted leaves to the root, DP [u, j ] is exactly the minimum cost of disconnecting j terminals inside Hu while keeping u connected to its parent. At the root, the state j = k represents disconnecting at least k terminals from the root leaf corresponding to s. Hence DP [r, k ] is the exact optimum tree value. 44.3 The Approximation Algorithm 

Algorithm 1 General-graph TMEC via cut-dominating trees 

Require: Undirected graph G = ( V, E ), edge costs c, root s, terminals T , quota k. 

> 1:

Sample a cut-dominating tree H using Theorem 3.  

> 2:

Solve TMEC exactly on H using Lemma 4.  

> 3:

Let FH be the tree edge set returned by the dynamic program.  

> 4:

Let XH = {v ∈ V : the leaf corresponding to v is disconnected from s in H − FH }. 

> 5:

Return the graph cut δG(XH ). 

Theorem 5 (General-graph TMEC approximation) . Threshold minimum edge cut on undirected general graphs admits a randomized polynomial-time expected 

O(log n)

approximation. Proof. Let X⋆ be an optimal TMEC solution in G. Thus 

s / ∈ X⋆, |X⋆ ∩ T | ≥ k, 

and 

cG(δG(X⋆)) = OPT G,

where OPT G = OPT E (G, s, T, k ). Sample a cut-dominating tree H from the distribution guaranteed by Theorem 3. Since X⋆ separates at least k terminals from s, the minimum tree edge set separating the leaves corresponding to X⋆ from the leaves corresponding to V \ X⋆ is feasible for the tree TMEC instance. Therefore, if FH is the optimum tree edge set found by the dynamic program, then 

cH (FH ) ≤ λH (X⋆).

By the expected distortion guarantee of Theorem 3, 

EH [λH (X⋆)] ≤ O(log n)cG(δG(X⋆)) = O(log n) OPT G .

Hence 

EH [cH (FH )] ≤ O(log n) OPT G .

Now define 

XH = {v ∈ V : the leaf corresponding to v is disconnected from s in H − FH }.

Since FH disconnects at least k terminal leaves from the root leaf, we have 

s / ∈ XH , |XH ∩ T | ≥ k. 

Thus δG(XH ) is a feasible TMEC solution in G.It remains to bound its graph cost. By the domination property of Theorem 3, 

cG(δG(XH )) ≤ λH (XH ).

Furthermore, FH itself separates the leaves in XH from the leaves in V \ XH . Therefore, 

λH (XH ) ≤ cH (FH ).

Combining these inequalities gives 

cG(δG(XH )) ≤ cH (FH ).

Taking expectation, 

EH [cG(δG(XH ))] ≤ EH [cH (FH )] ≤ O(log n) OPT G .

Therefore Algorithm 1 is an expected O(log n) approximation. 5Corollary 6 (High-probability version) . For any η ∈ (0 , 1) , by sampling 

O(log(1 /η )) 

independent decomposition trees, solving the tree instance on each, and returning the cheapest feasible graph cut obtained, one gets an 

O(log n)

approximation with probability at least 1 − η.Proof. If OPT G = 0, the expected-cost proof of Theorem 5 implies that each sampled solution has cost 0 with probability 1, so the claim is immediate. Assume OPT G > 0. For one sampled tree, Theorem 5 gives 

E [cG(δG(XH ))] ≤ C log n · OPT G

for some absolute constant C. By Markov’s inequality, Pr [ cG(δG(XH )) > 2C log n · OPT G] ≤ 12 .

After q independent repetitions, the probability that every sampled solution has cost larger than 2 C log n ·

OPT G is at most 2 −q . Choosing 

q = ⌈log 2(1 /η )⌉

gives success probability at least 1 − η.

# 5 Planar Threshold Minimum Cut 

In this section we prove a factor-2 approximation for planar TMEC and a 2∆-approximation for planar 

TMNC when every deletable vertex has maximum degree at most ∆. 

## 5.1 A Factor-2 Approximation for Planar TMEC 

We reduce planar TMEC to planar weighted balanced cut. We use the following theorem of Garg, Saran, and Vazirani [3]. 

Theorem 7 (Planar balanced cut approximation) . Let G = ( V, E ) be a planar graph with nonnegative edge costs and nonnegative vertex weights μ : V → R≥0. For every balance parameter b ≤ 1/3, there is a polynomial-time factor-2 approximation for the minimum-cost b-balanced edge cut when the vertex weights are unary. 

A cut is b-balanced if, after removing the cut edges, every connected component has weight at most (1 − b)μ(V ).

Theorem 8 (Planar TMEC ). Let G = ( V, E ) be a planar graph with nonnegative edge costs, root s, terminal set T , and quota k. Then planar TMEC admits a polynomial-time factor-2 approximation, assuming the unary-weight version of Theorem 7. Proof. Let 

m = |T |.

Define vertex weights μ by 

μ(s) = 2 m, μ(t) = 1 for every t ∈ T, 

and 

μ(v) = 0 for every v ∈ V \ ({s} ∪ T ).

6Then 

μ(V ) = 3 m. 

Set 

b = k

3m .

Since k ≤ m, we have 

b ≤ 13 .

Therefore Theorem 7 applies. The weights are bounded by 2 m and hence are unary-expandable in polynomial size. We first show that every feasible TMEC solution induces a feasible b-balanced cut. Let X ⊆ V satisfy 

s / ∈ X and |X ∩ T | ≥ k. 

The side containing s has weight at most 2m + ( m − k) = 3 m − k = (1 − b)μ(V ).

The side X has weight at most 

m ≤ 3m − k = (1 − b)μ(V ).

Thus δG(X) is a feasible b-balanced cut. Hence OPT bal ≤ OPT E (G, s, T, k ).

Now let F be the cut returned by the factor-2 planar balanced-cut algorithm. Then 

c(F ) ≤ 2 OPT bal ≤ 2 OPT E (G, s, T, k ).

Let R be the connected component containing s in G − F , and define 

X = V \ R. 

Since F is b-balanced, the root component R satisfies 

μ(R) ≤ 3m − k. 

Because s ∈ R, we have 

μ(R) = 2 m + |R ∩ T |.

Therefore 2m + |R ∩ T | ≤ 3m − k, 

which implies 

|R ∩ T | ≤ m − k. 

Hence 

|X ∩ T | ≥ k. 

Also s / ∈ X. Thus X is feasible for TMEC .Finally, every edge of δG(X) belongs to the removed edge set F , so 

c(δG(X)) ≤ c(F ) ≤ 2 OPT E (G, s, T, k ).

Therefore planar TMEC admits a factor-2 approximation. 75.2 A 2∆ -Approximation for Bounded-Degree Planar TMNC 

We now derive a constant-factor approximation for planar TMNC when the maximum degree of deletable vertices is bounded. Let 

D = V \ ({s} ∪ T )be the set of deletable vertices, and define ∆ = max {deg( v) : v ∈ D}.

We assume that the TMNC instance has a finite feasible solution. If no finite feasible solution exists, then the problem is infeasible under the undeletable root and terminal constraints. Let 

Call = X

> v∈D

c(v).

Choose a sufficiently large number 

M > 2∆ Call .

For notational convenience, set 

c(s) = M

and 

c(t) = M for every t ∈ T. 

For every edge e = {u, v } ∈ E, define the derived edge cost 

w(e) = min {c(u), c (v)}.

This gives a planar TMEC instance on the same graph. 

Lemma 9 (Node optimum induces a bounded-cost edge cut) . Let OPT N be the optimum value of the planar 

TMNC instance, and let OPT wE be the optimum value of the derived planar TMEC instance with edge costs 

w(e) = min {c(u), c (v)}. Then 

OPT wE ≤ ∆ OPT N .

Proof. Let S⋆ be an optimal node separator for TMNC , so 

c(S⋆) = OPT N .

Let R be the connected component containing s in G − S⋆, and define 

X⋆ = V \ (R ∪ S⋆).

Since S⋆ disconnects at least k terminals from s, we have 

s / ∈ X⋆ and |X⋆ ∩ T | ≥ k. 

Therefore X⋆ is feasible for the derived edge-cut threshold problem. Every edge in δG(X⋆) has one endpoint in X⋆ and the other endpoint in S⋆. Otherwise, an edge from 

X⋆ to the root component would make the two vertices belong to the same connected component of G − S⋆.Hence, for every crossing edge e = {u, v } incident to a separator vertex v ∈ S⋆, we have 

w(e) ≤ c(v).

Therefore 

w(δG(X⋆)) ≤ X

> v∈S⋆

deg( v)c(v).

8Since every deletable vertex has degree at most ∆, 

w(δG(X⋆)) ≤ ∆ X

> v∈S⋆

c(v) = ∆ OPT N .

Thus OPT wE ≤ ∆ OPT N .

Lemma 10 (Edge cut induces a node separator) . Let X ⊆ V satisfy 

s / ∈ X and |X ∩ T | ≥ k. 

Suppose also that 

w(δG(X)) < M. 

Then one can construct in polynomial time a node separator SX ⊆ D such that at least k terminals are disconnected from s in G − SX and 

c(SX ) ≤ w(δG(X)) .

Proof. Since 

w(δG(X)) < M, 

no edge in δG(X) can have both endpoints undeletable. Indeed, if an edge had both endpoints in {s} ∪ T ,then its edge cost would be M , forcing w(δG(X)) ≥ M , a contradiction. Therefore every crossing edge e ∈ δG(X) has at least one finite-cost deletable endpoint. For every edge 

e = {u, v } ∈ δG(X), choose a deletable endpoint of minimum cost and add it to SX . Since 

w(e) = min {c(u), c (v)},

we have 

c(SX ) ≤ X

> e∈δG(X)

w(e) = w(δG(X)) .

The set SX hits every edge crossing between X and V \ X. Hence, after deleting SX , no vertex of 

X \ SX can be connected to s through a crossing edge. Moreover, SX ⊆ D, so no terminal is deleted. Since |X ∩ T | ≥ k, at least k terminals are disconnected from s in G − SX . Thus SX is a feasible TMNC 

solution. 

Theorem 11 (Bounded-degree planar TMNC ). Let G = ( V, E ) be a planar graph with nonnegative vertex deletion costs. Let s be an undeletable root, let T be a set of undeletable terminals, and let k be a quota. Suppose the TMNC instance has a finite feasible solution and every deletable vertex has degree at most ∆.Then planar TMNC admits a polynomial-time 2∆ -approximation. Proof. Construct the derived planar TMEC instance with edge costs 

w({u, v }) = min {c(u), c (v)},

where c(s) = M and c(t) = M for all t ∈ T , with 

M > 2∆ Call .

By Theorem 8, we can find a set X ⊆ V such that 

s / ∈ X, |X ∩ T | ≥ k, 

and 

w(δG(X)) ≤ 2 OPT wE .

9By Lemma 9, OPT wE ≤ ∆ OPT N .

Therefore 

w(δG(X)) ≤ 2∆ OPT N .

Since the instance has a finite feasible solution, OPT N ≤ Call .

Thus 

w(δG(X)) ≤ 2∆ Call < M. 

Hence Lemma 10 applies. It constructs a node separator SX ⊆ D such that at least k terminals are disconnected from s and 

c(SX ) ≤ w(δG(X)) .

Consequently, 

c(SX ) ≤ w(δG(X)) ≤ 2∆ OPT N .

Thus SX is a feasible TMNC solution of cost at most 2∆ OPT N .

# 6 Conclusion 

We introduced and analyzed threshold minimum cut problems with rooted terminal quotas. For general undirected graphs, we gave an exact-feasibility O(log n) approximation for threshold minimum edge cut using a cut-dominating tree decomposition and an exact tree dynamic program. For planar graphs, we proved a factor-2 approximation for threshold minimum edge cut by reducing the quota condition to planar weighted balanced cut. Finally, for bounded-degree planar graphs, we proved a 2∆-approximation for threshold minimum node cut by converting vertex deletion costs into edge costs and then applying the planar edge-cut result. The results leave several natural directions open. The most direct one is to remove the factor ∆ in the planar node-cut approximation, which would likely require a direct approximation for planar balanced vertex separator or a more faithful transformation from vertex costs to planar edge cuts. A second direction is to improve the exact-quota general-graph edge-cut ratio below O(log n). Existing small-set-expansion methods suggest better bicriteria bounds, but exact preservation of the quota |X ∩ T | ≥ k remains the key technical obstacle. 

# References 

[1] Sanjeev Arora, Satish Rao, and Umesh Vazirani. Expander flows, geometric embeddings and graph partitioning. Journal of the ACM , 56(2):5:1–5:37, 2009. [2] Nikhil Bansal, Uriel Feige, Robert Krauthgamer, Konstantin Makarychev, Viswanath Nagarajan, Joseph Naor, and Roy Schwartz. Min-max graph partitioning and small set expansion. SIAM Journal on Computing , 43(2):872–904, 2014. [3] Naveen Garg, Hanif D. Saran, and Vijay V. Vazirani. Finding separator cuts in planar graphs within twice the optimal. SIAM Journal on Computing , 29(1):159–179, 1999. [4] Tom Leighton and Satish Rao. Multicommodity max-flow min-cut theorems and their use in designing approximation algorithms. Journal of the ACM , 46(6):787–832, 1999. [5] Harald R¨ acke. Optimal hierarchical decompositions for congestion minimization in networks. In Proceed-ings of the 40th Annual ACM Symposium on Theory of Computing , pages 255–264. ACM, 2008. 10