---
title: "Finite-Dimensional Type I von Neumann Algebras in PyTorch: A GPU-Accelerated Framework for Random Block-Diagonal Operators"
source: arxiv
title_zh: 用于Random Block-Diagonal Operators的Finite-Dimensional Type I von Neumann Algebras in PyTorch：GPU-Accelerated 框架
abstract_zh: "Limitations 与 future work are outlined。\n\n核心做法：作者提出 \\texttt{torch\\_vn\\_algebra}，open-source Python library built on PyTorch 用于 numerical experiment。\n\n主要结果：library is validated against analytical expectations (Haar moments，trace properties)。\n\n值得关注的点在于：library provides：\\bullet compact batched tensor representation (B,C,k_{\\max},k_{\\max}) that handles。"
abstract_en: "We present \\texttt{torch\\_vn\\_algebra}, an open-source Python library built on PyTorch for numerical experiments with finite-dimensional Type I von Neumann algebras (direct sums of matrix algebras). The library provides:   $\\bullet$ a compact batched tensor representation $(B,C,k_{\\max},k_{\\max})$ that handles both Monte Carlo samples and multiple direct summands;   $\\bullet$ lazy evaluation of operators to avoid unnecessary memory allocation;   $\\bullet$ generation of random operators with arbitrary eigenvalue distributions (user-provided samplers) and various unitary ensembles (Haar, $\\mathrm{SU}(n)$, COE, CSE, diagonal phases);   $\\bullet$ functional calculus via SVD (absolute value, square root, inverse, entropy) and a hybrid method for extreme eigenvalues (exact diagonalisation for $k_{\\max}\\le256$, otherwise power iteration);   $\\bullet$ three trace functionals (blunt, normalised subspace trace, and the von Neumann tracial state);   $\\bullet$ GPU-accelerated batched linear algebra for moderate-scale Monte Carlo studies (e.g., $2\\times10^4$ samples of $100\\times100$ operators).   The library is validated against analytical expectations (Haar moments, trace properties). Performance benchmarks on a Tesla P100 GPU are presented and discussed. Limitations and future work are outlined. The code is open-source."
authors: "Irina Nikolaeva, Andrej Novikov"
date: 2026-06-14
journal: cs.MS
source_link: "https://arxiv.org/pdf/2606.15882v1"
pdf: "https://arxiv.org/pdf/2606.15882v1"
score: 6.949999999999999
tldr: "围绕《用于Random Block-Diagonal...》；作者提出 \\texttt{torch\\_vn\\_algebra}...；library is validated against analyti..."
tldr_cn: "围绕《用于Random Block-Diagonal...》；作者提出 \\texttt{torch\\_vn\\_algebra}...；library is validated against analyti..."
tldr_en: "We present \\texttt{torch\\_vn\\_algebra}, an open-source Python library built on PyTorch for numerical experiments with finite-dimensional Type I von Neumann algebras (direct sums of matrix algebras). The library provides: $\\bullet$ a compact batched tensor representation $(B,C,k_{\\max},k_{\\max})$ that handles both Monte Carlo samples and multiple direct summands; $\\bullet$ lazy evaluation of operators to avoid unnecessary memory allocation..."
selection_source: fresh_fetch
motivation: Limitations 与 future work are outlined。
method: "核心做法：作者提出 \\texttt{torch\\_vn\\_algebra}，open-source Python library built on PyTorch 用于 numerical experiment。"
result: 主要结果：library is validated against analytical expectations (Haar moments，trace properties)。
conclusion: "值得关注的点在于：library provides：\\bullet compact batched tensor representation (B,C,k_{\\max},k_{\\max}) that handles。"
---

## 摘要
Limitations 与 future work are outlined。

核心做法：作者提出 \texttt{torch\_vn\_algebra}，open-source Python library built on PyTorch 用于 numerical experiment。

主要结果：library is validated against analytical expectations (Haar moments，trace properties)。

值得关注的点在于：library provides：\bullet compact batched tensor representation (B,C,k_{\max},k_{\max}) that handles。

## Abstract
We present \texttt{torch\_vn\_algebra}, an open-source Python library built on PyTorch for numerical experiments with finite-dimensional Type I von Neumann algebras (direct sums of matrix algebras). The library provides:   $\bullet$ a compact batched tensor representation $(B,C,k_{\max},k_{\max})$ that handles both Monte Carlo samples and multiple direct summands;   $\bullet$ lazy evaluation of operators to avoid unnecessary memory allocation;   $\bullet$ generation of random operators with arbitrary eigenvalue distributions (user-provided samplers) and various unitary ensembles (Haar, $\mathrm{SU}(n)$, COE, CSE, diagonal phases);   $\bullet$ functional calculus via SVD (absolute value, square root, inverse, entropy) and a hybrid method for extreme eigenvalues (exact diagonalisation for $k_{\max}\le256$, otherwise power iteration);   $\bullet$ three trace functionals (blunt, normalised subspace trace, and the von Neumann tracial state);   $\bullet$ GPU-accelerated batched linear algebra for moderate-scale Monte Carlo studies (e.g., $2\times10^4$ samples of $100\times100$ operators).   The library is validated against analytical expectations (Haar moments, trace properties). Performance benchmarks on a Tesla P100 GPU are presented and discussed. Limitations and future work are outlined. The code is open-source.
