---
title: Toward Robust In-Context Segmentation via Concept Guidance
source: arxiv
title_zh: 通过Concept Guidance实现的Toward Robust In-Context Segmentation
abstract_zh: "In-context segmentation (ICS) requires 模型 to segment target regions in query image 使用 only few refer。\n\n核心做法：Specifically，CG-ICS introduces concept reasoning 模块 that uses MLLM to propose candidates 与 SAM3-driv。\n\n主要结果：Despite recent progress，prior ICS studies have largely overlooked critical aspect：system robustness。\n\n值得关注的点在于：Extensive experiments on standard ICS benchmarks 表明 that CG-ICS not only 达到当前最优 accuracy but also su。"
abstract_en: "In-context segmentation (ICS) requires a model to segment target regions in a query image using only a few reference images and their corresponding masks, without updating any parameters. Despite recent progress, prior ICS studies have largely overlooked a critical aspect: system robustness, ie, whether the model can produce stable segmentation results for the same query under different references. In this work, we revisit ICS from the robustness perspective and introduce a novel paradigm, Concept-Guided In-Context Segmentation (CG-ICS), which performs segmentation by extracting high-level semantic concepts from references rather than relying solely on low-level visual matching. Specifically, CG-ICS introduces a concept reasoning module that uses an MLLM to propose candidates and a SAM3-driven scoring function with tree-search refinement to select reliable textual concepts, together with a parallel visual exemplar route that provides query-side spatial grounding via a simple context construction. Both the textual concept and the visual exemplar are then used to activate the segmentation capability of a frozen SAM3 backbone. Extensive experiments on standard ICS benchmarks demonstrate that CG-ICS not only achieves state-of-the-art accuracy but also substantially improves robustness, yielding a more reliable ICS system with significantly reduced variance across diverse reference choices."
authors: "Zhigang Chen, Xiawu Zheng, Rongrong Ji"
date: 2026-06-26
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.28149v1"
pdf: "https://arxiv.org/pdf/2606.28149v1"
score: 6.35
tldr: 围绕《通过Concept Guidance实现的Tow...》；Specifically，CG-ICS introduces c...；Despite recent progress，prior ICS st...
tldr_cn: 围绕《通过Concept Guidance实现的Tow...》；Specifically，CG-ICS introduces c...；Despite recent progress，prior ICS st...
tldr_en: "In-context segmentation (ICS) requires a model to segment target regions in a query image using only a few reference images and their corresponding masks, without updating any parameters. Despite recent progress, prior ICS studies have largely overlooked a critical aspect: system robustness, ie, whether the model can produce stable segmentation results for the same query..."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "robustness | 鲁棒性 | IELTS"]
motivation: In-context segmentation (ICS) requires 模型 to segment target regions in query image 使用 only few refer。
method: 核心做法：Specifically，CG-ICS introduces concept reasoning 模块 that uses MLLM to propose candidates 与 SAM3-driv。
result: 主要结果：Despite recent progress，prior ICS studies have largely overlooked critical aspect：system robustness。
conclusion: 值得关注的点在于：Extensive experiments on standard ICS benchmarks 表明 that CG-ICS not only 达到当前最优 accuracy but also su。
---

## 摘要
In-context segmentation (ICS) requires 模型 to segment target regions in query image 使用 only few refer。

核心做法：Specifically，CG-ICS introduces concept reasoning 模块 that uses MLLM to propose candidates 与 SAM3-driv。

主要结果：Despite recent progress，prior ICS studies have largely overlooked critical aspect：system robustness。

值得关注的点在于：Extensive experiments on standard ICS benchmarks 表明 that CG-ICS not only 达到当前最优 accuracy but also su。

## Abstract
In-context segmentation (ICS) requires a model to segment target regions in a query image using only a few reference images and their corresponding masks, without updating any parameters. Despite recent progress, prior ICS studies have largely overlooked a critical aspect: system robustness, ie, whether the model can produce stable segmentation results for the same query under different references. In this work, we revisit ICS from the robustness perspective and introduce a novel paradigm, Concept-Guided In-Context Segmentation (CG-ICS), which performs segmentation by extracting high-level semantic concepts from references rather than relying solely on low-level visual matching. Specifically, CG-ICS introduces a concept reasoning module that uses an MLLM to propose candidates and a SAM3-driven scoring function with tree-search refinement to select reliable textual concepts, together with a parallel visual exemplar route that provides query-side spatial grounding via a simple context construction. Both the textual concept and the visual exemplar are then used to activate the segmentation capability of a frozen SAM3 backbone. Extensive experiments on standard ICS benchmarks demonstrate that CG-ICS not only achieves state-of-the-art accuracy but also substantially improves robustness, yielding a more reliable ICS system with significantly reduced variance across diverse reference choices.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **robustness**：鲁棒性（IELTS）
