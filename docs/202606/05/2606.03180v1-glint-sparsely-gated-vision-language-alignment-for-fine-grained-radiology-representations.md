---
title: "GLINT: Sparsely Gated Vision-Language Alignment for Fine-Grained Radiology Representations"
source: arxiv
title_zh: 用于Fine-Grained Radiology Representations的GLINT：Sparsely Gated Vision-Language Alignment
abstract_zh: "This poses central challenge：prior approaches spread weight densely across all patches rather 相比 con。\n\n核心做法：To address this，作者提出 GLINT (Gated Language-Image alignmeNT)，框架 that explicitly 模型s this sparse corre。\n\n主要结果：Vision-language 模型s (VLMs) 用于 radiology have emerged as scalable paradigm by leveraging image-report。\n\n值得关注的点在于：In downstream evaluation，GLINT 优于 both SSL encoders 与 medical VLMs on classification，report generati。"
abstract_en: "Vision-language models (VLMs) for radiology have emerged as a scalable paradigm by leveraging image-report pairs naturally produced in clinical workflows. However, this pairing reveals a mismatch in scale: each finding occupies only a small region of the image, yet supervision is provided only at the global image-report level. This poses a central challenge: prior approaches spread weight densely across all patches rather than concentrating on the sparse subset relevant to a given query. To address this, we present GLINT (Gated Language-Image alignmeNT), a framework that explicitly models this sparse correspondence. On the alignment side, we introduce Sparsely Gated Alignment, a novel architecture in which a sigmoid gate over a separate gate embedding space activates only the patches relevant to each textual query, enforcing explicit sparsity. On the representation side, we add Dense Feature Regularization, which anchors the trainable encoder's intermediate features to a frozen self-supervised learning (SSL) teacher, preserving the fine-grained patch features that the gate relies on. The same recipe applies to both 2D chest X-ray (CXR) and 3D chest computed tomography (CT), built with DINOv3 and V-JEPA 2.1, respectively. GLINT enables zero-shot classification, grounding, and segmentation from free-text queries, and to our knowledge is the first to demonstrate zero-shot segmentation on 3D CT volumes without mask supervision. Notably, the most pronounced gains arise on zero-shot grounding and segmentation, where sparse, query-specific localization is required, consistent with our design intent. In downstream evaluation, GLINT outperforms both SSL encoders and medical VLMs on classification, report generation, and segmentation."
authors: "Jonggwon Park, Seongeun Lee, Junhyun Park, Hannah Yun, Hyunwoong Kim, Sohyun Jeong, Hyewon Kang, Byungmu Yoon, Kyoyun Choi"
date: 2026-06-02
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.03180v1"
pdf: "https://arxiv.org/pdf/2606.03180v1"
score: 7.8
tldr: 围绕《用于Fine-Grained Radiology...》；To address this，作者提出 GLINT (Gate...；Vision-language 模型s (VLMs) 用于 radiol...
tldr_cn: 围绕《用于Fine-Grained Radiology...》；To address this，作者提出 GLINT (Gate...；Vision-language 模型s (VLMs) 用于 radiol...
tldr_en: "Vision-language models (VLMs) for radiology have emerged as a scalable paradigm by leveraging image-report pairs naturally produced in clinical workflows. However, this pairing reveals a mismatch in scale: each finding occupies only a small region of the image, yet supervision is provided only at the global image-report level."
selection_source: fresh_fetch
vocabulary: ["classification | 分类 | CET6", "localization | 定位 | CET6", "segmentation | 分割 | CET6/IELTS", "downstream | 下游任务 | 学术高频", "alignment | 对齐 | CET6", "embedding | 嵌入表示 | 学术高频", "clinical | 临床的 | CET6"]
motivation: This poses central challenge：prior approaches spread weight densely across all patches rather 相比 con。
method: 核心做法：To address this，作者提出 GLINT (Gated Language-Image alignmeNT)，框架 that explicitly 模型s this sparse corre。
result: 主要结果：Vision-language 模型s (VLMs) 用于 radiology have emerged as scalable paradigm by leveraging image-report。
conclusion: 值得关注的点在于：In downstream evaluation，GLINT 优于 both SSL encoders 与 medical VLMs on classification，report generati。
---

## 摘要
This poses central challenge：prior approaches spread weight densely across all patches rather 相比 con。

核心做法：To address this，作者提出 GLINT (Gated Language-Image alignmeNT)，框架 that explicitly 模型s this sparse corre。

主要结果：Vision-language 模型s (VLMs) 用于 radiology have emerged as scalable paradigm by leveraging image-report。

值得关注的点在于：In downstream evaluation，GLINT 优于 both SSL encoders 与 medical VLMs on classification，report generati。

## Abstract
Vision-language models (VLMs) for radiology have emerged as a scalable paradigm by leveraging image-report pairs naturally produced in clinical workflows. However, this pairing reveals a mismatch in scale: each finding occupies only a small region of the image, yet supervision is provided only at the global image-report level. This poses a central challenge: prior approaches spread weight densely across all patches rather than concentrating on the sparse subset relevant to a given query. To address this, we present GLINT (Gated Language-Image alignmeNT), a framework that explicitly models this sparse correspondence. On the alignment side, we introduce Sparsely Gated Alignment, a novel architecture in which a sigmoid gate over a separate gate embedding space activates only the patches relevant to each textual query, enforcing explicit sparsity. On the representation side, we add Dense Feature Regularization, which anchors the trainable encoder's intermediate features to a frozen self-supervised learning (SSL) teacher, preserving the fine-grained patch features that the gate relies on. The same recipe applies to both 2D chest X-ray (CXR) and 3D chest computed tomography (CT), built with DINOv3 and V-JEPA 2.1, respectively. GLINT enables zero-shot classification, grounding, and segmentation from free-text queries, and to our knowledge is the first to demonstrate zero-shot segmentation on 3D CT volumes without mask supervision. Notably, the most pronounced gains arise on zero-shot grounding and segmentation, where sparse, query-specific localization is required, consistent with our design intent. In downstream evaluation, GLINT outperforms both SSL encoders and medical VLMs on classification, report generation, and segmentation.

## 英语词汇
- **classification**：分类（CET6）
- **localization**：定位（CET6）
- **segmentation**：分割（CET6/IELTS）
- **downstream**：下游任务（学术高频）
- **alignment**：对齐（CET6）
- **embedding**：嵌入表示（学术高频）
- **clinical**：临床的（CET6）
