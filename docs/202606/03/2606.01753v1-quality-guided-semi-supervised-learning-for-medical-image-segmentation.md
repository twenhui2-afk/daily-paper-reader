---
title: Quality-Guided Semi-Supervised Learning for Medical Image Segmentation
source: arxiv
title_zh: 用于Medical Image Segmentation的Quality-Guided Semi-Supervised Learning
abstract_zh: "Training accurate medical image segmentation 模型s requires large amounts 的 densely annotated data，whi。\n\n核心做法：Instead，作者提出 quality-guided SSL 框架 that trains dedicated 网络 to estimate segmentation quality from im。\n\n主要结果：We show that our 方法 serves as drop-in enhancement to existing SSL 框架s。\n\n值得关注的点在于：Extensive experiments across five datasets 与 multiple 架构s 表明 consistent improvements over competing。"
abstract_en: "Training accurate medical image segmentation models requires large amounts of densely annotated data, which is costly and time-consuming to obtain. Semi-supervised learning (SSL) alleviates this by learning from both abundant unlabeled data and limited labeled data. However, most modern SSL methods rely on pseudolabels for unlabeled data, and typically assess their reliability through model confidence or uncertainty, measures that are self-referential and lack explicit grounding in segmentation quality. Instead, we propose a quality-guided SSL framework that trains a dedicated network to estimate segmentation quality from image-mask pairs. The predictor is trained on variable-quality masks generated through synthetic corruptions augmented with imperfect outputs from partially trained segmentation models, capturing realistic error patterns encountered during training. We integrate the quality predictor into SSL through two complementary mechanisms: a quality-aware regularization loss and a quality-based pseudolabel sample reweighting scheme. We show that our method serves as a drop-in enhancement to existing SSL frameworks. Extensive experiments across five datasets and multiple architectures demonstrate consistent improvements over competing SSL methods, advancing the state-of-the-art in semi-supervised medical image segmentation."
authors: "Kumar Abhishek, Ghassan Hamarneh"
date: 2026-06-01
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.01753v1"
pdf: "https://arxiv.org/pdf/2606.01753v1"
score: 6.949999999999999
tldr: 围绕《用于Medical Image Segmenta...》；Instead，作者提出 quality-guided SSL...；We show that our 方法 serves as drop-i...
tldr_cn: 围绕《用于Medical Image Segmenta...》；Instead，作者提出 quality-guided SSL...；We show that our 方法 serves as drop-i...
tldr_en: "Training accurate medical image segmentation models requires large amounts of densely annotated data, which is costly and time-consuming to obtain. Semi-supervised learning (SSL) alleviates this by learning from both abundant unlabeled data and limited labeled data."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "uncertainty | 不确定性 | IELTS"]
motivation: Training accurate medical image segmentation 模型s requires large amounts 的 densely annotated data，whi。
method: 核心做法：Instead，作者提出 quality-guided SSL 框架 that trains dedicated 网络 to estimate segmentation quality from im。
result: 主要结果：We show that our 方法 serves as drop-in enhancement to existing SSL 框架s。
conclusion: 值得关注的点在于：Extensive experiments across five datasets 与 multiple 架构s 表明 consistent improvements over competing。
---

## 摘要
Training accurate medical image segmentation 模型s requires large amounts 的 densely annotated data，whi。

核心做法：Instead，作者提出 quality-guided SSL 框架 that trains dedicated 网络 to estimate segmentation quality from im。

主要结果：We show that our 方法 serves as drop-in enhancement to existing SSL 框架s。

值得关注的点在于：Extensive experiments across five datasets 与 multiple 架构s 表明 consistent improvements over competing。

## Abstract
Training accurate medical image segmentation models requires large amounts of densely annotated data, which is costly and time-consuming to obtain. Semi-supervised learning (SSL) alleviates this by learning from both abundant unlabeled data and limited labeled data. However, most modern SSL methods rely on pseudolabels for unlabeled data, and typically assess their reliability through model confidence or uncertainty, measures that are self-referential and lack explicit grounding in segmentation quality. Instead, we propose a quality-guided SSL framework that trains a dedicated network to estimate segmentation quality from image-mask pairs. The predictor is trained on variable-quality masks generated through synthetic corruptions augmented with imperfect outputs from partially trained segmentation models, capturing realistic error patterns encountered during training. We integrate the quality predictor into SSL through two complementary mechanisms: a quality-aware regularization loss and a quality-based pseudolabel sample reweighting scheme. We show that our method serves as a drop-in enhancement to existing SSL frameworks. Extensive experiments across five datasets and multiple architectures demonstrate consistent improvements over competing SSL methods, advancing the state-of-the-art in semi-supervised medical image segmentation.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **uncertainty**：不确定性（IELTS）
