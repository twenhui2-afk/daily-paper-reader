---
title: "From Gradient Clipping to Structural Refinement: Improving DPSGD for Medical Image Segmentation"
source: arxiv
title_zh: 用于Medical Image Segmentation的由 Gradient Clipping to Structural Refinement：Improving DPSGD
abstract_zh: "Medical image segmentation is widely used 用于 disease detection but relies on sensitive data，raising。\n\n核心做法：Finally，作者提出 adaptive DP-Morph variant that captures class-specific structures 与 enhances 性能 in mult。\n\n主要结果：We further 表明 that combining clipping strategies 结合 morphological refinement improves segmentation q。\n\n值得关注的点在于：We evaluate these 方法s across binary 与 multi-class tasks 与 analyze gradient alignment，showing that pr。"
abstract_en: "Medical image segmentation is widely used for disease detection but relies on sensitive data, raising privacy concerns as trained models can leak information. Differential privacy, typically implemented via Differential Private Stochastic Gradient Descent (DPSGD), provides a solution, though at the cost of reduced utility. Recent DPSGD variants, including Automatic clipping (Auto-S), Normalised SGD with perturbation (NSGD), and Per-sample adaptive clipping (PSAC), have shown promise in image classification, but their behavior in medical segmentation remains underexplored. We evaluate these methods across binary and multi-class tasks and analyze gradient alignment, showing that prior assumptions, particularly for PSAC, do not consistently hold. We further demonstrate that combining clipping strategies with morphological refinement improves segmentation quality under privacy constraints. Finally, we propose an adaptive DP-Morph variant that captures class-specific structures and enhances performance in multi-class settings."
authors: "Shiva Parsarad, Parth Shandilya, Isabel Wagner"
date: 2026-06-19
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.21763v1"
pdf: "https://arxiv.org/pdf/2606.21763v1"
score: 8.049999999999999
tldr: 围绕《用于Medical Image Segmenta...》；Finally，作者提出 adaptive DP-Morph v...；We further 表明 that combining clippin...
tldr_cn: 围绕《用于Medical Image Segmenta...》；Finally，作者提出 adaptive DP-Morph v...；We further 表明 that combining clippin...
tldr_en: "Medical image segmentation is widely used for disease detection but relies on sensitive data, raising privacy concerns as trained models can leak information. Differential privacy, typically implemented via Differential Private Stochastic Gradient Descent (DPSGD), provides a solution, though at the cost of reduced utility."
selection_source: fresh_fetch
vocabulary: ["classification | 分类 | CET6", "segmentation | 分割 | CET6/IELTS", "alignment | 对齐 | CET6", "detection | 检测 | CET6"]
motivation: Medical image segmentation is widely used 用于 disease detection but relies on sensitive data，raising。
method: 核心做法：Finally，作者提出 adaptive DP-Morph variant that captures class-specific structures 与 enhances 性能 in mult。
result: 主要结果：We further 表明 that combining clipping strategies 结合 morphological refinement improves segmentation q。
conclusion: 值得关注的点在于：We evaluate these 方法s across binary 与 multi-class tasks 与 analyze gradient alignment，showing that pr。
---

## 摘要
Medical image segmentation is widely used 用于 disease detection but relies on sensitive data，raising。

核心做法：Finally，作者提出 adaptive DP-Morph variant that captures class-specific structures 与 enhances 性能 in mult。

主要结果：We further 表明 that combining clipping strategies 结合 morphological refinement improves segmentation q。

值得关注的点在于：We evaluate these 方法s across binary 与 multi-class tasks 与 analyze gradient alignment，showing that pr。

## Abstract
Medical image segmentation is widely used for disease detection but relies on sensitive data, raising privacy concerns as trained models can leak information. Differential privacy, typically implemented via Differential Private Stochastic Gradient Descent (DPSGD), provides a solution, though at the cost of reduced utility. Recent DPSGD variants, including Automatic clipping (Auto-S), Normalised SGD with perturbation (NSGD), and Per-sample adaptive clipping (PSAC), have shown promise in image classification, but their behavior in medical segmentation remains underexplored. We evaluate these methods across binary and multi-class tasks and analyze gradient alignment, showing that prior assumptions, particularly for PSAC, do not consistently hold. We further demonstrate that combining clipping strategies with morphological refinement improves segmentation quality under privacy constraints. Finally, we propose an adaptive DP-Morph variant that captures class-specific structures and enhances performance in multi-class settings.

## 英语词汇
- **classification**：分类（CET6）
- **segmentation**：分割（CET6/IELTS）
- **alignment**：对齐（CET6）
- **detection**：检测（CET6）
