---
title: "From Reconstruction to Decision: A Post-Encoder Plug-in Adapter for Curvilinear Segmentation"
source: arxiv
title_zh: 用于Curvilinear Segmentation的由 Reconstruction to Decision：Post-Encoder Plug-in Adapter
abstract_zh: "Meanwhile，modern segmentation pipelines increasingly rely on strong but hard-to-modify foundation en。\n\n核心做法：作者提出 PEPA，lightweight Post-Encoder Plug-in Adapter 用于 2D curvilinear segmentation pipelines 结合 acces。\n\n主要结果：Experiments on five medical 与 industrial benchmarks show that adding PEPA to frozen-encoder baseline。\n\n值得关注的点在于：结合 only \\sim0.26M additional parameters，PEPA offers practical post-encoder enhancement 用于 structure-。"
abstract_en: "Curvilinear object segmentation, including vessels and cracks, is challenging due to extreme spatial sparsity and topological fragility, where small local errors can cause severe structural disconnections. Meanwhile, modern segmentation pipelines increasingly rely on strong but hard-to-modify foundation encoders whose heavy downsampling limits fine structural recovery. Motivated by this, we focus on the post-encoder stage and study two recurring and actionable failure modes: a reconstruction bottleneck in high-resolution feature restoration and a decision bottleneck in binarization. We present PEPA, a lightweight Post-Encoder Plug-in Adapter for 2D curvilinear segmentation pipelines with accessible decoder/head features and target, query, or class descriptors. PEPA couples (i) Target-Conditioned Snake Upsampling (TCSU), which uses target-conditioned continuous snake-like sampling to better recover thin and tortuous structures during upsampling, and (ii) Target-Adaptive Differentiable Thresholding (TADT), which predicts target-specific thresholds and optimizes a soft-threshold surrogate with explicit safeguards against trivial bias shifting. Under this post-encoder interface, PEPA can be attached to both prompt-based decoders and conventional dense predictors. Experiments on five medical and industrial benchmarks show that adding PEPA to frozen-encoder baselines yields consistent improvements, with gains in topological connectivity (clDice) typically exceeding those in region overlap (IoU), indicating improved structural continuity. With only $\\sim$0.26M additional parameters, PEPA offers a practical post-encoder enhancement for structure-centric segmentation."
authors: "Qin Lei, Jiang Zhong, Xin Xiao, Yuming Yang, Hao Wu"
date: 2026-06-22
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.23486v1"
pdf: "https://arxiv.org/pdf/2606.23486v1"
score: 8.25
tldr: 围绕《用于Curvilinear Segmentati...》；作者提出 PEPA，lightweight Post-Encod...；Experiments on five medical 与 indust...
tldr_cn: 围绕《用于Curvilinear Segmentati...》；作者提出 PEPA，lightweight Post-Encod...；Experiments on five medical 与 indust...
tldr_en: "Curvilinear object segmentation, including vessels and cracks, is challenging due to extreme spatial sparsity and topological fragility, where small local errors can cause severe structural disconnections. Meanwhile, modern segmentation pipelines increasingly rely on strong but hard-to-modify foundation encoders whose heavy downsampling limits fine structural recovery."
selection_source: fresh_fetch
vocabulary: ["reconstruction | 重建 | CET6", "segmentation | 分割 | CET6/IELTS"]
motivation: Meanwhile，modern segmentation pipelines increasingly rely on strong but hard-to-modify foundation en。
method: 核心做法：作者提出 PEPA，lightweight Post-Encoder Plug-in Adapter 用于 2D curvilinear segmentation pipelines 结合 acces。
result: 主要结果：Experiments on five medical 与 industrial benchmarks show that adding PEPA to frozen-encoder baseline。
conclusion: 值得关注的点在于：结合 only \sim0.26M additional parameters，PEPA offers practical post-encoder enhancement 用于 structure-。
---

## 摘要
Meanwhile，modern segmentation pipelines increasingly rely on strong but hard-to-modify foundation en。

核心做法：作者提出 PEPA，lightweight Post-Encoder Plug-in Adapter 用于 2D curvilinear segmentation pipelines 结合 acces。

主要结果：Experiments on five medical 与 industrial benchmarks show that adding PEPA to frozen-encoder baseline。

值得关注的点在于：结合 only \sim0.26M additional parameters，PEPA offers practical post-encoder enhancement 用于 structure-。

## Abstract
Curvilinear object segmentation, including vessels and cracks, is challenging due to extreme spatial sparsity and topological fragility, where small local errors can cause severe structural disconnections. Meanwhile, modern segmentation pipelines increasingly rely on strong but hard-to-modify foundation encoders whose heavy downsampling limits fine structural recovery. Motivated by this, we focus on the post-encoder stage and study two recurring and actionable failure modes: a reconstruction bottleneck in high-resolution feature restoration and a decision bottleneck in binarization. We present PEPA, a lightweight Post-Encoder Plug-in Adapter for 2D curvilinear segmentation pipelines with accessible decoder/head features and target, query, or class descriptors. PEPA couples (i) Target-Conditioned Snake Upsampling (TCSU), which uses target-conditioned continuous snake-like sampling to better recover thin and tortuous structures during upsampling, and (ii) Target-Adaptive Differentiable Thresholding (TADT), which predicts target-specific thresholds and optimizes a soft-threshold surrogate with explicit safeguards against trivial bias shifting. Under this post-encoder interface, PEPA can be attached to both prompt-based decoders and conventional dense predictors. Experiments on five medical and industrial benchmarks show that adding PEPA to frozen-encoder baselines yields consistent improvements, with gains in topological connectivity (clDice) typically exceeding those in region overlap (IoU), indicating improved structural continuity. With only $\sim$0.26M additional parameters, PEPA offers a practical post-encoder enhancement for structure-centric segmentation.

## 英语词汇
- **reconstruction**：重建（CET6）
- **segmentation**：分割（CET6/IELTS）
