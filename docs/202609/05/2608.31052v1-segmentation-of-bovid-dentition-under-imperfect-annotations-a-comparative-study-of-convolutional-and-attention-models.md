---
title: "Segmentation of Bovid Dentition Under Imperfect Annotations: A Comparative Study of Convolutional and Attention Models"
source: arxiv
title_zh: Convolutional与Attention 模型s的Bovid Dentition Under Imperfect Annotations：Comparative Study的Segmentation
abstract_zh: "We find that while these preprocessing choices have limited effect on quantitative metrics such as D。\n\n核心做法：Advances in machine learning (ML) have shifted this task away from traditional rule-based heuristics。\n\n主要结果：However，semantic segmentation DNNs crucially depend on expertly designed mask targets to learn from。\n\n值得关注的点在于：This paper presents comparative study 的 segmentation 架构s，ranging from convolutional backbones to vis。"
abstract_en: "Semantic segmentation decomposes an image into distinct mask regions corresponding to different object categories, such as people, cars, signs or buildings. Advances in machine learning (ML) have shifted this task away from traditional rule-based heuristics such as edge detection, towards deep neural networks (DNN) that learn to classify pixels directly. However, semantic segmentation DNNs crucially depend on expertly designed mask targets to learn from, and imperfect or misaligned masks can interfere with a model's ability to learn effectively.   This paper presents a comparative study of segmentation architectures, ranging from convolutional backbones to vision transformers, applied to the B.O.V.I.D. dataset, a corpus of high-resolution bovid dental photographs paired with hand-made segmentation masks not originally designed for ML-based training. We evaluate a range of preprocessing and alignment techniques to mitigate the resulting label imperfections. We find that while these preprocessing choices have limited effect on quantitative metrics such as Dice score and mIoU, their qualitative impact on predicted masks is substantial."
authors: "Keith G. Mills, Evan B. Sanders, Gregory J. Matthews, Juliet K. Brophy"
date: 2026-08-31
journal: cs.CV
source_link: "https://arxiv.org/pdf/2608.31052v1"
pdf: "https://arxiv.org/pdf/2608.31052v1"
score: 7.1
tldr: 围绕《Convolutional与Attention...》；Advances in machine learning (ML...；However，semantic segmentation DNNs c...
tldr_cn: 围绕《Convolutional与Attention...》；Advances in machine learning (ML...；However，semantic segmentation DNNs c...
tldr_en: "Semantic segmentation decomposes an image into distinct mask regions corresponding to different object categories, such as people, cars, signs or buildings. Advances in machine learning (ML) have shifted this task away from traditional rule-based heuristics such as edge detection, towards deep neural networks (DNN) that learn to classify pixels directly."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "alignment | 对齐 | CET6", "detection | 检测 | CET6"]
motivation: We find that while these preprocessing choices have limited effect on quantitative metrics such as D。
method: 核心做法：Advances in machine learning (ML) have shifted this task away from traditional rule-based heuristics。
result: 主要结果：However，semantic segmentation DNNs crucially depend on expertly designed mask targets to learn from。
conclusion: 值得关注的点在于：This paper presents comparative study 的 segmentation 架构s，ranging from convolutional backbones to vis。
---

## 摘要
We find that while these preprocessing choices have limited effect on quantitative metrics such as D。

核心做法：Advances in machine learning (ML) have shifted this task away from traditional rule-based heuristics。

主要结果：However，semantic segmentation DNNs crucially depend on expertly designed mask targets to learn from。

值得关注的点在于：This paper presents comparative study 的 segmentation 架构s，ranging from convolutional backbones to vis。

## Abstract
Semantic segmentation decomposes an image into distinct mask regions corresponding to different object categories, such as people, cars, signs or buildings. Advances in machine learning (ML) have shifted this task away from traditional rule-based heuristics such as edge detection, towards deep neural networks (DNN) that learn to classify pixels directly. However, semantic segmentation DNNs crucially depend on expertly designed mask targets to learn from, and imperfect or misaligned masks can interfere with a model's ability to learn effectively.   This paper presents a comparative study of segmentation architectures, ranging from convolutional backbones to vision transformers, applied to the B.O.V.I.D. dataset, a corpus of high-resolution bovid dental photographs paired with hand-made segmentation masks not originally designed for ML-based training. We evaluate a range of preprocessing and alignment techniques to mitigate the resulting label imperfections. We find that while these preprocessing choices have limited effect on quantitative metrics such as Dice score and mIoU, their qualitative impact on predicted masks is substantial.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **alignment**：对齐（CET6）
- **detection**：检测（CET6）
