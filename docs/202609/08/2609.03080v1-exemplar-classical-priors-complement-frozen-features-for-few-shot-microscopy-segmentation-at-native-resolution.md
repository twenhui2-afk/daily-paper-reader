---
title: "Exemplar: Classical Priors Complement Frozen Features for Few-Shot Microscopy Segmentation at Native Resolution"
source: arxiv
title_zh: 用于Few-Shot Microscopy Segmentation at Native Resolution的Exemplar：Classical Priors Complement Frozen Features
abstract_zh: "Segmenting new biomedical dataset usually means domain-specific 模型 trained on substantial annotation。\n\n核心做法：作者提出 Exemplar，few-shot segmenter that fuses frozen DINOv3 backbone 结合 fixed bank 的 classical native-。\n\n主要结果：In few-mask，native-resolution regime，classical priors 与 frozen self-supervised features are compleme。\n\n值得关注的点在于：At eight masks nnU-Net overtakes it on panel mean，chiefly on centreline agreement，but takes 16-77x l。"
abstract_en: "Segmenting a new biomedical dataset usually means a domain-specific model trained on substantial annotation, or a foundation model steered at inference time. We present Exemplar, a few-shot segmenter that fuses a frozen DINOv3 backbone with a fixed bank of classical native-resolution filter responses in one lightweight head, fitted from the support masks alone. In the few-mask, native-resolution regime, classical priors and frozen self-supervised features are complementary: fused in one head, a single fixed configuration spans eleven biomedical imaging datasets. Under the same head, the classical bank alone reaches 0.693 on the eleven-dataset panel, scored by foreground intersection-over-union or centreline Dice, and the frozen features alone 0.672; the bank leads on seven of the eleven and the features on the rest, and fused they reach 0.782. Against five forward-pass few-shot methods, Exemplar leads in 54 of 55 method-dataset comparisons, 52 of them significant after Holm correction. From a single annotated mask it reaches 0.703 on the same panel, against 0.682 for a from-scratch nnU-Net trained on that same mask. At eight masks nnU-Net overtakes it on the panel mean, chiefly on centreline agreement, but takes 16-77x longer to fit."
authors: "Michal Průšek, Adam Novozámský, Filip Šroubek"
date: 2026-09-02
journal: cs.CV
source_link: "https://arxiv.org/pdf/2609.03080v1"
pdf: "https://arxiv.org/pdf/2609.03080v1"
score: 6.25
tldr: 围绕《用于Few-Shot Microscopy Se...》；作者提出 Exemplar，few-shot segmenter...；In few-mask，native-resolution regime...
tldr_cn: 围绕《用于Few-Shot Microscopy Se...》；作者提出 Exemplar，few-shot segmenter...；In few-mask，native-resolution regime...
tldr_en: "Segmenting a new biomedical dataset usually means a domain-specific model trained on substantial annotation, or a foundation model steered at inference time. We present Exemplar, a few-shot segmenter that fuses a frozen DINOv3 backbone with a fixed bank of classical native-resolution filter responses in one lightweight head, fitted from the support masks alone."
selection_source: fresh_fetch
vocabulary: ["foundation model | 基础模型 | 学术高频", "segmentation | 分割 | CET6/IELTS", "annotation | 标注 | CET6", "inference | 推理 | CET6"]
motivation: Segmenting new biomedical dataset usually means domain-specific 模型 trained on substantial annotation。
method: 核心做法：作者提出 Exemplar，few-shot segmenter that fuses frozen DINOv3 backbone 结合 fixed bank 的 classical native-。
result: 主要结果：In few-mask，native-resolution regime，classical priors 与 frozen self-supervised features are compleme。
conclusion: 值得关注的点在于：At eight masks nnU-Net overtakes it on panel mean，chiefly on centreline agreement，but takes 16-77x l。
---

## 摘要
Segmenting new biomedical dataset usually means domain-specific 模型 trained on substantial annotation。

核心做法：作者提出 Exemplar，few-shot segmenter that fuses frozen DINOv3 backbone 结合 fixed bank 的 classical native-。

主要结果：In few-mask，native-resolution regime，classical priors 与 frozen self-supervised features are compleme。

值得关注的点在于：At eight masks nnU-Net overtakes it on panel mean，chiefly on centreline agreement，but takes 16-77x l。

## Abstract
Segmenting a new biomedical dataset usually means a domain-specific model trained on substantial annotation, or a foundation model steered at inference time. We present Exemplar, a few-shot segmenter that fuses a frozen DINOv3 backbone with a fixed bank of classical native-resolution filter responses in one lightweight head, fitted from the support masks alone. In the few-mask, native-resolution regime, classical priors and frozen self-supervised features are complementary: fused in one head, a single fixed configuration spans eleven biomedical imaging datasets. Under the same head, the classical bank alone reaches 0.693 on the eleven-dataset panel, scored by foreground intersection-over-union or centreline Dice, and the frozen features alone 0.672; the bank leads on seven of the eleven and the features on the rest, and fused they reach 0.782. Against five forward-pass few-shot methods, Exemplar leads in 54 of 55 method-dataset comparisons, 52 of them significant after Holm correction. From a single annotated mask it reaches 0.703 on the same panel, against 0.682 for a from-scratch nnU-Net trained on that same mask. At eight masks nnU-Net overtakes it on the panel mean, chiefly on centreline agreement, but takes 16-77x longer to fit.

## 英语词汇
- **foundation model**：基础模型（学术高频）
- **segmentation**：分割（CET6/IELTS）
- **annotation**：标注（CET6）
- **inference**：推理（CET6）
