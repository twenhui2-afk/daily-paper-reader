---
title: "Data Scout: Targeted Web Crawling for Domain-Specific Pretraining Corpora"
source: arxiv
title_zh: 用于Domain-Specific Pretraining Corpora的Data Scout：Targeted Web Crawling
abstract_zh: "dominant approach to building domain-specific pretraining corpora is to filter large web archives su。\n\n核心做法：作者提出 Data Scout，which inverts this：instead 的 filtering archive，it directs targeted crawl。\n\n主要结果：Because probe is only domain-specific component，Data Scout can in principle apply to any domain 结合 s。\n\n值得关注的点在于：But payoff is not just efficiency：63.2% 的 these pages are missing from CommonCrawl altogether，yet ju。"
abstract_en: "The dominant approach to building domain-specific pretraining corpora is to filter large web archives such as CommonCrawl. This works well for popular domains but breaks down for specialized ones, where relevant content is sparse and often beyond the reach of popularity-driven crawlers. We present Data Scout, which inverts this: instead of filtering an archive, it directs a targeted crawl. An LLM expands a root topic into a taxonomy and thousands of search queries; the returned URLs (seeds) are grouped by subdomain and screened with a user-supplied classifier (the probe), admitting each subdomain on the basis of a small sample. This works because relevance has a sharp boundary at the subdomain level: in mathematics, a page is 21x more likely to be relevant than one on a sibling subdomain. With the FineMath classifier as the probe, 21.9% of crawled pages are high-quality math content, 70x the 0.31% rate from filtering a comparable web sample, so the crawl wastes far less effort. But the payoff is not just efficiency: 63.2% of these pages are missing from CommonCrawl altogether, yet just as useful for training. Continued pretraining of Llama-3.2-3B on 1.9B Data Scout tokens matches FineMath corpus on GSM8k. Because the probe is the only domain-specific component, Data Scout can in principle apply to any domain with such a classifier."
authors: "Chirag Garg, Eelaaf Zahid, Farhan Ahmed, Jay Pankaj Gala, Eric Butler, Heiko Ludwig"
date: 2026-09-04
journal: cs.LG
source_link: "https://arxiv.org/pdf/2609.05766v1"
pdf: "https://arxiv.org/pdf/2609.05766v1"
score: 6.949999999999999
tldr: 围绕《用于Domain-Specific Pretra...》；作者提出 Data Scout，which inverts th...；Because probe is only domain-specifi...
tldr_cn: 围绕《用于Domain-Specific Pretra...》；作者提出 Data Scout，which inverts th...；Because probe is only domain-specifi...
tldr_en: "The dominant approach to building domain-specific pretraining corpora is to filter large web archives such as CommonCrawl. This works well for popular domains but breaks down for specialized ones, where relevant content is sparse and often beyond the reach of popularity-driven crawlers."
selection_source: fresh_fetch
vocabulary: ["pretraining | 预训练 | 学术高频"]
motivation: dominant approach to building domain-specific pretraining corpora is to filter large web archives su。
method: 核心做法：作者提出 Data Scout，which inverts this：instead 的 filtering archive，it directs targeted crawl。
result: 主要结果：Because probe is only domain-specific component，Data Scout can in principle apply to any domain 结合 s。
conclusion: "值得关注的点在于：But payoff is not just efficiency：63.2% 的 these pages are missing from CommonCrawl altogether，yet ju。"
---

## 摘要
dominant approach to building domain-specific pretraining corpora is to filter large web archives su。

核心做法：作者提出 Data Scout，which inverts this：instead 的 filtering archive，it directs targeted crawl。

主要结果：Because probe is only domain-specific component，Data Scout can in principle apply to any domain 结合 s。

值得关注的点在于：But payoff is not just efficiency：63.2% 的 these pages are missing from CommonCrawl altogether，yet ju。

## Abstract
The dominant approach to building domain-specific pretraining corpora is to filter large web archives such as CommonCrawl. This works well for popular domains but breaks down for specialized ones, where relevant content is sparse and often beyond the reach of popularity-driven crawlers. We present Data Scout, which inverts this: instead of filtering an archive, it directs a targeted crawl. An LLM expands a root topic into a taxonomy and thousands of search queries; the returned URLs (seeds) are grouped by subdomain and screened with a user-supplied classifier (the probe), admitting each subdomain on the basis of a small sample. This works because relevance has a sharp boundary at the subdomain level: in mathematics, a page is 21x more likely to be relevant than one on a sibling subdomain. With the FineMath classifier as the probe, 21.9% of crawled pages are high-quality math content, 70x the 0.31% rate from filtering a comparable web sample, so the crawl wastes far less effort. But the payoff is not just efficiency: 63.2% of these pages are missing from CommonCrawl altogether, yet just as useful for training. Continued pretraining of Llama-3.2-3B on 1.9B Data Scout tokens matches FineMath corpus on GSM8k. Because the probe is the only domain-specific component, Data Scout can in principle apply to any domain with such a classifier.

## 英语词汇
- **pretraining**：预训练（学术高频）
