---
title: Unbiased Monte Carlo Greeks for Discontinuous Payoffs
source: arxiv
title_zh: 用于Discontinuous Payoffs的Unbiased Monte Carlo Greeks
abstract_zh: "Pathwise differentiation 的 Monte Carlo estimators fails at payoff discontinuities，producing zero or。\n\n核心做法：industry workaround --- smoothing indicator functions --- introduces bias 与 requires per-product cal。\n\n主要结果：用于 payoff F (Z,θ) that is piecewise smooth 结合 discontinuities on surfaces \\{g_i = 0\\}，we show that s。\n\n值得关注的点在于：Benchmarks on QuantLib 模型s (GBM，Heston，Hull-White) show all Greeks within 0.1--4\\% 的 analytic or bum。"
abstract_en: "Pathwise differentiation of Monte Carlo estimators fails at payoff discontinuities, producing zero or biased sensitivities for barriers, autocallables, and digital options. The industry workaround --- smoothing the indicator functions --- introduces bias and requires per-product calibration. We derive a correction formula that restores unbiased Greeks without smoothing. For a payoff $F(Z,θ)$ that is piecewise smooth with discontinuities on surfaces $\\{g_i = 0\\}$, we show that the sensitivity decomposes into a pathwise term (computed by standard AAD) plus a sum of boundary corrections, each involving the payoff jump, the Gaussian density at the boundary, and the sensitivity of the boundary to the parameter. The correction is computed by Newton root-finding in the normal-random space, with the jump evaluated by two forward replays of the pricing kernel. The implementation uses AADC (\\texttt{pip install aadc}), whose tape replay and automatic discontinuity tracking make the method fully automatic --- the quant writes standard pricing code, and the correction driver identifies and handles all discontinuities. We prove the formula for arbitrary compositions of smooth functions and indicator functions (not just outer products), covering real autocallable payoff structures with recursive alive/dead logic. Benchmarks on QuantLib models (GBM, Heston, Hull-White) show all Greeks within 0.1--4\\% of analytic or bump-and-revalue references."
authors: Evgeny Lakshtanov
date: 2026-09-05
journal: q-fin.CP
source_link: "https://arxiv.org/pdf/2609.06137v1"
pdf: "https://arxiv.org/pdf/2609.06137v1"
score: 6.549999999999999
tldr: "围绕《用于Discontinuous Payoffs的...》；industry workaround --- smoothin...；用于 payoff F (Z,θ) that is piecewise..."
tldr_cn: "围绕《用于Discontinuous Payoffs的...》；industry workaround --- smoothin...；用于 payoff F (Z,θ) that is piecewise..."
tldr_en: "Pathwise differentiation of Monte Carlo estimators fails at payoff discontinuities, producing zero or biased sensitivities for barriers, autocallables, and digital options. The industry workaround --- smoothing the indicator functions --- introduces bias and requires per-product calibration."
selection_source: fresh_fetch
motivation: Pathwise differentiation 的 Monte Carlo estimators fails at payoff discontinuities，producing zero or。
method: 核心做法：industry workaround --- smoothing indicator functions --- introduces bias 与 requires per-product cal。
result: "主要结果：用于 payoff F (Z,θ) that is piecewise smooth 结合 discontinuities on surfaces \\{g_i = 0\\}，we show that s。"
conclusion: "值得关注的点在于：Benchmarks on QuantLib 模型s (GBM，Heston，Hull-White) show all Greeks within 0.1--4\\% 的 analytic or bum。"
---

## 摘要
Pathwise differentiation 的 Monte Carlo estimators fails at payoff discontinuities，producing zero or。

核心做法：industry workaround --- smoothing indicator functions --- introduces bias 与 requires per-product cal。

主要结果：用于 payoff F (Z,θ) that is piecewise smooth 结合 discontinuities on surfaces \{g_i = 0\}，we show that s。

值得关注的点在于：Benchmarks on QuantLib 模型s (GBM，Heston，Hull-White) show all Greeks within 0.1--4\% 的 analytic or bum。

## Abstract
Pathwise differentiation of Monte Carlo estimators fails at payoff discontinuities, producing zero or biased sensitivities for barriers, autocallables, and digital options. The industry workaround --- smoothing the indicator functions --- introduces bias and requires per-product calibration. We derive a correction formula that restores unbiased Greeks without smoothing. For a payoff $F(Z,θ)$ that is piecewise smooth with discontinuities on surfaces $\{g_i = 0\}$, we show that the sensitivity decomposes into a pathwise term (computed by standard AAD) plus a sum of boundary corrections, each involving the payoff jump, the Gaussian density at the boundary, and the sensitivity of the boundary to the parameter. The correction is computed by Newton root-finding in the normal-random space, with the jump evaluated by two forward replays of the pricing kernel. The implementation uses AADC (\texttt{pip install aadc}), whose tape replay and automatic discontinuity tracking make the method fully automatic --- the quant writes standard pricing code, and the correction driver identifies and handles all discontinuities. We prove the formula for arbitrary compositions of smooth functions and indicator functions (not just outer products), covering real autocallable payoff structures with recursive alive/dead logic. Benchmarks on QuantLib models (GBM, Heston, Hull-White) show all Greeks within 0.1--4\% of analytic or bump-and-revalue references.
