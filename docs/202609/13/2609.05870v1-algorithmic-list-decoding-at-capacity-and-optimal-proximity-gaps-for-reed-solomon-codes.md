---
title: Algorithmic List Decoding at Capacity and Optimal Proximity Gaps for Reed-Solomon Codes
source: arxiv
title_zh: 用于Reed-Solomon Codes的Algorithmic List Decoding at Capacity与Optimal Proximity Gaps
abstract_zh: "We give unified hidden-derivative 框架用于 list decoding 与 mutual correlated agreement 的 ordinary Reed--。\n\n核心做法：用于 every fixed slack γ>0，every sufficiently large block length n，every prime q\\ge n，与 every dimensio。\n\n主要结果：Intersecting these varieties 结合 equations from full agreement support yields both field-size-indepen。\n\n值得关注的点在于：用于 every fixed curve degree \\ell，at most n^{O_{γ,\\ell}(1)} parameters on curve f_0+zf_1+\\cdots+z^\\el。"
abstract_en: "We give a unified hidden-derivative framework for list decoding and mutual correlated agreement of ordinary Reed--Solomon codes over prime fields, on arbitrary prescribed evaluation sets. For every fixed slack $γ>0$, every sufficiently large block length $n$, every prime $q\\ge n$, and every dimension $1\\le k\\le(1-γ)n$, a deterministic algorithm finds all codewords within relative distance $1-k/n-γ$ in $q^{O_γ(1)}$ time. The final list has size $n^{O_γ(1)}$, independently of $q$. Both statements extend to bounded-input-list recovery, with constants depending additionally on the input-list bound.   For every fixed curve degree $\\ell$, at most $n^{O_{γ,\\ell}(1)}$ parameters on a curve $f_0+zf_1+\\cdots+z^\\ell f_\\ell$ admit a nearby codeword whose exact agreement support is not a maximal jointly explained support of the coefficient words. For lines this gives MCA error $n^{O_γ(1)}/q$, with no proximity loss.   The interpolation stage reparameterizes and optimizes the hidden-derivative construction of Brakensiek, Chen, Putterman, Zhang, and Zheng; differential root enumeration uses Kopparty's algorithm. We then prove that a specialization-safe differential equation has a cover by constant-dimensional varieties of polynomial cumulative degree, outside polynomially many parameter values. Intersecting these varieties with equations from the full agreement support yields both the field-size-independent list bound and exact-support MCA."
authors: Fernando Granha Jeronimo
date: 2026-09-05
journal: cs.IT
source_link: "https://arxiv.org/pdf/2609.05870v1"
pdf: "https://arxiv.org/pdf/2609.05870v1"
score: 6.8
tldr: "围绕《用于Reed-Solomon Codes的Alg...》；用于 every fixed slack γ>0，every s...；Intersecting these varieties 结合 equa..."
tldr_cn: "围绕《用于Reed-Solomon Codes的Alg...》；用于 every fixed slack γ>0，every s...；Intersecting these varieties 结合 equa..."
tldr_en: "We give a unified hidden-derivative framework for list decoding and mutual correlated agreement of ordinary Reed--Solomon codes over prime fields, on arbitrary prescribed evaluation sets. For every fixed slack $γ>0$, every sufficiently large block length $n$, every prime $q\\ge n$, and every dimension $1\\le k\\le(1-γ)n$, a deterministic algorithm finds all codewords within relative distance $1-k/n-γ$..."
selection_source: fresh_fetch
motivation: We give unified hidden-derivative 框架用于 list decoding 与 mutual correlated agreement 的 ordinary Reed--。
method: "核心做法：用于 every fixed slack γ>0，every sufficiently large block length n，every prime q\\ge n，与 every dimensio。"
result: 主要结果：Intersecting these varieties 结合 equations from full agreement support yields both field-size-indepen。
conclusion: "值得关注的点在于：用于 every fixed curve degree \\ell，at most n^{O_{γ,\\ell}(1)} parameters on curve f_0+zf_1+\\cdots+z^\\el。"
---

## 摘要
We give unified hidden-derivative 框架用于 list decoding 与 mutual correlated agreement 的 ordinary Reed--。

核心做法：用于 every fixed slack γ>0，every sufficiently large block length n，every prime q\ge n，与 every dimensio。

主要结果：Intersecting these varieties 结合 equations from full agreement support yields both field-size-indepen。

值得关注的点在于：用于 every fixed curve degree \ell，at most n^{O_{γ,\ell}(1)} parameters on curve f_0+zf_1+\cdots+z^\el。

## Abstract
We give a unified hidden-derivative framework for list decoding and mutual correlated agreement of ordinary Reed--Solomon codes over prime fields, on arbitrary prescribed evaluation sets. For every fixed slack $γ>0$, every sufficiently large block length $n$, every prime $q\ge n$, and every dimension $1\le k\le(1-γ)n$, a deterministic algorithm finds all codewords within relative distance $1-k/n-γ$ in $q^{O_γ(1)}$ time. The final list has size $n^{O_γ(1)}$, independently of $q$. Both statements extend to bounded-input-list recovery, with constants depending additionally on the input-list bound.   For every fixed curve degree $\ell$, at most $n^{O_{γ,\ell}(1)}$ parameters on a curve $f_0+zf_1+\cdots+z^\ell f_\ell$ admit a nearby codeword whose exact agreement support is not a maximal jointly explained support of the coefficient words. For lines this gives MCA error $n^{O_γ(1)}/q$, with no proximity loss.   The interpolation stage reparameterizes and optimizes the hidden-derivative construction of Brakensiek, Chen, Putterman, Zhang, and Zheng; differential root enumeration uses Kopparty's algorithm. We then prove that a specialization-safe differential equation has a cover by constant-dimensional varieties of polynomial cumulative degree, outside polynomially many parameter values. Intersecting these varieties with equations from the full agreement support yields both the field-size-independent list bound and exact-support MCA.
