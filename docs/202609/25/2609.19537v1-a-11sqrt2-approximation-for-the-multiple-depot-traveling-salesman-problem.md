---
title: "A $(1+1/\\sqrt{2})$-Approximation for the Multiple-Depot Traveling Salesman Problem"
source: arxiv
title_zh: "用于Multiple-Depot Traveling Salesman Problem的(1+1/\\sqrt{2})-Approximation"
abstract_zh: "metric multiple-depot TSP (MD-TSP) is natural extension，where graph contains depots 与 clients，与 obje。\n\n核心做法：In this paper，作者引入 (1+1/\\sqrt{2})-approximation algorithm。\n\n主要结果：When number 的 depots is part 的 input，adaptation 的 Christofides--Serdyukov heuristic yields approxima。\n\n值得关注的点在于：Balancing coefficients 的 these two terms by setting both to \\sqrt{2}-1 yields claimed approximation。"
abstract_en: "The metric traveling salesman problem (TSP) is a fundamental problem in combinatorial optimization that asks for a minimum-cost tour covering all clients in a metric graph. The metric multiple-depot TSP (MD-TSP) is a natural extension, where the graph contains depots and clients, and the objective is to compute a minimum-cost set of tours covering all clients, with each tour starting and ending at the same depot. When the number of depots is part of the input, an adaptation of the Christofides--Serdyukov heuristic yields an approximation ratio of $2$. In this paper, we introduce a $(1+1/\\sqrt{2})$-approximation algorithm. Like the Christofides--Serdyukov heuristic, our algorithm first computes a rooted spanning forest (RSF), then a matching to correct its odd degrees, and finally obtains a solution by shortcutting. However, instead of using a minimum-cost RSF, we construct an RSF by a primal-dual algorithm for a natural cut relaxation. The algorithm grows rootless components and the component containing all depots at different rates, adding an edge when its dual constraint becomes tight. Vertex labels record the times at which clients first become connected to a depot. The two-speed growth provides a joint bound on the forest cost and two label-dependent terms that also arise in bounding the parity-correction cost. Balancing the coefficients of these two terms by setting both to $\\sqrt{2}-1$ yields the claimed approximation ratio."
authors: "Jingyang Zhao, Yuxi Liu, Mingyu Xiao"
date: 2026-09-17
journal: cs.DS
source_link: "https://arxiv.org/pdf/2609.19537v1"
pdf: "https://arxiv.org/pdf/2609.19537v1"
score: 6.0
tldr: "围绕《用于Multiple-Depot Traveli...》；In this paper，作者引入 (1+1/\\sqrt{2}...；When number 的 depots is part 的 input..."
tldr_cn: "围绕《用于Multiple-Depot Traveli...》；In this paper，作者引入 (1+1/\\sqrt{2}...；When number 的 depots is part 的 input..."
tldr_en: "The metric traveling salesman problem (TSP) is a fundamental problem in combinatorial optimization that asks for a minimum-cost tour covering all clients in a metric graph. The metric multiple-depot TSP (MD-TSP) is a natural extension, where the graph contains depots and clients, and the objective is to compute a minimum-cost set of tours covering all..."
selection_source: fresh_fetch
vocabulary: ["optimization | 优化 | CET6"]
motivation: metric multiple-depot TSP (MD-TSP) is natural extension，where graph contains depots 与 clients，与 obje。
method: "核心做法：In this paper，作者引入 (1+1/\\sqrt{2})-approximation algorithm。"
result: 主要结果：When number 的 depots is part 的 input，adaptation 的 Christofides--Serdyukov heuristic yields approxima。
conclusion: "值得关注的点在于：Balancing coefficients 的 these two terms by setting both to \\sqrt{2}-1 yields claimed approximation。"
---

## 摘要
metric multiple-depot TSP (MD-TSP) is natural extension，where graph contains depots 与 clients，与 obje。

核心做法：In this paper，作者引入 (1+1/\sqrt{2})-approximation algorithm。

主要结果：When number 的 depots is part 的 input，adaptation 的 Christofides--Serdyukov heuristic yields approxima。

值得关注的点在于：Balancing coefficients 的 these two terms by setting both to \sqrt{2}-1 yields claimed approximation。

## Abstract
The metric traveling salesman problem (TSP) is a fundamental problem in combinatorial optimization that asks for a minimum-cost tour covering all clients in a metric graph. The metric multiple-depot TSP (MD-TSP) is a natural extension, where the graph contains depots and clients, and the objective is to compute a minimum-cost set of tours covering all clients, with each tour starting and ending at the same depot. When the number of depots is part of the input, an adaptation of the Christofides--Serdyukov heuristic yields an approximation ratio of $2$. In this paper, we introduce a $(1+1/\sqrt{2})$-approximation algorithm. Like the Christofides--Serdyukov heuristic, our algorithm first computes a rooted spanning forest (RSF), then a matching to correct its odd degrees, and finally obtains a solution by shortcutting. However, instead of using a minimum-cost RSF, we construct an RSF by a primal-dual algorithm for a natural cut relaxation. The algorithm grows rootless components and the component containing all depots at different rates, adding an edge when its dual constraint becomes tight. Vertex labels record the times at which clients first become connected to a depot. The two-speed growth provides a joint bound on the forest cost and two label-dependent terms that also arise in bounding the parity-correction cost. Balancing the coefficients of these two terms by setting both to $\sqrt{2}-1$ yields the claimed approximation ratio.

## 英语词汇
- **optimization**：优化（CET6）
