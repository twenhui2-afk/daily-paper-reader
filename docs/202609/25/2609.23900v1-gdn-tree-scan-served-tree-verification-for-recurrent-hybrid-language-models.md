---
title: "GDN Tree-Scan: Served Tree Verification for Recurrent-Hybrid Language Models"
source: arxiv
title_zh: 用于Recurrent-Hybrid Language 模型s的GDN Tree-Scan：Served Tree Verification
abstract_zh: "Tree speculative decoding verifies multiple candidate continuations in one target forward pass。\n\n核心做法：作者提出 GDN Tree-Scan，served verifier 用于 Gated-DeltaNet hybrid language 模型s integrated 为 vLLM。\n\n主要结果：Recurrent-hybrid language 模型s break this assumption：candidate row must also carry recurrent state th。\n\n值得关注的点在于：Empirical equivalence evidence is scoped to recurrent-oracle probability-rescore (p-rescore) closure。"
abstract_en: "Tree speculative decoding verifies multiple candidate continuations in one target forward pass. For attention-only transformers, the verifier mainly needs an ancestry mask. Recurrent-hybrid language models break this assumption: a candidate row must also carry the recurrent state that native sequential decode would have produced along its root-to-node path. Otherwise, a verifier can use a correct attention mask while still conditioning on an impossible recurrent history.   We present GDN Tree-Scan, a served verifier for Gated-DeltaNet hybrid language models integrated into vLLM. The system combines FlashAttention-2 tree-bias attention, branch-local GDN scan/replay, device-side multidraft commitment, and accepted-chain-only state publication. On the public Qwen3.6-27B-FP8 checkpoint, in a clean batch-one (B=1) SWE/Codex decode gate at temperature 0.6, a six-node root-branch tree increases committed tokens/event by 17.2% at near-native verify-forward time and reaches 23.88 token-weighted decode tokens/s versus 18.80 for native five-step MTP (E5), a 27.0% token-weighted decode-throughput gain. The per-request-equal latency view is +4.0%, and end-to-end task wall time remains prefill-heavy. Empirical equivalence evidence is scoped to recurrent-oracle probability-rescore (p-rescore) closure within the observed native flip floor, not a full distribution-distance proof."
authors: Zhiyuan Ma
date: 2026-09-20
journal: cs.LG
source_link: "https://arxiv.org/pdf/2609.23900v1"
pdf: "https://arxiv.org/pdf/2609.23900v1"
score: 6.0
tldr: 围绕《用于Recurrent-Hybrid Langu...》；作者提出 GDN Tree-Scan，served verifi...；Recurrent-hybrid language 模型s break...
tldr_cn: 围绕《用于Recurrent-Hybrid Langu...》；作者提出 GDN Tree-Scan，served verifi...；Recurrent-hybrid language 模型s break...
tldr_en: "Tree speculative decoding verifies multiple candidate continuations in one target forward pass. For attention-only transformers, the verifier mainly needs an ancestry mask."
selection_source: fresh_fetch
motivation: Tree speculative decoding verifies multiple candidate continuations in one target forward pass。
method: 核心做法：作者提出 GDN Tree-Scan，served verifier 用于 Gated-DeltaNet hybrid language 模型s integrated 为 vLLM。
result: 主要结果：Recurrent-hybrid language 模型s break this assumption：candidate row must also carry recurrent state th。
conclusion: 值得关注的点在于：Empirical equivalence evidence is scoped to recurrent-oracle probability-rescore (p-rescore) closure。
---

## 摘要
Tree speculative decoding verifies multiple candidate continuations in one target forward pass。

核心做法：作者提出 GDN Tree-Scan，served verifier 用于 Gated-DeltaNet hybrid language 模型s integrated 为 vLLM。

主要结果：Recurrent-hybrid language 模型s break this assumption：candidate row must also carry recurrent state th。

值得关注的点在于：Empirical equivalence evidence is scoped to recurrent-oracle probability-rescore (p-rescore) closure。

## Abstract
Tree speculative decoding verifies multiple candidate continuations in one target forward pass. For attention-only transformers, the verifier mainly needs an ancestry mask. Recurrent-hybrid language models break this assumption: a candidate row must also carry the recurrent state that native sequential decode would have produced along its root-to-node path. Otherwise, a verifier can use a correct attention mask while still conditioning on an impossible recurrent history.   We present GDN Tree-Scan, a served verifier for Gated-DeltaNet hybrid language models integrated into vLLM. The system combines FlashAttention-2 tree-bias attention, branch-local GDN scan/replay, device-side multidraft commitment, and accepted-chain-only state publication. On the public Qwen3.6-27B-FP8 checkpoint, in a clean batch-one (B=1) SWE/Codex decode gate at temperature 0.6, a six-node root-branch tree increases committed tokens/event by 17.2% at near-native verify-forward time and reaches 23.88 token-weighted decode tokens/s versus 18.80 for native five-step MTP (E5), a 27.0% token-weighted decode-throughput gain. The per-request-equal latency view is +4.0%, and end-to-end task wall time remains prefill-heavy. Empirical equivalence evidence is scoped to recurrent-oracle probability-rescore (p-rescore) closure within the observed native flip floor, not a full distribution-distance proof.
