Title: Embedding rooted blow-ups of tree posets

URL Source: https://arxiv.org/pdf/2609.23543v1

Published Time: Tue, 22 Sep 2026 01:28:48 GMT

Number of Pages: 13

Markdown Content:
# Embedding rooted blow-ups of tree posets 

Balázs Patkós ∗

Abstract 

A tree poset T is a poset whose Hasse diagram is a tree. Bukh proved that if a family 

F ⊆ 2[n] contains ( h(T ) − 1 + ε)( n  

> ⌊n
> 2⌋

) sets, then F contains a weak copy of T , where h(T )is the height of T , the number of elements in a longest chain of T . Several strengthenings and generalizations of this result have been obtained. We prove the following robust variant. For a tree poset T and x ∈ T , the b-blow-up T (x, b ) rooted at x is the tree poset that we obtain from T by replacing every element u by bd new elements, where d is the distance d(x, u ) in the Hasse diagram of T and an edge uv with v being closer to x is replaced by edges such that every new copy of v is joined to b new copies of u such that these new copies form pairwise disjoint sets for the copies of v. We prove that for any tree poset T , x ∈ T , and ε > 0 there exists δ such that if F ⊆ 2[n] contains ( h(T ) − 1 + ε)( n  

> ⌊n
> 2⌋

)

sets, then F contains a weak copy of T (x, ⌊δn ⌋). This settles a conjecture of Treglown and the author. As applications, we derive the known asymptotic counting and random versions of Bukh’s theorem from this stronger embedding result, and obtain new maximal anti-Ramsey results for tree posets. 

# 1 Introduction 

We use standard notation: [ n] denotes the set of the first n positive integers, and for any set S, we write 2 S and (Sk

) to denote its power set and the family of its k-element subsets, respectively. One of the starting points of extremal finite set theory is the theorem of Sperner stating that if F ⊆ 2[n] does not contain F, F ′ with F ⊆ F ′, then |F| ≤ ( n  

> ⌊n
> 2⌋

) and equality holds if and only if F = ( [n]  

> ⌊n
> 2⌋

) or F = ( [n]  

> ⌈n
> 2⌉

). To generalize forbidden containment patterns, Katona and Tarján [12] introduced the following definition: a family G of sets is a weak copy of the poset ( P, ⪯), if there exists a bijection ι : P → G such that p ⪯ q implies 

ι(p) ⊆ ι(q). We say that F is P -free if it does not contain any weak copies of P . Determining La( n, P ) = max {|F| : F ⊆ 2[n], F is P -free } is an extensively studied extremal problem, see [1, 10] and Chapter 7 of [9] for surveys. A natural way to construct a large P -free family is to consider as many middle layers of 2[n] as possible. Formally, let e(P ) denote the maximum integer k such that ∪ki=1 

( [n]  

> ⌊n−k
> 2⌋+i

) is 

P -free for all n. Then by definition, we have La( n, P ) ≥ (e(P )+ o(1)) ( n  

> ⌊n
> 2⌋

). It was conjectured that for posets P , it holds that e(P ) = lim n→∞ La( n,P )

( n  

> ⌊n
> 2⌋

) =: π(P ). (It is not known whether the limit π(P ) exists for all posets, so π−(P ) and π+(P ) are used for liminf and limsup, respectively.) This conjecture was refuted by Ellis, Ivan, and Leader [7], so it is interesting to see how far one can push the extremal result for posets with the property e(P ) = π(P ). Supersaturation, 

> ∗

HUN-REN Alfréd Rényi Institute of Mathematics and Department of Computer Science and Information Theory, Budapest University of Technology and Economics, email: patkos@renyi.hu 

1arXiv:2609.23543v1 [math.CO] 20 Sep 2026 counting results and probabilistic versions of the extremal problem were obtained in [2, 8, 11, 15]. The most important class of posets for which e(P ) = π(P ) was proved [5] is that of tree posets. The directed Hasse diagram −→

H (P ) of ( P, ⪯) is a directed graph with vertex set P and −→pq 

is an arc if and only if p ⪯ q and there does not exist z̸ = p, q with p ⪯ z ⪯ q. The undirected Hasse diagram H(P ) is obtained from −→

H (P ) by dropping the orientation of arcs. A tree poset 

is a finite nonempty poset whose undirected Hasse diagram is a tree. Its height h(T ) is the maximum number of elements in a chain. Note that for any tree poset T , e(T ) = h(T ) − 1holds. 

Theorem 1 (Bukh, [5]) . For any tree poset T , we have π(T ) = h(T ) − 1.

As mentioned earlier, Theorem 1 was strengthened in several directions. One important tool in some of the proofs of these results was to show that a family of size ( h(T ) − 1 + ε)( n  

> ⌊n
> 2⌋
> )

contains not only a copy of T , but a robust version of T . Here is the formal definition introduced in [15]. 

Definition 2. Let T be a tree poset and x ∈ T , and let d ≥ 2 be a positive integer. The 

d-blow-up T (x, d ) rooted at x is the tree poset whose Hasse diagram is defined as follows: for each u ∈ T , if u is at distance ρ from x in the undirected Hasse diagram of T , then 

u is replaced with dρ elements u1, u 2, . . . , u dρ

; furthermore, if uv is an edge of the Hasse diagram of T with v being at distance ρ − 1 from x in T , then the uis are partitioned into 

dρ−1 pairwise disjoint sets U 1, U 2, . . . , U dρ−1

each of size d, and for every j ∈ [dρ−1], vj is joined to all members of U j . The orientation of all such edges is the same as that of uv .The authors of [15] formulated a general conjecture that they proved for a special class of tree posets. Their results were extended to a different subclass of tree posets in [13]. 

Conjecture 3 (Conjecture 1.3 in [15]) . For every tree poset T and every ε > 0, there exist 

δ > 0 and n0, depending only on T and ε, such that the following holds for every n ≥ n0

and every x ∈ T . If F ⊆ 2[n] with |F| ≥ (h(T ) − 1 + ε)( n

> ⌊n/ 2⌋
> )

, then F contains a copy of 

T (x, ⌊δn ⌋).

Our main result confirms Conjecture 3. 

Theorem 4. Conjecture 3 is true. 

The proofs of tree poset results fall into two categories. Some of them [2, 4, 11, 14] follow and strengthen the original ideas of Bukh [5]: define a sequence T = T0 ⊃ T1 ⊃ · · · ⊃ Tℓ

of tree posets such that Tℓ and Tj−1 \ Tj are chains for all 1 ≤ j ≤ ℓ, and then embed Tℓ

first and then keep extending the embedding by adding the chain corresponding to Tj−1 \ Tj .Other proofs [8, 13, 15] (that so far worked only for special classes of tree posets) used a "graph theoretic" approach: they extended the embedding by always adding an edge . Our proof uses this second approach. The Lubell mass of a family is defined as 

λn(A) = ∑ 

> A∈A
> (

n

|A|

> )−1

.

If C is a uniformly random maximal chain in 2 [n], then λn(A) = E|A ∩ C | and λn(A) ≥|A| /( n

> ⌊n/ 2⌋
> )

. For sets A, B ⊆ [n], write dist( A, B ) = |A△B| for the Hamming distance of A

and B. Set BR(A) = {B ⊆ [n] : dist( A, B ) ≤ R}. All asymptotic statements are as n → ∞ ,with every parameter other than n fixed unless stated otherwise. 22 Proof of Theorem 4 

Let us describe briefly the main steps of the proof before getting into the details. The first step, Proposition 5, is the completely new element, the other two steps, Lemma 7 and Lemma 8 use the ideas of [13]. A bipartite containment graph B = B(A, B) has vertex set A ∪ B 

with A, B forming an edge if and only if A ⊆ B. Note that the definition is not symmetric, so B(A, B)̸ = B(B, A). Proposition 5 states that a family F of size ( h + ε)( n   

> ⌊n
> 2⌋
> )

contains subfamilies F0, F1, . . . , Fh such that the minimum degree in all Bi = B(Fi−1, Fi) is large. Previous proofs [8, 13] could only establish that the minimum degree is large in all A parts or in all B parts and so those proofs only worked for monotone tree posets (tree posets with a smallest or a largest element). The second step, Lemma 7, settles the case when in all graphs B(Fi−1, Fi) the edges correspond to pairs A ⊆ B with bounded |B \ A|. Then Lemma 8 handles the case where for some i B (Fi−1, Fi) has large set differences on its edges. As mentioned before, the ideas of these lemmas are from [13]. 

Proposition 5. Fix integers h ≥ 1, Z ≥ 2, and a real ε > 0. There exist c > 0 and n0,depending only on h, Z, ε , such that for n ≥ n0 every family F ⊆ 2[n] with |F| ≥ (h + ε)( n

> ⌊n/ 2⌋
> )

contains nonempty, pairwise disjoint families V0, . . . , Vh and bipartite containment graphs 

Bi = B(Vi−1, Vi), for 1 ≤ i ≤ h, with the following properties. Each graph has a type 

ti ∈ { 1, . . . , Z, +}, and: 1. if ti = j ≤ Z, all edges have size difference exactly j, and Bi has minimum degree at least cn j ;2. if ti = + , all edges have size difference greater than Z, and Bi has minimum degree at least cn Z−1.Proof. Let A = {F ∈ F : || F | − n/ 2| < n 2/3}. The binomial tail bound shows that 

|F \ A| ≤ |{ G ⊆ [n] : || G| − n/ 2| ≥ n2/3}| = o(( n

> ⌊n/ 2⌋
> )

). Thus, for sufficiently large n,

|A| ≥ (h + ε

2 ) 

> (

n

⌊n/ 2⌋

> )

, λ := λn(A) ≥ h + ε

2 . (1) Let m = ⌊n/ 3⌋ and define Dj = (mj 

> )

(1 ≤ j ≤ Z), D+ = 1 

> n2
> (mZ+1
> )

. For every strict containment A ⊂ B in A, define its type by 

τ (A, B ) = 

> {

|B| − | A|, |B| − | A| ≤ Z, 

+, |B| − | A| > Z. 

Let A, B ∈ A be distinct comparable sets. For a uniformly random maximal chain C ,Pr( B ∈ C | A ∈ C ) = 

> (

n − | A||B \ A|

> )−1

, A ⊂ B,  

> (

|A||A \ B|

> )−1

, B ⊂ A. 

The upper binomial parameter is at least m, while |A△B| ≤ 2n2/3 < m/ 2 for sufficiently large n. Monotonicity of binomial coefficients therefore gives, if the edge has type t,Pr( B ∈ C | A ∈ C ) ≤ D−1 

> t

.

3For type + the stronger bound ( mZ+1  

> )−1

= ( n2D+)−1 holds. In particular, if for some real θ,fewer than θD t edges of one type t are assigned to A, their total expected contribution to the number of assigned edges with both ends on C is at most 

θ Pr( A ∈ C ). (2) Color every member of A independently and uniformly with colors 0 , . . . , h . For a maximal chain C , list its members of A in increasing order as A1 ⊂ · · · ⊂ AX , X = |A ∩ C |. Partition this list into consecutive blocks of length h + 1, ignoring the final incomplete block. A block is good if its colors, in increasing order, are exactly 0 , 1, . . . , h .Put p = ( h + 1) −h−1. For every fixed maximal chain, each block is good with probability 

p. If gχ(C ) denotes the number of good blocks under a coloring χ, then 

EχEC gχ(C ) = p EC 

> ⌊

Xh + 1 

> ⌋

≥ ph + 1 

> (

EC X − h) = ph + 1 (λ − h).

Here ⌊ Xh+1 ⌋ ≥ X−hh+1 for every nonnegative integer X. Fix a coloring χ attaining at least the average, so that 

EC gχ(C ) ≥ ph + 1 (λ − h). (3) We aim to choose Vi from the ith color class. There are W = ( Z + 1) h type sequences t = ( t1, . . . , t h). Choose the positive constant 

θ = p ε    

> 2
> 2( h+1) W(h+ε
> 2)

. For each sequence, start a fresh pruning process on all the initial color classes. Between colors i − 1 and i, use all containments of type ti. Repeatedly delete any set having fewer than θD ti current neighbors in one of its required adjacent parts. A set in color class i with 1 ≤ i ≤ h − 1 has a requirement on each side; a set in color class 0 or h has only its one adjacent requirement. At each deletion, choose one deficient direction and record all edges from the deleted set to its currently surviving neighbors in that direction. Assign those recorded edges to the deleted set. Record only this one direction, even if both are deficient. All choices are made with the fixed coloring; the deletion order and recorded edge sets do not depend on the maximal chain used in the expectations. If a process terminates with any surviving sets, there are survivors in every part: each survivor has a positive number of neighbors in every required adjacent part, and this propagates along the path of parts. Its surviving graphs have minimum degrees at least θD ti

at both ends. These give the required families V0, V1, . . . , Vh by the definition of Dj s and by 

Dj = Θ( nj ) and D+ = Θ( nZ−1). Suppose, for a contradiction, that every process deletes all A. Let Et be the recorded edges for one sequence, and let E = ⋃ 

> t

Et. For any edge set J put 

NJ (C ) = |{{ A, B } ∈ J : A, B ∈ C }| .

In a fixed process each set is deleted once, recording fewer than θD t edges of one type. Each recorded edge is assigned to the endpoint deleted when it was recorded. Summing (2) over the sets of A yields 

EC NEt (C ) ≤ θ ∑

> A∈A

Pr( A ∈ C ) = θλ. 

An edge may occur in several sequences, which only increases the sum on the right in the following union bound: 

EC NE (C ) ≤ W θλ. (4) 4Consider a chain A0 ⊂ A1 ⊂ · · · ⊂ Ah with colors 0 , 1, . . . , h , respectively. Its consecutive edges determine the sequence ti = τ (Ai−1, A i). In the process for this sequence, consider the first of these h + 1 sets to be deleted. All other sets of the chain are still present. In the chosen deficient direction the chain has an adjacent set, and their edge is therefore recorded. Hence every such colored chain has at least one edge in E that connects two of its consecutive sets. In particular, every good block on C contains a recorded edge. Different blocks live on disjoint subchains of C , so these edges are distinct. Thus gχ(C ) ≤ NE (C ) for every maximal chain. By (3) and (4), ph + 1 (λ − h) ≤ W θλ. (5) But (1) implies λ − h ≥ ε 

> 2

λ/ (h + ε 

> 2

), and consequently the left side of (5) is at least 

p ε

> 2

(h + 1)( h + ε 

> 2

) λ = 2 W θλ, 

contrary to (5). Some type sequence therefore has a nonempty surviving system. Its parts are disjoint because they belong to different color classes. For large n and 1 ≤ j ≤ Z + 1, (mj 

> )

≥ (n/ 4) j /j !. Thus a common admissible constant in the statement is, for example, 

c = θ

4Z+1 (Z + 1)! .

This constant depends only on h, Z, ε .

Remark 6. The case h = 1 of Proposition 5 is implicitly proved in [13] in a much simpler way. The case h = 2 can also be proved along those lines: for a family F ⊆ { G ⊆ [n] : || G| − n/ 2| ≤ 

n2/3} with |F| ≥ (2 + ε)( n   

> ⌊n
> 2⌋
> )

let us define F2 := {F ∈ F : λ|F |(FF, −) ≥ 1 + ε/ 3} and 

F0 := {F ∈ F : λn−| F |(FF, +) ≥ 1 + ε/ 3}, where FF, − = {F ′ ∈ F : F ′ ⊆ F } and FF, + = {F ′ \

F : F ⊆ F ′ ∈ F} . A standard argument from [8] shows that λn(F \F 2), λ n(F \F 0) ≤ (1+ ε/ 3) and thus |F 2|, |F 0| ≥ (1 + 2 ε/ 3) ( n  

> ⌊n
> 2⌋
> )

. Therefore F1 = F2 ∩ F 0 has size at least ε   

> 3
> (n
> ⌊n
> 2⌋
> )

. All degrees of F1 to both F0 and F2 are high by the Lubell mass conditions, and the standard graph theory proof yields the desired subgraphs B1 and B2.A finite poset Q is saturated if every maximal chain has h(Q) elements. For a saturated poset of height t + 1, write Q0, . . . , Q t for its natural levels, i.e. Qi is the set of minimal elements in Q \ ∪ i−1

> j=0

Qj , and let ℓ(u) = i for u ∈ Qi. As a result of saturation, every Hasse edge joins consecutive levels and is oriented upward. We use this notation for saturated tree posets throughout the rest of the proof. 

Lemma 7. Let Q be a fixed saturated tree poset of height t + 1 , with levels Q0, . . . , Q t, and fix a root y ∈ Q. Let V0, . . . , Vt ⊆ 2[n] be nonempty, pairwise disjoint families. For each 

1 ≤ i ≤ t, fix a positive integer ji and a bipartite graph Bi = B(Vi−1, Vi), all of whose edges are containments A ⊂ B with |B| − | A| = ji. Suppose Bi has minimum degree at least cn ji ,where c > 0 is fixed, and give each edge of Bi the weight ji; thus w(uv ) = jmax {ℓ(u),ℓ (v)}.There are β > 0 and n0, depending only on the fixed data, such that for n ≥ n0 and any prescribed A ∈ V ℓ(y), the blow-up Q(y, ⌊βn ⌋) has an injective order-preserving embedding sending its root to A, with level i represented in Vi and every edge between consecutive levels embedded using Bi.Moreover, put 

R = max  

> v∈Q
> ∑
> e∈P(y,v )

w(e),

5where P (y, v ) is the unique root-to-v path. The embedding can be chosen in BR(A), with pairwise disjoint sets of changed coordinates along each root-to-vertex path. The same conclusion holds for every integer 1 ≤ b ≤ βn in place of ⌊βn ⌋.Proof. The case |Q| = 1 is immediate. Suppose q = |Q| ≥ 2. Color every set in BR(A) \ { A}

independently and uniformly with the q original vertices of Q, and give A color y.Regard an edge as a rooted edge uv , with u the parent, regardless of its containment direction. Put i = max {ℓ(u), ℓ (v)} and j = ji = w(uv ). Thus u, v lie on consecutive levels and their edge in an embedding should use b ≤ ⌊ βn ⌋ edges of Bi. For any P ∈ V ℓ(u) with dist( P, A ) ≤ R − j, all its Bi-neighbors in Vℓ(v) lie in BR(A). Apart from possibly A, their auxiliary colors are independent and uniform. Since there are at least cn j neighbors, Chernoff’s inequality gives probability e−Ω( nj ) ≤ e−Ω( n) of having fewer than cn j /(4 q) neighbors of color 

v. There are only O(nR) sets in BR(A) and constantly many rooted edges. A union bound gives a coloring satisfying all these requirements simultaneously. For an original vertex u ∈ Q, put 

s(u) = ∑ 

> e∈P(y,u )

w(e).

We now start embedding Q(y, b ). We fix an ordering y = u1, u 2, . . . , u q of Q such that vertices of Py,u i precede ui for all i. We first place y to A and then embed all copies of vertices in the fixed order. All copies of u ∈ Q are embedded at once using only sets of auxiliary color u.Whenever H is chosen as a child of P , require (H△P ) ∩ (P △A) = ∅. (6) This implies 

H△A = ( P △A) ˙ ∪ (H△P ). (7) Thus all copies of u have distance exactly s(u) from A.Fix a nonroot original vertex v, let u be its parent, and suppose all copies of u are already embedded. Put j = w(uv ). Since s(u) + j ≤ R, every parent set P has at least cn j /(4 q)neighbors of color v. At most 

R 

> (

nj − 1

> )

= O(nj−1)of them change an element in P △A, since H is determined by H△P once P is given. For large n, each parent therefore has at least cn j /(8 q) eligible candidates satisfying (6). For each distinct candidate H, assign it independently and uniformly to one of its eligible parents. By (7), any such parent P satisfies P △A ⊆ H△A. Since |H△A| ≤ R, a candidate has at most 2 R eligible parent sets. Every parent receives an expected number of candidates at least cn j /(8 q2R). Its received count is a sum of independent Bernoulli variables, and Chernoff’s inequality gives Pr 

> (

it receives fewer than c

8q2R+1 n candidates 

> )

= e−Ω( n).

Choose 0 < β ≤ min {1, c/ (8 q2R+2 )} and take any integer 1 ≤ b ≤ βn . There are at most 

qn q−1 parents in Q(y, b ). A union bound therefore shows that an allocation giving every parent at least b candidates exists. Pick b candidate for each parent. Perform this allocation once for each original nonroot vertex v, with all its copies allocated in a single batch. No set of color v has been used at an earlier stage: v has a unique original parent, and different original vertices use different colors. The allocation makes copies of v

distinct. This proves injectivity, and (7) establishes the radius bound. 6The estimates and the lower bound on n are uniform over the prescribed root A and over host graphs satisfying the stated degree bounds. In particular, the root may be selected using an earlier part of an embedding. (This will be important in the proof of Lemma 8!) For q ≥ 2 one may take 

β = min 

> {

1, cq 2R+5 

> }

. (8) 

Lemma 8. Let S be a fixed saturated tree poset of height h + 1 ≥ 2, with levels S0, . . . , S h.Fix x ∈ S, put s = |S|, and define integers 

K0 = 1 , Ka+1 = 2( s − 1) Ka + s (0 ≤ a ≤ h), Z = Kh+1 + 2 . (9) 

Suppose there are nonempty, pairwise disjoint host families V0, . . . , Vh and graphs Bi with the properties in Proposition 5, for this Z and some fixed c > 0.Then, for some δ > 0 and all sufficiently large n, ∪hi=0 Vi contains a weak copy of 

S(x, ⌊δn ⌋), with level i represented in Vi. The constants can be chosen uniformly over x and all type sequences. Proof. There are h + 1 disjoint intervals ( K0, K 1], (K1, K 2], . . . , (Kh, K h+1 ], but at most h

bounded types among the graphs B1, . . . , B h. Choose a ∈ { 0, . . . , h } for which ( Ka, K a+1 ]contains none of these bounded types. Call a relation of S short if it has bounded type 

j ≤ Ka, and long otherwise. Because the chosen interval is empty, every long relation has either bounded type j > K a+1 or type +. Its minimum degree is at least cn Ka+1 +1 . For type + this follows from Z − 1 = Kh+1 + 1 ≥ Ka+1 + 1. The idea is to break up S into components with only short edges and embed them using Lemma 7. Then for long edges use the fact that the minimum degrees in the corresponding 

Bi are very large to obtain that the centers of the balls used by the embeddings guaranteed by Lemma 7 can be placed so far from each other that the balls are pairwise disjoint and therefore the whole embedding is injective. Delete from H(S) all edges of long relations. Each subposet Q of S corresponding to a component of the remainder lies in a maximal consecutive block of levels p, . . . , q whose intervening relations are all short. Q is itself saturated, of height q − p + 1. Indeed, saturation of S allows every vertex of Q to be extended downward to level p and upward to level q

along a maximal chain of S. These chains use only short edges and therefore stay in Q.Consequently every maximal chain of Q starts on level p, ends on level q, and has q − p + 1 elements. After shifting level indices, the natural levels of Q are Q ∩ Sp, . . . , Q ∩ Sq, with host families Vp, . . . , Vq. Every edge of Bp+1 , . . . , B q uses a bounded size difference at most 

Ka, so Lemma 7 applies in precisely this setting. Root S at x, and for every component Q give Q the unique root y ∈ Q closest to x.Put L = ( s − 1) Ka. Remember that the weight of an edge in Bi is the type of Bi. As all its edges are short, every weighted root-to-vertex path in Q has total weight at most L.Lemma 7 embeds Q(y, b ) from any prescribed root set A ∈ V ℓ(y) inside BL(A), for b = ⌊δn ⌋

and sufficiently small fixed δ > 0. This choice can be made uniformly. For example, since 

|Q| ≤ s and its weighted radius is at most ( s − 1) Z, (8) permits 

δ = min  

> {

12 , cs 2(s−1) Z+6 

> }

. (10) Isolated components need no restriction. There are only finitely many rooted component types and bounded weight assignments under consideration, so a common lower bound on n

also suffices. 7Deleting the corresponding long edges from S(x, b ) leaves components of the form Q(y, b ). Indeed, every component root is either the root of S(x, b ) or a child reached through a long edge; every remaining original edge still supplies exactly b children away from its component root. Joining these components to a root in the direction inherited from the deleted long edge gives a rooted tree. Now we start embedding ( x, b ). Fix an ordering x = y1, y 2, . . . , y k of the roots of all components Q such that dist H(S)(x, y i) ≤ dist H(S)(x, y j ) for all i ≤ j ≤ k. A root yi should have bdist H(S)(x,y i) copies. We intend to embed them such that all image sets Aαi (1 ≤ i ≤ k,1 ≤ α ≤ bdist H(S)(x,y i)) of all roots have pairwise Hamming distance greater than 2 L. Then the balls BL(Aαi ) are pairwise disjoint. Start at any set in Vℓ(x), and embed the blow-up of the entire short-edge component of x using Lemma 7. Suppose we have embedded all the blow-ups of all components with roots y1, . . . , y i−1.Then for yi there is a unique v ∈ S a neighbor of yi in H(S) that lies on the path from yi

to x. Then, by definition of the roots and the order y1, . . . , y k, all required copies of v in 

S(x, b ) have already been embedded, and yiv is a long edge of S. Writing d = dist H( S)(x, v ), in S(x, b ) there are bd = Θ( nd) copies of v, and each has b children that are copies of yi. All these bd+1 = Θ( nd+1 ) children are roots of new components. The parent copies may lie in fewer than bd old components, since an old component can contain several copies of v. We attach the new components one by one . At each choice the condition of having Hamming distance greater than 2 L is imposed against every component root selected so far, including all roots already selected for copies of yi.Suppose a new component must be attached by the long edge yiv to an already embedded parent set P , the image of a copy of v. The corresponding graph Bj provides at least cn Ka+1 +1 

candidate root sets, by its minimum degree condition, in the required containment direction. The total number of already selected roots is at most 

|S(x, b )| = ∑

> u∈S

bdist H(S)(x,u ) ≤ sb s−1 ≤ sn s−1. (11) Each forbids at most |B 2L(Aαi )| = ∑2Li=0  

> (ni
> )

= O(n2L) candidates, so the total number of forbidden sets is O(ns−1+2 L), whereas 

Ka+1 + 1 = 2( s − 1) Ka + s + 1 = s + 2 L + 1 .

Consequently a candidate A outside all the forbidden balls exists. Choose A as the new component root and apply Lemma 7 inside BL(A). This ball is disjoint from every previously used component ball, so the component embedding uses no previously chosen set. Lemma 7 is uniform over the prescribed root, so this adaptive choice causes no additional condition. Repeat for every required long child. The bound (11) remains valid throughout. Embed-dings are injective within components by Lemma 7, and between components by disjointness of their balls. Short edges are respected by the local embeddings, and long edges by the choice of component roots. Finally, s, Z are independent of x, and (10) is uniform over x and the type sequence. The remaining lower bounds on n can be made uniform by taking a maximum over finitely many possibilities. To prove Theorem 4, we will need to show that the blow-up of a tree poset is a subposet of the blow-up of a saturated tree poset. This is mainly a result of Bukh from his paper [5]. For this reason we just sketch the proof of the following lemma. A poset ( P, ⪯) is a 

strong subposet of ( Q, ⪯′) if there exists an injection ι : P → Q with p ⪯ p′ if and only if 

ι(p) ⪯′ ι(p′). A family G of sets is a strong copy of ( P, ⪯) if there exists a bijection ι : P → G 

such that p ⪯ p′ if and only if ι(p) ⊆ ι(p′). 8Lemma 9. 

1. [5, Lemma 5] For any tree poset T there exists a saturated tree poset S with h(T ) = h(S)

such that T is a strong subposet of S.2. If x ∈ T ⊆ S for tree posets as in part (i), then T (x, b ) is a strong subposet of S(x, b )

for any b ≥ 1.Sketch of proof. Consider the level partition Q0, Q 1, . . . Q h−1 of T and write ℓT (u) = i if 

u ∈ Qi as before. For every arc −→uv of −→

H (T ) subdivide the arc by ℓT (v) − ℓT (u) − 1 vertices and if u ∈ T is a maximal vertex with ℓT (u) < h − 1, then append an upward directed path with h − 1 − ℓT (u) vertices to u. The resulting poset S is clearly a tree poset with 

h(S) = h(T ), and ℓT (u) = ℓS (u) for all u ∈ T . Also, by construction, for any pair u, u ′ ∈ T ,we have dist H(T )(u, u ′) ≤ dist H(S)(u, u ′). To see part (2), one uses this distance property. We fix an order x = u1, u 2, . . . , u |T | of T

such that i < j implies dist H(T )(ui, y ) ≤ dist H(T )(uj , y ). We embed copies of ui in T (y, b ) into copies of ui in S(y, b ). If all copies of all ui have been embedded for all i < j , then let uij be the neighbor of ( uj on the uj x-path in H(T ). The edge uij uj of T is replaced in S by a path of length dist H(S)(uij , u j ) ≥ 1. For every already embedded copy of uij , the corresponding rooted path in ( x, b ) has bdist H(S)(uij ,u j ) ≥ b distinct terminal copies of uj . Moreover, the sets of terminal copies arising from distinct copies of uij are disjoint. We may therefore select b of them for every copy of uij , obtaining the required copies of uj . Repeating this inductively gives a strong embedding of T (x, b ) into S(x, b ). 

Proof of Theorem 4. If h(T ) = 1, the tree poset has one vertex, and the conclusion is immediate for sufficiently large n. Suppose h(T ) = h + 1 ≥ 2. By Lemma 9, it suffices to prove the assertion for the saturated tree poset S. Choose Z

from (9) for this S and h. Apply Proposition 5 to F, obtaining compatible host graphs with a constant c = c(ε, h, Z ) > 0. Lemma 8 embeds S(x, ⌊δn ⌋) for some fixed δ > 0, and then 

T (x, ⌊δn ⌋) ⊆ S(x, ⌊δn ⌋). The saturated extension S and the parameters h, |S|, Z depend only on T . The constant of Proposition 5 depends only on ε, h, Z , and the constants of Lemma 7 and Lemma 8 are uniform over the finitely many original roots x ∈ T . This gives δ and n0 depending only on 

T, ε , as required. 

# 3 Corollaries 

In this section we gather some known and new consequences of Theorem 4. First, let Forb( n, T )denote the collection of weak T -free subfamilies of 2 [n]. As all subfamilies of a T -free family are T -free, we immediately obtain | Forb( n, T )| ≥ 2La( n,T ) = 2 (h(T )−1+ o(1)) ( n  

> ⌊n
> 2⌋

). The next theorem was already proved in [2] and in a stronger form in [11], but it is immediately implied by Theorem 4 and a result from [15]. 

Theorem 10. For every fixed tree poset T of height h,

| Forb( n, T )| = 2 (h−1+ o(1)) ( n  

> ⌊n
> 2⌋

).

Proof. Theorem 1.5 of Patkós and Treglown [15] states that if the linear blow-up conjecture holds for a tree poset T and at least one root x ∈ T , then 

| Forb( n, T )| = 2 (h(T )−1+ o(1)) ( n  

> ⌊n
> 2⌋

).

9Theorem 4 establishes their hypothesis for every tree poset and, in fact, for every choice of the root. The theorem is therefore an immediate consequence of their result. Next we consider the random version of the forbidden subposet problem. Let P(n, p ) be the random subfamily of 2 [n] obtained by retaining every set independently with probability p.Write La( P(n, p ), T ) for the largest size of a T -free subfamily of P(n, p ). The next theorem was proved in a stronger form, but with completely different proof in [11]. 

Theorem 11. Let T be any fixed tree poset of height h ≥ 2. If p = p(n) satisfies pn → ∞ ,then, with high probability, 

La( P(n, p ), T ) = (h − 1 + o(1) )p 

> (

n

⌊ n 

> 2

⌋

> )

.

The proof uses the two-stage graph-container algorithm of [15] (which in turn originates in the proof for the case T being a chain in [3]). The only part of the argument that was specific to trees of radius at most two was the first-stage supersaturation lemma. The following observation supplies its replacement. 

Lemma 12. For every fixed tree poset T of height h, every x ∈ T , and every ε > 0, there is a constant c = c(T, x, ε ) > 0 such that every A ⊆ 2[n] with 

|A| ≥ (2 h − 2 + ε) 

> (

n

⌊ n 

> 2

⌋

> )

contains a weak copy of T (x, cn 2).Proof. Let T + be obtained from T by subdividing every edge of H(T ) once, orienting both new edges consistently with the original edge. The Hasse diagram of T + is again a tree. Every directed path with h − 1 edges in T becomes a directed path with 2 h − 2 edges in 

T +, and contracting the subdivision vertices maps every directed path of T + to one in T .Consequently, h(T +) = 2 h − 1. Regard x as a vertex of T +. By Theorem 4, A contains 

T +(x, δn ) for some δ > 0. If u ∈ T is at distance r from x in H(T ), then it is at distance 2 r from x in H(T +). Hence 

T +(x, d ) contains d2r copies of u. Moreover, along every original edge, the two successive 

d-fold branchings combine into one d2-fold branching. Keeping only the vertices corresponding to the original vertices of T therefore gives the containment T (x, d 2) ⊆ T +(x, d ). Taking 

d = δn proves the lemma with, for example, c = δ2/2 for all sufficiently large n.

Proof of Theorem 11. Fix x ∈ T and ε > 0. For ε/ 4, Theorem 4 and Lemma 12 yield δ and 

c, respectively. Let η = min {δ, c }. We describe the two-stage container lemma obtained by running the algorithm from [15]. We fix an order O of sets in 2 [n], an order T = t1, t 2, . . . , t |T |

of elements of T that starts with x = t1 and for every i T [t1, . . . , t i] is a tree poset, and for every d ≥ 1 we fix an order Td of weak copies of T (x, d ) in 2 [n]. The input of the algorithm is a T -free family F ⊆ 2[n]. We start by setting G0 = 2 [n], H0 = ∅.Then for every i if Gi−1, Hi−1 are defined, then in Step i, we consider the largest d such that Gi−1 contains a copy of T (x, d ) and fix the copy T that comes first in Td. If the set Gx

corresponding to x in T is not in F, then we let Gi = Gi−1 \ { Gx}, Hi = Hi−1 and proceed to Step i + 1. If Gx ∈ F , then we start building a copy of T in T ∩ F in the fixed order T. Suppose we have found Gx = Gt1 , . . . , G tj−1 in T ∩ F such that these form a copy of T [t1, . . . , t j−1]and let tℓ be the parent of tj , i.e. the neighbor of tj on the tj x-path in H(T ). Consider the 10 d neighbors of Gtℓ in T corresponding to tj in the order O. If any of them belongs to F,then let Gtj be the first one in O, remove all neighbors preceding Gtj from Gi−1 and proceed to find Gtj+1 . If no neighbor belongs to F, then we remove all neighbors and Gt1 , . . . , G tj−1

from Gi−1 and whatever remains is Gi and set Hi = Hi−1 ∪ { Gt1 , G t2 , . . . , G tj−1 }. As F is 

T -free, there will be a j for which Gtj is not defined. So either we just remove Gx from Gi−1

or we move at most |T | − 1 sets from Gi−1 ∩ F to Hi and remove further at least d sets from 

Gi−1. This implies that Hi ⊆ F for all i. We terminate the algorithm once d drops below ηn .Let this happen at step i2.We analyze the algorithm in two stages. Let i1 be the first index when d drops below ηn 2

and let H1 = Hi1 . By the above, we included new sets in H1 in at most 2 n/(ηn 2) steps, so 

|H 1| = OT,η (2 n/n 2). By Lemma 12, |G i1 | ≤ (2 h − 2 + ε/ 4) ( n  

> ⌊n
> 2⌋

). Similarly, in the next and final stage, there are at most |G i1 |/(ηn ) steps when new sets are included into Hi. Therefore letting H2 = Hi2 \ H 1 we have |H 2| = OT,η (( n  

> ⌊n
> 2⌋

)/n ) and H1, H2 ⊆ F . Also, by Theorem 4, for g(H1 ∪ H 2) := Gi2 we have |g(H1 ∪ H 2)| ≤ (h − 1 + ε/ 4) ( n  

> ⌊n
> 2⌋

) and as we never removed sets of F without moving them to Hi, we have F ⊆ H 1 ∪ H 2 ∪ g(H1 ∪ H 2). Since the algorithm worked with fixed orders O, T, Td, it follows as in [15] (and in [3]) that although H1 and H2

may be the same for different T -free families F, g(H1 ∪ H 2) depends only on H1 and H2 and not on F. Also, Gi1 depends only on Hi1 , so H2 is a subset of a set depending on H1 of size at most C2

( n  

> ⌊n
> 2⌋

)/n .To bound P(∃F ⊆ P (n, p ) T -free with |F| ≥ p(h − 1 + ε)( n  

> ⌊n
> 2⌋

)), we bound the probability of the event that for some H1 and H2, we have H1 ⊆ P (n, p ), H2 ⊆ P (n, p ) and |P (n, p ) ∩

g(H1 ∪ H 2)| ≥ p(h − 1 + ε/ 2) ( n  

> ⌊n
> 2⌋

). Note that pn → ∞ implies |H 1|, |H 2| = o(p( n  

> ⌊n
> 2⌋

)) and so a T -free family of size p(h − 1 + ε)( n  

> ⌊n
> 2⌋

) should contain at least p(h − 1 + ε/ 2) ( n  

> ⌊n
> 2⌋

) sets from g(H1 ∪ H 2). As H1, H2, g (H1 ∪ H 2) are pairwise disjoint, for fixed H1 and H2, these are independent events, and by the Chernoff bound the probability of the last one is at most 

e−ε2p( n  

> ⌊n
> 2⌋

)/(100 h2) as E|P (n, p ) ∩ g(H1 ∪ H 2)| ≤ p(h − 1 + ε/ 4) ( n  

> ⌊n
> 2⌋

). Therefore, taking the union bound for each possible H1, H2, we obtain the upper bound 

∑

> a≤C12n/n 2

(2n

a

)

pa ∑

> b≤C2

( n  

> ⌊n
> 2⌋

)/n 

((2 h − 2 + ε/ 4) ( n  

> ⌊n
> 2⌋

)

b

)

pbe−ε2p( n  

> ⌊n
> 2⌋

)/(100 h2)

For the first sum, we have 

∑

> a≤C12n/n 2

(2n

a

)

pa ≤ exp 

(

O

( 2n

n2 log n

)) 

= exp( o(p

( n

⌊ n 

> 2

⌋

)

)) ,

because ( n  

> ⌊n
> 2⌋

) = Θ(2 n/√n) and 2nn−2 log np( n  

> ⌊n
> 2⌋

) = O

( log npn √n

)

= o(1). For the second sum, we have 

∑

> b≤C2

( n  

> ⌊n
> 2⌋

)/n 

((2 h − 2 + ε/ 4) ( n  

> ⌊n
> 2⌋

)

b

)

pb ≤ exp 

O

( n  

> ⌊n
> 2⌋

)

n log(2 + pn )

 = exp( o(p

( n

⌊ n 

> 2

⌋

)

)) ,

as log(2 + pn )/(pn ) = o(1). As the negative exponent in the probability that |g(H1 ∪H 2)∩P (n, p )| ≥ p(h−1+ ε/ 2) ( n  

> ⌊n
> 2⌋

)

is Ω( p( n  

> ⌊n
> 2⌋

)), we get, with high probability, every T -free subfamily of P(n, p ) has size at most (h − 1 + ε)p( n  

> ⌊n
> 2⌋

).For the lower bound, the union of the middle h − 1 levels is T -free and has size ( h − 1 + 

o(1)) ( n  

> ⌊n
> 2⌋

). Its intersection with P(n, p ) has size ( h − 1 − o(1)) p( n  

> ⌊n
> 2⌋

) with high probability by Chernoff’s inequality. Since ε > 0 was arbitrary, the result follows. 11 The final consequence of Theorem 4 we consider is about maximal anti-Ramsey numbers of tree posets. Following the graph-theoretic problem of Burr, Erdős, Graham, and Sós [6], in [13] for non-negative integers n, m and a poset P with La( n, P ) < m ≤ 2n, the maximal anti-Ramsey number ar m(n, m, P ) is the smallest integer k such that for any F ⊆ 2[n] of size 

m there exists a coloring using k colors with all copies of P in F being rainbow (all elements receiving distinct colors). This is easily seen to be min {χ(GP (F)) : F ⊆ 2[n], |F| = m}, where 

GP (F) is the graph with vertex set F and F, F ′ ∈ F being joined by an edge if and only if F

contains a copy G of P with F, F ′ ∈ G . For a tree poset T one can consider the family Mh of 

h = h(T ) middle layers, i.e. Mn,h = ∪hi=1    

> ([n]
> ⌊n−h
> 2⌋+i
> )

. By symmetry, for two sets F, F ′ ∈ M n,h 

whether {F, F ′} is an edge in GT (Mn,h ) depends only on |F ∩F ′|, so GT (Mn,h ) is a generalized Johnson graph. As shown in [13], by known results on Johnson graphs, for every tree poset there exists an integer mT such that χ(GT (Mn,h )) = Θ( nmT ). It is conjectured in [13] that for any ε > 0 there exists δ > 0 such that ar m(n, (h − 1 + ε)( n  

> ⌊n
> 2⌋
> )

, T ) ≥ δn mT . The authors prove their conjecture for tree posets T that contain an element comparable to all other elements of T and that have a so-called branching property. We claim that Theorem 4 implies the conjecture for further tree posets. Instead of defining a not very natural property that would suffice to prove the statement, we just show an example. 

x ab c de f z

Figure 1: a tree poset with no element being comparable to all other elements For the tree poset T in Figure 1, we have mT = 2. Two sets F, F ′ ∈ ( [n]   

> ⌊n−32⌋+i
> )

are joined in GT (Mn,h ) if and only if |F △F ′| equals the graph distance of two elements with rank i

(ranks are from 1 to 3). Equivalently, two sets F, F ′ of rank i are joined if |F ∩ F ′| = |F | − 2or for i = 2 , 3 if |F ∩ F ′| = |F | − 1. So all layers have chromatic number Θ( n2). Now consider a family F ⊆ 2[n] of size (2 + ε)( n  

> ⌊n
> 2⌋
> )

. By Theorem 4 it contains a copy T

of the blow-up T (x, δn ) for some δ > 0. There are ( δn )2 sets Z in T corresponding to z. It is easy to see that for any two elements z1, z 2 ∈ T (x, δn ) corresponding to z, there exists a copy of T in T (x, δn ) that contains z1, z 2. Indeed, if z1, z 2 have a common neighbor c1, then with 

c3, z3, d3, a3 forming a path in T (x, δn ) and c1̸ = c3, we have a bijection ι : T → T (x, δn )with ι(e) = z1, ι (f ) = z2, ι (b) = c1, ι (x) = x, ι (c) = c3, ι (z) = z3, ι (d) = d3, ι (a) = a3.Finally, if the shortest path between z1, z 2 in T (x, δn ) passes through x and c1, c 2 are the other two elements of the path with ci being a neighbor of zi, then the mapping 

ι(x) = x, ι (b) = c1, ι (e) = z1, ι (c) = c2, ι (z) = z2 can be extended to a bijection. This shows that Z forms a clique in GT (T ) and thus χ(GT (T )) ≥ (δn )2.

AI declaration : The author had a proof of Proposition 5 in the special case h = 3 (as sketched in Remark 6), the general proof was found by ChatGPT 5.6. All other mathematical ideas and proofs are due to the author. The first draft of the paper was written by ChatGPT which was then checked and re-written by the author who claims full responsibility of the content. 12 References 

[1] M. Axenovich, R.R. Martin, B. Patkós, Extremal poset theory, in A. Gagarin, R. Behrend, M. Lettington, and I. Aliev (eds.), Surveys in Combinatorics 2026 , London Mathematical Society Lecture Note Series 505, Cambridge University Press: 31–74, 2026. [2] J. Balogh, R.I. Garcia, M.C. Wigal, On the number of P -free set systems for tree posets 

P , Order , 42(3):593–605, 2025. [3] J. Balogh, R. Mycroft, A. Treglown, A random version of Sperner’s theorem, Journal of Combinatorial Theory, Series A , 128 (2014), 104-110. [4] E. Boehnlein, T. Jiang, Set families with a forbidden induced subposet, Combin. Probab. Comput. , 21(4):496–511, 2012. [5] B. Bukh, Set families with a forbidden subposet, Electronic Journal of Combinatorics 

16 (1) (2009), Paper R142. [6] S. A. Burr, P. Erdős, R. L. Graham, V. T. Sós, Maximal antiramsey graphs and the strong chromatic number, J. Graph Theory , 13(3):263–282, 1989. [7] D. Ellis, M.R. Ivan, I. Leader, Turán densities for daisies and hypercubes. Bulletin of the London Mathematical Society, 56(12) (2024), 3838-3853. [8] D. Gerbner, D.T. Nagy, B. Patkós, M. Vizer, Supersaturation, counting, and randomness in forbidden subposet problems, Electron. J. Combin. , 28(1) (2021) #P1.40. [9] D. Gerbner, B. Patkós, Extremal Finite Set Theory , Chapman and Hall/CRC, Boca Raton, 2018. [10] J.R. Griggs, W.T. Li, Progress on poset-free families of subsets. In Recent trends in combinatorics (2016) pp. 317–338. Cham: Springer International Publishing. [11] T. Jiang, S. Longbrake, S. Spiro, L. Yepremyan, Tree posets: Supersaturation, enumera-tion, and randomness, Canadian Journal of Mathematics . Published online 2025:1–32. [12] G. O. H. Katona, T. G. Tarján, Extremal problems with excluded subgraphs in the 

n-cube, in Graph Theory , Łagów, 1981, Lecture Notes in Math., vol. 1018, Springer, Berlin, 1983, pp. 84–93. [13] B. Li, B. Patkós, C. Wang, Maximal anti-Ramsey problems for posets, preprint (2026). arXiv:2608.30354. [14] B. Patkós, Anti-Ramsey forbidden poset problems, Discrete Appl. Math. , 393:111–120, 2026. [15] B. Patkós, A. Treglown, On some extremal and probabilistic questions for tree posets, 

Electronic Journal of Combinatorics 31 (1) (2024), Paper P1.19. 13