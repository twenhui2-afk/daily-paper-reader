Title: Deterministic and Random Bipartite Matching on General Networks: Convex Flow Reformulation, Asymptotic Properties, and Fast Algorithms

URL Source: https://arxiv.org/pdf/2609.10938v1

Published Time: Fri, 11 Sep 2026 00:32:28 GMT

Number of Pages: 34

Markdown Content:
# Deterministic and Random Bipartite Matching on General Networks: Convex Flow Reformulation, Asymptotic Properties, and Fast Algorithms 

## Yuhui Zhai, Yanfeng Ouyang 

Abstract 

Minimum-distance bipartite matching on general networks has numerous applications in sciences, engineering, business, and sociology contexts. In transportation, the problem fre-quently arises in mobility service systems, such as ride-hailing, freight logistics, and crew assignment, where an equal number of supply and demand points are distributed along the edges of an arbitrary given network. This paper first focuses on deterministic problems. We present an exact edgewise-separable convex-flow reformulation which explores the cumulative supply-demand imbalance profile of each network edge and represents each matched point pair as one unit of flow routed through the network. By introducing a smooth monotone rearrangement approximation of the edge-wise imbalance profiles, which is shown to be asymptotically exact, the convex-flow reformulation’s can be solved efficiently by standard algorithms such as the Newton’s method. If we further conduct a first-order resistance-based approximation of the convex program, a one-step Laplacian-based estimator for the matching solution and cost can be analytically derived in closed forms. Next the paper studies random problems where supply and demand point distributions are described probabilistically. We show that the expected optimal matching distance scales with the square root of the number of points if the supply/demand point distributions are identical, or linearly otherwise. In the former case, the optimal flow is proven to be centered, symmetric, and sub-Gaussian, such that the fraction of matching pairs across different edges vanishes asymptotically. In the latter case, the limiting resistance network characterizes how supply-demand imbalance is redistributed across the network and further provides an asymptotic closed-form cost estimate. This also motivates a fast algorithm that approximate the optimal flow by projecting the limiting flow onto the flow-conservation feasible set. Numerical experiments with a variety of networks of varying sizes and topologies show that the proposed convex-flow and resistance-based estimators closely approximate the exact matching cost while substantially reducing computation time. In contrast to the original formulation, whose computational complexity scales cubically with the number of points, once the points are sorted on the edges, the computational complexity of all proposed estimators is independent of the number of points and instead depends only on the network size. The convex-flow formulation consistently achieves the highest accuracy, whereas the resistance-based and one-shot estimators provide increasingly faster alternatives, particularly for large-scale problems. Moreover the proven theoretical properties of the random matching solution are numerically verified by large-scale simulations. 

# 1 Introduction 

Minimum-cost bipartite matching, also known as the linear assignment problem, is a fundamen-tal problem in combinatorial optimization. Given two finite sets of equal cardinality and a cost associated with pairing two elements across the sets, the goal is to find a one-to-one matching that minimizes the total assignment cost. This problem has numerous applications in science and engineering fields, and it is a basic one in mobility and logistic systems as well. Examples include matching drivers to ride-hailing passengers (Ouyang and Yang, 2023; ¨Ozkan and Ward, 2020), 1arXiv:2609.10938v1 [math.OC] 10 Sep 2026 parcel couriers to delivery orders (Yildiz and Savelsbergh, 2019), and optimal drone assignment in truck-drone hybrid delivery (Murray and Chu, 2015). Matching decisions directly affect pas-senger waiting time, empty vehicle mileage, and service efficiency and reliability. In the field of transportation, supply and demand points are often located along the edges of a transportation network, and matching costs are induced by shortest-path distances on the network. Also, most transportation applications (e.g., ride hailing) involve two very large sets of points (e.g., on the order of millions), and hence efficiency in solving such problems is important for both real-time operations and system-level planning. There is a rich literature on ways to solve the bipartite matching problem efficiently. Classical methods include the Hungarian algorithm (Kuhn, 1955), the shortest augmenting path algorithms (Jonker and Volgenant, 1987), and auction-based methods (Bertsekas, 1988). Modern implemen-tations of these algorithms are highly optimized and can solve general cost-matrix instances with strong cubic-time performance (Crouse, 2016). These methods are broadly applicable, since they can accommodate arbitrary pairwise cost matrices without relying on any problem assumptions. However, this generality also prevents them exploiting additional geometric or network structures that arises when matching cost is induced by spatial locations or transportation networks. In large-scale mobility applications, constructing the full pairwise cost matrix and solving the assign-ment problem can become computationally expensive, especially when matching decisions must be updated online (Abeywickrama et al., 2021). Multiple approaches have been proposed to ac-celerate matching by pruning the candidate set (Yang et al., 2020), constructing sparse bipartite graphs (Alonso-Mora et al., 2017), batching nearby requests (Zheng et al., 2018), or avoiding un-necessary pairwise travel-cost computations (Abeywickrama et al., 2021). These methods improve scalability, but they still operate within a pairwise candidate-graph or cost-matrix framework that scales polynomially with the number of points. A related perspective comes from the theory of optimal transport, which studies how to move mass from one probability distribution to another at minimum transportation cost (Santambrogio, 2015). Minimum-cost bipartite matching can be viewed as the finite empirical counterpart of the optimal transport problem: each supply point and demand point is an atom of equal mass, and the goal is to transport every supply atom to one demand atom at minimum total cost. The optimal-transport viewpoint is especially useful when the ground cost has spatial or network structure. On a line, the 1-Wasserstein distance equals the area between cumulative distribution curves, so the matching cost can be computed from an imbalance curve rather than from all pairwise distances (Santambrogio, 2015). A similar simplification is available on trees. Since any two points on a tree are connected by a unique path, cutting an edge uniquely determines the amount of mass that must cross the cut. The transport cost can therefore be written as the sum over all edge cuts, of the area under the absolute edge-level cumulative imbalance curve (Evans and Matsen, 2012). While these results are inspiring and promising, the problem in general networks are more challenging because the presence of cycles make the cross-edge flow no longer uniquely determined by local edge cuts. Along this direction of inquiry, Treleaven et al. (2013) study bipartite matching with shortest-path distances on a general network and reduce the problem to a minimum-cost convex-flow formulation. Later, Treleaven and Frazzoli (2014) consider the continuous distribution analogue and formulate the Earth Mover’s Distance on road networks as a finite-dimensional convex optimization problem. These studies mainly focus on deterministic reformulation of matching problems on a network, but do not explore properties of the problem as well as efficient solution methods. In addition, little attention has been cast on analyzing the stochastic behavior of the matching problem under randomly distributed supply and demand. In transportation applications, interest has also been cast on developing closed-form analytical formulas that explain how optimal matching cost depends on problem size, distance metric, spatial distributions, and/or network structure. Such results are useful for planning applications, e.g., to have a simple way to evaluate expected matching cost under any system planning decisions. In ride-hailing system, for example, spatial models are often used to approximate pickup time of matching distance between idle vehicles and waiting passengers (Wang et al., 2024; Fan et al., 2026). A stream of literature on this topic studies the expectation of the optimal matching cost when supply and demand points are described by random distributions, which is called the random 2bipartite matching problem. Existing studies establish some asymptotic scaling laws and predict the expected matching cost in continuous metric spaces (M´ ezard and Parisi, 1985; Caracciolo et al., 2014, 2017; Shen et al., 2024; Shen and Ouyang, 2026). Studies in this direction, however, have not addressed the problem on a general network. Many related questions remain open, such as where matching inefficiency is generated, how network topology affects the matching performance, and how local supply-demand imbalances lead to matches across the network. In light of these gaps, this paper develops an interpretable approximation framework for random bipartite matching on general networks. We use edgewise cumulative supply-demand imbalance profiles as the representation of a realized matching instance, rather than point-based bipartite assignment. This greatly reduces the size of the optimization problem (from number of points to number of network edges). While existing convex-flow formulation shows that any matching problem instance can be solved through edgewise potentials (Treleaven et al., 2013; Treleaven and Frazzoli, 2014), our focus is to use this representation as an analytical and computation tool. In particular, we study how the shape of each edge’s imbalance profile determines the marginal cost of perturbing the boundary flow, which leads to an edge-induced resistance and a Laplacian-based approximation that estimates the total matching cost and the network-level matching flow. This paper makes four main contributions. First, we build on the edgewise convex-flow repre-sentation and introduce a smooth monotone-rearrangement approximation of the edge imbalance profiles. This approximation yields a smooth convex-flow formulation that can be solved accu-rately and efficiently using standard numerical methods, such as Newton’s method. Second, we derive KKT conditions of the smoothed convex-flow formulation and show that their first-order linearization admits an electric-network interpretation, where each edge has an induced resistance determined by the local slope. This resistance characterizes the edge’s local sensitivity to flow perturbation and leads to a computationally efficient electric-network analogy and approximation. Third, we provide a series of analyses on the random bipartite matching problem on networks. We establish that the asymptotic scaling of the expected matching cost is of order √n when supply and demand points follow the same distribution, or of order n otherwise. We further character-ize the corresponding limiting convex-flow problems when the number of points goes to infinity: when the supply and demand points follow identical distributions, we show that the likelihood of inter-edge matching diminishes as sub-Gaussian. Fourth, when the point distributions are non-identical, we can use the asymptotic limiting problem and the induced edge resistance as a basis to construct a one-shot estimator for deterministic large-scale random matching problems. The esti-mator adjusts the limiting flow through a single projection to satisfy the realized flow-conservation constraints, thereby avoiding iterative computations. It provides a computationally very efficient approximation when the supply and demand distributions differ and the problem size is sufficiently large. The remainder of the paper is organized as follows. Section 2 presents the deterministic convex-flow formulation, the smooth monotone-rearrangement approximation, the resistance-based lin-earization, and extensions to a few more general problems. Section 3 analyzes random bipartite matching on general networks and derives properties of asymptotic limiting flow and cost approx-imations. Section 4 validates the proposed formulations with numerical experiments on different networks. Section 5 concludes the paper and discuss future research directions. 

# 2 Separable Convex Flow Formulation 

This section develops a separable convex flow representation of the deterministic bipartite match-ing problem on a general network, which features an edgewise additive objective function that drastically simplifies both computation and asymptotic analysis. In what follows, unless stated otherwise, we exclusively use bold symbols to denote vectors or matrices, and assume that a vector is a column vector. We also use the convention that a vector or a matrix can be formed by properly stacking indexed scalars, and the vector with a subscript indicates the corresponding scalar element; for example, x := ( xa)a∈A denotes the vector indexed by the elements in a set A, and xa = xa the element corresponding to a ∈ A.32.1 Basic setting 

In a general undirected network ( V, E), edge e ∈ E has length le ≥ 0 and end nodes v− 

> e

∈ V and 

v+ 

> e

∈ V . The edge vector is denoted as l = ( le)e∈E . To uniquely track the location of any point 

w on edge e, which is xw distance away from v− 

> e

, we impose an arbitrary local coordinate axis from v− 

> e

to v+ 

> e

, such that this point will be recorded to have coordinates ( e, x w) ∈ E × [0 , l e]. The collection of points on edge e ∈ E that is within distance x away from v− 

> e

can be represented by the following set: 

Le(x) := {(e, x w) : xw ∈ [0 , x ]}.

Clearly, all points on edge e are represented by the set Le(le), and all points in the entire network are in the following set: 

L := [

> e∈E

Le(le),

and L = P 

> e∈E

le is the total edge length of the network. Accordingly, based on the local coordinate axis of each node, the sets of outflow and inflow arcs can be defined respectively as follows: 

δ−(v) := {e ∈ E : v− 

> e

= v}, δ+(v) := {e ∈ E : v+ 

> e

= v}, ∀ v ∈ V .

Now, suppose a finite sets of supply points U and a finite set of demand points V are distributed along the edges of the networks; i.e., U ⊂ L , V ⊂ L . These two sets are of equal cardinality n;i.e., |U | = |V | = n. The distance between any two points is measured along the shortest network path, and denoted by d(·, ·), while ties, if any, are broken arbitrarily. For notation convenience, we further specify the point subsets on each edge; i.e., 

Ue := U ∩ L e(le), Ve := V ∩ L e(le), ∀ e ∈ E .

We seek the minimum-distance bipartite matching among these two sets of points, which can be formulated as the following standard integer programming (IP): min   

> {yuv }

ZIP = X

> u∈U

X

> v∈V

yuv d(u, v ),

s.t. X

> u∈U

yuv = 1 , ∀ v ∈ V, 

X

> v∈V

yuv = 1 , ∀ u ∈ U, yuv ∈ { 0, 1}, ∀ u ∈ U, ∀ v ∈ V. 

(1) Such a formulation enjoys total unimodularity and hence can be efficiently solved in polynomial time; e.g., the state-of-art Jonker-Volgenant algorithm (Crouse, 2016) has a time complexity of 

O(n3). 

## 2.2 Flow-based formulations 

Given any bipartite matching solution, we send one unit of flow between every matched pair (u, v ) ∈ U × V along its shortest path from u ∈ U to v ∈ V . This flow is considered positive (with a value of +1) at a location ( e, x ) ∈ L e(le) if its shortest path traverses this location in the direction from v− 

> e

→ v+ 

> e

, or negative (with a value of −1) otherwise. The total net flow traversing (e, x ) from all matched pairs is denoted fe(x). Its value varies within e based on the distributions of supply and demand points inside this edge, which can be described by a local cumulative net supply-demand imbalance profile on e up to any position x, as follows 

Se(x) := |U ∩ L e(x)| − | V ∩ L e(x)|, ∀ e ∈ E , x ∈ [0 , l e].

While the matched points could be on different edges, the values of fe(x) and Se(x) are clearly related, as summarized in the following lemma. 4Lemma 1. For any feasible matching solution in the network, 

fe(x) = fe(0) + Se(x), ∀ e ∈ E , x ∈ [0 , l e].

Proof. While this lemma should be intuitive, we still give a formal proof here. First, along any edge 

e, in between any two consecutive points in Ue ∪ Ve, the value fe(x) must stay constant because no supply or demand point exist inside this interval (and hence no additional flow is generated). Next, we show that fe(x) increases (or decreases) by one unit at each supply (or demand) point. For a point w ∈ Ue located at xw, if we choose ε → 0+ such that no other point from Ue ∪ Ve

lies in ( xw − ε, x w + ε), then we can claim that fe(xw + ε) = fe(xw − ε) + 1. To see this, let w′

be its matched point on the same edge or elsewhere. If flow w → w′ is in direction v− 

> e

→ v+ 

> e

, it does not traverse any x ∈ (xw − ε, x w), but does traverse all x ∈ (xw, x w + ε). All other matched pair flows contribute to fe(xw + ε) and fe(xw − ε) equally. Hence, fe(xw + ε) = fe(xw − ε) + 1. If w → w′ is in direction v+ 

> e

→ v− 

> e

, it does not traverse any x ∈ (xw, x w + ε) but does traverse all x ∈ (xw − ε, x w). Hence, fe(xw − ε) = fe(xw + ε) − 1. In case the point at xw belongs to the other set, w ∈ Ve, by symmetry, it can be proven similarly that fe(xw + ε) = fe(xw − ε) − 1. This implies that, along edge e from v− 

> e

to v+ 

> e

, the variation of net flow fe(x) is captured exactly by the cumulative imbalance function Se(x). This completes the proof. Lemma 1 shows that, for any feasible matching solution, the flow profile on each edge is completely determined by the set of boundary flow values {fe(0) } together with the local imbalance profiles {Se(x)} (which is already known). This motivates us to define the following potential function Ψ e(·; ·) : R → R induced by any boundary-flow value ce and any edge profile ξe : [0 , l e] →

R:Ψe(ce; ξe) := 

Z le

> 0

|ξe(x) − ce|dx. 

The following lemmas present the basic properties of such a potential function, which is convex and Lipshitz continuous. 

Lemma 2. For any given ξe, e ∈ E , the function c 7 → Ψe(c; ξe) is convex. For any c1, c 2 ∈ R, and any edge profiles ξ1, ξ 2 ∈ L1([0 , 1]) , Ψe has the Lipschitz property 

|Ψe(c1; ξ1) − Ψe(c2; ξ2)| ≤ le (∥ξ1 − ξ2∥L1 + |c1 − c2|) .

When ξe takes integer values only, it is also affine on each open interval c ∈ (k, k + 1) , ∀ k ∈ Z.Proof. We prove these properties by definition. For all c1, c 2 ∈ R, and θ ∈ [0 , 1], by triangular inequality, we prove convexity from the following: 

θΨe(c1; ξe) + (1 − θ)Ψ( c2; ξe) = 

Z le

> 0

|θ(ξe(t) − c1)|dt +

Z le

> 0

|(1 − θ)( ξe(t) − c2)|dt

≥

Z le

> 0

|θ(ξe(t) − c1) + (1 − θ)( ξe(t) − c2)|dt = Ψ( θc 1 + (1 − θ)c2; ξe).

Meanwhile, Lipschitz continuity is shown as follows: 

|Ψe(c1; ξ1) − Ψe(c2; ξ2)| =

Z le

> 0

|ξ1(x) − c1| − | ξ2(x) − c2|dx ≤ le (∥ξ1 − ξ2∥L1 + |c1 − c2|) .

Finally, when ξe takes integer values only, consider an arbitrary c ∈ (k, k + 1), where k ∈ Z.

|ξe(x) − c| =

(

ξe(x) − c, ξe(x) ≥ k + 1 ,c − ξe(x), ξe(x) ≤ k. 

5Thus, by definition, Ψe(c; ξe) = 

Z le

> 0

|ξe(x) − c|dx =

Z

> ξe(x)≥k+1

(ξe(x) − c)d x +

Z

> ξe(x)≤k

(c − ξe(x))d x

=

"Z

> x:ξe(x)≤k

dx −

Z

> x:ξe(x)≥k+1

dx

#

c +

"Z

> x:ξe(x)≥k+1

ξe(x)d x −

Z

> x:ξe(x)≤k

ξe(x)d x

#

,

which is clearly an affine function with respect to c. This completes the proof. When ce := −fe(0) and ξe = Se, the potential function Ψ e(ce; ξe) = R le 

> 0

|fe(x)|dx, which gives the total absolute distance traversed by the net flow on edge e. Let c = ( ce)e∈E denote the vector of boundary flows. Note we already know the following input data: (i) the vector of edge-wise total imbalances s = ( Se(le)) e∈E ∈ Z|E| ; and (ii) the outflow (or inflow) node-edge incidence matrix I+

(or I−), whose ( v, e )-th entry equals 1 if e ∈ δ+(v) (or if e ∈ δ−(v)), or 0 otherwise. Any feasible flow {fe(x)} must satisfy the conservation — at each nodal junction, the net in-coming flow must be equal to the net out-going flow; i.e., for each v ∈ V :

X

> e∈δ−(v)

fe(0) − X

> e∈δ+(v)

fe(le) = X

> e∈δ−(v)

fe(0) − X

> e∈δ+(v)

[fe(0) + Se(le)] = 0 .

Since ce := −fe(0), the above can be written into the following matrix form: 

Ic = I+s, (2) where I = I+ − I−. We will next show that the optimal objective value of (1) can be obtained by solving the following formulation: min 

> c∈Z|E|

ZCF = X

> e∈E

Ψe(ce; Se), s.t. (2) . (3) In other words, we will show ZCF = ZIP . The proof proceeds in two steps. First, Lemma 3 below shows that every feasible boundary flow vector c can be converted into a feasible matching solution with the same objective value, and hence ZIP ≤ ZCF . Next, Lemma 4 shows that every feasible matching induces a feasible boundary flow vector whose objective value is no larger than the corresponding matching distance, and hence ZCF ≤ ZIP .

Lemma 3. Given any feasible flow profile {fe(x), ∀(e, x )}, after sorting all points on each network edge based on the local x-coordinate (in O(n log n) time), there exists an O(n)-time algorithm to construct a feasible solution to (1) with an objective value of 

X

> e∈E

Z le

> 0

|fe(x)|dx. 

Proof. A feasible matching solution can be constructed by slicing the area between the fe(x) curve and x-axis into horizontal unit-height layers (i.e., height = 1). Since fe(x) takes an integer value everywhere, each horizontal layer corresponds to two consecutive integers, k and k + 1, where 

k ∈ Z. For k ≥ 0, the layer begins at an upward jump of fe(x) from k to k + 1, which can be caused by either a supply point at that location or a unit flow entering the edge (which has to be at x = 0). The layer ends at a downward jump from k + 1 to k, which can be caused by either a demand point at that location or a unit flow leaving the edge (which has to be at x = le). Thus, each upward jump from k to k + 1 can be paired with the first subsequent downward jump from k + 1 to k, and one unit of flow is sent along the interval between these two jump locations. This interval length (or the area size of the unit-height layer) corresponds to either a local match distance between a supply point and a demand point on edge e, or a local segment of a shortest path that traverses e. For k < 0, everything is exactly similar and hence omitted. This is 6illustrated in Figure 1(a), where supply and demand points are represented by red circle markers and blue triangle markers, respectively. Each arrow indicates a point-to-point match. Summing over the areas of all horizontal unit-height layers gives exactly P

> e∈E

R le 

> 0

|fe(x)|dx.The above slicing idea also leads to the following scanning procedure which, in linear time, can construct a feasible matching solution in a network. First, virtual supply or demand points are added to the two endpoints of each edge e wherever the boundary flows are non-zero; i.e., if 

fe(0 +) > 0 (or < 0), we place |fe(0 +)| virtual supply (or demand) points at x = 0; if fe(l− 

> e

) > 0(or < 0), we place |fe(l− 

> e

)| virtual demand (or supply) points at x = le. Then we scan all real and virtual points on each edge in an increasing order of their local x-coordinates, and use the above slicing procedure to form matches. It is easy to show that this procedure can be done in O(n)computation time with the use of a last-in first-out (LIFO) stack, since each supply or demand point corresponds to exactly two operations (addition to or removal from the stack). After all edges are processed, any path segments that involve virtual points must be concate-nated to obtain supply-demand point matches. Due to flow conservation, the virtual supply and demand points as every network vertex v ∈ V must be paired. We can arbitrarily concatenate two path segments (along two edges) that involve one virtual point of each type at the same vertex. This operation will be repeated until no virtual point remains in the path segments, and a feasible matching solution is found. This concatenation procedure does not affect the total matching dis-tance, and hence a feasible matching solution to (1) has an objective value of P

> e∈E

R le 

> 0

|fe(x)|dx.This completes the proof. 

Lemma 4. For any feasible matching solution that induces the set of flow profiles {fe(x), ∀(e, x )},the total matching distance is bounded from below by P

> e∈E

R le 

> 0

|fe(x)|dx.Proof. For any edge e ∈ E and position x ∈ [0 , l e], by definition, the net flow fe(x) is the number of shortest paths crossing x in the direction v− 

> e

→ v+ 

> e

minus the number of those crossing x in the opposite direction v+ 

> e

→ v− 

> e

. Thus, the total number of shortest paths crossing x, given by the sum of these two counts, is at least |fe(x)|. Now consider an infinitesimal edge segment [ x, x +d x]. Any unit of flow that crosses this infinitesimal segment contributes d x to the total matching distance. Hence, the total contributions from all flow paths crossing this infinitesimal segment is at least 

|fe(x)|dx. This is illustrated in Figure 1(b). Since this argument holds for every x ∈ [0 , l e] and for all e ∈ E , integrating this quantity over x and summing it across e, P

> e∈E

R le 

> 0

|fe(x)|dx, gives a lower bound to the total matching distance. This completes the proof. 

Figure 1: Illustrations for the proofs of Lemmas 3 and 4. Lemmas 3 and 4 together establish the equivalence between the optimal objective values from (1) and (3), as summarized in the following theorem. 

Theorem 1. At optimality, ZIP = ZCF .

7In the above analysis, the boundary flow ce = −fe(0) takes integer values for all e ∈ E . Yet, we notice that the flow conservation constraints in (3) satisfy total unimodularity. As such, it may be convenient to relax the integrality requirement and allow ce to take real values, as in the following formulation. min 

> c∈R|E|

ZRLX = X

> e∈E

Ψe(ce; Se), s.t. (2) . (4) The relaxed optimal objective ZRLX can be shown to also equal that of the original formulation (3), as stated below. 

Theorem 2. At optimality, ZCF = ZRLX .Proof. Let crlx ∈ R|E| denote the optimal solution to (4). For any given integer-valued Se, e ∈ E ,Lemma 2 says that Ψ e(ce; Se) is affine over ce ∈ [⌊crlx  

> e

⌋, ⌈crlx  

> e

⌉]. Since I is totally unimodular and 

I+s is integral, there exists an integral extreme point cint ∈ Z|E| that is an optimal solution to (4) while satisfying the following additional constraints: ⌊crlx  

> e

⌋ ≤ cint  

> e

≤ ⌈ crlx  

> e

⌉, ∀ e ∈ E . The objective value of such a restricted problem is equal to ZRLX . Since cint is a feasible solution to (3), we have ZCF ≤ ZRLX . On the other hand, ZRLX ≤ ZCF since (4) is a relaxed problem. This proves that ZRLX = ZCF .Theorems 1 and 2 reveal that the original pairwise matching problem (1) can be equivalently represented by a continuous flow formulation (4). Although this formulation can already be solved by standard optimization techniques, it still does not reveal interesting properties mainly because the main input data, imbalance profiles {Se(x), ∀(e, x )} are not differentiable everywhere. In the following section, we further analyze an approximate formulation based on a smooth representation of {Se(x), ∀(e, x )}, which leads to a set of interpretable optimality conditions, and will be useful in analyzing the stochastic version of the bipartite matching problem. 

## 2.3 Approximate flow formulation and linear-time algorithm 

Figure 2: Illustration of profiles Se, Q e, Q e,ϵ .To characterize and approximate {Se(x), ∀(e, x )}, we introduce two additional functions that are associated with an edge profile ξe:

Fe(c; ξe) := 

Z

> x:ξe(x)≤c

dx, Qe(t; ξe) := inf {c : Fe(c; ξe) ≥ t}.

The first function Fe : R → [0 , l e] gives the total length of edge e where ξe(x) ≤ c. It is easy to see that Fe is non-decreasing and right-continuous. Since Fe may not be differentiable, we use 8F − 

> e

(c; ξe) := lim y↑c Fe(y; ξe). Note that Qe is the generalized inverse of Fe, and geometrically, 

Qe(·; ξe) gives the monotonic “cumulative distribution” of the values in profile ξe. Since Ψ e

depends only on the distribution of the profile values, i.e., the total edge length below each level 

ce, replacing ξe by Qe(·; ξe) preserves the equivalent matching costs. For example, Figures 2(a) and (b) visualize the distributional equivalence of Se and Qe(·; Se), because for any fixed c value, the blue spans in the two figures have the same total length. Such a property is formalized in the following lemma: 

Lemma 5. For any ξe and any c ∈ R,

Ψe(c; ξe) = Ψ e(c; Qe(·; ξe)) .

Proof. By the generalized-inverse identity, for any c ∈ R,

Qe(x; ξe) ≤ c ⇔ Fe(c; ξe) ≥ x. 

Therefore, λ({x : Qe(x; ξe) ≤ c}) = Fe(c; ξe), where λ(·) is the Lebesgue measure. By definition, 

λ({x : Se(x) ≤ c}) = Fe(c; Se). Hence, ξe and Qe(·; ξe) have the same total edge length under every level c. Since Fe is non-decreasing, Qe(·; ξe) is also non-decreasing. Therefore, Qe(·; ξe) is a monotone rearrangement of ξe. Finally, the integrals over ξe, Q e are the same; i.e., Ψe(c; Se) = 

Z le

> 0

|ξe(x) − c|dx =

Z le

> 0

|Qe(x; ξe) − c|dx = Ψ e(c; Qe(·; ξe)) .

This completes the proof. Lemma 5 indicates that an equivalent formulation can be obtained by replacing the original profile Se by its monotone representation Qe(·; Se). This has two benefits. First, both the ob-jective value and the optimality condition are preserved. Second, the rearranged representation 

Qe(·; Se) allows us to conveniently impose a smooth approximation, Qe,ϵ controlled by a smooth-ing parameter ϵ > 0. In this study, we assume that Qe,ϵ is twice differentiable, strictly increasing, and that Qe,ϵ converges to Qe(·; Se) in L1(0 , l e) as ϵ ↓ 0; i.e., 

Qe,ϵ ∈ C 2([0 , l e]) , Qe,ϵ > 0, lim  

> ϵ↓0

max  

> e∈E

∥Qe,ϵ (·) − Qe(·; Se)∥L1(0 ,l e) = 0 . (5) The red dashed curve in Figure 2(b) shows an example of Qe,ϵ .Now we propose the following approximate convex-flow formulation. 

Theorem 3. The optimal total matching distance in a general network is approximated by the optimal objective value Z of the following approximate formulation with respect to c:

min 

> c∈R|E|

Z = X

> e∈E

Ψe(ce; Qe,ϵ ), s.t. (2) . (6) 

with convergence as ϵ ↓ 0,

lim  

> ϵ↓0

|Z − ZCF | = 0 .

Proof. We prove the theorem by showing the following: 

|Z − ZRLX | = min 

> c

X

> e∈E

Ψe(ce; Qe(·; Se)) − min 

> c

X

> e∈E

Ψe(ce; Qe,ϵ )

≤ min 

> c

X

> e∈E

Ψe(ce; Qe(·; Se)) − Ψe(ce; Qe,ϵ ) ≤ X

> e∈E

Z le

> 0

|Qe(x; Se) − Qe,ϵ (x)|dx → 0.

The first inequality follows the monotonicity of the minimum; the second inequality also follows Lemma 5, and the last convergence follows the assumptions in (5). From Theorem 1, we know 

ZCF = ZRLX . This completes the proof. 9The solution to such constrained optimization formulation can be easily obtained via analysis of its Lagrangian, as explained below. 

Lemma 6. For any fixed ξe and any c ∈ R, the subdifferential of Ψe(c; ξe), ∀ e ∈ E , is given by 

∂Ψe(c; ξe) = [2 F − 

> e

(c; ξe) − le, 2Fe(c; ξe) − le].Proof. The subdifferential of |ξe(x) − c| is simply 

∂|ξe(x) − c| =



{− 1}, ξe(x) > c, 

[−1, 1] , ξe(x) = c, 

{1} ξe(x) < c. 

Thus, the subdifferential of Ψ e(c; ξe) is: 

∂Ψe(c; ξe) = 

Z le

> 0

∂|ξe(x) − c|dx

=

Z

> x:ξe(x)<c

1d x +

Z

> x:ξe(x)= c

∂|ξe(t) − c|dx +

Z

> x:ξe(x)>c

−1d x. 

Since − R 

> x:ξe(x)= c

dx ≤ R 

> x:ξe(x)= c

∂|ξe(x) − c|dx ≤ R 

> x:ξe(x)= c

dx, we have: 

∂Ψe(c; ξe) = 

Z

> x:ξe(x)<c

dx −

Z

> x:ξe(x)>c

dx +

"

−

Z

> x:ξe(x)= c

dx, 

Z

> x:ξe(x)= c

dx

#

=

Z

> x:ξe(x)<c

dx − le −

Z

> x:ξe(x)<c

dx −

Z

> x:ξe(x)= c

dx

!

+

"

−

Z

> x:ξe(x)= c

dx, 

Z

> x:ξe(x)= c

dx

#

=

"

2

Z

> x:ξe(x)<c

dx − le, 2

Z

> x:ξe(x)≤c

dx − le

#

= [2 F − 

> e

(c; ξe) − le, 2Fe(c; ξe) − le].

This completes the proof. We denote c∗ as the optimal solution to (6). The optimality condition is summarized below. 

Theorem 4. An optimal solution c∗ and its corresponding Lagrangian multiplier Λ∗ ∈ R|V| must satisfy: 

c∗ = Q(Λ∗) and 0 = IQ (Λ∗) − I+s,

where 

Q(Λ) = 



Qe,ϵ 

 le − (ITΛ)e

2

 

> e∈E

.

Proof. The edge-wise first-order conditions of the optimization problem are given by: 0 ∈ ∂Ψe(c∗ 

> e

; Qe,ϵ ) + ( ITΛ∗)e = [2 F − 

> e

(c∗ 

> e

; Qe,ϵ ), 2Fe(c∗ 

> e

; Qe,ϵ )] − le + ( ITΛ∗)e, ∀ e. 

Since Qe,ϵ ∈ C 2 and Q′ 

> e,ϵ

> 0, Fe(·; Qe,ϵ ) is continuous. This simplifies the optimality conditions to 

le − (ITΛ∗)e

2 = Fe(c∗ 

> e

; Qe,ϵ ) ⇒ c∗ 

> e

= Qe

 le − (ITΛ∗)e

2 ; Qe,ϵ 



= Qe,ϵ 

 le − (ITΛ∗)e

2



, ∀ e. 

(7) This proves the form of c∗. Meanwhile, the rest of the theorem simply comes from the primal feasibility condition. This completes the proof. 10 The optimal solution c∗ can be efficiently solved by a standard Newton method. Define the residual of the flow conservation as: 

IQ (Λ) − I+s.

Its corresponding Jacobian with respect to Λ is: 

∇ (IQ (Λ)) = I∇Q(Λ) = − 12 I diag 

 

Q′

> e,ϵ

 le − (ITΛ)e

2

 

> e∈E



IT,

where the diagonal term represents an edge “conductance” matrix. 1 In a general network with points sorted on each edge, each Newton iteration depends only on the network topology (and is independent of n). As such, the approximate convex-flow formulation (6) can be solved in linear time complexity O(n). Once c∗ is obtained, we can construct a feasible matching solution to (3) based on the following two-step heuristic algorithm. Step 1 We define a new vector crnd by rounding each element of c∗ to the nearest integer, such that 

|crnd  

> e

− c∗ 

> e

| ≤ 12 , ∀ e. If crnd satisfies the flow conservation constraints, then it is already a feasible solution to (3); Step 2 If crnd does not satisfy flow conservation, we heuristically perturb it into one, denoted cfes ,that satisfies both integrality and flow conservation. In so doing, we initially identify from 

crnd two node subsets: V+ = {v : ∆ v > 0, v ∈ V} and V− = {v : ∆ v < 0, v ∈ V} , and iteratively update them. In each iteration, we arbitrary pick two nodes, u ∈ V − and v ∈ V +,and send a unit of flow along an arbitrary u − v path. For each edge e within the path, the value of crnd  

> e

increases by one unit if the path direction matches the edge direction; or otherwise it decreases by one unit. This operation does not affect flow conservation at any nodes other than u, v , and hence it decreases both the total deficit and total surplus values by one unit each. Repeat this operation 12 ∥∆∥1 times, we shall get ∆ = 0 and an integral feasible solution cfes .If points are already sorted on each edge, the above heuristic has a desirable O(n) time complexity. The resulted feasible solution cfes has a bounded optimality gap, as shown in the theorem below. 

Theorem 5. Suppose cint is the optimal solution to (3) . Then 

0 ≤ X

> e∈E

Ψe(cfes ; Se) − X

> e∈E

Ψe(cint ; Se) ≤

 |E| 

2 + 12



L + o(1) .

Proof. From Lemma 2, Ψ e is le-Lipchitz continuous, and hence 

X

> e∈E

Ψe(c∗ 

> e

; Qe,ϵ ) − Ψe(crnd  

> e

; Qe,ϵ ) ≤ X

> e∈E

le|c∗ 

> e

− crnd  

> e

| ≤ L

2 .

If crnd  

> e

satisfied the flow conservation constraints, then the above is already the optimality gap. Otherwise, we define a residual vector ∆ = I+s − Ic rnd ∈ Z|V| ,

where a positive value of ∆ v indicates a deficit at node v ∈ V and a negative value indicates a surplus. With balanced supply/demand points, it is trivial to see that 1T∆ = 0, where 1 is a conformable vector of 1’s. Notice that Ic ∗ = I+s, and thus the 1-norm of the residual vector 

∥∆∥1 = ∥I(c∗ − crnd )∥1 = X

> v∈V

X

> e∈δ+(v)

(c∗ 

> e

− crnd  

> e

) − X

> e∈δ−(v)

(c∗ 

> e

− crnd  

> e

) ≤ X

> v∈V

deg( v)2 = |E| .

> 1More about this matrix will be discussed in the next section.

11 In the heuristic iterations, each unit of added flow travels at most L distance. Thus, 

X

> e∈E

Ψe(crnd  

> e

; Qe,ϵ ) − Ψe(cfes  

> e

; Qe,ϵ ) ≤ ∥∆∥1

2 L ≤ |E| 

2 L. 

Now, from Theorem 3 and triangle inequality, we have 

X

> e∈E

Ψe(cfes  

> e

; Se) − Ψe(cint  

> e

; Se) = X

> e∈E

Ψe(cfes  

> e

; Qe(·; Se)) − Ψe(cint  

> e

; Qe(·; Se)) 

≤ X

> e∈E

Ψe(cfes  

> e

; Qe(·; Se)) − Ψe(cfes  

> e

; Qe,ϵ ) + X

> e∈E

Ψe(cfes  

> e

; Qe,ϵ ) − Ψe(crnd  

> e

; Qe,ϵ )

+ X

> e∈E

Ψe(crnd  

> e

; Qe,ϵ ) − Ψe(c∗ 

> e

; Qe,ϵ ) + X

> e∈E

Ψe(c∗ 

> e

; Qe,ϵ ) − Ψe(cint  

> e

; Qe(·; Se)) ≤

 |E| 

2 + 12



L + o(1) .

This completes the proof. 

## 2.4 Electricity network analogy, and a faster algorithm 

In this section, we show that the nonlinear optimality conditions in Theorem 4 has a linearized form that mimics a well-known electric network voltage-current problem. To see this, note that 

Qe,ϵ ∈ C 2 and is bounded, and hence its Taylor expansion at a reference point xe0 ∈ [0 , l e] can be written as 

c∗ 

> e

= Qe,ϵ 

 le − (ITΛ∗)e

2



=Qe,ϵ (xe0) + Q′ 

> e,ϵ

(xe0)

 le − (ITΛ∗)e

2 − xe0



+ O

 le − (ITΛ∗)e

2 − xe0

2!

.

For any multiplier vector ˜Λ ∈ R|V| (e.g., an approximation of Λ∗), define the associated flow ˜c = (˜ ce)e∈E where: ˜ce = Qe,ϵ (x0,e ) + Q′

> e,ϵ

(x0,e ) le − (IT ˜Λ)e

2 − x0,e 

!

, ∀ e ∈ E .

Based on vector x0 := ( x0,e )e∈E , we further define the intercept or “baseline” vector c0, the diagonal “resistance” matrix, R, and the “weighted Laplacian” matrix L as follows: 

c0(x0) = ( Qe,ϵ (x0,e )) e∈E , R(x0) = diag 

 1/Q ′

> e,ϵ

(x0,e )

> e∈E



, L(x0) = I(R(x0)) −1IT.

Now, we choose the reference points to be the edges’ midpoints (i.e., x0 := l/2), and denote 

c∗ 

> 0

:= c0(l/2) , R∗ := R(l/2) , L∗ := L(l/2) ,

and then ˜c = c∗ 

> 0

− 12 (R∗)−1IT ˜Λ. (8) Meanwhile, the flow conservation constraints I˜c = I+s becomes 

Ic ∗ 

> 0

− 12 I(R∗)−1IT ˜Λ = I+s, ⇒ ˜Λ = 2( L∗)† (Ic ∗ 

> 0

− I+s) = 2( L∗)†(Ic ∗ 

> 0

− I+s), (9) where † represents the Moore-Penrose inverse. Plugging Equation (9) back into Equation (8), we have: ˜c = c∗ 

> 0

− (R∗)−1IT (L∗)† (Ic ∗ 

> 0

− I+s).

12 It is easy to see that ˜ c is the unique solution to a quadratic minimum-energy electricity flow problem: min 

> c∈R|V|

12 (c − c∗

> 0

)TR∗(c − c∗

> 0

),

s.t. Ic = I+s,

and it yields a minimum objective value of 12 (Ic ∗ 

> 0

− I+s)T(L∗)†(Ic ∗ 

> 0

− I+s). (10) Note that c∗ 

> 0

has a natural interpretation in the context of electricity flow problem — the optimal current solution when the flow conservation constraint is dropped; i.e., in this case, the first-order optimality condition gives the optimal solution 

c∗ = c∗

> 0

.

In other words, c∗ 

> e

= Qe,ϵ (le/2) is precisely the uncoupled optimal boundary flow for each edge 

e ∈ E . When the conservation constraint Ic = I+s is imposed, the global optimizer c∗ becomes a coupled perturbation around this local optimum, and the magnitude of this perturbation is governed by the slope Q′

> e,ϵ

(le/2). In the bipartite matching context, it quantifies how much “point mass,” captured by Se, lies around its local optimum. A large Q′

> e,ϵ

(le/2) implies that the variation of Qe value near le/2is large, and hence the local growth of Fe is small. Since Fe measures how vertex “imbalance” accumulates along the edge, it acts as a local “density” of imbalance Se. Thus, a slow growth of Fe represents a relatively even distribution of points over the edge, and small deviation of 

ce incurs a small marginal cost. Hence, we call the term, Q′

> e,ϵ

(le/2), analogous to electricity current conductance, the “local conductance” of the edge. Its reciprocal is called the “local resistance” of the edge. Besides, we can make other analogies as well. Multiplier Λ∗ plays the role of node potential, and its value change across each edge is given by ITΛ∗. This is similar to the relationship between voltage and current; i.e., the boundary-flow vector c∗ then satisfies the “Ohm’s law:” current = conductance × voltage drop. Moreover, the conservation constraint becomes the “Kirchhoff’s law,” where − 12 I(R∗)−1ITΛ∗ = I+s, stating that the net injection of voltage at nodes equals the divergence of current. We next quantify the accuracy of the linear approximation, and shows how the linearization error vanishes as ∥l∥∞ decreases. 

Theorem 6. ∥c∗ − ˜c∥R → 0 as ∥l∥∞ → 0, where ∥·∥ R = p(·)TR(·).Proof. We first claim that for all e ∈ E , |(ITΛ∗)e| ≤ ∥ l∥∞. By Equation (7), 0 = 2 Fe(c∗ 

> e

; Qe,ϵ ) − le + ( ITΛ∗)e.

Since Fe(c∗ 

> e

; Qe,ϵ ) ∈ [0 , l e], we obtain |(ITΛ∗)e| ≤ le ≤ ∥ l∥∞. By Taylor’s expansion, 

c∗ = c0 − 12 R−1ITΛ∗ + r,

where r ∈ R|E| is the remainder vector of the higher-order term indexed by edges e ∈ E , and satisfies 

re ≤ sup x∈[0 ,l e] Q′′ 

> e,ϵ

(x)8 (ITΛ∗ 

> e

)2 ≤ max e∈E sup x∈[0 ,l e] Q′′ 

> e,ϵ

(x)8 ∥l∥2

> ∞

, ∀ e ∈ E .

This implies that ∥r∥R → 0 as ∥l∥∞ → 0. Moreover, both c∗ and ˜ c satisfy the flow conservation equation, such that 

Ic ∗ − I˜c = − 12 IR −1IT(Λ∗ − ˜Λ) + Ir = 0 .

13 Left multiplying both sides of the second equality by ( Λ∗ − ˜Λ)T, and rearranging the terms, we have the first equality below: 

∥IT(Λ∗ − ˜Λ)∥2 

> R−1

= 2 

√R−1IT(Λ∗ − ˜Λ)

T √Rr 



≤ 2∥√R−1IT(Λ∗ − ˜Λ)∥2 ∥√Rr ∥2 = 2 ∥IT(Λ∗ − ˜Λ)∥R−1 ∥r∥R.

The second line above follows the Cauchy-Schwarz inequality. Hence, 

∥IT(Λ∗ − ˜Λ)∥R−1 ≤ 2∥r∥R.

Finally, since c∗ − ˜c = − 12 R−1IT(Λ∗ − ˜Λ) + r, Cauchy-Schwarz inequality again gives the first inequality below: 

∥c∗ − ˜c∥R ≤ 12 ∥R−1IT(Λ∗ − ˜Λ)∥R + ∥r∥R = 12 ∥IT(Λ∗ − ˜Λ)∥R−1 + ∥r∥R ≤ 2∥r∥R.

Thus, as ∥l∥∞ → 0, ∥c∗ − ˜c∥R → 0. This completes the proof. Theorem 6 not only explains how the linearization error decreases with the maximum edge length in the network, but also motivates a simple edge-partition operation that can be used to con-trol the linear approximation error ∥c∗ −˜c∥R to an arbitrary tolerance δ. In so doing, we can simply compute the matrix trace, tr( R), and find a large value M ≥ ∥l∥∞ 

> 2δ1/2

(max e∈E sup x∈[0 ,l e] Q′′ 

> e,ϵ

(x)tr( R)1/2)1/2.Then, every edge e ∈ E can be partitioned into M sub-edges, each with an equal length le/M , by inserting new nodes to the edge. This operation does not change the underlying metric network or the random point realization process, and hence the distribution of the total matching cost remains the same. Yet, by reducing the maximum edge length, Theorem 6 can guarantee that the approximation error bound 2 ∥r∥R does not exceed the tolerance ϵ. With this accuracy guaran-tee, we can use ˜ c as a feasible flow solution and evaluate it using the original nonlinear objective function: ˜Z = X

> e∈E

Ψe(˜ ce; Se). (11) Such approximation can be computed in O(n log n) time with respect to n, same as the convex-flow formulation. However, it is typically faster in practice because it avoids iterative Newton updates and computes ˜ c in one step. 

## 2.5 Extensions 

We now show how the analysis framework can be extended to three variants of the assignment problem. The first direct extension is to allow each supply or demand point to carry an arbitrary “mass” value, such that the problem becomes the so-called transportation problem. For instance, a few depots may be regarded as a set of supply point each with a large mass, while multiple customers as demand points each with a smaller mass. Under this extension, the model formulation remains essentially unchanged; the only difference is that the magnitudes of the jumps in the imbalance profiles {Se} are now based on the points’ masses (rather than unit value). The second extension is to allow for directed edges in the network. This is natural in trans-portation networks where one-way traffic is often enforced. Recall that in the undirected setting, the orientation of each edge is arbitrarily determined in our model, and the edge flow {fe} is allowed to be positive or negative. For a directed edge e, we can simply force the edge orientation to follow the prescribed direction, and require {fe(x)} to be non-negative at every x on the edge. Note that for such an edge e,

fe(x) ≥ 0 ⇒ Se(x) − ce ≥ 0, ∀ x ∈ [0 , l e] ⇒ ce ≤ min  

> x∈[0 ,l e]

Se(x).

14 As such, directed edges can be incorporated into the convex-flow formulation framework by addi-tionally imposing the above edgewise inequality constraint. The objective function and the rest of the formulation remain unchanged. The third extension is to allow the two sets of points to have different cardinalities. Let 

|U | = n and |V | = m, and we assume n ≥ m without loss of generality. In such a case, only 

m point pairs can be matched, and accordingly the supply-side equality constraints in (1) are replaced by inequalities P 

> v∈V

yuv ≤ 1, ∀ u ∈ U . This problem can be easily converted into a balanced matching problem by introducing a single dummy demand point with a mass of ( n − m), and connecting it to every supply point in U with a zero-distance dummy edge. This augmented network can then be used directly as if the matching problem is now balanced. The above transformation notably increases the size of the network because n dummy edges must be added, and in turn, each supply point must now be treated as a new network node which further increases the number of edges. Therefore, as a compromise, the dummy demand may instead be connected to a subset of selected nodes of the underlying network, rather than to all individual supply points, with zero-distance dummy edges. A supply point, if unmatched to a demand point, can be connected to the dummy demand node via detour through the nearest of such selected nodes. Under this approximation, the number of dummy connections is reduced to 

|V| , and the flow-conservation equality in (3) is simply replaced by Ic ≤ I+s. The approximation error from the detour can be reduced arbitrarily small as each network edge is divided into short segments. 

# 3 Random Bipartite Matching 

In this section, we consider a random setting in which the points in U, V are i.i.d samples from two probability spaces ( L, F, μ ) and ( L, F, ν ), respectively. Here F is the σ-algebra on the network sample-space L, and the measures μ and ν specify the sampling distributions of one random point on L; i.e., for any event A ∈ F (e.g., a specific portion of an edge, or any part of a network), μ(A)(or ν(A)) is the probability that a random supply (or demand) point lies in A. Now, ZIP , Z CF , Z 

are random variables induced by the random point sets U and V , and we are interested in analyzing their expectations. 

## 3.1 Asymptotic Convergence 

Importantly, the realized random problem converge to a limiting problem, which substantially simplifies the analysis. Recall that n is the cardinality of the set U or V . For any edge e, each random point falls onto this edge Le(x) according to a Bernoulli trial, and hence Se(x) can be interpreted as the difference between the sums of two sets of Bernoulli variables, each of cardinality 

n. The imbalance profile {Se(x) : ∀ x ∈ [0 , l e]} has an asymptotic limit when n → ∞ . First, when 

μ(Le(x)) ̸ ≡ ν(Le(x)), by the law of large numbers, as n → ∞ , the sample mean of independent random variables converges to the population mean; or equivalently, 

Se(x)

n = 1

n

"X

> u∈U

1(u ∈ L e(x)) 

#

− 1

n

"X

> v∈V

1(v ∈ L e(x)) 

#

> n→∞

−−−−→ μ(Le(x)) − ν(Le(x)) , (12) where 1(·) denotes the indicator function. When μ(Le(x)) ≡ ν(Le(x)) , ∀ x, Se(x) is a balanced random walk. By Donsker’s invariance principle and independence between supply and demand points, if B is a standard Brownian bridge, then Se(x)/√n converges in distribution as follows: 

Se(x)

√n = 1

√n

"X

> u∈U

1(u ∈ L e(x)) 

#

− 1

√n

"X

> v∈V

1(v ∈ L e(x)) 

#

= 1

√n

"X

> u∈U

1(u ∈ L e(x)) − μ(Le(x)) 

#

− 1

√n

"X

> v∈V

1(v ∈ L e(x)) − ν(Le(x)) 

#

> n→∞

−−−−→ √2B(μ(Le(x))) .

(13) 15 As such, the following normalized edge imbalance profile have convergence: 

ζe(x) = lim 

> n→∞

( Se(x)

> nSe(x)
> √n

=

(

μ(Le(x)) − ν(Le(x)) , if μ(Le(x)) ̸ ≡ ν(Le(x)) ,

√2B(μ(Le(x))) , if μ(Le(x)) ≡ ν(Le(x)) .

Meanwhile, recall that (i) the edgewise matching cost Ψ e(ce; Se) is linear with respect to either argument and (ii) Lipschitz continuous with regard to ce, and (iii) the network flow conservation constraints are also linear. Therefore, if we use the limiting imbalance profile ζe(x) to replace Se(x)in 3 and s∞ := ( ζe(le)) e∈E to replace s in 2, then the solution to 3, denoted c∞ with objective value Z∞, will be simply the corresponding limits of normalized cint and ZCF , respectively. In other words, 

Z∞ = lim 

> n→∞

( ZCF  

> √n

, if μ = ν,  

> ZCF
> n

, if μ̸ = ν. 

This also implies how ZCF asymptotically scales with n; i.e., since Z∞ is clearly positive and finite, we have 

E[ZCF ] ≍

(√n, if μ = ν, n, if μ̸ = ν. (14) The convergence property also holds for Qe and the associated resistance matrices. First, the following theorem implies that we can use nQ e(·; ζe) as an asymptotic approximation of Qe(·; Se). 

Theorem 7. Assume every level set of ζe has a zero Lebesgue measure; that is, for every c ∈

R, e ∈ E ,

λ{x : ζe(x) = c} = 0 ,

then asymptotic convergence exists as follows: 

Qe(x; ζe) = 

(lim n→∞ Qe(x;Se) 

> n

, if μ(Le(x)) ̸ ≡ ν(Le(x)) ,

lim n→∞ Qe(x;Se) 

> √n

, if μ(Le(x)) ≡ ν(Le(x)) .

Proof. Here we only present the proof when μ(Le(x)) ̸ ≡ ν(Le(x)), since the proof for the other case is similar. For any c ∈ R,

Fe(nc ; Se) = 

Z

> x:Se(x)≤nc

dx =

Z

> x:Se(x)/n ≤c

dx. 

When every level set of ζe has a zero Lebesgue measure, the functional Fe(·; ζe) is continuous almost surely, i.e., 

F − 

> e

(c; ζe) = 

Z

> x:ζe(x)<c

dx =

Z

> x:ζe(x)≤c

dx = Fe(c; ζe).

By the continuous mapping theorem Durrett (2019), lim n→∞ Fe(nc ; Se) = Fe(c; ζe). The normal-ized inverse function thus has the following convergence property: 1

n Qe(x; Se) = 1

n inf {nc : Fe(nc ; Se) ≥ x} = inf {c : Fe(nc ; Se) ≥ x}→ inf {c : Fe(c; ζe) ≥ x} = Qe(x; ζe).

This completes the proof. 16 Furthermore, if Qe(·; ζe) is differentiable at le/2, the resistance matrix R∗ is well-defined. The following lemma states the condition under which this property holds. Intuitively, if ζe does not have “flat” regions around Qe(le/2; ζe), then Fe(·; ζe) will not have jumps at le/2, and thus Qe(·; ζe)will be differentiable at le/2. 

Lemma 7. Suppose the following conditions hold for a twice-differentiable edge profile ξe ∈ C 2,

ξ′

> e

(x)̸ = 0 , ∀ x : ξe(x) = Qe(le/2; ξe) = 0 ,

then Qe(·; ξe) is differentiable at le/2.Proof. We will use the coarea formula (Evans and Gariepy, 2015) to prove that Fe(·; ζe) is differ-entiable at le/2, which states that for an D-dimensional open set Ω ∈ RD , a Lipschitz continuous function f and a L1 function g,

Z

> Ω

g(x)|∇ f (x)|dx =

Z

> R

Z 

> f−1(t)

g(x)dHD−1

!

dt, 

where HD−1 is the ( D − 1)-dimensional Hausdorff measure. When D = 1, the above expression reduces to 

Z

> Ω

g(x)|f ′(x)|dx =

Z

> R

 X 

> x∈f−1(t)

g(x)

 dt. 

Let ξe ∈ C 2, Ω := [0 , l e], g(x) := 1{a < ξ e(x) ≤ b}/|ξ′

> e

(x)| for real values a < b , and f (x) := ξe(x). Then 

Fe(b; ξe) − Fe(a; ξe) = 

Z le

> 0

1{a < ξ e(x) ≤ b}dx =

Z

> R

 X

> x:ξe(x)= t

1{a < ξ e(x) ≤ b}|ξ′

> e

(x)|

 dt

=

Z ba

 X

> x:ξe(x)= t

1

|ξ′

> e

(x)|

 dt. 

Therefore, as b → a, by the fundamental theorem of calculus: 

F ′

> e

(Qe(le/2; ξe); ξe) = X

> {x:ξe(x)= Qe(le/2; ξe)}

1

|ξ′

> e

(x)| > 0.

Hence, Fe(·; ξe) is differentiable at Qe(le/2; ξe), and its inverse function Qe(·; ξe) is also differen-tiable at le/2 almost surely. This completes the proof. When Lemma 7 holds, we can define the limiting resistance matrix R∞ as 

R∞ = diag 

 1

Q′

> e

(le/2; ζe)



> e∈E



,

and the normalization immediately gives 

R∞ 

> e

=

(

lim n→∞ nR∗ 

> e

, if μ(Le(x)) ̸ ≡ ν(Le(x)) ,

lim n→∞ 

√nR∗ 

> e

, if μ(Le(x)) ≡ ν(Le(x)) .

Therefore, the resistance-based formulation in Section 2.4 applies directly to asymptotic point distribution profiles. The above analysis reveals the fact that resistance scaling differs sharply across the two distributional regimes. When μ(Le(x)) ̸ ≡ ν(Le(x)), the resistance decays at the faster rate of O(1 /n ). This is not surprising: if supply and demand are distributed differently on 17 the edge, an imbalance appears across edges, and more matched pairs must be routed through network junctions. The effective resistance therefore becomes smaller to allow larger edge flow that accommodates more inter-edge matches. In contrast, when μ(Le(x)) ≡ ν(Le(x)) and n → ∞ , most of the matching pairs occur locally, and the edge flow is generated only by negligible stochastic fluctuations. Consequently, the resistance decays more slowly, at rate O(1 /√n), reflecting a weaker likelihood of forming inter-edge matches – this property will be discussed further in the next subsection. 

## 3.2 Stochastic Flow under Identical Distributions 

In this section, we specifically focus on the case μ = ν and study the properties of cint . We will show that cint  

> e

is centered, symmetric, and sub-Gaussian for every edge e.

Theorem 8. when μ = ν, for all e ∈ E , random variable cint  

> e

has a centered, symmetric, and sub-Gaussian distribution with an O(n) variance proxy. Proof. By symmetry of inputs in (3), the optimal solution cint  

> e

is clearly centered and symmetric. We only need to show that cint  

> e

is a sub-Gaussian random variable. In so doing, we first show that 

|cint  

> e

| is bounded from above. For any e ∈ E ,Ψe(cint  

> e

; Se) = 

Z le

> 0

|Se(x) − cint  

> e

|dx ≥

Z le

> 0

|cint  

> e

| − | Se(x)| dx = le|cint  

> e

| − Ψe(0; Se).

Thus, for each e ∈ E ,

le|cint  

> e

| ≤ Ψe(cint  

> e

; Se) + Ψ e(0; Se) ≤

"X

> e∈E

Ψe(cint  

> e

; Se)

#

+ Ψ e(0; Se) = ZCF + Ψ e(0; Se),

where the second inequality follows nonnegativity of Ψ e. Hence, cint  

> e

is bounded from above by  

> 1
> le

[ZCF + Ψ e(0; Se)]. We now study the scaling and concentration properties of ZCF and Ψ e(0 , S e). From Equation (14), we know E[ZCF ] is O(√n). From Mcdiarmid’s inequality (McDiarmid, 1989), it is easy to see 

ZCF − E[ZCF ] is sub-Gaussian with an O(n) variance proxy. Similarly, we can show the properties of Ψ e(0; Se). Since Ψ e(0; Se) = R le 

> 0

|Se(x)|, we first analyze the properties of Se(x), which is a sum of 2 n independent random variables taking value from {− 1, 0, 1}. Each of these variables has a variance no larger than 1 /4, and hence: Var( Se(x)) = Var 

(" X

> u∈U

1(u ∈ L e(x)) 

#

−

"X

> v∈V

1(v ∈ L e(x)) 

#) 

= 2 nVar( 1(u ∈ L e(x))) ≤ n

2 .

Therefore by Cauchy-Schwarz inequality (Durrett, 2019), E[|Se(x)|] ≤ pn/ 2 = O(√n), which im-plies that E[Ψ e(0; Se)] = O(√n). Finally, adding or removing one supply or demand point changes 

Se(x) by at most 1 anywhere over [0 , l e], and therefore, by McDiarmid’s inequality (McDiarmid, 1989), Ψ e(0) − E[Ψ e(0)] is sub-Gaussian with an O(n) variance proxy. Therefore, if we denote Y = ZCF +Ψ e(0; Se), we can conclude that E[Y ] = O(√n) and Y −E[Y ]is sub-Gaussian with an O(n) variance proxy; i.e., there exists a positive constant C1 such that 

E[Y ] ≤ C1

√n, and for all t ≥ 0, there exists a positive constant C2 such that: 

P(|cint  

> e

| ≥ t) ≤ P(Y ≥ let) = P(Y − E[Y ] ≥ let − E[Y ]) ≤ exp 



− (let − E[Y ]) 2

C2n



. (15) It remains to be shown that for every t ≥ 0, there exists a constant C3 > 0 such that 

P(|cint  

> e

| ≥ t) ≤ 2 exp 



− t2

C3n



. (16) 18 We consider two cases. For t ≥ 2C1 

> √nle

, we have let − E[Y ] ≥ 12 let so that 

P(|cint  

> e

| ≥ t) ≤ P(Y − E[Y ] ≥ let − E[Y ]) ≤ exp 



− l2 

> e

t2

4C2n



⇔ C3 ≥ 4C2

l2

> e

.

For 0 < t < 2C1 

> √nle

, we choose C3 large enough to satisfy 2 exp 



− t2

C3n



≥ 1 ⇔ C3 ≥ 4C21

l2 

> e

log 2 .

Since a probability can at most be 1, Equation (16) holds trivially as long as we choose C3 =max { 4C2

> l2
> e

, 4C21  

> l2
> elog 2

}. This completes the proof. A direct corollary of Theorem 8 — which is formal proof for the scaling properties in Section 3.1 — is that the fraction of inter-edge matches diminishes as n → ∞ .

Corollary 1. when μ = ν, the fraction of inter-edge matches converges to zero as n → ∞ .Proof. Since E[|cint  

> e

|] ≤ pE[|cint  

> e

|2] = O(√n), the corresponding flow fe at position ( e, 0) satisfies: 

E[|fe(0) |] = E[|cint  

> e

|] = O(√n).

By symmetry, the flow at position ( e, l e) has the same scaling E[|fe(le)|] = O(√n). Since these two flow values dictate the number of points on edge e that are matched “outside of” e, then, 1

n E[|fe(0) | + |fe(le)|] = O

 1

√n

 n→∞ 

−−−−→ 0.

Summing the above across all edges, and the Corollary holds. As a final remark, the above properties provide theoretical support for the approximate network matching distance formulas in Zhai et al. (2026), which assume that the point matches are primarily local (within the same edge), and only a negligible fraction of matches are between points from different edges. The observations in the numerical experiments in that reference also indirectly corroborate these properties. 

## 3.3 A Fast Approximate Solution Algorithm under Nonidentical Dis-tributions 

As established in Section 3.1, when μ(Le(x)) ̸ ≡ ν(Le(x)), the normalized imbalance profile con-verges asymptotically to a limiting profile and leads to a limiting flow formulation, implying that the effect of sampling randomness vanishes asymptotically. This observation motivates the use of the limiting-flow formulation as a computationally inexpensive approximation for finite realized instances. However, the condition μ(Le(x)) ̸ ≡ ν(Le(x)) need not hold on every edge. We therefore first show that the stochastic contribution by those unqualified edges are asymptotically domi-nated by the deterministic flow on the remaining edges, and suitable regulation terms can be used to approximate the solution. Recall that the resistance of each edge R∗ 

> e

exhibits different asymptotic scalings according to the local imbalance μ(Le(x)) and ν(Le(x)): R∗ 

> e

is in order of O(1 /n ) when μ(Le(x)) ̸ ≡ ν(Le(x)), and in order of O(1 /√n) otherwise. Thus, edges satisfying the condition μ(Le(x)) ̸ ≡ ν(Le(x)) have a smaller resistance and can accommodate larger network-level flows. Such behavior gives rise to two asymptotic regimes: when μ = ν for the entire network, the optimal flow and matching cost are governed by stochastic fluctuations of order O(√n); when μ̸ = ν in the network, those edges with μ(Le(x)) ̸ ≡ ν(Le(x)) contribute most deterministic imbalance and asymptotically dominate the O(√n) stochastic fluctuation, resulting in network-level optimal flow and total matching cost to be on the order of n. When μ̸ = ν, some edges may be locally balanced, i.e., satisfying 19 μ(Le(x)) ≡ ν(Le(x)); for each of them, we can introduce a sufficiently small linear regulation term with constant δ > 0 that impose non-identical point distributions: 

μ(Le(x)) − ν(Le(x)) = δx. 

This regularization yields a non-degenerate limiting flow problem, from which both c∞ and R∞

are well-defined. The resulting limiting resistance can then be used to construct a fast approximate solution algorithm next. As established in Section 3.1, the normalized optimal flow has the asymptotic convergence cint /n → c∞. Equivalently cint = nc∞ + o(n), indicating that nc∞ captures the dominant O(n) component of the optimal flow. However, nc∞ generally does not satisfy the constraint I(nc∞) = I+s. Therefore, we use nc∞ as the baseline and approximate the remaining lower-order perturbation using a single feasibility projection. We define the one-shot normalized flow c1s as the R∞-weighted projection of c∞ onto the realized feasible space: 

c1s = argmin 

> c∈R|E|

12 (c − c∞)TR∞(c − c∞), s.t. Ic = I+ s

n . (17) This projection chooses the feasible flow that requires the smallest resistance-weighted correction from the limiting optimizer. Its closed-form solution is 

c1s = c∞ + ( R∞)−1IT[I(R∞)−1IT]† 

I+ s

n − Ic ∞

= c∞ + ( R∞)−1IT[I(R∞)−1IT]†I+  s

n − s∞

.

The second equality above holds because Ic ∞ = I+s∞. Thus, nc1s satisfies the original flow conservation constraint and provides a feasible estimate of the optimal objective value: 

Z1s = X

> e∈E

Ψe(nc1s ; Se).

It is observed that all quantities in the projection formula are determined by the underlying dis-tributional profiles, except for the realized imbalance vector s. The discrepancy between s/n and 

s∞ induces a correction to the limiting flow c∞. This leads to a one-shot asymptotic approxi-mation algorithm: the distribution-dependent quantities can be precomputed offline, and for each realized instance, the feasible flow is obtained by a single resistance-weighted projection. Such an offline-online decomposition scheme makes the one-shot approximation particularly useful when the matching problem is repeatedly solved on the same network under stable distributions. Once the limiting flow and the resistance matrix are precomputed, each new realization requires only the updated imbalance vector and a single projection, substantially reducing the online computational burden. 

# 4 Numerical Experiments 

This section presents numerical experiments to evaluate the proposed formulations and examine the theoretical properties of random bipartite matching on general networks. We first describe the experimental settings, including the tested networks, point distributions, and implementation details in Section 4.1. We then compare the accuracy and computational performance of determin-istic estimators ZIP , Z, ˜Z, and Z1s in Section 4.2. Last, in Section 4.3, we validate the asymptotic scaling of the total matching cost, and investigate the fluctuation of the optimal boundary flow to demonstrate asymptotically vanishing fractions of inter-edge matching. 

## 4.1 Experimental settings 

We validate the accuracy of the proposed network formulations using a series of Monte-Carlo sim-ulations. The tested networks are selected to represent increasing levels of topological complexity and size: a circular network, grid networks with different edge numbers (e.g., 4 × 4 and 31 × 31), 20 the Sioux Falls network, and the Chicago sketch network (Transportation Networks for Research Core Team, 2026). Figure 3 illustrates the corresponding network structures and the number of edges and nodes. 

Figure 3: Test graphs with geometric centers and limiting resistance. For the small-scale networks (e.g., circular, 4 × 4 grid, and Sioux Falls), we vary n ∈{ 100, 500, 1000, 1500, 2000 }. For larger-scale networks, we vary n ∈ { 500, 1000, 2000, 5000,10000, 20000 }.For large instances, e.g., when n ≥ 5000, the standard algorithm becomes computationally expen-sive. Therefore, we omit ZIP for these cases and use Z as the benchmark, based on the consistency with ZIP in smaller instances. For each instance, n supply points and n are demand points are randomly generated on the network edges according to predetermined edge-wise density distribu-tions. If we need to generate instances under μ = ν, we simply let supply and demand points to both be uniformly distributed everywhere along all network edges with equal probabilities. If we need to generate instances under μ̸ = ν, we still let supply points to be uniformly distributed everywhere in the network. However, we assign demand points heterogeneously to different edges based on the edges’ relative distances to an arbitrarily chosen “center” of the network (the red star markers in Figure 3), as follows: 

ν(A) = X

> e∈E

exp( −βd e)

P 

> e′∈E

exp( −βd e′ )

λ(A ∩ L e)

le

, ∀ A ∈ F ,

where β > 0 is a parameter that captures the relative level of heterogeneity. In other words, a demand point first “chooses” an edge e with probability exp( −βd e)/ P 

> e′∈E

exp( −βd e′ ), and is then positioned uniformly along that edge. The limiting profile and the limiting resistance are 21 easily computed as follows: 

ζe(x) = 1

L − exp( −βd e)

le

P 

> e′∈E

exp( −βd e′ ) x, ∀ e ∈ E , ∀ x ∈ [0 , l e],

R∞ = diag 1

L − exp( −βd e)

le

P 

> e′∈E

exp( −βd e′ )

> −1
> e∈E

!

.

Figure 3 visualizes the resistance of each edge when β = 10, with darker and thicker edges indicating larger resistance values. Edges near and very far away from the center generally exhibit smaller resistance because their larger supply-demand imbalance requires greater inter-edge flow across edges. Somewhere the middle range of distance, the demand density approximately equals the uniform supply density, and the resistance tends to be larger. For each combination of n, network, and distribution, 100 independent realizations are gener-ated and the optimal total matching distance ZIP is computed by the state-of-art JV algorithm (Crouse, 2016) as a benchmark. The sample mean across 100 realizations is recorded as the aver-age optimal matching distance. The computation time is also recorded. Meanwhile, to solve the convex flow-based formulations, we use the following smooth approximations: Ψe(c; Qe,ϵ ) = 

Z le

> 0

p(Se(x) − c)2 + ϵ2dx, Fe(c; Qe,ϵ ) = Ψ′

> e

(c; Qe,ϵ ) + le

2 = le

2 + 12

Z le

> 0

c − Se(x)

p(Se(x) − c)2 + ϵ2 dx, and Qe,ϵ = F −1 

> e

(·; Qe,ϵ ),

where Qe,ϵ → Qe as ϵ → 0. The smaller the value of ϵ, the closer the approximation of edge profile. 

## 4.2 Verification of deterministic estimators 

Unless otherwise specified, we set β = 10, use ϵ = 0 .1 for computing Z and Z1s , and use ϵ = 1 for computing ˜Z. The smaller value used for Z and Z1s provides a closer approximation to the original non-smooth edge potential, whereas the larger value used for ˜Z produces a smoother rearranged profile and a more stable local linearization. The sensitivity of these estimators to these choices is examined later in Section 4.2.3. 

4.2.1 Small-scale networks 

We start with the small-scale networks. Figures 4 and 5 show the objective values and relative errors for the small-scale networks. ZIP , Z, ˜Z, and Z1s are represented by black solid curves, red dashed curves with circle markers, green dotted curves with square markers, and blue dash-dot curves with triangle markers, respectively. First, it is observed that across all cases, the estimation by Z closely matches the benchmark ZIP , with average relative errors smaller than 1%; moreover, the error between them generally decreases as n increases. Second, the first-order approximation ˜Z also provides very accurate estimates in most cases, with relative errors generally below 5 %, except for the non-identical distributions case on the Sioux Falls network. For the non-identical cases, the relative error of ˜Z appears to increase as the network topology becomes more complex. This pattern arises because the first-order approximation relies on local linearization of Qe(·; Se), which is less accurate when the flow varies more across a complex network. Finally, we examine the one-shot fast approximation algorithm. The results show that Z1s becomes accurate with average errors smaller than 5% when n is sufficiently large, e.g., n ≥ 500. Its relative error consistently decreases as n becomes larger, which is consistent with our expectation. Next, we compare the computational performance of the four methods. Figures 6 shows the average running times of ZIP , Z, eZ, and Z1s in all realizations for the small networks. The average running times of Z, eZ, and Z1s remain below 0 .03, 0 .015, and 0 .005 seconds, respectively, over the tested range of n. Their running times increase only slightly with n, because the dimensions of 22 Figure 4: Comparison of objective values for small-scale networks. 

Figure 5: Comparison of relative errors for small-scale networks. 23 the corresponding flow and Laplacian systems depend primarily on the size of the network rather than on n. In contrast, the running times of ZIP increases rapidly with n because it requires creating the pairwise shortest-path cost matrix and solving the resulting assignment problem. Hence, although solving ZIP directly can be computationally efficient for very small instances, the flow-based estimators become substantially faster for sufficiently large n, e.g., n ≥ 500. Among the proposed methods, eZ is even faster than Z because it replaces the iterative Newton procedure with a single Laplacian solve, but Z1s achieves the fastest online computation time by using precomputed limiting solutions. 

Figure 6: Comparison of running time for small-scale networks. 

4.2.2 Large-scale networks 

We next validate the accuracy of estimators on larger-scale networks. Figures 7 and 8 show the results. All estimators are observed with trends similar to those observed on smaller networks: Z

provides the most accurate prediction; ˜Z is most accurate when the network structure is simple and the distributions are identical; Z1s is effective mainly in the non-identical point distribution regime when n is large. For the computational performance of the four methods, the average running times of Z, ˜Z, 

and Z1s remain below approximately 15, 0.5, and 0.1 seconds, respectively, over the tested range of 

n. Although the larger network size increases the dimensions of the flow and Laplacian systems, the running time of the proposed estimators still grow only mildly with n. In contrast, the computational cost of ZIP increases rapidly, and becomes expensive when n is large, e.g., n ≥

5000. These results demonstrate that ˜Z and Z1s provide substantial computational advantages on larger networks, with Z1s reaching the shortest online running time under non-identical point distributions. 

4.2.3 Sensitivity to parameter ϵ

We also conduct sensitivity analyses to examine how the choice of parameter ϵ affects the accuracy and computational efficiency of the estimators. We use the Sioux Falls network and fix n = 500. We vary ϵ over {0.01, 0.05, 0.1, 0.25, 0.5, 0.75, 1, 1.5, 2, 5, 10 }.Figure 10 shows the relative error of Z and ˜Z, as compared with ZIP . The results reveal a clear approximation tradeoff. The relative error of Z remains close to zero whenever ϵ is small, but increases as ϵ grows, as expected. In contrast, the error of ˜Z decreases substantially with ϵ, since 24 Figure 7: Comparison of objective values for for large-scale networks. 

Figure 8: Comparison of relative errors for for large-scale networks. 25 Figure 9: Comparison of running time for large-scale networks. the modified edge objective becomes increasingly quadratic and is therefore better represented by first-order approximations. Figure 11 shows the running time of the three methods for different ϵ. The running time of ZIP 

is independent of ϵ and therefore remains constant. In contrast, the running time of computing Z

decreases substantially under a large ϵ, e.g., ϵ ≥ 0.25. This occurs because a larger ϵ produces a smoother and better-conditioned edge objective, allowing the Newton iteration to converge more rapidly. This indicates the computational tradeoff: a smaller ϵ value provides a more accurate approximation of the original objective but requires a longer computation time, and vice versa. On the other hand, the running time of ˜Z is consistently the lowest and remains nearly unchanged across different ϵ values. In summary, one should choose the estimator with an appropriate ϵ based on the problem setting. When the network structure is simple, with a sufficiently large ϵ, the electric approximation 

Figure 10: Relative error by different values of ϵ.26 Figure 11: Running time by different values of ϵ.provides an efficient and relatively accurate estimate, especially when point distributions are nearly identical. When the network becomes more irregular and point distributions are drastically non-identical, the convex-flow formulation is more reliable and provides the most accurate estimations, and a moderate ϵ value can be used to accelerate the convergence of the Newton method. For very large-scale instances, when solving the full convex-flow problem becomes computationally expensive, the one-shot fast estimator provides a useful alternative. 

## 4.3 Verification of random bipartite matching 

In this section, we use the Sioux Falls network as an example and numerically validate the asymp-totic properties derived for random bipartite matching. 

4.3.1 Verification of scaling 

We first examine the scaling of the optimal matching cost as n increases. The theoretical anal-ysis predicts that E[Z]IP ≍ √n in identical distribution cases and E[ZIP ] ≍ n in non-identical distribution cases. To verify them, we plot in Figure 12 the normalized quantity E[ZIP ]/√n and 

E[ZIP ]/n , respectively, together with their 95% confidence intervals. In the identical distribution case, E[ZIP ]/√n remains approximately stable within a narrow range of [19 .9, 20 .6] for the wider range of n values. Similarly, in the non-identical distribution case, E[ZIP ]/n stabilizes within [8 .475 , 8.510]. These numerical results directly support the derived √n- and n-scaling properties. 

Figure 12: Verification of the scaling of normalized E[ZIP ]. 27 4.3.2 Verification of sub-Gaussian flow 

We next examine the flow properties in the identical point distribution case. Figure 13 shows the observed cint  

> e

values and fractions of inter-edge matches on randomly selected edges. In Fig-ure 13(a), for each selected edge, the 100 values of cint  

> e

obtained from independently simulated instances are represented by black circle markers; their means are represented by red horizontal lines. It is observed that, for each edge, the distribution of cint  

> e

is approximately symmetric and bell-shaped, and the mean remains close to zero. These observations are consistent with the the-oretical derivation that cint  

> e

is centered, symmetric, and sub-Gaussian. Figure 13(b) further shows the fraction of inter-edge matching among all matched pairs, which decreases steadily with n,indicating that matching becomes increasingly localized within individual edges. 

Figure 13: Verification of flow properties in identical distribution case. 

4.3.3 Verification of asymptotic convergence 

Last we examine the convergence of E[ZIP ]/n to Z∞ in the non-identical distribution cases, which is important for understanding the performance of the fast estimator Z1s . Figure 14 shows the comparison between E[ZIP ] and nZ ∞ for β ∈ { 0.1, 1, 5, 10 , 20 }. The values of E[ZIP ] and nZ ∞ are represented by solid curves and hollow markers, respectively, while different colors correspond to different values of β. For all tested values of β, the difference between E[ZIP ] and nZ ∞ decreases with n, supporting the convergence of E[ZIP ]/n to Z∞. However, the convergence is considerably slower when β is small (i.e., when demand point distribution is more uniform). For larger values of β, nZ ∞ closely approximates E[ZIP ] over most of the tested n values. 

Figure 14: Verification of the convergence of ZIP /n to Z∞.28 5 Conclusion 

This paper develops a flow model based framework for analyzing and approximating determin-istic and random minimum-distance bipartite matching problems on general networks. First, the matching problem is reformulated as a separable convex-flow problem. By representing each matched pair as one unit of flow transporting through the network, the total bipartite matching cost ZIP can be expressed as the sum of edgewise convex potential functions (determined by its cumulative supply-demand imbalance profile) that captures point matches within each edge, while network flow between edges captures point matches across edges. This reformulation replaces the full pairwise cost matrix with edgewise imbalance information and reveals how local matching costs and network-level matching jointly determine the optimal matching distance. We prove that the optimal objective value of the convex-flow formulation, ZCF , is exactly equal to ZIP .Second, to enhance computational tractability and revealing interpretable properties, we in-troduce a smooth and monotone rearranged approximation of the edge imbalance profiles. The resulted approximate smooth convex-formulation, with optimal objective value Z, can be solved efficiently through its KKT conditions using standard methods (e.g., the Newton method). Afirst-order linearization of these conditions further yields an electric-network interpretation of the bipartite matching problem. Under this interpretation, the boundary matching flow corresponds to the electric current, the Lagrangian multipliers correspond to the node potential, and each edge has an endogenous resistance induced by the local slope of the rearranged imbalance profile. This connection leads to a resistance-based approximation with objective value ˜Z that requires only a weighted Laplacian solution and therefore avoids repeated nonlinear optimization. The problem is further extended into more general settings, including supply and demand points with hetero-geneous masses, directed networks, and unbalanced matching problems with unequal number of supply and demand points. Third, we study the random bipartite matching problem, where supply and demand points are independently sampled from two predetermined spatial distributions on the network. The analysis identifies two fundamentally different limiting regimes. When supply and demand points on all edges follow identical probability distributions, the expected optimal matching distance scales with √n, and the optimal boundary flows are centered, symmetric, and sub-Gaussian. This finding justifies an approximate distance prediction formula in an earlier work (Zhai et al., 2026). As the number of points increases, matching becomes increasingly localized within each edge. When the supply and demand distributions differ in probability on at least some of the edges, a deterministic supply-demand imbalance dominates the stochastic fluctuation, and the total matching distance scales with n. In this regime, the normalized optimal flow asymptotically converges to a deterministic limiting flow, and the limiting edge resistances characterize how the distributional imbalance is “cancelled” through inter-edge matches across the network. These properties also support a one-shot approximate solution in which a limiting optimal network flow solution can be precomputed, and solution to each realized instance is a simple resistance-weighted projection from that limiting flow solution. The objective value induced by the projected flow is denoted by Z1s , which becomes increasingly accurate as the number of matched pairs grows. A series of numerical experiments are conducted to verify the theoretical results in networks with different sizes and topological structures. The smooth convex objective Z consistently pro-vides the most accurate predictions and closely ZIP , with average relative errors below 1% across all tested cases. The resistance-based estimate eZ achieves substantial computational savings and performs particularly well on simpler networks with identical point distributions. The one-shot estimator Z1s provides the fastest online computation in the non-identical distribution regime and becomes increasingly accurate when the number of matches grows. The numerical experiments also demonstrate the predicted trends of √n- and n- scaling, the concentration of the boundary flows, the asymptotically vanishing fraction of the inter-edge matches under the identical point distributions, and the convergence of the normalized matching cost to its deterministic limit under the non-identical point distributions. This study has several limitations. All proposed formulations focus primarily on static match-ing problems and the matching cost is measured by the shortest-path distance. Future studies 29 may extend the modeling framework to a more realistic scenario, including dynamic matching and more general cost functions (e.g., to account for factors such as traffic congestion and operation uncertainties). In addition, although the numerical experiments show that the resistance-based approximation performs particularly well on simple networks, the theoretical explanation for this behavior is not fully developed. Further analysis is needed to establish the convergence property or the error bound and clarify how the approximation accuracy depends on the network topology, problem scales, and imbalance in point distributions. 

# 6 Acknowledgments 

The first author used OpenAI’s ChatGPT, a large language model, to assist with visualization code refinement and enhancement. The authors reviewed and verified all AI-assisted output and remain fully responsible for the accuracy, integrity, and content of the manuscript. 30 References 

Abeywickrama, T., Liang, V., and Tan, K.-L. (2021). Optimizing bipartite matching in real-world applications by incremental cost computation. Proceedings of the VLDB Endowment ,14(7):1150–1158. Alonso-Mora, J., Samaranayake, S., Wallar, A., Frazzoli, E., and Rus, D. (2017). On-demand high-capacity ride-sharing via dynamic trip-vehicle assignment. Proceedings of the National Academy of Sciences , 114(3):462–467. Bertsekas, D. P. (1988). The auction algorithm: A distributed relaxation method for the assign-ment problem. Annals of Operations Research , 14:105–123. Caracciolo, S., D’Achille, M., and Sicuro, G. (2017). Random euclidean matching problems in one dimension. Physical Review E , 96(4). Caracciolo, S., Lucibello, C., Parisi, G., and Sicuro, G. (2014). Scaling hypothesis for the euclidean bipartite matching problem. Physical Review E , 90(1):012118. Crouse, D. F. (2016). On implementing 2d rectangular assignment algorithms. IEEE Transactions on Aerospace and Electronic Systems , 52(4):1679–1696. Durrett, R. (2019). Probability: Theory and Examples . Cambridge University Press, Cambridge, 5 edition. Evans, L. C. and Gariepy, R. F. (2015). Measure Theory and Fine Properties of Functions . CRC Press, Boca Raton, revised edition edition. Evans, S. N. and Matsen, F. A. (2012). The phylogenetic kantorovich–rubinstein metric for en-vironmental sequence samples. Journal of the Royal Statistical Society: Series B (Statistical Methodology) , 74(3):569–592. Fan, W., Chen, Z., and Gu, W. (2026). Approximation models for shared mobility rebalancing under structured spatial imbalance. Jonker, R. and Volgenant, A. (1987). A shortest augmenting path algorithm for dense and sparse linear assignment problems. Computing , 38(4):325–340. Kuhn, H. W. (1955). The hungarian method for the assignment problem. Naval Research Logistics Quarterly , 2(1–2):83–97. McDiarmid, C. (1989). On the method of bounded differences. In Siemons, J., editor, Surveys in Combinatorics, 1989 , volume 141 of London Mathematical Society Lecture Note Series , pages 148–188. Cambridge University Press. M´ ezard, M. and Parisi, G. (1985). Replicas and optimization. Journal de Physique Lettres ,46(17):771–778. Murray, C. C. and Chu, A. G. (2015). The flying sidekick traveling salesman problem: Optimiza-tion of drone-assisted parcel delivery. Transportation Research Part C: Emerging Technologies ,54:86–109. Ouyang, Y. and Yang, H. (2023). Measurement and mitigation of the “wild goose chase” phe-nomenon in taxi services. Transportation Research Part B: Methodological , 167:217–234. ¨Ozkan, E. and Ward, A. R. (2020). Dynamic matching for real-time ride sharing. Stochastic Systems , 10(1):29–70. Santambrogio, F. (2015). Optimal Transport for Applied Mathematicians: Calculus of Variations, PDEs, and Modeling . Birkh¨ auser, Cham. 31 Shen, S. and Ouyang, Y. (2026). Dynamic random bipartite matching under spatiotemporal heterogeneity. SSRN Electronic Journal . Preprint. Shen, S., Zhai, Y., and Ouyang, Y. (2024). Expected optimal distances of random bipartite matching in D-dimensional spaces: Approximate formulas and applications to mobility services. 

arXiv preprint arXiv:2406.12174 .Transportation Networks for Research Core Team (2026). Transportation networks for research. 

https://github.com/bstabler/TransportationNetworks . Chicago-Sketch dataset; accessed July 27, 2026. Treleaven, K., Bialkowski, J., and Frazzoli, E. (2013). An o(m log m) algorithm for bipartite matching with roadmap distances. arXiv preprint arXiv:1311.4609 .Treleaven, K. and Frazzoli, E. (2014). Computing earth mover’s distances on a road map with applications to one-way vehicle sharing. In 2014 American Control Conference , pages 5420–5427. Wang, G., Zhang, H., and Zhang, J. (2024). On-demand ride-matching in a spatial model with abandonment and cancellation. Operations Research , 72(3):1278–1297. Yang, H., Qin, X., Ke, J., and Ye, J. (2020). Optimizing matching time interval and matching radius in on-demand ride-sourcing markets. Transportation Research Part B: Methodological ,131:84–105. Yildiz, B. and Savelsbergh, M. (2019). Provably high-quality solutions for the meal delivery routing problem. Transportation Science , 53(5):1372–1388. Zhai, Y., Shen, S., and Ouyang, Y. (2026). Average distance of random bipartite matching in one-dimensional spaces and networks. Transportation Science , 60(3):367–386. Zheng, L., Chen, L., and Ye, J. (2018). Order dispatch in price-aware ridesharing. Proceedings of the VLDB Endowment , 11(8):853–865. 32 7 Notation table 

G: undirected network 

V: set of nodes in G

E: set of edges in Ge: an edge in E

v: a node in V

le: length of edge e

l: vector of edge lengths 

L: total network length 

v− 

> e

, v + 

> e

: two endpoint nodes of edge eδ−(v): set of outgoing edges from node vδ+(v): set of incoming edges to node vx: local coordinate on an edge 

xw: coordinate of point w on its edge (e, x ): position on edge e with local coordinate x

Le(x): set of positions on edge e between 0 and xU : set of supply points 

V : set of demand points 

Ue: set of supply points on edge eVe: set of demand points on edge en: number of supply points and demand points in the balanced case 

m: number of demand points in the unbalanced case 

d(·, ·): shortest-path distance on the network 

yuv : binary matching decision between supply point u and demand point vZIP : optimal objective value of the integer-programming matching formulation 

fe(x): net flow at position ( e, x )

Se(x): cumulative supply-demand imbalance profile on edge e

s: vector of edgewise total imbalances 

ce: boundary-flow shift on edge e

c: vector of boundary-flow shifts 

ξe: generic edge imbalance profile on edge e

Ψe(ce; ξe): edge potential induced by boundary value ce and profile ξe

ZCF : optimal objective value of the integer-valued convex-flow formulation 

ZRLX : optimal objective value of the continuous relaxation of the convex-flow formulation 

I+: incoming node-edge incidence matrix 

I−: outgoing node-edge incidence matrix 

I: incidence matrix 

cint : optimal integer-valued boundary-flow solution 

crlx : optimal solution of the relaxed convex-flow formulation 

crnd : rounded flow vector obtained from the relaxed solution 

cfes : integral feasible flow vector after flow-conservation repair ∆: flow-conservation residual after rounding 

Fe(c; ξe): cumulative length function of profile ξe on edge eF − 

> e

(c; ξe): left-limit cumulative length function 

Qe(·; ξe): generalized inverse of Fe

Qe(·; Se): monotone rearrangement of Se

Qe,ϵ : smooth approximation of Qe(·; Se)

ϵ: smoothing parameter 

Z: optimal objective value of the smooth approximate convex-flow formulation 

c∗: optimal solution of the smooth approximate convex-flow formulation 

Λ: Lagrangian multiplier vector for node conservation constraints 

Λ∗: optimal Lagrangian multiplier vector 

x0,e : reference point on edge e for Taylor linearization 33 x0: vector of reference points 

c0(x0): baseline flow vector from the local uncoupled optimum 

R(x0): diagonal resistance matrix at reference points x0

L(x0): weighted graph Laplacian induced by R(x0)

c∗

> 0

: midpoint baseline flow vector, evaluated at l/2

R∗: midpoint resistance matrix 

L∗: midpoint weighted Laplacian matrix ˜Λ: linearized approximation of the Lagrangian multiplier ˜c: electric or resistance-based approximate flow ˜Z: objective value obtained by evaluating ˜ c in the original edge potential 

∥ · ∥ R: resistance-weighted norm 

r: Taylor remainder vector in the electric approximation 

M : number of partitions used to subdivide each edge 

k: number of virtual absorption points per edge in the unbalanced approximation 

Vk: node set of the subdivided network induced by virtual points 

Ek: edge set of the subdivided network induced by virtual points 

a: absorbed-flow vector in the unbalanced formulation 

1: vector of ones with compatible dimension 

μ: probability measure for supply-point locations 

ν: probability measure for demand-point locations 

F: σ-algebra on the network sample space 

ζe(x): limiting normalized imbalance profile on edge e

s∞: limiting vector of terminal imbalance values 

Z∞: limiting objective value of the normalized convex-flow problem 

c∞: optimal solution of the limiting convex-flow problem 

B: standard Brownian bridge 

R∞: limiting resistance matrix 

L∞: limiting weighted Laplacian matrix 

c1s : one-shot asymptotic flow approximation 

Z1s : objective value induced by the one-shot approximation 

ninter  

> e

: number of matches crossing edge boundary on edge ewe: center-weighted distance score assigned to edge e

34