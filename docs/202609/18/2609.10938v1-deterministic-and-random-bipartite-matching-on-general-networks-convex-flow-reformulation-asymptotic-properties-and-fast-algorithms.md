---
title: "Deterministic and Random Bipartite Matching on General Networks: Convex Flow Reformulation, Asymptotic Properties, and Fast Algorithms"
source: arxiv
title_zh: "Deterministic与Random Bipartite Matching on General Networks：Convex Flow Reformulation，Asymptotic Properties,与Fast Algorithms"
abstract_zh: "In latter case，limiting resistance 网络 characterizes how supply-demand imbalance is redistributed 与 m。\n\n核心做法：This paper first focuses on deterministic problems 与 presents exact edgewise-separable convex-flow r。\n\n主要结果：We show that expected optimal matching distance scales 结合 square root 的 number 的 points if supply/de。\n\n值得关注的点在于：proven theoretical properties 的 random matching solution are numerically verified by large-scale Mon。"
abstract_en: "Minimum-distance bipartite matching on general networks has numerous applications various fields. This paper first focuses on deterministic problems and presents an exact edgewise-separable convex-flow reformulation. By introducing a smooth monotone rearrangement approximation of the edge-wise imbalance profiles, the convex-flow reformulation's can be solved efficiently. If we further conduct a first-order resistance-based approximation of the convex program, a one-step Laplacian-based estimator can be analytically derived in closed forms. The paper also studies random problems where supply and demand points are randomly distributed. We show that the expected optimal matching distance scales with the square root of the number of points if the supply/demand point distributions are identical, or linearly otherwise. In the former case, the optimal flow is proven to be centered, symmetric, and sub-Gaussian. In the latter case, the limiting resistance network characterizes how supply-demand imbalance is redistributed and motivates a fast algorithm that approximate the optimal flow based on the limiting resistance. Numerical experiments show that the proposed estimators closely approximate the exact matching cost while substantially reducing computation time. The proven theoretical properties of the random matching solution are numerically verified by large-scale Monte Carlo simulations."
authors: "Yuhui Zhai, Yanfeng Ouyang"
date: 2026-09-10
journal: math.OC
source_link: "https://arxiv.org/pdf/2609.10938v1"
pdf: "https://arxiv.org/pdf/2609.10938v1"
score: 6.0
tldr: 围绕《Deterministic与Random Bip...》；This paper first focuses on dete...；We show that expected optimal matchi...
tldr_cn: 围绕《Deterministic与Random Bip...》；This paper first focuses on dete...；We show that expected optimal matchi...
tldr_en: Minimum-distance bipartite matching on general networks has numerous applications various fields. This paper first focuses on deterministic problems and presents an exact edgewise-separable convex-flow reformulation.
selection_source: fresh_fetch
motivation: In latter case，limiting resistance 网络 characterizes how supply-demand imbalance is redistributed 与 m。
method: 核心做法：This paper first focuses on deterministic problems 与 presents exact edgewise-separable convex-flow r。
result: 主要结果：We show that expected optimal matching distance scales 结合 square root 的 number 的 points if supply/de。
conclusion: 值得关注的点在于：proven theoretical properties 的 random matching solution are numerically verified by large-scale Mon。
---

## 摘要
In latter case，limiting resistance 网络 characterizes how supply-demand imbalance is redistributed 与 m。

核心做法：This paper first focuses on deterministic problems 与 presents exact edgewise-separable convex-flow r。

主要结果：We show that expected optimal matching distance scales 结合 square root 的 number 的 points if supply/de。

值得关注的点在于：proven theoretical properties 的 random matching solution are numerically verified by large-scale Mon。

## Abstract
Minimum-distance bipartite matching on general networks has numerous applications various fields. This paper first focuses on deterministic problems and presents an exact edgewise-separable convex-flow reformulation. By introducing a smooth monotone rearrangement approximation of the edge-wise imbalance profiles, the convex-flow reformulation's can be solved efficiently. If we further conduct a first-order resistance-based approximation of the convex program, a one-step Laplacian-based estimator can be analytically derived in closed forms. The paper also studies random problems where supply and demand points are randomly distributed. We show that the expected optimal matching distance scales with the square root of the number of points if the supply/demand point distributions are identical, or linearly otherwise. In the former case, the optimal flow is proven to be centered, symmetric, and sub-Gaussian. In the latter case, the limiting resistance network characterizes how supply-demand imbalance is redistributed and motivates a fast algorithm that approximate the optimal flow based on the limiting resistance. Numerical experiments show that the proposed estimators closely approximate the exact matching cost while substantially reducing computation time. The proven theoretical properties of the random matching solution are numerically verified by large-scale Monte Carlo simulations.
