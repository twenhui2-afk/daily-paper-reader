---
title: "UI-VISA: U-Net Initialized Vascular Image Segmentation Architecture"
source: arxiv
title_zh: UI-VISA：U-Net Initialized Vascular Image Segmentation 架构
abstract_zh: "UI-VISA 达到 highest mean Dice 与 clDice scores across folds，与 paired Wilcoxon signed-rank test shows i。\n\n核心做法：作者提出 UI-VISA (U-Net Initialized Vascular Image Segmentation 架构)，hybrid pipeline that combines comple。\n\n主要结果：Pixel-wise deep learning approaches such as U-Net 达到 strong general-purpose segmentation 性能 but ofte。\n\n值得关注的点在于：Accurate segmentation 的 vascular structures in digital subtraction angiography (DSA) images remains。"
abstract_en: "Accurate segmentation of vascular structures in digital subtraction angiography (DSA) images remains challenging due to the thin, elongated, and branching nature of blood vessels. Pixel-wise deep learning approaches such as U-Net achieve strong general-purpose segmentation performance but often produce fragmented or discontinuous predictions in fine vascular regions, since they do not explicitly enforce structural connectivity. Region growing algorithms preserve spatial context and topological continuity, but are highly sensitive to seed point initialization and can be computationally expensive. We propose UI-VISA (U-Net Initialized Vascular Image Segmentation Architecture), a hybrid pipeline that combines the complementary strengths of both approaches. UI-VISA uses U-Net's foreground predictions as informed seed points for a CNN-guided region growing algorithm, which then iteratively refines the segmentation by enforcing local connectivity and recovering fine vessel details that U-Net alone tends to miss or over-predict. We evaluate UI-VISA against standalone U-Net and a prior region-growing-based method (VISA) using 5-fold cross-validation on 26 DSA images. UI-VISA achieves the highest mean Dice and clDice scores across folds, and a paired Wilcoxon signed-rank test shows the improvement in clDice is statistically significant ($p=0.023$), consistent with the method's design goal of preserving vascular connectivity, while the improvement in Dice does not reach significance ($p=0.104$)."
authors: "Asees Kaur, Suzanne S. Sindi, Erica M. Rutter"
date: 2026-09-01
journal: cs.CV
source_link: "https://arxiv.org/pdf/2609.01598v1"
pdf: "https://arxiv.org/pdf/2609.01598v1"
score: 6.75
tldr: 围绕《UI-VISA：U-Net Initialize...》；作者提出 UI-VISA (U-Net Initialized...；Pixel-wise deep learning approaches...
tldr_cn: 围绕《UI-VISA：U-Net Initialize...》；作者提出 UI-VISA (U-Net Initialized...；Pixel-wise deep learning approaches...
tldr_en: "Accurate segmentation of vascular structures in digital subtraction angiography (DSA) images remains challenging due to the thin, elongated, and branching nature of blood vessels. Pixel-wise deep learning approaches such as U-Net achieve strong general-purpose segmentation performance but often produce fragmented or discontinuous predictions in fine vascular regions, since they do not explicitly enforce structural connectivity."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS"]
motivation: UI-VISA 达到 highest mean Dice 与 clDice scores across folds，与 paired Wilcoxon signed-rank test shows i。
method: 核心做法：作者提出 UI-VISA (U-Net Initialized Vascular Image Segmentation 架构)，hybrid pipeline that combines comple。
result: 主要结果：Pixel-wise deep learning approaches such as U-Net 达到 strong general-purpose segmentation 性能 but ofte。
conclusion: 值得关注的点在于：Accurate segmentation 的 vascular structures in digital subtraction angiography (DSA) images remains。
---

## 摘要
UI-VISA 达到 highest mean Dice 与 clDice scores across folds，与 paired Wilcoxon signed-rank test shows i。

核心做法：作者提出 UI-VISA (U-Net Initialized Vascular Image Segmentation 架构)，hybrid pipeline that combines comple。

主要结果：Pixel-wise deep learning approaches such as U-Net 达到 strong general-purpose segmentation 性能 but ofte。

值得关注的点在于：Accurate segmentation 的 vascular structures in digital subtraction angiography (DSA) images remains。

## Abstract
Accurate segmentation of vascular structures in digital subtraction angiography (DSA) images remains challenging due to the thin, elongated, and branching nature of blood vessels. Pixel-wise deep learning approaches such as U-Net achieve strong general-purpose segmentation performance but often produce fragmented or discontinuous predictions in fine vascular regions, since they do not explicitly enforce structural connectivity. Region growing algorithms preserve spatial context and topological continuity, but are highly sensitive to seed point initialization and can be computationally expensive. We propose UI-VISA (U-Net Initialized Vascular Image Segmentation Architecture), a hybrid pipeline that combines the complementary strengths of both approaches. UI-VISA uses U-Net's foreground predictions as informed seed points for a CNN-guided region growing algorithm, which then iteratively refines the segmentation by enforcing local connectivity and recovering fine vessel details that U-Net alone tends to miss or over-predict. We evaluate UI-VISA against standalone U-Net and a prior region-growing-based method (VISA) using 5-fold cross-validation on 26 DSA images. UI-VISA achieves the highest mean Dice and clDice scores across folds, and a paired Wilcoxon signed-rank test shows the improvement in clDice is statistically significant ($p=0.023$), consistent with the method's design goal of preserving vascular connectivity, while the improvement in Dice does not reach significance ($p=0.104$).

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
