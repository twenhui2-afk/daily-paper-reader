---
title: "LISynSeg: Data-Centric Label-to-Image Synthesis for Cross-Modality Whole-Heart Segmentation"
source: arxiv
title_zh: 用于Cross-Modality Whole-Heart Segmentation的LISynSeg：Data-Centric Label-to-Image Synthesis
abstract_zh: "Whole-heart segmentation (WHS) in computed tomography (CT) 与 magnetic resonance imaging (MRI) is aff。\n\n核心做法：We investigate whether changes to data augmentation 与 training supervision can improve cross-modalit。\n\n主要结果：results show that modifying training data strategy can benefit 模型 development 用于 heterogeneous cardi。\n\n值得关注的点在于：Code 与 trained weights will be released at https://github.com/MedICL-VU/Care26_LISynSeg。"
abstract_en: "Whole-heart segmentation (WHS) in computed tomography (CT) and magnetic resonance imaging (MRI) is affected by acquisition shifts and heterogeneous cardiac annotations. Existing WHS systems combine architectural design, transfer learning, and generic spatial or intensity augmentation. We investigate whether changes to data augmentation and training supervision can improve cross-modality WHS while the segmentation architecture is held constant. We present LISynSeg, a data-centric approach that augments real-image nnU-Net training with label-to-image synthesis. Synthetic volumes are generated from cardiac label maps using contrast and acquisition perturbations calibrated to the training cohort, then mixed with real images to retain thoracic context absent from the labels (and thus the synthesized images). We model cardiac label variation through controlled changes in myocardial wall thickness and partial supervision of uncertain vessel endpoints. On the CARE Whole-Heart benchmark, synthetic-only training performs worse than the real-image nnU-Net baseline, whereas calibrated real-synthetic training improves cross-modality segmentation without changing the architecture; the improvement is larger for MRI than for CT. The results show that modifying the training data strategy can benefit model development for heterogeneous cardiac data. Code and trained weights will be released at https://github.com/MedICL-VU/Care26_LISynSeg."
authors: "Jiacheng Wang, Ivana Isgum, Ipek Oguz"
date: 2026-08-31
journal: cs.CV
source_link: "https://arxiv.org/pdf/2608.31073v1"
pdf: "https://arxiv.org/pdf/2608.31073v1"
score: 6.8999999999999995
tldr: 围绕《用于Cross-Modality Whole-H...》；We investigate whether changes t...；results show that modifying training...
tldr_cn: 围绕《用于Cross-Modality Whole-H...》；We investigate whether changes t...；results show that modifying training...
tldr_en: "Whole-heart segmentation (WHS) in computed tomography (CT) and magnetic resonance imaging (MRI) is affected by acquisition shifts and heterogeneous cardiac annotations. Existing WHS systems combine architectural design, transfer learning, and generic spatial or intensity augmentation."
selection_source: fresh_fetch
vocabulary: ["augmentation | 数据增强 | CET6", "segmentation | 分割 | CET6/IELTS", "benchmark | 基准测试 | CET6/IELTS", "synthesis | 合成 | CET6"]
motivation: Whole-heart segmentation (WHS) in computed tomography (CT) 与 magnetic resonance imaging (MRI) is aff。
method: 核心做法：We investigate whether changes to data augmentation 与 training supervision can improve cross-modalit。
result: 主要结果：results show that modifying training data strategy can benefit 模型 development 用于 heterogeneous cardi。
conclusion: "值得关注的点在于：Code 与 trained weights will be released at https://github.com/MedICL-VU/Care26_LISynSeg。"
---

## 摘要
Whole-heart segmentation (WHS) in computed tomography (CT) 与 magnetic resonance imaging (MRI) is aff。

核心做法：We investigate whether changes to data augmentation 与 training supervision can improve cross-modalit。

主要结果：results show that modifying training data strategy can benefit 模型 development 用于 heterogeneous cardi。

值得关注的点在于：Code 与 trained weights will be released at https://github.com/MedICL-VU/Care26_LISynSeg。

## Abstract
Whole-heart segmentation (WHS) in computed tomography (CT) and magnetic resonance imaging (MRI) is affected by acquisition shifts and heterogeneous cardiac annotations. Existing WHS systems combine architectural design, transfer learning, and generic spatial or intensity augmentation. We investigate whether changes to data augmentation and training supervision can improve cross-modality WHS while the segmentation architecture is held constant. We present LISynSeg, a data-centric approach that augments real-image nnU-Net training with label-to-image synthesis. Synthetic volumes are generated from cardiac label maps using contrast and acquisition perturbations calibrated to the training cohort, then mixed with real images to retain thoracic context absent from the labels (and thus the synthesized images). We model cardiac label variation through controlled changes in myocardial wall thickness and partial supervision of uncertain vessel endpoints. On the CARE Whole-Heart benchmark, synthetic-only training performs worse than the real-image nnU-Net baseline, whereas calibrated real-synthetic training improves cross-modality segmentation without changing the architecture; the improvement is larger for MRI than for CT. The results show that modifying the training data strategy can benefit model development for heterogeneous cardiac data. Code and trained weights will be released at https://github.com/MedICL-VU/Care26_LISynSeg.

## 英语词汇
- **augmentation**：数据增强（CET6）
- **segmentation**：分割（CET6/IELTS）
- **benchmark**：基准测试（CET6/IELTS）
- **synthesis**：合成（CET6）
