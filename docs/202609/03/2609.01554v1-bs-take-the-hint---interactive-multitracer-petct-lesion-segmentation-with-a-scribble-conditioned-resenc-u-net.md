---
title: "BS: Take the Hint - Interactive Multitracer PET/CT Lesion Segmentation with a Scribble-Conditioned ResEnc U-Net"
source: arxiv
title_zh: 结合Scribble-Conditioned ResEnc U-Net的BS：Take Hint - Interactive Multitracer PET/CT Lesion Segmentation
abstract_zh: "autoPET/CT V challenge addresses this by making segmentation interactive：user scribbles marking fore。\n\n核心做法：作者提出 our submission，scribble-conditioned residual encoder U-Net operating on four input channels：CT。\n\n主要结果：About 85% 的 that gain follows first scribble，与 spread between fold 模型s narrows five-fold over same r。\n\n值得关注的点在于：网络 is initialised from autoPET-III winning weights 与 extended from two to four input channels，结合 two。"
abstract_en: "Automated lesion segmentation in whole-body PET/CT is complicated by the variety of physiological tracer uptake patterns and by the differing appearance of lesions across tracers. The autoPET/CT V challenge addresses this by making segmentation interactive: user scribbles marking foreground and background are supplied alongside the image, and the algorithm is expected to exploit them. We present our submission, a scribble-conditioned residual encoder U-Net operating on four input channels: CT, PET, and a sparse scribble map for each of foreground and background. The network is initialised from the autoPET-III winning weights and extended from two to four input channels, with the two scribble channels zero-initialised so that the pretrained representation is preserved exactly at initialisation. Every model is fine-tuned per fold from the corresponding autoPET-III fold checkpoint, so that no validation case is seen during pretraining. PET intensities are normalised against a per-scan aorta blood-pool reference derived from a CT segmentation, which removes tracer- and centre-specific scaling without requiring lesion labels. At inference the five fold models are ensembled by averaging their softmax outputs per sliding-window patch, before Gaussian-weighted stitching. On the challenge's five-fold split, with each fold evaluated on its own validation cases, mean Dice is 0.554 and mean lesion-level F1 is 0.528 without scribbles, rising to 0.751 and 0.733 after five correction rounds. About 85% of that gain follows the first scribble, and the spread between fold models narrows five-fold over the same rounds, so interaction largely compensates for how well or badly a given model segments unaided."
authors: "Marven Sherif, Amgad Elmasry, Youssef Ghazal, Ayman Elghotni"
date: 2026-09-01
journal: cs.CV
source_link: "https://arxiv.org/pdf/2609.01554v1"
pdf: "https://arxiv.org/pdf/2609.01554v1"
score: 6.35
tldr: "围绕《结合Scribble-Conditioned R...》；作者提出 our submission，scribble-con...；About 85% 的 that gain follows first..."
tldr_cn: "围绕《结合Scribble-Conditioned R...》；作者提出 our submission，scribble-con...；About 85% 的 that gain follows first..."
tldr_en: "Automated lesion segmentation in whole-body PET/CT is complicated by the variety of physiological tracer uptake patterns and by the differing appearance of lesions across tracers. The autoPET/CT V challenge addresses this by making segmentation interactive: user scribbles marking foreground and background are supplied alongside the image, and the algorithm is expected to exploit them."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "pretraining | 预训练 | 学术高频", "inference | 推理 | CET6", "lesion | 病损 | IELTS"]
motivation: autoPET/CT V challenge addresses this by making segmentation interactive：user scribbles marking fore。
method: 核心做法：作者提出 our submission，scribble-conditioned residual encoder U-Net operating on four input channels：CT。
result: "主要结果：About 85% 的 that gain follows first scribble，与 spread between fold 模型s narrows five-fold over same r。"
conclusion: 值得关注的点在于：网络 is initialised from autoPET-III winning weights 与 extended from two to four input channels，结合 two。
---

## 摘要
autoPET/CT V challenge addresses this by making segmentation interactive：user scribbles marking fore。

核心做法：作者提出 our submission，scribble-conditioned residual encoder U-Net operating on four input channels：CT。

主要结果：About 85% 的 that gain follows first scribble，与 spread between fold 模型s narrows five-fold over same r。

值得关注的点在于：网络 is initialised from autoPET-III winning weights 与 extended from two to four input channels，结合 two。

## Abstract
Automated lesion segmentation in whole-body PET/CT is complicated by the variety of physiological tracer uptake patterns and by the differing appearance of lesions across tracers. The autoPET/CT V challenge addresses this by making segmentation interactive: user scribbles marking foreground and background are supplied alongside the image, and the algorithm is expected to exploit them. We present our submission, a scribble-conditioned residual encoder U-Net operating on four input channels: CT, PET, and a sparse scribble map for each of foreground and background. The network is initialised from the autoPET-III winning weights and extended from two to four input channels, with the two scribble channels zero-initialised so that the pretrained representation is preserved exactly at initialisation. Every model is fine-tuned per fold from the corresponding autoPET-III fold checkpoint, so that no validation case is seen during pretraining. PET intensities are normalised against a per-scan aorta blood-pool reference derived from a CT segmentation, which removes tracer- and centre-specific scaling without requiring lesion labels. At inference the five fold models are ensembled by averaging their softmax outputs per sliding-window patch, before Gaussian-weighted stitching. On the challenge's five-fold split, with each fold evaluated on its own validation cases, mean Dice is 0.554 and mean lesion-level F1 is 0.528 without scribbles, rising to 0.751 and 0.733 after five correction rounds. About 85% of that gain follows the first scribble, and the spread between fold models narrows five-fold over the same rounds, so interaction largely compensates for how well or badly a given model segments unaided.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **pretraining**：预训练（学术高频）
- **inference**：推理（CET6）
- **lesion**：病损（IELTS）
