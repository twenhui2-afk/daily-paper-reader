Title: A sharp density bound for 5-connected graphs with no  minor

URL Source: https://arxiv.org/pdf/2609.26041v1

Published Time: Wed, 23 Sep 2026 01:04:26 GMT

Number of Pages: 24

Markdown Content:
# A sharp density bound for 5-connected graphs with no K=7 minor 

## Caibing Chang 1∗ 

## , Zijian Deng 2† 

## , Qinfei Tang 3‡ and Caihong Yang 4§ 

> 1

Faculty of Science Guangdong University of Petrochemical Technology, Maoming, Guangdong 525000, China 

> 2

School of Mathematics and Statistics Lingnan Normal University, Zhanjiang, Guangdong 524048, China 

> 3

Department of Mathematics Research Fujian Institute of Education, Fuzhou, China 

> 4

School of Mathematics and Physics China University of Geosciences, Wuhan, China 

Abstract 

Let K=7 be obtained from K7 by deleting two independent edges. We prove that every 5-connected graph on n ≥ 7 vertices with at least 4 n − 9 edges contains a 

K=7 minor, settling Conjecture 1.4 of Dvořák, Norin and Rahman (arXiv preprint 2609.17760v1). The bound is sharp. We prove the stronger statement that every 4-bilight graph on n ≥ 4 vertices with at least 4 n − 9 edges contains either a K=7 minor or a K6 subgraph. Within their reduction framework, we strengthen the rooted-minor theorem. We show that every 4-light 5-rooted graph of rooted 4-density at least two has a model with two nonroot vertices and at most one missing edge incident with them. At the critical density, reductions preserve density exactly, which prevents them from creating a new K6 subgraph. 

Keywords Graph minor, extremal graph theory, rooted minor, connectivity. 

MSC 2020 05C35, 05C83, 05C40. 

# 1 Introduction 

All graphs are finite and simple, and e(G) = |E(G)|. Extremal graph-minor theory asks how many edges force a prescribed graph as a minor. Complete targets have been central to this question since Mader’s work [5]. Thomason [10] determined the asymptotic extremal function for large complete minors. For small complete targets, Jørgensen [4] determined the extremal function for K8 minors, and Song and Thomas [9] did so for K9 minors. For a nearly complete target, Song [8] determined the extremal function for K− 

> 8

minors. Here the target is K=7 , obtained from K7 by deleting two independent edges. It lies between K6 and K7 in the minor order, since contracting an edge joining endpoints of its 

> ∗

Email: changcaibing2018@163.com. 

> †

Email: zj1329205716@163.com. 

> ‡

Email: tqf9500@126.com (corresponding author). 

> §

Email: yangch@cug.edu.cn. 

1arXiv:2609.26041v1 [math.CO] 22 Sep 2026 two missing edges gives a K6 minor. We write K− 

> 7

for deletion of a single edge and K∨ 

> 7

for deletion of two edges with a common end. We study the exact density threshold for K=7

under 5-connectivity, where separations of order at most four are excluded. Jakobsen’s earlier extremal theorem concerns the family consisting of K=7 and K∨ 

> 7

. In the formulation recalled by Rolek [7], every graph on n ≥ 6 vertices with at least 4 n − 9edges either contains one of these two graphs as a minor or is a ( K6, 3)-cockade. Such a cockade is obtained recursively from copies of K6 by identifying triangles in two previously constructed graphs. For 5-connected graphs on at least seven vertices, the cockade exception is excluded, but the conclusion still allows either deletion pattern. Theorem 1.1 guarantees the specified graph K=7 at the same edge threshold. Dvořák, Norin and Rahman [2, Theorem 1.3] proved that every 5-connected graph on 

n ≥ 6 vertices with at least 4 n − 7 edges contains a K=7 minor. Their density theorem shows that K=7 -minor-free graphs are 6-colourable. They conjectured that the threshold could be lowered to 4 n − 9 when n ≥ 7. We prove this conjecture. 

Theorem 1.1. Every 5-connected graph G with n ≥ 7 vertices and e(G) ≥ 4n − 9 contains 

K=7 as a minor. 

We use the notion of 4-bilightness introduced in [2]. For a nonempty set Y ⊆ V (G), put 

∂GY = NG(Y ) \ Y, ρ4(G, Y ) = e(G[Y ]) + eG(Y, ∂ GY ) − 4|Y |.

The graph G is 4 -bilight if there do not exist disjoint, nonadjacent, nonempty vertex sets 

Y, Z such that 

|∂GY |, |∂GZ| ≤ 4, ρ4(G, Y ) > 0, ρ4(G, Z ) > 0.

Thus 4-bilightness rules out two separated regions that both have positive density behind boundaries of size at most four. Every 5-connected graph is 4-bilight. Indeed, a set with boundary at most four cannot have both itself and the complement of its closed neighbourhood nonempty. We prove the following stronger statement for 4-bilight graphs. 

Theorem 1.2. Let G be a 4-bilight graph with n ≥ 4 vertices and at least 4n − 9 edges. Then G contains K=7 as a minor or contains K6 as a subgraph. Proof of Theorem 1.1 from Theorem 1.2. Apply Theorem 1.2. Suppose that G contains a 6-vertex clique C. Since n ≥ 7, there is a nonempty component U of G − C. 5-connectivity gives |NG(U ) ∩ C| ≥ 5. Contracting U to a vertex and retaining C gives a K− 

> 7

minor, which contains K=7 .We also prove the following auxiliary theorem. 

Theorem 1.3. Every 4-bilight graph with n ≥ 3 vertices and at least 4n − 8 edges contains 

K=7 as a minor. 

The bound in Theorem 1.1 is sharp. For each ℓ ≥ 4, let Gℓ = P3 ∨ Cℓ, where ∨ denotes the join of two disjoint graphs. Then n = ℓ + 3 and 

e(Gℓ) = 2 + ℓ + 3 ℓ = 4 n − 10 .

Deleting at most four vertices leaves a connected graph. If both joined parts survive, this is immediate. If all three vertices of P3 are deleted, at most one vertex of the cycle is deleted. Deleting the entire cycle is possible only when ℓ = 4, in which case P3 survives. Thus Gℓ

2is 5-connected. Deleting the middle vertex of P3 leaves the planar bipyramid 2 K1 ∨ Cℓ.An apex graph has no K6 minor. A model avoiding the apex would lie in a planar graph, and deleting the branch set containing the apex from any other K6 model would leave a K5 model in a planar graph. Since K=7 contains a K6 minor (contract an edge joining endpoints of the two missing edges), Gℓ has no K=7 minor. The exception in Theorem 1.2 is also necessary. For t ≥ 1, consider 

Ft = K4 ∨ (K2 ⊔ tK 1). (1.1) This graph has n = t + 6 vertices and 4 n − 9 edges. Two nonempty nonadjacent sets avoid the universal K4. A set outside this K4 with boundary at most four has positive 4-density precisely when it contains both vertices of the distinguished K2. Hence two such positive sets cannot be disjoint, and Ft is 4-bilight. To see that Ft has no K=7 minor, suppose that a model exists. At most four of its seven branch sets meet the universal K4, so at least three branch sets avoid it. Every induced subgraph of K=7 on at least three vertices is connected. Those branch sets must therefore lie in a single component of K2 ⊔ tK 1, which has at most two vertices, a contradiction. Finally, Ft contains a K6 subgraph, as required by Theorem 1.2. Our proof follows the reduction method of Dvořák, Norin and Rahman [2]. Sections 2 and 4 adapt their reduction, linkage and separation arguments, keeping track of the number b ∈ { 3, 4} of vertices that must remain after a reduction. The rooted-minor results of [1, 2, 6], collected in Theorem 2.1, are used with their original hypotheses. Section 3 proves the rooted-minor lemmas needed at the lower density. Lemma 3.1 extends the internally 4-connected result to 4-light 4-rooted graphs of positive density. Theorem 3.3 gives a model in W+ at rooted density two, with at most one missing edge incident with a nonroot. This allows one root edge to remain missing on the other side of a 5-separation, provided the two missing edges are independent. Lemma 3.9 uses this choice to join the sides when r1 + r2 ≥ m + 1, where m counts the missing separator edges. Section 5 first proves Theorem 1.3. At density 4 n − 9, this theorem forces every admissible reduction to preserve density exactly. The density increment is the sum of the negative density of the removed fragment and the number of added boundary edges. Both are nonnegative, so both must vanish. Thus no edge is added between surviving vertices, and no new K6 subgraph is created. This permits the minimal-counterexample proof of Theorem 1.2. 

# 2 Preliminaries 

Fabila-Monroy and Wood [3] characterized the graphs that contain a K4 minor rooted at four prescribed vertices. Here we use density conditions to force rooted targets, principally with four or five roots. A rooted graph is a graph R together with a specified root set XR. A rooted model consists of pairwise disjoint nonempty connected branch sets, one for each target vertex, such that target edges are represented by edges between the corresponding branch sets. Each root branch set contains its prescribed root, and no other original root. Extra edges may be deleted. When the target has an independent root set, edges between original roots neither help nor obstruct such a model. We write 

ρ4(R) = e(R) − e(R[XR]) − 4|V (R) \ XR|.

For an unrooted graph we instead write ρ4(G) = e(G) − 4|V (G)|. The meaning will always be determined by whether roots have been specified. 3A fragment of a rooted graph is a nonempty set avoiding the roots. In an unrooted graph any nonempty vertex set is a fragment. A fragment Y is a k-fragment if |∂Y | = k,and a ( ≤ k)-fragment if |∂Y | ≤ k. Its associated rooted graph is 

RY = ( G[Y ∪ ∂Y ], ∂Y ), ρ4(RY ) = ρ4(G, Y ).

A k-rooted graph is 4 -light if every ( ≤ k − 1)-fragment has nonpositive 4-density. It is 

internally k-connected if there is no such fragment. A pair of disjoint nonadjacent fragments is a bifragment . It is dense if both fragment densities are positive. A separation (A, B ) of G has A ∪ B = V (G) and no edge between A \ B and B \ A.Its order is |A ∩ B|, and its right-hand side is RA,B = ( G[B], A ∩ B). In a rooted graph, a 

root separation additionally satisfies XG ⊆ A. A root separation of order |XG| is proper if 

A̸ = XG. Then |B| < |V (G)|. Right-hand sides of order at most the number of original roots inherit 4-lightness whenever they are contained in a 4-light rooted graph. Indeed, a smaller-boundary fragment in the right-hand side has exactly the same boundary and density in the original graph. For a root separation ( A, B ), an isolator is a root separation ( C, D ) of minimum order subject to C ⊆ A and B ⊆ D. By Menger’s theorem, its order equals the largest number of vertex-disjoint paths from XG to B. We choose these paths to meet XG only at their initial vertices and B only at their final vertices. A root in XG ∩ B is represented by a zero-length path. The final vertices, called terminators , belong to A ∩ B.If the maximum number of paths is |A ∩ B| = |XG|, any rooted model in RA,B transfers to the original roots by adding the paths to its root branch sets. More generally, restricting the paths to an isolator transfers a model on the terminators to its separator. No path internal vertex then meets a branch set in the interior of B.A dart is a 4-rooted graph whose root graph is P3 ⊔ K1 and whose unique nonroot vertex is adjacent to all four roots. In particular, its rooted 4-density is zero. Let D∗ be the 4-rooted graph with independent roots and two adjacent nonroots, both adjacent to every root. Let W consist of the 5-rooted graphs with independent roots and two nonroots p, q 

satisfying the following condition. Either p, q are nonadjacent and each is adjacent to every root, or pq is an edge and at most two of the ten root–nonroot edges are missing, with any two missing edges independent. These are the K2,↓5 and vampire templates of [2]. Let 

W+ ⊆ W consist of the templates missing at most one of the eleven possible edges with a nonroot end. Figure 1 shows the 4-root target and representative weak and strong 5-root targets. A 5-rooted graph is quite heavy if its density is at least two, or if its density is one and it has no nonroot adjacent to all five roots. For a graph on five vertices, let S5,t be the class of graphs with at most t edges, and let 

S− 

> 5,4

exclude K3 ⊔ K2 from S5,4. Define 

r r ≤ 0 1 2 3 4 5 6 r ≥ 7

Tr S5,0 S5,1 S5,3 S− 

> 5,4

S5,6 S5,8 S5,9 S5,10 

.

A 5-rooted graph is Tr-universal if it has every member of Tr as a rooted minor for every prescribed bijection onto its roots. 4x1 x2 x3 x4

p qD∗

x1 x2 x3 x4 x5

p qW ∈ W \ W +

x1 x2 x3 x4 x5

p qW + ∈ W +

Figure 1. Rooted templates. Open vertices are roots and filled vertices are nonroots. Dashed segments are missing edges, not edges of the graph. The middle template has two independent root–nonroot defects, whereas the right template has only one. The figures show representatives, not all members of W and W+. Diagram code prepared with OpenAI Codex (GPT-6). 

Theorem 2.1. The following statements hold. 1. Every 4-light 5-rooted graph R is Tρ4(R)-universal [1, Theorem 4]. See also [2, Theorem 2.6]. 2. If a 4-light k-rooted graph has positive density and k ≤ 3, then it has a rooted Kk

minor. If k = 4 , it has a rooted dart minor [2, Theorem 2.7 and Lemma 3.1]. 3. Every quite heavy 4-light 5-rooted graph has a rooted minor in W [2, Theorem 2.9]. 4. If (R, X ) is internally 4-connected, |X| = 4 , and e(R) ≥ 4|V (R)| − 9, then it has an 

X-rooted D∗ minor [6, Lemma 12]. 

We also use the elementary 3-root classification [1, Lemma 11]. A 3-rooted graph with no rooted triangle has a skeleton with at most one nonroot, and every component outside the skeleton has at most two neighbours in it. Here a skeleton is a supergraph of the induced graph on its vertex set, containing all roots. Only the stated bound on its size and on attachment sets is used below. A ( ≤ 4)-fragment Y is reducible if ρ4(G, Y ) ≤ 0 and either RY has a rooted clique on its boundary, or |∂Y | = 4, |Y | ≥ 2, and RY has a dart minor. In the unrooted case we additionally require |V (G) \ Y | ≥ 3. Its reducent is (G − Y ) ∪ D, 

where D is the rooted boundary clique or the dart supplied by the model. In the latter case its nonroot vertex is new. The reducent is a minor and has fewer vertices. It preserves original roots in the rooted case. If a boundary edges are added, then in an unrooted graph 

ρ4(( G − Y ) ∪ D) − ρ4(G) = −ρ4(G, Y ) + a ≥ 0. (2.1) Rooted density is also nondecreasing. Edges added between original roots are simply omitted from that count. 5Lemma 2.2. 1. In a 4-light 5-rooted graph, reducing an inclusionwise maximal reducible fragment preserves 4-lightness. 2. Let G be an unrooted 4-bilight graph, and let b ∈ { 3, 4}. Choose Y inclusionwise maximal among reducible fragments satisfying |V (G) \ Y | ≥ b. Then its reducent is 

4-bilight. Proof. Assertion (1) is [2, Lemma 3.3]. We give the unrooted argument with the complement-size condition explicit. It also proves the case b = 3 of (2). Let H = ( G − Y ) ∪ D be the reducent, put Q = ∂GY , and let y denote the new nonroot vertex when D is a dart. Suppose that H is not 4-bilight. By [2, Lemma 3.2], choose a dense ( ≤ 4)-bifragment ( S, T ) of H such that either V (D) avoids both interiors, or 

V (D) ∩ S̸ = ∅, V (D) ∩ T = ∅, |V (D) \ (S ∪ ∂H S)| ≤ 1,

and the rooted graph RHS is 4-light. In the first case neither the added boundary edges nor the new dart vertex contributes to the densities of S, T . Also neither interior has a neighbour in Y , since both avoid Q. Thus the same dense small bifragment exists in G, a contradiction. In the second case, Theorem 2.1(2) gives a boundary clique model in RHS when |∂H S| ≤ 3, and a dart model when |∂H S| = 4. We use the normalization from the proof of [2, Lemma 3.3], as follows. If D is a dart and its unique vertex outside S ∪ ∂H S is z, then 

y ∈ ∂H S. In this case, add y to S and replace the boundary root y by z, using the edge 

yz in its root branch set. If V (D) ⊆ S ∪ ∂H S and y ∈ ∂H S, add y to S and cancel that boundary root. A clique restricts to the remaining roots. A dart, with one root cancelled, has a triangle model on its other three roots. In all other cases leave S unchanged. Call the resulting fragment S1. This construction keeps S1 nonadjacent to T , gives 

|∂H S1| ≤ 4, and ensures 

S ⊆ S1, V (D) ⊆ S1 ∪ ∂H S1, V (D) \ Q ⊆ S1.

Its associated rooted graph has a boundary clique or dart model. For completeness, the assertion about cancelling a dart root follows directly from its root path abc and isolated root d. If the cancelled root is a or c, absorb the nonroot into d. If it is d, absorb the nonroot into a. If it is b, absorb b into c and the nonroot into d. In each case the three remaining roots form a triangle model. Undo the reduction and set 

Y ′ = Y ∪ (S1 \ (V (D) \ Q)).

Then ∂GY ′ = ∂H S1. Composing with the model defining the reduction lifts the boundary clique or dart to RGY ′ . Since S is positive and has boundary at most four, |S| ≥ 2. At most the single new dart vertex is removed from S1, so |Y ′| ≥ | Y | + 1 ≥ 2. The fragment T and its density are unchanged in G, and Y ′ is nonadjacent to T . Hence 4-bilightness of G forces ρ4(G, Y ′) ≤ 0. Furthermore, 

T ∪ ∂GT ⊆ V (G) \ Y ′.

A positive fragment T with k = |∂GT | ≤ 4 has |T | + k ≥ 6. Indeed, if t = |T | and t + k ≤ 5, then 

ρ4(G, T ) ≤

> (

t

2

> )

+ tk − 4t ≤ 0.

Consequently |V (G) \ Y ′| ≥ 6 ≥ b. Thus Y ′ is an admissible reducible fragment strictly containing Y , contrary to its maximality. This proves (2). 6We will need the following consequence of the linkage argument in [2]. It applies even when the whole graph already has a weak model. 

Lemma 2.3. Let R be a 4-light rooted graph with k ≤ 5 roots, with ρ4(R) ≤ 0 if k ≤ 4.Suppose that a root separation (A, B ) of order five has a right-hand side containing a member of W. If there is no linkage of size five from the original roots to B, then R has a reducible fragment containing B \ A.Proof. This is the non-full-linkage branch of [2, Corollary 4.2]. We recall the argument to specify the conclusion used here. Take an isolator ( C, D ) of order q ≤ 4. If q = k,choose ( C, D ) = ( XR, V (R)). Otherwise q < k . The rooted graph RC,D has nonpositive density, by the hypothesis when q = k and by 4-lightness otherwise. Restrict the weak template to the q terminators of a linkage within D. It supplies a rooted Kq, or a dart when q = 4 [2, Observation 4.1]. Transfer that model to C ∩ D. The fragment D \ C has at least two vertices, because it contains the two nonroot branch sets of the template in 

B \ A. It is therefore reducible. If its actual boundary is smaller than the separator, a clique model restricts to that boundary. A dart model forces every one of its four roots to have a neighbour in the interior. 

Lemma 2.4. Let R be a 4-light graph with at most four roots and ρ4(R) ≤ 0. Changing some of its roots into nonroots preserves both properties. Proof. This is [2, Observation 5.2]. Let X′ be the remaining roots, put M = XR \ X′,and let H denote the graph with root set X′. Consider a fragment Y of H with either 

Y = V (H) \ X′ or |∂H Y | < |X′|. Set Y ′ = Y \ M . In the first case, Y ′ = V (R) \ XR. In the second, 

|∂RY ′| ≤ | ∂H Y | + |M | < |XR|.

Hence ρ4(R, Y ′) ≤ 0, with the density of the empty set taken to be zero. Put a = |M ∩ Y |

and t = |∂H Y |. In either case |M | + t ≤ | XR| ≤ 4. The edges counted for Y but not for Y ′

number at most (a 

> 2
> )

+ at ≤ 4a. Therefore 

ρ4(H, Y ) ≤ ρ4(R, Y ′) + 

> (

a

2

> )

+ at − 4a ≤ 0.

This proves both assertions. We also use the density bound of [2, Theorem 2.8]: 

|V (G)| ≥ 3, G is 4-bilight , e(G) ≥ 4|V (G)| − 7 =⇒ K=7 ≼ G. (2.2) 73 Rooted minors 

## 3.1 Four roots 

Lemma 3.1. Every 4-light 4-rooted graph of positive 4-density has a rooted D∗ minor. Proof. Choose a counterexample R with the fewest vertices and delete edges between its four roots. In this proof, call a nonempty fragment clique-reducible if its boundary has size at most three and its associated rooted graph has a boundary clique minor. Its density is automatically nonpositive. There is no clique-reducible fragment. Otherwise choose an inclusionwise maximal one, Y , and replace it by the clique on Q = ∂RY , obtaining a smaller rooted minor R′ of at least the same density. If R′ is not 4-light, choose an inclusionwise minimal positive (≤ 3)-fragment Z. Its associated rooted graph is 4-light and has a boundary clique minor by Theorem 2.1(2). If Z ∩ Q = ∅, no new clique edge contributes to its density, and Z

is the same forbidden positive small fragment in R. Otherwise the clique property gives 

Q ⊆ Z ∪ ∂R′ Z. Undoing the reduction gives Y ∪ Z with boundary contained in ∂R′ Z.Compose the two clique models, and restrict to the actual boundary if it is smaller. Its density is nonpositive by 4-lightness, so it is a clique-reducible fragment strictly containing 

Y . This contradiction shows that R′ is 4-light. Minimality now gives a D∗ model in R′,and hence in R, again a contradiction. There is also no proper root 4-separation with positive right-hand side. That side is a smaller 4-light 4-rooted graph, so it has a D∗ model. A linkage of size four transfers the model. Otherwise an isolator has order q ≤ 3. The restriction of D∗ to any q of its roots has a rooted Kq minor. For q = 3, absorb its two nonroots into two distinct roots. For 

q = 2, absorb one nonroot into a root. The case q ≤ 1 is immediate. Transfer this clique to the isolator separator. Its interior is nonempty, has nonpositive density by 4-lightness, and is clique-reducible, a contradiction. Every nonroot has degree at least five. Deleting a nonroot v of degree at most four does not decrease density. If the resulting graph is 4-light, minimality is contradicted. Otherwise let Z be a positive small fragment after deletion. It must have been adjacent to v. Restoring v to its boundary produces a positive fragment of boundary exactly four. The left side of this root separation contains the four original roots and v. It is a proper root 4-separation, which was just excluded. We claim that R is internally 4-connected. A fragment with boundary zero or one is clique-reducible. If a fragment has boundary two, each component of its interior meets both boundary vertices, since otherwise there would be a fragment of smaller boundary. It therefore supplies a path between the two boundary vertices and is clique-reducible. For a 3-boundary fragment, a rooted triangle also makes it clique-reducible. If there is no rooted triangle, apply the 3-root skeleton classification stated after Theorem 2.1. Every component outside the skeleton is a fragment of the entire graph with boundary at most two, and so none exists. The original fragment then has at most one vertex. Such a vertex has degree at most three, contrary to the preceding paragraph. Complete the four roots to a clique, obtaining R∗. This preserves internal 4-connectivity, and 

e(R∗) = 4( |V (R)| − 4) + ρ4(R) + 6 ≥ 4|V (R)| − 9.

Theorem 2.1(4) gives a rooted D∗ model. Each added edge joins distinct original roots, which belong to distinct root branch sets. Thus no added edge is used within a branch set or for an edge of the independent-root target. The model already exists in R, a contradiction. 8Lemma 3.2. Suppose a 4-rooted graph R has a rooted D∗ minor. Its two nonroot branch sets can be chosen as A, B so that 

∂R(A ∪ B) = {d1, d 2, d 3, d 4},

every di has a neighbour in both A and B, and the four roots have pairwise disjoint paths to the corresponding di avoiding A ∪ B.Proof. Maximize |A ∪ B|, and then choose inclusionwise minimal trees for the root branch sets. Consider a leaf other than the prescribed root of one of these trees. If it has no neighbour in either A or B, delete it. If it meets exactly one of A, B , move it to that branch set. The tree’s parent edge preserves adjacency to that set, and adjacency to the other set is unaffected. Both alternatives contradict the choice of model. Thus every such leaf meets both A and B.If another vertex in the same root tree meets A, move the leaf to B. The other vertex preserves adjacency to A, and the parent edge preserves adjacency to B. This increases 

|A ∪ B|, a contradiction. The same reasoning applies with A, B interchanged. Hence a nontrivial root tree has a unique nonroot leaf, its only vertex meeting either nonroot branch set. The tree is a path to this leaf. A singleton root tree already has the required form. Call these four endpoints di. Any unused vertex adjacent to A ∪ B could be absorbed into a nonroot branch set. Maximality rules this out, proving the assertion about the entire external neighbourhood. 

## 3.2 Five roots 

Theorem 3.3. Every 4-light 5-rooted graph R with ρ4(R) ≥ 2 has a rooted minor in W+.

The proof contracts a suitable edge in a minimal counterexample and applies Lemmas 3.1 and 3.2 to a positive fragment of the contracted graph. We first need a local lemma about rooted stars. 

Lemma 3.4. Let L be a graph, let X ⊆ V (L), and suppose that 

5 ≤ | V (L)|, |V (L)| + |X| ≤ 8.

Suppose that vertices outside X have degree at least four and vertices in X have degree at least 5 − | X|. For every 5-set Z ⊇ X, there is a Z-rooted K1,4 minor whose centre is a vertex of Z \ X.Proof. Put U = V (L) \ Z. Then |U | ≤ 3 and |X| ≤ 3 − | U |. If U = ∅, any vertex in Z \ X

is already a centre. Suppose that L[U ] has an isolated vertex u. It is adjacent to at least four vertices of Z. If it meets all five, absorb it into any z ∈ Z \ X. Otherwise let y be its unique nonneighbour in Z. If y / ∈ X, then y has at least 5 − | U | neighbours in Z, which is more than |X|. If y ∈ X, it has at least 6 − | X| − | U | neighbours in Z, which is more than 

|X| − 1 since |X| + |U | ≤ 3. Thus some neighbour z of y lies in Z \ X. As y is the only root missed by u, the edge uz is present. The connected centre branch set {u, z } meets the other four roots. It remains that U has two or three vertices and L[U ] has no isolated vertex. Hence it is connected. If a vertex of Z \ X has no neighbour in U , its degree makes it adjacent to all other roots and it is a centre. Otherwise every vertex of Z \ X meets U . When |U | = 3, we have X = ∅, and absorbing U into any root gives the star. When |U | = 2, we have 

|X| ≤ 1. If its possible vertex x also meets U , the same construction works. If it does not, its degree is at least four, so it meets every other root. Absorb U into any root outside X.Unused vertices may be deleted. 9Suppose Theorem 3.3 is false, and choose a counterexample G minimizing its number of vertices, and then its number of edges. Let S be its five roots, and write dS (v) = |NG(v)∩S|.Root edges can be deleted, so G[S] is independent. We work with this fixed counterexample until the proof of Theorem 3.3 is complete. 

Lemma 3.5. G has no reducible (≤ 4) -fragment and no proper root 5-separation whose right-hand side has density at least two. Proof. An inclusionwise maximal reducible fragment could be reduced using Lemma 2.2(1), producing a smaller 4-light counterexample of at least the same density. For the second assertion, a smaller right-hand side inherits 4-lightness and has a W+ model by minimality. A full linkage transfers this strong model. If no full linkage exists, Lemma 2.3 gives the forbidden reducible fragment. The full-linkage case must transfer the W+ model. The existence of a weak W model alone is consistent with the choice of G.

Lemma 3.6. The graph G − S is connected, every nonroot has at most three neighbours in S, and every root has degree at least two. Moreover 

ρ4(G) = 2 , |V (G) \ S| ≥ 5.

Proof. Every positive-density component of G−S meets all five roots. Two such components give a rooted K2,↓5 after contraction. There is therefore exactly one positive-density component, and its density is at least two. Lemma 3.5 implies that it contains all nonroots. In particular, G − S is connected and every root has a neighbour. Suppose a nonroot z has d ≥ 4 root neighbours. Let Q1, . . . , Q t be the components of 

G − (S ∪ { z}). Each meets z, and 

> ∑
> i

ρ4(G, Q i) = ρ4(G) + 4 − d. (3.1) If d = 5, some component has positive density and thus has at least four root neighbours. Contracting it gives a W+ model with z. Suppose d = 4, and let x be the missing root neighbour of z. A component meeting all five roots gives the same contradiction. Every positive-density Qi consequently has boundary z and four roots. Its density cannot be at least two, by Lemma 3.5, since its root separation is proper. It has density one. Equation (3.1) now implies that there are at least two positive components. If a positive component meets x, merge it with z and retain another positive component. Otherwise merge z with any component meeting x, which exists, and retain a positive component. The merged branch set meets all five roots, the retained one meets at least four, and they are adjacent. This is a W+ model. If a root x has unique neighbour v, replacing x by v as a root on the remaining graph gives a proper root 5-separation whose right-hand side has density ρ4(G) + 4 − dS (v) ≥ 3. This contradicts Lemma 3.5. If ρ4(G) ≥ 3, delete any edge. Minimality forces the resulting graph to fail 4-lightness. Let Y be a positive small fragment there. The deleted edge must join Y to a vertex u

outside its closed neighbourhood. Otherwise Y already violates lightness in G. Restoring that edge gives a positive fragment of boundary exactly five and density at least two. Its root separation cannot be proper by Lemma 3.5. Its left side is therefore S, and u ∈ S has only the restored edge as a neighbour, contrary to its degree. Thus ρ4(G) = 2. Write k = |V (G) \ S|. If k ≤ 3, the bound on the number of root neighbours of each nonroot gives ρ4(G) ≤ (k 

> 2
> )

− k ≤ 0. If k = 4, equality with density two forces the nonroots to induce K4, each with three root neighbours. Regard each missing root pair as an edge of 10 a multigraph on S. It has four edges and maximum degree at most two, because every root has degree at least two in G. If an edge is repeated, its endpoints are saturated, and pair its two copies with the other two edges. Both pairs consist of disjoint edges. Otherwise this multigraph is P5, C4 ⊔ K1, or C3 ⊔ K2. In each case its edges can be partitioned into two pairs, one disjoint and the other sharing at most one endpoint. Pair the four nonroots accordingly and contract each pair. One resulting vertex meets all roots and the other at least four. They are adjacent. Again this is a W+ model. Hence k ≥ 5. For an edge uv with both ends nonroots, put τ (uv ) = |N (u) ∩ N (v)|. For an edge xv 

with x ∈ S, put 

τ (xv ) = |N (x) ∩ N (v)| + dS (v) − 1.

As S is independent, this is the triangle count after completing S to a clique. Counting the edges that merge or become root edges gives 

ρ4(G/e ) = ρ4(G) + 3 − τ (e) = 5 − τ (e). (3.2) 

Lemma 3.7. Some edge e of G satisfies τ (e) ≤ 3.Proof. Suppose that τ (e) ≥ 4 for every edge e.Since ∑

> v / ∈S

(d(v) + dS (v) − 8) = 2 ρ4(G) = 4 and there are at least five nonroots, some nonroot v satisfies d(v) + dS (v) ≤ 8. Put 

L = G[N (v)] and X = S ∩ N (v). If t ∈ N (v) \ S, then 

dL(t) = |N (v) ∩ N (t)| = τ (vt ) ≥ 4.

For x ∈ X, the definition of τ (vx ) instead gives dL(x) ≥ 5 − | X|. The graph G − S is connected and has at least five vertices, so v has a nonroot neighbour. The preceding degree bound for that neighbour gives |N (v)| ≥ 5. Thus |V (L)| + |X| ≤ 8, as required by Lemma 3.4. Take an isolator ( C, D ) of the root separation ( V (G) \ { v}, N [v]), and write q =

|C ∩ D| ≤ 5. A maximum linkage from S to N [v] has q paths and terminators T ⊆ N (v). Use zero-length paths at the roots in X, so X ⊆ T . Extend T to a 5-set Z ⊆ N (v). The proof of Lemma 3.4 provides a connected centre branch set Hz containing z ∈ Z \ X, with the other four roots of Z as singleton branch sets. The set Hz meets Z only at z and is adjacent to every other vertex of Z. Since S ∩ N (v) = X ⊆ Z and z / ∈ X, it contains no original root. Also v is adjacent to all these sets. Linkage interiors avoid N [v], so they avoid the local model. 

Case 1. q = 5 . Here T = Z. Since z / ∈ S, its linkage path has positive length. Remove its last vertex z from its root branch set and retain Hz as a nonroot branch set. Retain v as the other nonroot and use the other four linkage paths as root branch sets. The last edge of the path to z joins its remaining, nonempty root branch set to Hz . Both nonroot branch sets meet the other four root branch sets, and they are adjacent to each other. Only the edge from v to the root branch set formerly ending at z may be absent. These seven sets are disjoint and preserve all original roots, so they give a W+ model, a contradiction. 

Case 2. 1 ≤ q ≤ 4. Delete unused roots of Z, retaining Hz if its centre is not in T . For 

q = 2 or 3, construct a clique as follows. If z ∈ T , absorb v into any other terminator. That enlarged set and Hz meet every remaining terminator. If z / ∈ T , absorb Hz into one terminator and v into a different one. For q = 3 the third terminator meets both enlarged 11 sets. For q = 2 the two enlarged sets are adjacent. Each absorption uses an edge, so the branch sets remain connected, disjoint, and rooted at distinct vertices of T . For q = 1, retain the single terminator. Connectivity of G excludes q = 0. For q = 4, if z ∈ T , keep Hz as its root branch set. If z /∈ T , absorb Hz into a terminator t0 ∈ T . In either case the four root branch sets contain a 3-edge star and v is a nonroot adjacent to all four. Retain exactly two edges of that star and delete any other root edges. The root graph is P3 ⊔ K1, so the resulting model is a dart. The q disjoint linkage paths meet the q vertices of C ∩ D one each. Their tails in D

transfer the local clique or dart to these separator roots. A separator vertex in N [v] must itself be a terminator. Hence the transfer uses no other separator root within a branch set. Put F = D \ C. It avoids S, has boundary at most q ≤ 4, and contains at least 

|N [v]| − q ≥ 2 vertices. Thus ρ4(G, F ) ≤ 0 by 4-lightness. A clique model restricts to the actual boundary if necessary, by discarding the other root branch sets. In the dart case each of the four separator roots has a neighbour in F , since its branch set is connected to the nonroot branch set in F without using another root. Thus the actual boundary has size four. In either case F is reducible, contrary to Lemma 3.5. 

Completion of the proof of Theorem 3.3. Choose uv as in Lemma 3.7, and let J = G/uv ,with contraction vertex w. An edge of G has at most one root end, since S is independent. If uv has a root end, w retains its identity. Otherwise w is a nonroot. Thus J still has five distinct roots. Equation (3.2) gives ρ4(J) ≥ 2. A rooted W+ model in J would lift to G by expanding w within its branch set. Since 

J is smaller than the chosen counterexample, it must therefore fail 4-lightness. Choose an inclusionwise minimal positive ( ≤ 4)-fragment Y in J, and put Q = ∂J Y . The associated rooted graph RJY = ( J[Y ∪ Q], Q ) is 4-light. Indeed, a positive fragment inside it with boundary smaller than |Q| would also be a positive ( ≤ 4)-fragment properly contained in 

Y in J.If w / ∈ Y ∪ Q, the boundary and density of Y are unchanged in G, contradicting 4-lightness. If w ∈ Y , it is a nonroot and both u, v are nonroots. Theorem 2.1(2) supplies a boundary clique or dart model in RJY . Expanding w lifts this model to 

Y ′ = ( Y \ { w}) ∪ { u, v }.

Its boundary is Q, its density is nonpositive by 4-lightness of G, and |Y ′| = |Y | + 1 ≥ 2. Thus it is reducible, contrary to Lemma 3.5. We conclude that w ∈ Q.The set Y now survives unchanged when w is expanded. Its boundary has at most five vertices, and its density cannot decrease. If its boundary had at most four vertices, it would violate 4-lightness of G. Hence 

|Q| = 4 , ∂GY = ( Q \ { w}) ∪ { u, v }.

In particular, both u and v have neighbours in Y . The separation 

> (

V (G) \ Y, Y ∪ ∂GY )

is a proper root 5-separation because its left side contains every original root and also at least one nonroot among u, v . Let 

c = |NG(u) ∩ NG(v) ∩ Y |.

Exactly c pairs of edges from u, v to Y merge under contraction, so 

ρ4(G, Y ) = ρ4(J, Y ) + c. 

12 Its right-hand density cannot be at least two by Lemma 3.5. Since ρ4(J, Y ) is a positive integer, this gives 

ρ4(G, Y ) = ρ4(J, Y ) = 1 , c = 0 . (3.3) At this point RJY has exactly four roots, namely Q. It is 4-light by the minimal choice of Y , and its rooted density is ρ4(J, Y ) = 1 > 0. Thus Lemma 3.1 applies, and Lemma 3.2 gives its boundary normal form. Obtain adjacent nonempty connected sets A, B ⊆ Y , and put U = A ∪ B. Their union has exactly four external neighbours in RJY , each adjacent to both A and B. This is also the entire boundary in J. A vertex outside Y ∪ Q cannot have a neighbour in U ⊆ Y .If w is not one of those four neighbours, expanding it changes none of these adjacencies. If w is one of them but only one of u, v meets U , that endpoint inherits all its adjacencies to A, B . In either case U has boundary four in G and a rooted D∗ model with singleton boundary roots. To obtain a dart from such a model, absorb one nonroot branch set into one boundary root, retain the other as the dart nonroot, and keep two edges of the resulting 3-edge root star. Because |U | ≥ 2 and ρ4(G, U ) ≤ 0, this makes U reducible, again impossible. It follows that 

∂GU = {u, v, d 1, d 2, d 3}.

Each of A, B meets all three di and at least one of u, v , while both u and v meet U . If either branch set meets both endpoints, A, B and the five singleton boundary roots already form a W+ model. A linkage of size five transfers this model to S. If there is no such linkage, Lemma 2.3, applied to the root separation with right side G[U ∪ ∂GU ], produces a forbidden reducible fragment. Thus this case is excluded. In the remaining case, after interchanging A, B and u, v if necessary, A meets u but not 

v, and B meets v but not u. Choose the labels so that v is an original nonroot. At least one endpoint of uv is a nonroot. These branch sets form a weak W model on the boundary. Lemma 2.3 and irreducibility now force a linkage of size five from S to U ∪ ∂GU .Truncate each linkage path at its first vertex in that closed neighbourhood. Each path first enters the closed neighbourhood through its boundary, because a vertex outside it has no neighbour in U . There are five disjoint paths and exactly five boundary vertices, so their terminators are precisely those five vertices. Write Pt for the path ending at t ∈ ∂GU , and 

st for its original root. The paths avoid U , meet the boundary only at their terminators, and meet S only at their initial vertices. Since v / ∈ S, Pv has positive length. Define the five root branch sets by 

Rv = V (Pv) \ { v}, Rt = V (Pt) (t ∈ { u, d 1, d 2, d 3}),

and the two nonroot branch sets by A and B′ = B ∪ { v}. Because Pv has positive length, deleting its last vertex leaves a nonempty connected path Rv containing sv. No original root is moved into a nonroot branch set. The set B′ is connected because v has a neighbour in B. All seven sets are pairwise disjoint. Indeed, the paths are disjoint, avoid A ∪ B, and 

v has been removed from its root branch set before being added to B. Each Rt contains exactly its prescribed original root, and A, B ′ contain none. The construction is illustrated in Figure 2. 13 usu

vsv

d1

sd1

d2

sd2

d3

sd3

A B

(a) The linkage and local sets 

Ru Rv Rd1 Rd2 Rd3

A B′

(b) The seven branch sets 

Figure 2. The split case in Theorem 3.3. In (a), dashed vertical segments represent disjoint linkage paths. The marked last edge of Pv joins Rv to the vertex moved into B′ = B ∪ { v}.The paths may have different lengths, and paths other than Pv may have length zero. In (b), the dashed segment is the sole adjacency that may be absent. Every root set still contains its original root st. Diagram code prepared with OpenAI Codex (GPT-6). The following table gives witnesses for the required adjacencies. Pair of branch sets Witness 

A, B ′ an edge between A and BA, R u an edge from A to uA, R di an edge from A to di, for i = 1 , 2, 3

B′, R u the edge vu B′, R v the last edge of Pv

B′, R di an edge from B to di, for i = 1 , 2, 3The table accounts for the edge between the two nonroot branch sets, four root adjacencies of A, and all five root adjacencies of B′. These are ten distinct required adjacencies out of eleven. Only the edge between A and Rv may be absent. After deleting unwanted root edges, the seven sets give a rooted W+ model in G. This contradicts the choice of G and proves the theorem. 

Remark 3.8 . The density threshold two is best possible for Theorem 3.3. A vampire on seven vertices with exactly two independent missing root–nonroot edges is internally 5-connected as a rooted graph and has density one. It cannot have a W+ minor. Indeed, such a minor has the same seven vertices and would require at least ten nonroot-incident edges, whereas this vampire has nine. 

## 3.3 Separations of order five 

Lemma 3.9. Let (A, B ) be a 5-separation of a graph, and let m be the number of missing edges on S = A ∩ B. Suppose that both rooted sides are 4-light, have densities r1, r 2 ≥ 2,and satisfy r1 + r2 ≥ m + 1 . Then the graph contains K=7 as a minor. Proof. Use Theorem 3.3 on the side of smaller density. The other side has density at least 

s = max {2, ⌈(m + 1) /2⌉} . Write F for the graph of missing edges on the five separator roots. The classes Tr are nested, so Theorem 2.1(1) supplies any target in Ts. The following table records the relevant targets. 14 m s Ts target to be supplied 0–3 2 S5,3 F

4 3 S− 

> 5,4

F − f , or F if it is a star 5 3 S− 

> 5,4

F − f̸ ∼= K3 ⊔ K2

6 4 S5,6 F

7 4 S5,6 F − f

8 5 S5,8 F

9 5 S5,8 F − f

10 6 S5,9 F − f

Thus at most one root edge f need remain absent. If the W+ model has no defect, any such 

f is harmless. If its sole defect is the edge between its two nonroots, then f is automatically independent of that defect. It remains to consider a missing root–nonroot edge px , where 

x is a separator root. We choose f to avoid x.For m = 4, if an edge f of F avoids x, then F − f has three edges and belongs to S−

> 5,4

.If no such edge exists, all four edges are incident with x, so F = K1,4 and the whole of F

is an allowed target. For m = 5, some edge avoids x because dF (x) ≤ 4. If exactly one does, deleting it leaves a 4-edge star. If at least two do, at most one choice f can leave 

K3 ⊔ K2. In that event F is this disjoint union plus an edge joining its components, and only that joining edge can be deleted to recover the disjoint union. Choose another edge avoiding x. In all cases the chosen target lies in S−

> 5,4

.For m = 0 , 1, 2, 3, 6, 8, the target class contains F itself, so no root edge is left missing. For m = 7, deleting any edge leaves a 6-edge target in S5,6. For m = 9 and 10, deletion leaves respectively an 8-edge target in S5,8 and a 9-edge target in S5,9. In these last three rows, m > 4 ≥ dF (x) guarantees an edge f avoiding x.Finally compose the two rooted models, identifying corresponding root branch sets through their common separator vertex. Each union is connected. Different root sets remain disjoint, and the two nonroot branch sets lie only on the first side. Retain all existing root edges. The seven resulting branch sets miss at most two independent edges, and hence give a K=7 model. 

# 4 Irreducible graphs 

Fix b ∈ { 3, 4}. Call an unrooted graph b-irreducible if it has no reducible ( ≤ 4)-fragment Y

with |V (G) \ Y | ≥ b. Throughout this section, G is a 4-bilight, K=7 -minor-free, b-irreducible graph. We adapt the separation and contraction arguments of [2], checking that each reduction leaves at least b original vertices. The corresponding results are cited in the individual proofs. 

Lemma 4.1. Let (C, D ) be a separation of order at most four such that RC,D is 4-light and has nonpositive density. Suppose that 

|(C \ D) ∪ ∂G(C \ D)| ≥ b. 

There is no 5-separation (A, B ) with B ⊆ D for which RA,B is both 4-light and quite heavy. Proof. This is the enclosing argument of [2, Corollary 5.3], with b-irreducibility in place of minimal-counterexample irreducibility. Its explicit closed-interior hypothesis ensures that the forbidden reduction leaves at least b vertices. Remove from C the separator vertices with no neighbour in C \ D, and call the resulting set C′. Thus C′ = ( C \ D) ∪ ∂G(C \ D). In H = RC′,D , Lemma 2.4 gives nonpositive 15 density and 4-lightness. Each root of H has a neighbour in C \ D. As B ⊆ D, this implies that every root of H belongs to A. Consequently ( A ∩ D, B ) is a root 5-separation of 

H. Theorem 2.1(3) gives a weak template in its right-hand side. There are at most four original roots in H, so Lemma 2.3 supplies a reducible fragment Y of H. Its boundary and density are unchanged in G, since it avoids the roots of H and has no neighbour in C \ D.Also |V (G) \ Y | ≥ | C′| ≥ b. This contradicts b-irreducibility. 

Lemma 4.2. 1. If (A, B ) is a 5-separation and RA,B is quite heavy, then RB,A is 

4-light. 2. If (C, D ) has order at most four and Y is a quite heavy 5-fragment with Y ∪ ∂Y ⊆ C,then |C \ D| ≥ 3, ρ4(RC,D ) ≤ 0, and RC,D is 4-light. Proof. The conclusions are those of [2, Lemma 5.4 and Corollary 5.5]. Their use of irreducibility is replaced here by Lemma 4.1. The complement in that application has at least six vertices, so either value of b is allowed. For (1), suppose the opposite side contains a positive ( ≤ 4)-fragment L ⊆ A \ B. Then 

RA,B is 4-light, since a violating fragment in B \ A would form a dense small bifragment with L. The complete opposite side of L, rooted on ∂L , has nonpositive density and is 4-light for the same reason. Its other side has closed interior L ∪ ∂L of size at least six. Since B is contained in this opposite side, Lemma 4.1 is contradicted. For (2), quite heaviness implies |Y | ≥ 2, and hence |C \ D| ≥ | Y | + 5 − 4 ≥ 3. Let 

M ⊆ C ∩ D consist of vertices with no neighbour in D \ C, and put D′ = D \ M . This does not change the density of the right-hand side. Every vertex of C ∩ D′ has a neighbour outside C, and hence cannot belong to Y . Thus Y ⊆ C \ D′. The complete opposite side of Y is 4-light by (1), and ( C \ Y, D ′) is a root separation of it of order at most four. Its density is therefore nonpositive, proving ρ4(RC,D ) ≤ 0. Finally, if ( I, J ) is a small root separation of RC,D , then ( I ∪ C, J ) is a separation of G

of order at most four with Y ∪ ∂Y ⊆ I ∪ C. The density conclusion just proved applies to it. This shows that RC,D is 4-light. 

Lemma 4.3. Suppose that ρ4(G) = −d, where d ∈ { 8, 9}, and additionally that G contains no K5 subgraph when d = 9 . Every 5-separation has exactly one quite heavy side. If such a side is 4-light, it has a rooted K5 minor. Proof. The corresponding calculation in [2, Lemmas 5.7 and 5.8] uses global density −7and a side-density sum at least m + 3. Here the sum is m + 10 − d, and Lemma 3.9 handles the smaller threshold when both side densities are at least two. The extra K5 exclusion for 

d = 9 will ensure that the sum is still at least two. Let the root set of a 5-separation have m missing edges, and let its side densities be 

r1, r 2. Counting edges gives 

r1 + r2 = m + 10 − d. (4.1) When d = 9, the absence of K5 gives m ≥ 1. If neither side is quite heavy, then r1, r 2 ≤ 1, and equality in the resulting bound forces r1 = r2 = 1 and m = d − 8. Each side then contains a vertex adjacent to all five roots. Together with the roots these vertices give K=7 .The only possible missing edges are the edge between them and, when d = 9, one root edge. If both sides are quite heavy, Lemma 4.2(1) makes both 4-light. If one has density one, use Theorem 2.1(3) there. The other side has density m + 9 − d ≥ m and supplies all m

missing root edges by Theorem 2.1(1). The weak model with the root clique gives K=7 . If both densities are at least two, apply Lemma 3.9. Thus exactly one side is quite heavy. 16 The other density is at most one, so the heavy side has density at least m + 9 − d ≥ m.For 0 ≤ m ≤ 10, the table defining Tr shows that Tr contains every m-edge graph whenever 

r ≥ m. If the heavy side is 4-light, it therefore supplies all missing root edges. Retaining existing root edges gives its rooted K5 model. 

Lemma 4.4. Under the hypotheses of Lemma 4.3, there is no dense (≤ 5) -bifragment for which every 5-boundary member is quite heavy. Proof. We use the noncrossing argument of [2, Lemma 5.9 and Corollary 5.10]. The rooted clique now comes from Lemma 4.3 at density −d, and the only additional size check is for an enclosing reduction when b = 4. 

Two quite heavy 5-fragments. Suppose that Y, Z are nonadjacent 5-fragments and both are quite heavy. By Lemma 4.2(1), each associated rooted graph is 4-light. It is a rooted side inside the complete opposite side of the other fragment. We claim that their closed neighbourhoods are joined by five vertex-disjoint paths. Otherwise Menger’s theorem gives a separation ( C, D ) of order at most four with 

Y ∪ ∂GY ⊆ C, Z ∪ ∂GZ ⊆ D. 

Lemma 4.2(2) gives |C \ D| ≥ 3, nonpositive density and 4-lightness on the right. For b = 3, these facts suffice to apply Lemma 4.1, contradicting the heavy side on Z.For b = 4 we also need 

|(C \ D) ∪ ∂G(C \ D)| ≥ 4. (4.2) Put E = C \ D and Q = C ∩ D. If (4.2) fails, |E| ≥ 3 forces |E| = 3 and ∂GE = ∅. In particular there is no edge from E to Q. Quite heaviness implies |Y | ≥ 2, so |C| ≥ | Y |+5 ≥ 7. On the other hand, |C| = |E| + |Q| ≤ 3 + 4 = 7. Equality gives 

|C| = 7 , |Q| = 4 , |Y | = 2 , C = Y ∪ ∂GY. 

If both vertices of Y lie in E, their boundary has size at most one. If both lie in Q, it has size at most two. Here no vertex outside C can be a neighbour of Y , and there are no edges between the two blocks. Both possibilities contradict |∂GY | = 5. If instead one vertex lies in each block, they are nonadjacent and have at most two and three boundary neighbours, respectively. Consequently 

ρ4(G, Y ) ≤ 0 + (2 + 3) − 4 · 2 = −3,

contrary to positive density. This proves (4.2), and Lemma 4.1 again excludes the separator. Normalize the five paths to run from ∂GY to ∂GZ, with interiors avoiding both closed neighbourhoods. A shared boundary vertex uses a zero-length path. Each boundary has five vertices, so all its vertices are used exactly once. Apply Theorem 2.1(3) to RY and Lemma 4.3 to RZ . Transfer the rooted clique along the paths and combine it with the weak template on Y . The interiors of these models are disjoint, so they give a K=7 model, a contradiction. Let ( Y, Z ) be a bifragment as in the statement. Both boundaries cannot have size at most four, by 4-bilightness. If both have size five, the preceding case applies. Otherwise a positive fragment of boundary at most four lies in the complete opposite side of a quite heavy 5-fragment. This contradicts Lemma 4.2(1). 17 Lemma 4.5. Under the hypotheses of Lemma 4.3, contracting any edge of G preserves 

4-bilightness. Proof. We isolate the contraction step from the proof of [2, Lemma 6.2]. That proof uses it for a low-triangle edge in a minimal counterexample at density −7. Here we prove preservation for every edge, using b-irreducibility and Lemma 4.4 at density −d. No restriction on the number of triangles is needed for this step. Let J = G/uv , with contraction vertex w. Suppose that J has a dense ( ≤ 4)-bifragment (Y, Z ), chosen to minimize |Y | + |Z|. Each associated rooted graph is 4-light. Indeed, a positive fragment with boundary smaller than that rooted graph’s root set would be a smaller positive ( ≤ 4)-fragment in J, still nonadjacent to the other member. 

Case 1. The contraction vertex lies in an interior. Suppose w ∈ Y . The case w ∈ Z is symmetric. Theorem 2.1(2) supplies a boundary clique or dart in RJY . Expand w and put 

Y ′ = ( Y \ { w}) ∪ { u, v }.

Its boundary in G is exactly ∂J Y , so has size at most four. Expanding w within whichever branch set contains it lifts the rooted model to RGY ′ . The edge uv preserves connectivity. Also |Y ′| ≥ 2. The set Z has no neighbour w in J, since the two interiors are nonadjacent. Thus it has no neighbour u or v in G, and its boundary and positive density are unchanged. The sets Y ′, Z are nonadjacent, so 4-bilightness of G forces ρ4(G, Y ′) ≤ 0. Moreover, 

Z ∪ ∂GZ ⊆ V (G) \ Y ′, |Z ∪ ∂GZ| ≥ 6,

where the last inequality is the positive-fragment estimate from Lemma 2.2. Hence Y ′ is reducible and leaves at least 6 ≥ b vertices. This contradicts b-irreducibility. 

Case 2. The contraction vertex lies in neither interior. Both Y, Z survive as the same nonempty, disjoint, nonadjacent vertex sets in G. Expanding a boundary vertex increases its boundary size by at most one. Each remains positive and has boundary at most five. For T ∈ { Y, Z } write 

cT = |NG(u) ∩ NG(v) ∩ T |.

Exactly these pairs of incident edges were merged by contraction, so 

ρ4(G, T ) = ρ4(J, T ) + cT > 0.

If |∂GT | = 5, then w ∈ ∂J T , |∂J T | = 4, and both u, v belong to ∂GT . If ρ4(G, T ) ≥ 2, this 5-rooted side is quite heavy by definition. If its density is one, positivity and integrality force ρ4(J, T ) = 1 and cT = 0. No vertex of T then meets both u and v, and in particular none meets all five boundary roots. This also makes the side quite heavy. The resulting dense ( ≤ 5)-bifragment in G contradicts Lemma 4.4. 18 The next two lemmas concern vertices of degree at most seven. 

Lemma 4.6. Let L have between five and seven vertices and minimum degree at least four. For every 5-set S ⊆ V (L) it has an S-rooted K− 

> 5

minor. For every set of at most four prescribed roots it has the corresponding rooted clique minor. Proof. This is the local argument of [2, Lemma 6.4 and Corollary 6.5]. We include it to isolate its hypotheses. For five vertices, L is complete. For six vertices its complement is a matching. If uv is a missing edge on S, the outside vertex is adjacent to both u, v . Absorb it into u. At most one missing root edge remains. Suppose |V (L)| = 7 and let z1, z 2 be outside S. The complement has maximum degree two. Any vertex of complement degree two within S is adjacent in L to both zi. If the complement on S is a 5-cycle v1 . . . v 5, absorb z1 into v1 and z2 into v3. The same choice works if it contains a triangle or a 4-cycle on consecutively labelled vertices. At most one missing root edge remains. Otherwise that complement is a forest. If it has four edges, it is v1 . . . v 5. In this case, choose the outside labels so that v1z1 is an edge of L, and absorb 

z1 into v1 and z2 into v3. If it has at most three edges and contains a path v1v2v3, absorb both outside vertices into v2. Each endpoint meets at least one outside vertex in L, so only one root nonedge can remain. The remaining complement on S is a matching. Only the case of two edges ab, cd needs consideration. If an outside vertex meets both ends of either pair in L, absorb it into an end. Otherwise the complement contains matchings from {z1, z 2} onto {a, b } and onto 

{c, d }. Its degree bound gives z1z2 ∈ E(L). Relabel so that az 1, bz 2 ∈ E(L), and contract these two edges. Again at most the nonedge cd remains. For four prescribed roots, extend to five and use the first assertion. If both ends of the missing edge are prescribed, absorb the fifth branch set into one end. Otherwise discard it. This gives a rooted K4. Restriction yields the smaller rooted cliques. 

Lemma 4.7. Suppose additionally that |V (G)| ≥ 7, every edge of G is in at least four triangles, and v has degree at most seven. Put Q = N [v], and let Ci be the components of 

G − Q. Then 

|∂C i| ≤ 4, ρ4(G, C i) ≤ 0, ρ4(G) ≤ e(G[Q]) − 4|Q|.

Proof. This is the neighbourhood argument of [2, Lemma 6.6], with its local degree hypotheses stated separately. We use b-irreducibility only to exclude an isolated vertex and to exclude a boundary-clique reduction whose complement has at least six vertices. Thus the argument works for both b = 3 and b = 4. An isolated vertex would be reducible, since |V (G)| ≥ 7. Every other vertex is incident with an edge in at least four triangles and hence has degree at least five. Let L = G[N (v)]. It has between five and seven vertices and minimum degree at least four. Since v has no neighbour outside Q, every ∂C i is contained in N (v). If some Ci has at least five boundary vertices, choose five of them as roots in Lemma 4.6. Contract Ci, retain v, and combine with the rooted K− 

> 5

. The only possible missing edges are one root edge and the edge between v and the contracted component, which are independent. This gives K=7 , a contradiction. If ρ4(G, C i) > 0, put T = ∂C i and W = V (G) \ (Ci ∪ T ). This set contains v,is nonadjacent to Ci, and has boundary contained in T . Thus ρ4(G, W ) ≤ 0. Apply Lemma 4.6 to L with root set T , and retain only the root branch sets corresponding to 

∂GW . Each retained branch set avoids the other vertices of T , and every vertex of L − T

lies in W . The resulting boundary clique model therefore lies in G[W ∪ ∂GW ]. Hence 19 W is reducible, and its complement Ci ∪ T has at least six vertices. This contradicts 

b-irreducibility. Finally the components are pairwise nonadjacent, so 

ρ4(G) = e(G[Q]) − 4|Q| + ∑

> i

ρ4(G, C i) ≤ e(G[Q]) − 4|Q|. (4.3) 

# 5 Proofs of the main theorems 

## 5.1 Proof of Theorem 1.3 

Suppose Theorem 1.3 is false and choose a counterexample G with the fewest vertices. By (2.2), 

ρ4(G) = −8, |V (G)| ≥ 7. (5.1) The order bound follows also from (n 

> 2
> )

≥ 4n − 8 and n ≥ 3. Reducing an inclusionwise maximal reducible fragment preserves 4-bilightness, does not decrease density, and leaves at least three vertices. Minimality therefore shows that G is 3-irreducible. Apply Lemmas 4.3–4.5 with b = 3 and d = 8. If an edge uv has at most three common neighbours, then J = G/uv is 4-bilight and 

ρ4(J) = ρ4(G) + 3 − | N (u) ∩ N (v)| ≥ − 8.

It is a smaller counterexample, a contradiction. Every edge is thus in at least four triangles. Since G is 3-irreducible, it has no isolated vertex. Therefore G has minimum degree at least five. Its average degree is less than eight, so choose a vertex v of degree between five and seven, and put Q = N [v]. Lemma 4.7 gives 

e(G[Q]) ≥ 4|Q| − 8.

For |Q| = 6 this exceeds the number of possible edges. For |Q| = 7, it gives at least twenty edges and hence an K=7 subgraph. It remains that |Q| = 8 and e(G[Q]) ≥ 24. The complement on N (v) has maximum degree at most two and at most four edges. If it has at most three edges and a vertex of degree two, delete such a vertex. This leaves at most one nonedge. Otherwise its edges form a matching, and deleting an endpoint of a matching edge leaves at most two independent nonedges. If there are no nonedges, delete any vertex of N (v). Each case gives K=7 on seven vertices. If the complement F on N (v)has four edges, its nontrivial components are among the six types below. Isolated vertices are omitted, and path vertices are labelled consecutively. The operations are performed in 

G[Q]. The last column records its remaining nonedges.                                    

> Nontrivial components of FOperation in G[Q]Remaining nonedges
> C3⊔K2Delete a triangle vertex 2K2
> P5=x1· · · x5Delete x3x1x2, x 4x5
> P4⊔K2Delete x2of P4x3x4and the K2edge
> P3⊔2K2Delete the P3centre 2K2
> 2P3Contract the two centres None
> C4Retain for the next step C4

The list is exhaustive because a graph of maximum degree two is a union of paths and cycles, and four disjoint edges would require eight vertices in N (v). In the 2 P3 row the two centres are adjacent in G. Their sets of nonneighbours are disjoint. The contracted vertex is therefore adjacent to all remaining vertices, which also form a clique, giving K7.20 Every other row except C4 gives a K=7 subgraph after the indicated deletion. Thus the only remaining possibility is 

G[Q] = K4 ∨ (2 K2). (5.2) Write K for the 4-vertex clique and P, T for the two pairs in (5.2). Each pair induces an edge, and there are no edges between the pairs. Thus the complement on P ∪ T is K2,2, a 4-cycle. The other four vertices of Q are universal in G[Q]. Each component Ci of G − Q

has nonpositive density by Lemma 4.7. Since e(G[Q]) = 24, equality in (4.3) and (5.1) gives ρ4(G, C i) = 0 for every i. No component meets both P and T . Otherwise choose a path from p ∈ P to t ∈ T with its internal vertices in that component. Absorb all those internal vertices into p, keeping every vertex of Q in a distinct branch set. This adds the edge pt to the retained graph on Q. Any other new edges can be deleted. The complement 

C4 would become a 3-edge path. Deleting an internal vertex of this path gives K− 

> 7

.Let Y consist of P and all components meeting P , and let Z consist of T and all components meeting T . No component was assigned to both sets. Distinct components of 

G − Q have no edges between them, there is no edge between P and T , and any component adjacent to P or T was included in the corresponding set. Hence Y, Z are disjoint and nonadjacent, and every neighbour outside either set belongs to K. The zero densities of the added components give 

ρ4(G, Y ) = 1 + 8 − 4 · 2 + ∑  

> Cimeeting P

ρ4(G, C i) = 1 ,ρ4(G, Z ) = 1 + 8 − 4 · 2 + ∑  

> Cimeeting T

ρ4(G, C i) = 1 .

Here the first term counts the edge within the pair, and the second its eight edges to 

K. This dense small bifragment contradicts 4-bilightness and completes the proof of Theorem 1.3. 

## 5.2 Proof of Theorem 1.2 

Suppose Theorem 1.2 is false, and choose a counterexample G with the fewest vertices. It has neither an K=7 minor nor a K6 subgraph. By Theorem 1.3, 

ρ4(G) = −9, |V (G)| ≥ 7. (5.3) Orders four and five cannot meet the edge bound, and at order six the only graph meeting it is K6.

Lemma 5.1. The graph G is 4-irreducible. Proof. Suppose otherwise, and choose an inclusionwise maximal reducible fragment Y

subject to |V (G) \ Y | ≥ 4. Put Q = ∂GY and let J be the reducent. By Lemma 2.2(2), J

is 4-bilight. It is a minor of G and therefore has no K=7 minor. Let ε = 0 for a clique reduction and ε = 1 for a dart reduction, and let a be the number of edges added between surviving original vertices of Q. A dart introduces one new vertex and its four incident edges. A clique reduction introduces no vertex. Consequently 

|V (J)| = |V (G)| − | Y | + ε

and 

e(J) = e(G) − e(G[Y ]) − eG(Y, Q ) + a + 4 ε. 

21 Subtracting four times the vertex count gives 

ρ4(J) − ρ4(G) = −ρ4(G, Y ) + a ≥ 0.

Here both −ρ4(G, Y ) and a are nonnegative integers. Since |V (J)| ≥ | V (G) \ Y | ≥ 4, Theorem 1.3 applies to J. Its absence of a K=7 minor implies ρ4(J) < −8, and integrality gives ρ4(J) ≤ − 9. Combining this with (5.3) and the preceding inequality yields 

ρ4(J) = ρ4(G) = −9, ρ4(G, Y ) = 0 , a = 0 . (5.4) The last two equalities follow because their nonnegative integer contributions sum to zero. The equality a = 0 means that the reduction has added no edge between surviving original vertices. This is the property needed below. In particular, the graph induced by the surviving original vertices of J is exactly G − Y .In a clique reduction, every vertex of J is such a vertex, so a K6 subgraph of J would already be a K6 subgraph of G. In a dart reduction the only other vertex has degree four, whereas every vertex in a K6 subgraph has at least five neighbours. It cannot belong to a 6-clique, and any 6-clique avoiding it would again be present in G. Therefore J has no K6

subgraph. Finally, J has fewer vertices than G. A clique reduction removes the nonempty set Y ,while a dart reduction replaces at least two vertices by one. Hence J is a smaller 4-bilight graph on at least four vertices with ρ4(J) = −9, no K=7 minor and no K6 subgraph. This contradicts the minimal choice of G.

Lemma 5.2. G has no K5 subgraph. Proof. Suppose that C is a 5-vertex clique. We first show that ( G, C ) is 4-light. If not, choose a positive ( ≤ 4)-fragment Y avoiding C, put T = ∂GY , and write k = |T | ≤ 4 and 

B = G − Y . The set Y ∪ T has at least six vertices by the positive-fragment estimate in the proof of Lemma 2.2. Suppose first that B has k vertex-disjoint paths from T to C, with zero-length paths allowed for vertices in T ∩ C. Truncate each path at its first vertex of C. Each vertex of T

is the initial vertex of exactly one path, and the endpoints in C are distinct. Use each path as one root branch set. The edges between their distinct endpoints in C give a T -rooted 

Kk model in B. Every vertex of T starts its own path, so no retained branch set can pass through a different vertex of T .Set W = V (G) \ (Y ∪ T ). Since C ∩ Y = ∅ and |C| = 5 > k , W is nonempty. It is nonadjacent to Y and has boundary contained in T . Thus 4-bilightness forces ρ4(G, W ) ≤ 0. Restrict the clique model to the roots in ∂GW if necessary. Its retained branch sets avoid all other vertices of T , so it lies in G[W ∪ ∂GW ]. Hence W is reducible and its complement 

Y ∪ T has at least six vertices, contrary to Lemma 5.1. If k = 0, the same conclusion follows by deleting the component set W . The boundary clique is empty. Otherwise, let q < k be the maximum number of disjoint T –C paths. By Menger’s theorem, choose a separation ( I, J ) of B of order q with T ⊆ I and C ⊆ J. Put Q = I ∩ J

and U = J \ I. At least 5 − q vertices of C lie in U , so U is nonempty. Also U ∩ T = ∅

and ∂GU ⊆ Q. The separation gives this in B, and no vertex of U can meet Y because all neighbours of Y outside Y lie in T .A maximum family of q disjoint paths meets every vertex of Q, each on a different path. Indeed, every path must cross Q, and there are only q separator vertices. Each path contains exactly one separator vertex. Indeed, if one contained two, the other q − 1 disjoint 22 paths could not all cross the remaining q − 2 vertices. After its separator vertex, a path stays in J, since returning from I \ J would require a second separator vertex. Its tail to C is therefore a branch set in J with exactly one root in Q. The endpoints in C are distinct and mutually adjacent, so these tails give a Q-rooted clique model. Restrict it to the actual boundary ∂GU . The other separator vertices belong to discarded root branch sets. This gives a boundary clique model in G[U ∪ ∂GU ]. When q = 0, the empty boundary clique has the same conclusion. The set U is nonadjacent to the positive fragment Y , so ρ4(G, U ) ≤ 0 by 4-bilightness. Its complement contains Y ∪ T and therefore at least six vertices. Thus U is another forbidden reducible fragment. This proves that ( G, C ) is 4-light. Now 

ρ4(( G, C )) = ρ4(G) + 20 − e(G[C]) = −9 + 20 − 10 = 1 .

No vertex outside C meets all five vertices of C, since such a vertex would form a K6

subgraph with C. Hence ( G, C ) is quite heavy, and Theorem 2.1(3) supplies a weak W

model. Retain the already present root clique on C. The resulting seven branch sets miss only the edge between the two nonroots, or at most two independent root–nonroot edges. They therefore contain a K=7 model, the required contradiction. We can now apply the structural lemmas with b = 4, d = 9. In particular all edge contractions preserve 4-bilightness. If an edge uv has at most three common neighbours, then J = G/uv satisfies ρ4(J) ≥ − 9. It also has no K6 subgraph. A new 6-clique using the contraction vertex would have five other vertices that already form a K5 in G, contrary to Lemma 5.2. A 6-clique not using it would already be in G. As |V (J)| ≥ 6, minimality is contradicted. Every edge therefore lies in at least four triangles. Irreducibility excludes isolated vertices, so the minimum degree is at least five. The average degree is less than eight. Choose v with 5 ≤ d(v) ≤ 7 and put Q = N [v]. By Lemma 4.7, 

e(G[Q]) ≥ 4|Q| − 9. (5.5) If |Q| = 6, this forces G[Q] = K6. If |Q| = 7, it has at most two missing edges, and deleting at most two suitable vertices leaves a K5. Both cases contradict Lemma 5.2. Thus |Q| = 8 and e(G[Q]) ≥ 23. Let F be the complement of G[N (v)]. It has seven vertices, at most five edges and maximum degree at most two. It has no independent set of size four, since such a set together with v would be a K5 in G. If F were bipartite, one part would have size at least four. Hence it has an odd cycle. A 7-cycle exceeds the edge budget. A 5-cycle uses all five edges and leaves two isolated vertices, again giving an independent 4-set. The odd cycle is therefore a triangle. The triangle is a component because ∆( F ) ≤ 2. On the other four vertices there are at most two edges. With at most one edge there is an independent 3-set. With two edges sharing an endpoint, their two other endpoints and the fourth vertex form an independent 3-set. Either 3-set, together with one triangle vertex, would be an independent 4-set of F . Thus the two edges on the remaining vertices must be disjoint. Consequently 

F = K3 ⊔ 2K2.

Label the triangle abc and the two other edges de, f g . The edge ad is present in G[Q]. Contract it. The new vertex is adjacent to all six remaining vertices, and the only remaining nonedges are bc and f g , which are independent. The resulting minor is K=7 , the final contradiction. This proves Theorem 1.2 and hence Theorem 1.1. 23 Acknowledgements 

This work was supported by the National Natural Science Foundation of China under Grant No. 12161073, the Guangdong Basic and Applied Basic Research Foundation under Grant No. 2026A1515012764, the GDUPT Talent Recruitment Project (No. 2024rcyj1007), and the Scientific Research Funds at China University of Geosciences (Wuhan) (Project No. 2026039). 

# Declaration of generative AI and AI-assisted technologies 

OpenAI Codex contributed ideas and suggested a proof strategy for Theorem 3.3. It was also used to assist with the organization and editing of the manuscript and the preparation of reproducible diagram code. All theoretical results and proofs have been checked by the authors. 

# References 

[1] Z. Dvořák, Extremal function for rooted K5 minors , arXiv:2609.13818v1, 2026. 

https://arxiv.org/abs/2609.13818v1 .[2] Z. Dvořák, S. Norin and N. Rahman, Every graph with no K=7 minor is 6-colorable ,arXiv:2609.17760v1, 2026. https://arxiv.org/abs/2609.17760v1 .[3] R. Fabila-Monroy and D. R. Wood, Rooted K4-minors , Electron. J. Combin. 20 (2) (2013), P64. https://doi.org/10.37236/3476 .[4] L. K. Jørgensen, Contractions to K8, J. Graph Theory 18 (1994), 431–448. 

https://doi.org/10.1002/jgt.3190180502 .[5] W. Mader, Homomorphiesätze für Graphen , Math. Ann. 178 (1968), 154–168. 

https://doi.org/10.1007/BF01350657 .[6] S. Norin and A. Totschnig, Every graph with no K∨ 

> 7

-minor is 6-colorable ,arXiv:2507.03244v1, 2025. https://arxiv.org/abs/2507.03244v1 .[7] M. Rolek, The extremal function for K=9 minors , J. Graph Theory 94 (2020), 206–223. https://doi.org/10.1002/jgt.22515 .[8] Z.-X. Song, The extremal function for K− 

> 8

minors , J. Combin. Theory Ser. B 95 

(2005), 300–317. https://doi.org/10.1016/j.jctb.2005.04.006 .[9] Z.-X. Song and R. Thomas, The extremal function for K9 minors , J. Combin. Theory Ser. B 96 (2006), 240–252. https://doi.org/10.1016/j.jctb.2005.07.008 .[10] A. Thomason, The extremal function for complete minors , J. Combin. Theory Ser. B 

81 (2001), 318–338. https://doi.org/10.1006/jctb.2000.2013 .24