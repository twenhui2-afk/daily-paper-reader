Title: From IceCube to IT-Sphere: A Hybrid Quantum-Classical GNN for Banking IT Root Cause Analysis

URL Source: https://arxiv.org/pdf/2609.22822v1

Published Time: Tue, 22 Sep 2026 00:40:17 GMT

Number of Pages: 3

Markdown Content:
From IceCube to IT-Sphere :A Hybrid Quantum–Classical GNN for Banking IT Root Cause Analysis 

Antonio Greco, Riccardo Paoletti, Roberto Cappuccio, Mario Onorato 

IEEE Copyright Notice 

© 2026 IEEE. Personal use of this material is permitted. Permission from IEEE must be obtained for all other uses, in any current or future media, including reprinting/republishing this material for advertising or promotional purposes, creating new collective works, for resale or redistribution to servers or lists, or reuse of any copyrighted component of this work in other works. 

Publication status 

Accepted for publication in the Proceedings of the 2026 IEEE International Conference on Quantum Computing and Engineering (QCE26) , Metro Toronto Convention Centre, Toronto, ON, Canada, 13–18 September 2026. Paper ID POS2-1600, Poster Track. DOI: to appear. 

> This is the author’s accepted version. It is not the IEEE-published version of record.

Cite as 

A. Greco, R. Paoletti, R. Cappuccio, and M. Onorato, “From IceCube to IT-Sphere: A Hybrid Quantum–Classical GNN for Banking IT Root Cause Analysis,” in 2026 IEEE International Conference on Quantum Computing and Engineering (QCE) , Toronto, ON, Canada, 2026, to be published. 

@inproceedings{greco2026hqrca, author = {Greco, Antonio and Paoletti, Riccardo and Cappuccio, Roberto and Onorato, Mario}, title = {From {IceCube} to {IT-Sphere}: A Hybrid Quantum--Classical {GNN} for Banking {IT} Root Cause Analysis}, booktitle = {2026 IEEE International Conference on Quantum Computing and Engineering (QCE)}, address = {Toronto, ON, Canada}, year = {2026}, note = {to be published} }arXiv:2609.22822v1 [quant-ph] 19 Sep 2026 From IceCube to IT-Sphere :A Hybrid Quantum–Classical GNN for Banking IT Root Cause Analysis 

Antonio Greco ∗† , Riccardo Paoletti ∗† , Roberto Cappuccio ∗† , Mario Onorato ‡

> ∗

Department of Physical Sciences, Earth and Environment (DSFTA), University of Siena , Siena, Italy • †Istituto Nazionale di Fisica Nucleare (INFN) , Pisa, Italy • ‡IBM Consulting—Promontory, IBM Industry Diamond, Banking & Financial Markets , Milan, Italy a.greco19@student.unisi.it • riccardo.paoletti@unisi.it • roberto.cappuccio@pi.infn.it • monorato@promontory.com 

Abstract—We present Hybrid Quantum Root Cause Analysis (HQ-RCA), an industrially grounded workflow for root cause analysis in banking IT operations, built on a hybrid Quantum Graph Neural Network (QGNN): the classical backbone of DynEdge (the IceCube neutrino-reconstruction GNN, which we call standalone DynEdge ), with its classification head replaced by a Variational Quantum Circuit (VQC). On 13 months of anonymised IT data (13k alarm clusters) from a major European bank, its hybrid QGNN matches standalone DynEdge — the strongest classical baseline — on F1, while standalone DynEdge leads the ranking metrics. A readout-sensitivity and layout-robustness study, analysed via Dimensional Expressivity Analysis (DEA), shows that the effective parameter dimensionality (rank) of the quantum observable has no measurable correlation with F1; we therefore keep the simplest readout ⟨Z0⟩ (the Pauli-Z expectation on the first qubit), which in the deployed layout is rank-1, collapsing optimisation to a 1-D problem solvable by a gradient-free grid scan. Execution on IBM Heron r2 (no error mitigation) shows this gradient-free readout is executable on NISQ hardware after threshold recalibration. 

I. I NTRODUCTION 

Modern banks run continuous IT monitoring that emits a steady stream of alarms; industrial event-correlation tools cluster them by recurring patterns but are not primarily de-signed to reconstruct the causal chain across interconnected services — a diagnosis still made by hand. We cast root cause analysis (RCA) as a graph-reconstruction problem inspired by IceCube neutrino-event reconstruction. Each alarm cluster is a graph: a log wave (background cluster, analogous to an atmospheric muon) or a rare IT Event (an incident hiding, by assumption, a single root-cause alarm, analogous to a neutrino vertex). We term the banking IT ecosystem the IT-Sphere , the analogue of IceCube’s detector volume (Fig. 1) — a structural analogy, not system identity. Clusters are weakly labelled by a heuristic root-cause score, the labelling rule validated by domain experts. On a 2,000-graph subset (400-graph golden set; one train/test split and CV at a fixed seed (42), identical for every model) we benchmark Random Forest (RF), XGBoost (XGB), the IceCube DynEdge run end-to-end ( standalone DynEdge ), and the hybrid QGNN, which reuses that backbone with a VQC in place of the head (§II; Fig. 4). II. M ETHOD AND ARCHITECTURAL SELECTION 

We reuse DynEdge [1], the GNN built for IceCube neu-trino reconstruction, as the classical backbone — it handles         

> IceCube IT-Sphere concept
> DOM photon sensor Monitoring system Sensor unit
> Atmospheric-μevent Log wave Background cluster
> Astrophysical-νevent IT Event Signal cluster
> Cherenkov cascade Service-dep. cascade Cluster structure
> Interaction vertex Root-cause node Within-cluster goal

Fig. 1. IceCube ↔ IT-Sphere structural correspondence: each centre concept links a neutrino-detector notion (left) to its banking-IT counterpart (right) — a structural analogy, not a task identity.              

> θ|ψ(θ, x v)⟩⟨Z0⟩P(y|xv)ˆyvF1
> circuit meas. softmax τ-thresh P/R
> quantum classical

Fig. 2. HQ-RCA end-to-end workflow: a quantum block ( θ→⟨ Z0⟩) feeding a classical block ( ⟨Z0⟩→ F1). AP and ROC-AUC are read off P(y|xv ) directly (threshold-free). 

variable-topology graphs from Union–Find grouping. The VQC [2] is built from an encoding E and a parametric 

ansatz A. The end-to-end pipeline is: (i) Union–Find group-ing and principal component analysis (PCA) on the node features (8→7, 95% variance retained); (ii) DynEdge —four EdgeConv blocks (graph edge-convolution layers that update each node from its neighbours) producing a node embedding hv ∈ R1031 , with 1031=7+4 ×256 ; (iii) linear layer + tanh activation: R1031 → xv ∈ (−1, +1) 4; (iv) 4-qubit VQC encodes xv via E, applies the ansatz A, and measures the single-qubit baseline observable ⟨Z0⟩ (analysed in §III); (v) linear head outputs the root-cause probability P(y|xv , θ) = softmax( w ⟨Z0⟩(xv , θ) + b)y , where ⟨Z0⟩ ∈ [−1, +1] is the expectation of the Pauli observable Z0 = Z ⊗ I⊗3 on the first qubit, w, b ∈ R2 are trainable head parameters, and 

y ∈ { 0, 1} is the class label. F1 also depends on the data, preprocessing and the trained classical components; the map 

F1 : R12 ×[0 , 1] → [0 , 1] , (θ, τ ) 7 → F1, is the pipeline of Fig. 2 ( τ :decision threshold, fixed at τ =0 .5 in simulation, recalibrated on hardware, §IV). 

Architectural selection (encoding–ansatz sweep). Two base-lines motivate the search: (0a) RY (Y-rotation) encod-ing, one Ry per qubit, + StronglyEntanglingLayer ×1 (12 PQC params); (0b) a one-layer hardware-efficient embedding (HEE1) with an SO(4) two-qubit gate [3] (24 params). (0a) outperforms (0b), though the two differ in both axes. To disentangle the two axes we run an L-shaped sweep of 13 distinct configurations: (1) encoding fixed (RY, 9 ansatz variants) → best ansatz RandomLayers; (2) ansatz fixed (Ran-domLayers, 5 encodings incl. RY) → best encoding HEE2 (two HEE layers). Both axes strongly affect performance; 

HEE2+RandomLayers (12 VQC params) is selected (best 

F1=0 .931 , AP =0 .953 , AUC =0 .974 ; Fig. 4). Data scaling 

(2k →13k): F1=0 .915 ±0.017 , AP=0 .947 ±0.020 and ROC-AUC =0 .973 ±0.013 , each within 1σ of its 2k value (Fig. 4) — consistent at scale. III. E FFECTIVE DIMENSIONALITY AND READOUT CHOICE 

Effective dimensionality of the ⟨Z0⟩ readout. With the architecture fixed (HEE2+RandomLayers) and the baseline readout ⟨Z0⟩, we ask how many of the 12 parameters the read-out actually uses. Dimensional Expressivity Analysis (DEA) answers this through the thin SVD of the empirical observable 

Jacobian (not the state Jacobian of [4]) J=∂⟨Z0⟩/∂ θ from 200 uniformly sampled inputs (a 200 ×12 matrix); the rank counts singular values above a cutoff ε=10 −3 (well above the noise floor). Here J is rank-1: σ1=1 .336 while σ2, . . . , σ 12 sit at machine precision, ∼10 −16 (Fig. 3, left). Since P(y|x, θ)

depends on θ only through the scalar ⟨Z0⟩, the Fisher rank equals rank( J) exactly. The whole optimisation thus collapses onto one effective direction: F1 as a function of the lone effective angle θ0 — traced by a 40-point grid scan over one period — is a 2π-periodic step function with a single broad plateau (Fig. 3, right). Fine-tuning θ0 alone — the other eleven frozen at their trained values — reaches F1=0 .925 at 

θ∗ 

> 0

=6 .241 rad; with θ0 re-initialised at random, retraining still reaches F1=0 .911 (start-dependent). 

Effect of a richer readout. This rank-1 collapse is specific to ⟨Z0⟩, so we test richer readouts. On the same circuit, splits and hyperparameters we compare three readouts: the baseline 

⟨Z0⟩ (one Pauli expectation, no extra parameters); a parametric single-qubit readout M (ϕ)= P 

> i

ϕi⟨Zi⟩ (4 parameters); and a two-local Z readout M (ϕ, ξ )= P 

> i

ϕi⟨Zi⟩ + P 

> i<j

ξij ⟨ZiZj ⟩

(single and two-qubit Z terms, 10 parameters). The richer readouts raise the effective rank but not F1: ⟨Z0⟩ gives the best held-out F1 (0.931 ±0.009 , rank 1) and the smallest variance, against 0.916 ±0.004 for M (ϕ) and 0.892 ±0.031 

for the two-local Z readout (both rank 10); AP and ROC-AUC stay comparable across the three (Fig. 4). To isolate what the quantum part adds, we replace the VQC with a classical head of the same parameter count (Linear + tanh →scalar, not DynEdge’s native head). At τ =0 .5 it reaches F1 0.876 ±0.063 

(AP/ROC-AUC 0.961 /0.986 ); recalibrating only its threshold (τ ∗≈0.76 , 5-fold) lifts F1 to 0.907 ±0.020 — still below the VQC’s 0.931 ±0.009 — so its F1 deficit is not merely calibration. The classical head instead leads the threshold-          

> k
> σk
> singular value (log)  10 010 −210 −410 −610 −8
> ε=10 −3
> σ1=1 .336
> σ1σ3σ5σ7σ9σ11
> rank J=1
> σ2. . .σ 12 : machine prec.
> θ0
> F1(θ0)
> 0.925 00π/ 2π3π/ 22π
> ⋆
> θ∗
> 0 =6 .241
> single broad max plateau

Fig. 3. Left: DEA SVD spectrum (log) — only σ1=1 .336 exceeds ε=10 −3.Right: F1(θ0) from a 40-point grid scan, a single broad plateau ( ∼37% of period) peaking at θ∗ 

> 0

=6 .241 rad ( ⋆).              

> RF XGB DynEdge
> standalone
> ⟨Z0⟩M(ϕ)Z+ZZ noisy sim HW
> 0.80 0.85 0.90 0.95 1.00
> DEA rank: 110 10 11
> score
> F1AP AUC

Fig. 4. F1, AP, ROC-AUC for all models (2,000-graph subset; AP chance 

≈0.17 ); DEA rank below the quantum readouts. Error bars: σ, three held-out subsets (HW: five folds). 

free ranking metrics (AP/ROC-AUC). The quantum head is feasible, not decisively advantageous. Layout dependence of the rank. We test whether rank-1 is intrinsic to the architecture or specific to this layout. Sweeping the RandomLayers seed over its first 21 values (0–20), the ⟨Z0⟩ rank ranges from 0 to 9 (mean 3.3) — rank-1 is the exception, not the rule. Yet across these 21 layouts, F1 stays high and no correlation between 

F1 and the rank is observed (Pearson −0.08 , p=0 .73 ). We therefore adopt ⟨Z0⟩: rank-1 at the deployed layout, hence 1-D-optimisable and gradient-free on hardware. IV. NISQ HARDWARE EXECUTION 

Under simulated depolarising noise, F1 stays ≥0.90 up to a 5% two-qubit gate-error rate ( p2q =0 .05 ); a shot-noise sweep at p2q =0 .01 fixes 200 shots/node: F1=0 .918 , near the 0.922 

plateau at ≥500 shots. With θ0 at its §III optimum, it runs on 

ibm_fez (IBM Heron r2, no error mitigation) in ∼2 min. Device noise shifts the score distribution: at τ =0 .5 the model over-predicts and F1 falls to 0.587 , whereas the threshold-invariant ranking metrics are largely preserved (AP 0.927 ,ROC-AUC 0.957 , vs 0.960 /0.977 in simulation). Recalibrat-ing only the threshold ( τ ∗≈0.68 ), validated by 5-fold cross-validation, recovers F1=0 .915 ±0.037 (matching the noisy-simulator ∼0.92 ) — noise shifts the optimal threshold more than class separability. V. C ONCLUSIONS 

The hybrid QGNN matches standalone DynEdge — the strongest classical baseline — on F1 (DynEdge ahead on AP/AUC) and runs on ibm_fez without error mitigation at 

F1=0 .915 (recalibrated). This makes HQ-RCA a hardware-executable, quantum-assisted root-cause pipeline rather than a quantum advantage. The choice is metric-driven: in this setting, the hybrid head is operationally justified when de-ployment optimises thresholded detection ( F1) — e.g., binary escalation of an alarm cluster — where the VQC outperforms a parameter-matched classical head (§III); ranked alarm triage instead favours standalone DynEdge (AP/AUC). Future work :other embedding families; a new domain, mainframe transac-tions. ACKNOWLEDGMENTS 

The authors thank Mariagrazia Silvestro and Paolo Bandini for valuable discussions. No specific funding was received. Claude (Anthropic) assisted with the LaTeX/TikZ code for the figures. REFERENCES