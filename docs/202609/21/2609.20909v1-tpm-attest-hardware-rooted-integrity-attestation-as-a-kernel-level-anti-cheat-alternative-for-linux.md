---
title: "TPM-Attest: Hardware-Rooted Integrity Attestation as a Kernel-Level Anti-Cheat Alternative for Linux"
source: arxiv
title_zh: 用于Linux的TPM-Attest：Hardware-Rooted Integrity Attestation as Kernel-Level Anti-Cheat Alternative
abstract_zh: "Multiplayer PC gaming on Linux faces structural problem：anti-cheat systems that publishers require o。\n\n核心做法：This paper presents TPM-Attest，hardware-rooted remote attestation 框架 that uses Trusted Platform 模块 (。\n\n主要结果：Across 500 constructed tamper sessions we 达到 100% detection rate；incremental leaf caching reduces re。\n\n值得关注的点在于：full implementation is released as open-source software。"
abstract_en: "Multiplayer PC gaming on Linux faces a structural problem: the anti-cheat systems that publishers require operate as proprietary Ring 0 kernel modules that are architecturally incompatible with Linux's security model, GPL licensing, and stable ABI guarantees. We argue the right response is not to port these invasive modules to Linux, but to replace them entirely. This paper presents TPM-Attest, a hardware-rooted remote attestation framework that uses the Trusted Platform Module (TPM) 2.0 and the Linux Integrity Measurement Architecture (IMA) to prove, cryptographically, that a client booted cleanly and ran only authorised software -- without any kernel driver, without proprietary code, and without scanning player memory. The system intercepts Epic Online Services (EOS) SDK calls via a userspace LD_PRELOAD hook, gates session access on a live TPM quote bound to a server-issued nonce, and constructs an index-prefixed Merkle tree over the IMA log that is immune to duplicate-leaf collision attacks. Across 500 constructed tamper sessions we achieve a 100% detection rate; incremental leaf caching reduces repeat-attestation latency to under 3 seconds on real TPM 2.0 hardware. A controlled red-team evaluation against a live demo game confirms all four file-backed attack vectors are blocked while precisely characterising the two confirmed bypass conditions. The full implementation is released as open-source software."
authors: "Anudeep Gedela, A. Yaswanth"
date: 2026-09-17
journal: cs.CR
source_link: "https://arxiv.org/pdf/2609.20909v1"
pdf: "https://arxiv.org/pdf/2609.20909v1"
score: 6.35
tldr: 围绕《用于Linux的TPM-Attest：Hardw...》；This paper presents TPM-Attest，h...；Across 500 constructed tamper sessio...
tldr_cn: 围绕《用于Linux的TPM-Attest：Hardw...》；This paper presents TPM-Attest，h...；Across 500 constructed tamper sessio...
tldr_en: "Multiplayer PC gaming on Linux faces a structural problem: the anti-cheat systems that publishers require operate as proprietary Ring 0 kernel modules that are architecturally incompatible with Linux's security model, GPL licensing, and stable ABI guarantees. We argue the right response is not to port these invasive modules to Linux, but to replace them entirely."
selection_source: fresh_fetch
vocabulary: ["detection | 检测 | CET6"]
motivation: Multiplayer PC gaming on Linux faces structural problem：anti-cheat systems that publishers require o。
method: 核心做法：This paper presents TPM-Attest，hardware-rooted remote attestation 框架 that uses Trusted Platform 模块 (。
result: "主要结果：Across 500 constructed tamper sessions we 达到 100% detection rate；incremental leaf caching reduces re。"
conclusion: 值得关注的点在于：full implementation is released as open-source software。
---

## 摘要
Multiplayer PC gaming on Linux faces structural problem：anti-cheat systems that publishers require o。

核心做法：This paper presents TPM-Attest，hardware-rooted remote attestation 框架 that uses Trusted Platform 模块 (。

主要结果：Across 500 constructed tamper sessions we 达到 100% detection rate；incremental leaf caching reduces re。

值得关注的点在于：full implementation is released as open-source software。

## Abstract
Multiplayer PC gaming on Linux faces a structural problem: the anti-cheat systems that publishers require operate as proprietary Ring 0 kernel modules that are architecturally incompatible with Linux's security model, GPL licensing, and stable ABI guarantees. We argue the right response is not to port these invasive modules to Linux, but to replace them entirely. This paper presents TPM-Attest, a hardware-rooted remote attestation framework that uses the Trusted Platform Module (TPM) 2.0 and the Linux Integrity Measurement Architecture (IMA) to prove, cryptographically, that a client booted cleanly and ran only authorised software -- without any kernel driver, without proprietary code, and without scanning player memory. The system intercepts Epic Online Services (EOS) SDK calls via a userspace LD_PRELOAD hook, gates session access on a live TPM quote bound to a server-issued nonce, and constructs an index-prefixed Merkle tree over the IMA log that is immune to duplicate-leaf collision attacks. Across 500 constructed tamper sessions we achieve a 100% detection rate; incremental leaf caching reduces repeat-attestation latency to under 3 seconds on real TPM 2.0 hardware. A controlled red-team evaluation against a live demo game confirms all four file-backed attack vectors are blocked while precisely characterising the two confirmed bypass conditions. The full implementation is released as open-source software.

## 英语词汇
- **detection**：检测（CET6）
