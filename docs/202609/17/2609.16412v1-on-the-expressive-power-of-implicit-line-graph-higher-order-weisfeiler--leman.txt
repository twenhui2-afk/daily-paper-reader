Title: On the Expressive Power of Implicit Line-Graph Higher-Order Weisfeiler–Leman

URL Source: https://arxiv.org/pdf/2609.16412v1

Published Time: Wed, 16 Sep 2026 00:29:49 GMT

Number of Pages: 40

Markdown Content:
# On the Expressive Power of Implicit Line-Graph Higher-Order Weisfeiler–Leman 

Fan Yang 

Independent Researcher 

lukeyf.ubc@gmail.com 

# Abstract 

Whitney’s theorem allows isomorphism testing for connected simple graphs, apart from K3 and K1,3, to be formulated as distinguishing their line graphs. However, the relation between fixed-dimensional Weisfeiler–Leman (WL) expressivity on line graphs and on their roots remains unresolved. We study this relation through Implicit Line-Graph WL (ILG-k-WL), which is exactly k-WL on L(G), executed over the edges of G with line-graph relations derived from endpoint incidence and without explicitly constructing L(G). On the Whitney-general class, the relation between root-domain and line-graph WL depends on k. For k = 1 , 2,ILG-k-WL adds no distinguishing power beyond root-domain 1-WL and misses some pairs that 1-WL separates. For k = 3 , we prove the backward containment 

L(G) ≡3-WL L(H) ⇒ G ≡3-WL H. Strongly regular witness pairs, including the Shrikhande/rook pair, show that ILG-3-WL is strictly more expressive than 3-WL. The backward containment also extends to disconnected graphs with no isolated vertices when every connected component is Whitney-general. Deterministic ILG-3-WL separates all three substructure-counting witness pairs, all 105 pairs in SR25, and 359 of 400 BREC pairs. An untrained dense ILG-3-GNN gives the same pairwise verdicts on these evaluations. 

# 1 Introduction 

Graph neural networks (GNNs) are used in molecular property prediction [14], social-network analysis [16], combinatorial optimization [18], and program analysis [1]. Their ability to distinguish non-isomorphic inputs matters independently of the learning objective: if a graph encoder assigns two graphs the same representation, a downstream predictor cannot distinguish them from that representation alone. The Weisfeiler–Leman (WL) hierarchy formalizes this limitation. Standard message-passing neural networks (MPNNs) are bounded by 1-WL [21, 27], which fails to separate many regular and highly symmetric graphs. Higher-order GNNs use states indexed by multiple vertices, as in higher-order WL refinement. The k-GNN family operates on vertex subsets [21], whereas Provably Powerful Graph Networks (PPGN) use pair-indexed feature matrices and have provable 3-WL expressive power [20]. Graph transformations can also change what a fixed WL dimension detects. The line graph L(G)

represents every root edge by a vertex and joins two such vertices when their edges share an endpoint. For connected simple graphs G, H , Whitney’s theorem [26] states that L(G) ∼= L(H) implies 

G ∼= H, except when {G, H } = {K3, K 1,3}. This isomorphism statement does not imply equal fixed-dimensional WL power on roots and their line graphs. Existing line-graph models address community detection [9], link prediction [7], joint node–edge learning [17], and atomistic learning [10]. Other methods transform the refinement domain: LGAN forms local line graphs [11], ESAN processes selected subgraphs [3], and δ-k-LWL restricts tuple replacements to local neighbors [22]. Simplicial and cellular WL add cliques or cells and refine colors through their incidences [4, 5]. These works do not determine how fixed-dimensional WL on 

L(G) compares with the same dimension on G.

Accepted at the Learning on Graphs Conference (LoG 2026), oral presentation. arXiv:2609.16412v1 [cs.SI] 14 Sep 2026 Expressive Power of Implicit Line-Graph WL K3,3 prism Shrikhande 4 × 4 rook separated by 3-WL, not 1-WL separated by ILG-3-WL, not 3-WL 

Figure 1: Graph pairs separated by 3-WL (left) and ILG-3-WL (right). Left: 3-WL separates K3,3

and the triangular prism, whereas 1-WL does not. Right: ILG-3-WL separates the Shrikhande and 

4 × 4 rook graphs, whereas 3-WL does not; their four-clique counts are 0 and 8, respectively. This paper compares fixed-dimensional WL on G and L(G) through Implicit Line-Graph WL (ILG-k-WL), which runs k-WL over root edges using endpoint-derived line-graph relations without constructing L(G). The Shrikhande and rook graphs illustrate why this comparison is nontrivial. They are non-isomorphic strongly regular graphs with the same parameters and form a standard hard case for 3-WL [2, 5]. Yet 3-WL separates their line graphs; line-graph transformation can break strong regularity [28]. Figure 1 shows this pair. 

Contributions. We determine the relation between root-domain and line-graph WL for k ∈{1, 2, 3} on the Whitney-general class. ILG-k-WL is strictly weaker than root-domain k-WL for 

k = 1 , 2 (Corollary 2), whereas Theorem 1 and strongly regular witnesses give 3-WL ≺ ILG -3-WL (Corollary 5). One global pair update counts four-cliques exactly on every finite simple graph (Corollary 3). Backward transfer also extends to disconnected graphs with no isolated vertices and Whitney-general components (Theorem 2). Both backward-transfer theorems are formalized in Lean 4 (Appendix C). Direct ILG-k-WL is k-WL on L(G). For k ≥ 3, its global-edge form implements (k−1) -FWL using Θ( mk−1) root-edge tuple states and endpoint-derived line-graph relations. On bounded finite domains, ILG-k-GNNs can match any fixed number of global-edge ILG-k-WL rounds, and every parameterization is bounded by that refinement. We evaluate deterministic ILG-3-WL and an untrained dense ILG-3-GNN on the three substructure-counting witness pairs, all 105 SR25 pairs, and all 400 BREC pairs. Their verdicts coincide: 3/3,

105 /105 , and 359 /400 separations, respectively (Section 6). 

# 2 Background and Conventions 

Throughout, G and H denote finite, undirected, simple graphs, and L(G) and L(H) denote their line graphs. For one graph G, we write V (G) and E(G) for its vertex and edge sets and abbreviate 

n = |V (G)| and m = |E(G)| when no second graph is present. Edges are unordered two-element subsets e = {u, v }; an implementation may list the edges in any order and store the two endpoints of each edge in either order. For u ∈ V (G), let NG(u) and dG(u) denote its neighborhood and degree; we omit the subscript only when the graph is unambiguous. We write AG for the adjacency matrix of G, and IS , J S for the identity and all-ones matrices indexed by a finite set S. Vertices and edges of G, H are called root vertices and root edges ; a root edge e ∈ E(G) is canonically the line-graph vertex e ∈ V (L(G)) . We write Deg( G) = {{ dG(u) : u ∈ V (G)}} for the root degree multiset. The line graph L(G) has vertex set E(G), with two vertices adjacent exactly when the corresponding distinct edges of G share an endpoint. 

Definition 1 (Whitney-general class) . A graph is Whitney-general if it is connected, simple, and not isomorphic to K3 or K1,3. Whitney’s theorem [26] guarantees that its line graph determines it up to isomorphism. A graph is componentwise Whitney-general if it has no isolated vertices and every connected component is Whitney-general. 

> 2Expressive Power of Implicit Line-Graph WL

Definition 2 (Line-graph relation code) . For e, f ∈ E(G), write e ∼L(G) f when e̸ = f and 

e ∩ f̸ = ∅ — that is, when e and f are adjacent in L(G) — and define the three-valued relation code 

rG(e, f ) = 



0, e = f, 

1, e ∼L(G) f, 

2, otherwise .

The graph-level signature of a coloring c : S → C over a finite state set S is its multiset of colors 

{{ c(s) : s ∈ S}} ; a refinement procedure distinguishes two graphs when their graph-level signatures differ after stabilization or after a specified number of rounds. All occurrences of HASH below mean a fixed injective encoding of the displayed finite signature; only the induced color partition matters. When two graphs are compared, their refinements are run in parallel with the same encoding of signatures, while every replacement sweep remains inside its own graph. Thus equal color names have a graph-independent meaning and no vertexwise correspondence is assumed. 

WL convention. Throughout, 1-WL denotes ordinary vertex color refinement: from the constant initial color c0(v), it updates 

ct+1 (v) = HASH ( ct(v), {{ ct(w) : w ∈ NG(v)}} ) .

For every k ≥ 2, k-WL denotes the oblivious k-dimensional WL algorithm customary in graph learning [12, 21]: it colors ordered k-tuples with separate per-coordinate replacement . We use “k-WL” without further qualification for this convention throughout. For a graph G, a tuple v =(v1, . . . , v k) ∈ V (G)k, and k ≥ 2, initialize c0(v) = HASH(atp G(v)) , where the atomic type records all equality and adjacency relations among the coordinates, and update by 

ct+1 (v) = HASH 



ct(v), ({{ ct(v[i ← w]) : w ∈ V (G)}} )ki=1 



,

where v[i ← w] denotes the tuple with its i-th coordinate replaced by w:

v[i ← w] = ( v1, . . . , v i−1, w, v i+1 , . . . , v k).

This procedure stores Θ( nk) tuple states. For k ≥ 3, the non-oblivious or folklore algorithm on ordered (k−1) -tuples, denoted (k−1) -FWL, has the same stable graph-distinguishing power as this separate-coordinate k-WL algorithm [12]. It uses simultaneous all-coordinate replacement and 

Θ( nk−1) tuple states. This is an arity-shift equivalence of graph distinguishability, not a round-by-round identification of tuple colors. We use this global-replacement form in Section 3.3. For either ordinary 1-WL or the above k-WL convention, we write 

G ≡k-WL H

when the stable graph-level signatures of G and H, computed with the shared encoding convention above, are equal. We write G̸ ≡k-WL H when they differ. Unless features are stated explicitly, all graphs are initially uncolored: 1-WL starts from one constant vertex color and k-WL for k ≥ 2 starts from atomic types. For two refinement procedures A, B, we write A ⪯ B when every graph pair separated by A is also separated by B, and A ≺ B when the containment is strict. Two low-dimensional conventions matter below. On finite simple graphs, 1-WL and 2-WL have the same graph-distinguishing power [12]; and substituting k = 1 into the tuple rule above would give a trivial global multiset, which is why ordinary 1-WL is defined separately. 

# 3 Implicit Line-Graph WL 

Since V (L(G)) = E(G), k-WL on L(G) can be executed over the root edges. We give its first-order, direct higher-order, and arity-shifted global-edge forms. 

3.1 ILG-1-WL 

ILG-1-WL is ordinary 1-WL on L(G): each root edge e starts from a common color (or its given edge label) and updates from {{ ct(f ) : f̸ = e, f ∩ e̸ = ∅}} . Replacing the hash by neural message, aggregation, and update maps gives ILG-1-GNN. 

> 3Expressive Power of Implicit Line-Graph WL

Lemma 1 (ILG-1-GNN is an MPNN on the line graph) . With shared initial features and message, update, aggregation, and readout maps, an ILG-1-GNN on G and an MPNN on L(G) agree at every layer and at graph readout. Proof. Their neighborhoods coincide; see Appendix D.1. 

3.2 ILG-k-WL 

For k ≥ 2, direct ILG-k-WL applies the k-WL update of Section 2 to ordered tuples in E(G)k, with atomic types computed from rG and replacements ranging over E(G).

Proposition 1 (ILG-k-WL equals k-WL on the line graph) . For every graph G and k ≥ 1, ILG-k-WL on G and k-WL on L(G) produce identical state partitions after each round, up to canonical renaming of colors. Proof. Under V (L(G)) = E(G), the domains, atomic types, and replacement ranges coincide; see Appendices D.2 and D.5. 

3.3 Global-Edge Form with Θ( mk−1) Tuple-State Storage 

Direct ILG-k-WL uses Θ( mk) tuple states and Θ( mk+1 ) replacement entries per round. Applying the standard arity shift to L(G) gives the same stable distinguishing power on Θ( mk−1) tuples [12]; its k = 3 instance underlies PPGN [20]. Global-edge ILG-k-WL is this (k−1) -FWL formulation on ordered root-edge tuples: for each g ∈ E(G), its message collects the k−1 coordinate-wise replacements ct(e[i ← g]) . Appendix D.6 gives the full recurrence. This form uses Θ( mk−1) tuple states and Θ( mk) replacement entries per round. Endpoint compar-isons evaluate rG(e, f ) in O(1) time from a Θ( m)-space m × 2 table, avoiding Θ( m + mL) stored line-graph adjacency without changing tuple-state or update asymptotics. Appendices D.10 and E give the details and measurements. 

Corollary 1 (Global-edge ILG-k-WL has k-WL graph-distinguishing power on L(G)). For every simple graph G and every k ≥ 3, global-edge ILG-k-WL has the same graph-level distinguishing power as k-WL on L(G), using Θ( mk−1) rather than Θ( mk) tuple states. Proof. The recurrence is exactly (k−1) -FWL on L(G) (Appendix D.6), and the arity shift [12] gives the claim. 

# 4 Expressivity Analysis 

ILG-k-WL is strictly weaker than root-domain k-WL for k = 1 , 2, but strictly stronger for k = 3 ,on Whitney-general graphs. At k = 1 , 2, some pairs separated on the roots are not separated on their line graphs, even when the roots have equal order. At k = 3 , joint pair refinement on the line graphs determines the stable ordered-pair coloring of the roots and separates some pairs that root-domain 3-WL does not. The backward result extends componentwise to the disconnected class in Definition 1. 

4.1 First-Order: Loss at k = 1 

The restriction to Whitney-general roots removes the classical K3, K 1,3 ambiguity. Any loss shown below therefore occurs even though the line graph determines its root up to isomorphism. 

Proposition 2 (1-WL equivalence transfers to line graphs) . For all simple graphs G, H (without restriction): 

G ≡1-WL H =⇒ L(G) ≡1-WL L(H).

Thus, ILG-1-WL can separate a pair only if root-domain 1-WL also separates it. Proof sketch. By the fractional-isomorphism/equitable-partition characterization [15, 24], G ≡1-WL 

H iff their stable 1-WL colorings have matching class sizes and between-class neighbor counts. The table of these counts is the equitable quotient. The induced edge coloring bcG(e) = {{ cG(u), c G(v)}} ,where cG is the stable vertex coloring, is an equitable partition of L(G) whose class sizes and  

> 4Expressive Power of Implicit Line-Graph WL 04627513
> G
> 40623517
> H
> 17 15 57 27 26 06 04 46 36 37
> L(G)
> 04 06 16 15 35 25 26 27 17 07
> L(H)

Figure 2: Equal-order backward failure at first order. Root 1-WL separates G, H by their degree multisets, but L(G), L (H) have the same stable equitable quotient with cells of sizes 2, 4, 4. Line vertices are labeled by their root edges; hollow and filled vertices have degrees two and four. equitable quotient are determined by the 1-WL data of G. Since these agree for G and H, the stable 

1-WL colorings of L(G) and L(H) coincide, giving L(G) ≡1-WL L(H). See Appendix D.3 for the full argument. 

Proposition 3 (Equal-order backward failure at first order) . There exist Whitney-general graphs 

G, H with L(G) ≡1-WL L(H) but G̸ ≡1-WL H.Proof. The pair and its line graphs are displayed in Figure 2; both graphs are Whitney-general, and their root degree multisets differ, so 1-WL separates G and H in its first round. In either line graph, degree refinement followed by one neighbor-color refinement stabilizes with cells of sizes 2, 4, 4

and the same equitable quotient A: (0 , 2, 0) , B: (1 , 1, 2) , C: (0 , 2, 2) , so L(G) ≡1-WL L(H). The computation is verified in Appendix D.4. 

Remark 1 (Why k = 1 and k = 2 fail) . Root order is not the only reason backward transfer fails: a line color records endpoint-degree sums and an equitable quotient of edge neighborhoods, but not how those colors are distributed among root stars. Joint pair refinement at k = 3 additionally records common-neighbor data for ordered edge pairs, from which Section 4.3 recovers the stable ordered-pair coloring of the root. 

Corollary 2 (ILG is strictly weaker for k = 1 , 2). On the Whitney-general class: ILG-1-WL ≺ 1-WL , ILG-2-WL ≺ 2-WL .

Proof. The first statement follows from Propositions 2 and 3. For the second, apply the equivalence of 1-WL and 2-WL [12] both to the roots and to their line graphs, and use Proposition 1. 

4.2 Strictness at k = 3 

An srg( v, d, λ, μ ) is d-regular on v vertices, with λ and μ common neighbors for adjacent and nonadjacent vertex pairs, respectively. On two strongly regular graphs with the same parameters, the initial pair types and all simultaneous-replacement counts depend only on equality and adjacency. Induction leaves the 2-FWL colors parameter-determined. The arity shift then makes co-parametric non-isomorphic strongly regular graphs standard examples not separated by 3-WL [12]. 

Corollary 3 (Universal four-clique counting) . For every finite simple graph G, one global pair-refinement round of ILG-3-WL determines the exact number cl 4(G) of four-vertex cliques. More precisely, 

cl 4(G) = 16

(e, f ) ∈ E(G)2 : e ∩ f = ∅, NL(G)(e) ∩ NL(G)(f ) = 4 .

Consequently, ILG-3-WL-equivalent finite simple graphs have the same four-clique count. Proof. The first pair color records initial nonadjacency and the common-neighbor multiplicity. The resulting factor-six double count is proved in Appendix B.1. 

Proposition 4 (Shrikhande/rook separation at k = 3 ). On the Whitney-general class, ILG-3-WL can separate graph pairs that 3-WL on the root graph cannot. 

> 5Expressive Power of Implicit Line-Graph WL

Proof. The Shrikhande and 4 × 4 rook graphs are Whitney-general, co-parametric srg(16 , 6, 2, 2) 

graphs, so their root 3-WL signatures agree. The Shrikhande graph has no K4, whereas the rook graph has eight. By Corollary 3, the corresponding ordered-pair color therefore has multiplicities 0

and 48 , so pair refinement separates their line graphs, as does 3-WL by the arity shift. Proposition 1 identifies the direct line-graph test with ILG-3-WL. 

4.3 Backward Transfer on Whitney-General Graphs at k = 3 

The preceding witness shows that ILG-3-WL can be stronger, but not that it preserves every distinction made by root 3-WL. The next theorem proves that it does on Whitney-general graphs. 

Theorem 1 (Backward transfer at 3-WL) . For Whitney-general graphs G, H ,

L(G) ≡3-WL L(H) = ⇒ G ≡3-WL H. 

Proof idea. Let BG be the unsigned vertex–edge incidence matrix. The identity B⊤ 

> G

BG = 2 I+AL(G)

shows that 2I + AL(G) is the Gram matrix of the root incidence vectors. At k = 3 , stable line-pair colors span a matrix algebra closed under multiplication. The Gram identity therefore lets us recover endpoint degrees and local endpoint profiles without choosing the shared endpoints globally. Stable 

1-WL records vertex color classes and their neighbor counts but provides no such coherent pair algebra, so the same reconstruction is unavailable below k = 3 . Appendix A gives the full argument in five stages. 1. Line algebra (§A.1). The indicator matrices of stable 2-FWL colors span a coherent matrix algebra. Line-graph equivalence induces an isomorphism between the two such algebras. 2. Local root data (§A.2). The line algebra recovers endpoint degrees, star profiles (incident-edge counts by stable line color), and dart profiles (relation counts at a specified endpoint). Small roots K1, K 2, the paw, and the diamond are treated separately, while K3, K 1,3 are excluded. 3. Unequal endpoint profiles (§A.3). A common ordering of the transported profile labels orients these edges in both roots. Their signed incidence matrices then define mixed Gram matrices that are mapped by the line-algebra isomorphism. 4. Equal endpoint profiles (§A.4). No canonical orientation is available. A cancellation identity shows that either endpoint choice acts identically on matrices in the recovered root space. This recovers the cells for equal and adjacent root-vertex pairs. 5. All distances (§A.5–A.6). Induction on root distance recovers the remaining pair cells, whose disjoint 0-1 supports give Hadamard closure and hence the stable root pair algebra. The arity shift [12] converts this 2-FWL statement into Theorem 1. 

Corollary 4 (Connected graphs and the Whitney exception) . Let G, H be connected finite simple graphs. If L(G) ≡3-WL L(H), then either G ≡3-WL H or {G, H } = {K3, K 1,3}.Proof. If neither root belongs to the Whitney pair, both are Whitney-general and Theorem 1 applies. If one root belongs to that pair, its line graph is K3. Stable 3-WL equivalence preserves the order and degree multiset of the line graph, so the other line graph is also K3; Whitney’s theorem leaves exactly the two stated roots. 

Theorem 2 (Backward transfer for disconnected graphs) . For componentwise Whitney-general graphs G, H ,

L(G) ≡3-WL L(H) = ⇒ G ≡3-WL H. 

Proof idea. Stable pair refinement matches the components of L(G) and L(H). Since the roots have no isolated vertices, these are exactly the line graphs of their root components, so Theorem 1 applies to each matched pair. Combining the component equivalences proves the claim; see Appendix A.7. 

Corollary 5 (Strict containment at 3-WL) . On both the Whitney-general and componentwise Whitney-general classes, 

3-WL ≺ ILG-3-WL .

> 6Expressive Power of Implicit Line-Graph WL

Proof. Theorems 1 and 2 give containment, and Proposition 4 makes it strict. 

Remark 2 (Comparison with root 4-WL) . Root 4-WL and ILG-3-WL are incomparable, even on connected Whitney-general simple graphs. Root 4-WL distinguishes the incidence graphs of two nonisomorphic Steiner triple systems of order 13 , whereas ILG-3-WL does not. Conversely, ILG-3-WL distinguishes a compressed parity pair over K5 that root 4-WL does not. Appendix B.2 gives both witnesses and their exact traces. 

# 5 Higher-Order Neural Architecture 

The global-edge recurrence defines a neural model on the same tuple domain. For k ≥ 3, one hidden state is stored for each ordered (k−1) -tuple of root edges. A message associated with replacement edge g jointly encodes the k − 1 states obtained by placing that same g in each coordinate. We display the first nontrivial case, k = 3 , whose states are ordered edge pairs. Let qe ∈ Rde be an edge feature. All qe are the same constant on an unlabeled graph. Finite vertex labels can instead be included in qe as the unordered pair of endpoint labels; isolated vertices are outside the line-domain representation. This uses labels, not unique vertex or edge identifiers. Let 

Emb r : {0, 1, 2} → Rdr embed the line-graph relation code; an injective one-hot map suffices for exact WL simulation. Let dh be the hidden-state width, so h(ℓ) 

> ef

∈ Rdh . Pair states start from 

h(0)  

> ef

= ϕ0 (qe, q f , Emb r (rG(e, f ))) , e, f ∈ E(G).

One layer computes 

M (ℓ) 

> ef

= X

> g∈E(G)

ψℓ



h(ℓ) 

> gf

, h (ℓ) 

> eg

, q g , Emb r (rG(g, e )) , Emb r (rG(g, f )) 



,

then updates 

h(ℓ+1)  

> ef

= ηℓ



h(ℓ) 

> ef

, M (ℓ)

> ef



.

After Tnet layers, a graph readout may combine pair and edge summaries: 

hG = READOUT 

 X 

> e,f ∈E(G)

γread (h(Tnet ) 

> ef

), X

> e∈E(G)

ξ(qe)

 .

Our implementations use multilayer perceptrons (MLPs) for the maps above: ϕ0 initializes pair states, 

ψℓ produces messages, ηℓ updates states, and γread , ξ feed the invariant readout. The corresponding WL color already contains qg and the relation codes, so the neural message receives no extra structural information. Vertex relabeling leaves the graph representation unchanged, and reordering the internal edge list only permutes tuple states (Appendix D.9). Appendix D.8 gives the general-k forward pass. 

Proposition 5 (Expressivity of global-edge ILG-k-GNN) . Fix k ≥ 3, a fixed number of rounds, and a graph class with bounded edge count and a finite alphabet of edge features. Finite vertex labels may be included when they are encoded invariantly in the edge features as above. There are choices of Emb r , ϕ 0, ψ ℓ, η ℓ, γ read , ξ , and READOUT for which the summed multiset encodings and final readout are injective on every finite domain encountered. With these choices, the GNN simulates the corresponding feature-labeled global-edge ILG-k-WL refinement. Conversely, if two feature-labeled graphs have identical refinement histories, no instance whose messages use only the stated tuple states, edge features, and relation codes can separate them through an invariant readout. Proof sketch. Bounded edge count and a finite feature alphabet allow only finitely many replacement entries. The multiset-encoding lemma of Xu et al. [27] therefore gives injective summed encodings, and induction over layers assigns each refinement color a distinct hidden state. Conversely, every hidden state is a function of its refinement color, so equal color histograms force equal invariant readouts. Appendix D.7 gives the full construction. 

> 7Expressive Power of Implicit Line-Graph WL

Table 1: Deterministic refinement results and neural separation counts on the evaluated datasets and graph pairs. BREC is split into four aggregate categories: Regular combines the Simple Regular, Strongly Regular, 4-Vertex Condition, and Distance Regular families; Extension and CFI combine their respective BREC subfamilies. Benchmark entries count separated pairs; the last three columns give one verdict for each substructure-counting witness pair. The PPGN BREC entry † is sourced from Wang and Zhang [25]. The ILG-3-GNN entry ‡ reports an untrained dense evaluation on the three substructure-counting witness pairs, all 105 SR25 pairs, and all 400 BREC main pairs. Neural rows use the protocols in Appendix F.                                                                            

> BREC pairs Substructure-counting witness pairs Method WL bound SR25 Basic Regular Extension CFI All C3C4K4
> Deterministic refinement
> ILG-1-WL —0/105 0/60 0/140 0/100 0/100 0/400 ✗✗✗
> 3-WL —0/105 60/60 50/140 100/100 60/100 270/400 ✓✓✗
> ILG-3-WL —105/105 60/60 139/140 100/100 60/100 359/400 ✓✓✓
> Neural architectures
> ILG-1-GNN ILG-1-WL 0/105 0/60 0/140 0/100 0/100 0/400 ✗✗✗
> Morris 3-GNN set 3-WL 0/105 60/60 50/140 97/100 0/100 207/400 ✓✓✗
> PPGN 3-WL 0/105 60/60 50/140 100/100 23/100 233/400 †✓✓✗
> ILG-3-GNN ‡ILG-3-WL 105/105 60/60 139/140 100/100 60/100 359/400 ✓✓✓

# 6 Expressivity Evaluation 

Evaluation objective. We report graph-pair separations by deterministic refinements and neural realizations, together with the relevant WL upper bounds. Table 1 covers the three substructure-counting witness pairs, all 105 SR25 pairs, and all 400 BREC pairs. 

6.1 Deterministic Refinement 

For each deterministic method computed in this study, two graphs are separated when their graph-level signatures differ. A pair is reported as not separated only after refinement stabilizes. The mathematical recurrence uses exact canonical signatures. Regression tests on a finite collection of graphs compare ILG with WL on explicitly constructed line graphs, checking the implementation against Proposition 1. SR25 contains 15 graphs with parameters srg(25 , 12 , 5, 6) , giving 105 unordered pairs [2]. BREC contains 400 specified non-isomorphic pairs whose main graphs are constructed to be 1-WL-indistinguishable [25]. Its CFI category is based on the Cai–Fürer–Immerman construction [6, 25]. The three substructure-counting witness pairs are C6 versus C3 ⊔ C3, C8 versus C4 ⊔ C4, and Shrikhande versus the 4 × 4 rook graph. The pairs differ in their numbers of triangles, four-cycles, and four-cliques, respectively. If a method does not separate one of these pairs, it cannot count that pattern on all graphs; separation alone does not prove that it can. For four-cliques, Corollary 3 supplies the stronger universal counting result. In the neural block, “set 3-WL” denotes the subset-based refinement associated with Morris 3-GNN, not the ordered-tuple 3-WL used elsewhere in the paper. The table’s WL bound column lists the deterministic refinement whose distinguishing power upper-bounds that of each neural architecture. A dash marks a deterministic method. On the substructure-counting witness pairs, 3-WL separates the triangle and four-cycle pairs but not Shrikhande/rook. Since the latter roots have different four-clique counts, their shared signature proves that 3-WL cannot count four-cliques in general. ILG-3-WL separates all three pairs. The same difference is universal for four-clique counts by Corollary 3. On SR25, root-domain 3-WL assigns one signature to all 15 graphs, whereas ILG-3-WL separates every pair. On BREC, ILG-3-WL improves over root-domain 3-WL only in the Regular aggregate. It separates 

139 /140 pairs there, broken down as 50 /50 Simple Regular, 50 /50 Strongly Regular, 20 /20 4-Vertex Condition, and 19 /20 Distance Regular; root-domain 3-WL separates 50 /140 . 3-WL already separates every pair in Basic and Extension, and both refinements separate 60 /100 CFI pairs. 

> 8Expressive Power of Implicit Line-Graph WL

The remaining 41 pairs comprise 20 CFI-3-WL pairs, 20 CFI-4-WL pairs, and one Distance Regular pair. On the tested CFI blocks, ILG-3-WL and root-domain 3-WL have identical pairwise outcomes. We make no claim about other CFI families. The only Distance Regular pair not separated is BREC pair 380 (zero-based); both refinements stabilize without distinguishing it. 

6.2 Neural Evaluation 

The ILG-1-GNN and Morris 3-GNN rows use pair-specific training and the evaluation protocols detailed in Appendix F. The PPGN category counts, totaling 233 pairs, are taken from Wang and Zhang [25, Table 2]. For ILG-3-GNN, we evaluate an untrained dense model separately on each of the three substructure-counting witness pairs, the 105 SR25 pairs, and the 400 BREC main pairs, using the same fixed parameters for both graphs in a pair. The model has constant uncolored edge features, invariant equality/incident/disjoint relation codes, width 32 , five layers, sine activations, and a multiscale readout. Each graph is evaluated in its stored ordering and under three relabelings. A pair is counted as separated only when its minimum cross-graph distance exceeds both within-graph diameters by the fixed numerical tolerance. Every separation uses all four copies; a failed pair may stop after two. The resulting verdicts are 3/3 on the substructure-counting witness pairs, 105 /105 on SR25, and 

359 /400 on BREC. Across all three suites, the neural pairwise verdicts coincide with deterministic ILG-3-WL. All SR25 pairs and all three substructure-counting witness pairs are 1-WL-indistinguishable. ILG-

1-GNN separates none of them, consistent with Lemma 1 and Proposition 2. Morris 3-GNN uses set-based refinement rather than the ordered-tuple 3-WL studied here. It separates 0/105 SR25 pairs and 207 /400 BREC pairs, separating the triangle and four-cycle pairs but not the four-clique pair. Appendix F specifies the trained and untrained protocols and their invariance checks. 

# 7 Discussion and Conclusion 

The measurements in Appendix E follow the complexity analysis: explicit and implicit line-domain refinement have the same pair-state and per-round update costs, while endpoint access avoids storing the line-graph structure. On SR25, root 4-WL is faster than ILG-3-WL in the shared implementation. This is a cost comparison, not an expressivity ordering: the two methods are incomparable, and ILG-3-WL distinguishes pairs that root 4-WL misses (Remark 2). On Whitney-general graphs, ILG-k-WL is strictly weaker than root-domain k-WL for k = 1 , 2, but strictly stronger for k = 3 ; the latter strict containment also holds on componentwise Whitney-general graphs. At k = 3 , ILG-3-WL counts four-cliques on every finite simple root graph (Corollary 3) and separates more pairs than root-domain 3-WL on SR25 and in BREC’s Regular aggregate. On the tested CFI blocks, the root and line-domain refinements have the same pairwise outcomes. The backward-transfer theorems and Shrikhande/rook pair prove 3-WL ≺ ILG -3-WL on both classes. The untrained dense ILG-3-GNN reproduces the deterministic ILG-3-WL verdicts on the substructure-counting witness pairs, SR25, and BREC. This comparison tests graph-isomorphism expressivity rather than supervised prediction. Our k = 1 result shows that ILG-1-WL is strictly weaker than root 1-WL on Whitney-general graphs. Other line-graph networks report improved task performance. This may arise because the line-graph representation makes useful structural information easier for the model to use, together with task-specific edge features, training, or model bias. Many standard graph-classification datasets contain few hard 1-WL collisions [29]. Since edges are the native line-graph objects, link-level tasks provide a natural setting for downstream evaluation [19]. Backward transfer for k ≥ 4 remains open. The k = 3 proof reconstructs the coherent matrix algebra of stable pair refinement; extending it would require a corresponding reconstruction of higher-arity root tuple colors and simultaneous endpoint choices. Repeated line-graph transformation may also simplify isomorphism testing with 3-WL: at most two transformations take every connected graph other than C3, C 4, C 5 outside the strongly regular class [28]. Determining whether this allows 3-WL to distinguish additional graph pairs is an interesting direction for future work. 

> 9Expressive Power of Implicit Line-Graph WL

# References 

[1] Miltiadis Allamanis, Marc Brockschmidt, and Mahmoud Khademi. Learning to represent programs with graphs. In International Conference on Learning Representations , 2018. URL 

https://openreview.net/forum?id=BJOFETxR-. 1 [2] Muhammet Balcilar, Pierre Héroux, Benoit Gaüzère, Pascal Vasseur, Sébastien Adam, and Paul Honeine. Breaking the limits of message passing graph neural networks. In Proceedings of the 38th International Conference on Machine Learning , volume 139 of Proceedings of Machine Learning Research , pages 599–608. PMLR, 2021. URL https://proceedings. mlr.press/v139/balcilar21a.html . 2, 8 [3] Beatrice Bevilacqua, Fabrizio Frasca, Derek Lim, Balasubramaniam Srinivasan, Chen Cai, Gopinath Balamurugan, Michael M. Bronstein, and Haggai Maron. Equivariant subgraph aggregation networks. In International Conference on Learning Representations , 2022. URL 

https://openreview.net/forum?id=dFbKQaRk15w . 1 [4] Cristian Bodnar, Fabrizio Frasca, Nina Otter, Yuguang Wang, Pietro Liò, Guido Montúfar, and Michael Bronstein. Weisfeiler and Lehman go cellular: CW networks. In Advances in Neural Information Processing Systems , volume 34, pages 2625–2640. Curran Associates, Inc., 2021. URL https://proceedings.neurips.cc/paper_files/paper/2021/hash/ 157792e4abb490f99dbd738483e0d2d4-Abstract.html . 1 [5] Cristian Bodnar, Fabrizio Frasca, Yuguang Wang, Nina Otter, Guido F. Montúfar, Pietro Liò, and Michael M. Bronstein. Weisfeiler and Lehman go topological: Message passing simplicial networks. In Proceedings of the 38th International Conference on Machine Learning , volume 139 of Proceedings of Machine Learning Research , pages 1026–1037. PMLR, 2021. URL 

https://proceedings.mlr.press/v139/bodnar21a.html . 1, 2 [6] Jin-Yi Cai, Martin Fürer, and Neil Immerman. An optimal lower bound on the number of variables for graph identification. Combinatorica , 12(4):389–410, 1992. doi: 10.1007/ BF01305232. 8 [7] Lei Cai, Jundong Li, Jie Wang, and Shuiwang Ji. Line graph neural networks for link prediction. 

IEEE Transactions on Pattern Analysis and Machine Intelligence , 44(9):5103–5113, 2022. doi: 10.1109/TPAMI.2021.3080635. 1 [8] Gang Chen and Ilia Ponomarenko. Lectures on coherent configurations. Online lecture notes, 2019. URL https://www.pdmi.ras.ru/~inp/ccNOTES.pdf . Updated edition. 28 [9] Zhengdao Chen, Lisha Li, and Joan Bruna. Supervised community detection with line graph neural networks. In International Conference on Learning Representations , 2019. URL 

https://openreview.net/forum?id=H1g0Z3A9Fm . 1 [10] Kamal Choudhary and Brian DeCost. Atomistic line graph neural network for improved materials property predictions. npj Computational Materials , 7:185, 2021. doi: 10.1038/ s41524-021-00650-1. 1 [11] Lin Du, Lu Bai, Jincheng Li, Lixin Cui, Hangyuan Du, Lichi Zhang, Yuting Chen, and Zhao Li. LGAN: An efficient high-order graph neural network via the line graph aggregation. 

Proceedings of the AAAI Conference on Artificial Intelligence , 40(25):20914–20922, 2026. doi: 10.1609/aaai.v40i25.39232. 1 [12] Jiarui Feng, Lecheng Kong, Hao Liu, Dacheng Tao, Fuhai Li, Muhan Zhang, and Yixin Chen. Extending the design space of graph neural networks by rethink-ing folklore Weisfeiler-Lehman. In Advances in Neural Information Processing Sys-tems , volume 36, pages 9029–9064. Curran Associates, Inc., 2023. doi: 10.52202/ 075280-0397. URL https://proceedings.neurips.cc/paper_files/paper/2023/ hash/1cac8326ce3fbe79171db9754211530c-Abstract-Conference.html . 3, 4, 5, 6, 12, 33 [13] Frank Fuhlbrück, Johannes Köbler, and Oleg Verbitsky. Identifiability of graphs with small color classes by the Weisfeiler–Leman algorithm. SIAM Journal on Discrete Mathematics , 35 (3):1792–1853, 2021. doi: 10.1137/20M1327550. 12, 13 [14] Justin Gilmer, Samuel S. Schoenholz, Patrick F. Riley, Oriol Vinyals, and George E. Dahl. Neural message passing for quantum chemistry. In Proceedings of the 34th International Conference on Machine Learning , volume 70 of Proceedings of Machine Learning Research , pages 1263–1272. PMLR, 2017. URL https://proceedings.mlr.press/v70/gilmer17a.html . 1 

> 10 Expressive Power of Implicit Line-Graph WL

[15] Martin Grohe. Descriptive Complexity, Canonisation, and Definable Graph Structure Theory ,volume 47 of Lecture Notes in Logic . Cambridge University Press, 2017. doi: 10.1017/ 9781139028868. 4, 32 [16] William L. Hamilton, Zhitao Ying, and Jure Leskovec. Inductive representation learning on large graphs. In Advances in Neural Information Processing Systems , volume 30, pages 1024–1034. Curran Associates, Inc., 2017. URL https://proceedings.neurips.cc/paper/2017/ hash/5dd9db5e033da9c6fb5ba83c7a7ebea9-Abstract.html . 1 [17] Xiaodong Jiang, Ronghang Zhu, Pengsheng Ji, and Sheng Li. Co-embedding of nodes and edges with graph neural networks. IEEE Transactions on Pattern Analysis and Machine Intelligence ,45(6):7075–7086, 2023. doi: 10.1109/TPAMI.2020.3029762. 1 [18] Elias Khalil, Hanjun Dai, Yuyu Zhang, Bistra Dilkina, and Le Song. Learn-ing combinatorial optimization algorithms over graphs. In Advances in Neu-ral Information Processing Systems , volume 30, pages 6348–6358. Curran Asso-ciates, Inc., 2017. URL https://proceedings.neurips.cc/paper/2017/hash/ d9896106ca98d3d05b8cbdf4fd8b13a1-Abstract.html . 1 [19] Veronica Lachi, Francesco Ferrini, Antonio Longa, Bruno Lepri, Andrea Passerini, and Manfred Jaeger. Bridging theory and practice in link representation with graph neural net-works. In Advances in Neural Information Processing Systems , volume 38, 2025. doi: 10.52202/085713-4090. URL https://proceedings.neurips.cc/paper_files/paper/ 2025/hash/b1cd72276feff8173462e3f733ac66f8-Abstract-Conference.html . 9 [20] Haggai Maron, Heli Ben-Hamu, Hadar Serviansky, and Yaron Lipman. Provably powerful graph networks. In Advances in Neural Information Processing Systems , volume 32, pages 2156–2167. Curran Associates, Inc., 2019. URL https://proceedings.neurips.cc/paper/2019/ hash/bb04af0f7ecaee4aae62035497da1387-Abstract.html . 1, 4, 39 [21] Christopher Morris, Martin Ritzert, Matthias Fey, William L. Hamilton, Jan Eric Lenssen, Gaurav Rattan, and Martin Grohe. Weisfeiler and Leman go neural: Higher-order graph neural networks. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 33, pages 4602–4609, 2019. doi: 10.1609/aaai.v33i01.33014602. URL https://ojs.aaai.org/ index.php/AAAI/article/view/4384 . 1, 3, 39 [22] Christopher Morris, Gaurav Rattan, and Petra Mutzel. Weisfeiler and Leman go sparse: Towards scalable higher-order graph embeddings. In Advances in Neural Information Processing Systems , volume 33, pages 21824–21840. Curran Associates, Inc., 2020. URL https://proceedings.neurips.cc/paper_files/paper/2020/hash/ f81dee42585b3814de199b2e88757f5c-Abstract.html . 1 [23] Ilia Ponomarenko. On the WL-dimension of circulant graphs of prime power order. Algebraic Combinatorics , 6(6):1469–1490, 2023. doi: 10.5802/alco.315. 12, 28 [24] Motakuri V. Ramana, Edward R. Scheinerman, and Daniel Ullman. Fractional isomorphism of graphs. Discrete Mathematics , 132(1–3):247–265, 1994. doi: 10.1016/0012-365X(94)90241-0. 4, 32 [25] Yanbo Wang and Muhan Zhang. An empirical study of realized GNN expressiveness. In Ruslan Salakhutdinov, Zico Kolter, Katherine Heller, Adrian Weller, Nuria Oliver, Jonathan Scarlett, and Felix Berkenkamp, editors, Proceedings of the 41st International Conference on Machine Learning , volume 235 of Proceedings of Machine Learning Research , pages 52134–52155. PMLR, 21–27 Jul 2024. URL https://proceedings.mlr.press/v235/wang24cl.html .8, 9, 39, 40 [26] Hassler Whitney. Congruent graphs and the connectivity of graphs. American Journal of Mathematics , 54(1):150–168, 1932. doi: 10.2307/2371086. 1, 2, 14 [27] Keyulu Xu, Weihua Hu, Jure Leskovec, and Stefanie Jegelka. How powerful are graph neural networks? In International Conference on Learning Representations , 2019. URL https: //openreview.net/forum?id=ryGs6iA5Km . 1, 7, 35 [28] Fan Yang and Xingyue Huang. Theoretical insights into line graph transformation on graph learning. In NeurIPS 2024 Workshop on Symmetry and Geometry in Neural Representations ,2024. URL https://neurips.cc/virtual/2024/101444 . Poster. 2, 9 [29] Markus Zopf. 1-WL expressiveness is (almost) all you need. In 2022 International Joint Conference on Neural Networks , 2022. 9 

> 11 Expressive Power of Implicit Line-Graph WL

# A Proof of the 3-WL Backward-Transfer Theorem 

This appendix first proves the connected Whitney-general core, Theorem 1, using joint pair refinement, and then derives the componentwise extension in Theorem 2. Throughout the core proof, G, H 

denote the two root graphs and L(G), L (H) their line graphs; we do not rename either line graph. “Pair refinement” means the joint ordered-pair update below, i.e., 2-FWL. By the standard arity-shift theorem, its stable graph-distinguishing power equals that of the paper’s separate-coordinate 3-WL [12]. The symbol ◦ denotes the Hadamard (entrywise) product. 

Proof outline. The proof proceeds in five steps. First, we characterize stable pair refinement by its coherent matrix algebra. Second, we determine root endpoint degrees and star and dart profiles from the stable pair algebra of L(G). Third, we use these profiles to construct root-side algebras and an isomorphism Ω induced by the line-algebra isomorphism. Fourth, we construct the root profile cells at distances zero and one, treating unequal and equal profile fibers separately. Fifth, we construct the remaining cells by induction on root distance. The resulting algebras are Hadamard closed, and Ω

then restricts to an isomorphism of the stable root pair algebras. 

A.1 Coherent-algebra characterization of pair refinement 

We first record the standard characterization of joint pair refinement by coherent configurations. Proposition 2.3 of Fuhlbrück et al. [13] identifies the stable joint-pair partition with the coherent closure of the graph relations, and their Lemma 2.4 proves that an algebraic isomorphism preserves every refinement round; see also Ponomarenko [23, Sections 2.2, 2.5, and 3.1]. Those references call the joint update on ordered pairs 2-WL. In the graph-learning terminology used here it is 2-FWL; its equivalence in stable graph-distinguishing power to separate-coordinate 3-WL is the separate arity-shift result [12]. We include the short derivation needed here so that the matrix identities and their index order are explicit. For a graph G, pair refinement assigns colors to ordered pairs by the joint-replacement update 

cGt+1 (x, y ) = HASH  cGt (x, y ),   cGt (z, y ), c Gt (x, z ) : z ∈ V (G)  .

The order of the two entry components agrees with the global-replacement formula in Section 3.3. Some references list the two entry coordinates in the reverse order; the fixed coordinate swap is a bijection on entries and does not change the induced refinement partition. The initial color cG 

> 0

(x, y )

records whether x = y, whether x and y are adjacent, or whether they are distinct and nonadjacent. Write cG 

> ∞

for the stable coloring, attained at the first round after which one further update no longer refines the induced partition of ordered pairs. Call two graphs pair-refinement equivalent when the stable graph-level signatures of their joint pair refinements, computed with the shared injective encoding of Section 2, are equal. By the standard arity-shift theorem, this is equivalent in stable graph-distinguishing power to G ≡3-WL H [12]. For a stable color ρ, define its 0-1 relation matrix by 

(RGρ )xy = 1[cG

> ∞

(x, y ) = ρ].

We use the same symbol RGρ for the matrix and for its support relation {(x, y ) : cG

> ∞

(x, y ) = ρ},writing (x, y ) ∈ RGρ accordingly. At stabilization, one more refinement round does not split a color class. Hence, if both (x, y ) and (x′, y ′) have stable color τ , their stable multisets of replacement entries are equal. The multiplicity of the entry (σ, ρ ) is therefore independent of the chosen pair of color τ . This defines the intersection number 

pτρσ = {z : ( x, z ) ∈ RGρ , (z, y ) ∈ RGσ } (( x, y ) ∈ RGτ ).

This representative-independence is the standard intersection-number axiom for a coherent configura-tion [23, Section 2.2, condition (C3)]; the argument above shows directly why stable pair refinement satisfies it. The reversed order (σ, ρ ) is exactly the entry order in the update: (z, y ) has color σ and 

(x, z ) has color ρ. Entrywise, for (x, y ) ∈ RGτ ,

(RGρ RGσ )xy = X 

> z∈V(G)

(RGρ )xz (RGσ )zy 

= {z : ( x, z ) ∈ RGρ , (z, y ) ∈ RGσ } = pτρσ .

Because the stable relations partition V (G)2, this entrywise identity is equivalent to 

RGρ RGσ = X

> τ

pτρσ RGτ .

> 12 Expressive Power of Implicit Line-Graph WL

The matrices RGρ have disjoint supports and sum to JV (G). The equality and adjacency relations are unions of stable classes because they are distinguished initially, so their real span AG contains 

IV (G) and AG, as well as JV (G). The displayed product formula proves closure under ordinary multiplication. Coordinate swap permutes the stable classes: this follows by induction from the symmetric initial types. If a⊤, b ⊤ denote the reversed colors at one round, then an entry (a, b )

becomes (b⊤, a ⊤) after reversing the outer pair. Thus (RGρ )⊤ is another basis relation; write ρ⊤ for its color, so (RGρ )⊤ = RGρ⊤ , and write |RGρ | for the number of ones in RGρ . Disjointness gives 

RGρ ◦ RGσ = 1[ρ = σ]RGρ .

Thus AG is closed under transpose and Hadamard multiplication as well. Call a real linear space of square matrices that contains the identity and all-ones matrices and is closed under transpose, ordinary multiplication, and Hadamard multiplication a coherent matrix algebra ; AG is the stable pair coherent algebra of G.

Lemma 2 (Stable-pair algebra characterization) . Graphs G, H are pair-refinement equivalent if and only if there is a bijection π between their stable pair colors such that the linear map 

φ(RGρ ) = RHπ(ρ)

maps IV (G), J V (G), A G to IV (H), J V (H), A H and preserves transpose, ordinary multiplication, Hadamard multiplication, and matrix trace. Proof. Run pair refinement on G and H in parallel with the shared injective color encoding fixed in Section 2. If the stable signatures agree, matching color names gives a bijection π and preserves every relation multiplicity |RGρ | = |RHπ(ρ)|. A stable color τ records its initial equality/adjacency type and the multiplicity of every replacement entry. The observation above therefore shows that π preserves every intersection number pτρσ , with the same statement for the transposed colors. In particular, 

π(ρ⊤) = π(ρ)⊤. Consequently the displayed linear map preserves ordinary multiplication and transpose. It preserves Hadamard multiplication by the disjoint-support identity above. Initial types give preservation of I, A , summing all basis matrices gives preservation of J, and matching relation sizes gives trace preservation: tr( RGρ ) = |RGρ | for an equality-type color and is zero otherwise. Hence trace is preserved on the whole span. Conversely, suppose such a basis bijection and linear map exist. Preservation of I, A , transpose, and multiplication preserves the initial types and all intersection numbers. More explicitly, uniqueness of expansion in the disjoint basis shows from φ(IV (G)) = IV (H) and φ(AG) = AH that π preserves equality and adjacency type; uniqueness of the expansion of RρRσ then shows that it preserves every 

pτρσ . To see explicitly why this recovers the refinement, observe inductively that each round-t color is a union of stable relations. For a stable relation RGτ , the multiplicity of a round-t entry (b, a ) is the sum of pτρσ over the stable relations RGρ contained in a and RGσ contained in b. Thus the transported unions have identical entry multisets at the next round. This is the same induction as in Fuhlbrück et al. [13, Lemma 2.4], with the two entry components reversed to match our convention. It remains to match graph-level color multiplicities. Trace preservation applied to RGρ (RGρ )⊤ gives 

|RGρ | = tr  RGρ (RGρ )⊤ = |RHπ(ρ)|.

Thus the matched stable colors have the same refinement data and the same multiplicities, so their stable graph-level signatures agree. This is the standard coherent-configuration characterization quoted above. Equivalently, AG is the smallest coherent matrix algebra containing IV (G), J V (G), A G [13, Proposi-tions 2.1 and 2.3]. We also use that a joint level set of finitely many matrices in AG is a union of basis relations. Its indicator belongs to AG by finite Hadamard interpolation. Explicitly, for a transported pair of matrices, take the union of the finite sets of entries attained on the two sides. For a requested value a in that union, choose the Lagrange polynomial that is one at a and zero at every other value in the union, and evaluate it entrywise using Hadamard products. If a is not attained on one side, the resulting equality indicator on that side is the zero matrix. A requested value outside the union is assigned the zero indicator on both sides. The Hadamard product of these single-matrix indicators is the desired joint-level-set indicator. Because the same polynomial is used on both sides, the entire construction is transported. This convention applies to every entrywise selector below. 

> 13 Expressive Power of Implicit Line-Graph WL

The paw is a triangle with one pendant edge, and the diamond is K4 with one edge removed. These names are used only for the two small local configurations isolated below. 

Lemma 3 (Exceptional roots handled directly) . Assume G, H are Whitney-general and L(G) ≡3-WL 

L(H). If either root is K1, K 2, the paw, or the diamond, then G ∼= H.Proof. On a diagonal pair (e, e ), the multiplicity of adjacency-colored replacement entries records 

dL(G)(e); hence pair refinement preserves line-graph order and degree multiplicities. Equivalently, the line degree on a diagonal fiber is the sum of the out-valencies of its outgoing adjacent basis relations, while the fiber size is its trace; all these quantities are transported. A connected root with an empty line graph is K1, and a connected root with a one-vertex line graph is K2. The line graph of the paw has degree multiset (3 , 3, 2, 2) : the two degree-three vertices are universal, so this uniquely determines K4 minus one edge. The line graph of the diamond has degree multiset (4 , 3, 3, 3, 3) : the degree-four vertex is universal and the other four vertices induce C4, so the line graph is uniquely 

K1 ∨ C4, where ∨ denotes graph join. In either case L(G) and L(H) are isomorphic. Neither line graph is K3, so Whitney’s theorem [26] gives G ∼= H.

A.2 Stable line data and root profiles 

For the remainder, let G, H be Whitney-general roots satisfying L(G) ≡3-WL L(H), and assume by Lemma 3 that both roots have order at least three and that neither is the paw or the diamond. These cases are separated from the general construction for the following reasons. For roots of orders one and two, the line algebra contains no adjacent basis relation. The paw and diamond are the additional Whitney-general roots for which, respectively, the degree collision (A.3) of Lemma 4 and the wedge ambiguity (A.4) of Lemma 6 occur. Lemma 3 treats all four cases directly and proves 

G ∼= H. Thus only the construction below excludes them. Let AL(G) have basis relations RL(G) 

> ρ

. Write DL(G) 

> α

for those basis relations supported on the coordinate diagonal; each is a diagonal 0-1 projector. Define the associated edge fiber 

EGα = {e ∈ E(G) : ( DL(G) 

> α

)ee = 1 }.

Define RL(H) 

> ρ

, D L(H) 

> α

, EHα analogously. We call RL(G) 

> ρ

adjacent when its support is contained in 

AL(G), and disjoint when its support consists of ordered pairs of disjoint root edges. If RL(G) 

> ρ

=

DL(G) 

> α

RL(G) 

> ρ

DL(G) 

> β

, write src( ρ) = α and tgt( ρ) = β; its out-valency is 

νρ = {f ∈ E Gβ : ( e, f ) ∈ RL(G) 

> ρ

} , e ∈ E Gα .

Every basis relation lies in a unique block EGα × E Gβ , so its source and target fibers are well defined. The intersection-number identities make νρ independent of e, and it is positive for a nonempty basis relation. Summing over the adjacent relations from Eα to Eδ gives the fiber valency qαδ : every e ∈ E α

has exactly qαδ line neighbors in Eδ . We refer to this constancy as equitability of the diagonal fibers; the numbers qαδ are sums of intersection numbers. Lemma 2 supplies 

φ : AL(G) −→ A L(H)

matching all stated algebra operations and trace, with φ(RL(G) 

> ρ

) = RL(H) 

> ρ

and φ(DL(G) 

> α

) = DL(H)

> α

after synchronizing the labels. From now on, unadorned Rρ, D α, Eα denote the L(G)-relation, 

L(G)-projector, and G-edge fiber, respectively. Their transported mates retain the explicit notation 

RL(H) 

> ρ

, D L(H) 

> α

, EHα . We call any such matched objects transported . Coordinate unit vectors are denoted by eα or eρ, according to the index set. For a matrix M , 1[M = a] denotes the entrywise 

0-1 matrix with (e, f )-entry 1[Mef = a]; entrywise inequalities such as 1[M > 0] are interpreted analogously. Finally, every automorphism of a root induces an automorphism of its line graph and, by induction on rounds, preserves each refinement color; stable colors, fibers, and all profiles defined below are therefore automorphism invariant. For a root vertex u, define its star profile 

s(u) = ( sα(u)) α, sα(u) = |{ e ∈ E α : u ∈ e}| .

> 14 Expressive Power of Implicit Line-Graph WL

Thus s(u) counts incident root edges by their stable diagonal color in L(G). A dart is an incident pair (u, e ) with u ∈ e. For e ∈ E α, define its dart profile 

d(u, e )ρ = |{ f̸ = e : u ∈ f, (e, f ) ∈ Rρ}| ,

indexed by all basis relations; it is supported on the adjacent relations leaving Eα, and only those coordinates appear below. It refines the u-wing of e, namely the other edges incident with u, by the stable relation from e to each such edge. Summing coordinates by target fiber recovers the star profile: 

sβ (u) = 1[α = β] + X  

> ρadjacent : src( ρ)= α, tgt( ρ)= β

d(u, e )ρ. (A.1) The indicator accounts for e itself, which is included in s(u) but excluded from d(u, e ).For adjacent root edges e = uv, f = uw , call u the center and v, w the outer vertices. The wedge is 

closed if vw is an edge, and open otherwise; in the closed case vw is its closing edge. For a root quantity q, call a transported relation label mixed if q is nonconstant on its support in either root or has different constant values on the two transported supports. Thus proving that a label is not mixed proves both within-root constancy and cross-root transport. For a root edge e = uv , put 

δu = dG(u) − 1, δv = dG(v) − 1, te = |NG(u) ∩ NG(v)|;

in comparisons between two representatives, d and N refer to the root containing the displayed representative. For f ∈ NL(G)(e), let λe(f ) = |NL(G)(e) ∩ NL(G)(f )|, and define 

LD G(e) =  λe(f ) : f ∈ NL(G)(e) .

For integers 0 ≤ δu ≤ δv and 0 ≤ t ≤ δu, define the multiset 

F (δu, δ v , t ) = [ δu]t ⊎ [δu − 1] δu−t ⊎ [δv ]t ⊎ [δv − 1] δv −t, (A.2) where ⊎ is multiset union and [a]b denotes b copies of a.

Lemma 4 (Line-degree multisets and their collisions) . The diagonal pair color of a root edge 

e determines LD G(e), and the multiset is transported. Moreover, LD G(e) = F (δu, δ v , t e) with 

δu ≤ δv , and F determines its three arguments except for the single collision 

F (0 , 2, 0) = F (1 , 1, 1) = [1 , 1] . (A.3) 

Proof. The diagonal pair color of e determines the multiset. Indeed, A2 

> L(G)

is in the coherent algebra, so it has a constant value ℓρ on every adjacent basis relation Rρ. If e ∈ E α, then 

LD G(e) = ] 

> ρadjacent src( ρ)= α

[ℓρ]νρ .

Both ℓρ and the out-valency νρ are intersection-number data, so this multiset is also transported. Listing the two endpoint wings gives LD G(e) = F (δu, δ v , t e): each wing at an endpoint of degree d

contributes one half of (A.2), with te of its edges closed by the opposite endpoint. For the uniqueness claim, |F | = δu + δv ; for nonempty F , max F = δv − 1 when te = 0 and δv when te > 0. If two candidates have the same te = 0 status, these data recover δu, δ v ; for te > 0, the identity 

X F = δu(δu − 1) + δv (δv − 1) + 2 te

recovers te. A collision across the two statuses must compare a positive-t candidate (a, b, t ) with 

(a − 1, b + 1 , 0) : equality of cardinalities and maxima gives these two shifts. The zero-t multiset contains b + 1 copies of its maximum b, which forces a = b; otherwise the positive candidate has at most a < b such copies. With a = b, the zero-t candidate also contains a − 1 copies of a − 2, absent from the positive candidate. Hence a = 1 , and equality of the remaining multiplicities forces t = 1 .This is exactly (A.3). The empty case is immediate. 

Lemma 5 (Endpoint-degree recovery) . Every diagonal fiber of AL(G) fixes its unordered endpoint degrees, and these labels are transported by φ.

> 15 Expressive Power of Implicit Line-Graph WL

Proof. By Lemma 4, only the collision (A.3) could make a transported diagonal fiber mixed in its unordered endpoint degrees. In root language, (A.3) confuses a pendant (1 , 3) -edge with a triangular 

(2 , 2) -edge. Suppose two representatives of a transported diagonal fiber Eα, either in one root or one in each root, realize the two cases. Write the pendant edge as e = pc , with the other edges at the degree-three vertex c equal to f1 = ca, f 2 = cb . Write the triangular edge as e′ = uv in uvw , with 

f ′ 

> 1

= uw, f ′ 

> 2

= vw . The degree-two vertices u, v are adjacent true twins, so swapping them is a root automorphism; it shows that f ′

> 1

, f ′ 

> 2

have one diagonal color β. The α-edge e′ has exactly these two line neighbors. Equitability of the diagonal α-fiber therefore makes the two line neighbors f1, f 2 of e

both β-colored. If d(w) = 2 , then f ′ 

> 1

is again triangular of type (2 , 2) , whereas f1, having a degree-three endpoint, must be pendant by (A.3). Thus d(a) = d(b) = 1 , and connectedness gives K1,3 on the pendant side and K3 on the triangular side. If d(w) > 2, uniqueness of F for the matched β-edges transports both their endpoint degrees and their value te = 1 . Explicitly, {2, d (w)} = {3, d (a)}, so d(w) = 3, d (a) = 2 , and the common-neighbor count gives one common neighbor of c, a , necessarily b.Thus ab is an edge, symmetrically d(b) = 2 , and p, c, a, b form a paw and exhaust their degrees. For 

g = ab , the line vertices e, g have the same open neighborhood, namely {f1, f 2}. Swapping these line-graph twins is an automorphism, so g also has color α. Thus f1 has two α-neighbors. Equitability at the matched β-vertices f1, f ′ 

> 1

forces f ′ 

> 1

to have two as well. Because the β-fiber has endpoint type {2, 3}, it differs from α; hence f ′ 

> 2

is not the second one, and the third edge h = wr at w has color α. Since d(w) = 3 , (A.3) makes h pendant, and the other root is also a paw. In the within-root comparison, the same exhausted configurations are components. Thus the only propagated collisions are the Whitney pair and the paw, and both are excluded by the standing hypotheses. 

Lemma 6 (Central degree and wedge type) . Every adjacent basis relation fixes its central degree and whether its wedges are open or closed, and these labels are transported by φ.Proof. Take an adjacent pair e = uv, f = uw . Its coherent common-neighbor number is 

(A2

> L(G)

)ef = d(u) − 2 + 1[vw ∈ E(G)] . (A.4) If a relation were mixed in its central degree, (A.4) would force a closed wedge of degree c and an open wedge of degree c + 1 . Lemma 5 fixes the unordered endpoint degrees of each source and target fiber. For either leg, equality of those unordered pairs reads {c, d (closed outer )} = {c + 1 , d (open outer )}.Thus the closed outer endpoint has degree c + 1 and the open outer endpoint degree c; all four wedge legs have endpoint type {c, c + 1 }.Let Πhh be the sum of the diagonal projectors of endpoint type {c + 1 , c + 1 }, and set 

M = AL(G)Πhh , Q = M M ⊤.

Here Qef counts the high–high edges adjacent in L(G) to both e and f . In the closed realization the only such common neighbor is the closing high–high edge, so Qef = 1 . In the open realization the center is the common high endpoint of e, f , so every high–high edge adjacent to e is also adjacent to f , and Qef = Qee . Constancy on the common basis relation first equates this open value to the closed value one; constancy and transport on the source and target diagonal fibers then equate the corresponding diagonal values. Since Qee counts high–high edges at the high endpoint of e,every involved degree-(c + 1) endpoint consequently has exactly one high-degree neighbor. Let 

U = 1[Q = 1] and put T = AL(G) ◦ U . The two realizations now have the following data: center degree outer degrees (T 2)ef 

open c + 1 c, c c − 2

closed c c + 1 , c + 1 0

Indeed, in the open realization the unique high–high edge at the center is not a T -neighbor of itself, whereas each of the other c − 2 center edges is a common T -neighbor of e, f . In the closed realization the center is low, and each high outer endpoint has only the closing high–high edge; the closing edge itself is not a T -neighbor, so there is no common T -neighbor. Constancy of T 2 on the common relation gives c − 2 = 0 , hence c = 2 . A source edge in the closed realization then has two 

T -neighbors: the other wedge leg and the remaining edge at its degree-three outer endpoint. In the open realization it has the other wedge leg, plus a second T -neighbor exactly when its degree-two outer endpoint is adjacent to the center’s unique high-degree neighbor. Since (T 2)ee is the T -degree 

> 16 Expressive Power of Implicit Line-Graph WL

of e, equality of the corresponding source diagonal entries forces this edge, and the target calculation forces its analogue at the other outer vertex. The four vertices form a diamond and exhaust their degrees, so connectedness makes the root the diamond, contrary to the hypothesis. For an edge fiber Eα, collect the fiber valencies into 

qα = ( qαβ )β + 2 eα.

The two endpoint wings of every α-edge uv , together with the edge itself once at each endpoint, give 

s(u) + s(v) = qα. (A.5) For an adjacent basis relation Rγ with source Eα, target Eβ , and representative e = uv , f = uw ,define the vectors 

h⋆γ (δ) = ( AL(G)Dδ AL(G))ef , hd 

> γ

(ρ) = ( RρAL(G))ef ,

indexed by the diagonal fibers δ and by the adjacent basis relations ρ, respectively. The first counts the common line neighbors of e, f lying in Eδ ; the second counts the common line neighbors g with 

(e, g ) ∈ Rρ. Both are sums of intersection numbers, hence constant on Rγ and transported. 

Profile-difference observation. We use the following observation twice. For a closed adjacent relation Rγ , suppose each representative has a profile of the form vγ − eℓ, where the vector vγ is transported and depends only on γ, and the label ℓ is carried by the closing edge of the representative. If such a relation is mixed between profiles x̸ = y whose representatives carry closing labels ℓx and 

ℓy, subtraction gives 

y − x = eℓx − eℓy ,

so ℓx̸ = ℓy, and the two profiles differ, by one, in exactly the two coordinates ℓx, ℓ y. If moreover the fixed unordered endpoint-profile pair of the source fiber is transported and contains the central profile of every representative, then that pair equals {x, y} in each root, and x, y are the only profiles realized on Rγ .

Lemma 7 (Endpoint star profiles) . Every diagonal fiber of AL(G) fixes its unordered endpoint star profiles, and every open adjacent basis relation fixes its central star profile. These labels are transported by φ.Proof. Let Rγ be an open adjacent relation with source fiber Eα and target fiber Eβ . The common line neighbors of an open wedge e = uv, f = uw are exactly the other edges at u, so 

s(u) = eα + eβ + h⋆γ . (A.6) Every nonempty relation has positive constant out-valency on its source fiber. Thus, if an open relation leaves Eα, every α-edge is the source of a pair in that relation; (A.6) supplies one endpoint profile and (A.5) supplies the other. If none leaves Eα, then for each α-edge uv , every neighbor of either endpoint other than the other endpoint is adjacent to both. Here write N [x] = N (x) ∪ { x}

for the closed neighborhood. Swapping the adjacent true twins u, v , whose closed neighborhoods coincide, is a root automorphism; its induced line-graph automorphism fixes e and interchanges its two wings. Stable colors are automorphism invariant, so the profiles agree coordinatewise and equal qα/2. This proves the endpoint-profile assertion, while (A.6) proves the open central-profile assertion. 

Lemma 8 (Central star profiles and class sizes) . Every adjacent basis relation fixes its central star profile, and every star-profile class size is determined and transported. Proof. By Lemma 7, only a closed relation Rγ , with source Eα and target Eβ , could be mixed between central profiles x̸ = y. If its x-centered representative has closing-edge fiber χ, direct listing gives 

h⋆γ = x − eα − eβ + eχ,

so the central profile is (eα +eβ +h⋆γ )−eχ, of the form required by the profile-difference observation with the closing-edge fiber as label. With ξ the closing fiber of a y-centered representative, the principle gives 

y − x = eχ − eξ , (A.7) 

> 17 Expressive Power of Implicit Line-Graph WL

and, since Lemma 7 fixes and transports the unordered endpoint pair of the source and target fibers, that pair equals {x, y} and no other central profile occurs on Rγ . For K ∈ { G, H }, let 

CK 

> x

= {u ∈ V (K) : sK (u) = x}, CK 

> y

= {u ∈ V (K) : sK (u) = y},

where sK denotes the star profile in K. We suppress the root superscript inside a one-root calculation. Since both central labels occur in the source and target fibers, Lemma 7 makes every source or target edge of γ join Cx to Cy. In the x-centered realization the χ-edge joins the two outer Cy-vertices, so the fixed endpoint pair of fiber χ is {y, y}. Since x̸ = y, (A.7) gives ξ̸ = χ; moreover xχ = 0 ,and its χ-coordinate gives yχ = 1 . Consequently (xχ, y χ) = (0 , 1) . Symmetrically the ξ-fiber joins 

Cx to itself and (xξ , y ξ ) = (1 , 0) . Hence the ξ- and χ-edges form perfect matchings on Cx and Cy,respectively. Take a x-centered representative e = uv ∈ E α, f = uw ∈ E β , closed by vw ∈ E χ, and let u¯u ∈ E ξ

be the matching edge at u. For an x-centered γ-pair, this matching edge is its unique common ξ-edge; for a y-centered pair, the closing edge is the unique common ξ-edge. In the latter realization, let 

Rη , R θ be the two relations through that witness. Their middle fiber is the ξ-fiber, so every (η, θ )-intermediate is a common ξ-edge. Therefore pγηθ = 1 , while every other decomposition through the ξ-fiber has multiplicity zero. Transport makes u¯u the unique witness with (e, u ¯u) ∈ Rη and 

(u¯u, f ) ∈ Rθ. In the y-centered realization the witness is the closing edge, so both leg relations are closed. Wedge-type transport (Lemma 6) now supplies ¯uv, ¯uw , producing a K4 with opposite matching-edge fibers ξ, χ .For every edge fiber δ / ∈ { ξ, χ }, (A.7) gives xδ = yδ . If this common coordinate is positive, the fixed endpoint pair of δ contains both profiles and therefore is {x, y}; if it is zero, no δ-edge meets either class. Together with the matchings, this shows that Cx ∪ Cy has no edge to a third profile class. Connectedness yields V (G) = CG 

> x

˙∪ CG 

> y

, and analogously for H. After deleting the two internal matchings, the remaining bipartite graph is κ-regular, where κ = P 

> δ / ∈{ ξ,χ }

xδ = P 

> δ / ∈{ ξ,χ }

yδ .Let Dξ be the diagonal projector onto the ξ-matching edges and define 

Mξ = AL(G)Dξ AL(G), Wξ = AL(G) ◦ Mξ .

Since Dξ and AL(G) are transported, so are Mξ , W ξ , and W 2 

> ξ

, and all are constant on basis relations. For an adjacent pair, a common ξ-edge is either the unique matching edge at an x-center or the closing matching edge at an y-center; simplicity prevents both alternatives from occurring at once. Hence Wξ is 0-1. The Wξ -subgraph indexed by a fixed ξ-edge z ¯z has as its vertices the cross edges incident with z or ¯z. Two are Wξ -adjacent exactly when they share the same x-endpoint, or when they share a y-endpoint across the ξ-matching. Thus it consists of two κ-cliques, together with the possibly partial cross-matching zy ↔ ¯zy . Two distinct vertices in one clique have exactly the other κ − 2 clique vertices as common Wξ -neighbors, whereas a cross-match pair has no common 

Wξ -neighbor. Therefore 

(W 2 

> ξ

)ef =

κ − 2, e̸ = f lie in the same clique ,

0, e, f form a cross-match pair . (A.8) The preceding construction makes the x-centered pair a same-clique pair and the y-centered pair a cross match. Here κ ≥ 2. If κ = 2 , every vertex of the resulting K4 has no additional incident edge, so connectedness implies that the root is K4. Edge transitivity of K4 then puts all its edges in one diagonal line-algebra fiber, so every vertex has the same star profile and x = y. Thus κ ≥ 3,and the values in (A.8) differ, contradicting constancy or transport of W 2 

> ξ

on Rγ . This proves the central-profile assertion; it remains to compute the class sizes. On the G-side, for an incident fiber Eα,

|CG 

> x

|xα =

|E α|, Eα joins x to a distinct profile ,

2|E α|, Eα joins x to itself .

Every star-profile class has such a fiber with xα > 0, because the connected nontrivial roots under consideration have no isolated vertices. Since |E α| = tr( Dα) is transported, these formulas recover and transport every profile-class size. 

Lemma 9 (Endpoint dart profiles) . Every diagonal fiber of AL(G) fixes its unordered pair of endpoint dart profiles, and every open adjacent relation Rγ fixes its source dart, namely d(e ∩ f, e ) = eγ + hd 

> γ

.These labels are transported by φ.

> 18 Expressive Power of Implicit Line-Graph WL

Proof. For a source fiber Eα, every line neighbor of e = uv shares exactly one endpoint with e, so the two wings split the out-valencies: 

d(u, e ) + d(v, e ) = ( νρ)ρ (e = uv ∈ E α). (A.9) For an open relation γ, the common line neighbors g of the wedge e = uv, f = uw with (e, g ) ∈ Rρ

are exactly the ρ-colored edges of the u-wing other than f , so the open-star listing gives 

d(e ∩ f, e ) = eγ + hd 

> γ

. (A.10) If an open relation leaves Eα, (A.10) fixes one endpoint dart, and (A.9) fixes the other. If no open relation leaves, the true-twin automorphism of Lemma 7 swaps the two wings and makes the profiles equal. This proves the diagonal assertion, and (A.10) is the open source-dart assertion. 

Lemma 10 (Source and target dart profiles) . For a representative e = uv, f = uw of an adjacent basis relation, the relation fixes the source dart d(u, e ) and the target dart d(u, f ), and these labels are transported by φ.Proof. Consider the source dart; the target statement follows by transpose. If the two endpoint star profiles of its source fiber differ, Lemmas 7 and 8 identify the central wing, and the fixed unordered dart pair, together with (A.1), identifies its dart profile: under (A.1), the two dart profiles recover the two distinct endpoint star profiles, so the fixed central star profile selects the correct dart. For equal endpoint star profiles, no adjacent relation can be centered at a degree-one endpoint. At degree two, each dart has one other incident edge, so relation γ fixes the unit dart profile eγ . An open relation at any degree is fixed by (A.10). Suppose that a closed relation γ, centered at a root vertex of degree d ≥ 3, is mixed between source-dart profiles x̸ = y. Interchanging G, H if necessary, take the x-centered representative in 

G; the y-centered representative may lie in G or in H. In the x-centered representative, let ρ be the relation of the first closing leg (e, g ), where g is the closing edge. Direct listing gives 

d(u, e ) = eγ + hd 

> γ

− eρ,

of the form required by the profile-difference observation, with the closing-leg relation as label. With 

σ the closing-leg relation of a y-centered representative, the principle gives 

y − x = eρ − eσ , (A.11) and, since Lemma 9 fixes and transports the unordered dart pair of the source fiber, that pair equals 

{x, y} and these are the only source-dart profiles on Rγ ; in particular ρ̸ = σ. Write 

(xρ, y ρ) = ( a, a + 1) , (xσ , y σ ) = ( b + 1 , b ). (A.12) We first prove a, b > 0. If a = 0 , then (A.9) gives the out-valency νρ = xρ + yρ = 1 . Thus Rρ has exactly one target from every source edge. Write fρ(e) for this target and form 

ZL(G) 

> ρ

= (( RρAL(G)) ◦ AL(G))AL(G), ZL(H) 

> ρ

= φ(ZL(G) 

> ρ

).

Functionality also forces ρ̸ = γ: in the x-centered closed realization, otherwise the other wedge leg and the closing edge would be two distinct Rρ-targets of e. Hence in the y-centered realization the unique Rρ-target is a third edge at the center. On either side K ∈ { G, H }, functionality makes (ZL(K) 

> ρ

)ef count line vertices adjacent simultaneously to e, f, f ρ(e). It is zero when fρ(e)

is the closing edge and d − 3 when fρ(e) is a third edge at the center. Indeed, functionality gives 

(RL(K) 

> ρ

AL(K))eg = AL(K)(fρ(e), g ); the Hadamard product restricts g to a common neighbor of 

e and fρ(e), and the final multiplication by AL(K) additionally requires g ∼ f . For d ≥ 4 this contradicts constancy or transport on Rγ . If d = 3 , distinguish the two representatives as follows. In the star realization write e = uv, f = uw, f ρ(e) = uz . Write the closing representative as 

ˆe = ˆ uˆv, ˆf = ˆ u ˆw, with closing edge ˆg = ˆ v ˆw; this hatted realization is the x-centered one in G,while the star realization lies on the side containing the y-centered representative. Then (ˆ e, ˆg) ∈ Rρ;define Rθ by (ˆ g, ˆf ) ∈ Rθ . Functionality gives pγρθ = 1 , and transport forces (uz, f ) ∈ Rθ in the star realization. Both pairs through the witness are closed in the closing realization. Wedge-type transport (Lemma 6) therefore makes the corresponding wedges in the star realization closed: they supply 

vz, wz , while the original closed γ-wedge supplies vw .

> 19 Expressive Power of Implicit Line-Graph WL

In the star realization, Rγ , R ρ, R θ are centered at the cubic vertex u. In the closing realization these three relations are centered at ˆu, ˆv, ˆw, respectively. Central-degree transport (Lemma 6) therefore makes all three vertices cubic. The target edge ˆg of Rρ and the target edge ˆf of Rθ consequently have endpoint-degree pair {3, 3}; transporting their diagonal-fiber labels back gives d(z) = d(w) = 3 .Finally, the equal endpoint star profiles of the source fiber give d(v) = d(u) = 3 , since degree is the sum of the star-profile coordinates. Hence the displayed K4 is cubic and, by connectedness, is the whole root. Its darts are automorphic, contradicting x̸ = y. Therefore a > 0; the symmetric argument gives b > 0.Every mixed relation is closed by (A.10). By (A.9), the source fiber’s fixed dart pair {x, y} has total valency vector x + y. Thus the closing ρ-leg opposite an x-dart is centered at the y-dart, while 

xρ = a > 0 gives a ρ-relation centered at an x-dart; hence ρ is mixed, and symmetrically so is σ. For 

λ ∈ { ρ, σ }, let rx and ry be its closing-leg labels in its x- and y-centered realizations. Applying the listing that produced (A.11) to Rλ gives 

y − x = erx − ery .

Comparison with (A.11) forces rx = ρ and ry = σ.Fix e = uv , with dart profiles x at u and y at v. If a ρ- or σ-neighbor in the u-wing is f = uz ,the closing rule supplies the edge vz , which is a ρ-neighbor in the v-wing. The two source sets are disjoint because Rρ̸ = Rσ , and simplicity makes the map uz 7 → vz injective. Therefore 

xρ + xσ ≤ yρ.

By (A.12), this is a + ( b + 1) ≤ a + 1 , contradicting b > 0.

A.3 The profile-oriented ordinary algebra 

Let CG and CH be the recovered star-profile classes of G and H, with the transport bijection written 

C 7 → CH . Let BG ∈ { 0, 1}V (G)×E(G) and BH ∈ { 0, 1}V (H)×E(H) be the unsigned incidence matrices, so BG(u, e ) = 1[u ∈ e], and similarly for H. For C ∈ CG, define the diagonal 0-1

projectors 

(P GC )uu ′ = 1[u = u′ ∈ C], (P HCH )u′v′ = 1[u′ = v′ ∈ CH ].

For a diagonal edge fiber Eα, let {d+ 

> α

, d− 

> α

} be its unordered endpoint dart-profile pair. If the two profiles differ, choose the same ordering of their transported profile labels on G and H, and orient every α-edge toward its +-dart. Let eBG,α ∈ {− 1, 0, 1}V (G)×E(G) be the signed incidence matrix with entry +1 at the +-dart endpoint, entry −1 at the other endpoint, and zero off that fiber; define 

eBH,α analogously. This ordering serves only to synchronize the transported labels; equal-dart-profile fibers are not oriented. Put 

IG = {BG} ∪ { eBG,α : d+

> α

̸ = d− 

> α

}, IH = {BH } ∪ { eBH,α : d+

> α

̸ = d− 

> α

}.

For each SG ∈ I G, write SH ∈ I H for the incidence matrix with the same transported label. The next lemma relates the root profile projectors to the stable line algebra. Placing a profile-class projector between two incidence matrices produces an edge-indexed Gram matrix that belongs to the stable line algebra and is transported. 

Lemma 11 (Mixed Grams) . For SG, T G ∈ I G and C ∈ CG,

S⊤ 

> G

P GC TG ∈ A L(G),

and 

φ(S⊤ 

> G

P GC TG) = S⊤ 

> H

P HCH TH .

Proof. The (e, f )-entry is X 

> u∈e∩f∩C

SG(u, e ) TG(u, f ). (A.13) It is zero for disjoint edges; the source and target fibers also determine whether either signed incidence is zero. On an adjacent basis relation, Lemmas 8 and 10 fix the central star profile and, whenever a signed incidence occurs, its source or target sign. On a diagonal relation, the unordered endpoint dart profiles (Lemma 9) fix both star memberships through (A.1). A signed factor can then be nonzero only for the fiber containing e = f . If both factors are unsigned or both are signed, (A.13) counts the 

> 20 Expressive Power of Implicit Line-Graph WL

endpoints in C. With exactly one signed factor it is the signed C-incidence; this is zero when both endpoints have the same star profile, and otherwise its sign is fixed by the synchronized +/− dart labels. Thus (A.13) is constant on every basis relation of AL(G) and depends only on transported labels. Consequently 

S⊤ 

> G

P GC TG = X

> τ

mτ Rτ

with the same coefficients mτ after transport to H, proving both claims. Define the root-side space 

WG = span  {P GC : C ∈ CG} ∪ { P GC SGM T ⊤ 

> G

P GD : C, D ∈ CG, S G, T G ∈ I G, M ∈ A L(G)} .

(A.14) Define WH by the same formula with the transported H-side objects. Here M is indexed by 

E(G) × E(G), so each incidence sandwich is a V (G) × V (G) root matrix. The next lemma makes 

WG an ordinary matrix algebra; Hadamard closure is proved only after its value cells are constructed. We call the matrices P GC projector atoms and the displayed incidence sandwiches incidence atoms .

Lemma 12 (Root-algebra transport and compression) . The space WG is a unital real algebra—that is, it contains the identity matrix and is closed under real linear combinations and ordinary matrix multiplication—and is closed under transpose. The atomwise prescriptions 

P GC 7 −→ P HCH ,P GC SGM T ⊤ 

> G

P GD 7 −→ P HCH SH φ(M )T ⊤ 

> H

P HDH

(A.15) 

extend to a well-defined, unital, transpose- and trace-preserving algebra isomorphism 

Ω : WG −→ W H .

For every Z ∈ W G and UG, V G ∈ I G,

U ⊤ 

> G

ZV G ∈ A L(G) (A.16) 

compatibly with Ω, φ . Moreover, IV (G), J V (G), A G belong to WG and are transported to 

IV (H), J V (H), A H .Proof. Algebra structure. The identity is P 

> C∈CG

P GC , and transposing an atom exchanges its two outer profile blocks and incidence matrices and replaces M by M ⊤. Multiplication by a projector atom either annihilates a mismatched outer block or leaves the matching block unchanged, so only products of two incidence atoms require calculation. Products with unequal middle projectors vanish; the remaining products reduce as 

(P GC SGM T ⊤ 

> G

P GD )( P GD UGQV ⊤ 

> G

P GF ) = P GC SG

 M (T ⊤ 

> G

P GD UG)QV ⊤ 

> G

P GF .

The middle factor belongs to AL(G) by Lemma 11. Thus WG is a unital transpose-closed algebra, and (A.15) is formally multiplicative and transpose preserving. 

Well-definedness. To check independence of the chosen expression, first note that a product whose profile-block indices do not close has trace zero on both sides. For a cyclic product of incidence atoms, with Cr = C0, cyclicity of trace gives the explicit contraction 

tr 

> r

Y

> i=1

P GCi−1 SiMiT ⊤ 

> i

P GCi

!

= tr  M1(T ⊤ 

> 1

P GC1 S2)M2 · · · Mr (T ⊤ 

> r

P GC0 S1) ,

where indices are cyclic. Each parenthesized factor is a transported mixed Gram, so the right-hand side is a transported trace in AL(G). Projector-only words have trace zero or a transported class size, and any other projector factors are absorbed into adjacent profile blocks. By linearity, all trace words in the spanning atoms are therefore preserved. If a formal linear combination Z represents zero and 

eZ is its formal image under (A.15), then 

tr( eZ⊤ eZ) = tr( Z⊤Z) = 0 .

> 21

Expressive Power of Implicit Line-Graph WL 

The left side is the squared real Frobenius norm of eZ, so eZ = 0 . The prescription is therefore independent of the chosen representation. The same construction with φ−1 is inverse on every spanning atom, so Ω is bijective. 

Compression. Compressing an incidence atom gives 

U ⊤ 

> G

(P GC SGM T ⊤ 

> G

P GD )VG = ( U ⊤ 

> G

P GC SG)M (T ⊤ 

> G

P GD VG) ∈ A L(G),

while for a projector atom this is the mixed-Gram lemma. This proves (A.16) and its transport compatibility. 

The matrices I, J, and A. Let DG = P 

> C∈CG

d(C)P GC , where d(C) is the common root degree on profile class C, and define DH analogously. Since d(C) = P 

> α

sα(C), star-profile transport gives 

Ω( DG) = DH . The roots here are connected and have order at least three, so all degrees are positive; hence D−1 

> G

= P 

> C

d(C)−1P GC lies in WG and is transported to D−1 

> H

. The line-side all-ones matrix 

JE(G) belongs to AL(G), and entry inspection gives 

BGB⊤ 

> G

= DG + AG, BGJE(G)B⊤ 

> G

= DGJV (G)DG. (A.17) For any M ∈ A L(G), expanding the root identity on both sides shows 

BGM B ⊤ 

> G

= X

> C,D

P GC BGM B ⊤ 

> G

P GD ,

so (A.15) transports this unblocked sandwich to BH φ(M )B⊤ 

> H

. In particular, BGB⊤ 

> G

= BGIE(G)B⊤ 

> G

,and both sandwiches in (A.17) are sums of the profile-blocked atoms in (A.14). Therefore the matrices 

IV (G) = X

> C

P GC , AG = BGB⊤ 

> G

− DG, JV (G) = D−1 

> G

BGJE(G)B⊤ 

> G

D−1 

> G

.

belong to WG and are transported. 

A.4 Diagonal and root-edge profile cells 

For any finite index set S and square matrix M indexed by S, write 

Diag S (M ) = IS ◦ M

for coordinate-diagonal extraction. We use S = V (G), V (H) for root matrices and S = E(G), E (H)

for line-indexed matrices. Call a diagonal edge fiber unequal or equal according as its two endpoint dart profiles differ or agree. For a fiber Eα, set 

∆α = X

> C∈CG

sα(C)P GC ,

where sα(C) is the common coordinate on C; this is the diagonal matrix with entry sα(u) at u. Thus 

∆α ∈ W G directly, without using diagonal extraction. If the fiber is unequal, also set 

Σα = 12



BGDα eB⊤ 

> G,α

+ eBG,α DαB⊤

> G



.

This incidence formula lies in WG. Edge by edge, its two rank-one terms cancel at the two off-diagonal endpoint positions and contribute +1 and −1 at the two diagonal positions. Thus its diagonal entry is the number of +-darts minus the number of −-darts of that fiber. Both matrices are transported. 

Lemma 13 (Diagonal extraction) . For every Z ∈ W G,

Diag V (G)(Z) ∈ W G, Ω(Diag V (G)(Z)) = Diag V (H)(Ω Z). (A.18) 

Proof. Projector atoms are already diagonal, so it suffices to treat an incidence atom 

F = P GC SGM T ⊤ 

> G

P GC .

Terms with different outer projectors have zero diagonal. Explicitly, 

Fuu = 1[u ∈ C] X 

> e,f ∋u

SG(u, e )Mef TG(u, f ).

22 Expressive Power of Implicit Line-Graph WL 

For fixed (u, e ), separate f = e and group the remaining f ’s by adjacent basis relation Rρ. The matrix M is constant on each Rρ, the coordinate d(u, e )ρ gives its multiplicity, and the recovered target dart profile fixes the corresponding entry of TG. Hence the inner sum depends only on the source fiber together with the dart profile and its SG-entry. The separated f = e contribution is likewise fixed by the source diagonal fiber and the two incidence labels. More explicitly, for an incident dart (u, e ) whose edge lies in Eα and whose dart profile is δ, set 

κS,T,M α,δ := SG(u, e ) X

> f

Mef TG(u, f ).

The preceding grouping proves that this number is independent of the chosen incident dart with fiber α and profile δ. Thus the full diagonal sum groups by dart type with a concrete, well-defined coefficient. Write δα for the common endpoint dart profile of an equal fiber and δ+ 

> α

, δ − 

> α

for the ordered profiles of an unequal fiber. An equal fiber contributes κS,T,M α,δ α (∆ α)uu . For an unequal fiber, the two dart counts satisfy 

n±(u) = (∆ α)uu ± (Σ α)uu 

2 .

Consequently the exact matrix identity is 

Diag V (G)(F ) = X

> α

P GC



κS,T,M α,δ α ∆α, Eα equal ,κS,T,M α,δ +

> α

+ κS,T,M α,δ −

> α

2 ∆α + κS,T,M α,δ +

> α

− κS,T,M α,δ −

> α

2 Σα, Eα unequal .

Every summand lies in WG. The same coefficients occur on H, while P GC , ∆α, Σα are carried to their corresponding H-matrices. This proves both assertions in (A.18). Equal-dart-profile fibers require no canonical orientation. Fix one such fiber Eα, choose an arbitrary orientation only for the following proof, and let bBα have entry +1 at the chosen positive endpoint, −1

at the other endpoint, and zero elsewhere, including on columns outside Eα. Put Kα = bB⊤ 

> α

BG. For an adjacent f , the value Kα(e, f ) is +1 or −1 according to which oriented endpoint of e is shared. Equality of the two endpoint dart profiles therefore says, for every adjacent basis relation Rρ and every e ∈ E α, X  

> f:( e,f )∈Rρ

Kα(e, f ) = 0 . (A.19) Indeed, the summands are +1 for the Rρ-neighbors in the positive wing of e and −1 for those in the negative wing, and equality of the two endpoint dart profiles equates the two counts. 

Lemma 14 (Equal-fiber cancellation) . For every Z ∈ W G,

Diag E(G)( bB⊤ 

> α

ZB G) = 0 , Diag E(G)(B⊤ 

> G

Z bBα) = 0 . (A.20) 

Consequently, for every edge e = uv ∈ E α,

Zuu = Zvv , Zuv = Zvu . (A.21) 

Proof. For SG ∈ I G and a profile class C,

bB⊤ 

> α

P GC SG = Kα ◦ (B⊤ 

> G

P GC SG).

For disjoint edge pairs both sides vanish. For an adjacent off-diagonal pair this is the unique shared-endpoint contribution; on the diagonal both sides vanish because the two endpoints have the same star profile and no eBG,β ∈ IG is supported on this equal fiber. For an incidence atom, put Q = B⊤ 

> G

P GC SG ∈ A L(G) and Q′ = M T ⊤ 

> G

P GD BG ∈ A L(G). The e-diagonal entry of bB⊤ 

> α

(P GC SGM T ⊤ 

> G

P GD )BG is 

X

> f

Kα(e, f )Q(e, f )Q′(f, e ).

23 Expressive Power of Implicit Line-Graph WL 

For fixed e, Q(e, f ) is constant on Rρ, while Q′(f, e ) is constant on R⊤ 

> ρ

. Diagonal and disjoint relations contribute zero, and (A.19) annihilates each adjacent relation. Projector atoms vanish similarly. This proves the first identity in (A.20). Apply it to Z⊤ ∈ W G and transpose the resulting diagonal to obtain the second. Orient e = uv positively at u. The two equations at e are 

Zuu + Zuv − Zvu − Zvv = 0 , Zuu + Zvu − Zuv − Zvv = 0 .

Their sum and difference give (A.21). The conclusion no longer contains bBα, so it is independent of the arbitrary orientation used in the proof. Fix a basis Z1, . . . , Z t of WG and the transported basis ΩZ1, . . . , ΩZt of WH . For an ordered root pair on each side define its complete W-profile 

ζG(u, v ) = (( Z1)uv , . . . , (Zt)uv ),ζH (u′, v ′) = ((Ω Z1)u′v′ , . . . , (Ω Zt)u′v′ ).

Replacing the chosen basis applies an invertible linear transformation to every profile vector and there-fore leaves its joint level sets unchanged. Thus the profile cells defined below are basis independent. Use the finite common index set 

Λ = {ζG(u, v ) : u, v ∈ V (G)} ∪ { ζH (u′, v ′) : u′, v ′ ∈ V (H)}.

Thus Λ includes values attained on only one root; the induction will show that no such one-sided nonempty cell remains at a fixed distance. For every nonnegative integer d and every λ ∈ Λ, define 

P Gd,λ (u, v ) = 1[dist G(u, v ) = d, ζ G(u, v ) = λ].

Define P Hd,λ by the same formula using ζH and dist H . These matrices are defined even when their support is empty; we call a nonzero one a profile cell . We suppress a superscript only in a one-root calculation. Every matrix in WG is constant on each G-profile cell by definition. 

Lemma 15 (Distance-zero and distance-one cells) . For every λ ∈ Λ and d ∈ { 0, 1}, the matrix P Gd,λ 

lies in WG, and Ω( P Gd,λ ) = P Hd,λ , including when either displayed matrix is zero. Proof. Diagonal cells. Diagonal extraction and polynomial interpolation of commuting diagonal matrices construct every profile cell on IV (G). Namely, apply finite ordinary polynomial interpolation jointly to 

Diag V (G)(Z1), . . . , Diag V (G)(Zt).

Their ordinary products evaluate coordinatewise because the matrices are diagonal, so this step does not assume Hadamard closure of WG. For each coordinate use the union of the values attained by the corresponding diagonal matrices on G and H, as in the interpolation convention above. Equation (A.18) therefore transports the resulting diagonal selector, including a zero selector for a requested value unattained on one side. 

Unequal fibers. Let Eα be unequal and define its endpoint-selector incidences 

B± 

> G,α

= 12 (BGDα ± eBG,α ).

Each α-column selects its ±-endpoint. Since B± 

> G,α

is a linear combination of BGDα, eBG,α , multi-plying Dα onto (A.16) shows that (BsG,α )⊤ZB tG,α ∈ A L(G) for s, t ∈ { +, −} . Its e-diagonal entry is Zusut for e = u+u−. Joint level sets over the basis Zi, intersected with the diagonal projector 

Dα, therefore give transported diagonal projectors Π+− 

> α,λ

and Π−+

> α,λ

. Explicitly, Π+− 

> α,λ

selects the edge e = u+u− when ζG(u+, u −) = λ, while Π−+ 

> α,λ

selects it when ζG(u−, u +) = λ. They are transported because all the compressed basis matrices used in their joint level sets are transported. The ordered root-edge cell on this fiber is 

B+

> G,α

Π+−

> α,λ

(B−

> G,α

)⊤ + B−

> G,α

Π−+

> α,λ

(B+

> G,α

)⊤. (A.22) After expanding B±

> G,α

, absorb every Dα and Π-factor into the line-algebra middle matrix. Inserting 

IV (G) = P 

> C

P GC on both root sides then expresses every resulting term as an incidence atom in (A.14). Thus (A.22) lies in WG and is transported. 

> 24

Expressive Power of Implicit Line-Graph WL 

Equal fibers. If Eα is equal, Lemmas 13 and 14 apply as follows. Equation (A.16) places both compressed terms below in AL(G); (A.18) transports the extracted root diagonal, and Hadamard multiplication by the transported Dα preserves the line algebra. Hence the diagonal line matrix 

bαZ = 12 Dα ◦



B⊤ 

> G

ZB G − B⊤ 

> G

Diag V (G)(Z)BG



has value Zuv = Zvu at e = uv . Indeed, 

(B⊤ 

> G

ZB G)ee − (B⊤ 

> G

Diag V (G)(Z)BG)ee = Zuv + Zvu = 2 Zuv ,

where the last equality is (A.21). Interpolate the joint values of the bαZi to obtain a diagonal line projector Πα,λ , per the interpolation convention; then 

BGΠα,λ B⊤ 

> G

− Diag V (G)(BGΠα,λ B⊤ 

> G

)

is the corresponding symmetric ordered root-edge cell. Because the root is simple, its off-diagonal support consists exactly of the two orientations of the selected root edges; diagonal extraction removes only the endpoint counts. Summing the fiberwise selectors with the same λ proves the lemma. The fiberwise selectors use the same common value index Λ, so an unattained requested value contributes the zero projector. Applying the same construction to Ω−1 shows that nonzero distance-zero and distance-one cells correspond bijectively and, more strongly, establishes the displayed equality for every λ ∈ Λ.

A.5 Induction on root distance 

We extend Lemma 15 by strong induction on root distance. The induction uses ordered edge pairs whose four endpoint distances identify a unique endpoint pair at the current distance. 

Lemma 16 (Profile cells at all root distances) . For every nonnegative integer d and every λ ∈ Λ,

P Gd,λ belongs to WG and Ω( P Gd,λ ) = P Hd,λ , including zero cells. Proof. Let 

(Γ Gs )uv = 1[dist G(u, v ) = s], (Γ Hs )u′v′ = 1[dist H (u′, v ′) = s]

be the 0-1 distance matrices. The assertion holds for s = 0 , 1 by Lemma 15. Suppose all profile cells at distances below d ≥ 2 have been constructed and transported. Then ΓGs = P 

> μ∈Λ

P Gs,μ and 

ΓHs = P 

> μ∈Λ

P Hs,μ = Ω(Γ Gs ) for s < d . Hence 

ΓG 

> ≥d

= JV (G) − X

> s<d

ΓGs ∈ W G, ΓGd−2 ∈ W G.

Define the two transported line-algebra matrices 

N L(G) 

> d

= B⊤ 

> G

ΓGd−2BG, T L(G) 

> d

= B⊤ 

> G

ΓG

> ≥d

BG.

Their (e, f )-entries count, respectively, the ordered endpoint pairs (x, y ) ∈ e × f at distance exactly 

d − 2 and at distance at least d. Define the corresponding matrices for H. Equation (A.16) gives 

φ(N L(G) 

> d

) = N L(H) 

> d

and φ(T L(G) 

> d

) = T L(H) 

> d

.

Witness edge-pair identification. Finite level-set interpolation in the stable line algebra gives the transported 0-1 matrix 

QL(G) 

> d

= 1[N L(G) 

> d

> 0] ◦ 1[T L(G) 

> d

= 1] ∈ A L(G), QL(H) 

> d

= φ(QL(G) 

> d

).

Thus QL(G) 

> d

is a union of stable line-graph basis relations, rather than necessarily a single basis relation. If (QL(G) 

> d

)ef = 1 , choose x ∈ e, y ∈ f with dist G(x, y ) = d − 2, which is possible because 

(N L(G) 

> d

)ef ̸ = 0 . Moving across either root edge changes distance by at most one, so every pair in 

e × f has distance at most d. The equality (T L(G) 

> d

)ef = 1 now says that exactly one ordered endpoint pair has distance d. We call (e, f ) a witness edge pair for this unique distance-d endpoint pair. The same argument applies on H.

25 Expressive Power of Implicit Line-Graph WL 

Conversely, every ordered pair (u, v ) at distance d has a witness edge pair. Take a geodesic u =

x0, x 1, . . . , x d = v and put e = x0x1, f = xd−1xd. The four endpoint distances are d, d −

1, d − 1, d − 2: the displayed geodesic gives the upper bounds, and the triangle inequality with 

dist G(u, v ) = d gives the matching lower bounds. Hence (N L(G) 

> d

)ef ̸ = 0 and (T L(G) 

> d

)ef = 1 . This also covers d = 2 , where x1 = xd−1.

Profile readout. For Z = P 

> i

aiZi ∈ W G, put zμ = P 

> i

aiμi for μ ∈ Λ. For every s < d , the already constructed and transported matrix 

LGs,Z = X

> μ∈Λ

zμP Gs,μ 

is entrywise equal to Z ◦ΓGs , but its membership in WG follows directly from the induction hypothesis, without assuming Hadamard closure. Define LHs, ΩZ by the same coefficient formula; then LHs, ΩZ =Ω( LGs,Z ). Now form 

M L(G) 

> d,Z

= B⊤ 

> G

ZB G − X

> s<d

B⊤ 

> G

LGs,Z BG. (A.23) Hence (M L(G) 

> d,Z

)ef = Zuv when (u, v ) is the unique distance-d endpoint pair associated with (e, f ).Compression compatibility shows that the analogous matrix on H is M L(H) 

> d, ΩZ

= φ(M L(G) 

> d,Z

). Each term of (A.23) belongs to AL(G) by (A.16), so M L(G) 

> d,Z

is a transported line-algebra matrix. Fix a profile value λ = ( λ1, . . . , λ t), and let 

(KL(G) 

> d,λ

)ef = ( QL(G) 

> d

)ef tY

> i=1

1[( M L(G) 

> d,Z i

)ef = λi].

This is again a union of stable basis relations. Its transported mate 

KL(H) 

> d,λ

= φ(KL(G) 

> d,λ

)

selects exactly the witness edge pairs whose unique distance-d endpoint pair has complete profile λ.By the interpolation convention, an unattained requested coordinate gives the zero selector on that side. 

Multiplicity correction. Put 

F Gd,λ = BGKL(G) 

> d,λ

B⊤ 

> G

.

This matrix belongs to WG, and its H-side counterpart is its image under Ω. The matrix F Gd,λ is supported on root distances at most d, because every endpoint pair of a selected witness edge pair has distance at most d. If F Gd,λ = P 

> i

aiZi, put fμ = P 

> i

aiμi for μ ∈ Λ. The induction hypothesis constructs and transports the lower-layer matrix 

LG<d,λ = X

> s<d

X

> μ∈Λ

fμP Gs,μ .

Entrywise this matrix equals P

> s<d

(F Gd,λ ◦ ΓGs ); this equality is an observation about entries, not an appeal to Hadamard closure. For a root pair at distance below d, the lower-distance sum removes the corresponding entry of F Gd,λ ; above distance d, that entry is zero by the support observation. At distance d, every selected witness edge pair contributing to the (u, v )-entry has (u, v ) as its unique distance-d endpoint pair. The entry is then zero unless its profile is λ, and otherwise counts those witness edge pairs. Since F Gd,λ ∈ W G, its value on the complete profile λ = ( λ1, . . . , λ t) is the explicit scalar 

wd,λ := X

> i

aiλi.

The transported matrix is P 

> i

aiΩZi, so it has the same scalar on the transported H-profile. We therefore have the entrywise identities 

bF Gd,λ := F Gd,λ − LG<d,λ = wd,λ P Gd,λ , Ω( bF Gd,λ ) = wd,λ P Hd,λ . (A.24) This argument does not use Hadamard closure of WG: each lower-distance restriction in the definition of bF was defined first as the already constructed profile-cell sum displayed above. If P Gd,λ ̸ = 0 , the 

26 Expressive Power of Implicit Line-Graph WL 

geodesic construction above gives a selected witness edge pair, so wd,λ > 0. If instead P Hd,λ ̸ = 0 , the identical argument on H gives the same conclusion for the common scalar. Dividing (A.24) then constructs both cells at once: 

P Gd,λ = 1

wd,λ 

bF Gd,λ , P Hd,λ = 1

wd,λ 

Ω( bF Gd,λ ).

If the cell occurs on neither root, both cell matrices are zero. This proves the induction step for every 

λ ∈ Λ, including empty cells, and strong induction establishes the lemma for all root distances. 

A.6 Completion of the proof 

Proof of Theorem 1. If either root is K1, K 2, the paw, or the diamond, Lemma 3 proves the result. We may therefore use the standing hypotheses of the general construction above. The nonempty profile cells from Lemma 16 are pairwise disjoint 0-1 matrices and sum to JV (G).Every matrix in WG is its profile-value-weighted sum of these cells, while every cell belongs to WG.Disjoint nonzero 0-1 cells are linearly independent, so these two containments show that they form a basis of WG, and 

P Gd,λ ◦ P Gd′,μ = 1[( d, λ ) = ( d′, μ )] P Gd,λ .

Thus WG is Hadamard closed, and the same holds for WH . Since Ω maps the profile cells and maps 

IV (G), J V (G), A G to IV (H), J V (H), A H , it preserves Hadamard products as well as the operations in Lemma 12. We now restrict Ω to the stable root algebras by proving the two required inclusions separately. Let 

AG and AH denote the stable root pair algebras from the first subsection. Since WG is now a coherent matrix algebra containing IV (G), J V (G), A G, minimality gives AG ⊆ W G; similarly AH ⊆ W H .Consider the preimage 

BG = {Z ∈ W G : Ω( Z) ∈ A H }.

Preservation of ordinary and Hadamard multiplication, transpose, and I, J, A makes BG a coherent matrix algebra containing IV (G), J V (G), A G. Minimality therefore gives AG ⊆ B G, or Ω( AG) ⊆AH . Applying the same argument to 

BH = {Y ∈ W H : Ω −1(Y ) ∈ A G}

gives Ω−1(AH ) ⊆ A G, equivalently AH ⊆ Ω( AG). Hence Ω restricts to an isomorphism 

Ψ : AG −→ A H

preserving ordinary and Hadamard multiplication, transpose, trace, and I, J, A .It remains to verify the basis-bijection hypothesis of Lemma 2. Let Cst  

> G

, Cst  

> H

be the stable root-color sets and expand 

Ψ( RGρ ) = X

> σ∈C st
> H

aρσ RHσ .

Because RGρ ◦ RGρ = RGρ , Hadamard preservation gives a2 

> ρσ

= aρσ , so every coefficient is 0 or 1.Images of distinct source relations are Hadamard-orthogonal, and 

X

> ρ∈C st
> G

Ψ( RGρ ) = Ψ( JV (G)) = JV (H).

Consequently each target relation RHσ occurs in exactly one of the displayed images. Injectivity of Ψ makes every source image nonzero. Finally, the relation matrices are bases and Ψ is a linear isomorphism, so 

|C st  

> G

| = dim AG = dim AH = |C st  

> H

|.

Thus the partition of Cst  

> H

into the nonempty supports of the source images has singleton parts. There is a bijection π : Cst  

> G

→ C st  

> H

with Ψ( RGρ ) = RHπ(ρ). All the remaining hypotheses of Lemma 2 are inherited from Ψ; in particular relation sizes agree because |RGρ | = tr( RGρ (RGρ )⊤). The lemma now gives G ≡3-WL H.

27 Expressive Power of Implicit Line-Graph WL 

A.7 Componentwise extension to disconnected roots 

We now remove connectedness without changing the connected reconstruction above. For a graph X,let Comp( X) denote its set of connected components, including isolated vertices. 

Lemma 17 (Component matching for stable pair refinement) . If finite graphs X, Y satisfy X ≡3-WL 

Y , then their components admit a bijection β : Comp( X) → Comp( Y ) such that 

X[C] ≡3-WL Y [β(C)] (C ∈ Comp( X)) .

Conversely, any such componentwise bijection implies X ≡3-WL Y .Proof. Assume first that the graphs are nonempty. Let X, Y be their stable-pair coherent configura-tions and let φ : X → Y be the trace-preserving algebraic isomorphism induced by joint refinement, with φ(AX ) = AY . The least equivalence relation 

eX = ⟨IX ∪ AX ⟩

has exactly the connected components of X as its classes. It is a parabolic of X, and algebraic isomorphisms transport this equivalence closure, so φ(eX ) = eY [23, Section 2.7, equation (5)]. Use the unique decomposition of a partial parabolic into indecomposable partial parabolics. The isomorphism φ bijects these summands and preserves their numbers of classes; after pairing their classes, it restricts on any paired classes C, D to an algebraic isomorphism of the corresponding restricted coherent configurations [8, Definition 2.1.20, Lemma 2.1.21, Proposition 2.3.25, and Exercise 2.7.31]. This restriction maps AX |C×C to AY |D×D . It therefore maps the smallest coherent algebra generated by identity, all-ones, and adjacency on X[C] into the corresponding stable pair algebra on Y [D]; applying the inverse restriction gives the reverse inclusion, hence equality. The restricted isomorphism also preserves relation multiplicities. The coherent-algebra characterization from Lemma 2 gives X[C] ≡3-WL Y [D]. Repeating this for every class yields β.For the converse, use the bijective-pebble characterization of counting logic. In each round, take the disjoint union of Duplicator’s bijections for the matched component games. Pebbled vertices in different components stay in different matched components, so equality and adjacency are preserved. The empty-graph case is immediate. 

Proof of Theorem 2. Apply Lemma 17 to the assumed equivalence of L(G) and L(H). The line graph of a connected component containing an edge is connected, and each connected line-graph component arises this way. Because G and H have no isolated vertices, this gives a bijection between their root components. For each matched pair C, D ,

L(C) ≡3-WL L(D).

Both C and D are Whitney-general, so Theorem 1 gives C ≡3-WL D. The converse direction of Lemma 17 now yields G ≡3-WL H.

# B Four-Clique Counting and Incomparability Witnesses 

B.1 Universal four-clique count 

The initial pair color distinguishes nonadjacent line vertices, which for a simple root are distinct disjoint edges. In the first global update of an ordered pair (e, f ), the multiplicity of the replacement entry (adj , adj) is exactly |NL(G)(e) ∩ NL(G)(f )|; hence the graph-level histogram determines the set in Corollary 3. Write e = ab and f = cd . Their common line neighbors are precisely the present edges among 

ac, ad, bc, bd , so the count is four exactly when the four endpoints induce a K4. Conversely, every K4

has three unordered pairs of opposite edges, hence six ordered pairs, and the endpoint union identifies the clique uniquely. This proves the formula. The Lean development separately checks the local equivalence, the six-to-one fiber count, the first-round histogram implication, and the global-edge ILG-3 corollary. 

> 28

Expressive Power of Implicit Line-Graph WL 

B.2 Exact witnesses for comparison with root 4-WL 

We verify the two directions in Remark 2 using exact joint 3-FWL on the roots and exact joint 2-FWL on their line graphs. Refinement is collision-safe, uses one shared color namespace for each compared pair, and runs until separation or stability. The color traces, including the atomic round, are witness roots root 4-WL ILG-3-WL 

STS(13) incidence pair 14 → 67 → 101 ( separates ) 3 → 5 → 7 → 7 ( stable equal )

K5 compressed parity pair 15 → 43 → 43 ( stable equal ) 3 → 8 → 46 ( separates )

For the first pair, translate the base blocks {0, 1, 4} and {0, 2, 7} modulo 13 , then apply the Pasch trade 

014 , 027 , 179 , 249 ←→ 017 , 024 , 149 , 279 .

The two systems have respectively 13 and 8 Pasch configurations, so their connected incidence roots, each with (n, m ) = (39 , 78) , are nonisomorphic. Figure 3 shows the two roots under a shared cyclic layout. 012345678910 11 12 

> 014 027 249 179

cyclic STS(13) 012345678910 11 12 

> 017 024 279 149

Pasch-traded STS(13) 

Figure 3: The STS(13) incidence witness. Each panel is a root graph: hollow circles are points, squares are blocks, and an edge means that the point belongs to the block. Under the displayed alignment, 74 light-gray edges are shared; the Pasch trade removes the four orange edges and adds the four blue edges. Root 4-WL separates the pair, whereas ILG-3-WL does not. For the reverse pair, replace every vertex of K5 by the eight parity-constrained bitstrings on its four incident base edges, joining states in adjacent gadgets when their shared-edge bits agree. One root uses even parity in every gadget; the other uses odd parity at one base vertex. Both are connected 

16 -regular graphs with (n, m ) = (40 , 320) ; the first contains 64 copies of K5 and the second contains none. Figure 4 shows these roots under a shared five-gadget layout. 

29 Expressive Power of Implicit Line-Graph WL V0

> V1
> V2V3
> V4

all-even parity V0

> V1
> V2V3
> V4

one-odd parity at V0  

> V0V1
> bits agree
> V0V1
> bits differ after alignment

Figure 4: The compressed parity witness over K5. Each Vi contains eight bitstring states, shown as graph vertices; states in two gadgets are adjacent when their bits for the corresponding base edge agree. The left root uses even parity in every gadget, while the right root uses odd parity in V0. Under the displayed alignment, 288 gray edges are shared; the 32 orange edges are removed and the 32 blue edges are added. Each main panel shows all 40 vertices and 320 edges, and the inset enlarges the changed V0–V1 bundle. ILG-3-WL separates the pair, whereas root 4-WL does not. All four roots are therefore nonisomorphic, connected, simple, and Whitney-general. The repro-duction script experiments/deterministic/run_incomparability_certificate.py recon-structs both witness pairs and reproduces these refinement traces. 

# C Machine Verification 

The accompanying Lean 4 development formalizes finite simple undirected graphs, line graphs, collision-free WL and folklore-WL refinements, the direct and global-edge ILG definitions, and the message-passing model used in the neural expressivity theorem. Top-level import files organize the formalization into line-graph correspondences, first-order results, third-order results, and neural expressivity. The formalized results include the ILG/line-graph refinement and GNN correspondences, forward transfer, the equal-order first-order counterexample, the universal factor-six four-clique identity and its global-edge ILG-3 consequence, the Shrikhande–rook separation, the connected completion with the Whitney exception, and the connected and componentwise backward-transfer theorems. The formalization of backward transfer follows the reconstruction argument in Appendix A and proves the relation between pair refinement and the separate-coordinate 3-WL formulation used in the paper. It also formalizes the lower- and upper-bound directions for global-edge ILG-GNN expressivity. Theorem 1 is stated in the stable-signature form used in the paper and proved equivalent to the all-round collision-free formulation used internally. The Lean development covers the k = 3 arity-shift instance used by the backward theorems; the general arity shift and the cited k = 2 consequence are taken from the literature. Lean checks all modules containing the stated theorems. The development contains no sorry ,

admit , or custom axiom declarations. A separate Lean file records the axioms used by the main results. The top-level imports, build instructions, and theorem index mapping each numbered paper result and Appendix A lemma to its Lean declaration are available in the accompanying repository: 

https://github.com/lukeyf/ilg-wl .

> 30

Expressive Power of Implicit Line-Graph WL 

# D Proofs of ILG Equivalence and Expressivity Results 

D.1 ILG-1-GNN Equals MPNN on L(G)

We give the detailed proof of Lemma 1. Let G be a finite simple undirected graph. Under the canonical identification E(G) = V (L(G)) , define the ILG neighborhood of e ∈ E(G) by 

NILG (e) = {f ∈ E(G) : f̸ = e, e ∩ f̸ = ∅} .

By the definition of the line graph, 

NILG (e) = NL(G)(e). (D.1) Consider a Tnet -layer MPNN on L(G) of the form 

h(ℓ+1)  

> e

= Uℓ



h(ℓ) 

> e

, AGG ℓ

n

Mℓ



h(ℓ) 

> e

, h (ℓ) 

> f

, ω L(G)

> ef



: f ∈ NL(G)(e)

o 

,

where Mℓ is the message map, Uℓ is the update map, AGG ℓ is permutation invariant, and ωL(G) 

> ef

is an optional edge feature on the line graph. In the untyped case it is constant. If typed line-graph adjacencies are used, assume the ILG layer uses the corresponding feature 

ωILG  

> ef

= ωL(G) 

> ef

for all f ∈ NILG (e). (D.2) The matching ILG-1-GNN on G is 

˜h(ℓ+1)  

> e

= Uℓ

˜h(ℓ) 

> e

, AGG ℓ

n

Mℓ

˜h(ℓ) 

> e

, ˜h(ℓ) 

> f

, ω ILG 

> ef



: f ∈ NILG (e)

o 

.

Both models are initialized by the same edge features: 

h(0)  

> e

= ˜h(0)  

> e

= ϕ0(qe) for all e ∈ E(G).

We prove by induction that 

h(ℓ) 

> e

= ˜h(ℓ) 

> e

for all e ∈ E(G) (D.3) at every layer ℓ. The base case ℓ = 0 is the shared initialization above. Suppose (D.3) holds at layer ℓ.For a fixed edge e, the MPNN message multiset at the line-graph vertex e is 

ML(G) 

> e

=

nn 

Mℓ



h(ℓ) 

> e

, h (ℓ) 

> f

, ω L(G)

> ef



: f ∈ NL(G)(e)

oo 

.

Using the neighborhood identity (D.1), the feature identity (D.2), and the induction hypothesis, this multiset is equal to 

MILG  

> e

=

nn 

Mℓ

˜h(ℓ) 

> e

, ˜h(ℓ) 

> f

, ω ILG 

> ef



: f ∈ NILG (e)

oo 

.

Because AGG ℓ is a function of the multiset rather than the ordering of its elements, 

AGG ℓ(ML(G) 

> e

) = AGG ℓ(MILG  

> e

).

Applying the same update map Uℓ gives 

h(ℓ+1)  

> e

= ˜h(ℓ+1)  

> e

.

Thus (D.3) holds at layer ℓ + 1 , and induction proves the layerwise equivalence. If the implementation stores line-graph vertices in an ordering different from the edge ordering used by the ILG layer, let PE be the corresponding permutation matrix. The same argument gives 

Z(ℓ) 

> L(G)

= PE eZ(ℓ) for all ℓ. 

Any graph readout that is invariant to the order of vertices or edges therefore satisfies 

READOUT 



{h(Tnet ) 

> e

: e ∈ V (L(G)) }



= READOUT 



{˜h(Tnet ) 

> e

: e ∈ E(G)}



.

The equivalence is therefore exact for edge-only ILG-1-GNN. 

31 Expressive Power of Implicit Line-Graph WL 

D.2 Atomic Types on the Line Graph 

Let G be simple and undirected. A vertex of L(G) is a root edge e ∈ E(G). For ordered edge tuples 

e = ( e1, . . . , e k) ∈ E(G)k, the atomic type of e as a tuple of vertices in L(G) is determined by equality and adjacency among tuple coordinates: 

atp L(G)(e) =  1[ei = ej ], 1[ei ∼L(G) ej ] 

> 1≤i,j ≤k

.

Because ei ∼L(G) ej if and only if ei̸ = ej and ei ∩ ej̸ = ∅, this atomic type can be computed from the root-edge endpoints without building L(G). For k = 2 , equality and adjacency collapse to the three-valued relation code 

rG(e, f ) = 0 if e = f, rG(e, f ) = 1 if e ∼L(G) f, rG(e, f ) = 2 otherwise .

Thus every initial color used by k-WL on L(G) is an endpoint-incidence function on G.

Remark 3 (Completeness of the relation code for k = 2 ). The three-valued code rG(e, f ) determines the atomic type of the ordered pair (e, f ) in L(G). The only line-graph relations between two vertices are equality, adjacency, and non-adjacency. Since line-graph adjacency is symmetric for simple undirected G, the code is symmetric: rG(e, f ) = rG(f, e ). The initial atomic type for an ordered pair is therefore (rG(e, f ), r G(f, e )) = ( rG(e, f ), r G(e, f )) , which is determined by rG(e, f ) alone. 

D.3 Forward Transfer: 1-WL Equivalence Implies Line-Graph 1-WL Equivalence 

We prove Proposition 2. Suppose G ≡1-WL H. We argue directly with the stable 1-WL (color-refinement) colorings; this is equivalent to the fractional-isomorphism characterization [15, 24] and avoids constructing an explicit doubly stochastic matrix on edges. Let cG and cH be the stable 1-WL vertex colorings of G and H. For stable colors a, b , let qG(a, b ) be the number of neighbors of color b at a vertex of color a; define qH analogously. Define an induced coloring of the line graph by 

bcG(e) = {{ cG(u), c G(v)}} for e = {u, v } ∈ E(G),

and likewise bcH on L(H).

bcG is equitable on L(G). Fix e = {u, v }. Its neighbors in L(G) are the root edges sharing u or v,other than e. For any target color {{ a, b }} , the number of such edges f with bcG(f ) = {{ a, b }} is determined by cG(u), c G(v) and the quotient numbers qG, with the contribution of e itself removed. It is therefore constant on each bcG-class. Thus bcG is equitable; the same proof applies to bcH . We do not require these induced colorings to be the coarsest equitable partitions. 

Transfer. Because G ≡1-WL H, a stable-color bijection identifies the class sizes and quotient numbers 

qG, q H . It therefore maps bcG-classes to bcH -classes while preserving their sizes and their equitable quotients. Starting color refinement from these matched equitable partitions gives identical stable histograms; coarsening back to the uniform initialization cannot separate the graphs either. Therefore 

L(G) ≡1-WL L(H).

D.4 Equal-Order Failure of First-Order Backward Transfer 

For the roots in Proposition 3, the different root degree histograms 4226 and 3612 prove 1-WL inequivalence. To verify the line premise without relying on software, start line-graph color refinement from degrees. In both line graphs, two vertices have degree two and eight have degree four. The next round splits the degree-four vertices according to whether they have a degree-two neighbor, producing cells A, B, C of sizes 2, 4, 4. Their common quotient is 

A : (0 , 2, 0) , B : (1 , 1, 2) , C : (0 , 2, 2) .

This partition is equitable and therefore stable. Thus the two line graphs have identical stable color-class sizes and quotients, while the roots do not. This also explains why an argument based only on root order or inverse-line-graph uniqueness cannot prove first-order backward transfer. 

> 32 Expressive Power of Implicit Line-Graph WL

D.5 Equivalence of ILG-k-WL and k-WL on L(G)

We prove Proposition 1. For k = 1 , the ILG update uses exactly the line-neighbor set identified in the proof of Lemma 1; hence its color refinement is exactly 1-WL on L(G). Assume for the remainder of this subsection that k ≥ 2.Let cL(G) 

> t

, c ILG ,G t : E(G)k → C t denote the two colorings, run with the shared encoding convention of Section 2. Both start from the same atomic type: 

cL(G)0 (e) = cILG ,G  

> 0

(e) = HASH(atp L(G)(e)) .

For e = ( e1, . . . , e k) and g ∈ E(G), write 

e[i ← g] = ( e1, . . . , e i−1, g, e i+1 , . . . , e k).

The separate-coordinate k-WL update on L(G) is 

cL(G) 

> t+1

(e) = HASH 



cL(G) 

> t

(e),

nn 

cL(G) 

> t

(e[i ← g]) : g ∈ E(G)

oo ki=1 



.

The ILG-k-WL update is 

cILG ,G t+1 (e) = HASH 



cILG ,G t (e),

nn 

cILG ,G t (e[i ← g]) : g ∈ E(G)

oo ki=1 



.

The two displayed recurrences have the same domain, initialization, replacement range, and update. Induction on t therefore gives 

cL(G) 

> t

(e) = cILG ,G t (e) (e ∈ E(G)k)

at every round. If two implementations use different injective color names, the same induction instead supplies the canonical bijection between those names. 

Graph-level consequence. Since the tuple domains are identical and the colors agree at every round, their graph-level signatures agree: 

nn 

cILG ,G t (e) : e ∈ E(G)koo 

=

nn 

cL(G) 

> t

(e) : e ∈ E(G)koo 

for all t. 

Therefore any graph pair separated by k-WL on L(G) is separated by ILG-k-WL on G, and conversely. 

D.6 Equivalence of Global-Edge ILG-k-WL and k-WL on L(G)

We prove Corollary 1 for k ≥ 3, writing the update explicitly for k = 3 .

From direct ILG-k-WL to the global-replacement form. The direct ILG-k-WL state space is 

E(G)k. Its separate-coordinate update contains k replacement multisets and enumerates O(mk+1 )

replacement entries per round; canonical sorting gives the O(mk+1 log m) worst-case time in Table 2. The global-edge construction instead uses the equivalent global-replacement form on (k−1) -tuples. At k = 3 , the pair coloring of L(G) has the update 

ϑL(G) 

> t

(g; e, f ) :=  cFWL ,L (G) 

> t

(g, f ), c FWL ,L (G) 

> t

(e, g ),cFWL ,L (G) 

> t+1

(e, f ) = HASH 



cFWL ,L (G) 

> t

(e, f ),

nn 

ϑL(G) 

> t

(g; e, f ) : g ∈ E(G)

oo 

.

This pair-state process has the same stable graph-distinguishing power as the separate-coordinate 

3-WL convention fixed in Section 2 [12]. 

Restriction to k ≥ 3. For k = 2, global replacement has one-edge states. Its message 

{{ ct(g) : g ∈ E(G)}} contains no relation between g and the current edge, so it cannot recover line-graph adjacency; for example, it fails on P4 versus K1,3. Adding rG(g, e ) restores exactly 1-WL power. The k = 2 comparison therefore follows from the graph-level equivalence of 1-WL and 

2-WL. 

> 33

Expressive Power of Implicit Line-Graph WL 

Specialization to the line graph. All atomic information needed to initialize this pair coloring is given by rG(·, ·) (Remark 3). Define the global-edge coloring cGE ,G t by 

cGE ,G  

> 0

(e, f ) = HASH( rG(e, f )) ,

and, with 

ϑGE ,G t (g; e, f ) :=  cGE ,G t (g, f ), c GE ,G t (e, g ), r G(g, e ), r G(g, f ),cGE ,G t+1 (e, f ) = HASH 



cGE ,G t (e, f ),

nn 

ϑGE ,G t (g; e, f ) : g ∈ E(G)

oo 

.

Redundancy of the extra relation codes. At every round t ≥ 0, the color cGE ,G t (e, g ) refines its initial color and therefore determines rG(g, e ). Symmetrically, cGE ,G t (g, f ) determines rG(g, f ).Thus the two appended relation codes are functions of the first two entry coordinates and cannot refine the multiset partition. 

Partition equivalence by induction. At t = 0 , both colorings are injective functions of rG(e, f ),so they induce the same partition. Assume a color-name bijection πt identifies their partitions at round t. The global-replacement multiset is 

SFWL ,L (G) 

> t

(e, f ) = 

nn 

ϑL(G) 

> t

(g; e, f ) : g ∈ E(G)

oo 

.

The global-edge multiset is 

SGE ,G t (e, f ) = 

nn 

ϑGE ,G t (g; e, f ) : g ∈ E(G)

oo 

.

Apply πt to the first two coordinates. The preceding paragraph shows that the remaining two coordinates are fixed functions of them, so the two multisets determine one another. Their full update signatures therefore induce the same partition at round t + 1 , giving πt+1 .

D.7 Neural Simulation of the Tuple Refinement 

We prove Proposition 5 for k = 3 , where the states are edge pairs. The general construction replaces pairs by ordered (k−1) -tuples. 

Setup. Fix a feature-labeled graph class G with |E(G)| ≤ mmax for all G ∈ G , and assume that its edge features range over a fixed finite alphabet. At k = 3 , the global-edge ILG-k-GNN layer is 

M (ℓ) 

> ef

= X

> g∈E(G)

ψℓ



h(ℓ) 

> gf

, h (ℓ) 

> eg

, q g , Emb r (rG(g, e )) , Emb r (rG(g, f )) 



,h(ℓ+1)  

> ef

= ηℓ(h(ℓ) 

> ef

, M (ℓ) 

> ef

).

Simulation direction (lower bound). We construct functions ψℓ, η ℓ such that h(ℓ) 

> ef

encodes the feature-labeled discrete color cGE ,G ℓ (e, f ).

Base case. Choose Emb r injective and choose ϕ0 to be injective on the finite domain 

{(qe, q f , Emb r (rG(e, f ))) : e, f ∈ E(G), G ∈ G} . For the feature-labeled refinement, set 

cGE ,G  

> 0

(e, f ) = HASH( qe, q f , r G(e, f )) .

Then h(0)  

> ef

= ϕ0(qe, q f , Emb r (rG(e, f ))) injectively encodes this initial color. 

Induction step. Suppose h(ℓ) 

> ef

= χℓ(cGE ,G ℓ (e, f )) for an injective code χℓ : Dℓ → Rdh . Define the 

replacement entry 

τ Gℓ (g; e, f ) = 



cGE ,G ℓ (g, f ), c GE ,G ℓ (e, g ), r G(g, e ), r G(g, f ), q g



.

The entry set is finite, with cardinality at most 

|D ℓ|2 32 |{ qg }| .

34 Expressive Power of Implicit Line-Graph WL 

Consequently  τ Gℓ (g; e, f ) : g ∈ E(G) ranges over a finite family of multisets of size at most 

mmax .By the multiset-encoding lemma [27], for any countable domain and bounded multiset size, there exists a function ωℓ and a dimension d such that 

Φℓ

  τ Gℓ (g; e, f ) : g ∈ E(G)  := X

> g∈E(G)

ωℓ(τ Gℓ (g; e, f )) ∈ Rd

is injective in the multiset argument. Since χℓ and Emb r are injective, the neural message inputs determine τ Gℓ . Choose ψℓ to realize ωℓ on this finite input domain; an MLP of sufficient width can do so. Then 

M (ℓ) 

> ef

= X

> g∈E(G)

ψℓ(· · · ) = Φ ℓ

  τ Gℓ (g; e, f ) : g ∈ E(G) ,

which is injective in the replacement multiset. Choose ηℓ to map (h(ℓ) 

> ef

, M (ℓ) 

> ef

) injectively to a new code for cGE ,G ℓ+1 (e, f ). The discrete update shows that these two inputs determine the new color. At the final round, pair colors and edge features range over finite domains. Since the final hidden states injectively encode pair colors and both domains and their multiset sizes are finite, choose γread 

and ξ so that their sums injectively encode the respective multisets. Choose READOUT injective on the resulting finite set of summary pairs. The graph output then injectively encodes the complete feature-labeled refinement signature. 

Upper bound (converse). Run the feature-labeled refinement jointly on G and H, so equal color names have a graph-independent meaning. We prove by induction that, for fixed network parameters, there is one function Fℓ such that, for K ∈ { G, H },

h(ℓ) 

> ef

= Fℓ(cGE ,K ℓ (e, f )) (e, f ∈ E(K)) .

The base case follows from the initial color above. At the induction step, the new color determines the old color and the complete multiset of replacement entries τ Kℓ (g; e, f ). The induction hypothesis therefore determines every message input, its multiset, the aggregate, and the updated state. This defines Fℓ+1 independently of K.If the two graphs are not separated by the feature-labeled refinement, the multiplicity of every final pair color is the same on both sides; the same is true for every edge-feature color represented on the diagonal. Hence the graph readout 

hK = READOUT 

 X 

> e,f ∈E(K)

γread (h(Tnet ) 

> ef

), X

> e∈E(K)

ξ(qe)

 , K ∈ { G, H },

receives identical multisets of inputs and gives hG = hH . No tuplewise or edgewise bijection between the graphs is used. 

D.8 Algorithmic Specification 

This section specifies the global-edge ILG-k-GNN forward pass for general k. The procedure operates directly on G without constructing the adjacency list of L(G). Fix an edge ordering 

EG = ( e1, . . . , e m) of E(G).

Step 1: edge features. Construct an edge feature matrix QG ∈ Rm×de . For unlabeled simple graphs, (QG)i may be a constant vector. With vertex labels, (QG)i concatenates a constant edge indicator with the one-hot labels of the two endpoints in sorted order. 

Step 2: relation access. After locally indexing the incident root vertices, store the endpoint table 

PG ∈ Nm×2, whose i-th row lists the two endpoint indices of ei. A relation code is computed in 

O(1) by comparing two rows: rG(ei, e j ) = 0 when i = j, 1 when the rows share an entry, and 2

otherwise. Constructing and retaining PG uses O(m) time and space. The layer evaluates rG only for the index pairs of the current tuple chunk rather than retaining a relation matrix. In Steps 3–4, (RG)ij 

denotes a call to this accessor, not a stored tensor. Implementations that require a dense relation tensor may instead materialize RG ∈ { 0, 1, 2}m×m, using O(m2) space. This tensor is not required by the model definition or by an implementation that computes relations on demand. 

> 35

Expressive Power of Implicit Line-Graph WL 

Step 3: tuple initialization. For every ordered (k−1) -tuple i = ( i1, . . . , i k−1), compute 

Z(0)  

> i

= ϕ0



(QG)i1 , . . . , (QG)ik−1 ,  (RG)ip,i q



> 1≤p<q ≤k−1



.

This yields Z(0) ∈ Rmk−1×dh .

Step 4: global-edge message aggregation. For each layer ℓ and each tuple i, aggregate over the global edge index g:

M(ℓ) 

> i

=

> m

X

> g=1

ψℓ

 Z(ℓ)

> i[p←g]

k−1 

> p=1

, (QG)g ,  (RG)g,i p

k−1

> p=1



,

where i[p ← g] denotes the tuple with coordinate p replaced by g. Then update Z(ℓ+1)  

> i

=

ηℓ(Z(ℓ) 

> i

, M(ℓ) 

> i

).

Step 5: invariant readout. After Tnet layers, produce graph logits by applying an invariant readout, where ∥ denotes vector concatenation: 

zG = MLP 

"X

> i

γread (Z(Tnet ) 

> i

) X

> i

ξ(( QG)i)

#! 

.

Any fixed edge and tuple ordering may be used. Under a vertex relabeling, RG, QG, Z are permuted consistently, and the final sums are unchanged. 

D.9 Permutation Equivariance and Invariance 

We prove equivariance and invariance for the k = 3 instantiation (edge pairs); the general (k−1) -tuple case is identical. Let σ : V (G) → V (H) be a relabeling isomorphism and define its induced edge bijection by 

σE ({u, v }) = {σ(u), σ (v)}.

For e ∈ E(G), write ¯e = σE (e). Primes denote features and hidden states on H = σ(G).

Relation preservation. For any e, f ∈ E(G), the edges share an endpoint if and only if their images do. Therefore 

rH (¯ e, ¯f ) = rG(e, f ).

Initialization. If edge features satisfy q′ 

> ¯e

= qe (i.e., features are defined intrinsically), then the initialized pair tensor satisfies 

h′(0) ¯e, ¯f = ϕ0(q′

> ¯e

, q ′¯f , Emb r (rH (¯ e, ¯f ))) = ϕ0(qe, q f , Emb r (rG(e, f ))) = h(0)  

> ef

.

Induction. Assume h′(ℓ)¯e, ¯f = h(ℓ) 

> ef

for all e, f ∈ E(G). The message for the relabeled pair is 

M ′(ℓ)¯e, ¯f = X

> g′∈E(H)

ψℓ



h′(ℓ)  

> g′,¯f

, h ′(ℓ)¯e,g ′ , q ′ 

> g′

, Emb r (rH (g′, ¯e)) , Emb r (rH (g′, ¯f )) 



.

Substituting g′ = σE (g), using the bijection E(G) → E(H), and applying the induction hypothesis, feature identity, and relation preservation gives 

M ′(ℓ)¯e, ¯f = X

> g∈E(G)

ψℓ



h(ℓ) 

> gf

, h (ℓ) 

> eg

, q g , Emb r (rG(g, e )) , Emb r (rG(g, f )) 



= M (ℓ) 

> ef

.

Thus h′(ℓ+1) ¯e, ¯f = ηℓ(h(ℓ) 

> ef

, M (ℓ) 

> ef

) = h(ℓ+1)  

> ef

.

36 Expressive Power of Implicit Line-Graph WL 

Invariance. The graph readout sums over all ordered edge pairs and all edges. Since σE : E(G) →

E(H) is a bijection, the multisets 

nn 

h′(Tnet )¯e, ¯f : e, f ∈ E(G)

oo 

and 

nn 

h(Tnet ) 

> ef

: e, f ∈ E(G)

oo 

are identical, and likewise for edge features. Therefore the invariant readouts are equal: hH =

hG.

D.10 Complexity Details 

Let n = |V (G)|, m = |E(G)|, mL = |E(L(G)) | = P 

> v∈V(G)

 dG(v)2

, and fix k ≥ 3. Table 2 reports worst-case upper bounds for fixed k and fixed neural feature widths. Discrete update signatures are streamed one target tuple at a time. For the neural implementation, C is the target-tuple chunk size. 

Table 2: Worst-case complexity of the root- and line-domain formulations. Initialization is performed once; iterative costs are per refinement round or neural layer.                                                          

> Method Time Peak space Root G(k−1) -FWL O(nk−1+n+m)initialization;
> O(nklog n)per round
> O(nk−1+n+m)
> Direct ILG-k-WL O(mk)initialization;
> O(mk+1 log m)per round
> O(mk+m)
> Explicit L(G) ( k−1) -FWL O(mk−1+m+mL)initialization;
> O(mklog m)per round
> O(mk−1+m+mL)
> Global-edge ILG-k-WL O(mk−1+m)initialization;
> O(mklog m)per round
> O(mk−1+m)
> Global-edge ILG-k-GNN O(mk−1+m)initialization; O(mk)
> per layer
> O(mk−1+Cm )per layer with checkpointing

Direct ILG-k-WL stores k-tuples, whereas global-edge ILG-k-WL stores (k−1) -tuples; this accounts for the additional factor of m in the direct formulation. Explicit (k−1) -FWL on L(G) and global-edge ILG-k-WL have the same per-round complexity. They differ in whether line-graph adjacency is stored or computed from root-edge endpoints. Since mL ≤  m

> 2

 = O(m2), both use O(m2)

total worst-case space at k = 3 . The O(m) versus O(m + mL) difference refers only to graph storage, excluding tuple states. If exact color identifiers occupy a dense integer range of suitable size, array-based counting removes the sorting factor. T rounds or layers multiply iterative time by T . The reported ILG-3-GNN experiment and the empirical cost experiment both execute the dense ordered edge-pair state space, so their implementation does not alter these bounds. The deterministic refinement runner represents each canonical signature by a deterministic 128 -bit digest. A digest collision is therefore possible but highly unlikely. 

# E Empirical Cost and Scaling 

Objectives. At k = 3 , we test four claims: line-domain cost scales with m; computing adjacency from root endpoints avoids storing E(L(G)) but leaves the m2 states and m3 updates unchanged; root 4-WL can be cheaper on dense roots; and explicit and implicit implementations return the same signatures. 

Experimental setup. We compare root 2-FWL (the paper’s root 3-WL), root 3-FWL (root 4-WL), 

2-FWL on an explicitly materialized L(G), and global-edge ILG-3-WL. The explicit and implicit methods use the same deterministic pair-refinement implementation with 128 -bit digests. The explicit method constructs and retains E(L(G)) , including isolated line vertices, whereas the implicit method queries the atomic relation from the root-edge endpoints. We checked their final signatures for equality on every measured call; all checks passed. All methods use dense state spaces, one graph per call, and complete refinement rounds. 

> 37

Expressive Power of Implicit Line-Graph WL 

The primary sweep uses connected d-regular roots on n = 25 vertices for d ∈{2, 4, 6, 8, 12 , 16 , 20 , 24 }, with three fixed seeds per degree (the endpoints are C25 and K25 ). This fixes every root-domain state and replacement count while varying m = 25 d/ 2. Each call performs atomic initialization and exactly one complete refinement round, avoiding graph-dependent stabi-lization depth. A fresh single-threaded process first performs one warm-up and then three timed calls; Table 3 reports the median over the resulting nine observations. Peak resident set size (RSS) is measured without allocation tracing in a separate fresh process on the fixed first seed. Root generation and interpreter start-up are excluded from time, while explicit line-graph construction is included. The reference run used an Apple M2 CPU, macOS 26.5.1, Python 3.12.4, and NetworkX 3.6.1. All algorithmic calls are single-process with library thread counts fixed to one. The benchmark runner records every repetition, seed, IQR, pre-call RSS, graph count, software version, and exact output digest. As with the other deterministic experiments, colors use 128 -bit digests, so equality has negligible rather than mathematically zero collision risk. 

Table 3: Fixed-n = 25 one-round cost sweep. Times are median seconds; four representative degrees are shown, while the exponent fit uses all six points with m ≥ 75 . RSS columns report absolute fresh-process peaks in MiB for one fixed graph. At the displayed degrees, (m, m L) equals 

(25 , 25) , (75 , 375) , (150 , 1650) , and (300 , 6900) , respectively. Explicit and implicit signatures agreed throughout.                                                   

> End-to-end one-round time [s] Peak RSS [MiB] Method States Entries/round d= 2 d= 6 d= 12 d= 24 d= 12 d= 24
> Root 3-WL n2n30.0138 0.0152 0.0160 0.0117 52.6 52.1 Root 4-WL n3n40.4926 0.5934 0.6698 0.3813 54.6 54.5 Explicit L(G) 2 -FWL m2m30.0142 0.2655 1.8221 12.8667 54.9 58.4 Implicit ILG-3-WL m2m30.0134 0.2693 1.7507 11.4022 55.3 60.8

Density-sweep results. For the six points with m ≥ 75 , an ordinary least-squares fit of log( time )

against log m gives slope 2.60 (95% CI [2 .18 , 3.03] , R2 = 0 .986 ) for explicit line refinement and 

2.60 ([2 .33 , 2.87] , R2 = 0 .995 ) for implicit ILG. These finite-range slopes describe the measured regime; the benchmark separately records exactly m3 replacement entries per round, which is the claimed combinatorial count. Root 3- and 4-WL retain fixed state and replacement counts throughout the sweep; their measured constants still vary with the color alphabet. The line-domain runtime crosses root 4-WL between m = 100 and m = 150 . At the SR25 scale (n, m ) = (25 , 150) , implicit ILG takes 2.61 times the one-round wall time of root 4-WL. 

Isolating structural storage. To separate graph representation from tuple state, we first fix m = 150 

and compare four connected roots with sharply different degree concentration. Endpoint-table preparation and explicit line-graph materialization are timed without refinement. Allocation peaks are traced separately after root generation, avoiding the interpreter baseline that would mask the small endpoint table. 

Table 4: Fixed-m = 150 structural representation cost. Each root has the same m2 = 22 ,500 

subsequent line-domain pair states. Times are medians of five calls after one warm-up; allocation is the peak traced Python heap in a separate fresh process. 

Endpoint table Explicit L(G)

Root n mL Time [ms] Peak [MiB] Time [ms] Peak [MiB] 

P151 151 149 0.099 0.026 0.510 0.058 Cubic 100 300 0.083 0.026 0.604 0.077 SR25 25 1,650 0.091 0.025 2.357 0.368 

K1,150 151 11,175 0.089 0.025 15.151 2.017 

The same structural-only experiment on Pm+1 and K1,m for m ∈ { 100 , 200 , 400 , 800 } gives endpoint-allocation slopes 0.97 on both families. Explicit materialization has slope 0.95 on paths, where mL = m − 1, and 1.86 on stars, where mL =  m

> 2

. At m = 800 , endpoint preparation takes 

38 Expressive Power of Implicit Line-Graph WL 

0.409 ms and 0.130 MiB on the star, versus 454 .7 ms and 59 .0 MiB for explicit materialization. Once the common pair refinement is included at m = 150 , however, the four implicit one-round times occupy only 1.38 –1.46 s and the explicit times 1.41 –1.76 s despite a 75 -fold range in mL. Thus the endpoint representation removes the structural term, but the m2 state and m3 replacement work dominate the full k = 3 computation. 

Stabilized SR25 case study. Finally, we run root 3-WL, root 4-WL, and ILG-3-WL to stabilization on all 15 SR25 graphs. For each graph and method, the reported time is the median of three calls after one warm-up; totals sum the 15 graph medians. Peak RSS is the maximum over 15 separate memory processes. 

Table 5: End-to-end stabilized cost on the 15 SR25 graphs, yielding 105 pairwise separation verdicts. All graphs have (n, m, m L) = (25 , 150 , 1650) .                      

> Method States Entries/round Separated Total time [s] Peak RSS [MiB] Root 3-WL 625 15,625 0/105 0.196 52.6 Root 4-WL 15,625 390,625 105/105 48.171 61.6 Implicit ILG-3-WL 22,500 3,375,000 105/105 230.442 67.4

On SR25, ILG has 36 times the root-3-WL state and 216 times its replacement count. Relative to root 

4-WL, it has 1.44 times the state and 8.64 times the per-round replacements; stabilized implicit ILG is 4.78 times slower and uses 1.09 times the peak RSS in this reference implementation. Root 4-WL and ILG-3-WL separate all 105 pairs. This SR25 cost comparison does not order their expressivity. Root 4-WL and ILG-3-WL are incomparable, so the higher ILG cost can still yield separations that root 4-WL misses (Remark 2). 

# F Neural Evaluation Protocols 

This appendix specifies the pair-trained neural baselines and the untrained ILG-3-GNN experiment in Table 1. The ILG-3-GNN architecture and run seed are fixed across the substructure-counting witness pairs, SR25, and BREC evaluations, and no parameter optimization is performed. 

Pair-trained rows. For locally computed pair-trained entries, we initialize and train a separate model for each graph pair and evaluation seed; parameters are not shared across pairs. Training uses a Siamese cosine-embedding loss with target −1 and Adam. The protocol uses q = 32 copies of each graph under different vertex orderings; here q is the number of copies. A pair counts as separated when its paired-comparison statistic exceeds the BREC threshold 72 .34 and its isomorphic-copy reliability statistic remains below that threshold [25]. For the controls, substructure-counting witness pairs, SR25, and ILG-1-GNN BREC results, seeds 

0, 1, 2 are used and a pair counts as separated when at least two seeds pass both checks. The two controls are an isomorphic copy and the Whitney pair K3, K 1,3. Each SR25 pair is trained and evaluated separately, and the table aggregates the 105 decisions. The locally compared state domains are root edges for ILG-1-GNN, unordered root 3-subsets for Morris 3-GNN, and ordered root-vertex pairs for PPGN [20, 21]. 

ILG-1-GNN. The ILG-1-GNN grid uses the pair-trained protocol on the controls, substructure-counting witness pairs, SR25, and BREC. None of the reported pairs is separated. This is consistent with the theoretical upper bound: under the canonical edge/line-vertex identification, the architecture is an MPNN on L(G) and is bounded by 1-WL on that graph. 

Morris 3-GNN on BREC. We use a fixed configuration with two shared full-replacement layers of width 64 , mean pooling over all unordered vertex triples, and seeds 100 , 200 , 300 . After verifying that every seed produces valid results for all 400 pairs, we select the seed with the largest number of separations; pairs from different seeds are not combined. The value 207 /400 is reported as a neural separation count rather than as equality with a computed pairwise set-3-WL bound. 

> 39 Expressive Power of Implicit Line-Graph WL

PPGN. The BREC value 233 /400 is sourced from Wang and Zhang [25, Table 2] under that paper’s protocol. The SR25 and substructure-counting witness-pair cells are computed locally using the pair-trained protocol above. The row therefore reports graph-pair separation under these source-specific protocols. 

Untrained ILG-3-GNN configuration. We fix run seed 500 and deterministically derive one fresh initialization for every substructure-counting witness pair, SR25 pair, and BREC pair. The same initialized parameters are used for both graph sides, with no parameter updates. The global-edge architecture maintains dense ordered root-edge-pair states and uses constant edge features, the invariant equality/incident/disjoint relation codes, width 32 , five replacement layers, a 16 -dimensional graph output, σ(x) = sin(5 x), and multiscale sum readout. A parameter-free layer normalization is applied to the graph output before distances are computed. Evaluation uses CPU float64 arithmetic and order-stabilized reductions. The inputs contain no vertex identifiers, edge identifiers, or exact refinement colors, and the single run seed is used throughout. 

Relabeling decision. For each graph, we construct four copies: the stored ordering and three independently relabeled copies. Thus q = 4 for the reported result. Let zGa , z Hb be their graph outputs and define 

B = min  

> a,b

∥zGa − zHb ∥2,WG = max  

> a,a ′

∥zGa − zGa′ ∥2, WH = max  

> b,b ′

∥zHb − zHb′ ∥2.

The pair is counted as separated only if 

B − max {WG, W H } − τ > 0, τ = √10 −7 · 72 .34 ≈ 2.6896 × 10 −3.

Here τ is the direct-ℓ2 floor induced by the BREC covariance ridge 10 −7 and decision threshold 

72 .34 . We use this direct ℓ2 criterion rather than the BREC RPC statistic. Every reported separation uses all four copies; the two-copy stage may reject a pair early. Nonfinite values and execution errors are treated as failures to separate. We apply the same configuration to the three substructure-counting witness pairs, the 105 SR25 pairs, and the 400 BREC main pairs; the rule separates 3/3, 105 /105 , and 359 /400 , respectively. Every evaluation yields valid finite outputs. After computing the neural decisions, we compare each suite with deterministic ILG-3-WL, and the pairwise verdicts coincide. The same protocol does not separate either the isomorphic C6 control or the Whitney pair K3, K 1,3. Since L(K3) ∼= L(K1,3) ∼= K3,every invariant line-graph model with the constant features used here must give the Whitney pair the same output. 

Dense execution. All reported ILG-3-GNN results use the dense ordered edge-pair state space. The implementation also supports exact-color state compression. It groups states with identical exact-refinement transition signatures while retaining the size of each group. For fixed weights, this is an execution optimization of the same recurrence; compression-group indices are not supplied as features, targets, or readout coordinates. The table therefore reports untrained separation counts under the rule above rather than BREC RPC scores or supervised-training results. Full configurations, source runners, tests, and reproduction commands are provided in the accompa-nying repository: https://github.com/lukeyf/ilg-wl .

# GenAI Usage Statement 

Generative AI was used to survey related work, to develop the detailed arguments of the proofs in Appendix A, to assist in constructing the Lean 4 formalization, to assist in designing and implementing the empirical complexity evaluation, and for language editing and code assistance. The choice of research direction and of proof strategy was made by the author. The author reviewed all AI-assisted material, and the formal development is checked by the Lean 4 kernel. The author takes responsibility for the contents of the paper and artifact.