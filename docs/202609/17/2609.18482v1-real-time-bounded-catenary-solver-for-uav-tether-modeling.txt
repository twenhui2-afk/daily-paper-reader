Title: Real-Time Bounded Catenary Solver for UAV Tether Modeling

URL Source: https://arxiv.org/pdf/2609.18482v1

Published Time: Thu, 17 Sep 2026 01:01:12 GMT

Number of Pages: 8

Markdown Content:
# Real-Time Bounded Catenary Solver for UAV Tether Modeling 

Max Beffert and Andreas Zell Cognitive Systems Group, University of T¨ ubingen, Germany 

max.beffert@uni-tuebingen.de 

Abstract — For non-stationary tethered multirotor UAVs in real-world conditions, simulating the forces imposed on the drone by the aerodynamic drag of the tether becomes crucial, with online use cases placing a hard bound on the maximum solve time. In previous work, a quasi-analytical catenary tether model reached a mean solve time of 0.51 ms using a general-purpose root finder, but without any worst-case guarantees or proven convergence. In this work, we reformulate the inner solver by reducing the catenary boundary-value problem to a single transcendental equation in one well-conditioned unknown. We derive a closed-form bracket and prove monotonicity and convexity as well as existence and uniqueness of the root, which together guarantee convergence of the solver. We further propose a two-regime initial guess which approximates the true root within 3.4 % and reduces the mean iteration count by 68 .0 % to 2.36 compared to the textbook initialization. Building on the hybrid root-finding method rtsafe (Newton-Raphson with bisection fallback giving bounded iteration counts), we implement a specialized variant that exploits the problem structure to omit unnecessary checks while retaining correctness, which gives up to 1.3 times speedup. With the proposed solver the full tether model achieves a nearly constant solve time of 6.9 μs on average and 7.7 μs at worst, a 

40 times speedup over an optimized re-implementation of the previous method, while agreeing with it to a relative deviation of 8.7 × 10 −9. Because the reformulation leaves the underlying physical model untouched, the experimental validation of the previous work carries over unchanged. We further demonstrate its suitability for embedded, resource-constrained platforms with a Lua implementation running directly in ArduPilot on a drone’s flight controller, where it stays well inside the scheduling budget with a mean solve time of 0.74 ms .

I. I NTRODUCTION 

A fundamental limitation of multirotor UAVs is their short flight time compared to helicopters and fixed-wing aircraft due to their lower efficiency. One promising approach to achieve continuous operation is to power the drone from the ground through a tether. This is already implemented in some practical applications but mainly for static operation in low wind scenarios. The reason for this limited scope is that the cable aerodynamics become relevant in cases with higher airspeed due to motion or wind. For precise system design, control, trajectory planning, and simulation, it is therefore crucial to estimate or measure the forces the tether imposes on the drone due to its weight and aerodynamic drag. In previous work [1], two complementary quasi-static tether models were proposed for this purpose, an analytical one based on catenary theory and a numerical one that discretizes the tether into segments and lumped masses. Both were validated against tension measurements on a real-world flight and shown to have close agreement with each other and the measurements. The analytical model is the fast path of that framework and the one intended for use in real-time scenarios, for example inside a control or estimation loop. However, the solver at its core had no convergence guarantees or upper bounds on runtime. It achieved an acceptable mean solve time of 

0.51 ms , but with a high variance, making it unsuitable for real-time use. Furthermore, it is questionable whether the solve speed and implementation would transfer well to the low computational budget of the drone’s flight controller. The contributions of this work are:  

> •

reduction of the catenary problem to a single transcen-dental equation in one well-conditioned unknown, with closed-form recovery of the remaining parameters,  

> •

proof of guaranteed convergence and bounded iteration count,  

> •

an initial guess that reduces the mean iteration count by 

68 .0 % compared to the textbook initialization,  

> •

a specialized solver achieving up to 1.3 times speedup over textbook rtsafe , 

> •

an evaluation on real flight data, with a nearly constant runtime showing a 40 times speedup over an optimized re-implementation of the previous method,  

> •

a Lua implementation running on the drone’s flight controller with limited resources. II. R ELATED WORK 

For a broader overview of tether modeling approaches refer to [1]. Here we recall only what is needed to place the present contribution, and refer to Table I for the overall positioning compared to other analytical and quasi-analytical methods. Purely analytical catenary models [2], [3] are fast but neglect aerodynamic forces entirely. Borgese et al. [2] avoid an implicit solve altogether by measuring the tether angle directly at both ends and recovering the shape parameter in closed form; we therefore omit it from Table I, since no solve is involved. Jain et al. [3] compute the tether shape between a series of drones. They state that the catenary parameters are solved numerically without specifying the method or reporting solve time or convergence behavior. Quasi-analytical models add drag under a uniformity as-sumption [1], [4]. Bigi et al. [4] solve the inner catenary shape with a fixed-point iteration and state that it converges for all possible values. They mention a runtime of below 1 s 

for the whole approach, but how much of that is taken up by the catenary solve is not stated, and no iteration bounds are given. arXiv:2609.18482v1 [cs.RO] 16 Sep 2026 Where the catenary boundary-value problem is judged too costly to solve online, some works avoid it differently: Talke et al. [5] note that the transcendental catenary equation admits no purely analytic solution and instead perform an offline brute-force sweep over a discretized grid of relative positions and tether lengths, fitting the resulting tension, length, and departure angle to low-order polynomials. Numerical quasi-static formulations [1], [6], [7] and fully dynamic ones [8], [9] resolve per-segment drag and, in the dynamic case, transient behavior, at a computational cost that grows with the discretization. Beffert et al. [1] showed via real-world validation that quasi-analytical methods have close agreement with numerical quasi-static methods for common cases of tethered drones at reduced computational cost. Most of the literature does not target fast runtime, and solve time figures are therefore often not provided. Further-more, we are not aware of any previous work that gives real-time guarantees such as a proven convergence and a bound on iteration count. 

A. Previous Method 

This work improves the analytical model proposed in [1], which rests on the observation that a quasi-static tether follows the catenary curve in a uniform potential field. By assuming that the aerodynamic load acts equally along the whole tether, the drag force and tether weight can be combined into a single unified potential field. Therefore, the shape follows the catenary curve, but in a frame rotated such that the potential field points downward. To obtain the tether shape, the endpoints are rotated into that frame, solved there, and the sampled shape and tensions are rotated back. The total drag depends on the exposed area of the ca-ble, which in turn depends on the vertical length of the tether, which is itself an output of the solve. Therefore, the algorithm starts from the vertical distance |∆y| (the case without any sag) and refines it iteratively by solving the shape and recomputing the vertical length until it converges. For scenarios typically observed with tethered drones, the first estimate is already close and the loop converges quickly. At the core of the method sits a numerical solver that finds the catenary parameters that describe the cable shape for a given configuration of endpoint coordinates and tether length. The constraint equations ensure that the endpoints lie on the catenary and that the arc length matches the tether length. In [1] this system of three coupled equations is handed to 

scipy.fsolve , a general-purpose Newton-type root finder. To achieve successful solves over a wide range of conditions, multiple sets of initial guesses based on parabolic approx-imation and heuristics have to be tried until one succeeds. The cost of these guesses was somewhat counteracted by statistical analysis trying ones first that are more likely to succeed, but it remained a source of solve time variance. The approach was demonstrated to work in practice, but for the solver used no convergence guarantees or runtime bounds could be given. The present work replaces only the inner catenary solver with one where these guarantees can be given and proven but leaves the surrounding formulation, including the rotated-frame and drag-estimation outer loop, unchanged. III. M ETHODS 

A. Analytical Problem Formulation 

The cable follows a catenary curve given by 

y(x) = a cosh  (x − x0)/a  + y0. (1) Therefore, the tether shape is fully determined by the scale parameter a > 0 and the vertex coordinates (x0, y 0). Deter-mining these three unknowns requires three constraints: both endpoints lie on the curve and the arc length between them is equal to the cable length L. As shown in Appendix A, com-bining these equations and simplifying reduces the system to 

L2 − ∆y2 = 4 a2 sinh 2 b, b := ∆x

2a . (2) Substituting the definition of b, taking the positive square root, and considering a > 0 gives 

g(a) = 2 a sinh 

 |∆x|

2a



− pL2 − ∆y2 = 0 . (3) While (3) is a standard reduction of the catenary problem [10], it is numerically unfavorable for root-finding:  

> •

a ranges over the unbounded interval (0 , ∞), which is unsuitable for bracketing methods  

> •

the argument |∆x|/2a diverges as a → 0, causing 

sinh( ·) to diverge  

> •

as a → ∞ the derivative of g vanishes, stalling Newton-type solvers Instead, we solve for the unsigned quantity ˆb := |∆x|/2a,by inverting the substitution and eliminating a instead of b:

g(ˆb) = |∆x| sinh ˆb

ˆb − pL2 − ∆y2 = 0 , (4) which matches (12) in Appendix A.3. Unlike a, ˆb is well suited for root finding:  

> •

ˆb is confined to a finite interval with analytical bounds  

> •

no singularity: sinh( ˆb)/ˆb → 1 as ˆb → 0 

> •

the derivative of g remains well-behaved over the whole domain We therefore solve (4) for ˆb and recover a, x0, and y0 in closed form as detailed in Appendix A.4. 

B. Well-posedness Guards 

Two conditions must hold for (4) to admit a root, and both are checked before the solver is entered. First, the cable must be strictly longer than the straight-line distance between the endpoints, L > p∆x2 + ∆ y2 + ε with ε =10 −6 m. A taut or over-stretched cable is outside the scope of the inextensible catenary model and is rejected rather than approximated. Second, the horizontal span must not be degenerate. For 

∆x = 0 , eq. (4) collapses to 0 = pL2 − ∆y2, which the first condition rules out. We therefore require |∆x| > ε .TABLE I: Comparison of analytical tether modeling approaches. For real-time use, guaranteed convergence, bounded runtime, and low latency are desired.                            

> Work Method Drag Guaranteed Convergence Bounded Runtime Low-Latency Jain et al. [3] analytical no not stated not stated not stated Bigi et al. [4] quasi-analytical yes yes (no proof) no not stated ( 1 s full method) Beffert et al. [1] quasi-analytical yes no no yes ( 0.51 ms )
> Ours quasi-analytical yes yes (proven) yes yes ( 6.9μs)

Note that the length condition is invariant under the frame rotation for drag and only needs to be tested once, but ∆x

is measured in the rotated frame and is re-tested at every iteration of the outer loop. Dividing (4) by |∆x| leaves sinh( ˆb)/ˆb = r with r := pL2 − ∆y2/|∆x|, so the geometry enters only through r.The first condition keeps it strictly above 1, the second below 

rmax := L/ε = 3 × 10 7 for our 30 m tether. It measures excess length relative to horizontal span: r → 1 is a taut cable, while large r arises from slack, a small horizontal span, or both. 

C. Solver 

Equation (4) is solved using rtsafe , a hybrid root-finding method introduced in Numerical Recipes [11] that combines the guaranteed convergence of bisection with the fast local convergence of Newton’s method. The algorithm maintains a bracket [ˆblo , ˆbhi ] known to contain the root, and refines it iteratively. At each iteration, a candidate step is proposed using the Newton update 

ˆbn+1 = ˆbn − g(ˆbn)/g ′(ˆbn). (5) This step is accepted only if it remains within the current bracket and shrinks it at a sufficient rate. Otherwise, the algorithm falls back to a bisection step, ˆbn+1 = ( ˆblo +ˆbhi )/2.At the end of every iteration the function is evaluated at the new point to determine whether ˆbn+1 becomes the new upper or lower bracket bound. Because the bracket always contains the root (Ap-pendix D) and its width strictly decreases, rtsafe cannot diverge or oscillate the way standard Newton can when the starting point is poor. Convergence is therefore guaranteed. For real-time use, it is also important to have an upper bound on how many iterations are necessary to converge. Let ∆n := |ˆbn+1 − ˆbn| denote the length of the step taken at iteration n, whether it was a Newton or a bisection step. A proposed Newton step is then accepted only if 

gn

g′

> n

≤ ∆n−2

2 , (6) i.e., if it is at most half as long as the step taken two iterations earlier. The step length is therefore halved at least every two iterations, giving an explicit worst-case bound on the number of iterations needed to reach a step-length tolerance εb with 

Nmax = 2 ⌈log 2(ˆbhi /ε b)⌉. With our choice of εb = 10 −4

(Sec. IV-B) this gives Nmax = 34 for r = 100 and Nmax =44 even at rmax . In practice the bound is never approached. Over a logarithmic sweep of 20 000 values of r ∈ (1 , r max ]

the solver required at most three iterations and never fell back to a bisection step. We can therefore infer that the initial guess lands in the quadratic-convergence regime. Newton-Raphson by itself is already guaranteed to con-verge for g(ˆb) since it is monotonic and convex (Appendix B) [12], but without any guarantees of how many iterations are necessary. What rtsafe adds is an upper bound for the iterations needed to converge. This makes it suitable for real-time applications, where bounded worst-case runtime is crucial, not just the average solve time. Solving the catenary with rtsafe requires providing valid bounds for ˆb. As previously discussed, ˆb is positive, so 0

is used as the lower bound. The upper bound is determined analytically (Appendix E) with r := pL2 − ∆y2/|∆x|:

ˆbhi =

q

−10 + p100 + 120( r − 1) . (7) This bound stays close to the true root, so the number of bisection steps compared to a perfect bracket is below half a step for r ≤ 100 and below four even at rmax 

(Appendix E.1). In standard rtsafe , the middle of the bracket is used as the initial guess for the first Newton step. This works, but is not optimal, since Newton-Raphson only converges quadratically close to the root, so the quality of the guess strongly impacts solve time. Instead, an analytical guess is used to initialize the first Newton step. For small ˆb, the upper bound (7) serves as an accurate initial guess; for large ˆb, we derive a Lambert 

W approximation in Appendix F, with Λ := ln(2 r). The optimal regime boundary at 3.77 was determined numerically (Sec. IV-A). 

ˆb ≈

q

−10 + p100 + 120( r − 1) , r < 3.77 ,

Λ + ln Λ + ln Λ /Λ, r ≥ 3.77 .

(8) Since the Lambert W approximation is not guaranteed to underestimate the root, we clamp the guess to [0 , ˆbhi ].While the problem can be solved with textbook rtsafe , it is possible to remove some unnecessary checks by using spe-cific function knowledge. The main optimization is removing the bracket validation, which confirms that a sign change occurs within the bracket in standard rtsafe . In this case the bracket is always valid because it is calculated analytically (Appendix E). This is significant since the bracket validation requires two costly evaluations of g(ˆb).Furthermore, when deciding whether a Newton step can be accepted, checking whether it falls below the lower bracket is not necessary. This follows from the monotonic convex 2 4 6 8 10 12 

r

0246810 relative error of  ̂  b0 [%]       

> r∗= 3.77 3.4 %
> √−10 + √100 + 120( r− 1) Λ + ln Λ + ln Λ/Λ

Fig. 1: Relative error of the two initial guesses (8) compared to the actual root. The curves cross at r∗ = 3 .77 at a relative error of 

3.4 % , which is where we place the regime boundary. 

TABLE II: Number of iterations and ratio of rtsafe compar-ing the textbook initialization at the bracket midpoint against our analytic guess, evaluated on Flight 1’s 266 solves. The analytic guess reduces the mean iteration count by 68 .0 % 

and is never worse on any individual solve.             

> Initialization Mean Median Max Midpoint 7.38 711
> Analytic 2.36 33
> Ratio 3.122 ×2.33 ×3.67 ×

nature of the function and is shown in detail in Appendix C. This is not an expensive check, but it happens every iteration, so the savings add up with higher iteration counts. IV. E XPERIMENTS AND RESULTS 

All desktop measurements were taken on an Intel Core i7-9700 CPU. The embedded results come from a Pixhawk 6C flight controller running ArduPilot V4.6.3. We use two real flights, Flight 1 from [1], which comprises 170 tether config-urations ( 266 inner-loop solves) with r ranging from 1.050 

to 2723 .957 (median 2.008 ), and Flight 2 , which comprises 

4875 configurations. 

A. Initial Guess 

Fig. 1 shows the accuracy as a sweep over r for both initial-guess approximations of ˆb. As an initial guess, the reused upper bound is exact in the limit r → 1 and its error grows with r, while the Lambert W form tightens as r grows. We use the intersection at r∗ = 3 .77 as the regime boundary, which minimizes the worst-case error to 3.4 % . Furthermore, Table II shows that using the analytical guess reduces the average rtsafe iterations by 68 .0 % compared to the textbook implementation that uses the bracket center. 

B. Choice of the rtsafe Tolerance 

The termination criterion for rtsafe is the step length falling below a tolerance threshold, ∆n < εb. This is effectively a bound on how close the found ˆb is to the actual root. Each iteration costs one evaluation of g and g′, and with a mean of only 2.36 iterations (Table II), every additional TABLE III: Sweep of the rtsafe step tolerance εb on Flight 1’s 266 solves, using 64-bit double precision. Errors are the deviation of the endpoint force from the reference setting εb = 10 −14 ; the residual is the arc-length error of the recovered catenary. No case failed to converge at any tolerance.                                                                                      

> Force error [ N]Residual [ m]
> εbMean Max Mean Max
> 10 −14 ——1.3×10 −14 7.8×10 −14
> 10 −10 2.1×10 −16 4.4×10 −15 1.3×10 −14 7.8×10 −14
> 10 −88.9×10 −16 9.0×10 −15 1.3×10 −14 7.8×10 −14
> 10 −65.9×10 −14 1.5×10 −12 4.8×10 −13 1.1×10 −11
> 10 −53.9×10 −12 5.6×10 −11 3.1×10 −11 4.9×10 −10
> 10 −42.3×10 −92.1×10 −84.2×10 −96.1×10 −8
> 10 −31.9×10 −71.7×10 −61.5×10 −61.2×10 −5

TABLE IV: Comparison of total solve time in ns and ratio between textbook and specialized rtsafe implementations, compiled (C, Numba) and interpreted (Python, Lua 5.3). Minimum over 25 interleaved batches of 20 000 solves (C, Numba) or 4000 solves (Lua, Python) for a typical example with 4 iterations, evaluated on a desktop CPU. We observe higher speedup in interpreted languages due to per-dispatch costs and fewer opportunities for compiler optimization.                

> C (gcc) Numba Lua Python Textbook 187 .3193 .01561 2314
> Specialized 169 .9182 .21234 1775
> Ratio 1.102 ×1.059 ×1.265 ×1.304 ×

digit of precision is expensive. In the quadratic-convergence regime one extra evaluation buys about four decimal digits, so tightening εb from 10 −4 to 10 −8 increases the mean iteration count by roughly 34 % .A threshold below the resolution of the floating-point format can never be met, since the iteration step ∆n is a difference of two representable numbers and is therefore either zero or at least one unit in the last place. ArduPilot’s Lua interpreter uses 32-bit floats, where the 23-bit mantissa gives a resolution of 2−23 ≈ 10 −7, so any tolerance below that cannot be reached. The 64-bit doubles used in Python have a 52-bit mantissa and thus give a resolution of 10 −16 .In cases where the threshold is chosen below the floating-point precision, rtsafe still converges but will not achieve the desired tolerance and might waste iterations that do not improve the accuracy. Table III shows a sweep over different tolerance values. We use εb = 10 −4, which has sufficient accuracy while ensuring lower iteration counts and avoiding issues with floating-point precision. 

C. Runtime Cost of One Catenary Solve 

As shown in Table IV, the specialization gives a speedup of 1.102 in C and 1.059 with the Numba JIT. On the given example with 4 iterations it reduces the number of function evaluations from 6 to 4, so a larger difference might be TABLE V: Breakdown of one catenary solve, textbook vs. specialized rtsafe , each as a percentage of that language’s textbook total (Python: 2314 ns , Lua: 1561 ns ). Evaluated for a typical example with 4 iterations on a desktop CPU. We observe that due to per-dispatch cost the function evaluation does not dominate the runtime.                          

> Python Lua Component Textbook Spec. Textbook Spec. Bracket validation 17 .5 % —15 .7 % —Function evaluation 32 .2 % 32 .2 % 27 .6 % 27 .6 %
> Iteration step 24 .2 % 20 .3 % 34 .5 % 30 .1 %
> Remainder 26 .1 % 24 .2 % 22 .2 % 21 .4 %
> Total 100 .0 % 76 .7 % 100 .0 % 79 .1 %

expected. We see two plausible reasons why compiled code benefits less. First, the lower bracket is the constant 0, so the compiler can precompute sinh(0) and cosh(0) , leaving only cheap arithmetic for the runtime evaluation of g(0) . Second, the bracket validation can be overlapped with the following Newton steps by the out-of-order processor to hide part of its latency. In interpreted languages every operation incurs a dispatch overhead that dominates its cost, so the number of operations translates more directly into runtime, which is consistent with the larger speedups for Python and Lua. All the optimizations performed by the compiler and the processor pipeline make it difficult to break down the runtime of different components of the algorithm. Therefore, to measure the individual parts, Table V uses Python and Lua, which are interpreted languages, so the total solve time can be assumed to be the sum of the solve times of the parts. This measurement was done by replacing parts of the algorithm with variable lookups. Additionally, the variable lookups were timed by themselves to remove their overhead. As expected we see a bigger speedup where the optimized version differs from textbook rtsafe . Another interesting observation is that due to the interpreter overhead the function evaluation no longer dominates the runtime. 

D. Runtime of the Full Tether Simulation 

Figure 2 shows a comparison between the previous and proposed method on Flight 1. Both converge on all 170 

configurations, and their tension profiles agree to a relative deviation of 8.7 × 10 −9. Note that we are comparing against an optimized version of the previous method that is already 

1.9 times faster than the runtime stated in [1]. The proposed formulation solves in 6.9 μs on average, against 275 .9 μs

for the baseline: a speedup of 40 times, or 74 times over the published 0.51 ms . The standard deviation drops from 

136 .9 μs, where it depends on how many initial guesses were tried before convergence, to 0.3 μs, and the worst case of 

7.7 μs still beats the fastest baseline solve. For a real-time solver, such a bounded worst case matters as much as the average cost. Figure 3 shows the Lua implementation running directly in ArduPilot on the drone flight controller during Flight 2. Every one of its 4875 configurations converged, with a mean Baseline Proposed 10 1   

> 10 2
> 10 3
> Solve Time [μs] μ=275.9 μs μ=6.9 μs 40×
> Mean Median IQR (25-75%) Whiskers (1.5×IQR)
> Fig. 2: Comparison between the solve time of the previous and proposed method on Flight 1 ( 170 configurations), evaluated on a desktop CPU running in Python with Numba. Both converge on the same result in all cases and the proposed method shows mean speedup of 40 times with a nearly constant runtime. Note the log scale.

solve time of 0.74 ms and a worst case of 3.58 ms , which is inside the 100 ms budget of the 10 Hz scheduling loop. This is also reflected by the dispatch interval, which stays within 3.06 ms of the 100 ms nominal period. This indicates that the script is never aborted or skipped by the scheduler. The elevated jitter in the dispatch interval lines up with the solve time jitter, without a corresponding change in iteration count, ruling out higher computational cost as the cause. We therefore conclude that the solve time jitter stems from the way Lua is run on ArduPilot rather than from higher computational cost of the algorithm. The real-time operating system schedules the Lua interpreter as a low-priority task, so it can be interrupted by more important processes and resumed afterward. The additional time is therefore spent waiting for other tasks to complete, so the processor can be used again to finish the simulation. V. C ONCLUSION 

In this work, the catenary solved at the core of the quasi-analytical tether model of [1] is reformulated to a single transcendental equation in the unsigned quantity ˆb, with the remaining catenary parameters recovered in closed form. Unlike the shape parameter of the catenary, ˆb is confined to a finite interval, free of singularities, and has a well-behaved derivative throughout its domain. Building on this formulation, a closed-form upper bracket is derived and its tightness proven. Furthermore, we prove that the reduced formulation is both monotonically increasing and convex, thus guaranteeing convergence. The problem is solved via a simplified implementation of 

rtsafe , a hybrid root-finding method comprised of Newton-Raphson steps with a fallback to bisection, which gives a guaranteed upper bound to the iteration count. Exploiting the proven monotonicity and convexity, the specialized version omits unnecessary checks while retaining correctness and 01234

> Solve Time [ms] mean = 0.74 ms

0510 Total Iterations 

0 100 200 300 400 500 Flight time [s] 100 102 104 Dispatch Interval [ms] Fig. 3: Telemetry from the Lua script running in ArduPilot on the drone flight controller during Flight 2. All 4875 configurations converged. Elevated jitter in the dispatch interval (target 10 Hz )coincides with solve time jitter at constant iteration count, indicating interruptions by the real-time operating system. 

achieving up to 1.3 times speedup over textbook rtsafe . Fur-thermore, a two-regime initial guess was proposed, consisting of the reused upper bound and a Lambert W approximation. It was shown to reduce the mean iteration count by 68 .0 % to 

2.36 by achieving a worst-case approximation error of 3.4 % ,ensuring fast convergence. Effectively these improvements result in a solve time of 

6.9 μs on average and 7.7 μs at worst for the full tether model. Compared to an average of 275 .9 μs with the previous method, this is a 40 times speedup. The standard deviation drops from 136 .9 μs to 0.3 μs, making the runtime essentially constant, which is an important factor for real-time use. The two formulations agree to a relative deviation of 8.7 × 10 −9;therefore, the correctness claims and experimental validation from [1] are unaffected. The guarantees cover the inner solve; the unchanged outer drag loop is capped at 10 iterations and never exceeded 3 in our data, but is not formally guaranteed to converge. Because the tolerance is chosen against the platform’s floating-point resolution, and because the custom rtsafe 

needs only basic math operations, the solver ports easily to different platforms. This was demonstrated by writing a Lua implementation that runs directly in ArduPilot on the drone’s flight controller. It achieved a mean runtime of 0.74 ms 

during flight, successfully solving all cases and staying within the budget of the scheduling loop, demonstrating its real-time capability. The assumptions of the underlying model are unchanged: the tether is inextensible with uniform mass and drag dis-tribution along its length, and the configuration is quasi-static and planar. While we implement the 2D case, under the given assumptions the solution stays planar in 3D [13], which means the proposed improvements extend to that as well. Cases where higher physical fidelity is required remain the domain of the numerical model of [1], so the two methods continue to complement each other. Future work includes applying the model within online wind es-timation, model predictive control, and software-in-the-loop simulation, where its strength of bounded worst-case runtime provides the most benefit. The implementation will be made publicly available upon publication of this work. REFERENCES [1] M. Beffert and A. Zell, “Low-Latency Quasi-Static Modeling of UAV Tether Aerodynamics,” in 2026 International Conference on Unmanned Aircraft Systems (ICUAS) , Jun. 2026, pp. 1231–1238. DOI: 10.1109/ICUAS69441.2026.11598598 [2] A. Borgese, D. C. Guastella, G. Sutera, and G. Muscato, “Tether-Based Localization for Cooperative Ground and Aerial Vehicles,” 

IEEE Robotics and Automation Letters , vol. 7, no. 3, pp. 8162–8169, Jul. 2022. DOI: 10.1109/LRA.2022.3187504 [3] K. P. Jain, P. Kotaru, M. d. Sa, K. Sreenath, and M. W. Mueller, “Tethered Power for a Series of Quadcopters: Analysis and Applications.” arXiv, Sep. 2022. DOI: 10.48550/arXiv.2203.08180 [4] N. Bigi, A. Nˆ eme, K. Roncin, J.-B. Leroux, G. Bles, C. Jochum, and Y. Parlier, “Analytical Tether Model for Static Kite Flight,” in Airborne Wind Energy , R. Schmehl, Ed. Singapore: Springer Singapore, 2018, pp. 57–78. ISBN 978-981-10-1947-0 Series Title: Green Energy and Technology. DOI: 10.1007/978-981-10-1947-0 3 [5] K. A. Talke, M. De Oliveira, and T. Bewley, “Catenary Tether Shape Analysis for a UAV - USV Team,” in 2018 IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS) , Oct. 2018, pp. 7803–7809. DOI: 10.1109/IROS.2018.8594280 [6] J. Koenemann, P. Williams, S. Sieberling, and M. Diehl, “Modeling of an airborne wind energy system with a flexible tether model for the optimization of landing trajectories,” IFAC-PapersOnLine , vol. 50, no. 1, pp. 11 944–11 950, Jul. 2017. DOI: 10.1016/j.ifacol.2017.08.1037 [7] M. Zanon, S. Gros, J. Meyers, and M. Diehl, “Airborne Wind Energy: Airfoil-Airmass Interaction,” IFAC Proceedings Volumes , vol. 47, no. 3, pp. 5814–5819, Jan. 2014. DOI: 10.3182/20140824-6-ZA-1003.00258 [8] E. Dicembrini, M. Scanavino, F. Dabbene, and G. Guglieri, “Modelling and simulation of a tethered UAS,” in 2020 International Conference on Unmanned Aircraft Systems (ICUAS) . Athens, Greece: IEEE, Sep. 2020, pp. 1801–1808. DOI: 10.1109/ICUAS48674.2020.9214006 [9] F. Muttin, “Umbilical deployment modeling for tethered UAV detecting oil pollution from ship,” Applied Ocean Research , vol. 33, no. 4, pp. 332–343, Oct. 2011. DOI: 10.1016/j.apor.2011.06.004 [10] M. Beitelschmidt, “The Catenary Line: Numerical Aspects and Solutions for Special Boundary Conditions,” Proceedings in Applied Mathematics and Mechanics , vol. 25, no. 3, 2025. DOI: 10.1002/pamm.70016 [11] W. H. Press, S. A. Teukolsky, W. T. Vetterling, and B. P. Flannery, 

Numerical Recipes 3rd Edition: The Art of Scientific Computing ,3rd ed. USA: Cambridge University Press, Aug. 2007. ISBN 978-0-521-88068-8. [Online]. Available: https://numerical.recipes/ [12] L. Thorlund-Petersen, “Global convergence of Newton’s method on an interval,” Mathematical Methods of Operations Research , vol. 59, no. 1, pp. 91–110, Jan. 2004. DOI: 10.1007/s001860300304 [13] B. Chakrabarti and J. A. Hanna, “Catenaries in viscous fluid,” 

Journal of Fluids and Structures , vol. 66, pp. 490–516, Oct. 2016. DOI: 10.1016/j.jfluidstructs.2016.04.009 [14] R. M. Corless, G. H. Gonnet, D. E. G. Hare, D. J. Jeffrey, and D. E. Knuth, “On the LambertW function,” Advances in Computational Mathematics , vol. 5, no. 1, pp. 329–359, Dec. 1996. DOI: 10.1007/BF02124750 APPENDIX A Catenary Reduction to One Unknown The curve is a catenary of the form 

y(x) = a cosh  (x − x0)/a  + y0.

Given two endpoints p1 = ( x1, y 1), p2 = ( x2, y 2) on the curve and a prescribed arc length L between them, the goal is to determine the three unknowns: the scale parameter a > 0

and the vertex coordinates (x0, y 0).A.1 Substitution of Normalized Coordinates The arc length L is the sum of the signed arc lengths from the vertex to p1 and p2,

L = a sinh 

 x2 − x0

a



− a sinh 

 x1 − x0

a



.

Introducing the normalized coordinates 

u1 = x1 − x0

a , u2 = x2 − x0

a

this becomes L = a   sinh u2 − sinh u1

. Similarly, the vertical separation is 

∆y = y2 − y1 = a   cosh u2 − cosh u1

.

A.2 Elimination of the Midpoint Parameter Applying the addition theorems 

sinh( s ± d) = sinh s cosh d ± cosh s sinh d, 

cosh( s ± d) = cosh s cosh d ± sinh s sinh d, 

gives the formula as 

L = 2 a cosh 

 u1 + u2

2



sinh 

 u2 − u1

2



,

∆y = 2 a sinh 

 u1 + u2

2



sinh 

 u2 − u1

2



.

With the substitutions 

m := u1 + u2

2 ,b := u2 − u1

2 = x2 − x1

2a = ∆x

2a ,

these read 

L = 2 a cosh m sinh b, (9) 

∆y = 2 a sinh m sinh b. (10) Squaring and subtracting eliminates m via the identity 

cosh 2 m − sinh 2 m = 1 , leaving 

L2 − ∆y2 = 4 a2 sinh 2 b. (11) A.3 Reduction to a Single Unknown Substituting a := ∆x 

> 2b

turns the previous relation into 

L2 − ∆y2 = 4 

 ∆x

2b

2

sinh 2 b =

 ∆xb

2

sinh 2 b. 

Taking the positive square root ( ˆb denotes the unsigned b)yields a single transcendental equation in one unknown, 

g(ˆb) = |∆x| sinh ˆb

ˆb − pL2 − ∆y2 = 0 . (12) A.4 Recovery of the Remaining Parameters Once (12) is solved for ˆb, the original three unknowns follow in closed form. Because of the positive square root, (12) only gives ˆb as the magnitude of b, so the sign must be recovered from ∆x:

a = ∆x

sgn(∆ x) 2 ˆb = |∆x|

2ˆb . (13) Dividing (10) by (9) gives tanh( m) = ∆ y/L . Since the algebraic L in this ratio can be negative, using the prescribed positive L instead yields the unsigned ˆm,

ˆm = atanh 

 ∆yL



.

With u1 = m − b and the recovered sign, 

u1 = sgn(∆ x) ( ˆ m − ˆb),

and finally 

x0 = x1 − a u 1, (14) 

y0 = y1 − a cosh u1. (15) This completes the recovery of the unknowns (a, x 0, y 0).B Monotonicity and Convexity of g(ˆb)

We can write g(ˆb) = |∆x| f (ˆb) − C with constants C and 

|∆x|, where f (ˆb) := sinh( ˆb)/ˆb. Then the monotonicity and convexity of g on (0 , ∞) follow directly from those of f .The hyperbolic sine has the standard Taylor expansion 

sinh ˆb =

> ∞

X

> n=0

ˆb 2n+1 

(2 n + 1)! = ˆb + ˆb3

3! + ˆb5

5! + · · · 

Dividing the series term-by-term by ˆb (for ˆb̸ = 0 ) lowers each exponent by one, resulting in the series: 

f (ˆb) = sinh ˆb

ˆb = 1 + ˆb2

3! + ˆb4

5! + · · · =

> ∞

X

> n=0

ˆb 2n

(2 n + 1)! =

> ∞

X

> n=0

cnˆb2n, cn := 1(2 n + 1)! > 0

Differentiating termwise, 

f ′(ˆb) = 

> ∞

X

> n=1

2n c n ˆb2n−1,f ′′ (ˆb) = 

> ∞

X

> n=1

2n(2 n − 1) cn ˆb2n−2.

Every coefficient cn is positive, and for ˆb > 0 every power 

ˆb2n−1 and ˆb2n−2 is nonnegative, so both series consist entirely of nonnegative terms: 

f ′(ˆb) > 0 and f ′′ (ˆb) > 0 for all ˆb > 0. (16) Hence f , and therefore g, is strictly increasing and strictly convex on (0 , ∞).C Correctness of the Simplified Bracket Check Our implementation omits the check from standard rtsafe 

that ensures a Newton step never goes below the lower bracket bound. We show that the lower bracket is never violated, given the convexity and monotonicity established in Appendix B. 

a) Lemma: Let g be convex and strictly increasing on 

(0 , ∞), and let ˆb∗ ∈ (0 , ∞) be its root. For any ˆbn > 0, the Newton step ˆbn+1 = ˆbn − g(ˆbn)/g ′(ˆbn) satisfies ˆbn+1 ≥ ˆb∗.

b) Proof: Convexity means the tangent line at ˆbn lies below g everywhere: 

g(x) ≥ g(ˆbn) + g′(ˆbn)( x − ˆbn) for all x > 0.

Evaluating at x = ˆb∗ and using g(ˆb∗) = 0 ,

0 ≥ g(ˆbn) + g′(ˆbn)( ˆb∗ − ˆbn).

Since g′(ˆbn) > 0 by monotonicity, dividing by g′(ˆbn)

preserves the inequality and we can rearrange to: 

ˆb∗ ≤ ˆbn − g(ˆbn)

g′(ˆbn) = ˆbn+1 . (17) Since the bracket invariant guarantees ˆb∗ ≥ ˆblo at every step, the lemma gives ˆbn+1 ≥ ˆb∗ ≥ ˆblo for any positive iterate. The Newton step can therefore never undershoot past ˆblo .D Existence of a Unique Root Since g is strictly increasing on (0 , ∞) (Appendix B), it has at most one root in the interval. It remains to show that 

g changes sign across the domain, so a root exists. As ˆb → 0+, f (ˆb) → 1, so g(0 +) = |∆x| − C. By the first well-posedness guard of Sec. III-B, L2 > ∆x2 + ∆ y2,which gives C2 = L2 − ∆y2 > ∆x2 and thus C > |∆x|.Consequently g(0 +) < 0. Since f (ˆb) → ∞ as ˆb → ∞ , so does g(ˆb). There must therefore be a zero crossing in the interval (0 , ∞). Combined with the injectivity established above, this shows that the root exists and is unique. E Upper Bound for ˆb

We seek a closed-form equation for ˆbhi with g(ˆbhi ) ≥ 0,i.e., f (ˆbhi ) ≥ r, where r := pL2 − ∆y2/|∆x|. Recall from Appendix D that well-posedness requires C > |∆x|, thus 

r > 1, which is used throughout this derivation. Since every coefficient cn in the series 

f (ˆb) = P∞ 

> n=0

cnˆb2n is positive (Appendix B), truncating the series only discards positive values. Therefore any partial sum is a lower bound on f for ˆb ≥ 0. Truncating after the quartic term gives 

f (ˆb) ≥ h(ˆb) := 1 + ˆb2

6 + ˆb4

120 for all ˆb ≥ 0. (18) We use this approximation to find ˆbhi , so we solve h(ˆb) = r.Substituting w := ˆb2 turns this into a quadratic, 

w2

120 + w

6 +(1 −r) = 0 ⇐⇒ w2 +20 w+120(1 −r) = 0 ,

with solution 

w = −10 + p100 + 120( r − 1) .

The other root is negative for r > 1 and hence discarded, since w = ˆb2 ≥ 0 is required. Taking the positive square root gives the closed-form bound 

ˆbhi = √w =

q

−10 + p100 + 120( r − 1) . (19) In practice, we scale this by a factor of (1 + 10 −6) to guard against floating-point round-off, ensuring g(ˆbhi ) > 0 strictly rather than borderline. E.1 Tightness of the Bound As r → 1+ the root ˆb∗ → 0, so the discarded higher-order terms vanish quickly and the bracket becomes exact. The relevant regime is therefore large r, where the quartic term of (18) dominates and gives ˆbhi ∼ (120 r)1/4, while the root grows only logarithmically, ˆb∗ ∼ ln(2 r) (Appendix F.1). The bracket thus loosens as r1/4/ ln r. Because bisection to a tolerance of εb needs log 2(ˆbhi /ε b) steps, the penalty compared to a perfect bracket is log 2(ˆbhi /ˆb∗), which is below half a step for r ≤ 100 and below four steps even at rmax .F Initial Guess for the Root of g(ˆb)

As in Appendix E, write r := pL2 − ∆y2/|∆x| > 1, so that the root satisfies f (ˆb) = r with f (ˆb) = sinh( ˆb)/ˆb. For small ˆb, the upper bound ˆbhi (19) is itself a tight estimate of the root and is reused directly as the initial guess. For large 

ˆb we derive a closed-form approximation below. F.1 Large-ˆb Regime For ˆb ≫ 1, the e−ˆb term in sinh ˆb/ ˆb = ( eˆb − e−ˆb)/2ˆb is exponentially negligible, giving the approximation 

f (ˆb) ≈ eˆb

2ˆb .

Solving f (ˆb) = r for ˆb,

eˆb

2ˆb = r ⇐⇒ (−ˆb) e−ˆb = − 12r ,

matches the defining relation z = W (z) eW (z) of the Lambert W function, with z = −1/(2 r) and W (z) = −ˆb.Recall from Appendix D that well-posedness gives r > 1

and therefore z ∈  − 12 , 0 ⊂  − 1 

> e

, 0, which is the interval on which W has two real branches. Since W0(0) = 0 , the principal branch yields ˆb → 0 as z → 0− and is therefore inconsistent with the large-ˆb regime. We instead take the 

W−1 branch, which gives ˆb = −W−1(−1/(2 r)) . Its standard asymptotic expansion as z → 0−, with Λ := ln(2 r) [14], gives 

ˆb ≈ Λ + ln Λ + ln Λ /Λ (r large ). (20)