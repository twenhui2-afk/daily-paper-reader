---
title: "AgenTeeth: A Model-Agnostic Framework for Suppressing Hallucination in Frozen Vision-Language Models on Dental X-Rays via Tool Evidence Injection"
source: arxiv
title_zh: 用于Suppressing Hallucination in Frozen Vision-Language 模型s on Dental X-Rays 通过 Tool Evidence Injection的AgenTeeth：模型-Agnostic 框架
abstract_zh: "Vision-language 模型s (VLMs) remain largely unreliable on panoramic dental radiographs 与 can rely on l。\n\n核心做法：作者提出 AgenTeeth，模型-agnostic，tool-augmented 框架 that grounds frozen VLMs 使用 seven specialized dental vi。\n\n主要结果：We release 框架，all seven expert 模型s，与 dentist-annotated dataset 用于 alveolar bone-loss detection in pa。\n\n值得关注的点在于：question-aware orchestrator selects relevant tools，whose detections are mapped to FDI tooth numbers。"
abstract_en: "Vision-language models (VLMs) remain largely unreliable on panoramic dental radiographs and can rely on learned anatomical priors rather than evidence in the image. This is particularly problematic for tooth localization and spatial reasoning, and fine-tuned dental VLMs can retain the same spatial biases. We present AgenTeeth, a model-agnostic, tool-augmented framework that grounds frozen VLMs using seven specialized dental vision experts. A question-aware orchestrator selects the relevant tools, whose detections are mapped to FDI tooth numbers or anatomical regions and returned as structured findings together with annotated image overlays. A fresh synthesis call then answers the question using this evidence, without fine-tuning the underlying VLM. On MMOral-OPG-Bench, AgenTeeth improves four backbone VLMs by 12.9-23.0 percentage points over their baselines. Our strongest configuration reaches 65.66% on open-ended VQA, compared with 45.35% for OralGPT-Plus. The advantage also holds at matched scale: a frozen Qwen2.5-VL-7B-Instruct with AgenTeeth reaches 48.11%, exceeding OralGPT-Plus built on the same backbone after supervised fine-tuning and reinforcement learning for tool use. We release the framework, all seven expert models, and a dentist-annotated dataset for alveolar bone-loss detection in panoramic radiographs."
authors: "Ahmed Rafid, Fariya Ahmed, Rumman Adib, Mehedi Ahamed, Ajwad Abrar, Tareque Mohmud Chowdhury"
date: 2026-09-15
journal: cs.CV
source_link: "https://arxiv.org/pdf/2609.17800v1"
pdf: "https://arxiv.org/pdf/2609.17800v1"
score: 8.5
tldr: 围绕《用于Suppressing Hallucinat...》；作者提出 AgenTeeth，模型-agnostic，tool-...；We release 框架，all seven expert 模型s，与...
tldr_cn: 围绕《用于Suppressing Hallucinat...》；作者提出 AgenTeeth，模型-agnostic，tool-...；We release 框架，all seven expert 模型s，与...
tldr_en: "Vision-language models (VLMs) remain largely unreliable on panoramic dental radiographs and can rely on learned anatomical priors rather than evidence in the image. This is particularly problematic for tooth localization and spatial reasoning, and fine-tuned dental VLMs can retain the same spatial biases."
selection_source: fresh_fetch
vocabulary: ["localization | 定位 | CET6", "fine-tuning | 微调 | 学术高频", "detection | 检测 | CET6", "synthesis | 合成 | CET6"]
motivation: Vision-language 模型s (VLMs) remain largely unreliable on panoramic dental radiographs 与 can rely on l。
method: 核心做法：作者提出 AgenTeeth，模型-agnostic，tool-augmented 框架 that grounds frozen VLMs 使用 seven specialized dental vi。
result: 主要结果：We release 框架，all seven expert 模型s，与 dentist-annotated dataset 用于 alveolar bone-loss detection in pa。
conclusion: 值得关注的点在于：question-aware orchestrator selects relevant tools，whose detections are mapped to FDI tooth numbers。
---

## 摘要
Vision-language 模型s (VLMs) remain largely unreliable on panoramic dental radiographs 与 can rely on l。

核心做法：作者提出 AgenTeeth，模型-agnostic，tool-augmented 框架 that grounds frozen VLMs 使用 seven specialized dental vi。

主要结果：We release 框架，all seven expert 模型s，与 dentist-annotated dataset 用于 alveolar bone-loss detection in pa。

值得关注的点在于：question-aware orchestrator selects relevant tools，whose detections are mapped to FDI tooth numbers。

## Abstract
Vision-language models (VLMs) remain largely unreliable on panoramic dental radiographs and can rely on learned anatomical priors rather than evidence in the image. This is particularly problematic for tooth localization and spatial reasoning, and fine-tuned dental VLMs can retain the same spatial biases. We present AgenTeeth, a model-agnostic, tool-augmented framework that grounds frozen VLMs using seven specialized dental vision experts. A question-aware orchestrator selects the relevant tools, whose detections are mapped to FDI tooth numbers or anatomical regions and returned as structured findings together with annotated image overlays. A fresh synthesis call then answers the question using this evidence, without fine-tuning the underlying VLM. On MMOral-OPG-Bench, AgenTeeth improves four backbone VLMs by 12.9-23.0 percentage points over their baselines. Our strongest configuration reaches 65.66% on open-ended VQA, compared with 45.35% for OralGPT-Plus. The advantage also holds at matched scale: a frozen Qwen2.5-VL-7B-Instruct with AgenTeeth reaches 48.11%, exceeding OralGPT-Plus built on the same backbone after supervised fine-tuning and reinforcement learning for tool use. We release the framework, all seven expert models, and a dentist-annotated dataset for alveolar bone-loss detection in panoramic radiographs.

## 英语词汇
- **localization**：定位（CET6）
- **fine-tuning**：微调（学术高频）
- **detection**：检测（CET6）
- **synthesis**：合成（CET6）
