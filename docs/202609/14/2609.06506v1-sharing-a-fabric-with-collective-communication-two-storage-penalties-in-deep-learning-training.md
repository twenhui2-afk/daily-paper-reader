---
title: "Sharing a Fabric with Collective Communication: Two Storage Penalties in Deep Learning Training"
source: arxiv
title_zh: 结合Collective Communication：Two Storage Penalties in Deep Learning Training的Sharing Fabric
abstract_zh: "Distributed DL training on HPC systems often shares one 网络 fabric between NCCL/RCCL collective commu。\n\n核心做法：I/O stall latency affects any storage path that traverses shared fabric，whereas all-reduce 网络 conten。\n\n主要结果：使用 real GNN training workload on Slingshot-11 system，we show that this sharing imposes two distinct。\n\n值得关注的点在于：By second epoch，once local cache is fully warmed，DataLoader stalls are eliminated entirely，allowing。"
abstract_en: "Distributed DL training on HPC systems often shares one network fabric between NCCL/RCCL collective communication and parallel-filesystem I/O. Using a real GNN training workload on a Slingshot-11 system, we show that this sharing imposes two distinct costs. The primary cost is heavy-tailed DataLoader stalls: the typical DataLoader wait is just 15 ms at steady state, yet spikes to multiple seconds in 28% of Lustre iterations and 12% of VAST iterations. The secondary cost is traffic-class contention on collective communication: Lustre I/O stalls the all-reduce by up to 145$\\times$ in an isolated benchmark. The two costs arise from different mechanisms. I/O stall latency affects any storage path that traverses the shared fabric, whereas all-reduce network contention occurs only when storage and collective communication share the same traffic class. Their common root cause is that storage I/O traverses the shared fabric. This work shows that node-local NVMe staging via DYAD (Our code is publicly available at https://github.com/flux-framework/dyad) eliminates both effects by keeping storage I/O off that path. Across a full training epoch, DYAD achieves a 7.4 times speedup over direct Lustre reads and a 1.06 times speedup over VAST. By the second epoch, once the local cache is fully warmed, DataLoader stalls are eliminated entirely, allowing DYAD to reach a 1.31 times speedup over VAST."
authors: "Chen Wang, Wenzhao Wu, Hyojin Kim, Jae-Sung Yeom"
date: 2026-09-06
journal: cs.DC
source_link: "https://arxiv.org/pdf/2609.06506v1"
pdf: "https://arxiv.org/pdf/2609.06506v1"
score: 6.6
tldr: 围绕《结合Collective Communicati...》；I/O stall latency affects any st...；使用 real GNN training workload on Sli...
tldr_cn: 围绕《结合Collective Communicati...》；I/O stall latency affects any st...；使用 real GNN training workload on Sli...
tldr_en: "Distributed DL training on HPC systems often shares one network fabric between NCCL/RCCL collective communication and parallel-filesystem I/O. Using a real GNN training workload on a Slingshot-11 system, we show that this sharing imposes two distinct costs."
selection_source: fresh_fetch
vocabulary: ["benchmark | 基准测试 | CET6/IELTS"]
motivation: Distributed DL training on HPC systems often shares one 网络 fabric between NCCL/RCCL collective commu。
method: 核心做法：I/O stall latency affects any storage path that traverses shared fabric，whereas all-reduce 网络 conten。
result: 主要结果：使用 real GNN training workload on Slingshot-11 system，we show that this sharing imposes two distinct。
conclusion: 值得关注的点在于：By second epoch，once local cache is fully warmed，DataLoader stalls are eliminated entirely，allowing。
---

## 摘要
Distributed DL training on HPC systems often shares one 网络 fabric between NCCL/RCCL collective commu。

核心做法：I/O stall latency affects any storage path that traverses shared fabric，whereas all-reduce 网络 conten。

主要结果：使用 real GNN training workload on Slingshot-11 system，we show that this sharing imposes two distinct。

值得关注的点在于：By second epoch，once local cache is fully warmed，DataLoader stalls are eliminated entirely，allowing。

## Abstract
Distributed DL training on HPC systems often shares one network fabric between NCCL/RCCL collective communication and parallel-filesystem I/O. Using a real GNN training workload on a Slingshot-11 system, we show that this sharing imposes two distinct costs. The primary cost is heavy-tailed DataLoader stalls: the typical DataLoader wait is just 15 ms at steady state, yet spikes to multiple seconds in 28% of Lustre iterations and 12% of VAST iterations. The secondary cost is traffic-class contention on collective communication: Lustre I/O stalls the all-reduce by up to 145$\times$ in an isolated benchmark. The two costs arise from different mechanisms. I/O stall latency affects any storage path that traverses the shared fabric, whereas all-reduce network contention occurs only when storage and collective communication share the same traffic class. Their common root cause is that storage I/O traverses the shared fabric. This work shows that node-local NVMe staging via DYAD (Our code is publicly available at https://github.com/flux-framework/dyad) eliminates both effects by keeping storage I/O off that path. Across a full training epoch, DYAD achieves a 7.4 times speedup over direct Lustre reads and a 1.06 times speedup over VAST. By the second epoch, once the local cache is fully warmed, DataLoader stalls are eliminated entirely, allowing DYAD to reach a 1.31 times speedup over VAST.

## 英语词汇
- **benchmark**：基准测试（CET6/IELTS）
