Title: Lattice point visibility along polynomials has density one

URL Source: https://arxiv.org/pdf/2609.06309v1

Published Time: Wed, 09 Sep 2026 01:00:02 GMT

Number of Pages: 4

Markdown Content:
# Density one for lattice point visibility along polynomials with at least two distinct roots 

Abraham Lobsenz 

Dartmouth College 

abe.e.lobsenz.29@dartmouth.edu 

September 4, 2026 

Abstract 

We prove that every nonzero integer polynomial with at least two distinct complex roots has lattice point visibility density one, resolving the polynomial visibility conjecture of Chaubey–Pandey in the generality proposed by Lobsenz–Phillips. Visibility is taken along the curves y = tF (x) with rational t,with a point visible if no positive lattice point on the same curve has a smaller horizontal coordinate. The proof uses a greatest common divisor cutoff to reduce the problem to finitely many equations of the form F (b) = qF (a), with 0 < q < 1; for each equation, the positive integers a admitting a positive integer solution b < a form a set of density zero. This elementary argument removes the proper-power hypothesis of earlier work without requiring estimates uniform in the ratio. 

2020 Mathematics Subject Classification. Primary 11B05; Secondary 11H06, 11P21. 

Keywords. Lattice point visibility, polynomial lines of sight, natural density, integer points. 

# Introduction 

A classical question in number theory asks what proportion of integer lattice points are visible from the origin along straight lines. In the positive quadrant, visibility is equivalent to coprimality of the coordinates, giving the density 6 /π 2. Following Chaubey–Pandey [CP21] and Lobsenz–Phillips [LP26a], we ask the same question for polynomial lines of sight y = tF (x), with t ∈ Q. We prove that every nonzero integral polynomial with at least two distinct complex roots has visibility density one. 

# 1 Statement and reduction 

We use the visibility definition and counting notation of [LP26a, Definition 2.1 and §2]. For F ∈ Z[x], a point (a, h ) ∈ Z2 

> >0

is visible along F if there is t ∈ Q with h = tF (a) and a is the least positive integer u for which 

tF (u) is a positive integer. Otherwise it is invisible along F . A point ( b, k ) ∈ Z2 

> >0

blocks (a, h ) if b < a and 

h = tF (a), k = tF (b) for the same t ∈ Q. For a positive integer N , write [1 , N ] def 

= {n ∈ Z : 1 ≤ n ≤ N }, and put Visible F (N ) def 

= {(a, h ) ∈ [1 , N ]2 : ( a, h ) is visible along F },

Invisible F (N ) def 

= {(a, h ) ∈ [1 , N ]2 : ( a, h ) is invisible along F }.

(1) These sets partition [1 , N ]2. The visibility density is 

D(F ) def 

= lim  

> N→∞

#Visible F (N )

N 2 , (2) whenever the limit exists. 

Theorem 1. Let F ∈ Z[x] \ { 0} have at least two distinct complex roots. Then D(F ) = 1 ; equivalently, 

#Invisible F (N ) = o(N 2).

This proves the Visibility Density Conjecture of Lobsenz–Phillips [LP26a, Conjecture 1.1], extending the origin-passing conjecture of Chaubey–Pandey [CP21, Conjecture 1.6], resolved by Chaubey, Pandey, and Regavim [CPR26]. Lobsenz–Phillips proved the assertion for F = f m with m ≥ 2 and f having at least two distinct roots [LP26a, Theorems 1.2–1.3], with quantitative bounds sharpened for quadratic f in [LP26b]. We exclude F = 0, for which no positive lattice point is visible. 1arXiv:2609.06309v1 [math.NT] 5 Sep 2026 Remark 2 (Sharpness) . The two-root hypothesis is necessary: nonzero polynomials with fewer than two distinct roots have visibility density strictly less than one; the constant case has density zero since only points with a = 1 are visible, and the one-root case is treated in [LP26a, §1, equation (7)]. Replacing F by −F leaves visibility unchanged, by replacing t by −t, as observed in [LP26a, §1]. We may therefore assume that the leading coefficient is positive. Following [LP26a, §2], choose nF ≥ 1 such that, whenever a > n F ,

F (a) > max (0, F (1) , . . . , F (a − 1) ). (3) Such a choice is possible because F is nonconstant (it has at least two distinct roots), and a nonconstant polynomial with positive leading coefficient is eventually strictly increasing and tends to infinity. The columns 

a ≤ nF contain only OF (N ) points. Put γF (a, h ) def 

= gcd( |F (a)|, h ). Let ( a, h ) be invisible with a > n F , and write γ = γF (a, h ). Its unique possible scale is t = h/F (a) > 0, and a blocking point ( b, k ) satisfies 1 ≤ b < a, k = hF (b)

F (a) ∈ Z>0, 0 < F (b) < F (a).

As in [LP26a, Lemma 2.3], the identity hF (b) = kF (a) gives a divisibility condition: since F (a)/γ and h/γ 

are coprime, F (a)/γ | F (b). Thus F (b) = jF (a)/γ for some integer 1 ≤ j < γ . Writing j/γ = r/s in lowest terms gives 

F (b) = rs F (a), 1 ≤ r < s, gcd( r, s ) = 1 , s | γ. (4) In particular, bounding γ leaves only finitely many ratios. 

Remark 3. In [LP26a], we used Pila’s bound [Pil96] to count blocking pairs on auxiliary curves, and in [LP26b, §§3–4] we refined these counts using Pell equations. To prove density one, however, it is enough to discard an arbitrarily small proportion of lattice points and show that the remaining invisible points have density zero. The GCD cutoff leaves only finitely many ratios to consider, each of which can be treated separately. We thus avoid the summation over ratios that required the proper-power hypothesis in the earlier proofs. This gives a more general density theorem, but does not recover their quantitative estimates. 

# 2 The GCD cutoff 

Lemma 4. For every nonzero F ∈ Z[x] and every ε > 0 there is an integer M ≥ 1 such that, for every 

N ≥ 1, 1

N 2 #{(a, h ) ∈ [1 , N ]2 : γF (a, h ) > M } < ε. 

Proof. Write d = deg F . Choose an integer K ≥ 2 so large that no prime p > K divides every coefficient of 

F . This excludes only finitely many primes, since F has a nonzero coefficient. For any prime p > K , the congruence F (a) ≡ 0 (mod p) has at most d residue classes. Hence #{(a, h ) ∈ [1 , N ]2 : p | F (a), p | h} ≤ d

( Np + 1 

) Np .

Any prime dividing h is at most N , and for p ≤ N we have N/p + 1 ≤ 2N/p . Summing over K < p ≤ N and enlarging the sum to all integers gives the bound, uniform in N ,#{(a, h ) ∈ [1 , N ]2 : ∃p > K, p | γF (a, h )}

N 2 ≤ 2d ∑

> n>K

1

n2 ≤ 2dK . (5) For any fixed positive integer E, the proportion of heights divisible by pE is at most p−E . Outside a set of pairs of proportion at most ∑ 

> p≤K

p−E , none of these small-prime powers divides h. If neither exceptional event occurs, then 

γF (a, h ) ≤ M def 

= ∏

> p≤K

pE−1.

Choose K with 2 d/K < ε/ 2, and then E with ∑ 

> p≤K

p−E < ε/ 2. Combining the two exceptional sets proves the claim. 23 Fixed ratios are sparse 

Lemma 5. Let F ∈ R[x] have degree d ≥ 2, positive leading coefficient, and at least two distinct complex roots. For each q ∈ (0 , 1) , the set 

Aq (F ) def 

= {a ∈ Z>0 : ∃b ∈ Z>0, b < a, F (b) = qF (a)}

has natural density zero. Proof. Write 

F (x) = cdxd + cd−1xd−1 + OF (xd−2), α = q1/d ∈ (0 , 1) , β = cd−1(α − 1) 

dc d

.

For any fixed η > 0, the polynomials F (αx + β ± η) − qF (x) have degree d − 1 and leading coefficients 

±dc dαd−1η. Consequently, for large a,

F (αa + β − η) < qF (a) < F (αa + β + η).

Along solutions with a → ∞ we have b → ∞ , since F (b) = qF (a) → ∞ . Also αa + β − η → ∞ , so b and both comparison arguments eventually lie in the range where F is strictly increasing. The displayed inequalities therefore give αa + β − η < b < αa + β + η. As η is arbitrary, this proves 

b = αa + β + oF,q (1) . (6) Suppose first that α is irrational. Fix an integer L ≥ 1 and set 

δL = min  

> 1≤j≤L

dist( jα, Z) > 0.

Beyond a fixed threshold, the error in (6) has absolute value less than δL/3. Two such solutions with 1 ≤ a′ − a ≤ L would give dist ((a′ − a)α, Z) ≤ | (b′ − b) − α(a′ − a)| < 2δL/3,

a contradiction. Thus every block of L + 1 consecutive sufficiently large integers contains at most one member of Aq (F ). Its upper density is at most 1 /(L + 1); letting L → ∞ proves the assertion. Suppose next that α is rational. Choose an integer D ≥ 1 such that Dα ∈ Z. Then b − αa ∈ D−1Z.Equation (6) shows that, if arbitrarily large solutions exist, necessarily β ∈ D−1Z and all sufficiently large solutions satisfy b = αa + β exactly. If infinitely many solutions existed, the polynomial F (αx + β) − qF (x)would have infinitely many roots. Since a nonzero polynomial has only finitely many roots, this would imply 

F (αx + β) = qF (x) for every x ∈ C.

We now use the root-permutation argument of [LP26a, Proposition 3.3], in its contraction form. Let 

RF

> def

= {ρ ∈ C : F (ρ) = 0 } and T (z) = αz + β. The identity implies T (RF ) ⊆ RF . As T is injective and RF

finite, T (RF ) = RF . But diam( RF ) = diam( T (RF )) = α diam( RF ).

Since 0 < α < 1, the diameter is zero, contrary to the existence of two distinct roots. In this case Aq (F ) is in fact finite. 34 Completion of the proof 

Proof of Theorem 1. Fix ε > 0 and choose M from Lemma 4. Let 

AM (F ) def 

= ⋃

> 2≤s≤M

⋃

> 1≤r<s
> gcd( r,s )=1

Ar/s (F ).

This is a finite union of sets of density zero by Lemma 5, so #( AM (F ) ∩ [1 , N ]) = oF,M (N ). By (4), every invisible point in [1 , N ]2 either has a ≤ nF , has γF (a, h ) > M , or has a ∈ A M (F ). Consequently, #Invisible F (N ) ≤ nF N + εN 2 + N #( AM (F ) ∩ [1 , N ]) .

It follows that lim sup  

> N→∞

#Invisible F (N )

N 2 ≤ 0 + ε + lim  

> N→∞

#( AM (F ) ∩ [1 , N ]) 

N = ε. 

Since ε is arbitrary and the left side is nonnegative, #Invisible F (N ) = o(N 2), and hence D(F ) = 1. 

# Formalization 

A Lean formalization of Theorem 1 is available at 

https://github.com/abelobsenz/polynomial-visibility-lean .The version verified here is commit 6f83b9e . It uses Lean 4.28.0 and mathlib v4.28.0 (commit 8f9d9cf ), with a slightly coarser GCD cutoff bound that yields the same density theorem. 

# Acknowledgments 

The author would like to credit Tristan Phillips for his mentorship in the development of the two joint papers [LP26a, LP26b], many of the ideas of which carry over to this one. Additionally, the author acknowledges the help of AI (GPT-6-Astra) in proof discovery, Lean formalization, and manuscript drafting. The author assumes all responsibility for the arguments laid out here. 

# References 

[CP21] S. Chaubey and A. K. Pandey, On the density of visible lattice points along polynomials , preprint, 2021, arXiv:2109.08431; published as [CPR26]. [CPR26] S. Chaubey, A. K. Pandey, and S. Regavim, On the density of visible lattice points along polynomials ,in Some Contributions to Number Theory and Beyond , Fields Institute Communications, vol. 90, Springer, Cham, 2026, 211–225, https://doi.org/10.1007/978-3-032-10837-1_10 .[LP26a] A. Lobsenz and T. Phillips, Lattice point visibility along powers of polynomials , preprint, 2026, arXiv:2604.23050v3. [LP26b] A. Lobsenz and T. Phillips, Lattice point visibility along powers of quadratic polynomials , manuscript, 2026; to be posted simultaneously with the present paper. This reference will be updated with a link when available. [Pil96] J. Pila, Density of integer points on plane algebraic curves , Internat. Math. Res. Notices 1996 ,no. 18, 903–912, https://doi.org/10.1155/S1073792896000554 .4