---
title: Deterministic NC Quadratic Root Counting in Characteristic Two
source: arxiv
title_zh: Deterministic NC Quadratic Root Counting in Characteristic Two
abstract_zh: "Counting satisfying assignments 的 Boolean formulas is basic problem in theoretical computer science。\n\n核心做法：More generally，counting solutions 的 system 的 polynomial equations over \\F_2 is \\SharpP-complete。\n\n主要结果：Here we focus on more structured problem 的 counting solutions 的 single polynomial equation。\n\n值得关注的点在于：This replaces randomized canonical-form step in algorithm 的 Ishai et al。"
abstract_en: "Counting satisfying assignments of Boolean formulas is a basic problem in theoretical computer science, with $\\#3\\text{-}\\SAT$ as the standard $\\SharpP$-complete problem. More generally, counting the solutions of a system of polynomial equations over $\\F_2$ is $\\SharpP$-complete. Here we focus on the more structured problem of counting the solutions of a single polynomial equation. For polynomial equations over finite fields, Ehrenfeucht and Karpinski \\cite{computationalcomplexityofxorandcountingproblems1990} showed a sharp difference between degrees two and three: quadratic root counting is solvable in polynomial time, while the degree-three problem is $\\SharpP$-complete. Their quadratic algorithm is sequential. For fixed finite fields, Ishai et al.~\\cite{ishai2012randomizing} later gave deterministic parallel algorithms in odd characteristic and randomized parallel algorithms in characteristic two.   We give a deterministic $\\NC$ algorithm for exactly counting the solutions of a quadratic polynomial equation over every fixed finite field of characteristic two. Our algorithm separates the radical and uses the absolute trace to realize the bit distinguishing the two nondegenerate finite-field types as the Arf invariant \\cite{arf1941untersuchungen} of a quadratic form over $\\F_2$. It then recovers that invariant from an integral matrix using Browder's determinant criterion \\cite{browder2006complete}. This replaces the randomized canonical-form step in the algorithm of Ishai et al."
authors: "Sanyam Agarwal, Gorav Jindal"
date: 2026-09-11
journal: cs.CC
source_link: "https://arxiv.org/pdf/2609.12669v1"
pdf: "https://arxiv.org/pdf/2609.12669v1"
score: 6.85
tldr: 围绕《Deterministic NC Quadrat...》；More generally，counting solution...；Here we focus on more structured pro...
tldr_cn: 围绕《Deterministic NC Quadrat...》；More generally，counting solution...；Here we focus on more structured pro...
tldr_en: "Counting satisfying assignments of Boolean formulas is a basic problem in theoretical computer science, with $\\#3\\text{-}\\SAT$ as the standard $\\SharpP$-complete problem. More generally, counting the solutions of a system of polynomial equations over $\\F_2$ is $\\SharpP$-complete."
selection_source: fresh_fetch
motivation: Counting satisfying assignments 的 Boolean formulas is basic problem in theoretical computer science。
method: 核心做法：More generally，counting solutions 的 system 的 polynomial equations over \F_2 is \SharpP-complete。
result: 主要结果：Here we focus on more structured problem 的 counting solutions 的 single polynomial equation。
conclusion: 值得关注的点在于：This replaces randomized canonical-form step in algorithm 的 Ishai et al。
---

## 摘要
Counting satisfying assignments 的 Boolean formulas is basic problem in theoretical computer science。

核心做法：More generally，counting solutions 的 system 的 polynomial equations over \F_2 is \SharpP-complete。

主要结果：Here we focus on more structured problem 的 counting solutions 的 single polynomial equation。

值得关注的点在于：This replaces randomized canonical-form step in algorithm 的 Ishai et al。

## Abstract
Counting satisfying assignments of Boolean formulas is a basic problem in theoretical computer science, with $\#3\text{-}\SAT$ as the standard $\SharpP$-complete problem. More generally, counting the solutions of a system of polynomial equations over $\F_2$ is $\SharpP$-complete. Here we focus on the more structured problem of counting the solutions of a single polynomial equation. For polynomial equations over finite fields, Ehrenfeucht and Karpinski \cite{computationalcomplexityofxorandcountingproblems1990} showed a sharp difference between degrees two and three: quadratic root counting is solvable in polynomial time, while the degree-three problem is $\SharpP$-complete. Their quadratic algorithm is sequential. For fixed finite fields, Ishai et al.~\cite{ishai2012randomizing} later gave deterministic parallel algorithms in odd characteristic and randomized parallel algorithms in characteristic two.   We give a deterministic $\NC$ algorithm for exactly counting the solutions of a quadratic polynomial equation over every fixed finite field of characteristic two. Our algorithm separates the radical and uses the absolute trace to realize the bit distinguishing the two nondegenerate finite-field types as the Arf invariant \cite{arf1941untersuchungen} of a quadratic form over $\F_2$. It then recovers that invariant from an integral matrix using Browder's determinant criterion \cite{browder2006complete}. This replaces the randomized canonical-form step in the algorithm of Ishai et al.
