---
title: "TMPO: Trajectory Matching Policy Optimization for Diverse and Efficient Diffusion Alignment"
source: arxiv
title_zh: 用于Diverse与Efficient Diffusion Alignment的TMPO：Trajectory Matching Policy Optimization
abstract_zh: "Specifically，TMPO introduces Softmax Trajectory Balance (Softmax-TB) objective to match policy proba。\n\n核心做法：In contrast，作者提出 Trajectory Matching Policy Optimization (TMPO)，which replaces scalar reward maximiz。\n\n主要结果：Extensive results across diverse alignment tasks such as human preference，compositional generation 与。\n\n值得关注的点在于：Reinforcement learning (RL) has shown extraordinary potential in aligning diffusion 模型s to downstrea。"
abstract_en: "Reinforcement learning (RL) has shown extraordinary potential in aligning diffusion models to downstream tasks, yet most of them still suffer from significant reward hacking, which degrades generative diversity and quality by inducing visual mode collapse and amplifying unreliable rewards. We identify the root cause as the mode-seeking nature of these methods, which maximize expected reward without effectively constraining probability distribution over acceptable trajectories, causing concentration on a few high-reward paths. In contrast, we propose Trajectory Matching Policy Optimization (TMPO), which replaces scalar reward maximization with trajectory-level reward distribution matching. Specifically, TMPO introduces a Softmax Trajectory Balance (Softmax-TB) objective to match the policy probabilities of K trajectories to a reward-induced Boltzmann distribution. We prove that this objective inherits the mode-covering property of forward KL divergence, preserving coverage over all acceptable trajectories while optimizing reward. To further reduce multi-trajectory training time on large-scale flow-matching models, TMPO incorporates Dynamic Stochastic Tree Sampling, where trajectories share denoising prefixes and branch at dynamically scheduled steps, reducing redundant computation while improving training effectiveness. Extensive results across diverse alignment tasks such as human preference, compositional generation and text rendering show that TMPO improves generative diversity over state-of-the-art methods by 9.1%, and achieves competitive performance in all downstream and efficiency metrics, attaining the optimal trade-off between reward and diversity."
authors: "Jiaming Li, Chenyu Zhu, Zhiyuan Ma, Nanxi Yi, Youjun Bao, Li Sun, Quanying Lv, Xiang Fang, Daizong Liu, Jianjun Li, Kun He, Bowen Zhou"
date: 2026-05-09
journal: cs.LG
source_link: "https://arxiv.org/pdf/2605.10983v1"
pdf: "https://arxiv.org/pdf/2605.10983v1"
score: 7.05
tldr: 围绕《用于Diverse与Efficient Diff...》；In contrast，作者提出 Trajectory Matc...；Extensive results across diverse ali...
tldr_cn: 围绕《用于Diverse与Efficient Diff...》；In contrast，作者提出 Trajectory Matc...；Extensive results across diverse ali...
tldr_en: "Reinforcement learning (RL) has shown extraordinary potential in aligning diffusion models to downstream tasks, yet most of them still suffer from significant reward hacking, which degrades generative diversity and quality by inducing visual mode collapse and amplifying unreliable rewards. We identify the root cause as the mode-seeking nature of these methods, which maximize expected reward..."
selection_source: fresh_fetch
vocabulary: ["optimization | 优化 | CET6", "downstream | 下游任务 | 学术高频", "generative | 生成式的 | IELTS", "alignment | 对齐 | CET6", "denoising | 去噪 | 学术高频", "diffusion | 扩散 | 学术高频"]
motivation: Specifically，TMPO introduces Softmax Trajectory Balance (Softmax-TB) objective to match policy proba。
method: 核心做法：In contrast，作者提出 Trajectory Matching Policy Optimization (TMPO)，which replaces scalar reward maximiz。
result: 主要结果：Extensive results across diverse alignment tasks such as human preference，compositional generation 与。
conclusion: 值得关注的点在于：Reinforcement learning (RL) has shown extraordinary potential in aligning diffusion 模型s to downstrea。
---

## 摘要
Specifically，TMPO introduces Softmax Trajectory Balance (Softmax-TB) objective to match policy proba。

核心做法：In contrast，作者提出 Trajectory Matching Policy Optimization (TMPO)，which replaces scalar reward maximiz。

主要结果：Extensive results across diverse alignment tasks such as human preference，compositional generation 与。

值得关注的点在于：Reinforcement learning (RL) has shown extraordinary potential in aligning diffusion 模型s to downstrea。

## Abstract
Reinforcement learning (RL) has shown extraordinary potential in aligning diffusion models to downstream tasks, yet most of them still suffer from significant reward hacking, which degrades generative diversity and quality by inducing visual mode collapse and amplifying unreliable rewards. We identify the root cause as the mode-seeking nature of these methods, which maximize expected reward without effectively constraining probability distribution over acceptable trajectories, causing concentration on a few high-reward paths. In contrast, we propose Trajectory Matching Policy Optimization (TMPO), which replaces scalar reward maximization with trajectory-level reward distribution matching. Specifically, TMPO introduces a Softmax Trajectory Balance (Softmax-TB) objective to match the policy probabilities of K trajectories to a reward-induced Boltzmann distribution. We prove that this objective inherits the mode-covering property of forward KL divergence, preserving coverage over all acceptable trajectories while optimizing reward. To further reduce multi-trajectory training time on large-scale flow-matching models, TMPO incorporates Dynamic Stochastic Tree Sampling, where trajectories share denoising prefixes and branch at dynamically scheduled steps, reducing redundant computation while improving training effectiveness. Extensive results across diverse alignment tasks such as human preference, compositional generation and text rendering show that TMPO improves generative diversity over state-of-the-art methods by 9.1%, and achieves competitive performance in all downstream and efficiency metrics, attaining the optimal trade-off between reward and diversity.

## 英语词汇
- **optimization**：优化（CET6）
- **downstream**：下游任务（学术高频）
- **generative**：生成式的（IELTS）
- **alignment**：对齐（CET6）
- **denoising**：去噪（学术高频）
- **diffusion**：扩散（学术高频）
