---
title: "Regularity, Phase Transitions, and Uniform Inference for Proximal Counterfactual Quantile Processes"
source: arxiv
title_zh: "用于Proximal Counterfactual Quantile Processes的Regularity，Phase Transitions,与Uniform Inference"
abstract_zh: "Rather 相比 treating each threshold as separate proximal mean problem 结合 outcome \\mathbf 1\\{Y\\le y\\}，这。\n\n核心做法：This paper develops semiparametric theory 用于 counterfactual distribution，quantile，与 lower-tail risk。\n\n主要结果：We show that this dual equation，together 结合 minimal residual-moment condition required 用于 influence。\n\n值得关注的点在于：estimators rely on closed-form linear algebra，convex Tikhonov regularization，与 isotonic 投影用于 shape e。"
abstract_en: "This paper develops semiparametric theory for counterfactual distribution, quantile, and lower-tail risk processes under unmeasured confounding using proximal negative-control proxies. Rather than treating each threshold as a separate proximal mean problem with outcome $\\mathbf 1\\{Y\\le y\\}$, we study the continuum of inverse problems indexed by $y$. For each treatment arm $a$, the counterfactual CDF $F_a(y)=P\\{Y(a)\\le y\\}$ is represented by the primal bridge equation $T_a h_{a,y}=g_{a,y}$ and the linear functional $\\ell(h)=E\\{h(W,X)\\}$. The dual bridge $q_a$ solves $T_a^*q_a=1$, equivalently $E[\\mathbf 1(A=a)q_a(Z,X)-1\\mid W,X]=0$. We show that this dual equation, together with the minimal residual-moment condition required for the influence function to lie in $L_2(P_0)$, is the exact regularity boundary in a threshold-saturated observed-data proximal bridge model: $F_a(y)$ is pathwise differentiable if and only if a regular square-integrable dual bridge exists. The canonical gradient is \\[ h_{a,y}(W,X)-F_a(y)+\\mathbf 1(A=a)q_a(Z,X)\\{\\mathbf 1(Y\\le y)-h_{a,y}(W,X)\\}. \\] A singular-system characterization gives a Picard-type phase transition: root-$n$ regular estimation is possible exactly when $\\sum_j\\ell_{a,j}^2/s_{a,j}^2<\\infty$ and the residual moment is finite. Outside this region, finite-dimensional efficiency bounds diverge under residual-noise nondegeneracy, and Gaussian inverse benchmarks yield slower minimax rates. We further establish efficient CDF-process inference, cross-fitted uniform doubly robust expansions, finite-rank weak-proxy rate conditions, density-free simultaneous quantile bands by inversion of CDF bands, and lower-tail CVaR inference via a shortfall representation. The estimators rely on closed-form linear algebra, convex Tikhonov regularization, and isotonic projection for shape enforcement."
authors: Pengyun Wang
date: 2026-05-10
journal: stat.ME
source_link: "https://arxiv.org/pdf/2605.09257v1"
pdf: "https://arxiv.org/pdf/2605.09257v1"
score: 7.25
tldr: 围绕《用于Proximal Counterfactua...》；This paper develops semiparametr...；We show that this dual equation，toge...
tldr_cn: 围绕《用于Proximal Counterfactua...》；This paper develops semiparametr...；We show that this dual equation，toge...
tldr_en: "This paper develops semiparametric theory for counterfactual distribution, quantile, and lower-tail risk processes under unmeasured confounding using proximal negative-control proxies. Rather than treating each threshold as a separate proximal mean problem with outcome $\\mathbf 1\\{Y\\le y\\}$, we study the continuum of inverse problems indexed by $y$."
selection_source: fresh_fetch
vocabulary: ["inference | 推理 | CET6"]
motivation: "Rather 相比 treating each threshold as separate proximal mean problem 结合 outcome \\mathbf 1\\{Y\\le y\\}，这。"
method: 核心做法：This paper develops semiparametric theory 用于 counterfactual distribution，quantile，与 lower-tail risk。
result: 主要结果：We show that this dual equation，together 结合 minimal residual-moment condition required 用于 influence。
conclusion: 值得关注的点在于：estimators rely on closed-form linear algebra，convex Tikhonov regularization，与 isotonic 投影用于 shape e。
---

## 摘要
Rather 相比 treating each threshold as separate proximal mean problem 结合 outcome \mathbf 1\{Y\le y\}，这。

核心做法：This paper develops semiparametric theory 用于 counterfactual distribution，quantile，与 lower-tail risk。

主要结果：We show that this dual equation，together 结合 minimal residual-moment condition required 用于 influence。

值得关注的点在于：estimators rely on closed-form linear algebra，convex Tikhonov regularization，与 isotonic 投影用于 shape e。

## Abstract
This paper develops semiparametric theory for counterfactual distribution, quantile, and lower-tail risk processes under unmeasured confounding using proximal negative-control proxies. Rather than treating each threshold as a separate proximal mean problem with outcome $\mathbf 1\{Y\le y\}$, we study the continuum of inverse problems indexed by $y$. For each treatment arm $a$, the counterfactual CDF $F_a(y)=P\{Y(a)\le y\}$ is represented by the primal bridge equation $T_a h_{a,y}=g_{a,y}$ and the linear functional $\ell(h)=E\{h(W,X)\}$. The dual bridge $q_a$ solves $T_a^*q_a=1$, equivalently $E[\mathbf 1(A=a)q_a(Z,X)-1\mid W,X]=0$. We show that this dual equation, together with the minimal residual-moment condition required for the influence function to lie in $L_2(P_0)$, is the exact regularity boundary in a threshold-saturated observed-data proximal bridge model: $F_a(y)$ is pathwise differentiable if and only if a regular square-integrable dual bridge exists. The canonical gradient is \[ h_{a,y}(W,X)-F_a(y)+\mathbf 1(A=a)q_a(Z,X)\{\mathbf 1(Y\le y)-h_{a,y}(W,X)\}. \] A singular-system characterization gives a Picard-type phase transition: root-$n$ regular estimation is possible exactly when $\sum_j\ell_{a,j}^2/s_{a,j}^2<\infty$ and the residual moment is finite. Outside this region, finite-dimensional efficiency bounds diverge under residual-noise nondegeneracy, and Gaussian inverse benchmarks yield slower minimax rates. We further establish efficient CDF-process inference, cross-fitted uniform doubly robust expansions, finite-rank weak-proxy rate conditions, density-free simultaneous quantile bands by inversion of CDF bands, and lower-tail CVaR inference via a shortfall representation. The estimators rely on closed-form linear algebra, convex Tikhonov regularization, and isotonic projection for shape enforcement.

## 英语词汇
- **inference**：推理（CET6）
