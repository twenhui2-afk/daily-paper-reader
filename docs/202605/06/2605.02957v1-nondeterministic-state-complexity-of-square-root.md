---
title: Nondeterministic state complexity of square root
source: arxiv
title_zh: square root的Nondeterministic state complexity
abstract_zh: "We investigate nondeterministic state complexity 的 square-root operation \\sqrt{L}=\\{\\,w \\mid ww\\in L。\n\n核心做法：用于 n-state NFA accepting L，it was previously known that \\sqrt{L} can be accepted by NFA 结合 at most n。\n\n主要结果：In this paper，we close this gap completely 与 prove that n^{3} states are sufficient 与 necessary in w。\n\n值得关注的点在于：这篇文章的价值在于把《square root的Nondeterministic state complexity》这个问题讲清楚。"
abstract_en: "We investigate the nondeterministic state complexity of the square-root operation $\\sqrt{L}=\\{\\,w \\mid ww\\in L\\,\\}$ on regular languages represented by nondeterministic finite automata. For an $n$-state NFA accepting $L$, it was previously known that $\\sqrt{L}$ can be accepted by an NFA with at most $n^{3}$ states, while the best lower bound was only (n-1)(n-2)(n-3). In this paper, we close this gap completely and prove that $n^{3}$ states are sufficient and necessary in the worst case."
authors: Sergey Onishchenko
date: 2026-05-02
journal: cs.FL
source_link: "https://arxiv.org/pdf/2605.02957v1"
pdf: "https://arxiv.org/pdf/2605.02957v1"
score: 7.85
tldr: 围绕《square root的Nondetermini...》；用于 n-state NFA accepting L，it wa...；In this paper，we close this gap comp...
tldr_cn: 围绕《square root的Nondetermini...》；用于 n-state NFA accepting L，it wa...；In this paper，we close this gap comp...
tldr_en: "We investigate the nondeterministic state complexity of the square-root operation $\\sqrt{L}=\\{\\,w \\mid ww\\in L\\,\\}$ on regular languages represented by nondeterministic finite automata. For an $n$-state NFA accepting $L$, it was previously known that $\\sqrt{L}$ can be accepted by an NFA with at most $n^{3}$ states, while the best lower bound was only (n-1)(n-2)(n-3)."
selection_source: fresh_fetch
motivation: "We investigate nondeterministic state complexity 的 square-root operation \\sqrt{L}=\\{\\,w \\mid ww\\in L。"
method: "核心做法：用于 n-state NFA accepting L，it was previously known that \\sqrt{L} can be accepted by NFA 结合 at most n。"
result: "主要结果：In this paper，we close this gap completely 与 prove that n^{3} states are sufficient 与 necessary in w。"
conclusion: 值得关注的点在于：这篇文章的价值在于把《square root的Nondeterministic state complexity》这个问题讲清楚。
---

## 摘要
We investigate nondeterministic state complexity 的 square-root operation \sqrt{L}=\{\,w \mid ww\in L。

核心做法：用于 n-state NFA accepting L，it was previously known that \sqrt{L} can be accepted by NFA 结合 at most n。

主要结果：In this paper，we close this gap completely 与 prove that n^{3} states are sufficient 与 necessary in w。

值得关注的点在于：这篇文章的价值在于把《square root的Nondeterministic state complexity》这个问题讲清楚。

## Abstract
We investigate the nondeterministic state complexity of the square-root operation $\sqrt{L}=\{\,w \mid ww\in L\,\}$ on regular languages represented by nondeterministic finite automata. For an $n$-state NFA accepting $L$, it was previously known that $\sqrt{L}$ can be accepted by an NFA with at most $n^{3}$ states, while the best lower bound was only (n-1)(n-2)(n-3). In this paper, we close this gap completely and prove that $n^{3}$ states are sufficient and necessary in the worst case.
