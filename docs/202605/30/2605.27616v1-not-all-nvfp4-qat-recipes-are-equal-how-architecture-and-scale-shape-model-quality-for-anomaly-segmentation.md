---
title: "Not All NVFP4 QAT Recipes Are Equal: How Architecture and Scale Shape Model Quality for Anomaly Segmentation"
source: arxiv
title_zh: 用于Anomaly Segmentation的Not All NVFP4 QAT Recipes Are Equal：How 架构与Scale Shape 模型 Quality
abstract_zh: "这篇论文研究 three-way interaction 的模型架构，模型 scale，与 FP4 quantization-aware training (QAT) recipe on recall。\n\n核心做法：We find that 架构 choice has largest impact on quantization robustness，结合 attention-based 架构s showing。\n\n主要结果：Our results show that Swin Transformer is robust to QAT recipe choice across all scales，making it re。\n\n值得关注的点在于：At low capacity，FP4 can discretize softmax attention，but advanced QAT recipes prevent this collapse。"
abstract_en: "Real-time anomaly segmentation demands both high recall and efficient low-precision inference. We study the three-way interaction of model architecture, model scale, and FP4 quantization-aware training (QAT) recipe on a recall-critical brain tumor segmentation task, evaluating multiple architectures, scales, and QAT recipes under a unified protocol. We find that architecture choice has the largest impact on quantization robustness, with attention-based architectures showing remarkable resilience to recipe choice while CNN degrades under gradient-quantizing recipes at larger scales. At low capacity, FP4 can discretize softmax attention, but advanced QAT recipes prevent this collapse. At larger scales, advanced recipes mitigate gradient quantization noise that degrades CNN quality. Five-fold patient-level cross-validation confirms these findings are robust to data partition. Our results show that the Swin Transformer is robust to QAT recipe choice across all scales, making it the recommended architecture for FP4-quantized anomaly segmentation."
authors: "Zijian Du, Oleg Rybakov"
date: 2026-05-26
journal: cs.CV
source_link: "https://arxiv.org/pdf/2605.27616v1"
pdf: "https://arxiv.org/pdf/2605.27616v1"
score: 6.299999999999999
tldr: 围绕《用于Anomaly Segmentation的N...》；We find that 架构 choice has large...；Our results show that Swin Transform...
tldr_cn: 围绕《用于Anomaly Segmentation的N...》；We find that 架构 choice has large...；Our results show that Swin Transform...
tldr_en: "Real-time anomaly segmentation demands both high recall and efficient low-precision inference. We study the three-way interaction of model architecture, model scale, and FP4 quantization-aware training (QAT) recipe on a recall-critical brain tumor segmentation task, evaluating multiple architectures, scales, and QAT recipes under a unified protocol."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "transformer | Transformer 架构 | 学术高频", "robustness | 鲁棒性 | IELTS", "inference | 推理 | CET6"]
motivation: 这篇论文研究 three-way interaction 的模型架构，模型 scale，与 FP4 quantization-aware training (QAT) recipe on recall。
method: 核心做法：We find that 架构 choice has largest impact on quantization robustness，结合 attention-based 架构s showing。
result: 主要结果：Our results show that Swin Transformer is robust to QAT recipe choice across all scales，making it re。
conclusion: 值得关注的点在于：At low capacity，FP4 can discretize softmax attention，but advanced QAT recipes prevent this collapse。
---

## 摘要
这篇论文研究 three-way interaction 的模型架构，模型 scale，与 FP4 quantization-aware training (QAT) recipe on recall。

核心做法：We find that 架构 choice has largest impact on quantization robustness，结合 attention-based 架构s showing。

主要结果：Our results show that Swin Transformer is robust to QAT recipe choice across all scales，making it re。

值得关注的点在于：At low capacity，FP4 can discretize softmax attention，but advanced QAT recipes prevent this collapse。

## Abstract
Real-time anomaly segmentation demands both high recall and efficient low-precision inference. We study the three-way interaction of model architecture, model scale, and FP4 quantization-aware training (QAT) recipe on a recall-critical brain tumor segmentation task, evaluating multiple architectures, scales, and QAT recipes under a unified protocol. We find that architecture choice has the largest impact on quantization robustness, with attention-based architectures showing remarkable resilience to recipe choice while CNN degrades under gradient-quantizing recipes at larger scales. At low capacity, FP4 can discretize softmax attention, but advanced QAT recipes prevent this collapse. At larger scales, advanced recipes mitigate gradient quantization noise that degrades CNN quality. Five-fold patient-level cross-validation confirms these findings are robust to data partition. Our results show that the Swin Transformer is robust to QAT recipe choice across all scales, making it the recommended architecture for FP4-quantized anomaly segmentation.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **transformer**：Transformer 架构（学术高频）
- **robustness**：鲁棒性（IELTS）
- **inference**：推理（CET6）
