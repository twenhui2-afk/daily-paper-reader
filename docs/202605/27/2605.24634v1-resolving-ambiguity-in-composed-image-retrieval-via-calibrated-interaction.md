---
title: Resolving Ambiguity in Composed Image Retrieval via Calibrated Interaction
source: arxiv
title_zh: 通过Calibrated Interaction实现的Resolving Ambiguity in Composed Image Retrieval
abstract_zh: "Composed image retrieval (CIR) searches corpus 结合 reference image 与 text describing how to modify it。\n\n核心做法：作者引入 AmbiCIR，benchmark 与 human-validated user simulator that revive dormant auxiliary 与 dialogue ann。\n\n主要结果：We argue this is fundamentally at odds 结合 task。\n\n值得关注的点在于：We reframe CIR as calibrated intent resolution under uncertainty：retriever is wrapped in conformal p。"
abstract_en: "Composed image retrieval (CIR) searches a corpus with a reference image and a text describing how to modify it. Despite rapid progress from triplet-trained compositors to zero-shot and generative methods, essentially all systems share one assumption: that a query maps to a single target, scored by Recall@K against one annotation. We argue this is fundamentally at odds with the task. A query such as make it more formal does not name an image but a region of the corpus, and which member the user intends is genuinely underdetermined. This underspecification is the root of the well-known false-negative problem and leaves current models unable to tell a precise query from an ambiguous one. We reframe CIR as calibrated intent resolution under uncertainty: a retriever is wrapped in a conformal prediction layer that returns a candidate set with a coverage guarantee and whose size is a principled measure of ambiguity; when the set is large, an expected-information-gain policy asks the single most useful clarifying question, drawn from interpretable ambiguity axes, and the set contracts. We introduce AmbiCIR, a benchmark and human-validated user simulator that revive the dormant auxiliary and dialogue annotations of CIRR and extend the multiple-positive setting of CIRCO. Across open-domain and fashion benchmarks our method matches single-turn state of the art, confirming calibrated resolution is cost-free on precise queries, while reaching the intended target in a fraction of the interaction budget required by naive conversational baselines, and it is the first to report valid coverage and calibration for the task."
authors: "Amsisan Tran, Baogh Le, Tuan Kiet Pham, Sui Yang Guang"
date: 2026-05-23
journal: cs.CV
source_link: "https://arxiv.org/pdf/2605.24634v1"
pdf: "https://arxiv.org/pdf/2605.24634v1"
score: 8.299999999999999
tldr: 围绕《通过Calibrated Interaction...》；作者引入 AmbiCIR，benchmark 与 human-v...；We argue this is fundamentally at od...
tldr_cn: 围绕《通过Calibrated Interaction...》；作者引入 AmbiCIR，benchmark 与 human-v...；We argue this is fundamentally at od...
tldr_en: "Composed image retrieval (CIR) searches a corpus with a reference image and a text describing how to modify it. Despite rapid progress from triplet-trained compositors to zero-shot and generative methods, essentially all systems share one assumption: that a query maps to a single target, scored by Recall@K against one annotation."
selection_source: fresh_fetch
vocabulary: ["uncertainty | 不确定性 | IELTS", "annotation | 标注 | CET6", "generative | 生成式的 | IELTS", "benchmark | 基准测试 | CET6/IELTS", "retrieval | 检索 | CET6/IELTS"]
motivation: Composed image retrieval (CIR) searches corpus 结合 reference image 与 text describing how to modify it。
method: 核心做法：作者引入 AmbiCIR，benchmark 与 human-validated user simulator that revive dormant auxiliary 与 dialogue ann。
result: 主要结果：We argue this is fundamentally at odds 结合 task。
conclusion: 值得关注的点在于：We reframe CIR as calibrated intent resolution under uncertainty：retriever is wrapped in conformal p。
---

## 摘要
Composed image retrieval (CIR) searches corpus 结合 reference image 与 text describing how to modify it。

核心做法：作者引入 AmbiCIR，benchmark 与 human-validated user simulator that revive dormant auxiliary 与 dialogue ann。

主要结果：We argue this is fundamentally at odds 结合 task。

值得关注的点在于：We reframe CIR as calibrated intent resolution under uncertainty：retriever is wrapped in conformal p。

## Abstract
Composed image retrieval (CIR) searches a corpus with a reference image and a text describing how to modify it. Despite rapid progress from triplet-trained compositors to zero-shot and generative methods, essentially all systems share one assumption: that a query maps to a single target, scored by Recall@K against one annotation. We argue this is fundamentally at odds with the task. A query such as make it more formal does not name an image but a region of the corpus, and which member the user intends is genuinely underdetermined. This underspecification is the root of the well-known false-negative problem and leaves current models unable to tell a precise query from an ambiguous one. We reframe CIR as calibrated intent resolution under uncertainty: a retriever is wrapped in a conformal prediction layer that returns a candidate set with a coverage guarantee and whose size is a principled measure of ambiguity; when the set is large, an expected-information-gain policy asks the single most useful clarifying question, drawn from interpretable ambiguity axes, and the set contracts. We introduce AmbiCIR, a benchmark and human-validated user simulator that revive the dormant auxiliary and dialogue annotations of CIRR and extend the multiple-positive setting of CIRCO. Across open-domain and fashion benchmarks our method matches single-turn state of the art, confirming calibrated resolution is cost-free on precise queries, while reaching the intended target in a fraction of the interaction budget required by naive conversational baselines, and it is the first to report valid coverage and calibration for the task.

## 英语词汇
- **uncertainty**：不确定性（IELTS）
- **annotation**：标注（CET6）
- **generative**：生成式的（IELTS）
- **benchmark**：基准测试（CET6/IELTS）
- **retrieval**：检索（CET6/IELTS）
