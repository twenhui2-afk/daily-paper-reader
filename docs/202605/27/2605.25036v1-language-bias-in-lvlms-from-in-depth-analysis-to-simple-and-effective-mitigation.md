---
title: "Language Bias in LVLMs: From In-Depth Analysis to Simple and Effective Mitigation"
source: arxiv
title_zh: "由In-Depth Analysis to Simple与Effective Mitigation得到的Language Bias in LVLMs:"
abstract_zh: "Large Vision-Language 模型s (LVLMs) extend large language 模型s 结合 visual understanding，but remain vulne。\n\n核心做法：To address this，作者提出 two simple yet effective 方法s：Language Bias Regularization (LBR) which mitigates。\n\n主要结果：Extensive experiments across diverse 模型s 与 benchmarks 表明 effectiveness 的 our approach。\n\n值得关注的点在于：Our code is publicly available at https://github.com/lab-klc/LVLM-Language-Bias。"
abstract_en: "Large Vision-Language Models (LVLMs) extend large language models with visual understanding, but remain vulnerable to hallucination, where outputs are fluent yet inconsistent with images. Recent studies link this issue to language bias-the tendency of LVLMs to over-rely on text while neglecting visual inputs. Yet most analyses remain empirical without uncovering its underlying cause. In this paper, we provide a systematic study of language bias and identify its root in modality misalignment during training. Our analysis shows that both Visual Instruction Tuning (VIT) and Direct Preference Optimization (DPO) often prioritize textual improvements, which may cause LVLMs to overly lean toward language modeling rather than balanced multimodal understanding. To address this, we propose two simple yet effective methods: Language Bias Regularization (LBR) which mitigates language bias through regularization during instruction tuning, and Language Bias Penalty (LBP), which penalizes language bias in the DPO training process. Extensive experiments across diverse models and benchmarks demonstrate the effectiveness of our approach. LBR consistently improves performance on over ten general benchmarks, while LBP significantly reduces hallucination and improves trustworthiness. Together, these methods not only mitigate language bias but also advance the overall alignment of LVLMs, all without introducing any additional data or auxiliary models. Our code is publicly available at https://github.com/lab-klc/LVLM-Language-Bias."
authors: "Yangneng Chen, Jing Li"
date: 2026-05-24
journal: cs.CL
source_link: "https://arxiv.org/pdf/2605.25036v1"
pdf: "https://arxiv.org/pdf/2605.25036v1"
score: 8.0
tldr: 围绕《由In-Depth Analysis to Si...》；To address this，作者提出 two simple...；Extensive experiments across diverse...
tldr_cn: 围绕《由In-Depth Analysis to Si...》；To address this，作者提出 two simple...；Extensive experiments across diverse...
tldr_en: "Large Vision-Language Models (LVLMs) extend large language models with visual understanding, but remain vulnerable to hallucination, where outputs are fluent yet inconsistent with images. Recent studies link this issue to language bias-the tendency of LVLMs to over-rely on text while neglecting visual inputs."
selection_source: fresh_fetch
vocabulary: ["optimization | 优化 | CET6", "multimodal | 多模态 | 学术高频", "alignment | 对齐 | CET6"]
motivation: Large Vision-Language 模型s (LVLMs) extend large language 模型s 结合 visual understanding，but remain vulne。
method: 核心做法：To address this，作者提出 two simple yet effective 方法s：Language Bias Regularization (LBR) which mitigates。
result: 主要结果：Extensive experiments across diverse 模型s 与 benchmarks 表明 effectiveness 的 our approach。
conclusion: "值得关注的点在于：Our code is publicly available at https://github.com/lab-klc/LVLM-Language-Bias。"
---

## 摘要
Large Vision-Language 模型s (LVLMs) extend large language 模型s 结合 visual understanding，but remain vulne。

核心做法：To address this，作者提出 two simple yet effective 方法s：Language Bias Regularization (LBR) which mitigates。

主要结果：Extensive experiments across diverse 模型s 与 benchmarks 表明 effectiveness 的 our approach。

值得关注的点在于：Our code is publicly available at https://github.com/lab-klc/LVLM-Language-Bias。

## Abstract
Large Vision-Language Models (LVLMs) extend large language models with visual understanding, but remain vulnerable to hallucination, where outputs are fluent yet inconsistent with images. Recent studies link this issue to language bias-the tendency of LVLMs to over-rely on text while neglecting visual inputs. Yet most analyses remain empirical without uncovering its underlying cause. In this paper, we provide a systematic study of language bias and identify its root in modality misalignment during training. Our analysis shows that both Visual Instruction Tuning (VIT) and Direct Preference Optimization (DPO) often prioritize textual improvements, which may cause LVLMs to overly lean toward language modeling rather than balanced multimodal understanding. To address this, we propose two simple yet effective methods: Language Bias Regularization (LBR) which mitigates language bias through regularization during instruction tuning, and Language Bias Penalty (LBP), which penalizes language bias in the DPO training process. Extensive experiments across diverse models and benchmarks demonstrate the effectiveness of our approach. LBR consistently improves performance on over ten general benchmarks, while LBP significantly reduces hallucination and improves trustworthiness. Together, these methods not only mitigate language bias but also advance the overall alignment of LVLMs, all without introducing any additional data or auxiliary models. Our code is publicly available at https://github.com/lab-klc/LVLM-Language-Bias.

## 英语词汇
- **optimization**：优化（CET6）
- **multimodal**：多模态（学术高频）
- **alignment**：对齐（CET6）
