Title: Beyond Independence: on Jointly Normal Priors in Bayesian Inversion

URL Source: https://arxiv.org/pdf/2605.00332v1

Published Time: Mon, 04 May 2026 00:27:48 GMT

Number of Pages: 27

Markdown Content:
Beyond Independence: on Jointly Normal Priors in Bayesian Inversion 

Ruanui Nicholson ∗ , Matti Niskanen † , Oliver J. Maclaren ∗ , and Jari P. Kaipio ‡

Abstract. We consider joint inversion for two or more unknown parameters from observational data in the Bayesian framework. Standard approaches often either treat the parameters as independent or impose structural similarity through regularisation terms that can be difficult to interpret statistically. We instead construct jointly Gaussian prior models with prescribed Gaussian marginals, so that correlation between the param-eters can be incorporated without altering the marginal prior distributions. We propose a joint covariance construction that preserves the marginals, allows spatially varying cross-correlation, and supports uncer-tainty and inference in the correlation itself. The construction is valid for any strict contraction encoding the desired cross-correlation and is optimal in a canonical correlation sense under the principal square root factorisation. We demonstrate the method using prior sampling and several inference examples: alow-dimensional illustrative example and two higher-dimensional examples, including a PDE-constrained problem. The examples highlight both the potential pitfalls of ignoring or neglecting uncertainty in the correlation as well as reinforcing a key principle of the Bayesian paradigm: unknown quantities included in a model should be treated as random variables. 

Key words. Joint inversion, Multiphysics inversion, Bayesian inversion, Covariance matrices, Correlation 

1. Introduction. We consider joint inversion of distributed parameters in the Bayesian frame-work. The aim of joint inversion is to estimate two or more inversion parameters , denoted here by 

p ∈ P and m ∈ M , based on noisy observed data. In various settings there may be measured data from two independent problems. In such cases the setup is often modelled as (1.1) d1 = F1(p) + e1,d2 = F2(m) + e2,

with d1 ∈ Rq1 and d2 ∈ Rq2 denoting the vectors of measured data, F1 : P → Rq1 and F2 :

M → Rq2 denoting the associated parameter-to-observable mappings (i.e., the forward problems) and e1 ∈ Rq1 and e2 ∈ Rq2 denoting measurement errors in each of the problems. Such a setup is commonly referred to as the multi-physics case in the literature. The more general setup, however, is of the form 

d = G (p, m ) + e(1.2) where d ∈ Rq for q = q1 + q2 is the measured data, G : P × M → Rq the parameter-to-observable map (i.e., the joint forward problem), and e ∈ Rq the measurement errors in the data. Joint inversion problems naturally occur in geophysics [1–4], biomedical imaging [5–7], and non-destructive testing [8,9] among various other fields. Closely related problems also arise in co-kriging and multivariate spatial prediction, where a key task is to model and condition multiple correlated spatial fields [10, 11]. The most common approaches to joint inversion are framed within the deterministic setting and are often based on (generalised) total variation with the goal of enforcing 

structural similarity between the parameters of interest, see [12] for an in-depth review. Though in some cases it may be possible to interpret these approaches in a statistical manner, how to choose a suitable regularisation parameter, encode uncertainty or spatial variability in the level of structural similarity, or quantify the resulting uncertainties remains unclear. It is also well documented that total variation-based approaches suffer from theoretical limitations, such as discretisation invariance issues [13]. Moreover, if there is no prior knowledge of any relationship between the inversion parameters, the default approach is to treat them as independent, see, e.g., [9,14–16] among many. 

> ∗

Department of Engineering Science, University of Auckland, Auckland 1010, New Zealand. 

> †

Rocsole Limited, Kartanonkatu 2, 70700 Kuopio, Finland. 

> ‡

Department of Technical Physics, University of Eastern Finland, Kuopio Campus, 70211 Kuopio, Finland. 

1

> arXiv:2605.00332v1 [stat.ME] 1 May 2026

In the current paper, we focus on the design of joint Gaussian prior models for high-dimensional Bayesian inverse problems. More specifically, we consider the case where the marginal prior models for p and m are known and we wish to encode correlation (or at least the possibility of correlation) between the two parameters. As such, the problem reduces to finding a suitable joint covariance matrix. The somewhat related problem of finding a valid correlation matrix has been, and con-tinues to be, extensively studied [17–22]. However, these works are typically aimed at finding the 

nearest correlation matrix based on sample-approximated correlation matrices when missing data has compromised the correlations and led to a non-positive (semi-)definite matrix. Our underlying objectives for constructing the joint prior are fourfold: 1. The constructed joint prior should not alter the two marginal distributions π(p) and π(m); 2. Correlation between the parameters should be easy to incorporate (while still satisfying 1); 3. The approach should easily allow for uncertainty in the correlation. 4. Computational costs of the prior ∗ should not drastically increase using the joint prior. 

Contribution. The main contributions of this article are the provision of a covariance matrix which maintains the two marginal distributions (Objective 1) and easily allows for incorporation of a given possible spatially varying correlation (Objective 2). The proposed covariance formulation also allows for straightforward incorporation and quantification of uncertainty in the correlation (Objective 3). Finally, we provide a convenient approach to carry out the required computations involving the joint prior based on the marginal distributions so that the cost associated with eval-uating (and sampling from) the joint prior are not significantly more than carrying out the related computations for the case of independent parameters (Objective 4). We demonstrate the appli-cability of the approach on an illustrative low-dimensional problem, and two higher-dimensional problems, one of which is governed by a partial differential equation (PDE). The illustrative ex-amples serve to demonstrate both the dangers of neglecting uncertainty, namely uncertainty in the correlation between the two parameters, along with the benefits of modelling the uncertainty in the correlation. 

Paper overview. The remainder of the paper is organised as follows. In Section 2 we review the Bayesian approach to joint inversion and propose our method of constructing a jointly nor-mal model, as well as demonstrating its use on generating correlated samples. Several technical considerations are discussed and demonstrated on joint inversion for the maximum population and half-velocity constant of the Monod model in Section 3. Section 4 is used to demonstrate the ap-plicability of our proposed approach on two higher dimensional examples motivated by geophysical settings. Finally, in the last section, Section 5, we close with some conclusions. 

2. The Bayesian approach to joint inversion. We consider problems in the finite-dimensional setting with p ∈ Rn1 , m ∈ Rn2 and set n = n1 + n2. However, the results given here should hold in the infinite-dimensional (Hilbert space) setting [23] with special care taken to ensure the correct spaces are used [24, 25]. We denote by S+ 

> n

the space of n × n symmetric positive definite matrices. A matrix A ∈ Rn×q is said to be a strict contraction if ∥A∥2 < 1, i.e., all singular values of A are strictly less than 1. We denote the space of all n × q strict contractions by An×q.Posed within the Bayesian framework [23,26,27] the joint inversion problem becomes a problem of joint inference. The Bayesian approach treats all unknowns as random variables and naturally allows for the incorporation, and subsequent quantification, of uncertainty. The solution to the inference problem is the (joint) posterior density π(p, m |d) which by Bayes’ theorem can be written as 

πpost (p, m |d) ∝ π(d|p, m )π(p, m ),(2.1) where π(d|p, m ) denotes the likelihood function and π(p, m ) the joint prior which encodes our beliefs about the parameters prior to considering the data. However, it is well understood that the relevance of any resulting estimates is heavily dependent on the ability to accurately model and 

> ∗sampling from and/or evaluating
> 2

account for the various sources of uncertainty [26,27]. Failure to accurately account for the statistics of noise and/or errors typically results in a mis-specified likelihood, and consequently, misleading posterior estimates [28–31]. However, the same can be said for the prior: failure to model the prior uncertainty in the parameters can result in misleading posterior estimates . The design of prior densities for a single (distributed) parameter for Bayesian inference is a fairly mature field [32–36]. On the other hand, for joint inference of two or more distributed parameters, the literature is comparatively modest. 

2.1. A jointly normal model. Here we present an efficient means to construct a joint Gaussian distribution. The proposed approach can be seen as a generalisation of the so-called co-simulation 

approach [37]. The approach outlined here allows for a spatially varying correlation between the two parameter fields of interest, correlation between parameters of different dimensions, and (most importantly) uncertainty in the correlation itself. As stated previously, our approach is based on the construction of a joint Gaussian, thus provision of a suitable joint covariance matrix is the key to the approach. Before presenting our main results, we recall a key theorem. 

Theorem 2.1. Let H1 ∈ S+ 

> n1

and H2 ∈ S+ 

> n2

. Then the matrix 

H =

 H1 H12 

HT 

> 12

H2



(2.2) 

is positive definite if and only if there exists a strict contraction K ∈ An1×n2 such that H12 =

H 12 

> 1

KH 12 

> 2

.Proof. The result and proof can be found in, for example, [38, Theorem 7.7.7]. We then propose the following construction of the joint covariance matrix for a given desired cross-correlation encoded in C ∈ An1×n2 ;Γ = 

 Γp L−1 

> p

CL −Tm

L−1 

> m

CT L−Tp Γm



∈ Rn1+n2 ,(2.3) which, by the following corollary is a proper covariance matrix, i.e. Γ ∈ S+ 

> n

. That is, we now present our main results. 

Corollary 2.2 (Valid joint covariance with preserved marginals). Let Γp = ( LTp Lp)−1 ∈ S+ 

> n1

and 

Γm = ( LTmLm)−1 ∈ S+ 

> n2

, and suppose C ∈ An1×n2 . Then, taking n = n1 + n2, we have 

Γ = 

 Γp Γpm 

Γmp Γm



=

 Γp L−1 

> p

CL −Tm

L−1 

> m

CT L−Tp Γm



∈ S+ 

> n

,(2.4) 

that is, Γ is a proper covariance matrix. Proof. The proof follows immediately by setting H1 = Γ p, H2 = Γ m, and K = C in Theo-rem 2.1. 

Remark 2.3. The off-diagonal blocks of the proposed joint covariance matrix can equivalently be written in terms of prior covariance square roots, i.e., as Γ 1/2 

> p

CΓ1/2 

> m

and Γ 1/2 

> m

CT Γ1/2 

> p

when the principal square root factorisation is used. However, Bayesian inverse problems are often formulated in terms of (square root factors of) prior precision operators and we thus use the factorisations given in (2.4). Corollary 2.2 states Γ is a proper covariance matrix, it does not say anything about how well the desired correlation is encoded. In fact, the choice of Γ above is in general not optimal in terms of enforcing the (Pearson’s) correlation point-wise, i.e., there may exist other choices of Γ pm such that the correlation matrix Rn1×n2 ∋ Φ := diag(Γ p)− 12 Γpm diag(Γ m)− 12 is closer to C in a particular sense. However, when the principal square root factorisation is used, our choice is optimal in the 

> 3

sense of canonical correlation. Recall, canonical correlation can be interpreted as a multivariate analogue of Pearson’s correlation. Specifically, in canonical correlation analysis (CCA) the goal is to find v1 ∈ Rn1 and v2 ∈ Rn2 to maximise 

ρ = vT 

> 1

Γpm v2

q

vT 

> 1

Γpv1

q

vT 

> 2

Γmv2

,

and then repeat under orthogonality to produce the sequence 1 ≥ ρ1 ≥ ρ2, . . . , ρ min {n1,n 2} ≥ 0, which are termed the canonical correlations and are the singular values of the (canonical correlation) matrix Γ − 12 

> p

Γpm Γ− 12 

> m

with Γ − 12 

> p

and Γ − 12 

> m

the inverse principal square roots (for more on canonical correlation see, for example, [39, Chapter 12] or [40]). We then have the following theorem on the optimality of Γ. 

Theorem 2.4 (Optimality of Γ in canonical correlation). Let Γp ∈ S+ 

> n1

and Γm ∈ S+ 

> n2

, and suppose 

C ∈ An1×n2 . Then, letting n = n1 + n2, the unique solution to the optimisation problem: 

min 

> Γpm ∈Rn1×n2

Γ− 12 

> p

Γpm Γ− 12 

> m

− C

> 2
> F

s.t. Γ = 

 Γp Γpm 

Γmp Γm



∈ S+ 

> n

,(2.5) 

is Γpm = Γ 12 

> p

CΓ 12

> m

, i.e., we set L−1 

> p

and L−1 

> m

as the principal square roots of Γp and Γm, respectively. Proof. Recall, by Theorem 2.1 we have Γ ∈ S+ 

> n

if and only if Γ pm = L−1 

> p

KL −Tm for some contraction K ∈ An1×n2 . Substituting this in, we can rewrite the optimisation problem as: min 

> Γpm ∈Rn1×n2

Γ− 12 

> p

(L−1 

> p

KL −Tm )Γ − 12 

> m

− C

> 2
> F

s.t. Γ = 

 Γp Γpm 

Γmp Γm



∈ S+ 

> n

.

Taking Lp = Γ − 12 

> p

and Lm = Γ − 12 

> m

, the inverse principal square roots, we can rewrite the optimisation problem in terms of the contraction K:min 

> K∈An1×n2

∥K − C∥2 

> F

s.t. Γ = 

 Γp Γpm 

Γmp Γm



∈ S+ 

> n

, Γpm = L−1 

> p

KL −Tm ,(2.6) which has a minimiser of K = C. Furthermore, K = C is the unique minimiser as the squared Frobenius norm is strictly convex. 

Remark 2.5. The optimality statement in Theorem 2.4 is tied to the standard definition of the canonical correlation matrix, which uses the inverse principal square roots Γ −1/2 

> p

and Γ −1/2 

> m

. If a different choice of whitening filters is used, for example Cholesky factors, then an analogous state-ment can be obtained only after redefining the objective in terms of those whitening filters. More precisely, for fixed whitening filters Lp and Lm satisfying Γ p = ( LTp Lp)−1 and Γ m = ( LTmLm)−1, the construction Γ pm = L−1 

> p

CL −Tm is optimal for matching C in the transformed coordinates LpΓpm LTm.Thus different factorisations lead to different interpretations of the matrix C, even though they all produce valid joint covariance matrices. In Section 2.4 we provide additional motivation for choos-ing the principal square root. 

2.2. Generating samples. Here we show how to efficiently draw samples from the joint dis-tribution of π(p, m ). To this end, we present the following theorem showing how samples can be drawn from π(p, m ) and how the joint whitening filter can be constructed efficiently. 

Theorem 2.6. For p∗ ∈ Rn1 and m∗ ∈ Rn2 , let p ∼ N (p∗, Γp) and m ∼ N (m∗, Γm) where 

Γp = ( LTp Lp)−1 ∈ S+ 

> n1

and Γm = ( LTmLm)−1 ∈ S+ 

> n2

, and suppose C ∈ An1×n2 . Then, if s = ( p, m )

has a (joint) density of the form π(s) = N (s∗, Γ) with s ∈ Rn and Γ ∈ S+ 

> n

as in Theorem 2.2 and 

η ∼ N (0 , I ), samples from the joint density can be computed by 

˜s =

 L−1 

> p

0

L−1 

> m

CT L−1 

> m

D



η + s∗,(2.7) 

> 4

where the matrix D ∈ Rn2×n2 satisfies DD T = I − CT C (i.e., D is the defect operator of C). Moreover, 

L =

 Lp 0

−D−1CT Lp D−1Lm



(2.8) 

satisfies Γ = ( LT L)−1.Proof. First, to prove (2.7) it is clear that E[˜ s] = s∗. For the covariance, assuming without loss of generality that s∗ = 0, we have 

E ˜s˜sT  = E

 L−1 

> p

0

L−1 

> m

CT L−1 

> m

D



ηη T

L−Tp CL −Tm

0 DT L−Tm

 

=

 L−1 

> p

0

L−1 

> m

CT L−1 

> m

D

  L−Tp CL −Tm

0 DT L−Tm



=

 Γp L−1 

> p

CL −Tm

L−1 

> m

CT L−Tp L−1 

> m

(CT C + DD T )L−Tm



= Γ ,

since E[ηη T ] = I and CT C + DD T = I.On the other hand, to prove (2.8), since C ∈ An1×n2 , we have I − CT C ∈ S+ 

> n2

, implying D is invertible, and thus 

 LT L Γ = 

Lp −LpCD −T

0 LmD−T

  Lp 0

−D−1CT Lp D−1Lm

  Γp L−1 

> p

CL −Tm

L−1 

> m

CT L−Tp Γm



=

Lp −LpCD −T

0 LmD−T

  L−1 

> p

CL −1

> m

0 DT L−1

> m



= I, 

concluding the proof. 

2.3. Sampling examples. Here we provide two examples of generating samples from the joint density of two distributed parameters given the associated marginal densities and a desired corre-lation structure. 

Example 1. For the first example we assume both parameters are distributed over the rectangle Ω = [0 , 2] × [0 , 1]. We discretise the rectangle using the same regular lattice for each parameter, leading to p, m ∈ R5000 . The marginal priors are set as π(p) = N (0 , Γp) and π(m) = N (0 , Γm). The marginal covariance matrix Γ p is defined using a PDE-based covariance matrix [32, 35, 41] coming from a finite element discretisation of an elliptic PDE using linear Lagrange basis functions. More specifically, we set Γp = ( a1K + a2M + a3B)−2,(2.9) with K, M , and B the stiffness, mass, and boundary mass matrices, respectively, given by 

Kij =

Z

> Ω

Θ∇ϕi · ∇ ϕj dx, Mij =

Z

> Ω

ϕiϕj dx, Bij =

Z

> ∂Ω

ϕiϕj dx, (2.10) where {ϕ(x)}Ni=1 denotes the linear Lagrange basis functions, and i, j ∈ { 1, 2, . . . , n 1} where n1 =5000. The parameters a1, a 2 > 0 control the desired correlation length and variance, while Θ ∈ S+2

can be used to set anisotropy (i.e., different correlation lengths in different directions) and a3 ∝√a1a2 is used to mitigate so-called boundary effects, see for example [32, 41, 42]. Here we set 

a1 = 4 × 10 −2, a2 = 1, a3 = 0 .125, and Θ = I.On the other hand, the marginal covariance matrix Γ m is taken as the exponential squared covariance matrix [43], i.e., Γm(r) = exp 



− 12

 rℓ

2

, r = ∥x∥ ,(2.11) 

> 5

p (Example 1 (a) and 1 (b)) 

m (Example 1 (a)) 

m (Example 1 (b))                   

> Figure 1. Samples from the joint priors in Example 1. The first row shows three samples of p, the second row shows three corresponding samples of m, where the desired correlation structure is encoded using c(x) = 0 .999
> (Example 1 (a)), and the third row shows three corresponding samples of m, where the desired correlation is encoded using c(x) = 0 .999 for x≤1and c(x) = −0.999 for x > 1(Example 1 (b)). As expected, in the first case (Example 1 (a)), the two parameters appear to be strongly positively correlated throughout the domain, while in the second case (Example 1 (b)), the samples of the two parameters appear to be strongly positively correlated in the left half of the domain and strongly negatively correlated in the right half of the domain.

with ℓ = 0 .2 the correlation length. Finally, for the first example we consider two different correla-tion structures: (a) we attempt to encode homogeneous strong positive correlation throughout the domain by setting C = 0 .999 I, and (b) we attempt to encode strong positive correlation on the left half of the domain strong negative correlation right half of the domain, i.e., we set C = c(x)I, where 

c(x) = 0 .999 for x ≤ 1 and c(x) = −0.999 for x > 1. It is worth noting that (in the function space setting) the exponential squared covariance operator (2.11) generates smooth (i.e., infinitely dif-ferentiable) samples, while the PDE-based covariance operator (2.9) generates functions in H1(Ω), that is to say, the samples of m should be significantly smoother than the samples of p.Three samples from the joint densities for cases (a) and (b) are shown in Figure 1. The approach works as intended: the samples of the two parameters for Example 1 (a) appear to be strongly positively correlated throughout the domain, while the samples of the two parameters for Example 1 (b) appear to strongly positively correlated in the left half of the domain and strongly negatively correlated in the right half of the domain. We also see the difference in smoothness of the two parameters due to the difference in the associated marginal prior covariance matrices (see (2.9) and (2.11)). 

Example 2. In the second example we consider a case in which one parameter is distributed over the rectangle Ω = [0 , 2] × [0 , 1] while the other is distributed over the bottom boundary 

Bb = [0 , 2] × { 0}. We discretise the rectangle with 10000 nodes, of which 100 lie on Bb. As such 

p ∈ R10000 and m ∈ R100 . In this example we again take Γ p to be of the form (2.9), though now we take a1 = a2 = 1, a3 = 0 .125, and impose an anisotropic smoothness structure by setting Θ = diag(1 , 0.025) ∈ R2×2. We set Γ m again as the exponential squared covariance matrix (see (2.11)), but use a correlation length of ℓ = 0 .1. The desired correlation between the restriction of 

p to the boundary Bb and m is set at 0.999 homogeneously. This is done by taking C ∈ R10000 ×100 

with Cij = 0 unless i and j correspond to the same boundary node, in which case Cij = 0 .999. 

> 6

p

m and p|Bb             

> Figure 2. Three samples from the joint priors in Example 2. In the top row we show three samples of p, while on the bottom row we show the three corresponding samples of min red as well as the restriction of pto the boundary, i.e., p|Bb, in blue. The desired correlation structure is encoded using c(x) = 0 .999 for x∈Bb.

In Figure 2 we show three samples of p and m (along with the restriction of p to the Bb, i.e., 

p|Bb ) drawn from the joint density π(p, m ). As in the previous example, we see that the approach works as intended, with p|Bb and m being positively correlated across Bb. Moreover, the anisotropic smoothness of p and higher order smoothness of m are evident. 

2.4. Choice of factorisation. The proposed joint covariance matrix Γ given in (2.3) is a proper covariance matrix independent of the choice of factorisation used, that is, Γ ∈ S+ 

> n

for any L−1 

> p

and 

L−1 

> m

that satisfy Γ p = ( LTp Lp)−1 and Γ m = ( LTmLm)−1. However, as pointed out in Remark 2.5, for different choices of whitening filters, the resulting cross-correlation structure is different. To see this, consider two new factors, say ˆLp and ˆLm satisfying ( ˆLTp ˆLp)−1 = ( LTp Lp)−1 = Γ p and ( ˆLTm ˆLm)−1 = ( LTmLm)−1 = Γ m, but where Lp̸ = ˆLp, and Lm̸ = ˆLm. Then in general, 

L−1 

> p

CL −Tm̸ = ˆL−1 

> p

C ˆL−Tm ,(2.12) which leads to differences in the joint statistics, in particular, the actual resulting cross-correlation. There is an infinite number of choices for each of the whitening filters. The most frequently used whitening filters in PDE-constrained inference are the (upper or lower) Cholesky factor, and the principal square root, both of which are unique [44]. However, as we demonstrate below, use of the Cholesky factor typically induces some possibly unwanted structure into the cross-correlation matrix Φ, due to its triangular (causal) form. We therefore would generally advocate for using the principal square root (if computationally feasible). To shed some light on the effects of using the different whitening filter we consider a one dimensional example where the two parameters p and m are defined over a regular discretisation of Ω = [0 , 1]. Suppose further that the marginal densities are identical, i.e., p ∼ N (0 , Γp) and 

m ∼ N (0 , Γm) with Γ p = Γ m given by the exponential squared covariance matrix (see (2.11)) with correlation length ℓ = 0 .1. We attempt to encode strong positive correlation in the left half of the domain and strong negative correlation in the right half of the domain by taking c(x) = 0 .999 for 

x ≤ 12 and c(x) = −0.999 for x > 12 .The resulting cross-correlation and several samples from the joint distributions are shown in Figure 3 when taking the principal square root factorisation and when taking the Cholesky factori-sation. As alluded to above, use of the Cholesky factor induces some (spatial) skewness towards the right in the cross-correlation matrix, while the principal square root factor does not. In this example, this leads to samples from the joint distribution being positively correlated over most of the domain. On the other hand, samples from the joint distribution based on the principal square root are equally positively and negatively correlated over the domain. 

> 7

Correlation Principal square root Cholesky factor              

> Figure 3. Demonstration of the effects of different factorisations of Γpand Γm. On the left we show the desired correlation structure encoded in diag( C)(in black), along with the actual resulting diagonal of the correlation matrices
> Φ⋆(in blue) and Φ▷(in red) found using the principal square root and the Cholesky factor, respectively. In the centre and on the right we show samples from the joint prior of p(in blue) and m(in red) using the principal square root factor and Cholesky factor, respectively.

It is also worth pointing out at this point, for many Bayesian inverse problems the prior covari-ance matrices used are smoothing operators [23, 24, 32, 33]. Thus the left- and right-conditioning of 

C by L−1 

> p

and L−1 

> m

, respectively, tends to smooth the intended cross-correlation embedded in C, as seen in Figure 3. 

3. Ignorance isn’t always bliss. If there is no prior knowledge of any relationship (correlation or otherwise) between parameters, then the default approach is to treat them as independent, see e.g., [9, 14–16] among many. In the case of a joint Gaussian prior this amounts to setting the cross-correlation to 0, i.e., taking the prior covariance matrix as block diagonal. However, in the Bayesian framework, setting the correlation to 0 implies the correlation is known a priori to be 0. This can at best result in overly optimistic uncertainty estimates, and at worst can result in significant overconfidence in heavily biased estimates. To help remedy this situation one should, at least attempt to, model the uncertainty in the correlation. This can be done by taking C as a random variable with an associated prior density 

π(C). If no prior beliefs on the cross-correlation are held, a natural starting point is the uniform prior over contractions, i.e., π(C) = U(An1×n2 ). Independent of the choice of prior taken on C, the full posterior can be written as 

π(p, m, C |d) ∝ π(d|p, m, C )π(p, m, C ) = π(d|p, m )π(p, m |C)π(C),(3.1) since the likelihood is conditionally independent of C.Incorporating cross-correlation into the prior through the proposed methodology means the joint prior of π(p, m, C ) can be decomposed in a straightforward manner. Specifically, taking 

s = ( p, m ) ∈ Rn we have 

π(s, C ) = π(s|C)π(C)(3.2) 

∝ 1

|Γ| 12

exp 



− 12 ∥s − s∗∥2Γ−1



π(C)(3.3) = exp 



− 12



∥s − s∗∥2Γ−1 + ln |Γ|



π(C)(3.4) 

∝ exp 



− 12



∥s − s∗∥2Γ−1 + ln |I − CC T |



π(C),(3.5) where the last line follows from the fact that 

|Γ| = |Γp − (L−1 

> p

CL −Tm )Γ −1 

> m

(L−1 

> m

CT L−Tp )| · | Γm| = |L−1 

> p

| · | I − CC T | · | L−Tp | · | Γm| ∝ | I − CC T |.

It is worth noting that for cases in which n1 > n 2, for computational ease, Sylvester’s determinant theorem can be used, i.e., |I −CC T | = |I −CT C|. Furthermore, if C is diagonal we have |I −CC T | =

Qmin {n1,n 2} 

> i=1

(1 − C2

> ii

). 

> 8

3.1. Computational considerations. We will be particularly interested in accurately charac-terising the full posterior using Markov chain Monte Carlo (MCMC) methods (Section 3.2 provides motivation for this). To define the likelihood used throughout the numerical experiments, we con-sider the additive error model, i.e., a relationship between the data and parameters of the form in (1.2). We assume the observational errors are centred Gaussian and independent of the unknown parameters, i.e., e ∼ N (0 , Γe) with ( p, m ) ⊥ e. The likelihood is then [26] 

π(d|p, m ) ∝ exp 

n

− 12 ∥d − G (p, m )∥2Γ−1

> e

o

.(3.6) For PDE-based inverse problems with higher-dimensional unknown parameters (the problems of interest here) the computational costs of characterising the posterior are dominated by repeated forward solves, while statistical efficiency is dominated by how well the sampler mixes. To address these challenges, we propose (i) a Metropolis-within-Gibbs (MwG) approach to the sampling to leverage conditional distributions available from the joint prior, and (ii) a truncated Karhunen-Lo` eve (KL) expansion of the parameters to reduce the dimensions. 

An adaptive Metropolis-within-Gibbs sampler. The MwG sampler is well-suited to Bayesian in-verse problems involving hyper-parameters and has been widely applied to such problems, see for example, [45–49]. Setting s = ( p, m ) ∈ Rn, the approach is based on generating samples from the joint posterior π(s, C |d) by alternately updating s conditional on C, and C conditional on s. More precisely, starting from the current state 

s(k) ∼ π(s|C(k), d ) ∝ exp {− 12 α(s, C (k))},

where 

α(s, C ) = ∥d − G (s)∥2Γ−1 

> e

+ ∥s − s∗∥2Γ−1(C) ,

and then 

C(k+1) ∼ π(C|s(k), d ) ∝ exp {− 12 β(s(k), C )},

where 

β(s, C ) = ∥s − s∗∥2Γ−1(C) + ln |I − CC T | − 2 ln π(C).

In practice, when direct sampling from these conditional distributions is not available, the updates are carried out using Metropolis–Hastings steps. In the present work we restrict attention to Gaussian proposal distributions q(·|· ), although a range of more sophisticated proposal mechanisms and sampling strategies could also be employed. The resulting adaptive MwG scheme is summarised in Algorithm 3.1. 

Remark 3.1. The Metropolis-within-Gibbs approach is particularly well suited for cases in which the forward model is linear, i.e., G (s) = Gs for some G ∈ Rq×n, since in such cases π(s|C, d )is Gaussian. Consequently, updating s can be done directly by sampling from π(s|d, C ), avoiding the need for an additional Metropolis step, i.e., the approach simplifies to exact Gibbs updates for 

s, with only the updates for C requiring Metropolis-Hastings. 

Dimension reduction based on truncated Karhunen-Lo` eve expansion. A standard approach used to reduce the parameter dimension in Bayesian inverse problems is to use a truncated KL expansion of the parameters [50–52]. First, we consider the eigenvalue decomposition of each of the prior covariance matrices; Γp =

> n1

X

> i=1

λivivTi = V ΛV T , Γm =

> n2

X

> j=1

σj uj uTj = U ΣU T .(3.7) The truncated KL expansions of each of the parameters are then given by ˜p = p∗ +

> kp

X

> i=1

pλi ˆpivi, ˜m = m∗ +

> km

X

> j=1

√σj ˆmj uj ,(3.8) 

> 9

Algorithm 3.1 Adaptive Metropolis-within-Gibbs  

> 1:

Choose an initial state ( s(0) , C (0) ) ∈ Rn × A n1×n2 , and set k = 0  

> 2:

for k = 0 , 1, 2, . . . , M − 1 do  

> 3:

Propose s′ ∼ q(k) 

> s

( · | s(k)) 

> 4:

Compute 

as = min 

(

1, π(s′|C(k), d ) q(k) 

> s

(s(k)|s′)

π(s(k)|C(k), d ) q(k) 

> s

(s′|s(k))

) 

> 5:

Set s(k+1) = s′ with probability as; otherwise set s(k+1) = s(k) 

> 6:

Propose C′ ∼ q(k) 

> C

( · | C(k)) 

> 7:

Compute 

aC = min 

(

1, π(C′|s(k+1) , d ) q(k) 

> C

(C(k)|C′)

π(C(k)|s(k+1) , d ) q(k) 

> C

(C′|C(k))

) 

> 8:

Set C(k+1) = C′ with probability aC ; otherwise set C(k+1) = C(k) 

> 9:

Update the proposal parameters defining q(k) 

> s

and/or q(k) 

> C

if adaptation is used  

> 10:

end for 

where we take kp ≪ n1 and km ≪ n2. The goal of the joint inference problem is then to estimate ˆp ∈ Rkp and ˆ m ∈ Rkm , which have associated prior distributions ˆ p ∼ N (0 , I ) and ˆ m ∼ N (0 , I ). Furthermore, for a given desired cross-correlation encoded in C ∈ An×m, the joint prior covariance for ˆ p and ˆ m is (using (3.7)) ˆΓ = Λ− 12 V T 00 Σ− 12 U T

!

Γ V Λ− 12 00 U Σ− 12

!

=

 I V T CU U T CT V I



,

while the joint prior mean is (ˆ p∗, ˆm∗) = (0 , 0). 

3.2. Parameterisation, identifiability and estimability of C. In this section we briefly outline some important features related to the parameterisation, identifiability, and estimability of the matrix C.

Parameterisation and identifiability. First, we note that there are several standard choices of prior distributions used when estimating covariance and correlation matrices, e.g., Wishart priors and/or Lewandowski-Kurowicka-Joe (LKJ) priors [53]. However, in the current study the marginal prior covariance matrices should not be altered, thus a natural (non-informative) choice for the prior on C is π(C) = U(An1×n2 ), i.e., the uniform distribution over n1 × n2 strict contractions. Then, by Theorem 2.2, for any choice of C from π(C), the joint covariance matrix Γ is a proper covariance matrix. Thus, in theory, given π(C) and measured data we could attempt to estimate the n1 × n2 elements of C. However, without strong regularisation or additional structure the problem of estimating C is significantly under-determined. In the current paper we attempt to remedy this situation by (a) postulating only correlation structures based on diagonal matrices C

and (b) we enforce strong spatial structure within C. The choice of restricting to diagonal matrices 

C can be seen as prescribing (or estimating) only the canonical correlations themselves (recall the spectrum of Γ − 12 

> p

Γpm Γ− 12 

> m

= C is the canonical correlations) and leads to a uniform prior of the form 

π(C) = Π min {n1,n 2} 

> i=1

π(Cii ) = Π min {n1,n 2} 

> i=1

U(−1, 1) .

In the examples considered in the current work the parameters of interest p and m represent spatially distributed geophysical quantities. Nonspatial cross-correlations of p and m are usually estimated from samples obtained from various locations, for example, from boreholes, and treated as spatially homogeneous, suggesting corr( p(x), m (x)) ≡ c over x ∈ Ωk for a specific subsurface type. On the other hand, the (marginal) spatial correlation structures are supposed to be specified 

> 10

first. Accordingly, in the numerical examples considered here we restrict attention to diagonal contractions. To ensure |Cii | < 1, we reparameterise each of the correlation coefficients as Cii =tanh( γi), with γi ∈ R having the prior induced by the transformation, i.e., π(γi) = 12 sech 2(γi). 

Estimability. The maximum a posteriori (MAP) estimate, defined as the point † that maximises the posterior, is often used as a computationally efficient estimator of the parameters. However, like any other point estimate, the MAP estimate can poorly represent the posterior (see [26, Section 3.1.1]). Indeed, for the approach proposed here, when estimating both the parameters and the correlation (encoded via) C, the joint MAP estimate can be arbitrarily misleading, due to the unbounded form of the joint prior. We therefore use MCMC methods in this work and use the conditional mean (CM) estimate as the representative point estimate. This issue is illustrated in the Monod example of the next section, while more insights are given in Appendix A. 

3.3. A preliminary inference example. Here, we demonstrate the applicability of the proposed approach for inference on a simple algebraic model from microbiology and microbial ecology, the Monod model [56]. The Monod model is used to model bacterial population μ as a function of substrate concentration S in the form 

μ = pS m + S ,(3.9) where p denotes the maximum population, and m the half-velocity constant. The Monod model has been used in the literature previously to demonstrate proposed methods in Bayesian inference, see [57]. The parameters of interest ( p, m ) are to be estimated based on several measurements of the population at different substrate concentrations. Specifically, we take measurements at 

S = [28 , 55 , 83 , 110 , 138 , 225 , 375] which are corrupted by centred additive iid Gaussian noise. We consider two slightly different approaches to the problem. First, to illustrate the dangers of mis-modelling the correlation, we consider the estimation problem using different values for the cross-correlation. Secondly, we consider the problem when the cross-correlation is also taken to be a random variable. In all cases the prior means are taken as p∗ = 0 .4 and m∗ = 40, while the marginal prior standard deviations are set at σp = 0 .1 and σm = 10. In the first of these cases we solve the problem using c ∈ C := {− 0.99 , −0.85 , 0, 0.85 , 0.99 }, and for noise levels of standard deviation δe = 0 .1 and δe = 0 .03. That is, the prior and noise models are given by 

π(p, m ) = N

 0.440 



,

0.12 cc 10 2

 

, π(e) = N (0 , δ 2 

> e

I),

and for the second approach ( c being unknown) we set π(c) = U(−1, 1) and consider δe = 0 .03 only. Level set plots of each of the prior distributions and resulting posterior distributions for each 

c ∈ C and both noise levels are shown in Figure 4. The truth is taken as ( ptrue , m true ) = (0 .7, 65), so that we expect the joint inference to perform better (i.e., give posterior densities which support the truth better) when using the positively correlated priors. This is indeed the case, particularly in the case with larger noise. In both cases the posterior found when using a prior with c = 0 .85 gives the best results. In the second approach to the Monod model we take p, m and c as unknown and compute the joint posterior π(p, m, c |μ). A corner plot showing the one and two parameter marginal posterior distributions is shown in Figure 5. The approach seems to perform fairly well; The marginal densities of the parameters p and m contain the true values well, and the posterior for c indicates that a positive correlation is more likely than a negative one. The two dimensional marginals and the marginal distribution of c illustrate the potential pitfalls of using an optimisation-based inference approach. When c → 1 the posterior becomes unbounded and poorly represents the truth, see Appendix A for more insight into this. 

> †See [54, 55] for an infinite-dimensional interpretation
> 11

m

p p p

Prior Posterior ( δe = 0 .1) Posterior ( δe = 0 .03) 

Figure 4. Joint distributions for the Monod example with fixed values of c. On the left are the joint priors, in the middle are the joint posteriors for δe = 0 .1, while on the right are the joint posteriors for δe = 0 .03 . In all cases the colors red, yellow, black, green, blue are used for the cases c = −0.99 , c = −0.85 , c = 0 , c = 0 .85 , and c = 0 .99 ,respectively. Furthermore, in all cases the black cross represents the truth. 

m

p

c

c

m c

Figure 5. Corner plot for the posterior of the Monod example where c is also treated as an unknown and estimated. The true parameters are (ptrue , m true ) = (0 .7, 65) .

4. Higher-dimensional computational experiments. In this section we consider two examples motivated by geophysical situations in which the (discretised) parameters are significantly higher dimensional than the previous example. We first consider an example of co-Kriging, and secondly a subsurface flow problem where we estimate the permeability and recharge of an aquifer. 

12 In both examples the computational domain is taken as Ω = [0 , 2] × [0 , 1]. Furthermore, in both examples we consider a finite element method (FEM) discretisation of the parameters. For simplicity, both parameters, p and m, are represented using the same FEM basis functions, i.e., 

p = Pn1 

> i=1

piϕ(x) and m = Pn2 

> i=1

miψ(x) (note we take n1 = n2). The mesh consists of 1250 nodes, and 2352 triangular elements, thus (with a slight abuse of notation) the parameters of interest are the p, m ∈ R1250 coefficients of the FEM basis functions. In line with the rest of the paper, the PDE-based covariance matrix (Equation (2.9)) and the exponential-squared covariance matrix (Equation (2.11)) are used as the marginal prior covariance matrices. Specifically, for Γ p we use an exponential-squared covariance matrix with a correlation of ℓ = 0 .3, while for Γ m we use the PDE-based covariance matrix with a1 = 1 .5, a2 = 30, and 

a3 = 7 .5. Furthermore, in both examples we use our proposed approach to estimate p, m as well as C. We compare the results against those found when we assume a priori that the parameters p

and m are independent. In the first example we also show results found when using fixed values of the C.

Metrics. To investigate the performance of the proposed joint approach, we compare the results against those obtained under the standard assumption that the two parameters are a priori inde-pendent. Specifically, we consider the relative errors in the CM estimates and the true parameter values, 

E(p) = ptrue − p∗| d / ∥ptrue ∥ , E(m) = mtrue − m∗| d / ∥mtrue ∥ .(4.1) Furthermore, to quantify the uncertainty reduction (from prior to posterior), we introduce a mea-sure of relative posterior uncertainty 

U(p) = tr  Γp|d

 /tr (Γ p) , U(m) = tr  Γm|d

 /tr (Γ m) ,(4.2) such that U(·) ∈ [0 , 1], with smaller values indicating a larger reduction in uncertainty. We note that the aim is not necessarily to obtain smaller posterior variances, but to accurately model, incorporate, and quantify the uncertainty in the unknown parameters and the correlation. Finally, we also examine the difference in the point-wise posterior standard deviations 

Rn1 ∋ Dp = diag (Γ I

> p

) 12 − diag (Γ J

> p

) 12 , Rn2 ∋ Dm = diag (Γ I

> m

) 12 − diag (Γ J

> m

) 12 ,(4.3) where Γ I 

> p

(resp. Γ I

> m

) and Γ J 

> p

(resp. Γ J

> m

) are the posterior covariance matrices of p (resp. m) found using independent inference and joint inference, respectively. 

4.1. Computational example 1: Co-Kriging. For the first example we consider a classic co-kriging inspired problem. Specifically, we let Ω denote the computational domain of interest and assume we have direct (noisy) measurements of p and of m at different sets of measurement loca-tions. The full inference problem is then to compute the joint posterior π(p, m, C |d1, d 2) given the measurements d1 and d2 which are assumed to be of the form (4.4) d1 = B1p + e1

d2 = B2m + e2,

with B1 and B2 the point-wise restriction/observation operators which evaluate p and m, respec-tively, at the observation locations, see Figure 6. It is worth pointing out that as both forward models are linear, for a fixed correlation between 

m and p, the posterior is Gaussian and can be computed analytically [26]. However, when the goal is to also estimate the correlation, the inference problem is no longer linear, which can significantly increase the computational overheads. 

> 13

ptrue mtrue      

> Figure 6. The true parameters ptrue (left) and mtrue (right). In each plot the black dots indicate the locations in which the noisy (direct) point-wise measurements are taken.

4.2. Computational details for Example 1. In this example we parameterise the unknown correlation using C = cI , where c is a scalar with a uniform prior distribution, i.e., c ∼ U (−1, 1). That is, the correlation is assumed to be homogeneous throughout Ω. The total number of unknowns is thus 1250 + 1250 + 1 = 2501. On the other hand, the data consists of 60 point-wise measurements of p in the right half of the domain and 32 point-wise measurements of m in the top half of the domain, see Figure 6. To generate the true parameters (and data) we set a true correlation of ctrue = −0.9 and generate ptrue and mtrue as samples from the joint conditional distribution 

π(p, m |c = ctrue ). The resulting true parameters as well as the true potential induced by ptrue 

are shown in Figure 6. Synthetic data is then generated by applying the observation operators and adding white noise of the form e1 ∼ N (0 , δ 21 I) and e2 ∼ N (0 , δ 22 I). For this example we set 

δ1 = 1 /100 ×(max( B1ptrue )−min( B1ptrue )) and δ2 = 1 /100 ×(max( B2mtrue )−min( B2mtrue )). That is, both sets of synthetic data are corrupted by 1% noise. To further illustrate the importance of correctly accounting for the (uncertainty in the) cor-relation, for this example we also compute the posterior for two additional fixed choices of c:

c = ctrue = −0.9 and c = −ctrue = 0 .9. For each of the three cases of fixed correlations, 

c ∈ {− 0.9, 0, 0.9}, the CM estimates and conditional covariances of p and m are calculated us-ing the well known formulae [26, Eqs. (3.14) and (3.15)]. On the other hand, to compute the full joint posterior π(p, m, c |d1, d 2) we use the MwG approach outlined in Algorithm 3.1. However, as noted in Remark 3.1, for cases in which the forward model is linear, which is the case for Example 1, the MwG approach simplifies considerably, with only the correlation C requiring a Metropolis step for updating, and p and m updated by directly sampling from π(p, m |d, C ). Since in Example 1 we have C = cI , the correlation update reduces to a one-dimensional Metropolis step for the scalar parameter c. As noted in Section 3.1, we reparameterise the correlation as c = tanh ( γ). We initialize the chain for γ at γ = c = 0 and update using a Gaussian proposal centred at the current state with unit variance, i.e., 

γ′ = γ(k) + ξ(k), ξ(k) ∼ N (0 , 1) .

The MCMC is run for a total of 10 5 samples, with the first 10 3 discarded as burn-in ‡. The results found using each of fixed correlations and for the joint approach are discussed in the next section, while a discussion on the computational costs and effectiveness of the MCMC is left to Section 4.7. 

4.3. Results for Example 1. For Example 1, the CM estimates of both p and m obtained using independent and joint inference are shown in Figure 7, along with the true parameters used to generate the data. As expected, we see that when using independent inversion, the CM estimates represent the truth fairly well near the respective measurement locations. However, away from the measurement locations, the estimates found using independent inversion are less insightful and converge spatially towards the prior mean. On the other hand, when using the proposed joint       

> ‡The length of the burn-in may seem small, however, only the scalar parameter cis updated via a Metropolis step, while pand mare sampled directly from their conditional posterior distribution.
> 14

p

m     

> Figure 7. True parameters and CM estimates for Example 1. In the top row we show the CM found using independent inference (left), the true parameter ptrue (centre), and the CM found using joint inference (right). On the bottom row the corresponding plots for m.

p

m        

> Figure 8. The reduction in uncertainty for Example 1. On the top row we show the point-wise marginal posterior standard deviation for pfound using independent inference (left), the point-wise marginal posterior standard deviation for pfound using joint inference (centre), and the difference of the two Dp(see Equation 4.3). On the bottom row we show the equivalent figures for m.

approach the CM estimates improve significantly compared to the independent estimates as data from both models is taken into account when estimating both parameters. This is, of course, an intended benefit of carrying out the inversion jointly. For this example, the error in the CM estimates using the independent approach is E(p) = 0 .852 and E(m) = 0 .629, whereas the proposed joint approach reduces these to E(p) = 0 .513 and E(m) = 0 .589. In Figure 8 we show the point-wise posterior standard deviations obtained using independent and joint inference, together with their difference (defined (4.3)). As expected, the largest reduc-tion in uncertainty for each of the parameters is near the measurement locations. However, as discussed above for the CM estimates, using the joint approach incorporates both sets of data into both estimates. Consequently, we see that when using the proposed joint approach, the posterior uncertainty in p is significantly reduced where m is measured, and that the posterior uncertainty in m is significantly reduced where p is measured. These differences are made apparent in the plots of the differences in the point-wise posterior standard deviation shown in Figure 8. Quantitatively, the relative posterior uncertainties are U(p) = 0 .401 and U(m) = 0 .411 using independent inference, and U(p) = 0 .313 and U(m) = 0 .291 when using the joint approach. That is to say, in this example using the joint approach significantly reduces the posterior uncertainty for both parameters.           

> 15 Figure 9. Results for estimating the correlation cin Example 1. Shown on the left is the histogram of samples from the marginal posterior π(c|d), the prior π(c)(red line), and the truth ctrue (black dashed line), while in the centre we show the trace of the samples for cand on the right the sample autocorrelation.

A key benefit of the proposed joint approach is the ability to estimate the correlation also. In Figure 9 we show the marginal histogram of the samples of c (recall C = cI for this example), the trace of the MCMC samples of c, and the autocorrelation plot. The posterior places most of its mass on negative values of c, with the true value, ctrue = −0.9 being well contained within the bulk of the posterior. This is consistent with the improved reconstructions obtained using the proposed joint inference approach, and indicates that the data are informative about the sign and magnitude of the correlation. The trace plot and sample autocorrelation suggest that the chain mixes reasonably well, although a discussion of computational costs is deferred to Section 4.7. To further illustrate the importance of correctly accounting for the correlation, we also compute the posterior for two fixed choices of c: c = ctrue = −0.9 and c = −ctrue = 0 .9. The resulting CM estimates and posterior uncertainty are shown in Figure 10. Fixing c = ctrue results in CM estimates which agree well with the true parameters, while fixing c = −ctrue gives misleading estimates. The largest reductions in marginal posterior uncertainty occur near the measurement locations associated with the parameters. However, as the correlation is strong, measurements of one parameter also reduce the uncertainty in the other. As shown in Appendix B, the marginal posterior covariance is invariant under the sign change c 7 → − c in the present linear-Gaussian setting. Consequently, an incorrect sign of the correlation can yield a concentrated posterior in the wrong region of parameter space, so that the truth is poorly supported. 

p

m                             

> Figure 10. Results for Example 1 using fixed values of c. On the top row we show the CM estimate of pusing
> c=ctrue =−0.9(left), the CM estimate of pusing c=−ctrue = 0 .9(centre), and the point-wise marginal posterior standard deviation of pusing c=ctrue or c=−ctrue (right). Recall the point-wise marginal posterior standard deviation is invariant to the sign of c. In the bottom row we show the corresponding plots for m.
> 16

ptrue mtrue u      

> Figure 11. True parameters and data for Example 2. The true log-permeability ptrue (left), the true log-recharge
> mtrue (centre), as well as the resulting hydraulic-head u(right). In each plot the black dots indicate the locations of the measurements.

4.4. Computational example 2: Inversion for aquifer permeability and recharge. We now consider the more realistic subsurface flow problem of estimating the permeability and recharge of an aquifer based on measurements of hydraulic-head. This problem leads to a joint inverse problem governed by a single physical model. Specifically, taking the permeability κ = exp( p) and the recharge r = exp( m), we assume the hydraulic-head u satisfies (4.5) −∇ · (exp( p)∇u) = exp( m), in Ω 

u = 0 , on ∂Ω.

As well as measurements of the hydraulic-head, we assume we also have direct point-wise measure-ments (e.g., core samples or borehole measurements) of the permeability. As such, the goal in the second problem is to characterise the joint posterior π(p, m, C |d1, d 2) given the measurements of the form (4.6) d1 = B1u(p, m ) + e1

d2 = B2p + e2,

where B1 and B2 denote the point-wise observation operators. We assume the hydraulic-head is measured at shallow locations throughout the domain, while the direct measurements of p are taken along several well-paths, see Figure 11. In this example, we allow for the possibility of different correlations (between p and m) in different subdomains. Specifically, we divide the domain Ω into two subdomains, Ω 1 = {(x, y ) ∈

Ω | x ≤ 1} and Ω 2 = {(x, y ) ∈ Ω | x > 1}, and allow for different correlations in each of these subdomains, i.e., we take 

C = c(x)I, c(x) = 

(

c1, for x ≤ 1

c2, for x > 1,

(4.7) where |c1| < 1 and |c2| < 1 are a priori unknown §

4.5. Computational details for Example 2. We solve (4.5) for u using a Galerkin finite element method (FEM). More specifically, we represent the potential u and (initially) the permeability 

p and recharge m using 1250 continuous piecewise linear basis function on a regular mesh of 1250 nodes and 2352 triangular elements. To reduce the computational costs associated with the MCMC sampling, we use a truncated KL expansion of the parameters, as outlined in Section 3.1. Specifically, we truncate the expansions for p and m after kp = 50 and km = 100 (see Equation (3.8)), respectively, which captures approximately 99% and 94% of the variation in each parameter, respectively. This results in a total number of parameters to be inferred of 50 + 100 + 2 = 152. Similarly to the previous example, the true parameters are generated by setting a 

> §Although not considered here, it is worth noting that both the number and geometry of the subdomains could also be treated as unknown, and estimated. This, however, would significantly increase the associated computational costs, and is beyond the scope of the current study.
> 17

true correlation (in this case ctrue = (0 .8, −0.9)) and then sampling ptrue and mtrue from the joint conditional distribution π(p, m |c = ctrue ). Lastly, the data consists of 56 regularly spaced measurements of u and 45 measurements of p, see Figure 11. Additive noise of the form e1 ∼N (0 , δ 21 I) and e2 ∼ N (0 , δ 22 I) corrupts the measurements, where, for this example, we set δ1 =5/100 × (max( B1u(ptrue , m true )) − min( B1u(ptrue ))) and δ2 = 2 /100 × (max( B2ptrue ) − min( B2ptrue )). That is, the hydraulic-head measurements are corrupted by 5% noise while the direct measurements of p are corrupted by 2% noise. The true parameters and resulting measurements are shown in Figure 11. To characterise the posterior distributions for Example 2 we use MCMC for both the indepen-dent and joint approaches, since the forward model is nonlinear. In the independent case we sample the KL coefficients only, while in the joint approach we sample the KL coefficients and c1 and c2,which as described in Section 3.1, we reparameterise as c1 = tanh( γ1) and c2 = tanh( γ2). The parameters γ1 and γ2 are updated using an uncorrelated Gaussian random-walk proposals, i.e., we propose 

γ′ 

> i

= γ(k) 

> i

+ ξi, ξ(k) 

> i

∼ N (0 , 1) , i ∈ { 1, 2}.

As updating the correlations is cheap (compared to updating p and m), in this example we take 100 steps of Metropolis-Hastings updates for the correlation parameters γ1 and γ2 before each update of p and m. Since the dimensions of ˆ p and ˆm are significantly larger, we update them using an adaptive Metropolis step for improved computational efficiency [58]. Specifically letting ˆs = (ˆ p, ˆm) ∈ Rkp+km we use proposals of the form ˆs′ = ˆ s(k) + τ (k)A(k)ζ(k), ζ(k) ∼ N (0 , I ),

where A(k) is the Cholesky factor of the sample covariance matrix of the accepted samples, and 

τ (k) is a scaling parameter. In line with [58, 59], the scaling parameter is adapted during the sampling to push the acceptance ratio of the ( p, m ) samples towards 0 .23 which has been shown to be optimal for high-dimensional applications of random-walk Metropolis in which the target and proposal distributions are Gaussian [59, 60]. For both the independent and joint approaches we generate 10 6 samples and discard the first 2 × 10 5 as burn-in . Moreover, for both approaches we use a ‘warm-start’ for the MCMC. That is, we initialise the MCMC chains for ˆ p and ˆm at the projection of joint MAP estimate found using c1 = c2 = 0 of p and m onto to ˆV and ˆU , respectively, where ˆV = [ v1, v 2, . . . , v kp ] and ˆU = [ u1, u 2, . . . , u km ] (see Equations (3.7) and (3.8)). That is, we set (ˆ p(0) , ˆm(0) ) =( ˆV T (pMAP − p∗), ˆU T (mMAP − m∗)) (pMAP , m MAP ) := arg min 

> (p,m )∈Rn

12 ∥d − G (p, m )∥2Γ−1 

> e

+ 12 ∥p − p∗∥2Γ−1 

> p

+ 12 ∥m − m∗∥2Γ−1 

> m

.

On the other hand, we initialise the proposal covariance as a scaled version of the (Gauss-Newton) approximate posterior covariance matrix, i.e., 

A(0) = Lpost , Lpost LT 

> post

= Γ post := ( J(pMAP , m MAP )T Γ−1 

> e

J(pMAP , m MAP ) + Γ −10 )−1,

where Γ 0 is the block diagonal joint prior covariance matrix (i.e., with C = 0) and J(pMAP , m MAP )is the Jacobian of G evaluated at the joint MAP estimate. The MAP estimates and approximate marginal posterior uncertainty are shown in Figure 12. 

4.6. Results for Example 2. In Figure 13 we show the true log-permeability and log-recharge together with the conditional mean estimates obtained using independent and joint inference. As in Example 1, the independent approach recovers the parameters primarily in regions directly informed by the data, namely near the well paths for p and through the hydraulic-head observa-tions for the coupled system. By contrast, the proposed joint approach allows information to be transferred between the two parameters through the inferred prior correlation, leading to improved 

> 18

p

m    

> Figure 12. Laplace approximation used to ‘warm-start’ the MCMC sampling for Example 2. In the top row we show the true log-permeability ptrue (left), the MAP estimate (centre) and the approximate point-wise marginal posterior standard deviation (right).

p

m     

> Figure 13. True parameters and CM estimates for Example 2. In the top row we show the CM found using independent inference (left), the true log-permeability ptrue (centre), and the CM found using joint inference (right). On the bottom row we show the corresponsing plots for the log-recharge m.

reconstructions of both p and m, particularly for the recharge field m at the well locations (where 

p was measured directly). In Figure 14 we show the marginal posterior standard deviations obtained using independent and joint inference. In both cases the uncertainty is reduced most strongly near the data locations. However, under joint inference the reduction is not confined to those regions, the direct observations of p are also informative for (and reduce the uncertainty in) m. In this example we have E(p) = 0 .597 and E(m) = 0 .975, when using independent inference and E(p) = 0 .567 and E(m) = 0 .790 when using the joint approach. That is to say, in terms of reconstruction accuracy, the joint approach yields only a modest improvement for p but a much more substantial improvement for m. This is consistent with the model setup, since p is informed by both the hydraulic-head data and the direct point-wise measurements, whereas m is only observed indirectly through the PDE model and thus benefits more from the inferred correlation between the two parameters. In terms of uncertainty reduction, we obtain U(p) = 0 .311 and U(m) = 0 .494 using independent inversion, and U(p) = 0 .314 and U(m) = 0 .459 when using the joint approach. Thus, the joint approach produces a noticeable reduction in posterior uncertainty for m, while the uncertainty in 

p remains at a similar level. This is again consistent with the model setup, i.e., m benefits from the additional information transferred through the joint model. 

> 19

p

m               

> Figure 14. The reduction in uncertainty for Example 2. On the top we show the point-wise marginal posterior standard deviation for pfound using independent inference (left), the point-wise marginal posterior standard deviation for pfound using joint inference (centre), and the difference of the two, Dp(right). On the bottom row we show the corresponding plots for m.
> Figure 15. Results for estimating the correlation in Example 2. We show the joint marginal posterior π(c1, c 2|d)
> (left) as well as the marginal posteriors of π(c1|d)(centre) and π(c2|d)(right). In each of the plots the red line is used to indicate the (support) of the relevant prior distribution, while the black dot in the joint marginal plot (right) and black dotted lines in the marginal plots shows the true values.

Finally, Figure 15 shows the histograms of the inferred correlation parameters c1 and c2. The posterior clearly supports different correlations in the two subdomains x ≤ 1 and x > 1, with mass concentrated on positive values for c1 and negative values for c2, in agreement with the true values ( ctrue = (0 .8, −0.9)). The posterior distributions are broader than in Example 1, likely a result of the nonlinearity and smoothing behaviour of the forward problem. The trace plots and autocorrelation plot in Figure 16 indicate that both c1 and c2 are fairly well explored throughout the run, although c2 appears to exhibits stronger autocorrelation. The next section provides further discussion on the efficiency and effectiveness of the approaches. 

4.7. Computational costs. In this section we briefly consider the computational costs for Ex-ample 1 and Example 2. Specifically, the costs associated with characterising the posterior distri-butions using independent inference and joint inference. The key results are summarised in Table 1. For reference, all experiments were executed using a 10-core Apple M1 Pro with 32 GB RAM, with the forward model in Example 2 taking in the order of 1 .0 × 10 −3 seconds to evaluate. The additional cost of evaluating the posterior when using the proposed joint approach is negligible compared to the costs of evaluating the (forward models in the) likelihood, which is identical for the joint and independent approaches. That is to say, the cost of evaluating the posterior is (essentially) the same for the joint and independent approaches. Table 1 gives the key details on the results and the computational costs associated with carrying out the independent 

> 20

Figure 16. MCMC diagnostics for the correlation parameters c1 and c2 in Example 2. The left and centre panels show the trace plots for c1 and c2, respectively, while on the right we show the corresponding sample autocorrelation functions. 

Independent 

Joint 

Figure 17. MCMC diagnostics for the correlation parameters for the KL modes ˆp and ˆm in Example 2 using independent and joint inference. On the top row we show the trace plot of the first KL mode for the log-permeability 

ˆp1 using independent inversion (left), the trace plot of the first KL mode for the log-recharge ˆm1 using independent inversion and the sample autocorrelation for all KL modes using independent inversion. On the bottom we show the corresponding plots when using joint inversion and also show the sample autocorrelation for c1 and c2 in the autocorrelation plot (right). 

and joint inversion for both of the computational examples. As discussed in Sections 4.6 and 4.3, for the examples considered here, employing the proposed joint approach results in reductions in the posterior uncertainty and generally improves the CM estimates. To compare the computational costs of the proposed joint approach and independent inversion, in Table 1 we report the effective sample sizes (ESS) for the correlations ( c for Example 1 and c1 and c2 for Example 2) as well as 

21 Table 1                            

> Key metrics for Example 1 and 2 when solving independently and jointly using the proposed approach. Note for Numerical Example 1 both forward models are linear, and thus the associated independent inverse problems can be carried out directly (i.e., without sampling). Reported are the relative error for pand m(E(p)and E(m)), the relative uncertainty reduction for pand m(U(p)and U(m)), the total dimension of the parameters to be inferred (Dim.), the number of MCMC samples after discarding the burn-in (M), the effective sample size for the correlation coefficient(s) (ESS – C), and the median effective sample size for pand m(ESS – pand ESS – m, respectively). Note for Example 2 ESS – pand ESS – mare the median effective sample sizes of the KL mode coefficients ˆpand ˆm.

E(p) U(p) E(m) U(m) Dim. M ESS – C ESS – p ESS – m

Numerical Example 1 Ind. p 0.852 0.401 - - 1250 - - - -Ind. m - - 0.629 0.411 1250 - - - -Joint 0.513 0.313 0.589 0.291 2501 9 × 10 4 14818 29538 31965 Numerical Example 2 Ind. 0.597 0.311 0.975 0.494 150 8 × 10 5 - 904 804 Joint 0.567 0.314 0.790 0.459 152 8 × 10 5 153 106 455 493 the median ESS for ˆ p and the median ESS for ˆ m. The effective sample size is given by ESS = M

1 + 2 P∞ 

> w=1

r(w)(4.8) where M is the total number of retained samples (after burn-in is discarded) and r(w) is the estimated autocorrelation function for a lag w > 0. In practice the autocorrelation typically becomes noisy for large lags, and the summation in (4.8) is truncated – here we truncate the summation at the first lag for which the autocorrelation goes negative. Since the ESS is defined for each scalar parameter individually, we evaluate it for the correlation parameter(s) and for the coefficients of the first KL modes of both parameters. For Example 1, as both forward problems are linear, characterising the posterior using inde-pendent inference does not require MCMC and we thus do not report an ESS. On the other hand, using the proposed joint approach leads to a non-Gaussian posterior, which we characterise using MCMC. We compute a chain of length M = 9 .9 × 10 4 retained samples which has as an ESS for the correlation c of 14818. The estimated autocorrelation function for c is shown in Figure 9. For Example 2, we use MCMC for both the independent and joint approaches. For both approaches we compute a chain of length M = 8 × 10 5 retained samples. Using the independent approach, the MCMC chain for coefficient of the first KL mode of p (resp. m) , i.e., ˆ p1 (resp. ˆm1), has an ESS of 904 (resp. 804). Using the joint approach resulted in ESS of 455 and 493 for ˆ p1

and ˆ m1, respectively, while for c1 and c2 the ESS are 153 and 106, respectively. The trace plots for ˆ p1 and ˆ m1 and the estimated autocorrelation function for all of the KL-mode coefficients using both approaches are shown in Figure 17. The reduction in ESS for the joint approach indicates that, although explicitly accounting for uncertainty in the correlation can lead to more accurate uncertainty quantification, it incurs an additional computational cost, likely due to the increased complexity of the posterior distribution. 

5. Discussion and Conclusion. In the present work, we proposed a novel approach for carrying out joint inference in the Bayesian framework. The approach is applicable to cases where the marginal prior distribution for each of the parameters is Gaussian and fixed, and we wish to have a jointly normal prior. As such the construction of the joint covariance is the key task. We presented a possible covariance structure which is optimal in the canonical correlation sense, and can easily encode cross-correlation between the two parameters. Before carrying out joint inversion we considered several examples of generating random fields with given cross-correlations, including spatially varying cross-correlation, and random fields in different spatial dimensions. We considered joint inversion on a toy Monod model example to highlight the effects of miss-specifying the cross-correlation. This example also served to illustrate that the proposed approach 

> 22

is not, at least in its current form, immediately amenable to optimisation-based approaches such as computing the (joint) MAP estimate. In our case, that is, simultaneous estimation of the cor-relation, we advocate for the use of sampling-based approaches such as MCMC. Our approach can easily accommodate dimension reduction of the two (or more) parameters of interest via truncated KL expansions which can go some way to reducing the computational overheads associated with carrying out sampling-based approaches. Finally, we considered two higher-dimensional problems, one of which is governed by a PDE, where we estimated two random fields as well as the cross-correlation. In both examples, allowing for uncertainty in, and estimation of, the cross-correlation led to improved CM estimates and more concentrated marginal posteriors. We stress, however, that the aim is not simply to reduce posterior variance, but to accurately model and quantify the remaining uncertainty in the unknown parameters and their correlations. The overall conclusion of this work is a reaffirmation of a key component of the Bayesian ap-proach to inverse problems: anything unknown should be treated as a random variable. In particular, if the correlation between the two parameters is unknown, we should (at least attempt to) model and account for this uncertainty. 

Limitations. Of course, the approach we have presented also has some limitations. The two main limitations being related to computational overheads: 1. First, as illustrated in the Monod example (see Section 3.3) and further discussed in Appen-dix A, the approach proposed here is not immediately amenable to common optimisation-based approaches, i.e., the MAP estimate and Laplace approximation represent the posterior particularly poorly. As such we advocate for using sampling-based methods such as MCMC as a means to accurately characterise the posterior. However it is well known that such methods may in some cases be computationally infeasible. A possible means to reconcile the Laplace approximation approach may be to consider a suitable weighting on the correlation (hyper-) parameters, see for example [61] and the references therein. 2. Secondly, as alluded to in the Example 1 (see Section 4.1), even when the joint prior is Gaussian and the forward models are linear, using the proposed approach to jointly estimate the parameters and the correlation C results in a non-Gaussian posterior. Thus, sampling-based methods are required to characterise the posterior which can be computationally prohibitive. This represents a considerable computational cost increase, even when using the proposed MwG approach. Other than addressing the limitations noted above, future work directions could include using the proposed approach to generate two (or more) correlated level-set functions as a means or encoding structural similarity (rather than correlation) in the Bayesian framework.               

> REFERENCES
> [1] Luis A Gallardo and Max A Meju. Joint two-dimensional DC resistivity and seismic travel time inversion with cross-gradients constraints. Journal of Geophysical Research: Solid Earth , 109(B3), 2004. [2] Aria Abubakar, G Gao, Tarek M Habashy, and J Liu. Joint inversion approaches for geophysical electromagnetic and elastic full-waveform data. Inverse Problems , 28(5):055016, 2012. [3] Michael B Kowalsky, Stefan Finsterle, John Peterson, Susan Hubbard, Yoram Rubin, Ernest Majer, Andy Ward, and Glendon Gee. Estimation of field-scale soil hydraulic and dielectric parameters through joint inversion of GPR and hydrological data. Water Resources Research , 41(11), 2005. [4] Eldad Haber and Michal Holtzman Gazit. Model fusion and joint inversion. Surveys in Geophysics , 34(5):675– 695, 2013. [5] Xiaoqian Song, Maokun Li, Fan Yang, Shenheng Xu, and Aria Abubakar. Study on joint inversion algorithm of acoustic and electromagnetic data in biomedical imaging. IEEE Journal on Multiscale and Multiphysics Computational Techniques , 4:2–11, 2019. [6] Matthias J Ehrhardt, Kris Thielemans, Luis Pizarro, David Atkinson, S´ ebastien Ourselin, Brian F Hutton, and Simon R Arridge. Joint reconstruction of PET-MRI by exploiting structural similarity. Inverse Problems ,31(1):015001, 2014. [7] Julian Rasch, Eva-Maria Brinkmann, and Martin Burger. Joint reconstruction via coupled Bregman iterations
> 23

with applications to PET-MR imaging. Inverse Problems , 34(1):014001, 2017. [8] G Inglese and F Mariani. Corrosion detection in conducting boundaries. Inverse Problems , 20(4):1207, 2004. [9] Ruanui Nicholson and Matti Niskanen. Joint estimation of Robin coefficient and domain boundary for the Poisson problem. arXiv preprint arXiv:2108.09152 , 2021. [10] Hans Wackernagel. Multivariate geostatistics: an introduction with applications . Springer Science & Business Media, 2003. [11] Jean-Paul Chiles and Pierre Delfiner. Geostatistics: modeling spatial uncertainty . John Wiley & Sons, 2012. [12] Benjamin Crestel, Georg Stadler, and Omar Ghattas. A comparative study of regularizations for joint inverse problems. Inverse Problems , 35(2), 2018. [13] Matti Lassas and Samuli Siltanen. Can one use total variation prior for edge-preserving Bayesian inversion? 

Inverse Problems , 20(5):1537, 2004. [14] Beatriz Recinos, Daniel Goldberg, James R Maddison, and Joe Todd. A framework for time-dependent ice sheet uncertainty quantification, applied to three west antarctic ice streams. The Cryosphere , 17(10):4241–4266, 2023. [15] Noemi Petra, Hongyu Zhu, Georg Stadler, Thomas JR Hughes, and Omar Ghattas. An inexact gauss-newton method for inversion of basal sliding and rheology parameters in a nonlinear stokes ice sheet model. Journal of Glaciology , 58(211):889–903, 2012. [16] Niko H¨ anninen, Aki Pulkkinen, Aleksi Leino, and Tanja Tarvainen. Application of diffusion approximation in quantitative photoacoustic tomography in the presence of low-scattering regions. Journal of Quantitative Spectroscopy and Radiative Transfer , 250:107065, 2020. [17] Nicholas J Higham. Computing the nearest correlation matrix—a problem from finance. IMA journal of Numerical Analysis , 22(3):329–343, 2002. [18] Stephen Boyd and Lin Xiao. Least-squares covariance matrix adjustment. SIAM Journal on Matrix Analysis and Applications , 27(2):532–546, 2005. [19] Houduo Qi and Defeng Sun. A quadratically convergent Newton method for computing the nearest correlation matrix. SIAM journal on matrix analysis and applications , 28(2):360–385, 2006. [20] Houduo Qi, Zhonghang Xia, and Guangming Xing. An application of the nearest correlation matrix on web document classification. Journal of Industrial and Management Optimization , 3(4):701, 2007. [21] R¨ udiger Borsdorf, Nicholas J Higham, and Marcos Raydan. Computing a nearest correlation matrix with factor structure. SIAM Journal on Matrix Analysis and Applications , 31(5):2603–2622, 2010. [22] Nicholas J Higham and Natasa Strabic. Bounds for the distance to the nearest correlation matrix. SIAM Journal on Matrix Analysis and Applications , 37(3):1088–1102, 2016. [23] Andrew M Stuart. Inverse problems: A Bayesian perspective. Acta Numerica , 19:451–559, 2010. [24] Tan Bui-Thanh, Omar Ghattas, James Martin, and Georg Stadler. A computational framework for infinite-dimensional Bayesian inverse problems part I: The linearized case, with application to global seismic inver-sion. SIAM Journal on Scientific Computing , 35(6):A2494–A2523, 2013. [25] Noemi Petra, James Martin, Georg Stadler, and Omar Ghattas. A computational framework for infinite-dimensional Bayesian inverse problems, part II: Stochastic Newton MCMC with application to ice sheet flow inverse problems. SIAM Journal on Scientific Computing , 36(4):A1525–A1555, 2014. [26] Jari Kaipio and Erkki Somersalo. Statistical and Computational Inverse Problems , volume 160 of Applied Mathematical Sciences . Springer-Verlag New York, 2005. [27] Daniela Calvetti and Erkki Somersalo. An introduction to Bayesian scientific computing: ten lectures on subjective computing , volume 2. Springer Science & Business Media, 2007. [28] J.P. Kaipio and V. Kolehmainen. Approximate Marginalization Over Modeling Errors and Uncertainties in Inverse Problems , chapter 32, pages 644–672. Oxford University Press, 2013. [29] Daniela Calvetti, Matthew Dunlop, Erkki Somersalo, and Andrew M Stuart. Iterative updating of model error for Bayesian inversion. Inverse Problems , 34(2):025008, 2018. [30] Ruanui Nicholson, Noemi Petra, and Jari P Kaipio. Estimation of the Robin coefficient field in a Poisson problem with uncertain conductivity field. Inverse Problems , 34(11):115005, 2018. [31] Olalekan Babaniyi, Ruanui Nicholson, Umberto Villa, and Noemi Petra. Inferring the basal sliding coefficient field for the Stokes ice sheet model under rheological uncertainty. The Cryosphere , 15(4):1731–1750, 2021. [32] Lassi Roininen, Janne MJ Huttunen, and Sari Lasanen. Whittle-Mat´ ern priors for Bayesian statistical inversion with applications in electrical impedance tomography. Inverse Problems & Imaging , 8(2):561, 2014. [33] Jari P Kaipio, Ville Kolehmainen, Marko Vauhkonen, and Erkki Somersalo. Inverse problems with structural prior information. Inverse problems , 15(3):713, 1999. [34] Daniela Calvetti and Erkki Somersalo. Inverse problems: From regularization to Bayesian inference. Wiley Interdisciplinary Reviews: Computational Statistics , 10(3):e1427, 2018. [35] Finn Lindgren, H˚ avard Rue, and Johan Lindstr¨ om. An explicit link between Gaussian fields and Gaussian Markov random fields: The stochastic partial differential equation approach. Journal of the Royal Statistical Society: Series B (Statistical Methodology) , 73(4):423–498, 2011. [36] Neil K Chada, Lassi Roininen, and Jarkko Suuronen. Cauchy Markov random field priors for Bayesian inversion. 

arXiv preprint arXiv:2105.12488 , 2021. [37] Dean S Oliver. Gaussian cosimulation: Modelling of the cross-covariance. Mathematical Geology , 35(6):681–698, 

24 2003. [38] Roger A Horn and Charles R Johnson. Matrix analysis . Cambridge university press, 2012. [39] Theodore Wilbur Anderson. An Introduction to Multivariate Statistical Analysis , volume 3. John Wiley & Sons, Ltd, 2003. [40] Harold Hotelling. Relations between two sets of variates. Biometrika , 28(3-4):321–377, 1936. [41] Yair Daon and Georg Stadler. Mitigating the influence of the boundary on PDE-based covariance operators. 

Inverse Problems & Imaging , 12(5):1083, 2018. [42] Ustim Khristenko, Laura Scarabosio, Piotr Swierczynski, Elisabeth Ullmann, and Barbara Wohlmuth. Analysis of boundary effects on PDE-based sampling of Whittle–Mat´ ern random fields. SIAM/ASA Journal on Uncertainty Quantification , 7(3):948–974, 2019. [43] Carl Edward Rasmussen and Christopher K. I. Williams. Gaussian Processes for Machine Learning . MIT Press, 2005. [44] Gene H Golub and Charles F Van Loan. Matrix computations . JHU press, 2013. [45] Matthew M Dunlop, Marco A Iglesias, and Andrew M Stuart. Hierarchical bayesian level set inversion. Statistics and Computing , 27(6):1555–1584, 2017. [46] Christian P Robert, George Casella, and George Casella. Monte Carlo statistical methods , volume 2. Springer, 1999. [47] Sergios Agapiou, Johnathan M Bardsley, Omiros Papaspiliopoulos, and Andrew M Stuart. Analysis of the gibbs sampler for hierarchical inverse problems. SIAM/ASA Journal on Uncertainty Quantification , 2(1):511–544, 2014. [48] Lassi Roininen, Mark Girolami, Sari Lasanen, and Markku Markkanen. Hyperpriors for mat´ ern fields with applications in bayesian inversion. Inverse Problems & Imaging , 13(1), 2019. [49] Johnathan M Bardsley and Tiangang Cui. A metropolis-hastings-within-gibbs sampler for nonlinear hierarchical-bayesian inverse problems. In 2017 MATRIX Annals , pages 3–12. Springer, 2019. [50] Ian Jolliffe. Principal component analysis. Encyclopedia of statistics in behavioral science , 2005. [51] Antti Lipponen, Aku Seppanen, and Jari P Kaipio. Electrical impedance tomography imaging with reduced-order model based on proper orthogonal decomposition. Journal of Electronic Imaging , 22(2):023008, 2013. [52] Benjamin Peherstorfer, Karen Willcox, and Max Gunzburger. Survey of multifidelity methods in uncertainty propagation, inference, and optimization. Siam Review , 60(3):550–591, 2018. [53] Andrew Gelman, John B Carlin, Hal S Stern, and Donald B Rubin. Bayesian data analysis . Chapman and Hall/CRC, 1995. [54] Tapio Helin and Martin Burger. Maximum a posteriori probability estimates in infinite-dimensional Bayesian inverse problems. Inverse Problems , 31(8):085009, 2015. [55] Masoumeh Dashti, Kody JH Law, Andrew M Stuart, and Jochen Voss. MAP estimators and their consistency in Bayesian nonparametric inverse problems. Inverse Problems , 29(9):095017, 2013. [56] Arthur L Koch. The monod model and its alternatives. In Mathematical modeling in microbial ecology , pages 62–93. Springer, 1998. [57] Johnathan M Bardsley, Antti Solonen, Heikki Haario, and Marko Laine. Randomize-then-optimize: A method for sampling from posterior distributions in nonlinear inverse problems. SIAM Journal on Scientific Com-puting , 36(4):A1895–A1910, 2014. [58] Heikki Haario, Eero Saksman, and Johanna Tamminen. An adaptive Metropolis algorithm. Bernoulli , 7(2):223 – 242, 2001. [59] Christophe Andrieu and Johannes Thoms. A tutorial on adaptive mcmc. Statistics and computing , 18:343–373, 2008. [60] Andrew Gelman, Walter R Gilks, and Gareth O Roberts. Weak convergence and optimal scaling of random walk metropolis algorithms. The annals of applied probability , 7(1):110–120, 1997. [61] Daniela Calvetti, Erkki Somersalo, and A Strang. Hierarchical Bayesian models and sparsity: ℓ2-magic. Inverse Problems , 35(3):035003, 2019. 

Appendix A. Illustration of the pitfall of optimisation when estimating the correlation. 

Here we provide some further insight into why optimisation-based approaches to estimating both parameters p and m as well as the correlation C may be misleading. The issue is (possibly un-surprising, as the likelihood is independent of C) related to the prior. To illustrate this we set 

n1 = n2 = 1, i.e., both parameters are scalar p, m ∈ R, and assume π(p) = π(m) = N (0 , 1), with 

C = c ∈ R satisfying |c| < 1. If no other prior knowledge of the correlation between p and m is available, it is natural to postulate the uniform prior π(c) = U(−1, 1). In this case the joint prior can be written 

π(p, m, c ) ∝ 1(−1,1) (c) exp 

(

− 12

 p m 1 cc 1

−1  pm



− 12 ln(1 − c2)

)

,(A.1) 

25 where 1(−1,1) denotes the indicator function over the interval ( −1, 1). Consider now the log-prior; 

V (p, m, c ) := ln ( π(p, m, c )) ∝ − 12

 p m 1 cc 1

−1  pm



− 12 ln(1 − c2), |c| < 1,

which has gradient 

∇V = 1(c2 − 1) 



(p − cm )(m − cp )(c3 − c + ( p − cm )( m − cp )) /(c2 − 1) 

 , |c| < 1.(A.2) The only stationary point of V is at (0 , 0, 0) which is a saddle point, the Hessian being: 

∇2V (0 , 0, 0) = 



−1 0 00 −1 00 0 1

 .(A.3) For any value of |c| < 1, the level curves of V (and/or π) are ellipses, while fixing p and m we can always increase V (and thus π) by taking c → ± 1. Thus, if the data of the forward model(s) is not informative enough , an optimisation-based approach will return c = ±1 (or arbitrarily close to it). 

Appendix B. Invariance of the marginal posteriors to sign of correlation. 

To clarify the behaviour observed in Example 1 in the linear-Gaussian setting (see Section 4.3), we examine how the sign of the cross-correlation matrix C affects the joint prior precision matrix. In particular, we show that replacing C by −C leaves the diagonal blocks of the prior precision matrix unchanged, while reversing the sign of the off-diagonal blocks. This symmetry underlies the corresponding invariance of the marginal posterior covariance matrices in the separable linear setting with block-diagonal noise covariance. 

Lemma B.1. Let p and m be jointly Gaussian with joint covariance matrix 

S+ 

> n

∋ Γ = 

 Γp L−1 

> p

CL −Tm

L−1 

> m

CT L−Tp Γm



, C ∈ An1×n2 .(B.1) 

Then the diagonal blocks of the prior precision operator are invariant under the transformation 

C 7 → − C, while the off-diagonal blocks reverse sign. Proof. First, let the joint prior covariance matrix be written as Γ in (B.1). Then the joint prior precision matrix is given by Γ−1 =

 LTp (I − CC T )−1Lp −LTp C(I − CT C)−1Lm

−LTm(I − CT C)−1CT Lp LTm(I − CT C)−1Lm



.

Then it follows immediately that 

LTp (I − CC T )−1Lp = LTp (I − (−C)( −C)T )−1Lp,LTm(I − CT C)−1Lm = LTm(I − (−C)T (−C)) −1Lm,

i.e., the diagonal blocks of the prior precision operator are invariant to the sign of C, and 

−LTp C(I − CT C)−1Lm = LTp (−C)( I − (−C)T (−C)) −1Lm,

−LTm(I − CT C)−1CT Lp = LTm(I − (−C)T (−C)) −1(−C)T Lp,

i.e, the off-diagonal blocks of the prior precision operator change sign. 

> 26

Lemma B.1 shows that under the transformation C 7 → − C only the off-diagonal blocks of the prior precision matrix change sign. In the separable linear setting with block-diagonal noise covari-ance, it follows that the posterior precision matrix inherits the same symmetry. As an immediate consequence, we have the following corollary that shows that this implies invariance of the marginal posterior covariance matrices. 

Corollary B.2. Let s = ( p, m ) ∈ Rn be normally distributed with covariance matrix given by Γ

in (B.1), and let G ∈ Rq×n be the block diagonal matrix with diagonal blocks G1 ∈ Rq1×n1 and 

G2 ∈ Rq2×n2 . Furthermore, assume e ∈ Rq is Gaussian with block diagonal covariance matrix 

Γe ∈ Rq×q with diagonal blocks Γe1 ∈ Rq1×q1 and Γe2 ∈ Rq2×q2 . Then under the assumptions of Lemma B.1, in the linear setting, i.e, 

d = Gs + e, (B.2) 

then the marginal posterior covariance matrices of p and m are invariant under the sign change 

C 7 → − C.