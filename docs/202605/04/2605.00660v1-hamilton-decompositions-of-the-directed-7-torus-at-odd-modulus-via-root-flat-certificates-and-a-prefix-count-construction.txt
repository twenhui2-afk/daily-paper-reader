Title: Hamilton decompositions of the directed 7-torus at odd modulus via root-flat certificates and a prefix-count construction

URL Source: https://arxiv.org/pdf/2605.00660v1

Published Time: Mon, 04 May 2026 00:52:05 GMT

Number of Pages: 13

Markdown Content:
# Hamilton decompositions of the directed 7-torus at odd modulus via root-flat certificates and a prefix-count construction 

Sanghyun Park Yonsei University May 2026 

Abstract 

We prove that the directed seven-dimensional equal-side torus 

D7(m) = Cay ((Zm)7, {e0, e 1, . . . , e 6})

has a directed Hamilton decomposition for every odd integer m ≥ 3. The proof has two main contributions. First, we introduce the root-flat certificate : a named verification framework in which a Hamilton decomposition follows from three conditions on a single root flat, namely row Latinness, layer bijectivity, and primitive return maps. This abstraction was used informally in the earlier odd D5(m) construction; here it appears as Definition 1 and Theorem 2. Second, for every odd m ≥ 7, we give a uniform prefix-coordinate construction: one-layer prefix maps, a symbol-count criterion, and explicit count matrices produce all seven Hamilton factors without a finite search. The remaining moduli m = 3 , 5 are exactly the boundary where this prefix-count method cannot work; they are handled by finite root-flat certificates whose validity is checked in Lean 4. A Lean 4 formalization verifies the Cayley statement, with the symbolic branch and the finite boundary certificates checked in the same development. 

# 1 Introduction 

A Hamilton decomposition of a finite digraph is a partition of its arc set into directed Hamilton cycles. For the equal-side directed torus 

Dn(m) = Cay ((Zm)n, {e0, e 1, . . . , e n−1}),

each vertex has outdegree n. A Hamilton decomposition, if it exists, consists of exactly n

arc-disjoint directed Hamilton cycles whose union is the full Cayley arc set. Hamiltonicity of Cartesian products of directed cycles has a classical history. For two directed cycles, Trotter– Erdős gave an arithmetic criterion for the existence of a Hamilton cycle, while Curran–Witte analyzed Hamilton paths in two-factor products and proved that products of three or more nontrivial directed cycles are Hamiltonian [16, 5]. This places the equal-side directed torus within the standard Hamiltonicity theory of abelian Cayley digraphs; see also the surveys of Witte–Gallian and Curran–Gallian [17, 6]. The problem considered here is stronger. A Hamilton decomposition asks for a partition of the entire arc set into Hamilton cycles, not merely for one spanning directed cycle. Gen-eral Hamilton-decomposition questions are surveyed by Alspach–Bermond–Sotteau; product decompositions and related directed product phenomena appear in work of Foregger, Keating, Bogdanowicz, and Darijani–Miraftab–Witte Morris [1, 8, 9, 3, 4, 7]. The directed 3-torus is now known for all m ≥ 3, by return-map and odometer methods [13]; independent constructions and related explorations were also given by Knuth and by Aquino– Michaels [11, 2]. The odd D5(m) proof used a case-specific zero-set Latin table: one works on 1           

> arXiv:2605.00660v1 [math.CO] 1 May 2026 Uniform branch
> odd m≥7
> Section 3: prefix coordinates, one-layer factoriza-tion, count matrices
> Boundary branch
> m=3,5
> Section 4: finite root-flat certificates verified in Lean 4
> Root-flat certificate theorem
> Theorem 2: a valid certificate yields a Hamilton decomposition
> Main theorem
> Theorem 1: D7(m)has a Hamilton decomposition for every odd m≥3

Figure 1: Logical structure of the proof. The symbolic m ≥ 7 branch is proved in the paper; the boundary branch m = 3 , 5 is established by finite root-flat certificates checked in Lean 4. a root flat, uses the table for the only nonconstant layer, certifies the matching property by a finite exact-cover table, and then proves that a normalized return map is primitive [14]. That proof introduced the certificate philosophy, but it did not isolate the certificate theorem or a uniform large-modulus construction. The present paper contributes both. The first contribution is the root-flat certificate , intro-duced here as a named framework in Definition 1 and Theorem 2. It separates local certificate conditions from the lift to the Cayley graph: once row Latinness, layer bijectivity, and primi-tive return maps hold on one root flat, the full Cayley decomposition follows. This framework provides a common verification interface for the present construction and for the earlier odd 

D5(m) construction. The second contribution is the uniform prefix-count construction for dimension 7. For all odd m ≥ 7, we construct the seven Hamilton factors by a symbolic prefix-coordinate family and explicit 7 × 7 count matrices. This is a uniform construction for the large-modulus range, in contrast to the case-specific zero-set table used in the odd D5 proof. The two remaining odd moduli m = 3 and m = 5 are not merely inconvenient exceptions: Proposition 2 shows that the prefix-count method cannot possibly treat them. We therefore supply finite root-flat certificates for these two boundary cases, with the full tables stored as ancillary Lean source artifacts. 

Theorem 1 (Main theorem) . For every odd integer m ≥ 3, the directed torus 

D7(m) = Cay ((Zm)7, {e0, e 1, . . . , e 6})

admits a directed Hamilton decomposition. 

Figure 1 summarizes the division between the uniform symbolic branch and the two finite boundary branches. A separate diagonal Cartesian-power lift gives the multiplicative closure 

Da(m), D b(ma) =⇒ Dab (m),

for Hamilton decompositions of equal-side directed tori [12]. That mechanism is useful for com-posite dimensions but does not produce prime-dimensional base cases such as D5 or D7. The proof below is therefore a new prime-dimensional base construction rather than a consequence of the composite lift. 22 The root-flat certificate framework 

Let n ≥ 2 and m ≥ 2. Define the root flat 

An,m =

{

w = ( w0, . . . , w n−1) ∈ (Zm)n :

> n−1

∑

> i=0

wi = 0 

}

.

For 0 ≤ i ≤ n − 2, put 

qi = ei − en−1,

and put qn−1 = 0 . Thus qi ∈ An,m , and |An,m | = mn−1.On the original torus define the layer function 

σ(x) = x0 + x1 + · · · + xn−1 (mod m), Xt = {x : σ(x) = t}.

The map 

ιt : Xt −→ An,m , ιt(x) = x − te n−1

is a bijection. If x ∈ Xt, then using generator ei sends x to x + ei ∈ Xt+1 , and in root-flat coordinates this adds 

ιt+1 (x + ei) − ιt(x) = ei − en−1 = qi.

For i = n − 1, this is qn−1 = 0 .We now introduce the root-flat certificate. It is the verification interface used throughout this paper: the local data live on An,m , while Theorem 2 lifts those data to a Hamilton decomposition of the Cayley digraph. 

Definition 1 (Root-flat certificate) . An (n, m )-root-flat certificate consists of functions 

dt : An,m × Zn −→ Zn, t ∈ Zm,

where dt(w, κ ) is the direction used by color κ at layer t and root-flat point w. It is valid if the following hold. (RF1) Row Latinness. For every t ∈ Zm and w ∈ An,m , the map 

κ 7 −→ dt(w, κ )

is a permutation of Zn.(RF2) Layer bijectivity. For every t ∈ Zm and κ ∈ Zn, the map 

Pt,κ : An,m → An,m , Pt,κ (w) = w + qdt(w,κ )

is a bijection. (RF3) Primitive return. For every κ ∈ Zn, the return map 

Rκ = Pm−1,κ · · · P1,κ P0,κ 

is a single cycle on An,m .

Theorem 2 (Certificate implies Hamilton decomposition) . A valid (n, m )-root-flat certificate gives a directed Hamilton decomposition of Dn(m).

3Cayley generator root-flat step prefix label prefix displacement 

e0 q0 = e0 − e6 6 −p6

e1 q1 = e1 − e6 5 −p5

e2 q2 = e2 − e6 4 −p4

e3 q3 = e3 − e6 3 −p3

e4 q4 = e4 − e6 2 −p2

e5 q5 = e5 − e6 1 −p1

e6 q6 = 0 0 −p0 = 0 

Table 1: Fixed dictionary between Cayley directions, root-flat directions, and prefix labels. 

Proof. For color κ, define the arc set 

Eκ =

{( 

x, x + edσ(x)(ισ(x)x,κ )

)

: x ∈ (Zm)n}

.

For fixed x, condition (RF1) says that as κ varies, the directions used are all of Zn. Therefore the arc sets Eκ partition the outgoing arcs at every vertex, hence the full Cayley arc set. Condition (RF2) gives one incoming arc and one outgoing arc at every vertex for each fixed color. Thus each color class is a directed 1-factor. Since every arc increases σ by one, a color orbit returns to the same root flat exactly once every m steps. Starting from a different layer cyclically permutes the factors Pt,κ . By (RF2), each Pt,κ is a bijection, so the resulting cyclic products are conjugate to Rκ and have the same cycle structure. The cycle structure of a color class is therefore the cycle structure of the return map Rκ, with all lengths multiplied by m.By (RF3), Rκ is one cycle of length mn−1. Hence the color class is one directed Hamilton cycle of length mn. Since this holds for every color, the partition is a Hamilton decomposition. 

# 3 The uniform prefix-count construction for m ≥ 7

The symbolic branch of the D7 proof is most transparent in prefix coordinates. Let 

Q6 = ( Zm)6.

For 0 ≤ r ≤ 6, define 

pr = (1 , . . . , 1

︸ ︷︷ ︸

> r

, 0, . . . , 0) ∈ Q6.

We now identify these prefix coordinates with the root flat of Section 2. For w = ( w0, . . . , w 6) ∈

A7,m , define 

Φ( w)j =

> 6

∑

> h=7 −j

wh, 1 ≤ j ≤ 6.

Then Φ : A7,m → Q6 is a bijection. Its inverse is 

w6 = z1, wi = z6−i+1 − z6−i (1 ≤ i ≤ 5) , w0 = −z6,

where z = Φ( w). Moreover, for the root-flat directions qi = ei − e6 for 0 ≤ i ≤ 5 and q6 = 0 ,

Φ( w + qi) = Φ( w) − p6−i (0 ≤ i ≤ 5) , Φ( w + q6) = Φ( w) − p0.

Thus prefix label r corresponds to Cayley direction e6−r. In particular, once a layer map in prefix coordinates sends z to z − pr, the corresponding root-flat direction is 6 − r.We refer to Figure 2 and Table 1 throughout for the correspondence between Cayley di-rections and prefix labels. The symbols used in the construction below form a separate, state-dependent labeling. 4Root-flat side 

w = ( w0, . . . , w 6) ∈ A7,m , ∑ 

> i

wi = 0 .

qi = ei − e6 for 0 ≤ i ≤ 5, and q6 = 0 .

Prefix-coordinate side 

z = Φ( w) ∈ Q6, where 

zj = Φ( w)j = ∑6 

> h=7 −j

wh for 1 ≤ j ≤ 6.Inverse: w6 = z1, wi = z7−i − z6−i for 

1 ≤ i ≤ 5, and w0 = −z6.

Φ

Φ( w + qi) = Φ( w) − p6−i for 0 ≤ i ≤ 5, and Φ( w + q6) = Φ( w) − p0.Hence prefix label r corresponds to Cayley direction e6−r .

Figure 2: The root-flat/prefix-coordinate dictionary. The bijection Φ converts root-flat direc-tions qi into prefix displacements −p6−i. This correspondence connects the certificate frame-work of Section 2 to the prefix-count construction. 

symbol ξ prefix label used at a state z, where r = ρτ (z)0 0∆ ra, 2 ≤ a ≤ 6 a if r < a , and a − 1 if r ≥ a

Table 2: State-dependent symbol-to-prefix-label assignment for the prefix-count construction. No single Cayley direction corresponds to ∆ uniformly across states; instead, ∆ denotes the threshold-dependent slot, with label r = ρτ (z) at the state z.For a threshold τ ∈ Zm and z = ( z1, . . . , z 6) ∈ Q6, define 

ρτ (z) = 

{min {i : zi = τ }, if such i exists ,

6, otherwise .

The color variable is denoted by κ; the threshold variable is denoted by τ . This avoids using the same letter for two unrelated objects. We use the symbol set 

Σ6 = {0, ∆, 2, 3, 4, 5, 6}.

The symbol ∆ occupies the position that the integer label 1 would occupy among the numeric symbols; we name it separately because the corresponding map M ∆ 

> τ

depends on the threshold 

τ through ρτ , whereas the maps M aτ for numeric a have a uniform parametric form. For ξ ∈ Σ6, define maps M ξτ : Q6 → Q6 as follows: 

M 0 

> τ

(z) = z, M ∆ 

> τ

(z) = z − pρτ (z),

and, for the numeric symbols 2 ≤ a ≤ 6,

M aτ (z) = 

{z − pa, ρτ (z) < a, z − pa−1, ρτ (z) ≥ a. 

Thus the superscript is either the formal symbol ∆ or one of the numeric labels {0, 2, 3, 4, 5, 6};the arithmetic in the definition appears only in the numeric cases. 

Lemma 1 (Prefix-coordinate one-layer factorization) . For every m, every threshold τ ∈ Zm,and every z ∈ Q6, the seven maps 

M 0 

> τ

, M ∆ 

> τ

, M 2 

> τ

, M 3 

> τ

, M 4 

> τ

, M 5 

> τ

, M 6

> τ

use the seven labels 0, 1, . . . , 6 exactly once at z, and each map is a bijection of Q6.

5Proof. Let r = ρτ (z). The used labels are 

0, r, {a − 1 : 2 ≤ a ≤ r}, {a : r < a ≤ 6},

which are exactly 0, 1, . . . , 6.The map M ∆ 

> τ

has inverse 

y 7 −→ y + pρτ −1(y).

Indeed, if r = ρτ −1(y) and z = y + pr, then ρτ (z) = r. When r = 6 , this convention covers both possibilities: first occurrence at coordinate 6, and no occurrence at all. The two cases give the same map because both subtract p6 = (1 , . . . , 1) . Thus M ∆ 

> τ

(z) = y.For M aτ , 2 ≤ a ≤ 6, the inverse is also explicit. Given y, first set zi = yi + 1 for i < a . The predicate “some coordinate among the first a − 1 coordinates of z equals τ ” is equivalent to “some coordinate among the first a − 1 coordinates of y equals τ − 1”. If this predicate holds, set za = ya + 1 ; otherwise set za = ya. All later coordinates are unchanged. This recovers a unique preimage, so M aτ is bijective. We now prove the count criterion in a slightly stronger form. For 1 ≤ r ≤ 6, let Qr = ( Zm)r

and define 

ρ(r) 

> τ

(z) = 

> {

min {i : zi = τ }, if such i ∈ { 1, . . . , r } exists ,r, otherwise .

In dimension r we use the symbols 

{0, ∆, 2, . . . , r }

with the evident analogues of the maps above. The induction step also requires one auxiliary symbol T , denoting the full translation 

Tr(u) = u − (1 , . . . , 1) 

on Qr, because, when projecting from dimension r to dimension r − 1, the top numeric symbol becomes a full translation. 

Lemma 2 (One-cycle skew-product criterion) . Let X be a finite set, A : X → X a single cycle of length |X|, and let 

F : X × Zm → X × Zm, F (x, y ) = ( A(x), y + ϕ(x)) .

Put 

S = ∑

> x∈X

ϕ(x) ∈ Zm.

Then F is a single cycle on X × Zm if and only if S generates Zm, equivalently gcd( S, m ) = 1 .Proof. After one full turn of the base cycle, the second coordinate changes by S. Hence the orbit length over a base cycle is multiplied by the additive order of S in Zm. This order is 

m/ gcd( S, m ). Therefore the total orbit has size |X|m exactly when gcd( S, m ) = 1 .

Lemma 3 (Prefix-count criterion) . Let W = ( ξ0, . . . , ξ m−1) be a length-m word in Σ6. For thresholds τ0, . . . , τ m−1, the associated return map is 

RW = M ξm−1 

> τm−1

· · · M ξ1 

> τ1

M ξ0 

> τ0

.

Let 

N0, N ∆, N 2, N 3, N 4, N 5, N 6

be the symbol counts of W . If 

gcd( N0, m ) = 1 

6and 

gcd( Nk − N∆, m ) = 1 (2 ≤ k ≤ 6) ,

then the return map associated to W is a single m6-cycle on Q6, for any choice of thresholds in the layers. Proof. We prove the more general statement in dimension r ≤ 6, allowing the additional full-translation symbol T . Let the word length be m. Let NT be the number of occurrences of T .The criterion is: 

gcd( m − N0, m ) = 1 , gcd( Nk − N∆, m ) = 1 (2 ≤ k ≤ r).

Since m − N0 ≡ − N0 (mod m), this is the same first condition as in the statement. The symbol T is counted among the nonzero symbols in m − N0, but it does not appear in the later difference conditions. For r = 1 , all nonzero symbols, including the auxiliary symbol T when it occurs, act as the translation x 7 → x − 1 on Zm. Thus the return map is translation by minus the number of nonzero symbols, and it is a single cycle exactly when m − N0 is a unit modulo m.Assume the statement known in dimension r − 1. Write 

Qr = Qr−1 × Zm, z = ( u, y ).

Project each layer map to the u-coordinate. The symbols 0, ∆, 2, . . . , r − 1 project to the corresponding symbols in dimension r − 1. The top numeric symbol r projects to the full translation Tr−1 : u 7 → u − (1 , . . . , 1) . The full translation symbol T , if present, also projects to 

T . Therefore the projected word satisfies the induction hypotheses, because the first condition is unchanged and the difference conditions for 2 ≤ k ≤ r−1 are unchanged. Hence the projected return map on Qr−1 is one cycle. It remains to compute the drift in the last coordinate over one base cycle. We apply Lemma 2 with X = Qr−1, with A equal to the projected return map, and with ϕ(u) equal to the accumulated increment of the last coordinate during one full length-m word starting from base state u. The total drift S = ∑ 

> u

ϕ(u) is the sum of the following contributions. Consider a fixed occurrence of ∆ in the word. Immediately before this occurrence, the base state is obtained from the initial base state by a bijection of Qr−1. Hence, as the initial base point ranges over one base cycle, the state seen at this occurrence ranges over all of Qr−1 exactly once. The ∆-map decreases the last coordinate exactly when that state has no coordinate equal to the threshold used in that layer. This count is independent of the threshold: for any fixed 

τ ∈ Zm, the number of points of Qr−1 with no coordinate equal to τ is 

(m − 1) r−1.

Thus each occurrence of ∆ contributes −(m − 1) r−1 to the total drift. A fixed occurrence of the top numeric symbol r similarly sees every base state once. It decreases the last coordinate exactly when at least one coordinate of the base state equals the current threshold. Hence each occurrence of r contributes 

−(mr−1 − (m − 1) r−1).

The lower numeric symbols 2, . . . , r − 1 do not change the last coordinate. A full translation symbol T decreases the last coordinate for every u, hence contributes −mr−1 ≡ 0 (mod m) to the drift. Thus the total drift in the last coordinate is congruent modulo m to 

−N∆(m − 1) r−1 − Nr

> (

mr−1 − (m − 1) r−1) ≡ (Nr − N∆)( m − 1) r−1.

Because (m − 1) r−1 is a unit modulo m, Lemma 2 says that the final fiber is one cycle exactly when Nr − N∆ is a unit modulo m. This is the required last condition. The induction is complete. 7A count matrix has rows indexed by colors and columns ordered as 

(0 , ∆, 2, 3, 4, 5, 6) .

If all row sums and column sums are m, form the bipartite multigraph whose left vertices are the seven colors, whose right vertices are the seven symbols, and whose edge multiplicities are the entries of the matrix. This graph is m-regular and bipartite. By Hall’s theorem, equivalently by Kőnig’s line-coloring theorem [10], it has a perfect matching; removing that matching and repeating gives a decomposition into m perfect matchings. Each perfect matching is one layer. The matrices below were obtained by a constrained search over nonnegative integer entries: row and column sums are fixed at m, the bottom row is constrained to have N∆ = 0 so that one row absorbs the large N0 value, and the remaining entries are searched until all unit conditions of Lemma 3 hold simultaneously. The three parametric families reflect the residue class of m

modulo 6. The case m = 7 is written separately because the 6s + 1 family requires s ≥ 2 for nonnegativity. 

3.1 The matrix for m = 7 

1 2 0 0 0 0 41 2 0 0 0 3 11 1 0 0 3 2 01 1 0 3 2 0 01 1 3 2 0 0 01 0 2 1 1 1 11 0 2 1 1 1 1

> 

.

All row and column sums equal m = 7 . The values N0 are all 1, hence coprime to 7, and each difference Nk − N∆ is a unit modulo 7. Concretely, the first row corresponds to color 0: it uses the symbol 0 once, the symbol ∆ twice, no occurrences of symbols 2 through 5, and the symbol 6 four times; its row sum is 1 + 2 + 0 + 0 + 0 + 0 + 4 = 7 = m. A perfect-matching decomposition realizes these row counts as an actual seven-layer schedule μt, with each layer using every symbol exactly once. 

3.2 The matrix for m = 6 s + 1 , s ≥ 2

> 

1 s + 1 s − 1 s − 1 s − 1 s − 1 s + 3 1 s + 1 s − 1 s − 1 s − 1 s − 1 s + 3 1 s + 1 s − 1 s − 1 s − 1 s + 2 s

1 s s + 1 s + 1 s + 1 s − 1 s − 22 s − 1 s s s + 1 s + 1 s − 22 s − 1 s + 1 s + 1 s s s − 26s − 7 0 2 2 2 1 1

> 

.

All row and column sums equal m = 6 s + 1 . The values N0 are 1, 2, and m − 8, all coprime to 

m since m is odd and gcd( m − 8, m ) = gcd(8 , m ) = 1 . Each difference Nk − N∆ is in {± 1, ±2},hence is coprime to m.83.3 The matrix for m = 6 s + 3 , s ≥ 1

> 

1 s + 2 s s s s s

1 s + 2 s s s s s

1 s + 2 s s s s s

1 s − 1 s s s + 1 s + 1 s + 1 2 s − 1 s s s s + 1 s + 1 2 s − 1 s + 1 s + 1 s s s

6s − 5 0 2 2 2 1 1

> 

.

All row and column sums equal m = 6 s + 3 . The N0 values are 1, 2, and m − 8, all coprime to 

m since m is odd and gcd( m − 8, m ) = gcd(8 , m ) = 1 . Each difference Nk − N∆ is in {± 1, ±2},hence is coprime to m.

3.4 The matrix for m = 6 s + 5 , s ≥ 1

> 

1 s + 2 s s s s + 1 s + 1 1 s + 2 s s s s + 1 s + 1 1 s + 2 s s s s + 1 s + 1 1 s s + 1 s + 1 s + 1 s − 1 s + 2 2 s s + 1 s + 1 s + 1 s + 1 s − 12 s − 1 s + 1 s + 1 s + 1 s + 1 s

6s − 3 0 2 2 2 1 1

> 

.

All row and column sums equal m = 6 s + 5 . The N0 values are 1, 2, and m − 8, all coprime to 

m since m is odd and gcd( m − 8, m ) = gcd(8 , m ) = 1 . Each difference Nk − N∆ is in {± 1, ±2},hence is coprime to m.

Proposition 1 (Uniform prefix-count proof for m ≥ 7). If m ≥ 7 is odd, then D7(m) has a Hamilton decomposition. Proof. Use the appropriate count matrix above and decompose its bipartite multigraph into m

perfect matchings. Write the resulting layer schedule as 

μt : Z7 → Σ6, t ∈ Zm,

where μt(κ) is the prefix symbol assigned to color κ in layer t. Fix, for instance, the threshold 

τt = 0 in every layer; the count criterion allows arbitrary thresholds. For w ∈ A7,m , put z = Φ( w). The prefix map M μt(κ) 

> τt

sends z to z − pr for a uniquely determined label r ∈ { 0, . . . , 6}. Define the root-flat direction by 

dt(w, κ ) = 6 − r. 

By the prefix/root-flat dictionary above, 

Φ( w + qdt(w,κ )) = M μt(κ) 

> τt

(Φ( w)) .

Thus the layer maps constructed from the schedule are exactly of the root-flat form required in Definition 1. Lemma 1 proves row Latinness and layer bijectivity. Lemma 3 proves that every color return is a single m6-cycle. The certificate theorem then gives a Hamilton decomposition of 

D7(m).94 The boundary moduli m = 3 , 5

Proposition 2 (Boundary obstruction for the prefix-count family) . The prefix-count family of Section 3 cannot prove D7(3) or D7(5) . Here a prefix-count schedule means any schedule obtained from the seven-symbol layer factorization of Lemma 1. More precisely, when m < 7,no seven-color prefix-count schedule can make every return word primitive. Proof. Fix a color κ and consider its return word in the prefix-count construction. Project its return map to the first coordinate of Q6. The 0-symbol fixes the first coordinate, while every other symbol ∆, 2, 3, 4, 5, 6 decreases it by 1. Thus the first-coordinate factor of the return map is the translation 

y 7 −→ y − (m − N0).

If the full return map on Q6 is a single cycle, its projection to the first coordinate must be transitive on Zm. Hence this translation must be an m-cycle, which is equivalent to 

gcd( m − N0, m ) = 1 ,

or equivalently gcd( N0, m ) = 1 . In particular N0 ≥ 1 for every primitive color. Seven colors therefore require at least seven occurrences of the 0-symbol across all layers. But each prefix-count layer uses each symbol exactly once, so across all layers the total number of 0-symbols is exactly m. If m < 7, this is impossible. The obstruction is sharp: it identifies exactly which odd moduli lie outside the parameter range of the prefix-count construction. The point is not that D7(3) or D7(5) is intrinsically harder as a graph; rather, these moduli lie outside the parameter range of the prefix-count construction. This motivates the finite certificates below. 

Theorem 3 (Finite boundary certificates) . There are valid (7 , 3) - and (7 , 5) -root-flat certifi-cates. Computer-assisted proof. The certificates are not printed in this paper. The boundary return tables have 729 and 15625 root-flat states respectively, and the full color-layer direction data are too large for inline display in this paper. The m = 3 return table is small enough to print in principle, but we keep both boundary moduli in the same ancillary format so that the paper and the formalization use one certificate interface. They are stored as ancillary source files in the Lean formalization repository [15]. The certificates were produced in two stages. First, a finite exact-cover search over candidate root-flat zero-set selectors identifies selector layers compatible with the row-Latin and layer-bijection conditions. Second, a return-cycle search over the resulting candidate schedules locates one whose seven color returns are all single cycles. The proof relies only on the checker, not on the search: given the certificates, the three conditions of Definition 1 are verified directly. Concretely, for m = 3 the checker enumerates A7,3, verifies the row-Latin and layer-bijection conditions, and verifies that the seven color returns are single cycles of length 36 = 729 . For m =5, it performs the same check on A7,5, where the return cycles have length 56 = 15625 . The finite certificates are type-checked in the Lean 4 development. The source bundle records the D7 proof in D7Odd/Handoff/CanonicalFamily.lean , D7Odd/Torus.lean , and D7Odd/Cayley.lean ; the top-level Cayley theorem is 

D7Odd.D7_odd_cayley_unconditional .

The artifact uses Lean v4.30.0-rc2 and mathlib v4.30.0-rc2 . The recorded build command is 

lake build D5Odd D7Odd .

10 The top-level theorem has the interface 

theorem D7Odd.D7_odd_cayley_unconditional {m : Nat} [NeZero m] (hodd : Odd m) (hm3 : 3 <= m) : D7Odd.CayleyHamiltonDecompositionD7 m .

The boundary branch is assembled in the handoff files from the finite certificate objects for 

m = 3 and m = 5 , including the small root-flat certificates and the corresponding small Hamilton return checks. The relevant files are 

D7Odd/Handoff/SmallBranches.lean ,

D7Odd/Handoff/SmallRank3Certificates.lean ,

D7Odd/Handoff/SmallRank5Certificates.lean .For the boundary moduli, hand verification of seven 56 = 15625 -cycles is impractical; the Lean type checker takes the role of the printed finite certificate. 

Corollary 1. The directed tori D7(3) and D7(5) have Hamilton decompositions. Proof. Apply Theorem 2 to the certificates of Theorem 3. 

# 5 Proof of the main theorem 

Proof of Theorem 1. Let m ≥ 3 be odd. If m = 3 or m = 5 , the result follows from Corollary 1. If m ≥ 7, it follows from Proposition 1. These cases exhaust all odd m ≥ 3.

# 6 Discussion 

The odd D5(m) proof and the present D7(m) proof share the same root-flat philosophy: lo-cal arc partition and matching are separated from the primitive return-map problem. The difference is that for D7, the large-modulus branch is not another isolated zero-set table. It is the uniform prefix-count construction of Section 3. This gives a uniform proof for all odd 

m ≥ 7, while Proposition 2 precisely identifies why the two smaller odd moduli must be treated separately. The finite part also changes character from D5 to D7. In the D5 odd proof, the exceptional 

m = 3 return cycle has length 81 , small enough to print as a table. In D7, the boundary returns have lengths 729 and 15625 . This is the point at which paper-only finite verification becomes unnatural: the certificate is still finite and elementary, but the finite part is best reviewed together with the ancillary formal artifact. The Lean type checker plays the same role as the printed 81 -cycle table in the D5 paper, but at a scale where hand verification of seven 56 = 15625 -cycles is no longer reasonable. The next prime dimension D11 presents two distinct difficulties beyond what the present method handles. First, a direct zero-set certificate in D11 would involve exact-cover constraints over subsets of Z11 , far beyond the D5 and D7 tables. Second, the primitive return-map proof requires nested first-return sections at multiple scales or a finer rank invariant; a single first-return section of D5 type no longer suffices. Composite dimensions remain governed by the diagonal Cartesian-power lift [12]; prime dimensions require independent base constructions. The root-flat certificate framework gives a common language for such base constructions, even when the selector tables and return-map ranks become more elaborate. 11 Acknowledgments and AI disclosure 

This paper was prepared with the help of large language models for exploration and exposition, and a Lean 4 development for formal verification. Anthropic Claude Opus 4.7 contributed to exposition refinement and to exploratory discussion of the proof structure. OpenAI GPT-5.5 Pro played a substantial role in the exploration of count-matrix designs, exceptional-certificate construction, proof strategy, and exposition refinement. OpenAI GPT-5.5 Codex contributed to the Lean 4 formalization workflow. The mathematical content of the paper, including the prefix-count criterion and the validity of the finite certificates, is verified by the author; the Lean development provides independent machine verification of the finite certificates and of the final Cayley statement. 

# A Ancillary certificate format 

For each m ∈ { 3, 5}, the ancillary certificate contains a table 

dt : A7,m × Z7 → Z7, t ∈ Zm.

The checker constructs 

Pt,κ (w) = w + qdt(w,κ )

and verifies: (C1) For each (t, w ), the list (dt(w, 0) , . . . , d t(w, 6)) is a permutation of Z7.(C2) For each (t, κ ), the list Pt,κ (w) over all w ∈ A7,m is a permutation of A7,m .(C3) For each κ, the return permutation 

Pm−1,κ · · · P0,κ 

is a single cycle of length m6.The Lean files implementing this verification are part of the ancillary source bundle described in [15]. The repository records Lean 4.30 .0-rc2, mathlib 4.30 .0-rc2, and the build command 

lake build D5Odd D7Odd .

# References 

[1] B. Alspach, J.-C. Bermond, and D. Sotteau, Decomposition into cycles I: Hamilton de-compositions , in Cycles and Rays, NATO ASI Series C, vol. 301, Kluwer, 1990, pp. 9–18. [2] K. Aquino-Michaels, Completing Claude’s cycles: Multi-agent structured exploration on an open combinatorial problem , version v1.0.0, Zenodo, doi:10.5281/zenodo.19737970, 2026. [3] Z. R. Bogdanowicz, On decomposition of the Cartesian product of directed cycles into cycles of equal lengths , Discrete Applied Mathematics 229 (2017), 148–150. [4] Z. R. Bogdanowicz, Identifying Hamilton cycles in the Cartesian product of directed cycles ,AKCE International Journal of Graphs and Combinatorics 17 (2020), no. 1, 534–538. [5] S. J. Curran and D. Witte, Hamilton paths in Cartesian products of directed cycles , in Cycles in Graphs, Annals of Discrete Mathematics 27 (1985), 35–74. [6] S. J. Curran and J. A. Gallian, Hamiltonian cycles and paths in Cayley graphs and digraphs—a survey , Discrete Mathematics 156 (1996), 1–18. 12 [7] I. Darijani, B. Miraftab, and D. Witte Morris, Arc-disjoint Hamiltonian paths in Cartesian products of directed cycles , Ars Mathematica Contemporanea 25 (2025), no. 2, P2.10. [8] M. F. Foregger, Hamiltonian decompositions of products of cycles , Discrete Mathematics 24 (1978), 251–260. [9] K. Keating, Multiple-ply Hamiltonian graphs and digraphs , in Cycles in Graphs, Annals of Discrete Mathematics 27 (1985), 81–88. [10] D. Kőnig, Theorie der endlichen und unendlichen Graphen , Akademische Verlagsge-sellschaft, Leipzig, 1936. [11] D. E. Knuth, Claude’s cycles , preprint, March 2026, https://www-cs-faculty. stanford.edu/~knuth/papers/claude-cycles.pdf (accessed 29 April 2026). [12] Sanghyun Park, A diagonal Cartesian-power lift for Hamilton decompositions of directed tori , preprint, 2026. [13] Sanghyun Park, Hamilton decompositions of the directed 3-torus: a return-map and odome-ter view , arXiv:2603.24708, 2026. [14] Sanghyun Park, Hamilton decompositions of the directed 5-torus for odd modulus ,arXiv:2604.27140, 2026. [15] Sanghyun Park, Torus Hamilton Decomposition Program , Lean 4 formalization, release 0.0.2-d7 , commit 793467a8f92afcb19feaffc192ea6418123b117c , 2026, 

https://github.com/aria1th/Torus-Hamilton-Decomposition-Program/releases/ tag/0.0.2-d7 .[16] W. T. Trotter, Jr. and P. Erdős, When the Cartesian product of directed cycles is Hamil-tonian , Journal of Graph Theory 2 (1978), no. 2, 137–142. [17] D. Witte and J. A. Gallian, A survey: Hamiltonian cycles in Cayley graphs , Discrete Mathematics 51 (1984), no. 3, 293–304. 13