---
title: "Crab: A Semantics-Aware Checkpoint/Restore Runtime for Agent Sandboxes"
source: arxiv
title_zh: 用于Agent Sandboxes的Crab：Semantics-Aware Checkpoint/Restore Runtime
abstract_zh: "Autonomous agents act through sandboxed containers 与 microVMs whose state spans filesystems，processe。\n\n核心做法：Checkpoint 与 restore (C/R) 的 this state is needed 用于 fault tolerance，spot execution，RL rollout branc。\n\n主要结果：This gap hides massive sparsity：over 75% 的 agent turns produce no recovery-relevant state，so most ch。\n\n值得关注的点在于：On shell-intensive 与 code-repair workloads，Crab raises recovery correctness from 8% (chat-only) to 1。"
abstract_en: "Autonomous agents act through sandboxed containers and microVMs whose state spans filesystems, processes, and runtime artifacts. Checkpoint and restore (C/R) of this state is needed for fault tolerance, spot execution, RL rollout branching, and safe rollback-yet existing approaches fall into two extremes: application-level recovery preserves chat history but misses OS-side effects, while full per-turn checkpointing is correct but too expensive under dense co-location. The root cause is an agent-OS semantic gap: agent frameworks see tool calls but not their OS effects; the OS sees state changes but lacks turn-level context to judge recovery relevance. This gap hides massive sparsity: over 75% of agent turns produce no recovery-relevant state, so most checkpoints are unnecessary. Crab (Checkpoint-and-Restore for Agent SandBoxes) is a transparent host-side runtime that bridges this gap without modifying agents or C/R backends. An eBPF-based inspector classifies each turn's OS-visible effects to decide checkpoint granularity; a coordinator aligns checkpoints with turn boundaries and overlaps C/R with LLM wait time; and a host-scoped engine schedules checkpoint traffic across co-located sandboxes. On shell-intensive and code-repair workloads, Crab raises recovery correctness from 8% (chat-only) to 100%, cuts checkpoint traffic by up to 87%, and stays within 1.9% of fault-free execution time."
authors: "Tianyuan Wu, Chaokun Chang, Lunxi Cao, Wei Gao, Wei Wang"
date: 2026-04-30
journal: cs.OS
source_link: "https://arxiv.org/pdf/2604.28138v1"
pdf: "https://arxiv.org/pdf/2604.28138v1"
score: 7.1
tldr: 围绕《用于Agent Sandboxes的Crab：S...》；Checkpoint 与 restore (C/R) 的 thi...；This gap hides massive sparsity：over...
tldr_cn: 围绕《用于Agent Sandboxes的Crab：S...》；Checkpoint 与 restore (C/R) 的 thi...；This gap hides massive sparsity：over...
tldr_en: "Autonomous agents act through sandboxed containers and microVMs whose state spans filesystems, processes, and runtime artifacts. Checkpoint and restore (C/R) of this state is needed for fault tolerance, spot execution, RL rollout branching, and safe rollback-yet existing approaches fall into two extremes: application-level recovery preserves chat history but misses OS-side effects, while full per-turn checkpointing..."
selection_source: fresh_fetch
motivation: Autonomous agents act through sandboxed containers 与 microVMs whose state spans filesystems，processe。
method: 核心做法：Checkpoint 与 restore (C/R) 的 this state is needed 用于 fault tolerance，spot execution，RL rollout branc。
result: "主要结果：This gap hides massive sparsity：over 75% 的 agent turns produce no recovery-relevant state，so most ch。"
conclusion: "值得关注的点在于：On shell-intensive 与 code-repair workloads，Crab raises recovery correctness from 8% (chat-only) to 1。"
---

## 摘要
Autonomous agents act through sandboxed containers 与 microVMs whose state spans filesystems，processe。

核心做法：Checkpoint 与 restore (C/R) 的 this state is needed 用于 fault tolerance，spot execution，RL rollout branc。

主要结果：This gap hides massive sparsity：over 75% 的 agent turns produce no recovery-relevant state，so most ch。

值得关注的点在于：On shell-intensive 与 code-repair workloads，Crab raises recovery correctness from 8% (chat-only) to 1。

## Abstract
Autonomous agents act through sandboxed containers and microVMs whose state spans filesystems, processes, and runtime artifacts. Checkpoint and restore (C/R) of this state is needed for fault tolerance, spot execution, RL rollout branching, and safe rollback-yet existing approaches fall into two extremes: application-level recovery preserves chat history but misses OS-side effects, while full per-turn checkpointing is correct but too expensive under dense co-location. The root cause is an agent-OS semantic gap: agent frameworks see tool calls but not their OS effects; the OS sees state changes but lacks turn-level context to judge recovery relevance. This gap hides massive sparsity: over 75% of agent turns produce no recovery-relevant state, so most checkpoints are unnecessary. Crab (Checkpoint-and-Restore for Agent SandBoxes) is a transparent host-side runtime that bridges this gap without modifying agents or C/R backends. An eBPF-based inspector classifies each turn's OS-visible effects to decide checkpoint granularity; a coordinator aligns checkpoints with turn boundaries and overlaps C/R with LLM wait time; and a host-scoped engine schedules checkpoint traffic across co-located sandboxes. On shell-intensive and code-repair workloads, Crab raises recovery correctness from 8% (chat-only) to 100%, cuts checkpoint traffic by up to 87%, and stays within 1.9% of fault-free execution time.
