---
title: Hamilton decompositions of the directed 7-torus at odd modulus via root-flat certificates and a prefix-count construction
source: arxiv
title_zh: 通过root-flat certificates与prefix-count construction实现的directed 7-torus at odd modulus的Hamilton decompositions
abstract_zh: "We prove that directed seven-dimensional equal-side torus D_7 (m) = Cay ((Z/mZ)^7，{e_0，e_1，...，e_6})。\n\n核心做法：First，作者引入 root-flat certificate：named verification 框架 in which Hamilton decomposition 的 D_n (m) fol。\n\n主要结果：Second，用于 every odd m >= 7，we give uniform prefix-coordinate construction：one-layer prefix maps，symb。\n\n值得关注的点在于：Lean 4 formalization verifies Cayley statement，结合 symbolic branch 与 finite boundary certificates che。"
abstract_en: "We prove that the directed seven-dimensional equal-side torus D_7(m) = Cay((Z/mZ)^7, {e_0, e_1, ..., e_6}) admits a directed Hamilton decomposition for every odd integer m >= 3.   The proof has two main contributions. First, we introduce the root-flat certificate: a named verification framework in which a Hamilton decomposition of D_n(m) follows from three local conditions on a single root flat -- row Latinness, layer bijectivity, and primitive return maps. This abstraction was used informally in the earlier odd D_5(m) construction; here it appears as a definition and a theorem, providing a common verification interface for prime-dimensional base cases.   Second, for every odd m >= 7, we give a uniform prefix-coordinate construction: one-layer prefix maps, a symbol-count criterion, and explicit 7x7 count matrices produce all seven Hamilton factors without a finite search. The remaining moduli m = 3 and m = 5 are exactly the boundary where the prefix-count method provably cannot work; they are handled by finite root-flat certificates whose validity is checked in Lean 4. A Lean 4 formalization verifies the Cayley statement, with the symbolic branch and the finite boundary certificates checked in the same development."
authors: SangHyun Park
date: 2026-05-01
journal: math.CO
source_link: "https://arxiv.org/pdf/2605.00660v1"
pdf: "https://arxiv.org/pdf/2605.00660v1"
score: 8.4
tldr: "围绕《通过root-flat certificates...》；First，作者引入 root-flat certificate...；Second，用于 every odd m >= 7，we give u..."
tldr_cn: "围绕《通过root-flat certificates...》；First，作者引入 root-flat certificate...；Second，用于 every odd m >= 7，we give u..."
tldr_en: "We prove that the directed seven-dimensional equal-side torus D_7(m) = Cay((Z/mZ)^7, {e_0, e_1, ..., e_6}) admits a directed Hamilton decomposition for every odd integer m >= 3. The proof has two main contributions."
selection_source: fresh_fetch
motivation: "We prove that directed seven-dimensional equal-side torus D_7 (m) = Cay ((Z/mZ)^7，{e_0，e_1，...，e_6})。"
method: 核心做法：First，作者引入 root-flat certificate：named verification 框架 in which Hamilton decomposition 的 D_n (m) fol。
result: "主要结果：Second，用于 every odd m >= 7，we give uniform prefix-coordinate construction：one-layer prefix maps，symb。"
conclusion: 值得关注的点在于：Lean 4 formalization verifies Cayley statement，结合 symbolic branch 与 finite boundary certificates che。
---

## 摘要
We prove that directed seven-dimensional equal-side torus D_7 (m) = Cay ((Z/mZ)^7，{e_0，e_1，...，e_6})。

核心做法：First，作者引入 root-flat certificate：named verification 框架 in which Hamilton decomposition 的 D_n (m) fol。

主要结果：Second，用于 every odd m >= 7，we give uniform prefix-coordinate construction：one-layer prefix maps，symb。

值得关注的点在于：Lean 4 formalization verifies Cayley statement，结合 symbolic branch 与 finite boundary certificates che。

## Abstract
We prove that the directed seven-dimensional equal-side torus D_7(m) = Cay((Z/mZ)^7, {e_0, e_1, ..., e_6}) admits a directed Hamilton decomposition for every odd integer m >= 3.   The proof has two main contributions. First, we introduce the root-flat certificate: a named verification framework in which a Hamilton decomposition of D_n(m) follows from three local conditions on a single root flat -- row Latinness, layer bijectivity, and primitive return maps. This abstraction was used informally in the earlier odd D_5(m) construction; here it appears as a definition and a theorem, providing a common verification interface for prime-dimensional base cases.   Second, for every odd m >= 7, we give a uniform prefix-coordinate construction: one-layer prefix maps, a symbol-count criterion, and explicit 7x7 count matrices produce all seven Hamilton factors without a finite search. The remaining moduli m = 3 and m = 5 are exactly the boundary where the prefix-count method provably cannot work; they are handled by finite root-flat certificates whose validity is checked in Lean 4. A Lean 4 formalization verifies the Cayley statement, with the symbolic branch and the finite boundary certificates checked in the same development.
