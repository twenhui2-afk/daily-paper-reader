---
title: "When Labels Have Structure: Improving Image Classification with Hierarchy-Aware Cross-Entropy"
source: arxiv
title_zh: 结合Hierarchy-Aware Cross-Entropy的When Labels Have Structure：Improving Image Classification
abstract_zh: "Standard cross-entropy is default classification loss across virtually all 的 machine learning，yet it。\n\n核心做法：作者提出 Hierarchy-Aware Cross-Entropy (HACE)，drop-in replacement 用于 standard cross-entropy that incorpo。\n\n主要结果：In linear probing on frozen DINOv2-Large features，HACE 优于 all competing 方法s on all three datasets，结合。\n\n值得关注的点在于：We evaluate HACE on CIFAR-100，FGVC Aircraft，与 NABirds in two regimes：end-to-end training across six。"
abstract_en: "Standard cross-entropy is the default classification loss across virtually all of machine learning, yet it treats all misclassifications equally, ignoring the semantic distances that a class hierarchy encodes. We propose Hierarchy-Aware Cross-Entropy (HACE), a drop-in replacement for standard cross-entropy that incorporates a known class hierarchy directly into the loss. HACE combines two components: prediction aggregation, which propagates the model's probability mass upward through the class hierarchy to ensure that parent nodes accumulate the confidence of their children; and ancestral label smoothing, which distributes the ground-truth signal along the path from the true class to the root. We evaluate HACE on CIFAR-100, FGVC Aircraft, and NABirds in two regimes: end-to-end training across six architectures spanning convolutional and attention-based designs, and linear probing on frozen DINOv2-Large features. In end-to-end training, HACE improves accuracy over standard cross-entropy in 15 out of 18 architecture--dataset pairs, with a mean gain of 4.66\\%. In linear probing on frozen DINOv2-Large features, HACE outperforms all competing methods on all three datasets, with a mean improvement of 2.18\\% over the next best baseline."
authors: "April Chan, Davide D'Ascenzo, Sebastiano Cultrera di Montesano"
date: 2026-05-07
journal: cs.LG
source_link: "https://arxiv.org/pdf/2605.06274v1"
pdf: "https://arxiv.org/pdf/2605.06274v1"
score: 6.299999999999999
tldr: 围绕《结合Hierarchy-Aware Cross-...》；作者提出 Hierarchy-Aware Cross-Entro...；In linear probing on frozen DINOv2-L...
tldr_cn: 围绕《结合Hierarchy-Aware Cross-...》；作者提出 Hierarchy-Aware Cross-Entro...；In linear probing on frozen DINOv2-L...
tldr_en: "Standard cross-entropy is the default classification loss across virtually all of machine learning, yet it treats all misclassifications equally, ignoring the semantic distances that a class hierarchy encodes. We propose Hierarchy-Aware Cross-Entropy (HACE), a drop-in replacement for standard cross-entropy that incorporates a known class hierarchy directly into the loss."
selection_source: fresh_fetch
vocabulary: ["classification | 分类 | CET6"]
motivation: Standard cross-entropy is default classification loss across virtually all 的 machine learning，yet it。
method: 核心做法：作者提出 Hierarchy-Aware Cross-Entropy (HACE)，drop-in replacement 用于 standard cross-entropy that incorpo。
result: 主要结果：In linear probing on frozen DINOv2-Large features，HACE 优于 all competing 方法s on all three datasets，结合。
conclusion: 值得关注的点在于：We evaluate HACE on CIFAR-100，FGVC Aircraft，与 NABirds in two regimes：end-to-end training across six。
---

## 摘要
Standard cross-entropy is default classification loss across virtually all 的 machine learning，yet it。

核心做法：作者提出 Hierarchy-Aware Cross-Entropy (HACE)，drop-in replacement 用于 standard cross-entropy that incorpo。

主要结果：In linear probing on frozen DINOv2-Large features，HACE 优于 all competing 方法s on all three datasets，结合。

值得关注的点在于：We evaluate HACE on CIFAR-100，FGVC Aircraft，与 NABirds in two regimes：end-to-end training across six。

## Abstract
Standard cross-entropy is the default classification loss across virtually all of machine learning, yet it treats all misclassifications equally, ignoring the semantic distances that a class hierarchy encodes. We propose Hierarchy-Aware Cross-Entropy (HACE), a drop-in replacement for standard cross-entropy that incorporates a known class hierarchy directly into the loss. HACE combines two components: prediction aggregation, which propagates the model's probability mass upward through the class hierarchy to ensure that parent nodes accumulate the confidence of their children; and ancestral label smoothing, which distributes the ground-truth signal along the path from the true class to the root. We evaluate HACE on CIFAR-100, FGVC Aircraft, and NABirds in two regimes: end-to-end training across six architectures spanning convolutional and attention-based designs, and linear probing on frozen DINOv2-Large features. In end-to-end training, HACE improves accuracy over standard cross-entropy in 15 out of 18 architecture--dataset pairs, with a mean gain of 4.66\%. In linear probing on frozen DINOv2-Large features, HACE outperforms all competing methods on all three datasets, with a mean improvement of 2.18\% over the next best baseline.

## 英语词汇
- **classification**：分类（CET6）
