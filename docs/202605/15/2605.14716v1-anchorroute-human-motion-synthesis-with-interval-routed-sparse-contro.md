---
title: "AnchorRoute: Human Motion Synthesis with Interval-Routed Sparse Contro"
source: arxiv
title_zh: 结合Interval-Routed Sparse Contro的AnchorRoute：Human Motion Synthesis
abstract_zh: "Sparse anchors provide compact interface 用于 human motion authoring：users specify few root positions。\n\n核心做法：作者提出 AnchorRoute，sparse-anchor motion synthesis 框架 that uses anchors as shared scaffold 用于 both gene。\n\n主要结果：In benchmark evaluations，AnchorRoute 优于 prior sparse-control 方法s under sparse keyjoint protocol 与 co。\n\n值得关注的点在于：results show that learned anchor-conditioned generator 与 RouteSolver refinement are complementary：ge。"
abstract_en: "Sparse anchors provide a compact interface for human motion authoring: users specify a few root positions, planar trajectory samples, or body-point targets, while the system synthesizes the full-body motion that completes the under-specified intent. We present AnchorRoute, a sparse-anchor motion synthesis framework that uses anchors as a shared scaffold for both generation and refinement. Before generation, AnchorRoute converts sparse anchors into anchor-condition features and injects the resulting condition memory into a frozen Transition Masked Diffusion prior through AnchorKV and dual-context conditioning. This preserves the generation quality of the pretrained text-to-motion prior while learning sparse spatial control. After generation, the same anchors are evaluated as residuals: their timestamps define refinement intervals, and their residuals determine where correction should be concentrated. RouteSolver then refines the motion by projecting soft-token updates onto anchor-defined piecewise-affine interval bases. This couples generation-time anchor conditioning with residual-routed refinement under one anchor scaffold. AnchorRoute supports root-3D, planar-root, and body-point control within the same formulation. In benchmark evaluations, AnchorRoute outperforms prior sparse-control methods under the sparse keyjoint protocol and consistently improves anchor adherence across control families. The results show that the learned anchor-conditioned generator and RouteSolver refinement are complementary: the generator preserves text-motion quality, while RouteSolver provides a controllable path toward stronger anchor adherence."
authors: "Pengcheng Fang, Tengjiao Sun, Dongjie Fu, Xiaoyu Zhan, Yanwen Guo, Hansung Kim, Xiaohao Cai"
date: 2026-05-14
journal: cs.GR
source_link: "https://arxiv.org/pdf/2605.14716v1"
pdf: "https://arxiv.org/pdf/2605.14716v1"
score: 7.6499999999999995
tldr: 围绕《结合Interval-Routed Sparse...》；作者提出 AnchorRoute，sparse-anchor m...；In benchmark evaluations，AnchorRoute...
tldr_cn: 围绕《结合Interval-Routed Sparse...》；作者提出 AnchorRoute，sparse-anchor m...；In benchmark evaluations，AnchorRoute...
tldr_en: "Sparse anchors provide a compact interface for human motion authoring: users specify a few root positions, planar trajectory samples, or body-point targets, while the system synthesizes the full-body motion that completes the under-specified intent. We present AnchorRoute, a sparse-anchor motion synthesis framework that uses anchors as a shared scaffold for both generation and refinement."
selection_source: fresh_fetch
vocabulary: ["benchmark | 基准测试 | CET6/IELTS", "diffusion | 扩散 | 学术高频", "synthesis | 合成 | CET6"]
motivation: Sparse anchors provide compact interface 用于 human motion authoring：users specify few root positions。
method: 核心做法：作者提出 AnchorRoute，sparse-anchor motion synthesis 框架 that uses anchors as shared scaffold 用于 both gene。
result: 主要结果：In benchmark evaluations，AnchorRoute 优于 prior sparse-control 方法s under sparse keyjoint protocol 与 co。
conclusion: 值得关注的点在于：results show that learned anchor-conditioned generator 与 RouteSolver refinement are complementary：ge。
---

## 摘要
Sparse anchors provide compact interface 用于 human motion authoring：users specify few root positions。

核心做法：作者提出 AnchorRoute，sparse-anchor motion synthesis 框架 that uses anchors as shared scaffold 用于 both gene。

主要结果：In benchmark evaluations，AnchorRoute 优于 prior sparse-control 方法s under sparse keyjoint protocol 与 co。

值得关注的点在于：results show that learned anchor-conditioned generator 与 RouteSolver refinement are complementary：ge。

## Abstract
Sparse anchors provide a compact interface for human motion authoring: users specify a few root positions, planar trajectory samples, or body-point targets, while the system synthesizes the full-body motion that completes the under-specified intent. We present AnchorRoute, a sparse-anchor motion synthesis framework that uses anchors as a shared scaffold for both generation and refinement. Before generation, AnchorRoute converts sparse anchors into anchor-condition features and injects the resulting condition memory into a frozen Transition Masked Diffusion prior through AnchorKV and dual-context conditioning. This preserves the generation quality of the pretrained text-to-motion prior while learning sparse spatial control. After generation, the same anchors are evaluated as residuals: their timestamps define refinement intervals, and their residuals determine where correction should be concentrated. RouteSolver then refines the motion by projecting soft-token updates onto anchor-defined piecewise-affine interval bases. This couples generation-time anchor conditioning with residual-routed refinement under one anchor scaffold. AnchorRoute supports root-3D, planar-root, and body-point control within the same formulation. In benchmark evaluations, AnchorRoute outperforms prior sparse-control methods under the sparse keyjoint protocol and consistently improves anchor adherence across control families. The results show that the learned anchor-conditioned generator and RouteSolver refinement are complementary: the generator preserves text-motion quality, while RouteSolver provides a controllable path toward stronger anchor adherence.

## 英语词汇
- **benchmark**：基准测试（CET6/IELTS）
- **diffusion**：扩散（学术高频）
- **synthesis**：合成（CET6）
