---
title: "One Refiner to Unlock Them All: Inference-Time Reasoning Elicitation via Reinforcement Query Refinement"
source: arxiv
title_zh: 通过Reinforcement Query Refinement实现的One Refiner to Unlock Them All：Inference-Time Reasoning Elicitation
abstract_zh: "Large Language 模型s (LLMs) often fail to utilize their latent reasoning capabilities due to distribut。\n\n核心做法：In this paper，作者提出 ReQueR (\\textbf{Re}inforcement \\textbf{Que}ry \\textbf{R}efinement)，modular 框架 tha。\n\n主要结果：ReQueR yields consistent absolute gains 的 1.7\\%--7.2\\% across diverse 架构s 与 benchmarks，优于ing strong。\n\n值得关注的点在于：Code is available at https://github.com/newera-xiao/ReQueR。"
abstract_en: "Large Language Models (LLMs) often fail to utilize their latent reasoning capabilities due to a distributional mismatch between ambiguous human inquiries and the structured logic required for machine activation. Existing alignment methods either incur prohibitive $O(N)$ costs by fine-tuning each model individually or rely on static prompts that fail to resolve query-level structural complexity. In this paper, we propose ReQueR (\\textbf{Re}inforcement \\textbf{Que}ry \\textbf{R}efinement), a modular framework that treats reasoning elicitation as an inference-time alignment task. We train a specialized Refiner policy via Reinforcement Learning to rewrite raw queries into explicit logical decompositions, treating frozen LLMs as the environment. Rooted in the classical Zone of Proximal Development from educational psychology, we introduce the Adaptive Solver Hierarchy, a curriculum mechanism that stabilizes training by dynamically aligning environmental difficulty with the Refiner's evolving competence. ReQueR yields consistent absolute gains of 1.7\\%--7.2\\% across diverse architectures and benchmarks, outperforming strong baselines by 2.1\\% on average. Crucially, it provides a promising paradigm for one-to-many inference-time reasoning elicitation, enabling a single Refiner trained on a small set of models to effectively unlock reasoning in diverse unseen models. Code is available at https://github.com/newera-xiao/ReQueR."
authors: "Yixiao Zhou, Dongzhou Cheng, zhiliang wu, Yi Yang, Yu Cheng, Hehe Fan"
date: 2026-04-28
journal: cs.CL
source_link: "https://arxiv.org/pdf/2604.25444v1"
pdf: "https://arxiv.org/pdf/2604.25444v1"
score: 7.1499999999999995
tldr: 围绕《通过Reinforcement Query Re...》；In this paper，作者提出 ReQueR (\text...；ReQueR yields consistent absolute ga...
tldr_cn: 围绕《通过Reinforcement Query Re...》；In this paper，作者提出 ReQueR (\text...；ReQueR yields consistent absolute ga...
tldr_en: Large Language Models (LLMs) often fail to utilize their latent reasoning capabilities due to a distributional mismatch between ambiguous human inquiries and the structured logic required for machine activation. Existing alignment methods either incur prohibitive $O(N)$ costs by fine-tuning each model individually or rely on static prompts that fail to resolve query-level structural complexity.
selection_source: fresh_fetch
vocabulary: ["fine-tuning | 微调 | 学术高频", "alignment | 对齐 | CET6", "inference | 推理 | CET6"]
motivation: Large Language 模型s (LLMs) often fail to utilize their latent reasoning capabilities due to distribut。
method: "核心做法：In this paper，作者提出 ReQueR (\\textbf{Re}inforcement \\textbf{Que}ry \\textbf{R}efinement)，modular 框架 tha。"
result: "主要结果：ReQueR yields consistent absolute gains 的 1.7\\%--7.2\\% across diverse 架构s 与 benchmarks，优于ing strong。"
conclusion: "值得关注的点在于：Code is available at https://github.com/newera-xiao/ReQueR。"
---

## 摘要
Large Language 模型s (LLMs) often fail to utilize their latent reasoning capabilities due to distribut。

核心做法：In this paper，作者提出 ReQueR (\textbf{Re}inforcement \textbf{Que}ry \textbf{R}efinement)，modular 框架 tha。

主要结果：ReQueR yields consistent absolute gains 的 1.7\%--7.2\% across diverse 架构s 与 benchmarks，优于ing strong。

值得关注的点在于：Code is available at https://github.com/newera-xiao/ReQueR。

## Abstract
Large Language Models (LLMs) often fail to utilize their latent reasoning capabilities due to a distributional mismatch between ambiguous human inquiries and the structured logic required for machine activation. Existing alignment methods either incur prohibitive $O(N)$ costs by fine-tuning each model individually or rely on static prompts that fail to resolve query-level structural complexity. In this paper, we propose ReQueR (\textbf{Re}inforcement \textbf{Que}ry \textbf{R}efinement), a modular framework that treats reasoning elicitation as an inference-time alignment task. We train a specialized Refiner policy via Reinforcement Learning to rewrite raw queries into explicit logical decompositions, treating frozen LLMs as the environment. Rooted in the classical Zone of Proximal Development from educational psychology, we introduce the Adaptive Solver Hierarchy, a curriculum mechanism that stabilizes training by dynamically aligning environmental difficulty with the Refiner's evolving competence. ReQueR yields consistent absolute gains of 1.7\%--7.2\% across diverse architectures and benchmarks, outperforming strong baselines by 2.1\% on average. Crucially, it provides a promising paradigm for one-to-many inference-time reasoning elicitation, enabling a single Refiner trained on a small set of models to effectively unlock reasoning in diverse unseen models. Code is available at https://github.com/newera-xiao/ReQueR.

## 英语词汇
- **fine-tuning**：微调（学术高频）
- **alignment**：对齐（CET6）
- **inference**：推理（CET6）
