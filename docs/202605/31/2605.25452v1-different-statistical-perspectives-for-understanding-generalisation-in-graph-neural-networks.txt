Title: Different Statistical Perspectives for Understanding Generalisation in Graph Neural Networks

URL Source: https://arxiv.org/pdf/2605.25452v1

Published Time: Tue, 26 May 2026 01:49:26 GMT

Number of Pages: 15

Markdown Content:
# Different Statistical Perspectives for Understanding Generalisation in Graph Neural Networks 

Nil Ayday 1, Mahalakshmi Sabanayagam 2,Debarghya Ghoshdastidar 1* 

1Technical University of Munich, School of Computation, Information and Technology, Boltzmannstr. 3, Garching, 85748, Germany. 

2Australian Institute for Machine Learning, Adelaide University, Adelaide, SA 5000, Australia. *Corresponding author(s). E-mail(s): ghoshdas@cit.tum.de; Contributing authors: nil.ayday@tum.de; mahalakshmi.sabanayagam@adelaide.edu.au; 

Abstract 

Graph Neural Networks (GNN) are currently the most popular approach for learning and prediction on graph-structured data and are deployed in various fields, from social network analysis to drug discovery. However, there is limited mathematical understanding of the performance of GNNs. We discuss the various perspectives used to study statistical generalisation in GNNs. We identify three broad frameworks. The first approach, rooted in learning theory, relies on uni-form convergence bounds and the complexity of the hypothesis class of specific GNN architectures. This approach also builds on the expressivity of GNNs, typ-ically studied through the lens of graph isomorphism tests. The second principle is to simplify the neural architecture by analysing GNNs under the asymptotics of infinitely many parameters or infinite graph size. This approach approximates GNNs using Gaussian processes, neural tangent kernels or graphon neural net-work operators, which allow studying the generalisation or stability of trained GNNs. The third framework studies GNNs under random graph models, often the contextual stochastic block model, and derives non-asymptotic error rates using tools from high-dimensional statistics. We highlight some key theoretical results and discuss a few limitations and open research questions for each perspective. 

Keywords: Generalisation error, Graph isomorphism, Radom graph, Graphon, Contextual stochastic block model, Neural Gaussian process, Neural tangent kernel 

MSC Classification: 68Q32 , 68T07 , 62H12 , 05C80 

1

> arXiv:2605.25452v1 [stat.ME] 25 May 2026

# 1 Introduction 

The surge of interest in Graph Neural Networks (GNNs) stems from a fundamental shift in how we approach computationally intractable graph problems. For decades, search or learning problems on graphs, such as community detection , the travelling salesperson problem , protein folding , etc., were primarily viewed as combinatorial opti-misation problems. The trend in recent years has been to reframe these challenges as learning problems, typically solved using neural networks. For example, Chen et al. [1] “learn” the community detection problem by training a GNN on random graphs with communities generated from a stochastic block model [2]. Notably, GNNs allow more flexibility than standard neural network architectures, such as feedforward, con-volutional, and recurrent networks. While standard architectures operate on fixed grid-structured data, such as 2D or 3D-arrays for images, GNNs are capable of oper-ating on arbitrary non-Euclidean topologies, making them suitable for applications like molecular structure prediction. GNNs also allow for the design of predictors that are permutation-equivariant or permutation-invariant. Hence, GNNs are synonymous with the field of geometric deep learning [3] and are deployed in various fields [4, 5]. The theoretical analysis of GNNs is in a nascent stage, and diverse perspectives have evolved regarding the understanding of the performance of various GNN models. The different theoretical views are natural since one can study GNNs through the lens of graph theory, random graphs, non-Euclidean geometry, statistical learning theory, deep learning theory, etc. We focus on the statistical analysis of GNNs, which asks: 

What is the test error (or generalisation error) of a GNN trained with samples from a statistical law? Three broad frameworks are used to study generalisation in GNNs: 1. Statistical learning theoretic framework: Statistical learning theory provides the classical tools to study generalisation for any machine learning model and has been used in early statistical guarantees for GNNs. Such guarantees are useful if one can show that the GNN models can learn complex functions. Hence, this framework is related to the research on the expressive power of GNNs, which we also discuss. 2. Extension of deep learning theory to GNNs: Classical learning theory often fails to explain the performance of trained neural networks (NN), for example, benign overfitting or neural scaling laws . This has led to new tools for the asymptotic analysis of NNs, deriving nonparametric or operator limits of NNs. Recent work has extended such analysis to study GNNs, providing deeper insights. 3. Statistical network model viewpoint: The above frameworks are not adapted to the statistical nature of the graph data. A recent direction studies GNNs under specific random graph models, such as the contextual stochastic block model, using tools from high-dimensional statistics. Despite focusing on specific graph models, these results provide precise generalisation error rates and explain empirical observations. Before delving into the details of the above statistical frameworks, we formally describe GNNs, associated learning problems, and the notion of generalisation. 

Graph neural network (GNN) 

The GNN literature typically considers graphs with additional node features. Hence, a graph on n nodes is usually represented by G = ( A, X ), where X ∈ Rn×d0 with 2the i-th row representing the d0-dimensional node features of the i-th node, and A ∈

[0 , 1] n×n is the (weighted) adjacency matrix. Let D denote the diagonal matrix with 

Dii = Pnj=1 Aij . We discuss only static, undirected graphs. A L-layer GNN iteratively computes the node representations. The ℓ-th layer computes H(ℓ) ∈ Rn×dℓ , where row 

H(ℓ) 

> i

∈ Rdℓ is a representation of node-i. Starting with H(0) = X, a GNN computes 

H(ℓ) 

> i

= f



H(ℓ−1)  

> i

,

n

H(ℓ−1)  

> j

, A ij ∀j : Aij > 0

o 

, for ℓ = 1 , . . . , L, (1) where f (·) could be a general non-linear function that aggregates the representations in the local neighbourhood of node-i, obtained from the previous layer H(ℓ−1) , along with the edge information A. Since the aggregation f (·) essentially acts like “message-passing”, GNN architectures with the update-rule (1) are broadly known as message-passing GNNs (MP-GNN). One of the most popular examples of MP-GNN is a graph convolutional network (GCN) [6] (see Figure 1) with the update rule simplifying to 

H(ℓ+1) = σ



SH (ℓ)Wℓ



, for ℓ = 0 , 1, . . . , L − 1, (2) where Wℓ ∈ Rdℓ×dℓ+1 is trainable, σ(·) is an entry-wise non-linear map, like ReLU( z) = max {z, 0}, and the convolution operator S ∈ Rn×n is a function of the adjacency A,such as symmetric normalised adjacency Ssym = ( D+I)−1/2(A+I)( D+I)−1/2 or row-normalised adjacency Srow = ( D+I)−1(A+I). The addition of n×n identity matrix I

ensures that each node retains its information. The updates (1)–(2) provide nonlinear node representations H(L), which can be directly used for prediction problems on the nodes. To obtain a single representation of the graph G, a permutation-invariant read-out function is used: HG = READOUT 

n 

H(L) 

> i

: i = 1 , . . . , n 

o 

, such as the mean, the sum, or the maximum of H(L) 

> i

across all nodes. Several alternatives exist. Graph attention network (GAT) [7] replaces S in (2) with a learnable convolution in each layer 

S(ℓ) 

> ij

=exp 



˜σ

h 

H(ℓ) 

> i

Wℓ H(ℓ) 

> j

Wℓ

i

vℓ

 P  

> k∈{ i}∪{ k′:Aik ′>0}

exp 



˜σ

h 

H(ℓ) 

> i

Wℓ H(ℓ) 

> k

Wℓ

i

vℓ

 , (3) parameterized by trainable vℓ ∈ R2dℓ+1 , and non-linear activation ˜ σ(·). Thus, the message-passing in (3) is a softmax “attention” within a neighbourhood, providing improved performance in practice. There exist other architectures beyond MP-GNNs, such as spectral GNNs, where the update rule is in terms of the graph spectra, whereas graph transformers learn global interactions. We refer to [3–5, 8] for details. 

Statistical learning problems on graphs 

A variety of prediction problems on graphs is relevant in practice. We consider only the most prominent learning problems on graphs, which have also been theoretically studied in the literature. Statistical learning on graphs can broadly be distinguished into two settings. The first setting comprises problems where one has information about multiple graphs (training samples), and a predictor is learnt to make predictions 3𝐻 0 = 𝑋                       

> Message passing
> linear
> map
> nonlinear
> activation
> 𝑊 0𝜎 ∙
> Layer 1
> 𝐻 1=
> 𝜎 𝑆 𝐻 0𝑊 0
> Message passing
> 𝑊 𝐿 −1𝜎 ∙
> Layer 𝑳
> 𝐻 𝐿 =
> 𝜎 𝑆 𝐻 𝐿 −1𝑊 𝐿 −1READOUT
> for graph -level
> representation,
> example Mean -pooling
> 𝐻 𝐺 =1
> 𝑉 𝐺 ෍
> 𝑣 ∈𝑉 𝐺
> 𝐻 𝑣 𝐿 Fig. 1 L-layer GNN architecture, with expressions specific to graph convolutions (GCN) [6]. READ-OUT layer, used for graph-level tasks, combine node representations to a graph representation.

on new graphs; we call this a graph-level task , which includes examples such as the classification of different proteins or molecular structures. The second setting involves a single large graph, and the goal is to make predictions about the nodes or edges of the graph . Examples include predicting the political affiliations of individuals based on their interactions in a social network (node classification) or recommending specific products to buyers by estimating the probability of purchase (edge/link prediction). A prototypical graph-level supervised learning problem is posed as follows. One has access to a dataset {(Gj , y j ) : j = 1 , . . . , m }, where each Gj is a graph (for example, a protein structure) and yj is a class label or a real-valued measurement associated with Gj . The statistical framework assumes that {(Gj , y j )}mj=1 are independent and identically distributed (i.i.d.) samples from an unknown joint distribution D. Given a specified loss function, e.g., squared loss, the goal is to learn a predictor G 7 → f (G) that minimises the risk or generalisation error R(f ) = E(G,y )∼D 

loss( y, f (G)) . A generic approach to learning an optimal f is to assume a hypothesis class of predictors and minimise the empirical risk: bR(f ) = 1

> m

Pmj=1 loss( yj , f (Gj )) [see 9]. In the context of GNN, the following parameterisation is assumed: a non-linear GNN learns the graph representation G 7 → HG (Figure 1), and a simple linear or logistic model is fitted to the representation, such as f (G) = HGβ. The predictor G 7 → fθ(G) is parameterized by θ = ( β, {Wℓ, v ℓ}ℓ=0 ,...,L −1), comprising the weights in (2)–(3) and a linear probe β.The second setting pertains to a single large graph with n nodes. We discuss only node-level prediction , where one assumes that every node-i, i = 1 , . . . , n , has an associated label or value yi. Labels are only observed for m ≪ n nodes, and the goal is to predict the unseen labels. This setting is known as transductive inference and is also studied in statistics as random graph models with partially labelled communities [10]. The GNN approach parameterizes the predictor node-i 7 → f (i) as an L-layer GNN that models the node representation node-i 7 → H(L) 

> i

, and a simple linear or logistic probe is learnt on H(L) 

> i

, such as f (i) = H(L) 

> i

β. All parameters θ = ( β, {Wℓ, v ℓ}ℓ) are learnt by minimising a suitable training loss defined on the nodes with observed labels. 

# 2 Learning theoretic bounds on generalisation error 

We introduce some concepts of learning theory and refer to [9] for details. Consider the graph-level prediction setting. A hypothesis class F of predictors is assumed; here, it refers to F = {fθ : θ = ( β, {Wℓ, v ℓ}ℓ)}, that is, predictors corresponding to a specific GNN architecture (fixed depth L, layer widths), but with all possible train-able parameters. Given a dataset {(Gj , y j )}mj=1 ∼i.i.d D, it is assumed that we can 4obtain an empirical risk minimiser bf = arg min f ∈F bR(f ). The goal is to derive an upper bound on the generalisation error R( bf ) of the model bf , which is gener-ally obtained in terms of the complexity of the class F, typically characterised by its Vapnik-Chervonenkis (VC) dimension, Rademacher complexity, etc. [9]. For binary classification of graphs, the richness of F can be expressed in terms of its empirical Rademacher complexity bRm(F) = E

"

sup  

> f∈F

1

m

> m

X

> i=1

εif (Gi)

#

; the ability of F to fit random labels ε1, . . . , ε m ∼i.i.d Rademacher( {± 1}) on the training data G1, . . . , G m.Theorem 1 provides a generalisation error bound for GNNs for graph-level prediction.                                     

> Theorem 1(Graph-level generalisation error bound for GNNs [11]) Let Fdenote the hypothesis class for a specific GNN architecture, and let bf∈ F denote the empirical risk min-imiser, that is, the GNN that minimises the training error bR(f)given mtraining samples
> (Gj, y j)∼i.i.d. D. With probability at least 1−δ, with respect to the training samples,
> R(bf)≤bR(bf) + ObRm(F) +
> rlog(1 /δ )
> m
> !
> ≤inf
> f∈F R(f) + ObRm(F) +
> rlog(1 /δ )
> m
> !
> .

Theorem 1 decomposes the error bound into two factors: (i) the inductive bias 

inf f ∈F R(f ), the minimum error incurred using the specific GNN architecture, and (ii) the estimation error of learning the optimal predictor using finite samples, which is bounded by the Rademacher complexity. For a L-layer GNN and d-dimensional embedding in each layer, the empirical Rademacher complexity bRm(F) = O

 d·L·b√m



,where b is the maximum node degree of any graph G1, . . . , G m [11]. Thus, m ≫ dL 

samples are needed to achieve a low estimation error with a specific GNN. Alternative error bounds using VC-dimension, margin bounds, and PAC-Bayesian analysis are also known [see 8]. Theorem 1 illustrates the bias-variance trade-off: a complex GNN architecture reduces the inductive bias, but the complexity of F and the estimation error worsen, indicating over-fitting on the training data. To characterise the inductive bias and the complexity of F, we need to turn towards the graph isomorphism problem. 

The role of graph isomorphism in graph-level GNN generalisation 

The inductive bias in Theorem 1 characterises the expressive power of the GNN archi-tecture. For graph-level prediction, it is equivalent to asking: Which graphs can be distinguished by a GNN under the optimal choice of parameters? This relates to the graph isomorphism problem: given two graphs G1, G 2, each with n nodes, test whether 

G1 and G2 are isomorphic or not. Graph theory provides results on graph isomor-phism, which have gained recent attention due to their relation to the expressive power of GNNs. Notably, the Weisfeiler-Leman (WL) graph isomorphism test [12] is closely related to GNNs. The idea behind the WL test is to generate a multi-set representation of each graph by iteratively aggregating over the neighbourhoods and subsequently testing graph isomorphism by comparing the representations. This iterative algorithm, called 1-WL or colour refinement, proceeds as follows. Let c(t) 

> i

denote the colour of 51 1                                                                                           

> 11
> 1
> 11
> 11
> 1
> 𝐶 5
> 𝑃 5
> Iterations of 1 -WL algorithm
> bb
> bb
> b
> rr
> rr
> r
> Labelling
> stable from
> 2nd iteration
> aa
> bb
> b
> pp
> qq
> r
> Labelling
> stabilizes
> after 2nd
> iteration
> 99
> 99
> 9
> 33
> 33
> 3
> 55
> 88
> 9
> 22
> 33
> 3
> Node representations from GNN layers
> 𝐻 0=
> 1
> 1
> 1
> 1
> 1
> 𝐻 1=
> 3
> 3
> 3
> 3
> 3
> 𝐻 2=
> 5
> 8
> 9
> 8
> 5
> 𝐻 0=
> 1
> 1
> 1
> 1
> 1
> 𝐻 1=
> 3
> 3
> 3
> 3
> 3
> 𝐻 2=
> 9
> 9
> 9
> 9
> 9
> Specifications of GNN
> 𝐻 ℓ+1=𝑅𝑒𝐿𝑈 𝑆 𝐻 ℓ𝑊 ℓ
> with
> convolution 𝑆 =𝐼 +𝐴
> parameters 𝑊 0=𝑊 1=1
> Hash functions for 1 -WL
> =𝐻𝐴𝑆𝐻 ∙,∙
> =𝐻𝐴𝑆𝐻 ∙,∙,∙
> =𝐻𝐴𝑆𝐻 ∙,∙
> =𝐻𝐴𝑆𝐻 ∙,∙,∙
> =𝐻𝐴𝑆𝐻 ∙,∙,∙
> a
> b
> p
> q
> r
> 11
> 111
> ab
> abb
> bbb
> Graphs
> 11111
> Representation
> bbbbb
> Representation
> rrrrr
> Representation
> 11111
> Representation
> baabb
> Representation
> qppqr
> Representation

Fig. 2 Illustration of how the GNN layers (left) operate on the 5-cycle graph, C5, and the path graph with 5 nodes, P5. For the GNN, we assume that node features are 1 for every node. The illustration on right compares the GNN updates with iterations of the Weisfeiler-Leman algorithm (1-WL) for checking graph isomorphism. Further GNN specifications and the Hash functions in 1-WL are noted. 

node-i in iteration-t. Initially, all nodes are identically coloured, c(0) 1 = · · · = c(0) 

n = c.The colour of node-i is updated as c(t+1) 

i = HASH 



c(t)

i , {{ c(t)

j : ( i, j ) are adjacent }} 



,where {{ . . . }} denotes a multiset, and HASH function uniquely maps each multiset to a new colour. The iterations repeat until the colouring stabilises. As shown in Figure 2, the 1-WL iterations resemble the update of node representations H(ℓ) across the GNN layers. This equivalence has been systematically studied by several works [13– 15], thereby establishing the limitations of several GNN architectures. For example, the GCN architecture [6] uses only the 1-hop neighbourhood, it is limited by the power of the 1-WL algorithm and, hence, cannot distinguish between C10 (10-cycle) and 

C5 ∪ C5 (two disjoint 5-cycles). This has led to the proposal of new, more expressive GNNs that aggregate over larger neighbourhoods [13]. However, [14] shows that the distinguishing capabilities of any MP-GNN are limited by the k-WL algorithm. The equivalence to WL not only characterises the expressive power (or inductive bias) of GNNs, but also helps to study the complexity of the hypothesis class F in Theorem 1. This is formalised in [15], which relates the VC-dimension of F to the WL expressivity. 

Generalisation error bounds in node-level prediction 

The study of generalisation error requires a different perspective when one focuses on node-level prediction, as graph isomorphism-based analysis is not needed when problems are defined on a single graph. We discuss this using the binary node clas-sification problem. Given a graph G = ( X, A ) on n nodes, it is assumed that each node-i has a true label yi ∈ {± 1}, drawn from a statistical law. Only the labels of 

m < n nodes are known (assume y1, . . . , y m known), and the goal is to predict the labels of the remaining n − m nodes. A GNN learns a map node-i 7 → fθ (i) ∈ R

with parameters θ = ( β, {Wℓ, v ℓ}ℓ). The hypothesis class corresponding to the GNN architecture is defined as F = {fθ ∈ Rn : θ = ( β, {Wℓ, v ℓ}ℓ)}. For some specified loss function, such as 0-1 error, the empirical risk and the generalisation error are defined as 

bR(f ) = 1

m

mX

i=1 

loss( yi, f (i)) and R(f ) = 1

n

nX

i=1 

loss( yi, f (i)), respectively, and one aims to derive guarantees akin to Theorem 1. In this context, the complexity of the class F

can be characterised in terms of the transductive Rademacher complexity [16], defined as bRm,n (F) = 

 1

m + 1

n − m



Eε1,...,ε n

"

sup 

f ∈F 

nX

i=1 

εif (i)

#

, where εi ∈ {− 1, 0, +1 } are 6i.i.d. variables that are Rademacher with probability p ∈ (0 , 1) and 0 with probabil-ity 1 − p, indicating observed and unobserved labels. The following theorem from [17] shows that the generalisation error in node level prediction is influenced by the choice of convolution S, as well as its interaction with the node features through SX . [17] further proves that using VC-dimension leads to trivial bounds in this context. 

Theorem 2 (Generalisation error bound for node classification using GCN [17]) Consider the node classification setting. Let bf ∈ F be the empirical risk minimiser. Then 

R( bf ) ≤ bR( bf ) + bRm,n (F) + O



max 

 1

√m , 1

√n − m



·



1 + plog(1 /δ )



,

with probability at least 1 − δ. Furthermore, for a L-layer GCN (2) with convolution operator 

S and Lipschitz smooth activation, the transductive Rademacher complexity is bounded as 

bRm,n (H) ≤ O n2

m(n − m)

> L−1

X

> ℓ=0

∥S∥ℓ

> ∞

!

+ ∥S∥L

> ∞

∥SX ∥2→∞ 

√ln n

!

, (4) 

where ∥·∥ ∞ in the induced ∞-norm and ∥·∥ 2→∞ is the entry-wise 2 → ∞ -norm of a matrix. 

# 3 Deep Learning Asymptotics of GNNs 

The above generalisation error bounds are not adapted to the training process, result-ing in loose bounds that do not explain the empirical behaviour of trained GNNs. Notably, they cannot explain why many GNNs suffer from oversmoothing , i.e., their performance degrades drastically with depth (increasing L); or why certain GNNs perform well on homophilic graphs but not on heterophilic graphs . To answer these questions, one needs to characterise the GNN learnt via training: a challenging prob-lem for nonlinear neural networks. Hence, either generalisation error is studied for linear GNN [18–20], or the GNN is simplified under different asymptotic regimes. Studying neural networks in the limit of infinitely-many parameters has been a useful tool in deep learning theory, as it provides tractable characterisations of the lim-iting model that retain many statistical properties of the finite neural networks used in practice, such as the double descent phenomenon, scaling laws, etc. The asymp-totics can be achieved at an infinite depth ( L → ∞ ), at an infinite hidden-layer width (min ℓ∈{ 1,...,L } dℓ → ∞ ), and with infinite attention heads in attention-based mod-els (like GAT), among others. Depth asymptotics, L → ∞ , in GNNs lead to trivial representations due to oversmoothing. 1 This has been rigorously studied from a gen-eralisation error perspective in [18, 21], and approaches for mitigating oversmoothing via residual connections or multi-scale representations have been analysed [22, 23]. We discuss two asymptotic regimes that give useful characterisations of the trained GNN. 

Asymptotics of wide GNNs: Gaussian Process and Neural Tangent Kernel 

Consider node-level prediction with the GNN map node-i 7 → fθ(i) = H(L) 

> i

β, where 

θ = ( β, {Wℓ, v ℓ}ℓ). In practice, the parameters are randomly initialised θ = θ0 and are           

> 1This is easily verified in linear GCN. Here, the update rule (2) with σas the identity map simplifies to
> H(L)=SLXW L−1· · · W1. For the normalised convolution operators, matrix algebraic calculations show that SLconverges to a rank one matrix as L→ ∞ , and hence, GCN cannot distinguish between nodes [18].

7−0.5 0.0 0.5        

> GNN Output
> −10
> −8
> −6
> −4
> −20
> log Density
> GCN: 1000 Width −2−1012
> GNN Output
> −30
> −25
> −20
> −15
> −10
> −50
> log Density
> GAT: 1 Head 1000 Width −1.0−0.50.00.51.0
> GNN Output
> −12
> −10
> −8
> −6
> −4
> −20
> log Density
> GAT: 100 Heads 1000 Width

Fig. 3 Histograms of {fθ0 (i)}i=1 ,...,n for randomly ini-tialised θ0 in 1-layer GCN and GAT for Erd˝ os-R´ enyi graphs with n = 100 [24]. (left) For GCN with width di = 1000, histogram of fθ0 (i) is close to a Gaussian distribution (red line fitted with mean and variance). (middle) For GAT in (2)–(3), the histogram is not Gaussian. (right) In prac-tice, each layer of GAT learns multiple parallel attention heads S(ℓ). GAT with 1000 width and 100 heads shows a GP behaviour, indicating that GP-limit holds when width and number of attention heads are simultaneously infinite. 

Fig. 4 Comparison of the kernels for Graph NTK (left) and Graph NNGP (right) derived for a 2-layer GCN on CORA dataset ( n = 2708). Both kernels show a block structure, corresponding to the 7 communities in CORA graph, but the blocks are more prominent in the Graph NTK. The plots are adapted from [23]. 

updated via gradient descent or its variants to minimise the training error. The aim of width asymptotics of GNN, or broadly neural networks, is to approximate the GNN 

fθ by a model ˜fθ that is nonlinear with respect to the input but linear in trainable parameters; that is, fθ(·) ≈ ˜fθ(·) = θ⊤ϕ(·) for some non-trainable nonlinear map ϕ(·). The first approximation stems from the observation that if all parameters in θ = θ0

are initialised as independent centred Gaussians with appropriately scaled variances, then by the central limit theorem, the map node-i 7 → fθ0 (i) converges to a Gaussian process (GP) on the nodes as min ℓ∈{ 1,...,L } dℓ → ∞ [25] (see Figure 3). In the context of GNNs, this is referred to as a Graph NNGP , and the corresponding covariance kernel for GCN (2) has been derived in [26] (see Theorem 3), while [24] derives the kernel for GAT (2)–(3) and graph transformers. From a Bayesian perspective, the limiting GP {fθ0 (i)}i=1 ,...,n is viewed as a prior, and the posterior prediction on the unlabelled nodes can be obtained by solving a GP regression or classification given the labels 

{yi}i=1 ,...,m ≪n. One may view the NNGP as a random feature model ˜f (i) = H(L) 

> i

β,where H(L) 

> i

is obtained from the random initialisation and β is learnt; and, in the infinite-width limit, the kernel matrix is K = Eθ0

H(L)(H(L))⊤ ∈ Rn×n. Note that the Graph NNGP is an over-simplification of GNN since only the last layer β is learnt. A better approximation can be derived via Taylor expansion at the initialisation 

θ0 as fθ(·) ≈ fθ0 (·)+ ∇θfθ0 (·)⊤(θ −θ0)+ 12 (θ −θ0)⊤∇2

> θ

fθ0 (·)( θ −θ0). It has been shown that [27], under suitable random initialisation θ0, and in the infinite-width limit, the second-order term vanishes. Hence, fθ(·) can be approximated by a kernel machine, with the kernel given by Kij = Eθ0

(∇θ fθ0 (i)) ⊤∇θ fθ0 (j), accounting for the infinite-width limit of the gradient at random initialisation. This is called the neural tangent kernel (NTK) [28], which has been derived for node and graph-level GCN in [23, 29]. Theorem 3 and Figure 4 show the difference between Graph NNGP and Graph NTK. 

Theorem 3 (Node-level kernels of Graph NNGP and Graph NTK for GCN [23, 26]) Con-sider the L layer GCN in (2) with node-level output fθ (i) = H(L) 

> i

β, and assume that the activation σ is differentiable. For ℓ = 0 , . . . , L , define the matrices Σ0 = SXX ⊤S⊤ and 

Σℓ+1 = SE ℓS⊤, where Eℓ = Ez∼N (0 ,Σℓ)

σ(z)σ(z)⊤ and ˙Eℓ = Ez∼N (0 ,Σℓ)

 ˙σ(z) ˙ σ(z)⊤. The 

8node-level Graph NNGP kernel is given by Knngp = EL ∈ Rn×n and the Graph NTK is                            

> Kntk =
> L−1X
> k=0
> S
> 
> . . . S
> 
> S
> |{z }
> L−1−kterms
> 
> Σk⊙˙Ek
> 
> S⊤⊙˙Ek+1
> 
> S⊤⊙. . .
> 
> S⊤⊙˙EL−1∈Rn×n,
> where ⊙denotes Hadamard product. The NNGP and NTK limits hold assuming that the parameters are independently initialised as (Wℓ)ij ∼ N (0 ,1
> dℓ)and dℓ→ ∞ for ℓ= 1 , . . . , L .

The NNGP and NTK limits allow for approximating the complex GNNs with kernel machines, where the optimal parameters can be characterised, specifically in squared regression or margin-based classification. The approximate models are competitive with GNNs in practice [26, 29], and they also provide kernel-based generalisation error bounds for GNNs [29]. Notably, the NNGP and NTK limits retain the structure of the graph convolutions S, thereby making it possible to analyse the influence of the graph on GNN performance. For example, NTK shows why row-normalised convolution 

Srow and skip connections improve the performance of GCNs [23], and NNGP-based structural comparison reveals why GATs can avoid oversmoothing at depth [24]. On the practical front, Graph NTK helps to design deterministic robustness certificates against data poisoning, which is the most difficult adversarial setting for GNNs [30]. 

Asymptotics of the graph size: Graphon Neural Network 

GNNs can also be studied under the asymptotics of the graph size. The convergence of a sequence of undirected graphs A ∈ [0 , 1] n×n with an increasing number of nodes 

n is well studied. The convergence is defined in terms of cut-distance or subgraph counts, and the limiting object is a symmetric measurable function A : [0 , 1] 2 → [0 , 1], known as a graphon [31]. Hence, for a convergent sequence of growing graphs, the associated GNNs also converge to a measurable operator. This limiting object has been independently introduced as continuous GNNs [32] and graphon neural networks [33]. The necessity of studying the graphon limit of GNNs must be clarified: while the graph remains fixed for node-level predictions, the limit allows one to examine the stability of GNN predictions [32] and the transferability of trained GNNs in graph-level task [33]. The following exposition of Graphon Neural Network builds on (2) and is a sim-plification of [32, 33]. In the limit of n → ∞ , the vertex set is mapped to the interval [0 , 1], the graph adjacency converges to the graphon A : [0 , 1] 2 → [0 , 1], and the node representation at layer-ℓ is a map h(ℓ) : [0 , 1] → Rdℓ , viewed as dℓ measurable functions 

h(ℓ)(·) = [ h(ℓ, 1) (·) · · · h(ℓ,d ℓ)(·)]. The GCN (2) extends to graphon neural network as 

h(ℓ+1) = σ

h 

S(h(ℓ, 1) ) · · · S (h(ℓ,d ℓ))

i

Wℓ



, S(h)( x) = 

Z 10

A(x, x ′)

pdeg( x)pdeg( x′) h(x′)d x′.

The above convolution operator S on measurable functions extends the symmetric normalised convolution Ssym , and deg( x) = R 10 A(x, x ′)d x′ is the degree function. We do not make assumptions about node features, which could be viewed as a measurable function h(0) : [0 , 1] → Rd0 . Hence, the graphon neural network is an operator mapping 

h(0) 7 → h(L). One may also obtain the graph level representation via mean pooling as 9hG = R 10 h(L)(x)d x. [32, 33] provide rates for the convergence of node- and graph-level representation from discrete n-node GNNs to continuous graphon neural networks. For finite width GNNs and dense graphs, the convergence rates are O(n−1/2) [33]. [34] studies the asymptotics under both width and graph size, deriving a Graphon NTK .

# 4 Study of GNNs under Random Graph Models 

The study of random graphs has been a central theme at the intersection of statistics and network analysis. Specifically, analysis under specific random graph models has provided deep insights into the statistical and computational hardness of graph prob-lems, such as community detection, and has also led to important tools and techniques, such as message-passing algorithms and the non-backtracking operator [1, 2, 10]. Inter-estingly, the statistical analysis of GNNs under random graphs is relatively unexplored, with only a few works providing concrete statistical guarantees in this setting. The Contextual Stochastic Block Model (CSBM) [35, 36] is well suited for studying GNNs. In a CSBM( n, p, q, μ), the n nodes are split into two latent communities with labels y1, . . . , y n ∼i.i.d Rademacher( {± 1}). The node features X ∈ Rn×d0 are sampled from a Gaussian mixture model with Xi ∼ N (yiμ, I d0×d0 ), where μ ∈ Rd0 is specified. The graph A ∈ { 0, 1}n×n has independent undirected edges Aij ∼ Bernoulli( p) for 

yi = yj , and Aij ∼ Bernoulli( q) for yi̸ = yj , where 0 < q < p < 1 are specified. We state the following result, adapted from [35, 36], which considers a simplification of the node classification problem. The authors study 1-layer GCN (2) and 1-layer GAT (2)–(3) with fixed parameters W0, v 0, β and verify if the map node-i 7 → f (i) = H(1)  

> i

β

has the same sign as yi. We use the standard asymptotic notations O(·), Ω( ·), ω (·). 

Theorem 4 (Exact recovery of latent classes in CSBM using GCN or GAT [35, 36]) Consider a random graph (X, A ) ∼ CSBM( n, p, q, μ). Assume that p, q = ω

 (log n)2

> n



and p−qp+q = Ω(1) .Analysis of GCN: If ∥μ∥ = ω

 log n√n(p+q)



, there is a 1-layer GCN with sigmoid activation and specific θ = ( W0, β ) so that sign( fθ (·)) recovers labels y1, . . . , y n with probability 1 − o(1) .Analysis of GAT: If ∥μ∥ = ω  √log n, there exist parameters θ = ( W0, v 0, β ) for a 1-layer GAT with leaky ReLU activation in (3) such that, with probability 1−o(1) , sign( fθ (·)) exactly recovers all labels. However, the model mis-classifies Ω( n) nodes if ∥μ∥ = o

q log nn(p+q)



.Performance ignoring the graph A: As a baseline, it can be shown that if only the node fea-tures H(0) = X are used, then if ∥μ∥ = ω  √log n, then there exists a linear classifier 

fβ (i) = sign( H(0)  

> i

β) that exactly recovers all labels with probability 1 − o(1) . However, if 

∥μ∥ = O (1) , then H(0)   

> ii=1 ,...,n

are not separable by linear classifiers with probability Ω(1) .

Theorem 4 shows that for semi-sparse graphs, p, q = ω  (log n)2

> n

, a 1-layer GCN can exactly recover communities even when the node features X cannot be linearly separated. The statement for GAT is nuanced, as weak separation of features (small 

μ) leads to poor learning of the attention S(0) . However, Theorem 4 assumes fixed parameters and does not account for training the GNN. The only works that study the statistical performance of GNNs with learnt parameters are [18–20], where the analysis 10 is restricted to GCNs with linear activation. In particular, [20] precisely derives the generalisation error of linear GCN for node-level classification under a CSBM, thereby showing that GCN exhibits a double descent phenomenon, which is modulated by the graph signal-to-noise ratio n(p−q)

√n(p+q) . Observe that node features X and adjacency 

A are aligned in CSBM; hence, in Theorem 4, GCN can improve upon the linear classification of X. This improvement does not universally hold in practice. To study the effect of misalignment, [19] derives the generalisation error of linear GCN under a more general model, where the weighted graph may not completely align with the node features. The error in [19] identifies failure modes of GNN architectures, stemming from misalignment or from heterophilic graph structures. [18] introduces a contextual variant of the graphon model and derives generalisation error bounds for linear GCN. 

# 5 Discussion and Open Problems 

In this paper, we provide an overview of the different approaches for studying statistical generalisation in GNNs for graph-level and node-level prediction problems, exempli-fied through Theorems 1–4. To summarise, the learning theoretic perspective (Section 2) provides data-agnostic generalisation error bounds for GNNs. In conjunction with expressivity results for GNNs, these bounds can be useful for graph-level prediction. However, the bounds are not tight and may not reflect the empirical performance of trained GNNs. Asymptotic analysis of GNNs (Section 3), specifically the asymptotics in the network width, the attention heads, or the graph size, helps to simplify them to either kernel machines or operators on function space. This helps to study the sta-tistical performance or the stability of trained GNNs, albeit remaining agnostic to the data or graph model. The study of GNNs under specific random graph models (Section 4), such as CSBM, allows for a data-dependent analysis of statistical error in GNNs. This direction is emerging, and there are no statistical guarantees for trained GNNs beyond the specific case of linear GCNs. We note some interesting research directions and open questions, focusing on graph-, node-, or edge-level prediction problems. 

Node-level prediction. The statistics literature has made tremendous contributions to the community detection problem [2, 10], introducing CSBM and other models. While node-level prediction using GNN is closely related, the current GNN guarantees do not compare to the optimal estimators developed in statistics (for example, Theorem 4 holds only for semi-sparse graphs). More importantly, existing works do not provide precise error rates for trained GNNs, beyond the simple case of linear GNNs. 

> Research questions : The central open problem is to derive the generalisation error of a trained non-linear GNN under sparse CSBM with partial labels. This would answer if GNNs are optimal for CSBM in comparison to spectral or message passing estimators [2, 10]. One may also study whether GNNs provide benefits under general latent space models [18, 19].

Studying trained GNNs requires accounting for optimisation. Recent work on neural networks has investigated this problem for learning in high dimensions [37], but there are no similar studies on GNNs. An alternative is to use the NNGP or NTK limits of GNNs and study their error rates under CSBM. However, this is complicated since existing convergence guarantees either require fixed n for width dℓ → ∞ [27] or fixed 11 width for n → ∞ [33]. An interesting problem is to study the convergence rates to a Graphon NTK (or NNGP) as n, d ℓ → ∞ , which can be subsequently used to derive the non-asymptotic error rates for trained GNNs. 

Graph-level prediction. A potential path towards reconciling the theory of GNN-based graph-level prediction with statistical network analysis would be to consider hypothesis testing of random graphs. Recent work has developed testing procedures and statistical consistency under general models (inhomogeneous Erd˝ os-R´ enyi, graphon, exponential random graph models (ERGM)), as well as their relation to other graph learning problems [38, 39]. Graphon-based results for GNNs [18, 32, 33] provide a direction to study GNNs in the context of hypothesis testing of graphs or graph classification.   

> Research questions : Can one design a consistent GNN-based two-sample test for random graphs sampled from graphons or ERGMs? If a graphon neural network computes different representations hGfor two graphons, then its discretised GNN could potentially lead to a consistent test. On a related note, studying Weisfeiler-Leman tests [8, 14] in the context of hypothesis testing of random graphs could relate network statistics to graph isomorphism.

Edge-level prediction. Edge or link prediction is an important problem in statistical network analysis: Given a graph G with a partially observed adjacency, the goal is to predict the unobserved links. It is also closely related to the network model estimation 

problem. Little is known about the generalisation error of link prediction using GNNs. While the GNN literature typically uses the node representation H(L) for link predic-tion, a notable work [40] uses the graphon limit to show that such methods can fail in out-of-distribution generalisation to larger graphs and suggests learning represen-tations of node pairs. This idea is related to attention learning in GAT or transformer architectures; for example, S(ℓ) in (3) provides an estimate of edge probability. [24] derives an edge-level kernel in the GP limit of GAT, which could be used for link pre-diction, while the analysis of GAT with specific parameters under CSBM shows when the graph attention can or cannot predict the edges [36]. 

> Research questions : There is a need to derive statistical guarantees for GNN-based link prediction under general graph models, such as graphons [41]. Such results can both estab-lish the suitability of GNNs for network model estimation and rigorously explain the significance of attention learning in GATs, which implicitly modifies the graph structure.

Acknowledgements. The work of N. Ayday and D. Ghoshdastidar is supported by the German Research Foundation (DFG) Priority Program on Theoretical Founda-tions of Deep Learning (SPP-2298; GH-257/2-2). N. Ayday also acknowledges support from the Konrad Zuse School of Excellence in Reliable AI. 

# References 

[1] Chen, Z., Li, L., Bruna, J.: Supervised community detection with line graph neural networks. In: International Conference on Learning Representations. Openreview.net (2019) [2] Abbe, E.: Community detection and stochastic block models: Recent develop-ments. Journal of Machine Learning Research 18 (177), 1–86 (2017) 12 [3] Bronstein, M.M., Bruna, J., Cohen, T., Velickovi, P.: Geometric deep learning: Grids, groups, graphs, geodesics, and gauges. arXiv:2104.13478 (2021) [4] Wu, Z., Pan, S., Chen, F., Long, G., Zhang, C., Yu, P.S.: A comprehensive survey on graph neural networks. IEEE Transactions of Neural Networks and Learning Systems 32 (1), 4–24 (2021) [5] Yuan, C., Zhao, K., Kuruoglu, E.E., Wang, L., Xu, T., Huang, W., Zhao, D., Cheng, H., Rong, Y.: A survey of graph transformers: Architectures, theories and applications. arXiv preprint (arXiv:2502.16533) (2025) [6] Kipf, T.N., Welling, M.: Semi-supervised classification with graph convolutional networks. In: International Conference on Learning Representations (2017) [7] Velickovic, P., Cucurull, G., Casanova, A., Romero, A., Li` o, P., Bengio, Y.: Graph attention networks. In: International Conference on Learning Representations. Openreview.net (2018) [8] Vasileiou, A., Jegelka, S., Levie, R., Morris, C.: Survey on generalization theory for graph neural networks. Journal of Machine Learning Research (in press) (2026) [9] Bach, F.: Learning Theory from First Principles. The MIT Press, (2024) [10] Cai, T.T., Liang, T., Rakhlin, A.: Weighted message passing and minimum energy flow for heterogeneous stochastic block models with side information. Journal of Machine Learning Research 21 (11), 1–34 (2020) [11] Garg, V.K., Jegelka, S., Jaakkola, T.: Generalization and representational limits of graph neural networks. In: International Conference on Machine Learning. PMLR (2020) [12] Weisfeiler, B., Leman, A.A.: A Reduction of a Graph to a Canonical Form and an Algebra Arising During This Reduction. Nauchno-Technicheskaya Informatsia 

Ser. 2 (N9), 12–16 (1968) [13] Morris, C., Ritzert, M., Fey, M., Hamilton, W.L., Lenssen, J.E., Rattan, G., Grohe, M.: Weisfeiler and leman go neural: Higher-order graph neural networks. AAAI Press, vol. 18, pp. 4602–4609 (2019) [14] Geerts, F., Reutter, J.L.: Expressiveness and approximation properties of graph neural networks. In: International Conference on Learning Representations. Openreview.net (2022) [15] Morris, C., Geerts, F., T¨ onshoff, J., Grohe, M.: WL meet VC. In: International Conference on Machine Learning. PMLR, pp. 25275–25302 (2023) [16] El-Yaniv, R., Pechyony, D.: Transductive rademacher complexity and its appli-cations. Journal of Artificial Intelligence Research 35 , 193–234 (2009) 13 [17] Esser, P.M., Vankadara, L.C., Ghoshdastidar, D.: Learning theory can (some-times) explain generalisation in graph neural networks. In: Advances in Neural Information Processing Systems (2021) [18] Keriven, N.: Not too little, not too much: a theoretical analysis of graph (over)smoothing. In: Advances in Neural Information Processing Systems (2022) [19] Ayday, N., Sabanayagam, M., Ghoshdastidar, D.: Why does your graph neu-ral network fail on some graphs? insights from exact generalisation error. arXiv preprint (arXiv:2509.10337) (2025) [20] Shi, C., Pan, L., Hu, H., Dokmani´ c, I.: Homophily modulates double descent gen-eralization in graph convolution networks. Proceedings of the National Academy of Sciences 121 (8), 2309504121 (2024) [21] Oono, K., Suzuki, T.: Graph neural networks exponentially lose expressive power for node classification. In: International Conference on Learning Representations. Openreview.net (2020) [22] Oono, K., Suzuki, T.: Optimization and generalization analysis of transduction through gradient boosting and application to multi-scale graph neural networks. In: Advances in Neural Information Processing Systems (2020) [23] Sabanayagam, M., Esser, P.M., Ghoshdastidar, D.: Analysis of convolutions, non-linearity and depth in graph neural networks using neural tangent kernel. Transactions of Machine Learning Research (Openreview.net) (2023) [24] Ayday, N., Yang, L., Ghoshdastidar, D.: Gaussian process limit reveals structural benefits of graph transformers. arXiv preprint (arXiv:2603.17569) (2026) [25] Lee, J., Bahri, Y., Novak, R., Schoenholz, S.S., Pennington, J., Sohl-Dickstein, J.: Deep neural networks as gaussian processes. In: International Conference on Learning Representations. Openreview.net (2018) [26] Niu, Z., Anitescu, M., Chen, J.: Graph neural network-inspired kernels for gaussian processes in semi-supervised learning. In: International Conference on Learning Representations. Openreview.net (2023) [27] Liu, C., Zhu, L., Belkin, M.: On the linearity of large non-linear models: when and why the tangent kernel is constant. In: Advances in Neural Information Processing Systems (2020) [28] Jacot, A., Hongler, C., Gabriel, F.: Neural tangent kernel: Convergence and gen-eralization in neural networks. In: Advances in Neural Information Processing Systems (2018) [29] Du, S.S., Hou, K., Salakhutdinov, R., P´ oczos, B., Wang, R., Xu, K.: Graph neural 14 tangent kernel: Fusing graph neural networks with graph kernels. In: Advances in Neural Information Processing Systems (2019) [30] Sabanayagam, M., Gosch, L., G¨ unnemann, S., Ghoshdastidar, D.: Exact cer-tification of (graph) neural networks against label poisoning. In: International Conference on Learning Representations. Openreview.net (2025) [31] Lov´ asz, L.: Large Networks and Graph Limits. Colloquium Publications. Ameri-can Mathematical Society, vol. 60 (2012) [32] Keriven, N., Bietti, A., Vaiter, S.: Convergence and stability of graph convo-lutional networks on large random graphs. In: Advances in Neural Information Processing Systems (2020) [33] Ruiz, L., Chamon, L.F.O., Ribeiro, A.: Graphon neural networks and the transfer-ability of graph neural networks. In: Advances in Neural Information Processing Systems (2020) [34] Krishnagopal, S., Ruiz, L.: Graph neural tangent kernel: Convergence on large graphs. In: International Conference on Machine Learning. PMLR (2023) [35] Baranwal, A., Fountoulakis, K., Jagannath, A.: Graph convolution for semi-supervised classification: Improved linear separability and out-of-distribution generalization. In: International Conference on Machine Learning. PMLR, pp. 684–693 (2021) [36] Fountoulakis, K., Levi, A., Yang, S., Baranwal, A., Jagannath, A.: Graph attention retrospective. Journal of Machine Learning Research 24 (246) (2023) [37] Abbe, E., Adser` a, E.B., Misiakiewicz, T.: SGD learning on neural networks: leap complexity and saddle-to-saddle dynamics. In: Annual Conference on Learning Theory. PMLR, pp. 2552–2623 (2023) [38] Sabanayagam, M., Vankadara, L.C., Ghoshdastidar, D.: Graphon based clustering and testing of networks: Algorithms and theory. In: International Conference on Learning Representations. OpenReview.net (2022) [39] Ghosh, S., Karmakar, R.N., Lahiry, S.: Maximum entropy based testing in network models: ERGMs and constrained optimization. arXiv preprint (arXiv:2602.20844) (2026) [40] Zhou, Y., Kutyniok, G., Ribeiro, B.: OOD link prediction generalization capa-bilities of message-passing gnns in larger test graphs. In: Advances in Neural Information Processing Systems (2022) [41] Gao, C., Lu, Y., Zhou, H.H.: Rate-optimal graphon estimation. The Annals of Statistics 43 (6), 2624–2652 (2015) 15