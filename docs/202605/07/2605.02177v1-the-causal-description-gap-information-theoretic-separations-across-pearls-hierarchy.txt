Title: The Causal Description Gap: Information-Theoretic Separations Across Pearl's Hierarchy

URL Source: https://arxiv.org/pdf/2605.02177v1

Published Time: Tue, 05 May 2026 01:33:28 GMT

Number of Pages: 17

Markdown Content:
# The Causal Description Gap: Information-Theoretic Separations Across Pearl’s Hierarchy 

Seyed Morteza Emadi 

Kenan-Flagler Business School University of North Carolina at Chapel Hill 

seyed_emadi@kenan-flagler.unc.edu 

## Abstract 

Pearl’s causal hierarchy shows that observational, interventional, and counter-factual queries are qualitatively distinct. We ask a quantitative version of this question: how many additional bits are needed to specify higher-rung causal an-swers once lower-rung answers are known? We formalize this via query-class description length , the Kolmogorov complexity of the answer oracle induced by an SCM for a class of queries. Our main construction gives binary acyclic SCMs whose observational distribution has constant description length, while the single-variable interventional answer oracle has description length Θ( n2). A degree-sensitive upper bound shows that finite-gate-schema SCMs of indegree d

have observational–interventional gap at most O(nd log( en/d ) + n log n), making the quadratic construction order-optimal in the dense regime and a rooted-tree construction order-optimal for bounded indegree. The quadratic separation per-sists under ε-accurate total-variation descriptions for every fixed ε < 1/4. At the next rung, the full hard-do interventional oracle can still leave a Θ( n) counterfac-tual description gap. A general ambiguity-to-bits theorem and Shannon analogue show that these gaps equal the logarithm of residual higher-rung ambiguity up to lower-order terms. 

## 1 Introduction 

Predictive accuracy identifies what a system does under passive observation. Causal questions go further: interventional queries (“what if I set X?”) and counterfactual queries (“what would have happened if X had been different?”). Pearl’s causal hierarchy [ 11 ] formalizes these three levels (association, intervention, counterfactual), and the Causal Hierarchy Theorem [ 1 ] shows the hierarchy is generically strict: higher-rung queries cannot in general be answered from lower-rung information. But how large is the gap? Is the additional information bounded by a constant, or can it scale with system size? We give a quantitative answer in bits. For a structural causal model M , we define query-class description lengths DL 1(M ), DL 2(M ), DL 3(M ) as the Kolmogorov complexities (conditional on 

n) of the observational, interventional, and counterfactual answer objects, and causal description gaps ∆2|1, ∆3|2 as the additional bits needed conditional on the lower rung. The framework lets us treat “how much causal information is missing” as a precise quantity. 

Residual information after non-identification. Existing hierarchy and identifiability results ask whether a higher-rung answer is determined by lower-rung information. We study the residual-information problem that remains when identification fails: among all SCMs that agree on the lower-rung answer, how many bits are still needed to specify the higher-rung answer oracle? Our 

> Preprint.
> arXiv:2605.02177v1 [stat.ML] 4 May 2026

separations refine non-identifiability from a Boolean obstruction into a quantitative information measure. The familiar two-variable ambiguity X → Y versus X ← Y via deterministic copy is the one-bit version of this phenomenon; our main construction scales it to 2Θ( n2) residual mechanisms, forcing a Θ( n2)-bit gap. 

Contributions. 

1. Framework. We turn Pearl’s qualitative hierarchy into a quantitative information hierarchy by defining query-class description length and causal description gaps: the residual bits needed to specify higher-rung answer oracles after lower-rung answers are fixed (§3). 2. Observational–interventional separations and degree-sensitive optimality. Explicit SCM families with constant observational description length but Θ( n log n) and Θ( n2)

interventional gaps (§4–§5); a matching upper bound O(nd log( en/d ) + n log n) for finite-gate-schema SCMs of indegree d, making the tree and bipartite constructions order-optimal at d = O(1) and d = Θ( n) (§5.1). 3. Finite-precision and learning consequences. The quadratic gap survives ε-accurate total-variation descriptions for every ε < 1/4 (§5.2); consequently no observational learner beats the 2−m2

guessing rate in our family (§7.1). 4. Counterfactual gap and unifying ambiguity principle. Mechanisms with identical full hard-do oracles can still differ by Θ( n) counterfactual bits (§6); a general ambiguity-to-bits theorem and Shannon analogue show these gaps equal the log of residual higher-rung ambiguity up to lower-order terms (§7). 

Summary of constructions. Table 1 previews the three SCM families used to instantiate the framework. All three have constant-bit lower-rung descriptions; the gap in each row is at least the log of the higher-rung ambiguity (Theorem 7.2), and is matched by an explicit upper-bound encoding.                      

> Family Lower rung Higher rung Ambiguity Gap Where
> Rooted trees Obs Int 1nn−1Θ( nlog n)§4 Bipartite graphs Obs Int 12m2
> Θ( n2)§5 Modular XOR Int all CF 12mΘ( n)§6

Table 1: Three SCM families instantiating the framework. “Lower / Higher rung” name the query classes between which the gap is measured; “Ambiguity” is the number of higher-rung answer objects consistent with a single lower-rung answer. 

## 2 Related Work 

Pearl’s causal hierarchy. Pearl’s three-level hierarchy [ 11 , 12 ] is generically strict: the Causal Hierarchy Theorem [ 1 ] establishes non-identifiability, showing that some higher-rung queries are not functions of lower-rung information. Shpitser and Pearl [15] give complete identification algorithms; Ibeling and Icard [6] compare frameworks. We quantify the size of the residual answer set: even for simple binary acyclic SCMs, its description length can be Θ( n2).

Causal discovery. Constraint- and score-based methods identify causal graphs up to Markov equivalence from observations [ 5, 13 , 16 ]. Eberhardt et al. [3] and Shanmugam et al. [14] bound the number/sample complexity of interventions for identification. We give description-length lower bounds: even with infinite observational data, the obs–int information gap is Θ( n2) in our family. 

Description length and MDL. Kolmogorov complexity [ 9, 10 ] and MDL [ 4] measure the informa-tion needed to describe objects or models. Our use is different: the compressed object is neither the dataset nor the SCM parameterization, but the answer oracle for a query class. This distinction is what enables our separations: the observational answer oracle can have constant description length even when the corresponding interventional oracle requires Θ( n2) bits. 2Causal reasoning in AI. Recent benchmarks find uneven causal-reasoning performance in LLMs [ 7,8]; our results give one information-theoretic reason why observational predictive training alone need not induce interventional competence in worst-case structured families. 

Complexity across Pearl’s hierarchy. Dörfler et al. [2] study satisfiability across probabilistic, interventional, and counterfactual languages in Pearl’s hierarchy, proving strictly increasing com-putational complexity for certain languages (e.g., NP PP , PSPACE, and NEXP completeness under addition and marginalization). Their setting assumes the SCM is given and asks how hard it is to decide whether causal statements are jointly satisfiable; we bound the information content of higher-rung answer oracles given lower-rung ones, providing complementary views on the cost of climbing the hierarchy. 

## 3 Framework: Query-Class Description Length 

3.1 Structural Causal Models Definition 3.1 (Structural Causal Model) . A structural causal model (SCM) on binary variables 

X1, . . . , X n ∈ { 0, 1} is a tuple M = ( G, {fi}, P U ) where: • G is a directed acyclic graph (DAG) on [n] := {1, . . . , n }.• U1, . . . , U n are independent exogenous (noise) variables with distribution PU .• Structural equations specify each variable as a function of its parents and noise: 

Xi = fi

 (Xj )j∈pa G(i), U i

.

We require fi to be computable and PU to have rational probabilities, ensuring all quantities are well-defined. The SCM generates data as follows: sample (U1, . . . , U n) ∼ PU , then compute Xi in topological order. The resulting distribution PM (X1, . . . , X n) is the observational distribution .

Definition 3.2 (Intervention) . An intervention do( Xi = x) modifies M by replacing the equation for Xi with the constant Xi := x, leaving all other equations unchanged. The resulting distribution is 

P do( Xi=x) 

> M

.Interventions are the formal model of “what happens if we force Xi to take value x, breaking its dependence on its natural causes.” 

Definition 3.3 (Counterfactual) . For a fixed exogenous realization u = ( u1, . . . , u n), the counterfac-tual X(i←b)(u) is the value of the entire system (X1, . . . , X n) when we intervene with do( Xi = b)

but use the same noise realization u.Counterfactual queries ask about joint distributions like P (X, X (i←0) , X (i←1) ): the “parallel worlds” where we observe the actual outcome and both hypothetical interventions, all computed from the same underlying noise. 

3.2 Query Families: What We Need to Specify 

To measure information content at each level of the hierarchy, we define what must be specified to answer all queries at that level. 

Definition 3.4 (Query Families) . For an SCM M on n variables: • Observational: Obs (M ) := PM , the joint distribution over (X1, . . . , X n).• Single-node interventional: 

Int 1(M ) := 



PM ,  P do( Xi=0)  

> M

, P do( Xi=1) 

> M

ni=1 



.

This lists the observational distribution plus the 2n single-variable interventional distribu-tions. 3• Single-node counterfactual: 

CF 1(M ) := 



P  X, X (i←0) , X (i←1) ni=1 

.

For each variable i, this is the joint distribution of the actual world X and the two coun-terfactual worlds under do( Xi = 0) and do( Xi = 1) , evaluated on the same exogenous noise. 

3.3 Description Lengths and the Causal Gap Definition 3.5 (Kolmogorov Complexity) . Fix a universal prefix-free Turing machine. The Kol-mogorov complexity K(z) of a string z is the length of the shortest program that outputs z. The conditional complexity K(z | w) is the shortest program length when w is given as auxiliary input. All equalities hold up to an additive O(1) constant. Distributions are encoded as the string of their (rational, lowest-terms) probabilities; the choice of computable encoding affects K only by O(1) .

Definition 3.6 (Query-Class Description Length: General Form) . Let Q be a query class with a computable answer encoding Ans Q(M ) (a finite string summarizing the answers to all queries in Q

when posed to SCM M ). The query-class description length of M with respect to Q is 

DL( Q; M ) := K Ans Q(M ) | n.

The conditional gap between query classes Q2 and Q1 is 

∆( Q2 | Q1; M ) := K Ans Q2 (M ) | Ans Q1 (M ), n .

Specializing to the three rungs of the causal hierarchy gives the canonical lengths DL 1(M ) := 

K(Obs (M ) | n), DL 2(M ) := K(Int 1(M ) | n), and DL 3(M ) := K(CF 1(M ) | n).

Lemma 3.7 (Counting bound for Kolmogorov complexity) . Let z1, . . . , z N be N distinct strings and let w be an arbitrary auxiliary string. Then: 1. At least one zi satisfies K(zi | w) ≥ log 2 N − O(1) .2. For every c ≥ 0, all but at most a 2−c fraction of the zi satisfy K(zi | w) ≥ log 2 N − c −

O(1) .The unconditional version ( w empty) is the special case. We apply this lemma with w = n, w = ( P, n ),or w = ( I, n ) as needed. Proof. For any fixed w, there are at most 2k − 1 programs of length less than k (summing over lengths 0, 1, . . . , k − 1). Setting k = ⌊log 2 N ⌋ shows that fewer than N strings can have conditional complexity K(· | w) below log 2 N − O(1) , so at least one must satisfy K(zi | w) ≥ log 2 N − O(1) .For part (2), at most 2log 2 N −c = N · 2−c strings can have conditional complexity below log 2 N −

c − O(1) , so the remaining fraction ≥ 1 − 2−c satisfies the bound. 

Definition 3.8 (The Causal Description Gap) . The causal description gap is the additional informa-tion needed beyond observations: 

∆2|1(M ) := K(Int 1(M ) | Obs (M ), n ).

Similarly, ∆3|2(M ) := K(CF 1(M ) | Int 1(M ), n ).

Lemma 3.9 (Hierarchy Monotonicity) . DL 1(M ) ≤ DL 2(M ) + O(1) ≤ DL 3(M ) + O(1) .Proof. Direct from Definition 3.4: Obs (M ) is a component of Int 1(M ), and Int 1(M ) is a marginal of CF 1(M ). Both reductions are computable. Our constructions show these inequalities can be far from tight: the rest of the paper exhibits explicit families with large gaps. 4Lemma 3.10 (Conditioning on a constant-complexity object is free) . If K(Obs (M ) | n) = O(1) ,then K(Int 1(M ) | Obs (M ), n ) = K(Int 1(M ) | n) ± O(1) , and consequently ∆2|1(M ) =DL 2(M ) ± O(1) .Proof. The upper bound K(Int 1 | Obs , n ) ≤ K(Int 1 | n) + O(1) holds by ignoring the auxiliary input. For the lower bound, concatenate the constant-length program that outputs Obs (M ) from 

n with a program that outputs Int 1(M ) from (Obs (M ), n ): this yields K(Int 1 | n) ≤ K(Int 1 |

Obs , n ) + O(1) .In all our constructions DL 1 = O(1) , so by Lemma 3.10 the gap ∆2|1 equals DL 2 ±O(1) . Chain-rule overhead is O(log n) when conditioning on a non-trivial object; we omit it from theorem statements except where it affects the leading order. 

## 4 Warm-Up: The Rooted-Tree Family 

For intuition, structure n binary variables as a hidden rooted tree T on [n] with Xr := Ur ∼

Bernoulli(1 /2) and Xv := Xpa T (v) for v̸ = r. All trees produce the same observational distribution 

P ⋆ on {0n, 1n}, so DL 1 = O(1) , but do( Xi = 0) deterministically forces every descendant of i

to 0 (non-descendants remain ∼ Bernoulli(1 /2) ), so the n single-node interventions decode the descendant sets and hence T .

Theorem 4.1 (Rooted-tree separation) . Let Mn 

> tree

be the family of rooted-tree SCMs MT above. All members share Obs (MT ) = P ⋆ with DL 1(MT ) = O(1) . Moreover: 1. Upper bound. For every T , DL 2(MT ) ≤ (n − 1) log 2 n + O(log n).2. Lower bound. For uniformly random T on [n] and every c ≥ 0,

Pr ∆2|1(MT ) ≥ (n − 1) log 2 n − c − O(log n) ≥ 1 − 2−c.

Proof idea. The upper bound uses the Prüfer encoding of T ((n − 2) log 2 n bits plus O(log n) for the root); the lower bound counts: by Cayley’s formula there are nn−1 rooted labeled trees, and the descendant decoding shows the map T 7 → Int 1(MT ) is injective. Full proof in Appendix A. The bound is order-tight in the bounded-indegree regime (§5.1); allowing larger indegree raises the gap quadratically. 

## 5 Quadratic Separation: The Bipartite-Graph Construction 

We now reach the headline result. The hidden parameter is a bipartite graph G ⊆ A × B with 

|A| = |B| = m, encoding m2 bits of structure: a quadratic upgrade over the O(n log n) bits of a rooted tree. 

Definition 5.1 (Bipartite-graph SCM) . Let n = 2 m + 1 . Partition the variables into a root r, layer 

A = {a1, . . . , a m}, and layer B = {b1, . . . , b m}. For a bipartite graph G ⊆ A × B, the SCM MG

has Ur ∼ Bernoulli(1 /2) as the only random exogenous, and structural equations 

Xr := Ur , Xai := Xr for all ai ∈ A, Xbj := Xr ∧ ^ 

> (ai,b j)∈G

Xai for all bj ∈ B, 

where the empty AND is defined as 1.When Xr = 1 , layer A is all-ones and every AND-gate in B outputs 1; when Xr = 0 , everything is 0. So observationally every G yields the same distribution on {0n, 1n}. But do( Xai = 0) forces every neighbor bj of ai to 0 deterministically, while non-neighbors remain ∼ Bernoulli(1 /2) , so the 

m row-interventions reveal the entire m × m adjacency matrix, distinguishing all 2m2

choices of G.Proofs of all lemmas and theorems in this section are in Appendix B. 

Lemma 5.2 (All Graphs Have the Same Observations) . For every bipartite graph G ⊆ A × B, we have Obs (MG) = P ⋆, where P ⋆ assigns probability 1/2 to 0n and 1/2 to 1n.

5Lemma 5.3 (Interventions Reveal the Graph) . For any ai ∈ A and the intervention do( Xai = 0) :

P do( Xai =0)  

> MG

(Xbj = 0) = 

1 if (ai, b j ) ∈ G, 

1/2 if (ai, b j ) /∈ G. 

Thus the neighborhood NG(ai) := {bj : ( ai, b j ) ∈ G} is determined by P do( Xai =0)  

> MG

.

Lemma 5.4 (The Map G 7 → Int 1(MG) is Injective) . Different bipartite graphs yield different interventional families. 

Theorem 5.5 (Quadratic separation: Θ( n2)). Let n = 2 m + 1 and Mn 

> bip

:= {MG : G ⊆ A × B}

as in Definition 5.1. Then: 1. Upper bound. For every G, DL 2(MG) ≤ m2 + O(1) .2. Lower bound. For G ∼ Unif(2 A×B ) and every c ≥ 0,

Pr ∆2|1(MG) ≥ m2 − c − O(log n) ≥ 1 − 2−c.

In particular, taking c = m2/2 shows that a uniformly random graph has ∆2|1(MG) = Ω( n2) except with probability exp( −Ω( n2)) .Proof idea. Upper bound: encode G as an m × m binary adjacency matrix ( m2 bits) and let a fixed program simulate MG. Lower bound: by Lemma 5.4 there are 2m2

distinct interventional families, all sharing Obs = P ⋆ with DL 1 = O(1) , and the conditional counting bound gives the high-probability statement. Full proof in Appendix B. 

5.1 Degree-Sensitive Optimality 

The quadratic separation in Theorem 5.5 uses layer-B nodes whose indegree can grow with n. This is not an artifact: within natural finite-gate-schema SCM classes, the largest possible description gap scales with the number of possible parent choices. 

Definition 5.6 (Finite-gate-schema bounded-indegree SCM class) . A gate schema is a computable family γ = ( γk)k≥0 with γk : {0, 1}k × U γ → { 0, 1}, specified by a constant-length program independent of n and k (so the schema is defined uniformly across arities). Examples include the constant, copy, negation, parity, and unbounded-AND/OR schemas. Fix a finite set Γ of gate schemas and a finite set Π of finite-support rational exogenous noise distributions. Let Mn,d (Γ , Π) be the class of binary acyclic SCMs on n endogenous variables such that: 1. every endogenous variable has at most d endogenous parents; 2. each structural equation is obtained by choosing a schema γ ∈ Γ and applying its k-ary component γk to the chosen k ≤ d parent values and local exogenous noise; 3. each local exogenous noise distribution is chosen from Π.The libraries Γ and Π are fixed independently of n.The bipartite-graph SCMs of Definition 5.1 belong to Mn,n −1(Γ bip , Πbip ) for Γbip = {copy , AND }

and Πbip = {δ0, Bernoulli(1 /2) }, since the AND schema applies uniformly at every arity. The rooted-tree SCMs belong to Mn, 1({copy }, {δ0, Bernoulli(1 /2) }).

Theorem 5.7 (Degree-sensitive upper bound) . For every fixed finite gate-schema library Γ and finite noise library Π, and every 1 ≤ d ≤ n − 1, there exists a constant CΓ,Π such that every 

M ∈ M n,d (Γ , Π) satisfies 

DL 2(M ) ≤ CΓ,Π · n + n log 2

 dX

> k=0

n − 1

k



+ O(n log n).

In particular, 

DL 2(M ) = O



nd log en d + n log n



,

and consequently 

∆2|1(M ) ≤ O



nd log en d + n log n



.

6Proof idea. Encode an M ∈ M n,d (Γ , Π) by a topological order, each variable’s parent set, and its gate/noise choices; the binomial-sum inequality Pdk=0 

 n−1

> k

 ≤ (d + 1)( e(n − 1) /d )d for 

1 ≤ d ≤ n − 1 gives the asymptotic form. Full proof in Appendix B. 

Corollary 5.8 (Optimality of the tree and bipartite constructions) . Within finite-gate-schema SCM classes: if d = O(1) , every observational–interventional gap is at most O(n log n), achieved by the rooted-tree construction (Theorem 4.1); if d = Θ( n), the upper bound becomes O(n2), achieved by the bipartite construction (Theorem 5.5). The transition from Θ( n log n) to Θ( n2) is exactly the transition from bounded-degree to dense mechanisms. Proof. Substitute d = O(1) and d = Θ( n) into Theorem 5.7; the rooted-tree and bipartite con-structions lie in Mn, 1 and Mn,n −1 under the gate-schema libraries identified after Definition 5.6, realizing the rates via Theorems 4.1 and 5.5. 

5.2 Finite-Precision Robustness 

The previous results are stated for exact query answers. We now show that the quadratic separation is not an artifact of exact arithmetic: it persists even when interventional distributions only need to be specified up to constant total-variation error. 

Definition 5.9 (Approximate query-class description length) . Let dQ be a metric on answer objects for query class Q. For ε > 0, define 

Kε

 Ans Q(M ) | w := min |p| : dQ

 U (p, w ), Ans Q(M ) ≤ ε ,

where U is the fixed universal machine and w is an auxiliary input. The corresponding ε-approximate description gap is 

∆ε(Q2 | Q1; M ) := Kε

 Ans Q2 (M ) Ans Q1 (M ), n .

For interventional answer objects we use the metric 

dInt (I, I ′) := max 



TV( PI , P I′ ), max  

> i∈[n], b ∈{ 0,1}

TV  P do( Xi=b) 

> I

, P do( Xi=b)

> I′



,

where TV denotes total variation distance and PI , P I′ are the observational components of I, I ′.

Theorem 5.10 (Approximate quadratic separation) . Consider the bipartite family Mn 

> bip

with n =2m + 1 . For every fixed ε < 1/4:1. Upper bound. For every G, ∆ε(Int 1 | Obs ; MG) ≤ m2 + O(1) .2. Lower bound (existence). There exists G ⊆ A × B such that 

∆ε(Int 1 | Obs ; MG) ≥ m2 − O(log n).

3. Lower bound (high probability). For uniformly random G ∼ Unif(2 A×B ) and every c ≥ 0,

Pr  ∆ε(Int 1 | Obs ; MG) ≥ m2 − c − O(log n)  ≥ 1 − 2−c.

Proof idea. For the upper bound, an exact m2-bit encoding of G already lets a fixed program output 

Int 1(MG) exactly, hence ε-approximately. For the lower bound, distinct G differ on some edge 

(ai, b j ), and do( Xai =0) shifts the marginal of Xbj by exactly 1/2, so the ε-balls around the 2m2

answer oracles are pairwise disjoint for ε < 1/4; the conditional counting bound applies. Full proof in Appendix B. 

## 6 Counterfactual Separation: The Modular-XOR Construction 

The hierarchy continues: even the full hard-do interventional oracle (the joint specification of all hard atomic do-distributions on endogenous variables) can leave counterfactual queries underdetermined. The construction stacks m copies of the simplest 2-mechanism ambiguity: on a pair (X, Y ), the no-effect mechanism ( X, Y independent uniform) and the XOR mechanism ( X uniform, Y = X ⊕ UY

with independent UY uniform) are both uniform on {0, 1}2 and behave identically under every intervention, but disagree on every counterfactual that fixes the noise: under no-effect Y do( X=0) =

Y do( X=1) on the same UY , while under XOR they always differ. 7Definition 6.1 (Modular-XOR SCM) . Let n = 2 m. For a hidden string s ∈ { 0, 1}m, the SCM Ms

has m independent modules (Xt, Y t) with iid exogenous UXt , U Yt ∼ Bernoulli(1 /2) and 

Xt := UXt , Yt := 

UYt if st = 0 ,Xt ⊕ UYt if st = 1 .

Proofs of all lemmas and theorems in this section are in Appendix C. 

Lemma 6.2 (Observational Equivalence) . For all s ∈ { 0, 1}m, the observational distribution of Ms

is uniform on {0, 1}2m.

Lemma 6.3 (Interventional Equivalence) . For all s ∈ { 0, 1}m, the interventional family Int 1(Ms) is identical. 

Lemma 6.4 (Counterfactual Encoding) . The map s 7 → CF 1(Ms) is injective. 

Lemmas 6.2–6.4 already give a Θ( n) gap conditional on Int 1. The next definition and lemma upgrade 

Int 1 to the full hard-do interventional oracle , so the counterfactual gap survives even complete knowledge of all hard atomic do-distributions. 

Definition 6.5 (All finite interventions) . For an SCM M on variables X1, . . . , X n, define 

Int all (M ) :=  P do( XS =xS )

> M

  

> S⊆[n], x S∈{ 0,1}S

,

the collection of post-interventional distributions under all finite atomic interventions. Clearly 

Int 1(M ) is computable from Int all (M ).

Lemma 6.6 (Modular-XOR is indistinguishable under all interventions) . For all s, s ′ ∈ { 0, 1}m,

Int all (Ms) = Int all (Ms′ ).

Here “full hard-do interventional oracle” means Int all (M ) as in Definition 6.5: all hard atomic do-interventions on endogenous variables; it excludes soft, stochastic, edge, and exogenous interventions. 

Theorem 6.7 (Counterfactual gap after the full hard-do interventional oracle) . Let n = 2 m and 

Mn 

> xor

:= {Ms : s ∈ { 0, 1}m} as in Definition 6.1. Then: 1. DL( Int all ; Ms) = O(1) for all s.2. Upper bound. For every s, DL 3(Ms) ≤ m + O(1) .3. Lower bound. For s ∼ Unif( {0, 1}m) and every c ≥ 0,

Pr[ K(CF 1(Ms) | Int all (Ms), n ) ≥ m − c − O(1)] ≥ 1 − 2−c.

Thus even the full hard-do interventional oracle can leave a Θ( n) counterfactual description gap. Proof idea. By Lemma 6.6, Int all (Ms) does not depend on s, so conditioning on it is free. By Lemma 6.4, the map s 7 → CF 1(Ms) is injective on {0, 1}m, giving 2m distinct counterfactual answer objects. The conditional counting bound then yields the lower bound; an m-bit encoding of s gives the upper bound. Full proof in Appendix C. Since Int 1 is computable from Int all , Theorem 6.7 implies the single-node statement ∆3|2(Ms) = Θ( n) as a corollary. 

## 7 Ambiguity-to-Bits: A General Principle 

The three constructions instantiate a single template: hide a parameter inside the SCM that lower-rung queries cannot resolve but higher-rung queries can; the gap is at least the log of the number of distinct higher-rung answer oracles consistent with the lower-rung answer, and in our constructions this lower bound is matched up to lower-order terms . We formalize this in a Kolmogorov form (worst-case) and a Shannon form (average-case under any prior), stated for arbitrary query classes 

Q1, Q 2.

Definition 7.1 (General ambiguity class) . For query classes Q1, Q 2, a finite SCM family M, and a lower-rung answer a, define 

FQ2|Q1 (a; M) := {Ans Q2 (M ) : M ∈ M , Ans Q1 (M ) = a}.

8Theorem 7.2 (Ambiguity-to-bits, Kolmogorov form) . For any finite family M, query classes Q1, Q 2,and lower-rung answer a,

max     

> M∈M : Ans Q1(M)= a

K Ans Q2 (M ) | a, n  ≥ log 2 |F Q2|Q1 (a; M)| − O(1) .

Moreover, for every c ≥ 0, all but a 2−c fraction of the distinct strings in FQ2|Q1 (a; M) have conditional complexity at least log 2 |F Q2|Q1 (a; M)| − c − O(1) .Proof. The set FQ2|Q1 (a; M) is by construction a set of distinct finite strings. Apply the counting bound (Lemma 3.7) with auxiliary input w = ( a, n ).

Theorem 7.3 (Ambiguity-to-entropy, Shannon form) . Let Θ be a random hidden parameter, MΘ

the corresponding random SCM, and write L(Θ) := Ans Q1 (MΘ) for the lower-rung answer and 

U (Θ) := Ans Q2 (MΘ) for the higher-rung (“upper”) answer. If U is a deterministic injective function of Θ on each level set of L, then H(U (Θ) | L(Θ) = ℓ) = H(Θ | L(Θ) = ℓ), where H

denotes Shannon entropy. In particular, if Θ is uniform over an ambiguity class of size N , this conditional entropy equals log 2 N .Proof. Conditional on L(Θ) = ℓ, the map Θ 7 → U (Θ) is a bijection on the support; bijections preserve Shannon entropy. Instantiating both theorems on the three families gives ambiguity-class sizes |F Int 1|Obs (P ⋆; Mn

> tree

)| =

nn−1, |F Int 1|Obs (P ⋆; Mn

> bip

)| = 2 m2

, and |F CF 1|Int all (I⋆; Mn

> xor

)| = 2 m, yielding lower bounds 

Θ( n log n), Θ( n2), and Θ( n). In each construction these bounds are matched (up to lower-order terms) by an explicit upper-bound encoding (Prüfer sequence, adjacency matrix, hidden bit string), so the gap equals the log of the ambiguity in both Kolmogorov and Shannon senses. The causal description gap is therefore not a Kolmogorov-incompressibility artifact: under natural uniform priors it appears as a Shannon conditional-entropy gap of the same order. 

7.1 Learning-Theoretic Consequence 

Theorem 7.2 translates to a no-free-lunch result for observational learning. 

Corollary 7.4 (No-free-lunch for observational learners) . In the bipartite family Mn 

> bip

all 2m2

mechanisms share the same observational distribution P ⋆. Hence for every sample size N , an observational dataset D ∼ (P ⋆)N satisfies I(G; D) = 0 in the Shannon sense, and any learner that outputs an interventional oracle from D alone satisfies 

Pr  

> G∼Unif(2 A×B)

cInt 1 = Int 1(MG) ≤ 2−m2

,

while any per-query predictor has expected absolute error at least 1/4 (full statements and proofs in Appendix D). The bound is information-theoretic, not computational: no amount of observational data closes the gap. 

## 8 Conclusion 

We turned Pearl’s qualitative hierarchy into a quantitative information hierarchy. Non-identifiability can be large in a precise description-length sense: with constant-bit observations the residual interventional information can be Θ( n2), robust to constant total-variation error and order-optimal for dense finite-gate-schema SCMs; even the full hard-do interventional oracle can leave a Θ( n)

counterfactual gap. The governing quantity throughout is the log of residual higher-rung ambiguity. 

Scope and next steps. Our SCMs are binary, acyclic, adversarial by design, and need not satisfy positivity or faithfulness; this is a deliberate scope showing that observational adequacy alone imposes no small causal-description bound without further structure. How sparsity, positivity/noise, smoothness, or faithfulness shrink the residual ambiguity, the active-intervention complexity of closing the gap, and extensions beyond binary SCMs are natural next steps. 9References 

[1] Elias Bareinboim, Juan D. Correa, Duligur Ibeling, and Thomas Icard. On Pearl’s hierarchy and the foundations of causal inference. In Probabilistic and Causal Inference: The Works of Judea Pearl , pages 507–556. ACM Books, 2022. [2] Julian Dörfler, Benito van der Zander, Markus Bläser, and Maciej Liskiewicz. From probability to counterfactuals: The increasing complexity of satisfiability in Pearl’s causal hierarchy. In 

International Conference on Learning Representations (ICLR) , 2025. [3] Frederick Eberhardt, Clark Glymour, and Richard Scheines. On the number of experiments sufficient and in the worst case necessary to identify all causal relations among N variables. 

Proceedings of the UAI Conference , pages 178–184, 2005. [4] Peter D. Grünwald. The Minimum Description Length Principle . MIT Press, 2007. [5] Alain Hauser and Peter Bühlmann. Characterization and greedy learning of interventional Markov equivalence classes of directed acyclic graphs. Journal of Machine Learning Research ,13:2409–2464, 2012. [6] Duligur Ibeling and Thomas Icard. Comparing causal frameworks: Potential outcomes, struc-tural models, graphs, and abstractions. In Advances in Neural Information Processing Systems ,volume 36, 2023. [7] Zhijing Jin, Yuen Chen, Felix Leeb, Luigi Gresele, Ojasv Kamal, Zhiheng Lyu, Kevin Blin, Fernando Gonzalez Adauto, Max Kleiman-Weiner, Mrinmaya Sachan, and Bernhard Schölkopf. CLadder: Assessing causal reasoning in language models. In Advances in Neural Information Processing Systems , volume 36, 2023. [8] Emre Kıcıman, Robert Osazuwa Ness, Amit Sharma, and Chenhao Tan. Causal reasoning and large language models: Opening a new frontier for causality. Transactions on Machine Learning Research , 2024. [9] Andrey N. Kolmogorov. Three approaches to the quantitative definition of information. Prob-lems of Information Transmission , 1(1):1–7, 1965. [10] Ming Li and Paul Vitányi. An Introduction to Kolmogorov Complexity and Its Applications .Springer, 3rd edition, 2008. [11] Judea Pearl. Causality: Models, Reasoning, and Inference . Cambridge University Press, 2nd edition, 2009. [12] Judea Pearl and Dana Mackenzie. The Book of Why . Basic Books, 2018. [13] Jonas Peters, Dominik Janzing, and Bernhard Schölkopf. Elements of Causal Inference: Foundations and Learning Algorithms . MIT Press, 2017. [14] Karthikeyan Shanmugam, Murat Kocaoglu, Alexandros G. Dimakis, and Sriram Vishwanath. Learning causal graphs with small interventions. In Advances in Neural Information Processing Systems , volume 28, 2015. [15] Ilya Shpitser and Judea Pearl. Complete identification methods for the causal hierarchy. Journal of Machine Learning Research , 9:1941–1979, 2008. [16] Peter Spirtes, Clark Glymour, and Richard Scheines. Causation, Prediction, and Search . MIT Press, 2nd edition, 2000. 

## A Rooted-Tree Construction: Full Details 

This appendix gives the formal construction and full proofs underlying the Θ( n log n) rooted-tree separation. Theorem A.5 below restates and proves the main-text Theorem 4.1; together with Corollary 5.8 it shows the bound is order-optimal in the bounded-indegree regime. 10 Intuition: Hidden Information Flow 

Consider n variables that are always perfectly correlated: either all 0 or all 1, with equal probability. A passive observer sees only this correlation. But how does the correlation arise? Imagine the variables arranged in a tree, with one root variable Xr that is uniformly random, and all other variables copying their parent. The tree structure determines the direction of information flow ,but this structure is invisible to passive observation: all trees produce the same “all equal” distribution. Interventions reveal the hidden structure. If we force Xi = 0 , then: • All descendants of i in the tree become 0 (they copy from ancestors, and i is now 0). • All non-descendants remain random (they don’t depend on i). So interventions reveal the descendant structure, which determines the tree. Since there are nn−1

rooted labeled trees (by Cayley’s formula), there are nn−1 distinct interventional behaviors consistent with the same observations, and distinguishing among them requires log 2(nn−1) = ( n − 1) log n

bits. 

Formal Construction Notation. For a rooted tree T on [n] with root r, we write pa T (v) for the parent of v in T (defined for all v̸ = r), and Desc T (i) for the set of descendants of node i in T , including i itself. That is, 

j ∈ Desc T (i) if and only if i lies on the unique path from j to r.

Definition A.1 (Rooted-Tree SCM) . Let T be a rooted labeled tree on vertex set [n] with root r.Define the SCM MT :• Exogenous variables: Ur ∼ Bernoulli(1 /2) . All other Ui are constant (say, 0) and play no role. • Structural equations: 

Xr := Ur ,Xv := Xpa T (v) for each v̸ = r, 

where pa T (v) denotes the parent of v in the rooted tree T .In words: the root takes a random value in {0, 1}, and every other node copies its parent. Information flows from the root outward along the tree edges. 

Lemma A.2 (All Trees Have the Same Observations) . For every rooted tree T , the observational distribution of MT is: 

P ⋆(x1, . . . , x n) = 



1/2 if x1 = x2 = · · · = xn = 0 ,

1/2 if x1 = x2 = · · · = xn = 1 ,

0 otherwise .

Consequently, DL 1(MT ) = K(P ⋆ | n) = O(1) .Proof. Since T is a connected tree and every non-root node copies its parent, any value assigned to the root propagates to all nodes. Specifically: • If Ur = 0 : Then Xr = 0 . For any node v at distance 1 from r, we have Xv = Xpa T (v) =

Xr = 0 . By induction on distance from r, all nodes have value 0.• If Ur = 1 : By the same argument, all nodes have value 1.Since Ur ∼ Bernoulli(1 /2) , the outcome is 0n with probability 1/2 and 1n with probability 1/2.The distribution P ⋆ can be specified by a constant-length program: “output 1/2 for 0n and 1n, output 

0 otherwise.” Thus K(P ⋆ | n) = O(1) .11 Lemma A.3 (Interventions Reveal Descendants) . For any node i ∈ [n] and the intervention do( Xi =0) :

P do( Xi=0)  

> MT

(Xj = 0) = 

1 if j ∈ Desc T (i),

1/2 if j / ∈ Desc T (i),

where Desc T (i) is the set of descendants of i in T (including i itself). Proof. Under do( Xi = 0) , the equation for Xi becomes Xi := 0 . All other equations remain unchanged. 

Case 1: j ∈ Desc T (i). Every descendant of i lies on a path from i going away from the root. Along this path, each node copies its parent. Since Xi = 0 and descendants copy from ancestors (with i

being an ancestor), we have Xj = 0 deterministically. 

Case 2: j / ∈ Desc T (i). If j is not a descendant of i, then the path from the root r to j does not pass through i. The value of Xj depends only on nodes along this path, which are unaffected by the intervention on Xi. Thus Xj still equals Xr = Ur ∼ Bernoulli(1 /2) , so P (Xj = 0) = 1 /2.

Lemma A.4 (Descendant Sets Determine the Tree) . The collection of descendant sets {Desc T (i) : 

i ∈ [n]} uniquely determines the rooted tree T .Proof. We show how to reconstruct T from the descendant sets. 

Finding the root: The root r is the unique node with Desc T (r) = [ n] (every node is a descendant of the root). 

Finding parent-child relationships: For any non-root node v, its parent pa T (v) is characterized as follows. Consider all nodes u̸ = v such that v ∈ Desc T (u) (i.e., u is an ancestor of v). Among these, 

pa T (v) is the one with the smallest descendant set. To see why: along the unique path from v to the root r, descendant sets strictly increase: 

Desc T (v) ⊊ Desc T (pa T (v)) ⊊ Desc T (pa T (pa T (v))) ⊊ · · · ⊊ Desc T (r) = [ n].

The strict inclusions hold because each step toward the root adds at least the current node to the descendant set. Thus the parent has the smallest descendant set among all ancestors. This procedure reconstructs all parent-child edges, hence the entire tree. 

Theorem A.5 (Rooted-Tree Separation: Θ( n log n)). Let Mn 

> tree

:= {MT :

T is a rooted labeled tree on [n]}. Then: 1. The map T 7 → Int 1(MT ) is injective on Mn

> tree

.2. Lower bound: There exists T with ∆2|1(MT ) ≥ (n − 1) log 2 n − O(log n).3. Upper bound: For all T , DL 2(MT ) ≤ (n − 1) log 2 n + O(log n).4. High probability: For uniformly random rooted labeled tree T ,

P∆2|1(MT ) ≥ (n − 1) log 2 n − c − O(log n) ≥ 1 − 2−c.

Consequently, ∆2|1(MT ) = Θ( n log n) for all but an exponentially small fraction of trees. Proof. Part 1 (Injectivity): By Lemma A.3, from Int 1(MT ) we can read off: 

Desc T (i) = {j ∈ [n] : P do( Xi=0)  

> MT

(Xj = 0) = 1 }

for each i. By Lemma A.4, the descendant sets determine T . Thus different trees yield different interventional families. 

Part 2 (Lower bound): By Cayley’s formula, there are nn−1 rooted labeled trees on [n]. (Cayley’s formula gives nn−2 labeled unrooted trees on [n]; choosing a root from n vertices gives nn−1 rooted labeled trees.) By injectivity, there are nn−1 distinct elements in {Int 1(MT ) : T ∈ M n

> tree

}.12 r                    

> a1a2a3
> b1b2b3
> Observations:
> All variables equal.
> P(0 n) = P(1 n) = 1 /2DL 1(MG) = O(1)
> Intervention do( a1= 0) :
> b1, b 2→0(edges from a1)
> b3→random (no edge) Reveals NG(a1) = {b1, b 2}
> Red edges = hidden graph G(revealed by interventions)

Figure 1: The Bipartite-Graph Construction. The root r feeds into layer A (copy), and layer 

A feeds into layer B via AND gates controlled by graph G (red edges). Observationally, all 2m2

choices of G produce the same distribution. Interventions on layer A reveal which layer-B nodes are neighbors. By the counting bound for Kolmogorov complexity: among N distinct strings, at least one has complexity ≥ log 2 N − O(1) . Applying this with N = nn−1:

max  

> T

K(Int 1(MT ) | n) ≥ log 2(nn−1) − O(1) = ( n − 1) log 2 n − O(1) .

By Lemma A.2, DL 1(MT ) = O(1) for all T . By Lemma 3.10, ∆2|1(MT ) = DL 2(MT ) ± O(log n).

Part 3 (Upper bound): Given tree T , we can encode it using a Prüfer sequence: a sequence of 

n − 2 labels from [n], requiring (n − 2) log 2 n bits, plus O(log n) bits to specify the root. Total: 

(n − 1) log 2 n + O(log n) bits. A constant-length program can then simulate MT and output Int 1(MT ). Thus K(Int 1(MT ) | n) ≤

(n − 1) log 2 n + O(log n).

Part 4 (High probability): By part (2) of the counting bound (Lemma 3.7), among the nn−1 distinct strings {Int 1(MT )}, all but a 2−c fraction satisfy K(Int 1(MT ) | n) ≥ (n − 1) log 2 n − c − O(1) .Transferring to ∆2|1 via Lemma 3.10 incurs an additional O(log n) term. 

## B Bipartite-Graph Construction: Figures and Full Proofs 

This appendix supports Section 5 of the main paper. Figure 1 illustrates Definition 5.1; Figure 2 compares the resulting bound to the rooted-tree warm-up. We then restate and prove, in order, Lemmas 5.2–5.4, Theorem 5.5 (quadratic separation), Theorem 5.7 (degree-sensitive upper bound), and Theorem 5.10 (finite-precision robustness). 

Proof of Lemma 5.2. We analyze the two cases for the root’s value. 13 5 10 15 20 25 30 

0

50 

100 

150 

200 

250 

System Size n

> Causal Description Gap (bits)

DL 1 (Observations) = O(1) 

∆2|1 (Trees) = Θ( n log n)

∆2|1 (Bipartite) = Θ( n2)

Figure 2: The Causal Description Gap grows quadratically. Observational description length (blue) is constant. The tree construction (orange) achieves Θ( n log n). The bipartite construction (red) achieves Θ( n2), a quadratic gap between knowing “what happens” and knowing “why.” 

Case Ur = 0 : Then Xr = 0 . Since each Xai = Xr , we have Xai = 0 for all i. For each bj , the equation is: 

Xbj = Xr ∧ ^ 

> ai:( ai,b j)∈G

Xai = 0 ∧ (· · · ) = 0 .

Thus all variables are 0.

Case Ur = 1 : Then Xr = 1 and Xai = 1 for all i. For each bj :

Xbj = Xr ∧ ^ 

> ai:( ai,b j)∈G

Xai = 1 ∧ 1 ∧ · · · ∧ 1 = 1 .

(If bj has no neighbors, the empty AND is 1, so Xbj = 1 ∧ 1 = 1 .) Thus all variables are 1.Since Ur ∼ Bernoulli(1 /2) , the outcome is 0n or 1n each with probability 1/2. This is P ⋆,independent of G.

Proof of Lemma 5.3. Under do( Xai = 0) , the equation for Xai becomes Xai := 0 . The root 

Xr = Ur remains random, and all other layer-A nodes satisfy Xak = Xr for k̸ = i.

Case (ai, b j ) ∈ G: The equation for Xbj includes Xai as an AND input: 

Xbj = Xr ∧ Xai ∧ ^   

> ak̸=ai:( ak,b j)∈G

Xak .

Since Xai = 0 , the AND evaluates to 0 regardless of other inputs. Thus Xbj = 0 deterministically, so P (Xbj = 0) = 1 .

Case (ai, b j ) /∈ G: The equation for Xbj does not include Xai :

Xbj = Xr ∧ ^   

> ak:( ak,b j)∈G

Xak .

All terms in this AND equal Xr (since Xak = Xr for k̸ = i). If there are d such terms: 

Xbj = Xr ∧ Xr ∧ · · · ∧ Xr = Xr .

(If bj has no neighbors other than possibly ai, and (ai, b j ) /∈ G, then the AND is either empty (giving 

Xbj = Xr ) or involves only other ak that equal Xr .) Thus Xbj = Xr ∼ Bernoulli(1 /2) , so P (Xbj = 0) = 1 /2.14 Proof of Lemma 5.4. By Lemma 5.3, from Int 1(MG) we can extract: 

NG(ai) = {bj ∈ B : P do( Xai =0)  

> MG

(Xbj = 0) = 1 }

for each ai ∈ A. The neighborhoods {NG(ai)}mi=1 determine all edges of G:

(ai, b j ) ∈ G ⇐⇒ bj ∈ NG(ai).

Thus Int 1(MG) determines G uniquely. 

Proof of Theorem 5.5. Lower bound: There are 2m2

bipartite graphs G ⊆ A × B (each of the m2

potential edges is present or absent). By Lemma 5.4, there are 2m2

distinct interventional families. By Lemma 5.2, all share Obs (MG) = P ⋆ with DL 1 = O(1) .By the counting bound: among 2m2

distinct strings, at least one has Kolmogorov complexity 

≥ m2 − O(1) . By Lemma 3.10, ∆2|1(MG) = DL 2(MG) ± O(log n).The high-probability statement follows from the stronger counting bound: all but a 2−c fraction have complexity ≥ m2 − c − O(1) .

Upper bound: The graph G can be encoded as an m × m binary adjacency matrix, requiring 

m2 bits. Given G and n, a constant-length program simulates MG and outputs Int 1(MG). Thus 

K(Int 1(MG) | n) ≤ m2 + O(1) .

Proof of Theorem 5.7. An SCM in Mn,d (Γ , Π) can be encoded by listing: 1. a topological ordering of the n variables, using O(n log n) bits; 2. for each variable, its parent set, chosen from at most Pdk=0 

 n−1

> k

 possibilities; 3. for each variable, a gate from the fixed library Γ and a noise distribution from the fixed library Π, using O(1) bits per variable. The entire SCM thus has a description of length 

O(n log n) + n log 2

 dX

> k=0

n − 1

k



+ OΓ,Π(n).

Given this description and n, a fixed program can compute all single-node interventional distributions and output Int 1(M ). Hence the same expression upper-bounds K(Int 1(M ) | n) = DL 2(M ).Finally, dX

> k=0

n − 1

k



≤ (d + 1) 

 e(n − 1) 

d

d

for 1 ≤ d ≤ n − 1, yielding the stated asymptotic form. Since ∆2|1(M ) ≤ DL 2(M ) + O(1) , the same upper bound applies to the gap. 

Proof of Theorem 5.10. Upper bound. An exact m2-bit encoding of G as an m×m binary adjacency matrix lets a fixed program output Int 1(MG) exactly, hence also ε-approximately for any ε ≥ 0. So 

Kε(Int 1(MG) | Obs (MG), n ) ≤ m2 + O(1) .

Lower bound. Let G̸ = G′ be two distinct bipartite graphs. They differ on some edge (ai, b j );WLOG (ai, b j ) ∈ G and (ai, b j ) /∈ G′. By Lemma 5.3, 

P do( Xai =0)  

> MG

(Xbj = 0) = 1 , P do( Xai =0)  

> MG′

(Xbj = 0) = 1 /2.

Total variation distance does not increase under marginalization, so the full interventional distributions satisfy 

TV  P do( Xai =0)  

> MG

, P do( Xai =0) 

> MG′

 ≥ 12 .

Hence dInt (Int 1(MG), Int 1(MG′ )) ≥ 1/2. For ε < 1/4, the open ε-balls around the 2m2

answer objects are pairwise disjoint, so any ε-accurate description must still distinguish among 2m2

possibil-ities. Applying the conditional counting bound (Lemma 3.7, part 2) with the common observation 

P ⋆ as side information yields the existence and high-probability lower bounds. 15 C Modular-XOR Construction: Full Proofs 

This appendix supports Section 6. We prove, in order, Lemmas 6.2 (observational equivalence), 6.3 (single-node interventional equivalence), 6.4 (counterfactual encoding is injective in s), 6.6 (indistinguishability under all atomic interventions), and Theorem 6.7 (the Θ( n) counterfactual gap conditional on Int all ). 

Proof of Lemma 6.2. We show each module (Xt, Y t) is uniformly distributed on {0, 1}2.

Case st = 0 : Xt = UXt and Yt = UYt are independent Bernoulli(1 /2) variables. 

Case st = 1 : Xt = UXt ∼ Bernoulli(1 /2) . For Yt = Xt ⊕ UYt : for any fixed x ∈ { 0, 1},

P(Yt = y | Xt = x) = P(UYt = x ⊕ y) = 1 /2.

Thus Yt is uniform and independent of Xt. (Formally: P(Xt = x, Y t = y) = P(Xt = x)P(Yt =

y) = 1 /4.) Since modules are independent and each is uniform on {0, 1}2, the full distribution is uniform on 

{0, 1}2m.

Proof of Lemma 6.3. Consider any single-variable intervention do( Xt = x) or do( Yt = y).

Intervention do( Xt = x): The equation Xt := x replaces whatever Xt was. In module t:• If st = 0 : Yt = UYt , uniform. • If st = 1 : Yt = x ⊕ UYt , also uniform (since UYt is uniform). Either way, Yt is uniform and independent of the intervention value. Other modules are unaffected. 

Intervention do( Yt = y): The equation Yt := y replaces whatever Yt was. In both cases ( st = 0 or 

1), Xt = UXt remains uniform. Other modules unaffected. Since all single-variable interventional distributions are the same across all s, the entire family 

Int 1(Ms) is independent of s.

Proof of Lemma 6.4. For each module t, consider the counterfactual query: “What is P(Y (Xt←0)  

> t

=

Y (Xt←1)  

> t

)?” Let Y (Xt←b) 

> t

denote the value of Yt when we intervene with do( Xt = b), evaluated on the same noise UYt .

Case st = 0 : Y (Xt←0)  

> t

= UYt and Y (Xt←1)  

> t

= UYt . These are identical, so P(Y (Xt←0)  

> t

=

Y (Xt←1)  

> t

) = 1 .

Case st = 1 : Y (Xt←0)  

> t

= 0 ⊕ UYt = UYt and Y (Xt←1)  

> t

= 1 ⊕ UYt = 1 − UYt . These are always different, so P(Y (Xt←0)  

> t

= Y (Xt←1)  

> t

) = 0 .Thus the counterfactual query for module t outputs 1 if st = 0 and 0 if st = 1 . The m queries together determine s.Since CF 1(Ms) contains P (X, X (t←0) , X (t←1) ) for all t, which includes the joint distribution of 

Y (Xt←0)  

> t

and Y (Xt←1)  

> t

, it determines s.

Proof of Lemma 6.6. It suffices to consider a single module (Xt, Y t), since modules are independent and interventions factor across modules. Fix an arbitrary intervention on any subset of {Xt, Y t}.If Yt is intervened on, then Yt is fixed by the intervention. The remaining variable Xt, if not intervened on, equals UXt and is uniform in both the no-effect and XOR mechanisms. If Yt is not intervened on but Xt is set to x, then 

Yt =

UYt , st = 0 ,x ⊕ UYt , st = 1 .

16 In either case Yt is uniform Bernoulli and independent of all other modules. Finally, if neither variable is intervened on, then for st = 0 we have (Xt, Y t) = ( UXt , U Yt ), uniform on {0, 1}2; for st = 1 we have (Xt, Y t) = ( UXt , U Xt ⊕ UYt ), also uniform on {0, 1}2.Thus every possible intervention produces the same distribution regardless of st. Taking the product over independent modules proves the claim. 

Proof of Theorem 6.7. Part 1. By Lemma 6.6, Int all (Ms) is identical for all s. Its description is constant: every intervention fixes the intervened variables and leaves each non-intervened module uniformly distributed in the manner described in the proof of Lemma 6.6. 

Part 2 (Upper bound). Given s (m bits) and n, a constant-length program simulates Ms and outputs 

CF 1(Ms). Thus K(CF 1(Ms) | n) ≤ m + O(1) .

Part 3 (Lower bound). By Lemma 6.4, the map s 7 → CF 1(Ms) is injective. Hence there are 2m

distinct counterfactual answer objects consistent with the same full hard-do interventional oracle. The conditional counting bound (Lemma 3.7, part 2), applied with w = ( Int all (Ms), n ), yields the high-probability claim, since Int all (Ms) is constant across s and contributes only O(1) to the conditioning. 

## D Learning-Theoretic Consequences 

This appendix supports the brief discussion of §7.1 of the main paper. We give two no-free-lunch results for observational learners drawn from the bipartite family Mn 

> bip

(Definition 5.1): one bounds the joint-recovery success rate, the other a per-query absolute error. 

Corollary D.1 (No-free-lunch for observational learners) . Let G ∼ Unif(2 A×B ) in the bipartite family Mn

> bip

. Any learner A that receives an i.i.d. sample of any size from Obs (MG) and outputs a prediction cInt 1 satisfies 

P[cInt 1 = Int 1(MG)] ≤ 2−m2

.

Proof. All MG share the observational distribution P ⋆, so any sample from P ⋆ is statistically independent of G. The learner’s output, a function of the sample, is therefore independent of G.With 2m2

equiprobable targets {Int 1(MG)} (the map G 7 → Int 1(MG) is injective by Lemma 5.4) and output independent of the true target, the success probability is at most 2−m2

.

Corollary D.2 (Per-query prediction error) . Let G ∼ Unif(2 A×B ) in the bipartite family Mn

> bip

.Let ˆp be any predictor (possibly depending on an i.i.d. sample of any size from Obs (MG)). For an independent uniformly random pair (i, j ) ∈ [m] × [m], let pi,j (G) := P do( Xai =0)  

> MG

(Xbj = 0) . Then 

E|ˆpi,j − pi,j (G)| ≥ 14 .

Proof. By Lemma 5.3, pi,j (G) ∈ {1/2, 1} depending on whether (ai, b j ) ∈ G. For G ∼

Unif(2 A×B ) the edge indicator is Bernoulli (1 /2) , so pi,j (G) equals 1 and 1/2 each with prob-ability 1/2. The observational sample is independent of G, hence so is the predictor output ˆpi,j .Condition on ˆpi,j = a:

E|ˆpi,j − pi,j (G)| ˆpi,j = a = 12 |a − 1| + 12 |a − 12 | ≥ 14 ,

where the last step uses the triangle inequality |a − 1| + |a − 12 | ≥ | 1 − 12 | = 12 . Averaging over a

proves the claim. This is not a computational limitation; it is information-theoretic. No algorithm, however powerful, can reliably predict interventional outcomes from observations when the underlying mechanism is drawn from our family. 

Remark D.3 (Shannon Mutual Information is Zero) . In the bipartite family Mn

> bip

, a dataset D ∼

(P ⋆)N of N i.i.d. observational samples is independent of the hidden graph G, since all mechanisms share the same observational distribution. Therefore I(G; D) = 0 in the Shannon sense for every sample size N . The lower bounds in Corollary D.1 and Corollary D.2 follow from this information-theoretic independence and do not rely on uncomputability of Kolmogorov complexity. 17