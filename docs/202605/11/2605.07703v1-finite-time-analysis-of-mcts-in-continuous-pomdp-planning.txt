Title: Finite-Time Analysis of MCTS in Continuous POMDP Planning

URL Source: https://arxiv.org/pdf/2605.07703v1

Published Time: Mon, 11 May 2026 01:12:15 GMT

Number of Pages: 17

Markdown Content:
# Finite-Time Analysis of MCTS in Continuous POMDP Planning 

Da Kong 

Technion Autonomous Systems Program (TASP) Technion – Israel Institute of Technology 

Vadim Indelman 

Stephen B. Klein Faculty of Aerospace Engineering Faculty of Data and Decision Sciences Technion – Israel Institute of Technology 

## Abstract 

This paper presents a finite-time analysis for Monte Carlo Tree Search (MCTS) in Partially Observable Markov Decision Processes (POMDPs), with probabilistic concentration bounds in both discrete and continuous observation spaces. While MCTS-style solvers such as POMCP achieve empirical success in many appli-cations, rigorous finite-time guarantees remain an open problem due to the non-stationarity and the interdependencies induced by heuristic action selection (e.g., UCB). In the discrete setting, we address these challenges by extending the polyno-mial exploration bonus to UCB in POMDP setting, yielding polynomial concen-tration bounds for the empirical value estimation at the root node. For continuous observation spaces, we introduce an abstract partitioning framework and propose a finite-time bound on partitioning loss. Under mild conditions, we prove high-probability bound on value estimates in POMDPs with continuous observation space. Specifically, we propose Voro-POMCPOW, a variant of POMCPOW with finite-time guarantees that adaptively partitions the continuous observation space using Voronoi cells. This approach maintains a finite branching factor while pre-serving the original observation generator. Empirical validation demonstrates that the proposed Voro-POMCPOW shows competitive performance while providing theoretical guarantees. Although our analysis focuses on continuous POMDPs, the techniques developed herein are also applicable to continuous MDPs, closing another gap on the MDP side. 

## 1 Introduction 

Partially Observable Markov Decision Processes (POMDPs) provide a prime mathematical frame-work for sequential decision-making under uncertainty, which models all kinds of uncertainty into the observation model and transition model. While the exact solution of POMDPs is computa-tionally intractable due to the curse of dimensionality and the curse of history, online anytime algorithms have emerged as efficient and powerful approximate methods that scale to large problems. Specifically, Monte Carlo Tree Search (MCTS) demonstrates strong empirical success in POMDP planning, notably POMCP [Silver and Veness, 2010], in domains ranging from game playing to robotics [Kurniawati, 2022, Lauri et al., 2022]. Despite their widespread practical success, the theoretical analysis of MCTS in POMDPs—particularly the finite-time performance guarantees—remains under-explored. To our knowledge, the most up-to-date existing theoretical analysis provides asymptotic convergence guarantees, like in POMCP [Silver and Veness, 2010], which requires the simulation count to grow without bound but comes with no finite-time guarantees. For many safety-critical applications, such as autonomous driving or robot manipulation, finite-time guarantees are essential. A finite-time analysis of MCTS is challenging due to the non-stationary Multi-Armed Bandit (MAB), and 

> Preprint.
> arXiv:2605.07703v1 [cs.AI] 8 May 2026

interdependencies among samples. The recent finite-time analysis of MCTS [Shah et al., 2022] is limited to MDPs with a finite branching factor. In the context of POMDPs, MCTS-style solvers with finite-time guarantees are limited. As far as we know, the only existing work is by Barenboim and Indelman [2026], which provides deterministic anytime bounds for discrete POMDPs. However, probabilistic finite-time guarantees for MCTS in POMDPs are still missing. Moreover, the deterministic bound from [Barenboim and Indelman, 2026] does not extend to continuous observation spaces, which are prevalent in real-world applications. A finite-time analysis for continuous spaces in both MDPs and POMDPs is an open problem. This work bridges the aforementioned gaps by providing the first finite-time analysis of MCTS in POMDPs with probabilistic guarantees, in both discrete and continuous observation spaces. We first extend the polynomial exploration bonus of UCB [Shah et al., 2022] to the discrete POMDP setting, deriving polynomial concentration bounds for the empirical value estimate at the root node. Building on this, we propose the Corrected-POMCP solver, which incorporates the corrected UCB bonus and maintains finite-time guarantees. Next, we extend the analysis to continuous observation spaces. We introduce an adaptive partitioning framework to discretize the continuous observation space, thereby inducing a finite branching factor. Under mild assumptions and requirements for the partitioning scheme, we derive a probabilistic bound for MCTS value estimates in continuous POMDPs by applying a union bound over the partition-induced error and the MCTS estimation error. As a specific instance, we employ Voronoi partitioning and propose Voro-POMCPOW solver, an extension of POMCPOW with finite-time guarantees. Experimental results demonstrate competitive performance of both of the proposed solvers. Although our primary focus is on continuous POMDPs, the techniques developed are directly applicable to continuous MDPs, thereby also contributing to the finite-time analysis of MCTS in fully observable continuous domains. Our contributions are summarized as follows: • We present a finite-time analysis for MCTS in discrete POMDP planning with probabilistic guarantees, which can be applied to a corrected variant of POMCP. • We present the first finite-time analysis to continuous observation and state spaces for POMDPs by introducing an abstract partitioning scheme. This methodology is also applica-ble to continuous MDPs. • We propose solvers with formal finite-time guarantees in discrete and continuous POMDPs, namely Corrected-POMCP and Voro-POMCPOW, and validate their performance. 

## 2 Background 

POMDPs constitute a foundational framework for sequential decision-making under uncertainty. For a comprehensive background on POMDPs, we refer to surveys [Kurniawati, 2022, Lauri et al., 2022]. This paper focuses on the finite-time analysis of MCTS in the context of POMDPs. For a general overview of MCTS, we refer to the survey [ ´ Swiechowski et al., 2023]. In the fully observable MDP setting, Shah et al. [2022] provide a finite-time analysis with probabilistic bounds for MCTS in finite-branching MDPs. Dam et al. [2024] derive finite-time bounds for another variant of MCTS with a power-mean backup, later extended to continuous MDPs [Dam, 2025]. In contrast, finite-time guarantees for MCTS in POMDPs remain largely open. To our knowledge, the only existing result is the deterministic anytime bound for discrete POMDPs by Barenboim and Indelman [2026]. This work aims to bridge this gap by developing a finite-time analysis of MCTS for continuous POMDPs. Beyond finite-time analysis, the convergence rate of MCTS is of particular interest for anytime solvers, as it directly helps set the computational budget. In MDPs, Chang [2025] provides a convergence rate analysis of MCTS with different heuristic functions under probabilistic bounds. This paper also aims to contribute to the convergence rate analysis of MCTS in POMDPs, with clear benefits for the design of POMDP simplification strategies (e.g., [Kong and Indelman, 2024]) in anytime planning. A further key challenge addressed in this work is to handle continuous spaces while preserving performance guarantees. Progressive widening, employed in solvers such as POMCPOW [Sunberg 2and Kochenderfer, 2018], is a common technique for exploring in continuous spaces. Alternative geometric approaches include Voronoi partitioning: Lim et al. [2021] extend Optimistic Optimiza-tion [Munos, 2011] to Voronoi Optimistic Optimization (VOO) for continuous action selection, and Hoerger et al. [2024] construct a Voronoi-partitioned belief tree for continuous POMDPs. How-ever, these methods do not come with formal guarantees. MCTS POMDP approaches that support continuous observation spaces while maintaining performance guarantees remain an open problem. 

## 3 Preliminary 

3.1 Partially Observable Markov Decision Processes 

The basic model of Partially Observable Markov Decision Processes (POMDP) are defined as a tuple: 

⟨X , A, Z, PT , PZ , r, γ, b 0⟩, where b0 is the initial belief, X is the state space, A is the action space, 

Z is the observation space. The transition model (or motion model) is defined as PT (xk+1 |xk, a k),which describes the probabilistic transition of the state from xk ∈ X to xk+1 ∈ X under a certain action ak ∈ A . The observation model is defined as PZ (zk|xk), which describes the probability of observation zk ∈ Z given a certain state xk ∈ X . The reward function is considered to be state-dependent, r : X , A 7 → R, and satisfies the bounded reward assumption: r ∈ [−Rmax , R max ].Given that the true state is uncertain, a belief is maintained to represent the distribution of the current state with regard to history. The belief at time step k is defined as bk ≜ P(xk|hk), where the history hk

is defined as hk ≜ {z1: k, a 0: k−1}. We define the corresponding history space as Hk ≜ {hk}, which includes all possible history hk. A propagated history without the latest observation h− 

> k

is defined as: 

h− 

> k

= {z1: k−1, a 0: k−1}, and the corresponding propagated belief is b− 

> k

≜ P(xk|h− 

> k

). The Bayesian belief update is given by: bk = P (xk|hk) = ηk P (zk | xk)P (xk | h− 

> k

) = ηk P (zk | xk)b− 

> k

, where 

ηk is the Bayesian factor. We define the Bayesian belief update as the operator bk+1 = ψ(bk, a k, z k).The value function for a deterministic policy π over the planning horizon L is defined as: V π (bk) = 

r(bk, π k(bk)) + Pk+Li=k+1 γi−k E 

> zk+1: i|bk,π

r(bi, π i(bi)) . Here, for state-dependent rewards, r(b, a ) = 

E

> x∼b

[r(x, a )] , and π = ( π0, . . . , π L) with πi : Hi 7 → A , where Hi is the history space at time step i.In this paper, we use the history and belief space interchangeably. The corresponding Q-function is 

Qπ (bk, a k) = r(bk, a k) + E 

> zk+1 |bk,a k

V π (bk+1 ).The goal of a POMDP is to find the optimal policy sequence π∗ = ( π∗ 

> 0

, . . . π ∗

> L

) that maximizes the value function: π∗ = argmax π V π (bk). The optimal value function satisfies the Bellman optimality equation: V ∗(bk) = max ak

h

r(bk, a k)+ E 

> zk+1 |bk,a k

V ∗(bk+1 )i

. While the beliefs in the formulation above are theoretical, in practice these are often approximately represented by particle beliefs. Unless otherwise mentioned, we also consider this setting throughout the paper. Extending our results to the level of theoretical beliefs is possible, following a similar methodology as in [Lim et al., 2023], but outside the scope of this work. 

3.2 MCTS in POMDP Planning 

Monte Carlo Tree Search (MCTS) is a sampling-based, anytime planning algorithm that incrementally constructs a search tree under a given computational budget. When time budget permits, the algorithm performs successive iterations, each consisting of four stages: (i) selection , which traverses the tree from the root by recursively choosing child nodes according to a tree policy that balances exploration and exploitation, typically the Upper Confidence Bound (UCB) rule; (ii) expansion , which adds one or more unvisited nodes upon reaching a leaf; (iii) simulation , which generates a trajectory from the newly expanded node to a finite planning horizon using the default or rollout policy; and (iv) backup ,which propagates the simulated return backward along the visited trajectory to update value estimates and visitation counts. In an MDP setting, MCTS operates directly on state–action nodes, with the root corresponding to the initial state. In a POMDP setting, since the true state is unknown, planning must be performed over 

beliefs derived from action–observation histories. A direct adaptation of MCTS to POMDPs is to 3represent the tree by belief nodes, each associated with a history, and use the given generative model of the environment to sample trajectories, thereby constructing a belief tree. A notable MCTS-based POMDP solver is POMCP [Silver and Veness, 2010], which employs a UCT-style tree search. Each simulation begins by sampling a particle from the initial belief, then selects actions according to the UCT criterion and samples subsequent states and observations from the generative model. Rather than maintaining an explicit belief representation, POMCP approximates the belief at each history node using a particle set derived from the simulated trajectories. The tree is indexed by action–observation histories, with each node storing visitation counts and estimated values. During backup, these statistics are updated using the simulated return. MCTS incrementally constructs an estimator of a stochastic policy sequence π, where for each time step i, πi : Hi × A i 7 → [0 , 1] for a discrete setting. The estimated stochastic policy in MCTS is given by ˆπ(a|b) = N (b,a )  

> N(b)

, where N (b) is the visitation count of node b, and N (b, a ) is the visitation count of node (b, a ).Applying MCTS-style solvers in continuous POMDPs shows a significant challenge, primarily due to the infinite branching factor in action and observation spaces. Several notable solvers have been developed to work in continuous spaces, including POMCPOW [Sunberg and Kochenderfer, 2018], VOMCPOW [Lim et al., 2021], and ADVT [Hoerger et al., 2024]. These methods typically adopt a progressive widening strategy to selectively expand nodes in continuous spaces, thereby rendering the search tractable. However, existing methods lack dedicated mechanisms that can provide performance guarantees in continuous observation spaces. In MCTS, the value estimation is derived from the average of the sampled returns. Let Gi(bk) denote the return sample obtained from the ith iteration at the belief node bk. The empirical value estimate after N simulations at bk is given by: ˆVN (bk) = 1

> N

PNi=1 Gi(bk).

## 4 Finite-time Analysis in Discrete POMDP 

This section serves as a warm-up examination of the finite-time analysis of MCTS-style solvers in discrete POMDPs, with a particular focus on POMCP [Silver and Veness, 2010]. We first identify two principal challenges in the theoretical analysis: the non-stationary nature and the interdependencies inherent in the MCTS tree structure. The non-stationarity arises from the anytime simulation process: as iterations progress, the stochastic policy used for the value estimation shifts continuously. The interdependencies stem from heuristic-based action selection mechanisms, such as UCB1, which depend on historical selections and accumulated rewards. This dependency violates the i.i.d. assumption required by Hoeffding’s inequality, thereby complicating the analysis. For UCB-based methods specifically, an additional obstacle to establishing finite-time bounds— particularly probabilistic bounds—is the logarithmic term in the UCB function employed during action selection. As noted in [Audibert and Bubeck, 2009], UCB cannot achieve exponentially decaying bounds; only polynomial bounds are attainable. By extending the corrected UCB formulation proposed in [Shah et al., 2022] to the POMDP setting, this section presents a finite-time analysis of MCTS for discrete POMDP planning, establishing a probabilistic concentration bound in discrete settings. 

4.1 Corrected-POMCP 

We propose to replace the standard logarithmic exploration bonus of UCB1 in POMCP by a polyno-mial bonus form [Shah et al., 2022]. Concretely, for any tree node with history h and action a, the action selection process is conducted by: 

at ∈ arg max 

> a∈A (h)

n ˆQ(h, a ) + Bt,s (h, a )

o

, (1) where ˆQ(h, a ) is the empirical return estimate and the polynomial exploration bonus is 

Bt,s (h, a ) ≜ β1/ξ N (h)α/ξ N (h, a )−(1 −η). (2) Here N (·) is the visitation count, β > 1, ξ > 0 and η ∈ [1 /2, 1) are concentration parameters, and α

is a tuning exponent. 4By plugging the corrected UCB bonus into POMCP, we obtain the Corrected-POMCP solver. The solver largely follows POMCP’s process except for the action selection process. The detailed algorithm is presented in the Appendix as Algorithm 1. 

4.2 Theoretical Analysis 

This part of the theoretical analysis for discrete POMDPs largely follows the structure established for finite-branching-factor MDPs by Shah et al. [2022] (with their extension to stochastic models in the Appendix), extending it to a discrete POMDP setting. We begin by stating the assumptions underlying our analysis of discrete POMDPs. 

Assumption 4.1. The reward function r(b, a ) is bounded such that |r(b, a )| ≤ Rmax .

Assumption 4.2 (Observation support and branching factor) . There exists ϕZ > 0 such that for every reachable tree node (history or belief) x and action a:i. The observation distribution PZ (· | x, a ) has finite support; ii. For any observation with positive probability, 

PZ (z | x, a ) ≥ ϕZ , ∀z such that PZ (z | x, a ) > 0. (3) 

Consequently, each state-action pair (x, a ) admits at most MZ ≜ ϕ−1

> Z

 observation children in the belief tree. 

Building upon these assumptions, we now establish the notation and framework for the finite-time analysis. For each action a ∈ A , let {Ga,t }t≥1 denote the sequence of return samples generated by MCTS when the simulation at bk initiates with action a at the first decision step and subsequently follows the simulation/rollout policy. For n ≥ 1, we define the following quantities: ¯Ga,n ≜ 1

> n

Pnt=1 Ga,t , μa,n ≜ E[ ¯Ga,n ], and Qa ≜

lim n→∞ μa,n . Furthermore, we define: V ∗(b) ≜ max a∈A Q(b, a ), a ∗ = arg max a∈A Q(b, a ), and 

∆min ≜ min a̸=a∗ (V ∗ − Qa), δ ∗,n ≜ μa∗,n − V ∗.

At the t-th simulation from bk, let At ∈ A denote the action selection at bk, and let Na(t) ≜

Ptℓ=1 I{Aℓ = a} represent the cumulative number of times action a has been selected up to simulation t. We assume that the action selection process adheres to the corrected-UCB rule defined in (1). 

Theorem 1 (Polynomial Concentration of MCTS in Discrete POMDPs) . Let Assumptions 4.1 and 4.2 hold. Consider a POMDP belief tree of depth L, where the root b0 is at depth h = 0 and leaf nodes are at depth h = L. Let η(h) ∈ [1 /2, 1) be a fixed constant. Define the sequences of parameters 

{ξ(h)}Lh=0 and {β(h)}Lh=1 recursively updated from the leaves to the root (as shown in Appendix C.) Suppose the MCTS solver selects actions at any node h (at depth h < L ) using the Corrected-UCB index with level-dependent parameters (β(h+1) , ξ (h+1) , η (h+1) ) from the child level. Then, there exist constants β(0) > 1 (depending on Vmax , |A| , L, ∆min , M Z , ϕ Z ) such that for the root value estimate ˆVn(b0) after n simulations, the following concentration bounds hold for all n ≥ 1

and z ≥ 1:

P



n

 ˆVn(b0) − V ∗(b0)



≥ nη(0) 

z



≤ β(0) z−ξ(0) 

, (4) 

P



n

 ˆVn(b0) − V ∗(b0)



≤ − nη(0) 

z



≤ β(0) z−ξ(0) 

. (5) 

Proof sketch. The proof largely follows Shah et al. [2022] with their extension to stochastic models in the Appendix (Lemma A.1). Here we replace the state mixture process in MDP with observation mixture in POMDP. The proof is in Appendix C, with a closed-form parameter design in Appendix A.3. Theorem 1 provides a finite-time analysis of the empirical value estimate ˆVn(bk) of MCTS-style solvers in a discrete setting, specifically for the Corrected-POMCP. The theorem establishes both 5convergence and polynomial concentration bounds for the empirical value estimate. The polynomial concentration result provides high-probability bounds on the deviation of the empirical value estimate from the true value, with the deviation shrinking polynomially in the number of samples n. The bounds are given in terms of the parameters β(0) , ξ(0) , and η(0) , which depend on the problem parameters and are updated iteratively from the leaf nodes to the root node. 

## 5 Finite-time Analysis in Continuous POMDP 

This section extends the finite-time analysis to the continuous setting. We first identify a notable gap in the existing literature concerning theoretical analysis for POMDP planning in continuous spaces. The finite-time analyses for POMDPs include the probabilistic concentration bounds described in Section 4, which require a finite branching factor in the belief tree, as well as the deterministic bounds provided in [Barenboim and Indelman, 2026], which are similarly restricted to discrete POMDPs. In this section, we introduce an adaptive partitioning scheme for continuous spaces in POMDP planning and establish a theoretical foundation for a finite-time analysis in continuous POMDPs. Specifically, we focus on the continuous observation space, as the continuous action space can be addressed through a similar progressive widening approach, as demonstrated in VOMCPOW [Lim et al., 2021]; the extension is straightforward. 

5.1 Abstract Partitioning of Continuous Spaces 

To address the infinite branching factor arising from continuous observation spaces, we propose an abstract partitioning scheme to discretize the continuous space. We define a partition for each belief node with history h, a as P(h, a ), where each belief node has its own partition and each partition comprises a collection of cells C(h, a ) = {ci(h, a )} ∈ P (h, a ). We denote the number of cells as m(h, a ) ≜ C(h, a ) . For simplicity, we will omit the history for each cell in this section. For each cell c, we denote the set of observations associated with the cell as Z(c) = {z ∈ c} ⊆ Z 

and designate a representative center zc(c). For simplicity, we denote the center as zc in the later part. The boundaries between cells may be either explicit or implicit, depending on the specific partitioning method employed. Specifically, in conjunction with the partitioning, we define a general metric D(·, ·) to measure the distance between two observation realizations in the corresponding observation space. Typically, observations are assigned to the nearest cell center according to this metric, i.e. Z(c|P (h, a )) = z D(z, z c) ≤ D(z, z c′

), ∀ zc′̸ = zc .Based on this partitioning, we modify the history representation in POMDPs to incorporate cell information in place of raw observations: ˜ht = ( b0, a 0, c 1, a 1, c 2, · · · , a t−1, c t). However, for partitionings with explicitly defined boundaries, the cell information is difficult to represent di-rectly. We propose an alternative, commonly used approach that utilizes only the center point 

zc to represent all observations within a given cell. Under this formulation, the history becomes: 

˜ht = ( b0, a 0, z c

> 1

, a 1, z c

> 2

, · · · , a t−1, z ct ).

Under this center point representation, the value function is defined as: V p(˜bk) = r(bk, π p(˜bk)) + 

P 

> ck+1

P (ck+1 |˜bk, π p(˜bk)) V p(˜bk+1 ), where πp is a policy with policy space Πp : ˜Ht → A , where 

˜Ht is the set of all possible histories ˜ht. The belief update is given by: ˜bk+1 ≜ P (xk+1 | ˜hk+1 ) = 

ψ(˜bk, π p(˜bk), z ck+1 ).

Remark 1 (Justification for Center Point Representation) . The center point representation for each cell is commonly used in many MCTS-style solvers that partition the action space, e.g. [Lim et al., 2021, Hoerger et al., 2024]. The intuition is that if the cell is sufficiently small, the center point serves as a good representation of the cell. Although the center point representation sacrifices some fidelity compared to the full observation representation, it will facilitate the MCTS algorithm by visiting the same belief multiple times and improve the estimation of the value function at that belief. 

6Figure 1: The flow of the theoretical analysis. We bound the continuous POMDP with the partitioned POMDP at the theoretical value function level. Then we bound the theoretical and MCTS-estimated value function for the partitioned POMDP. Finally, we use the triangle inequality to combine the two bounds to obtain a final bound for the MCTS estimates in continuous POMDPs. 

5.2 Theoretical Analysis 

In this section, we provide a probabilistic bound for the MCTS estimation error in continuous POMDPs: |V ∗(b0)− ˆV p∗ 

> n

(b0)|. Here, V ∗(b0) is the theoretical value function for continuous POMDP and ˆV pn (b0) is the MCTS-estimated value function after n iterations for the partitioned POMDP. Our analysis proceeds in two steps: First, we establish a bound between the theoretical value function for the original problem and for the partitioned POMDP, |V ∗(b0) − V p∗(b0)|. Then, as we have already formed a finite-branching-factor POMDP via the abstract partitioning, we directly apply Theorem 1 to obtain a bound between the MCTS-estimated and the theoretical value function for the partitioned POMDP: |V p∗(b0) − ˆV p∗ 

> n

(b0)|. Finally, we use the triangle inequality to combine the two bounds to obtain a final bound for the MCTS estimates in continuous POMDPs, 

|V ∗(b0) − ˆV p∗ 

> n

(b0)| ≤ | V ∗(b0) − V p∗(b0)| + |V p∗(b0) − ˆV p∗ 

> n

(b0)|. (6) The overall flow of the theoretical analysis is shown in Fig. 1. Before presenting the bounding theorem between the theoretical value function for the original problem and for the partitioned POMDP, we first state the assumptions and requirements for the POMDP setting. 

Assumption 5.1 (Hölder continuity of belief update in observation) . There exist constants Lψ > 0

and α ∈ (0 , 1] such that for all b ∈ B , a ∈ A and z, z ′ ∈ Z , W 1 ψ(b, a, z ), ψ (b, a, z ′) ≤

Lψ D(z, z ′)α, where W 1 is the Wasserstein-1 distance, and ψ is the belief update function. 

Assumption 5.2 (Hölder continuity of the optimal value in belief) . There exist constants LV > 0

and β ∈ (0 , 1] such that for all b, b ′ ∈ B , V ∗(b) − V ∗(b′) ≤ LV W 1(b, b ′)β .

We define the node-wise covering radius and cell diameter by ε˜ha := sup z∈Z min c∈C (·| ˜ha ) D z, z c(˜ha ) and diam  C(· | ˜ha ) := max c∈C (·| ˜ha ) sup z,z ′∈c D(z, z ′).

Assumption 5.3. There exist constants Ccov > 0 and kZ > 0 such that for all r ∈ (0 , 1] , N (Z, r ) ≤

Ccov r−kZ and ε˜ha ≤ 2r, where N (Z, r ) is the smallest number of kZ -balls of radius r needed to cover Z.

Assumption 5.4. Fix any history–action node ˜ha that can appear in the search tree. Let ν˜ha denote the (marginal) distribution on Z of observations generated at node ˜ha , i.e., P (z|˜h, a ). There exist constants cν > 0 and r0 > 0 (uniform over all such ˜ha ) such that for all z ∈ Z and all r ∈ (0 , r 0],

ν˜ha 

 BZ (z, r ) ≥ cν rkZ , where BZ (z, r ) ≜ {z′ ∈ Z : D(z, z ′) ≤ r}.

Remark 2 (Justification for Assumptions) . The assumptions adopted in this work are mild and commonly used in the literature. The Hölder continuity assumption is a relaxed version for Lipschitz continuity, which is used in analyzing continuous POMDP planning [Kim et al., 2020, Lim et al., 2021]. Our polynomial shrinkage ratio for covering numbers similarly constitutes a relaxed version of the exponential shrinkage ratio considered in [Kim et al., 2020]. 

Then, we get the bounding theorem for partitioning loss. 7Theorem 2. Assume Assumptions 5.1–5.4. Fix a planning horizon L and let ˜HL be the set of reachable history–action nodes up to depth L. At each ˜ha ∈ ˜HL, replace the raw obser-vation z by the nearest center ˆz = q˜ha (z) from m(˜ha ) centers, with covering radius ε˜ha := sup z∈Z min j D z, z c,j (˜ha ). Then for any δ ∈ (0 , 1) , with probability at least 1 − δ,

|V ∗(b0) − V p∗(b0)| ≤ γ(1 − γL)1 − γ LV Lβψ max  

> ˜ha ∈˜HL

C log   m(˜ha )| ˜HL|

> δ



m(˜ha )

! 1

> kZ



> αβ

, (7) 

where C > 0 is a constant depending only on (Ccov , c ν , r 0, k Z ), and | ˜HL| is the size of history nodes at depth L.Proof sketch. This can be proved by first bounding the error at each node directly using the assump-tions, and then applying a union bound over all nodes. The proof is provided in Appendix D. We now present our main theorem that bounds the MCTS estimation error in continuous POMDPs. The following Theorem provides finite-time guarantees for MCTS in continuous POMDPs if the partitioning follows the above assumptions. 

Theorem 3 (MCTS Estimation Bound in Continuous POMDPs) . Assume Assumptions 5.1–5.4 and Assumption 4.1– 4.2 hold. Assume moreover that Theorem 1 applies to the partitioned (finite-branching) POMDP induced by the adaptive abstract partition, yielding concentration parameters 

(β(0) , ξ (0) , η (0) ) for the corresponding corrected-UCB MCTS estimator. Let ˆV pn (b0) be the root value estimate after n simulations on the partitioned tree, and let V ∗ 

> p

(b0) be the optimal value for the partitioned problem. Then for any δ1, δ 2 ∈ (0 , 1) , with probability at least 1 − (δ1 + δ2),

ˆV pn (b0) − V ∗(b0) ≤n−(1 −η(0) ) 2β(0) 

δ1

1/ξ (0) 

+ γ(1 − γL)1 − γ LV Lβψ max 

> ˜ha ∈HL

C

 log   m(˜ha ) |HL|

> δ2



m(˜ha )



> 1/k Z



> αβ

. (8) 

In particular, taking δ1 = δ2 = δ/ 2 yields a bound holding with probability at least 1 − δ.Proof sketch. The result follows by applying a union bound to combine the partitioning loss bound in Theorem 2 with the MCTS estimation bound for the discrete setting in Theorem 1. The observation noise from nonstationary partitioning can be addressed by a martingale analysis as discussed in the detailed proof in Appendix E. We can employ progressive widening in MCTS to realize the adaptive partitioning, the number of cells m(˜ha ) at node ˜ha is controlled so that m(˜ha ) ≤ kz N (ha )αz . The analysis of the partitioning in Theorem 2 is general and can be applied to any partitioning method that satisfying the stated assumptions, and Theorem 1 requires a fixed finite branching factor. Consequently, for the purpose of theoretical analysis, we consider a “frozen” instance of partitioning from the progressive widening process at a finite time. It is worth noting that in the adaptive setting, the observation mixture step in the proof of Theorem 1 requires minor modifications but yields equivalent results, as detailed in proof of Theorem 3 in Appendix E. A practical solver utilizing this framework is presented in the subsequent section. A parameter design with closed-form representation is given in Appendix A.3. 

5.3 Voro-POMCPOW 

Voronoi partitioning is a practical method to partition the continuous space, where the border of each partition is defined by the distributions of the sampled control points in a non-stationary way. In each Voronoi cell, all the points are closer to its center point than any other center points. In POMDP planning, Lim et al. [2021] use Voronoi partitioning to handle a continuous action space, adopting Voronoi Optimistic Optimization (VOO) to balance exploration and exploitation in the continuous action space, and Kim et al. [2020] use it for deterministic MDPs. However, it remains an open problem how to use Voronoi partitioning to discretize the state and observation spaces in POMDPs. 8Table 1: Mean returns for 10-step sim-ulations over 100 episodes on Modified LightDark 1D.       

> Method Mean Return ±std Voro-POMCPOW 6.036653 ±0.273107 POMCPOW 6.094985 ±0.300759

Table 2: Average post-run examination results for proposed bounds in Thm. 3.     

> Confidence Proposed Bound in Thm. 3
> 80% 1.992210
> 85% 2.003508
> 90% 2.022460

We propose to use Voronoi partition as the abstract partitioning introduced in the previous section to discretize the continuous observation space. For Voronoi partition, it is easy to satisfy the requirements of Assumptions 5.1–5.4. The partitioning is based on the distribution of the center points (or control points) zc. Each cell contains the observations that are closer to its center point than any other center points. As the sampling process proceeds, the partitioning becomes non-stationary but finer. Since we use the center points to represent cells, the samples will not be re-assigned to new cells when created. The proposed method, namely Voro-POMCPOW, is shown in Algorithm 2. It extends the progressive widening mechanism in observation space of POMCPOW to a Voronoi-based Progressive Widening, which can lead to the finite-time guarantee. For this work, we only consider the Voronoi partitioning in the observation space and consider the discrete action space. It is trivial to extend it to the continuous action space by using Voronoi Optimistic Optimization (VOO) method as in [Lim et al., 2021]. In this work, we consider a simple version of Voronoi partitioning, where each cell is represented by the center point only and the sample points will be used only for choosing cells and then be discarded. This follows the definition of in-cell estimation represented by the cell points only in Section 5.1. 

## 6 Empirical Results 

This section evaluates the empirical performance of the proposed solvers in comparison to POM-CPOW. Then we present the practical implementation of the probabilistic finite-time guarantee for MCTS in continuous POMDPs. We present a limited empirical evaluation here due to the page limit, and the contribution of this paper is primarily theoretical. We evaluate our method on the LightDark 1D benchmark, which has a discrete action space and a continuous observation space. We use POMCPOW as the baseline and compare it with the proposed Voro-POMCPOW. Table 1 reports the mean returns with standard deviations over 100 episodes. Although our method uses a more conservative polynomial exploration bonus to enable finite-time guarantees, it achieves competitive empirical performance compared with POMCPOW. Table 2 reports the post-run average results of our proposed bound in practice under different confidence levels in the modified LightDark 1D environment with 5000 iterations. The results demonstrate the practicality of the proposed bound in planning problems. The detailed experimental setup is provided in Appendix B. 

## 7 Conclusion 

This work presents the first finite-time analysis of MCTS in POMDPs, addressing the challenge of non-stationary value estimates. We established polynomial concentration bounds for the discrete setting and introduced an abstract partitioning framework for continuous observation spaces, shown by the Voro-POMCPOW. We also present empirical results for our proposed methods. These contributions not only bridge the gap between theory and practice in POMDP planning but also offer new analytical tools applicable to continuous MDPs. 

Limitations Our analysis assumes a fixed horizon planning problem and relies on assumptions on the observation space. Extending these results to infinite horizons remains an open challenge. Besides, this analysis can be extended to PFT-DPW-style algorithms, which is left for future work. We can also test the proposed bounds on more complex environments and solvers, which is left for future work as well. 98 Acknowledgments 

This work was supported by the Israel Ministry of Innovation, Science and Technology. 

## References 

Jean-Yves Audibert and Sébastien Bubeck. Minimax policies for adversarial and stochastic bandits. In COLT , pages 217–226, 2009. M. Barenboim and V. Indelman. Online pomdp planning with anytime deterministic optimality guarantees. Artificial Intelligence, accepted , 350:104442, 2026. doi: https://doi.org/10.1016/j. artint.2025.104442. Hyeong Soo Chang. On the convergence rate of mcts for the optimal value estimation in markov decision processes. IEEE Transactions on Automatic Control , 2025. Tuan Dam, Carlo D’Eramo, Jan Peters, and Joni Pajarinen. A unified perspective on value backup and exploration in monte-carlo tree search. Journal of Artificial Intelligence Research , 81:511–577, 2024. Tuan Quang Dam. Power mean estimation in stochastic continuous monte-carlo tree search. In Intl. Conf. on Machine Learning (ICML) , 2025. Marcus Hoerger, Hanna Kurniawati, Dirk Kroese, and Nan Ye. Adaptive discretization using voronoi trees for continuous pomdps. The International Journal of Robotics Research , 43(9):1283–1298, 2024. Beomjoon Kim, Kyungjae Lee, Sungbin Lim, Leslie Kaelbling, and Tomás Lozano-Pérez. Monte carlo tree search in continuous spaces using voronoi optimistic optimization with regret bounds. In 

AAAI Conf. on Artificial Intelligence , volume 34, pages 9916–9924, 2020. D. Kong and V. Indelman. Simplified pomdp with an alternative observation space and formal performance guarantees. In Proc. of the Intl. Symp. of Robotics Research (ISRR) , 2024. Hanna Kurniawati. Partially observable markov decision processes and robotics. Annual Review of Control, Robotics, and Autonomous Systems , 5(1):253–277, 2022. Mikko Lauri, David Hsu, and Joni Pajarinen. Partially observable markov decision processes in robotics: A survey. IEEE Transactions on Robotics , 39(1):21–40, 2022. Michael H Lim, Claire J Tomlin, and Zachary N Sunberg. Voronoi progressive widening: efficient online solvers for continuous state, action, and observation pomdps. In 2021 60th IEEE conference on decision and control (CDC) , pages 4493–4500. IEEE, 2021. Michael H Lim, Tyler J Becker, Mykel J Kochenderfer, Claire J Tomlin, and Zachary N Sunberg. Optimality guarantees for particle belief approximation of pomdps. Journal of Artificial Intelligence Research , 77:1591–1636, 2023. Rémi Munos. Optimistic optimization of a deterministic function without the knowledge of its smoothness. Advances in neural information processing systems , 24, 2011. Devavrat Shah, Qiaomin Xie, and Zhi Xu. Nonasymptotic analysis of monte carlo tree search. 

Operations Research , 70(6):3234–3260, 2022. David Silver and Joel Veness. Monte-carlo planning in large pomdps. In Advances in Neural Information Processing Systems (NeurIPS) , pages 2164–2172, 2010. Zachary Sunberg and Mykel Kochenderfer. Online algorithms for pomdps with continuous state, action, and observation spaces. In Proceedings of the International Conference on Automated Planning and Scheduling , volume 28, 2018. Maciej ´ Swiechowski, Konrad Godlewski, Bartosz Sawicki, and Jacek Ma´ ndziuk. Monte carlo tree search: A review of recent modifications and applications. Artificial Intelligence Review , 56: 2497–2562, 2023. doi: 10.1007/s10462-022-10228-y. 10 Supplementary Materials A Algorithms and Parameter Design 

A.1 Algorithm: Corrected-POMCP 

The algorithm for Corrected-POMCP, with the corrected UCB for the action selection process, is shown below: 

Algorithm 1 Corrected-POMCP 

Require: History h, Generative Model G, Parameters β, ξ, α, η , Planning horizon L 

> 1:

function SEARCH (h) 

> 2:

while Budget > 0 do  

> 3:

s ∼ B (h) ▷ Sample state from belief  

> 4:

SIMULATE (s, h, 0) 

> 5:

end while  

> 6:

return arg max a∈A Q(h, a ) 

> 7:

end function  

> 8:

function SIMULATE (s, h, d ) 

> 9:

if d ≥ L then return 0 

> 10:

end if  

> 11:

if h / ∈ T ree then ▷ Expansion  

> 12:

Initialize N (h) = 0 , ∀a : N (h, a ) = 0 , Q (h, a ) = 0  

> 13:

return ROLLOUT (s, h, d ) 

> 14:

end if  

> 15:

a∗ ← arg max a



Q(h, a ) + β1/ξ ·N (h)α/ξ  

> N(h,a )1−η



▷ Polynomial Exploration Bonus  

> 16:

(s′, z, r ) ∼ G (s, a ∗) ▷ Call Generative Model  

> 17:

R ← r + γ · SIMULATE (s′, (h, a ∗, z ), d + 1)  

> 18:

N (h) ← N (h) + 1; N (h, a ∗) ← N (h, a ∗) + 1 ▷ Update Statistics  

> 19:

Q(h, a ∗) ← Q(h, a ∗) + R−Q(h,a ∗)  

> N(h,a ∗)
> 20:

end function A.2 Algorithm: Voro-POMCPOW 

The algorithm for Voro-POMCPOW, which incorporates Voronoi partitioning for continuous observa-tion spaces, is shown below in Algorithm 2. 

A.3 Parameter Design 

Theorem 3 uses two groups of parameters: the corrected-UCB parameters inherited from Theorem 1 and the progressive-widening parameters kz , α z . For the corrected-UCB part, we can fix η ∈ [1 /2, 1) 

without depth dependence, set κ = η(1 − η), and choose a root tail exponent ξ(0) > 1. We set 

ξ(0) = ρ, ξ(ℓ+1) = ξ(ℓ) + 1 

κ , α(ℓ+1) = ξ(ℓ) + 1 = κξ (ℓ+1) , (9) for ℓ = 0 , . . . , L − 1. Hence, at a node h of depth ℓ,

α(ℓ+1) 

ξ(ℓ+1) = κ, (10) and the corrected-UCB bonus can be implemented as 

B(h, a ) = cℓ

N (h)η(1 −η)

N (h, a )1−η , (11) 11 Algorithm 2 Voro-POMCPOW  

> 1:

procedure SIMULATE (s, h, d ) 

> 2:

if d ≥ L then  

> 3:

return 0 

> 4:

end if  

> 5:

a ← ACTION SELECTION (h) 

> 6:

(s′, z, r ) ← G(s, a ) 

> 7:

if |C (z | ha )| ≤ kz N (ha )αz then  

> 8:

Create a new Voronoi cell c(z | ha ) with center z 

> 9:

C(z | ha ) ← C (z | ha ) ∪ { c(z | ha )} 

> 10:

total ← r + γ ROLLOUT (s′, haz, d + 1)  

> 11:

Update value estimates V (hac ) 

> 12:

else  

> 13:

Find nearest Voronoi cell ci ∈ C (z | ha ) to observation z 

> 14:

z ← Cell center zc of ci 

> 15:

total ← r + γ SIMULATE (s′, haz, d + 1)  

> 16:

Update value estimates V (hac ) 

> 17:

end if  

> 18:

N (h) ← N (h) + 1  

> 19:

N (ha ) ← N (ha ) + 1  

> 20:

Q(ha ) ← Q(ha ) + total − Q(ha )

N (ha ) 

> 21:

return total  

> 22:

end procedure 

where cℓ absorbs the conservative concentration constant. In experiments, we use 

cℓ = c0Vmax ,ℓ , Vmax ,ℓ = Rmax 

1 − γL−ℓ

1 − γ , (12) with Vmax ,ℓ = ( L − ℓ)Rmax when γ = 1 , and tune only c0. For the default choice η = 1 /2, this becomes 

B(h, a ) = cℓ

N (h)1/4

pN (h, a ) . (13) Thus the theoretical parameters ξ(ℓ) and α(ℓ) are depth-dependent, but the count exponents of the implemented bonus are not. For continuous observations, we use progressive widening m(˜ha ) ≤ kz N (˜ha )αz . We can set the progressive widening parameters according to the continuous space and the desired partitioning rate. 

## B Experimental settings 

Experimental settings are as follows. 

B.1 Modified Light-Dark 1D Model 

The Light-Dark 1D environment is a continuous-state POMDP defined as follows: • Transitions: x′ = x + a · ∆ + w, where w ∼ N (0 , σ 2

> tr

) and ∆ is the step size. • Observations: o ∼ N (x′, σ obs (x′)2). The noise scales with distance from the light source 

xlight :

σobs (x) = σmin + α|x − xlight | (14) where α is the observation slope. • Reward: R(s, a, s ′) = −| s′ − g| − Cstep , where g is the goal location. 

Parameters: Step size ∆ = 1 .0, σtr = 0 .1, xlight = 0 , σmin = 0 .1, α = 0 .5, goal g = 0 with radius 

0.2, Cstep = 0 .1, and γ = 0 .95 .12 B.2 Parameters Modified 1D LightDark environment. We use a compact, theorem-friendly variant of the 1D Light-Dark POMDP. The state, observation, and action spaces are X = [ −1, 1] , Z = [ −1.5, 1.5] , A =

{− 0.4, 0, 0.4}. The light source is located at xlight = 0 , and the goal is at xgoal = 0 .8. The transition model is 

xt+1 = Gate [−1,1] 

 xt + at + wt

, w t ∼ TruncNormal(0 , σ 2 

> T

; [ −wmax , w max ]) , (15) where Gate [−1,1] (y) = min {1, max {− 1, y }} , σT = 0 .02 , and wmax = 0 .06 . The observation noise is state dependent: σo(x) = σmin + ( σmax − σmin )|x − xlight |, with σmin = 0 .05 and σmax = 0 .35 .To ensure a nonzero observation-density floor, observations are sampled from the mixture 

zt ∼ (1 − kfloor )TruncNormal  xt, σ o(xt)2; [ −1.5, 1.5]  + kfloor Unif([ −1.5, 1.5]) , (16) where kfloor = 0 .05 . The reward is bounded in [0 , 1] :

r(x, a ) = 1 − min 



1, |x − xgoal |

2 + λ |a|

0.4 + τ ra(a)



, (17) where λ = 0 .05 , τ = 10 −4, and ra(−0.4) = 0 , ra(0) = 1 , ra(0 .4) = 2 . The initial belief is 

b0 = TruncNormal( −0.6, 0.15 2; [ −1, 1]) .

Experiment setting. We use horizon L = 3 , discount γ = 0 .95 , and n = 5000 simulations per search. Observation progressive widening uses kz = 8 and αz = 0 .5. The depth-scaled corrected-UCB bonus uses η = 1 /2, cℓ = c0Vmax ,ℓ , c0 = 1 , and Rmax = 1 . We set ξ(0) = 2 and β(0) = 2 ,which gives ξ(0:3) = (2 , 12 , 52 , 212) and α(1:3) = (3 , 13 , 53) ; the leaf-to-root recursion checks are verified before reporting the certificate. For confidence 1 − δ, we use δ1 = 0 .75 δ and δ2 = 0 .25 δ.The partition term uses C = 20 , kZ = 1 , and radius cap 1. Results are averaged over 100 independent runs with seeds 20260504 − 20260603 .The experiment is implemented in Julia and running on a machine with an i7-12700 CPU and 32GB RAM. 

## C Proof of Theorem 1 

The parameters are updated recursively as follows: 1. Leaf Initialization: Let ξ(L) be a sufficiently large constant dependent on the bounded reward support at the leaves. 2. Recursive Step: For each depth h = L − 1, . . . , 0, choose the exploration parameter α(h+1) 

satisfying: 

ξ(h+1) η(1 − η) ≤ α(h+1) < ξ (h+1) (1 − η), (18) and define the resulting concentration exponent for depth h as: 

ξ(h) = α(h+1) − 1. (19) 3. Condition: The leaf parameter ξ(L) is chosen large enough such that α(1) > 2.The proof of Theorem 1 largely follows the structure of Shah et al. [2022] but extending to the discrete POMDP setting. The main difference lies in the observation mixing step that handles the branching due to the observation and transition model in POMDPs. We provide a proof sketch below. 

Proof Sketch of Theorem 1. The proof proceeds by backward induction on the depth of the tree, from the leaf level ( h = L) to the root ( h = 0 ). We define a random return process {Gt} to satisfy the polynomial concentration property P(β, ξ, η ) (same as [Shah et al., 2022]) around a mean V ∗ if for all n ≥ 1, z ≥ 1:

P



n( ˆGn − V ∗) ≥ nη z



≤ βz −ξ , (20) and similarly for the lower tail. 13 1. Base Case (Depth h = L) At the leaf nodes, the value estimate is derived from the immedi-ate bounded reward. It satisfies Hoeffding’s inequality, which implies polynomial concentration 

P(β(L), ξ (L), η ) for any η ∈ [1 /2, 1) and sufficiently large ξ(L).

2. Inductive Step (Depth h + 1 → h) Assume that for all belief nodes at depth h + 1 , the MCTS value estimates satisfy P(β(h+1) , ξ (h+1) , η ). We show that the value estimate at a parent node b at depth h satisfies P(β(h), ξ (h), η ′). This step decomposes into two parts: Observation Mixing and Action Selection. 

Part A: Observation Mixing Consider a fixed action a at belief b. Executing a yields a stochastic observation z ∈ Z with probability pz ≥ ϕZ (Assumption 4.2). The empirical return ˆQa,n after n

visits to (b, a ) is a weighted sum of the children’s value estimates. Let Nz (n) be the visit count of observation z. We decompose the error: 

n( ˆQa,n − Q∗

> a

) = X

> z∈Z

Nz (n)( ˆVz,N z (n) − V ∗ 

> z

)

| {z }

> Recursive Estimation Error

+ X

> z∈Z

(Nz (n) − np z )V ∗

> z

| {z }

> Stochastic Observation Noise

. (21) • The Recursive Estimation Error is a sum of terms that, conditioned on Nz (n), satisfy the inductive hypothesis P(β(h+1) , ξ (h+1) , η (h+1) ). Since η(h+1) ≥ 1/2, the polynomial concentration is preserved under this weighted summation (see Lemma A.1 in [Shah et al., 2022]). • The Stochastic Observation Noise arises from the deviation of counts Nz (n) from their expected values np z . These counts follow a multinomial distribution and thus exhibit Sub-Gaussian concentration. Since Sub-Gaussian tails decay faster than polynomial tails, this noise is absorbed into the polynomial bound without degrading the exponent ξ(h+1) .Thus, the mixed return process for action a satisfies P(β(h+1) , ξ (h+1) , η (h+1) ).

Part B: Action Selection (Bandit) We treat the node b as a non-stationary bandit problem where each arm a satisfies P(β(h+1) , ξ (h+1) , η (h+1) ). We apply the Corrected-UCB rule with parameter 

α(h+1) . By Theorem 3 of Shah et al. [2022], provided that ξ(h+1) η(h+1) (1 − η(h+1) ) ≤ α(h+1) <ξ(h+1) (1 − η(h+1) ), the aggregated value estimate at node b satisfies the polynomial concentration 

P(β(h), ξ (h), η (h)) with: 

ξ(h) = α(h+1) − 1, and η(h) = α(h+1) 

ξ(h+1) (1 − η(h+1) ) . (22) This confirms the recursive parameter updates defined in the theorem. 

3. Conclusion By backward induction, the root node b0 satisfies P(β(0) , ξ (0) , η (0) ). Specifically, 

P



n| ˆVn(b0) − V ∗(b0)| ≥ nη(0) 

z



≤ β(0) z−ξ(0) 

, (23) which implies | ˆVn(b0) − V ∗(b0)| = Op(n−(1 −η(0) )).

## D Proof of Theorem 2 

Proof. Fix a planning horizon L. Let ˜ha be a possible history-action node. Let m(˜ha ) = |C (˜ha )|

be the number of cells at that node. We denote the set of cell centers as {zc

> 1

, . . . , z cm}. For any observation z ∈ Z , let q˜ha (z) = arg min zc∈C (˜ha ) D(z, z c) be the nearest-center quantizer. By the covering radius definition ˜ϵ˜ha in Assumption 5.3, we have D(z, q ˜ha (z)) ≤ ˜ϵ˜ha for all z.14 Step 1: One-Step Error ( Qm). To analyze the error propagation, we introduce an intermediate Q-function, Qm(˜h, a ), which represents the value of taking action a at history ˜h using the quantized 

observation for the immediate step, but assuming the optimal value function V ∗ for all future steps. Let the true Q-value be: 

Q∗(˜h, a ) = r(b, a ) + γEz∼PZ [V ∗(ψ(b, a, z ))] . (24) We define the modified Q-value Qm as: 

Qm(˜h, a ) ≜ r(b, a ) + γEz∼PZ [V ∗(ψ(b, a, q ˜ha (z)))] . (25) Note that Qm uses V ∗ in the expectation isolating the local error caused by center represented belief update. We bound the difference |Q∗(˜h, a ) − Qm(˜h, a )|. By Assumption 5.1 and 5.2, we have: 

|V ∗(ψ(b, a, z )) − V ∗(ψ(b, a, q ˜ha (z))) | ≤ LV Lβψ D(z, q ˜ha (z)) αβ ≤ LV Lβψ (˜ ϵ˜ha )αβ . (26) Integrating over z:

|Q∗(˜h, a ) − Qm(˜h, a )| ≤ γ

Z

> z∈Z

PZ (z|˜h, a )|V ∗(ψ(b, a, z )) − V ∗(ψ(b, a, q ˜ha (z))) |dz (27) 

≤ γL V Lβψ (˜ ϵ˜ha )αβ . (28) 

Step 2: Recursive Error. We now relate the optimal value function V ∗ to the partitioned optimal value function V p∗. Recall that V ∗(˜h) = max a Q∗(˜h, a ) and V p∗(˜h) = max a Qp∗(˜h, a ). Using the inequality | max f − max g| ≤ max |f − g|:

|V ∗(˜h) − V p∗(˜h)| ≤ max  

> a

|Q∗(˜h, a ) − Qp∗(˜h, a )|. (29) We apply the triangle inequality using our intermediate term Qm:

|Q∗(˜h, a ) − Qp∗(˜h, a )| ≤ | Q∗(˜h, a ) − Qm(˜h, a )|

| {z }

> Local Quantization Error

+ |Qm(˜h, a ) − Qp∗(˜h, a )|

| {z }

> Iterative Propagation Error

. (30) The first term is bounded by Eq. (28). The second term captures the difference in future values: 

|Qm(˜h, a ) − Qp∗(˜h, a )| = |γEz [V ∗(ψ(b, a, q (z)))] − γEz [V p∗(ψ(b, a, q (z)))] | (31) 

≤ γEz |V ∗(ψ(·)) − V p∗(ψ(·)) | ≤ γ max  

> b′

|V ∗(b′) − V p∗(b′)|. (32) Thus, the total error at depth t satisfies the recurrence: 

ϵt ≤ γL V Lβψ max(˜ ϵ)αβ + γϵ t+1 . (33) 

Step 3: Finite-Horizon Accumulation. Iterate recursively over the horizon L, we can get: 

|V ∗(b0) − V p∗(b0)| ≤ 

> L−1

X

> t=0

γt+1 LV Lβψ max 

> ˜ha ∈H L

(˜ ϵ˜ha )αβ = γ(1 − γL)1 − γ LV Lβψ



max 

> ˜ha

˜ϵ˜ha 

αβ 

. (34) 

Step 4: Probabilistic Bound. Finally, we bound ˜ϵ˜ha . By Assumptions 5.3 and 5.4, the covering radius with m samples satisfies P(˜ ϵ˜ha > 2r) ≤ Ccov r−kZ exp( −mc ν rkZ ). Solving for r such that the probability is bounded by δ/ |H L| (union bound over the tree) yields the high-probability bound: 

˜ϵ˜ha ≤ C log( m(˜ha )|H L|/δ )

m(˜ha )

!1/k Z

. (35) Substituting this into the accumulated error completes the proof. 15 E Proof of Theorem 3 

Sketch of Proof for Theorem 3. The proof relies on decomposing the total error into two distinct com-ponents: the approximation error (induced by discretizing the continuous space) and the estimation error (induced by the finite sampling in MCTS). Let V ∗(b0) denote the optimal value of the original continuous POMDP, and ˆV pn (b0) denote the MCTS value estimate at the root after n simulations using the adaptive partitioning. We introduce an intermediate term V p∗ 

> n

(b0), which represents the optimal value of the specific discrete POMDP instance induced by the Voronoi partition Pn existing at iteration n.Using the triangle inequality, we decompose the total error as: 

|V ∗(b0) − ˆV pn (b0)| ≤ | V ∗(b0) − V p∗ 

> n

(b0)|

| {z }

> Approximation Error

+ |V p∗ 

> n

(b0) − ˆV pn (b0)|

| {z }

> Estimation Error

(36) 

Bounding the Approximation Error (Term 1): This term can be bounded directly from Theo-rem 2. 

Bounding the Estimation Error (Term 2). Here, we use the Theorem 1’s result to bound the term. However, a minor modification will be made. For the Proof Part A of theorem 1, we need to make a modification, the stochastic observation noise is now non-stationary.Modification is provided at the end of this section. 

Union Bound: Theorem 3 is obtained by taking a union bound over the events where both the partitioning requirements (Theorem 2) and the MCTS concentration (Theorem 1 with modification) hold. By setting error probabilities δ1 = δ2 = δ/ 2, we obtain the final finite-time bound for POMDPs with the continuous observation space. 

Modification to Proof Part A’s Observation Noise of Theorem 1 

This is to make modification to the proof of Theorem 1 to handle the non-stationary observation noise. If the partitioning is adaptive or in a progressive widening way, we need the following modification to the proof of Theorem 1 with a martingale-based analysis to handle the non-stationarity of the discretized observation process. The key idea is to show that the observation-mixing noise term remains polynomially concentrated even when the discretized observations are non-stationary, which allows us to maintain the same concentration properties required for the UCB analysis. We only need to modify the Observation Noise bound in the proof Part A of Theorem 1. Fix a history–action node ˜ha in the tree induced by the current partition. Index by t = 1 , 2, . . . the successive visits to ˜ha during the simulations, and let Ft−1 be the σ-field generated by all randomness up to just before the t-th such visit. Due to the adaptive (time-varying) quantizer/partition used to discretize observations, the discrete child symbol ˜zt ∈ Z n(˜ha ) may be non-stationary . Define the conditional (time-varying) categorical probabilities 

pu,t ≜ P(˜ zt = u | F t−1), u ∈ Z n(˜ha ). (37) Let Nu(m) ≜ Pmt=1 1{˜zt = u} be the number of times child u is selected among the first m visits. Then 

Nu(m) −

> m

X

> t=1

pu,t =

> m

X

> t=1



1{˜zt = u} − pu,t 



=: Mu(m), (38) where (Mu(m)) m≥1 is a martingale with respect to (Fm) and has bounded increments |Mu(t) −

Mu(t − 1) | ≤ 1.Consider the observation-mixing noise term 

S(m) ≜ X

> u∈Z n(˜ha )

V ∗

> u



Nu(m) −

> m

X

> t=1

pu,t 



=

> m

X

> t=1



V ∗ 

> ˜zt

− E[V ∗ 

> ˜zt

| F t−1]



, (39) 16 where V ∗ 

> u

denotes the optimal value at the child corresponding to u. Hence (S(m)) m≥1 is also a martingale. Since rewards are bounded and the horizon is finite, there exists Vmax < ∞ such that 

|V ∗ 

> u

| ≤ Vmax for all u, and thus 

|S(t) − S(t − 1) | = V ∗ 

> ˜zt

− E[V ∗ 

> ˜zt

| F t−1] ≤ 2Vmax . (40) By Azuma–Hoeffding, for any x > 0,

P |S(m)| ≥ x ≤ 2 exp 



− x2

8mV 2max 



. (41) This bound does not require stationarity of (˜ zt). Moreover, exponential tails imply the polynomial tail used in Theorem 1: for any ξ > 0, let 

βξ ≜ sup 

> z≥1

2 zξ exp 



− z2

8V 2max 



< ∞. (42) Then for any η ≥ 12 and all m ≥ 1, z ≥ 1,

P |S(m)| ≥ mη z ≤ 2 exp 



− m2η−1z2

8V 2max 



≤ 2 exp 



− z2

8V 2max 



≤ βξ z−ξ . (43) Therefore, the observation-mixing noise remains polynomially concentrated (with the same exponent 

η) even under non-stationary discretized observations, and the rest of Proof Part A (and hence Theorem 1) proceeds unchanged. 17