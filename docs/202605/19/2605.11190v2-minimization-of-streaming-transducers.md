---
title: Minimization of Streaming Transducers
source: arxiv
title_zh: Streaming Transducers的Minimization
abstract_zh: "We provide general criteria 用于 existence 的 minimal 模型s 的 streaming transducers，namely devices that r。\n\n核心做法：This abstract 模型 subsumes classical (sub) sequential transducers (Schützenberger)，streaming string-t。\n\n主要结果：We then instantiate these criteria to obtain effective minimization results 用于 variants 的 latter 模型。\n\n值得关注的点在于：这篇文章的价值在于把《Streaming Transducers的Minimization》这个问题讲清楚。"
abstract_en: "We provide general criteria for the existence of minimal models of streaming transducers, namely devices that read an input word and produce an output value by iteratively updating an internal memory. This abstract model subsumes classical (sub)sequential transducers (Schützenberger), streaming string-to-string transducers (Alur-Černý), polynomial automata (Benedikt et al.), and variants of streaming string-to-tree transducers (Alur-D'Antoni). We then instantiate these criteria to obtain effective minimization results for variants of the latter model, where outputs are terms constructed incrementally by extending (tuples of) terms either at the leaves or at the roots."
authors: "Christian Bianchini, Gabriele Puppis"
date: 2026-05-11
journal: cs.FL
source_link: "https://arxiv.org/pdf/2605.11190v2"
pdf: "https://arxiv.org/pdf/2605.11190v2"
score: 6.3999999999999995
tldr: 围绕《Streaming Transducers的Mi...》；This abstract 模型 subsumes classi...；We then instantiate these criteria t...
tldr_cn: 围绕《Streaming Transducers的Mi...》；This abstract 模型 subsumes classi...；We then instantiate these criteria t...
tldr_en: "We provide general criteria for the existence of minimal models of streaming transducers, namely devices that read an input word and produce an output value by iteratively updating an internal memory. This abstract model subsumes classical (sub)sequential transducers (Schützenberger), streaming string-to-string transducers (Alur-Černý), polynomial automata (Benedikt et al.), and variants of streaming string-to-tree transducers (Alur-D'Antoni)."
selection_source: fresh_fetch
motivation: We provide general criteria 用于 existence 的 minimal 模型s 的 streaming transducers，namely devices that r。
method: 核心做法：This abstract 模型 subsumes classical (sub) sequential transducers (Schützenberger)，streaming string-t。
result: 主要结果：We then instantiate these criteria to obtain effective minimization results 用于 variants 的 latter 模型。
conclusion: 值得关注的点在于：这篇文章的价值在于把《Streaming Transducers的Minimization》这个问题讲清楚。
---

## 摘要
We provide general criteria 用于 existence 的 minimal 模型s 的 streaming transducers，namely devices that r。

核心做法：This abstract 模型 subsumes classical (sub) sequential transducers (Schützenberger)，streaming string-t。

主要结果：We then instantiate these criteria to obtain effective minimization results 用于 variants 的 latter 模型。

值得关注的点在于：这篇文章的价值在于把《Streaming Transducers的Minimization》这个问题讲清楚。

## Abstract
We provide general criteria for the existence of minimal models of streaming transducers, namely devices that read an input word and produce an output value by iteratively updating an internal memory. This abstract model subsumes classical (sub)sequential transducers (Schützenberger), streaming string-to-string transducers (Alur-Černý), polynomial automata (Benedikt et al.), and variants of streaming string-to-tree transducers (Alur-D'Antoni). We then instantiate these criteria to obtain effective minimization results for variants of the latter model, where outputs are terms constructed incrementally by extending (tuples of) terms either at the leaves or at the roots.
