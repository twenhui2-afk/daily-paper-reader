---
title: "DeCoDrift: Stabilizing Decoder Coupling in Closed-Loop Foundation Segmentation"
source: arxiv
title_zh: DeCoDrift：Stabilizing Decoder Coupling in Closed-Loop Foundation Segmentation
abstract_zh: "这篇论文研究 this phenomenon by instrumenting SAM's mask decoder 与 deriving ground-truth-free measures 的 p。\n\n核心做法：Building on this analysis，作者引入 DeCoDrift，training-free inference-time stabilization 框架 that constrai。\n\n主要结果：We show that this feedback loop can induce previously overlooked failure mode，decoder coupling drift。\n\n值得关注的点在于：More broadly，our results show that decoder-internal dynamics are not merely diagnostic：they provide。"
abstract_en: "Foundation segmentation models such as Segment Anything Model (SAM) are now routinely used in iterative pipelines, where each predicted mask is fed back as the next prompt. This practice turns segmentation into a closed-loop dynamical process, yet the decoder-level behavior of these systems remains largely unexamined. We show that this feedback loop can induce a previously overlooked failure mode, decoder coupling drift, in which the mask decoder's cross-attention progressively loses alignment with the target object, causing errors to accumulate across iterations. We study this phenomenon by instrumenting SAM's mask decoder and deriving ground-truth-free measures of prompt-image coupling, attention stability, and temporal consistency. On volumetric electron microscopy data, these decoder-internal signals reveal that standard iterative prompting systematically degrades attention alignment and temporal coherence relative to oracle-anchored feedback. We then formalize iterative prompting as a discrete-time dynamical system and show how proximal anchoring reduces error amplification in the feedback loop. Building on this analysis, we introduce DeCoDrift, a training-free inference-time stabilization framework that constrains prompt updates and preserves decoder coupling across iterations. Across extensive experiments, DeCoDrift consistently improves attention stability, temporal coherence, and segmentation quality over standard iterative prompting, without retraining or ground-truth supervision. More broadly, our results show that decoder-internal dynamics are not merely diagnostic: they provide actionable signals for stabilizing foundation segmentation models in closed-loop use."
authors: "H. M. Shadman Tabib, Md. Shamsuzzoha Bayzid, M Sohel Rahman"
date: 2026-05-25
journal: cs.CV
source_link: "https://arxiv.org/pdf/2605.25730v1"
pdf: "https://arxiv.org/pdf/2605.25730v1"
score: 6.3999999999999995
tldr: 围绕《DeCoDrift：Stabilizing De...》；Building on this analysis，作者引入 D...；We show that this feedback loop can...
tldr_cn: 围绕《DeCoDrift：Stabilizing De...》；Building on this analysis，作者引入 D...；We show that this feedback loop can...
tldr_en: "Foundation segmentation models such as Segment Anything Model (SAM) are now routinely used in iterative pipelines, where each predicted mask is fed back as the next prompt. This practice turns segmentation into a closed-loop dynamical process, yet the decoder-level behavior of these systems remains largely unexamined."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "volumetric | 体数据的 / 三维体积的 | 学术高频", "alignment | 对齐 | CET6", "inference | 推理 | CET6"]
motivation: "这篇论文研究 this phenomenon by instrumenting SAM's mask decoder 与 deriving ground-truth-free measures 的 p。"
method: 核心做法：Building on this analysis，作者引入 DeCoDrift，training-free inference-time stabilization 框架 that constrai。
result: 主要结果：We show that this feedback loop can induce previously overlooked failure mode，decoder coupling drift。
conclusion: 值得关注的点在于：More broadly，our results show that decoder-internal dynamics are not merely diagnostic：they provide。
---

## 摘要
这篇论文研究 this phenomenon by instrumenting SAM's mask decoder 与 deriving ground-truth-free measures 的 p。

核心做法：Building on this analysis，作者引入 DeCoDrift，training-free inference-time stabilization 框架 that constrai。

主要结果：We show that this feedback loop can induce previously overlooked failure mode，decoder coupling drift。

值得关注的点在于：More broadly，our results show that decoder-internal dynamics are not merely diagnostic：they provide。

## Abstract
Foundation segmentation models such as Segment Anything Model (SAM) are now routinely used in iterative pipelines, where each predicted mask is fed back as the next prompt. This practice turns segmentation into a closed-loop dynamical process, yet the decoder-level behavior of these systems remains largely unexamined. We show that this feedback loop can induce a previously overlooked failure mode, decoder coupling drift, in which the mask decoder's cross-attention progressively loses alignment with the target object, causing errors to accumulate across iterations. We study this phenomenon by instrumenting SAM's mask decoder and deriving ground-truth-free measures of prompt-image coupling, attention stability, and temporal consistency. On volumetric electron microscopy data, these decoder-internal signals reveal that standard iterative prompting systematically degrades attention alignment and temporal coherence relative to oracle-anchored feedback. We then formalize iterative prompting as a discrete-time dynamical system and show how proximal anchoring reduces error amplification in the feedback loop. Building on this analysis, we introduce DeCoDrift, a training-free inference-time stabilization framework that constrains prompt updates and preserves decoder coupling across iterations. Across extensive experiments, DeCoDrift consistently improves attention stability, temporal coherence, and segmentation quality over standard iterative prompting, without retraining or ground-truth supervision. More broadly, our results show that decoder-internal dynamics are not merely diagnostic: they provide actionable signals for stabilizing foundation segmentation models in closed-loop use.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **volumetric**：体数据的 / 三维体积的（学术高频）
- **alignment**：对齐（CET6）
- **inference**：推理（CET6）
