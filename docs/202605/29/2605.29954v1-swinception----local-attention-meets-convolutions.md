---
title: SwInception -- Local Attention Meets Convolutions
source: arxiv
title_zh: SwInception -- Local Attention Meets Convolutions
abstract_zh: "Sparse vision transformers have gained popularity as efficient encoders 用于 medical volumetric segmen。\n\n核心做法：To mitigate this weakness，作者提出 novel 架构 that further enhances Swin's inductive bias by introducing I。\n\n主要结果：Swin uses local attention to reduce complexity 与 yields excellent 性能用于 many tasks but still tends to。\n\n值得关注的点在于：Code 与 pre-trained weights can be accessed at https://github.com/Eiphodos/SwInception。"
abstract_en: "Sparse vision transformers have gained popularity as efficient encoders for medical volumetric segmentation, with Swin emerging as a prominent choice. Swin uses local attention to reduce complexity and yields excellent performance for many tasks but still tends to overfit on small datasets. To mitigate this weakness, we propose a novel architecture that further enhances Swin's inductive bias by introducing Inception blocks in the feed-forward layers. The introduction of these multi-branch convolutions enables more direct reasoning over local, multi-scale features within the transformer block. We have also modified the decoder layers in order to capture finer details using fewer parameters. We demonstrate a performance improvement on eleven different medical datasets through extensive experimentation. We specifically showcase advancements over the previous state-of-the-art backbones on benchmark challenges like the Medical Segmentation Decathlon and Beyond the Cranial Vault. By showing that the existing inductive bias in Swin can be further improved, our work presents a promising avenue for enhancing the capabilities of sparse vision transformers for both medical and natural image segmentation tasks. Code and pre-trained weights can be accessed at https://github.com/Eiphodos/SwInception."
authors: "David Hagerman, Roman Naeem, Jakob Lindqvist, Carl Lindström, Fredrik Kahl, Lennart Svensson"
date: 2026-05-28
journal: cs.CV
source_link: "https://arxiv.org/pdf/2605.29954v1"
pdf: "https://arxiv.org/pdf/2605.29954v1"
score: 6.85
tldr: 围绕《SwInception -- Local Att...》；To mitigate this weakness，作者提出 n...；Swin uses local attention to reduce...
tldr_cn: 围绕《SwInception -- Local Att...》；To mitigate this weakness，作者提出 n...；Swin uses local attention to reduce...
tldr_en: "Sparse vision transformers have gained popularity as efficient encoders for medical volumetric segmentation, with Swin emerging as a prominent choice. Swin uses local attention to reduce complexity and yields excellent performance for many tasks but still tends to overfit on small datasets."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "transformer | Transformer 架构 | 学术高频", "volumetric | 体数据的 / 三维体积的 | 学术高频", "benchmark | 基准测试 | CET6/IELTS"]
motivation: Sparse vision transformers have gained popularity as efficient encoders 用于 medical volumetric segmen。
method: "核心做法：To mitigate this weakness，作者提出 novel 架构 that further enhances Swin's inductive bias by introducing I。"
result: 主要结果：Swin uses local attention to reduce complexity 与 yields excellent 性能用于 many tasks but still tends to。
conclusion: "值得关注的点在于：Code 与 pre-trained weights can be accessed at https://github.com/Eiphodos/SwInception。"
---

## 摘要
Sparse vision transformers have gained popularity as efficient encoders 用于 medical volumetric segmen。

核心做法：To mitigate this weakness，作者提出 novel 架构 that further enhances Swin's inductive bias by introducing I。

主要结果：Swin uses local attention to reduce complexity 与 yields excellent 性能用于 many tasks but still tends to。

值得关注的点在于：Code 与 pre-trained weights can be accessed at https://github.com/Eiphodos/SwInception。

## Abstract
Sparse vision transformers have gained popularity as efficient encoders for medical volumetric segmentation, with Swin emerging as a prominent choice. Swin uses local attention to reduce complexity and yields excellent performance for many tasks but still tends to overfit on small datasets. To mitigate this weakness, we propose a novel architecture that further enhances Swin's inductive bias by introducing Inception blocks in the feed-forward layers. The introduction of these multi-branch convolutions enables more direct reasoning over local, multi-scale features within the transformer block. We have also modified the decoder layers in order to capture finer details using fewer parameters. We demonstrate a performance improvement on eleven different medical datasets through extensive experimentation. We specifically showcase advancements over the previous state-of-the-art backbones on benchmark challenges like the Medical Segmentation Decathlon and Beyond the Cranial Vault. By showing that the existing inductive bias in Swin can be further improved, our work presents a promising avenue for enhancing the capabilities of sparse vision transformers for both medical and natural image segmentation tasks. Code and pre-trained weights can be accessed at https://github.com/Eiphodos/SwInception.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **transformer**：Transformer 架构（学术高频）
- **volumetric**：体数据的 / 三维体积的（学术高频）
- **benchmark**：基准测试（CET6/IELTS）
