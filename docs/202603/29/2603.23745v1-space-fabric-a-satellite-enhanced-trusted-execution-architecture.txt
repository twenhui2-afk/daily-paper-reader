Title: Space Fabric: A Satellite-Enhanced Trusted Execution Architecture

URL Source: https://arxiv.org/pdf/2603.23745v1

Published Time: Thu, 26 Mar 2026 00:23:12 GMT

Number of Pages: 23

Markdown Content:
# Space Fabric: A Satellite-Enhanced Trusted Execution Architecture 

# Filip Rezabek 

SpaceComputer filip@spacecomputer.io 

# Dahlia Malkhi 

UCSB & SpaceComputer 

# Amir Yahalom 

SpaceComputer 

## Abstract 

The emergence of decentralized satellite networks and orbital com-puting platforms creates a pressing need for trust architectures that can operate without physical access to the hardware, without re-liance on pre-provisioned vendor secrets, and without dependence on a single manufacturer’s attestation service. Terrestrial Trusted Execution Environments (TEEs) are insufficient for this setting: hardware-based designs are susceptible to physical attacks. More fundamentally, most of the current platforms root their attestation chains in secrets provisioned during manufacturing, creating a pre-launch trust window and single-vendor dependency that cannot be independently audited. We present Space Fabric, an architecture that provides the miss-ing trust foundation for orbital computing by relocating the trusted computing stack to satellite infrastructure, exploiting a satellite’s post-launch physical inaccessibility as a tamper barrier unattain-able by terrestrial deployments. Our Satellite Execution Assur-ance Protocol binds workload execution to a specific satellite via a Byzantine-tolerant endorsement quorum of distributed ground stations, certifying not only what program executes inside the TEE but also where . A further contribution is fully on-orbit key genera-tion: cryptographic secrets are generated within co-located secure elements after launch, with no persistent signing secrets accessi-ble on Earth at any point. The residual trust assumption reduces to the operator correctly registering device identifiers, a standard operational requirement common to TEE deployments. To further reduce single-vendor dependence, Space Fabric distributes its hard-ware trust anchor across two independent secure elements, an NXP SE050 and a TROPIC01, both of which must co-sign attestation evidence. We implement Space Fabric on a USB Armory Mk II with ARM TrustZone, verify attestation end-to-end using Veraison, and provide a security analysis establishing both satisfaction arguments and impossibility bounds under a strong adaptive adversary. 

## Keywords 

TEE, Space, Security, TPM, SEAP, HSM 

## 1 Introduction 

The space domain is undergoing a fundamental transformation. The proliferation of low-cost small satellites and commercial launch services has made orbital infrastructure increasingly accessible, opening new possibilities not only for traditional space missions but also for general-purpose computation [ 22 , 56 , 60 ]. Recent pro-posals for Decentralized Satellite Networks envision constellations where multiple operators share orbital resources, pooling commu-nication, storage, and processing capabilities across organizational boundaries [ 42 ]. The trend towards AI further motivates the de-velopment of space data centers that can harness solar energy efficiently and benefit from passive thermal dissipation [ 22 , 56 , 60 ]. Early use cases — secure multi-tenant edge computing, sovereign data processing, on-orbit Key Management Service (KMS), and ver-ifiable scientific data provenance — are already within reach of current small satellite platforms. Yet this shift introduces a critical unsolved problem: how does a tenant or verifier establish trust in computation executing on orbital infrastructure they do not own, cannot physically inspect, and cannot independently monitor? In terrestrial cloud computing, this question is addressed by Trusted Execution Environments (TEEs), which isolate sensitive code and data from the rest of the system, including privileged software such as the operating system or hypervisor, promising strong confidentiality and integrity guarantees even on untrusted platforms [ 4, 27 ]. We differentiate between two main families of TEEs: process-based and Virtual Machine (VM)-based solutions. The main difference lies in the size of the Trusted Com-puting Base (TCB): process-based designs run only specific sensitive operations within the enclave, whereas VM-based solutions place the entire guest OS into the enclave. In practice, this translates into a trade-off between security and usability, as VM-based solutions do not require large code modifications and fit current cloud de-ployments. TEE features have driven widespread adoption: Intel Software Guard Extensions (SGX) and Trust Domain Extensions (TDX) [ 15 , 27 ], AMD Secure Encrypted Virtualization (SEV) [ 4], and ARM TrustZone (TZ) [ 45 ] and Confidential Computing Ar-chitecture (CCA) [ 6] are now deployed across data centers, edge devices, and consumer hardware alike. TEE remote attestation is used in applications including privacy-preserving machine learning and others [5, 25, 33]. However, directly transplanting terrestrial TEE architectures to orbital platforms is insufficient, for two reasons. First, existing TEEs were designed under a threat model that assumes physical access is possible and attempts to defend against it — yet their defenses are increasingly failing. Process-based TEEs such as SGX have been shown vulnerable to a growing number of side-channel and microarchitectural attacks [ 14 , 18 ]. VM-based approaches like SEV and TDX trade enclave-level granularity for a substantially larger TCB that includes the entire guest kernel, expanding the attack surface in a different dimension, while remaining susceptible to side-channel attacks from the hypervisor [ 48 , 49 ]. More fundamentally, all existing TEE architectures share a critical limitation: they cannot defend against an adversary with sustained physical access to the host machine. An attacker who can probe buses, extract memory contents, or tamper with hardware peripherals can undermine the very root of trust on which these systems depend, as demonstrated by recent attacks [ 14 , 49 ]. These are not implementation bugs but inherent limitations of terrestrial trusted computing. 

> 1
> arXiv:2603.23745v1 [cs.CR] 24 Mar 2026 Rezabek et al.

Second, and for the orbital context, more pressing, current TEEs root their attestation chains in secrets provisioned during manu-facturing: keys that the hardware vendor held, or may continue to hold, creating a pre-launch secret window and a single-vendor trust dependency that the verifier cannot independently audit [ 30 ]. For decentralized satellite networks, where multiple operators must mutually verify each other’s platforms without relying on a shared vendor, this model is fundamentally misaligned. Recent works aim to close the gap between the execution environment and its physical location, suggesting the need for extended attestation capabilities that can bind the workload to its execution location [ 47 ]. In addi-tion, most current TEE solutions lack verifiability of firmware and hardware, which is part of the TCB, and struggle with supply-chain attacks where the private keys used as a root of trust are provisioned during manufacturing. A trust architecture for orbital infrastruc-ture must therefore be bootstrappable without pre-provisioned secrets, auditable across vendor boundaries, and verifiable without dependence on any single manufacturer’s attestation service. A satellite in orbit, however, enjoys a property that no ground-based system can replicate: physical inaccessibility . Once deployed, its hardware is beyond the reach of any adversary short of a nation-state with anti-satellite capabilities, a threat model fundamentally different from on-Earth deployments. This transforms the design space: rather than building ever more complex defenses against physical attacks, the orbital environment eliminates the threat vec-tor entirely, allowing the security architecture to focus on software-level isolation, cryptographic binding, and distributed verification. This observation motivates Space Fabric , a satellite-native trust architecture that exploits these properties to provide the missing trust foundation for decentralized orbital computing. Space Fabric relocates the entire trusted computing stack, including Trusted Plat-form Module (TPM), TEE, and execution verification, to orbital in-frastructure. The on-board TEE provides additional hardware-level isolation for workloads running in orbit. These components are bound together through the Satellite Execution Assurance Protocol (SEAP), a novel protocol that cryptographically links a workload to a specific satellite by incorporating measurements unique to its orbital context and relying on a challenge-response protocol with on-Earth components. SEAP is the concrete instantiation of a broader security goal we call Proof of Execution Triangulation (Proof of ET): cryptographic evidence that a specific workload ex-ecuted on a specific orbital platform, not inside some TEE. This binding ensures that the computation is verifiably executing on the intended satellite, not on a cloned or replicated platform on the ground. Earth-side components facilitate secure communication with the satellite and allow remote parties to request, submit, and verify workloads, but the trust-critical execution remains entirely in orbit. Figure 1 outlines the general architecture of the satellite unit, where the hardware payload, e.g., Space Fabric HW, is connected via a communication modem, which provides external communication. A unique contribution of Space Fabric is fully on-orbit key gene-sis: all cryptographic signing keys, both satellite identity and attes-tation keys, are generated within the Secure Elements (SEs) after launch, with no secrets existing on Earth at any point. This elimi-nates the pre-launch secret window that TEEs require. Rather than trusting a vendor to erase provisioned secrets, the verifier’s pre-launch trust anchor consists solely of public, non-secret artifacts Satellite Unit 

> Communication Modem
> Hardware Payload Ethernet
> P: Peripheral
> External Comms I2C CAN ...

Figure 1: Simplified architecture of a satellite. Contains ex-ternal communication, which then forwards local signals to the communication modem. The modem is then connected via Ethernet to the hardware payload, e.g., Space Fabric. The payload can include several external peripherals, such as storage devices. 

such as device serial numbers and configuration hashes. To fur-ther reduce single-vendor dependence, Space Fabric distributes its hardware trust anchor across two independent SEs from different vendors, one closed-source and certified, the other fully open and auditable, both of which must co-sign attestation evidence. An ad-versary would need to simultaneously compromise both vendors’ silicon to forge a valid attestation proof. Space, however, is not without constraints. Limited power bud-gets, passive thermal dissipation, radiation exposure, and the launch cost per kilogram to orbit all impose strict requirements on the com-putational hardware that can be deployed. The current hardware selected for the implementation of Space Fabric is designed with these realities in mind, employing lightweight cryptographic pro-tocols and modest hardware payloads that fit within the envelope of modern small satellite platforms. Nevertheless, as more com-puting shifts to space, our architecture should scale as long as its requirements are met. We make the following key contributions: 

KC1 A systematic threat model for orbital TEE deployments that introduces an adversary model spanning satellite systems, ground infrastructure, and communication channels with bounded adaptive corruption capabilities (Section 4.2); 

KC2 The design and implementation of Proof of ET through SEAP, which cryptographically binds workload execution to a spe-cific orbiting platform through a Byzantine-tolerant endorse-ment quorum of distributed ground stations, together with a security analysis establishing both satisfaction arguments and impossibility bounds under a strong adaptive adversary (Section 5 and Section 6); 

KC3 A fully on-orbit key genesis model in which all cryptographic secrets are generated within SEs after launch, eliminating the pre-launch secret window present in popular TEE platforms, complemented by a dual-SE cross-verification architecture that removes single-vendor trust dependence from the attes-tation chain (Section 5.1 and Section 7); 

KC4 An end-to-end implementation of Space Fabric on a USB Ar-mory Mk II with ARM TrustZone, NXP SE050 and TROPIC01 secure elements, and Veraison-based attestation verifica-tion, with an evaluation demonstrating mitigation of a broad range of software- and hardware-level attacks (Section 7). 

## 2 Background 

This section provides background on the fundamental technologies underlying Space Fabric. We begin by examining TEEs, comparing  

> 2Space Fabric: A Satellite-Enhanced Trusted Execution Architecture Normal World
> App
> Secure World EL0 EL1 EL2 EL3
> App
> Guest OS
> Hypervisor
> Secure Monitor
> Secure fi rmware
> Trusted OS

Figure 2: Simplified architecture of an ARM TZ. Exception Level (EL) ensure separation of individual access layers. 

process-based and VM-based isolation approaches. We then sur-vey the software- and hardware-level vulnerabilities that motivate our design. Finally, we discuss TPMs, Hardware Security Modules (HSMs), and satellite computing, and outline Space Fabric’s strategy for closing the remaining trust gaps. 

## 2.1 Trusted Execution Environments 

TEEs provide isolated execution environments that protect sensitive computations from potentially malicious privileged software. Mod-ern TEE architectures fall into two broad categories: process-based isolation, which protects individual applications or code regions, and VM-based isolation, which secures entire virtual machines. 

2.1.1 Process-Based TEEs. Process-based TEEs isolate individual applications or secure enclaves within a running system, protecting them from the operating system and other software components. 

Intel SGX. provides hardware-enforced memory isolation through encrypted enclaves [ 15 ]. Each enclave operates in its own protected address space with memory encryption/decryption handled trans-parently by the CPU Memory Encryption Engine (MEE). A TEE remote attestation is a cryptographic proof, signed by a hardware-rooted key, that a specific piece of software is running unmodified inside a genuine, isolated TEE on a particular platform. Intel SGX supports attestation, enabling verifiers to confirm the identity and integrity of enclaves. However, SGX’s security model explicitly excludes physical side-channel attacks from its threat model [ 15 ]. Nevertheless, researchers have demonstrated practical key extrac-tion and data exfiltration from SGX enclaves. Furthermore, Intel SGX provides no protection against memory bus attacks, where adversaries with physical access can probe communication between the CPU and DRAM [14]. 

ARM TrustZone. partitions system resources into two worlds: a secure world for trusted operations and a normal world for un-trusted software [ 45 ], highlighted in Figure 2. Unlike Intel SGX’s enclave model, TrustZone provides system-wide isolation enforced by the processor’s security state. The TrustZone Address Space Con-troller (TZASC) restricts normal-world access to secure memory regions, while TrustZone Protection Controller (TZPC) governs pe-ripheral access. TrustZone does not provide hardware-based remote attestation natively; attestation must be implemented in software, typically through frameworks such as OP-TEE [54]. 

2.1.2 VM-Based TEE Solutions. VM-based TEEs extend isolation guarantees to the entire VMs, protecting guest VMs from potentially malicious hypervisors and host software. 

AMD SEV-Secure Nested Paging (SNP). encrypts guest VM mem-ory using per-VM keys managed by the AMD Secure Processor (ASP) [ 4]. SNP extends earlier SEV iterations by adding integrity protection through cryptographic hashes of memory pages, pre-venting a malicious hypervisor from rolling back or remapping encrypted pages. Remote attestation enables verifiers to confirm VM identity by measuring the initial memory contents and the CPU state. 

Intel TDX. isolates Trust Domains (TDs)—the TDX equivalent of Confidential VMs (CVMs)—through hardware-enforced mem-ory encryption and access controls [ 27 ]. The Secure Arbitration Mode (SEAM) mediates all interactions between the VM Manager (VMM) and TDs, enforcing TDX-specific protections and preventing unauthorized memory access. Multi-key Total Memory Encryption (MKTME) derives per-TD encryption keys from hardware secrets, ensuring memory confidentiality and integrity. Attestation builds on Intel’s SGX infrastructure: the TD Quoting Enclave (TDQE) generates TD-specific attestation keys, verified against Intel’s Intel Provisioning Certification Service (PCS). 

ARM CCA. provides hardware-enforced isolation for confidential workloads on ARM architectures through the Realm Management Extension (RME) [ 6 ]. RME partitions the physical address space into four distinct worlds: Secure, Non-secure, Realm, and Root, where the Realm world hosts CVMs that are protected from both the hypervisor and the Secure world. The Realm Management Monitor (RMM) mediates all interactions between the untrusted hypervisor and Realm VMs. Memory isolation is enforced by the Granule Protection Check (GPC), which tags physical memory pages with world assignments and prevents unauthorised cross-world access at the hardware level. 

2.1.3 Software-Level Vulnerabilities. Despite providing strong iso-lation boundaries, both TEE families are susceptible to software-level attacks that exploit the interfaces between trusted and un-trusted components. Process-based TEE must interact with the untrusted OS for sys-tem calls, I/O, and memory management. A compromised OS can manipulate enclave inputs and outputs, control page tables, and observe access patterns. In SGX, this enables controlled-channel attacks that infer secrets from page-fault sequences, as well as mi-croarchitectural side channels exploiting caches, branch prediction, and speculative execution [14, 15, 18]. VM-based TEEs face threats from the hypervisor, which retains control over scheduling, resource allocation, and address translation even though it is excluded from the confidentiality TCB. Recent work has demonstrated chosen-plaintext attacks on AMD SEV-SNP via nested page-table manipulation [ 48 ], and the broader challenge remains that the entire guest OS and vendor firmware are included in the TCB, substantially expanding the attack surface compared to process-based designs. 

2.1.4 Hardware-Level Vulnerabilities. Both TEE families remain fundamentally vulnerable to physical adversaries. Attackers with hardware access can probe memory buses, perform cold-boot at-tacks to recover DRAM contents, or exploit side channels through power consumption and electromagnetic emanations [ 2, 14 , 49 ]. TrustZone does not encrypt memory at all, and even state-of-the-art 

> 3Rezabek et al.

memory encryption has been defeated by recent DDR5 interposi-tion attacks [ 14 , 49 ]. These are not implementation bugs but inher-ent limitations of terrestrial trusted computing. These limitations motivate Space Fabric’s relocation of trust anchors to physically inaccessible orbital platforms. 

## 2.2 Trusted Platform Modules 

The TPM enhances platform security by providing hardware-rooted measurement, attestation, and key management primitives [ 50 ]. Un-like TEEs which focus on runtime isolation, TPMs establish trust in the platform’s boot-time configuration and provide cryptographic services bound to that configuration. During the boot process, each stage is measured and the result extended into Platform Configura-tion Registers (PCRs), append-only registers where new values are cryptographically combined with existing ones, creating a tamper-evident record of the boot chain. The TPM’s Endorsement Key (EK), provisioned during manufacturing with a certificate from the TPM vendor, anchors the attestation chain [ 50 ]. Attestation Keys (AKs) derived from the EK sign PCR-based quotes for remote verifiers without revealing platform identity. Sealing operations bind data to specific PCR values, ensuring that sealed secrets become accessible only when the platform boots into an expected configuration. 

## 2.3 HSM 

A HSM is a dedicated, tamper-resistant physical device designed to generate, store, and manage cryptographic keys and perform cryp-tographic operations, such as signing, encryption, and decryption, within a hardened boundary. Its defining property is that private key material never leaves the module in plaintext: applications send data in and receive results back, but the secrets themselves remain locked inside. HSMs typically enforce access policies, maintain au-dit logs, and offer protections against both physical probing and logical attacks. In the context of this work, the NXP SE050 and TROPIC01 fulfill an analogous role at embedded scale: it provides non-exportable key storage, on-chip Elliptic Curve Cryptography (ECC) signing, and a True Random Number Generator (TRNG) in a package suitable for a satellite board. While TPMs are primar-ily designed for platform attestation and boot-time measurement, HSMs are optimised for general-purpose cryptographic key man-agement and signing, without attestation capabilities and limited programmability. 

## 2.4 Satellite Computing 

Satellite computing represents an emerging paradigm where or-bital platforms provide computational and communication services beyond traditional ground-based infrastructure. Recent advances in small satellite technology, commercial launch services, and space-qualified computing have enabled deployment of increasingly so-phisticated processing capabilities in orbit [22]. 

2.4.1 Physical Security Advantages. Orbital platforms provide in-herent physical security properties unattainable by terrestrial in-frastructure. Physical inaccessibility. Satellites orbiting at altitudes of 500–2000 km Low Earth Orbit (LEO)or 35,786 km Geostationary Earth Orbit (GEO) remain beyond reach of casual adversaries. Ac-cessing a satellite requires either sophisticated ground-based attacks (jamming, spoofing) that target communication links rather than physical hardware, or space operations (rendezvous, docking) that impose extraordinary technical and financial barriers. This asymme-try fundamentally alters the threat model: while ground platforms must defend against both software and physical attacks, satellites need to primarily defend against software threats. Satellites are con-tinuously monitored through ground station networks and space surveillance systems. Any unauthorised approach, physical ma-nipulation, or unexpected orbital manoeuvre would be detected through changes in orbital parameters, communication patterns, or telemetry. While not perfect, sophisticated adversaries might conduct stealthy operations, which raises the bar substantially com-pared to terrestrial systems, where physical tampering might go unnoticed for extended periods. Satellite deployment options and latency considerations are de-tailed in Appendix B. 

## 2.5 Space Fabric’s Strategy 

As computation moves to orbital platforms and decentralized satel-lite networks emerge, the trust architecture must follow. The vul-nerabilities surveyed in this section reveal two distinct gaps in the current trusted computing landscape. The first is the physical access gap: terrestrial TEEs, regardless of their software isolation guarantees, cannot defend against an adversary with sustained physical proximity to the hardware. The second is the root-of-trust gap: existing attestation chains depend on secrets provisioned during manufacturing, creating a pre-launch trust window and single-vendor dependency that the verifier cannot independently audit. Space Fabric addresses both gaps through a layered strategy. To close the physical access gap, it relocates the trusted computing stack to orbital infrastructure, exploiting post-launch physical in-accessibility as a passive tamper barrier. To close the root-of-trust gap, it defers all cryptographic key genesis to orbit, generating identity and attestation keys within co-located SEs after launch and distributes the hardware trust anchor across two independent vendors, so that no single party’s silicon is solely responsible for the attestation guarantee. The SEAP then binds these components to-gether, certifying not only what program is running inside the TEE, but also where , through a Byzantine-tolerant endorsement quorum of distributed ground stations. Section 5 details this architecture and Section 6 analyses its security properties and limitations. 

## 3 Related Work 

The trust problem for orbital computing — establishing that a work-load executes on a specific satellite, inside a genuine TEE, with keys that no terrestrial party can forge — has not been addressed by prior work. Existing research spans process-based isolation, VM-based confidential computing, lightweight embedded TEE de-signs, and distance-bounding protocols, but all operate under the assumption that the hardware resides on Earth. We survey the most relevant threads below and position Space Fabric relative to each. The TEE landscape spans process-based isolation, VM-based confi-dential computing, and lightweight embedded designs. Ménétrey et al. [ 36 ] survey attestation mechanisms across these platforms, not-ing that TrustZone lacks built-in remote attestation and must rely 

> 4Space Fabric: A Satellite-Enhanced Trusted Execution Architecture

on software-based solutions such as OP-TEE. The IETF RATS archi-tecture [ 10 ] formalises the roles of Attester, Verifier, Relying Party, and Endorser, defining Passport and Background-Check attestation models that SEAP’s exchange instantiates. Veraison [ 59 ] imple-ments this architecture for heterogeneous platforms. Pass, Shi, and Tramèr [ 44 ] provide the foundational formal treatment of attested execution, defining an ideal functionality Gatt in the UC framework that captures isolated execution, attestation, and sealing. We use this abstraction for Fabric’s combination of GoTEE, SE050, TROPIC01, and SEAP, which is concretely instantiated. Google’s Confidential Space [ 24 ] and Microsoft Azure Attestation [ 38 ] demonstrate how TEE-based attestation enables multi-party confidential computing in cloud settings; Space Fabric extends this model to orbital infras-tructure where physical inaccessibility provides a tamper barrier unavailable in terrestrial data centres. Recent work on Data Center Execution Assurance (DCEA) [ 47 ] and Intel’s Platform Ownership Endorsement (POE) [ 28 ] aims to close the gap between where a workload runs and what code it executes, suggesting the need for extended attestation that binds workloads to their physical en-vironments. A goal SEAP addresses through its satellite-binding mechanism. Keystone [ 32 ] is an open framework for customisable TEEs on RISC-V using Physical Memory Protection (PMP) and a small Secu-rity Monitor to support remote attestation through SBI calls. Sanc-tum [ 16 ], an earlier RISC-V enclave design, adds cache side-channel resistance. OpenTitan [ 34 ] pursues open-source root-of-trust sili-con with Physical Unclonable Function (PUF) based key derivation, though keys are still written to One Time Programmable (OTP) during a manufacturer-controlled personalisation step, preserving a pre-launch secret window. The Tropic Square TROPIC01 [ 57 ], used in our architecture, is a fully open and auditable secure element with published RTL, firmware, and a RISC-V core. WaTZ [ 37 ] demonstrates that WebAssembly runtimes can exe-cute inside ARM TrustZone with full Remote Attestation (RA) sup-port and a formally verified attestation protocol. Its small footprint (265 kB) makes it suitable for resource-constrained environments. Integrating a similar WASM runtime into Space Fabric’s GoTEE Se-cure World would enable general-purpose, multi-tenant workload deployment through the same SEAP pipeline, which we identify as future work. As an extension to SEAP, we can also consider distance bound-ing. Distance-bounding protocols, originating with Brands and Chaum [ 11 ], use tightly timed challenge-response exchanges to upper-bound the physical distance between the prover and veri-fier, countering relay and distance-fraud attacks. Drimer and Mur-doch [ 19 ] demonstrated practical distance bounding for smartcard relay attacks, and Avoine et al. [ 7 ] provide a comprehensive survey of the threat model. SEAP’s anti-relay mechanism is conceptually related but differs fundamentally: rather than measuring round-trip time at RF speed (impractical over satellite links with variable propagation delays), SEAP exploits the bounded channel corrup-tion window and a Byzantine quorum threshold to ensure at least one honest, uncorrupted Ground Station (GS) participates in the endorsement. The impossibility results in Section 6.3 make explicit that SEAP cannot provide classical distance-bounding guarantees. As Space Fabric ultimately serves as a security solution to satel-lites, we consider Falco [ 21 ], who surveys cybersecurity threats in the New Space era, identifying ground segments and RF links as primary attack surfaces. The emerging space edge computing para-digm, exemplified by Thales Alenia Space’s Imagin-E payload on the ISS [ 13 ] and ESA’s Φsat-2 CubeSat [ 20 ] — motivates on-board com-putational architectures that can be remotely attested and securely updated. The Trusted Computing Group’s CyRes specification [ 58 ]addresses hardware roots of trust for resilient satellite platforms, and SpiderOak’s OrbitSecure [ 53 ] applies zero-trust encryption to satellite data protection. To our knowledge, Space Fabric is the first work to combine TEE-enabled remote attestation with a formally defined satellite-binding protocol and on-orbit key genesis, address-ing the trust requirements of decentralized orbital computing where no prior architecture has operated. 

## 4 System Deployment & Threat Model 

The software- and hardware-level vulnerabilities together with the root-of-trust and supply-chain limitations discussed in Section 2 establish that terrestrial TEE deployments face risks that cannot be fully mitigated by hardware or software improvements alone. 

## 4.1 System Deployment 

Accordingly, the primary objective of Space Fabric is to enable verifiable execution of software on orbital infrastructure that the workload owner does not control. The on-board TEE hosts arbitrary applications, whether operator-managed services or third-party workloads deployed by external tenants onto infrastructure they have no physical or administrative access to. In both cases, the core requirement is the same: a verifier that directly interacts with the Trusted Application (TA) must be able to verify what software is running and that it executes within an attested, isolated environ-ment on the claimed orbital platform. Space Fabric addresses both scenarios through a single attestation mechanism: cryptographic evidence rooted in on-board hardware trust anchors, endorsed by a distributed GS quorum, that proves the identity and integrity of the deployed software, the isolation of the execution environment, and its binding to a specific satellite. These guarantees must hold even if the satellite operator behaves adversarially and individual ground stations are compromised, as long as a threshold of honest ground stations participates in the endorsement protocol. The Space Fabric strategy for tackling this goal consists of three principal ingredients: 

TA attestation The guarantee that a particular TA is deployed in-side a TEE is addressed with a hardware-reinforced secure bootstrap procedure binding TA to a TEE. 

RA attestation The guarantee that the TEE executes the expected software stack is addressed via RA. A verifier confirms both the integrity of the running software and the authenticity of the hardware producing the evidence. 

Proof of Execution Triangulation The guarantee that the TEE and RA attestations come from a satellite is addressed via a Proof of ET. Proof of ET is cryptographic evidence that a specific workload executed on a specific hardware satellite platform. GSs collectively attest to communicating directly with the satellite using publicly identifiable signing keys. 

> 5Rezabek et al.

Proof of ET is the overarching security goal: cryptographic evi-dence that a specific workload executed on a specific orbital plat-form. SEAP, detailed in Section 5.3, is the concrete protocol that achieves this goal by orchestrating the challenge-response exchange between the satellite’s TEE and the distributed GS quorum. In other words, Proof of ET defines what the verifier needs to be convinced of, while SEAP defines how that conviction is established. We model the problem as a game between three main parties, a satellite S1, ground stations GS , and a tenant T - the party that deploys workloads onto, or relies on attestation evidence from, the orbital platform without trusting its operator. The following assumptions are made about the parties and the communication among them: 

Assumptions about satellites. A satellite has limited computation capabilities, as outlined in Section 2.4. Nevertheless, it can perform arbitrary computations. To meet Space Fabric’s requirements, each satellite carries a TPM and a TEE. We denote the TEE of satellite as 

STEE . As a feature of TEE, it can run arbitrary operations and has access to an encrypted storage that is mounted to the TEE during runtime. As used in current deployments on Earth, we assume that the storage’s encryption key is given to the TEE directly via a secure communication channel or can be read from via a driver from e.g., TPM or HSM. Satellites are geographically restricted, and at any given time, a satellite can communicate with a limited set of ground stations (or other satellites) along its trajectory. The trajectory of every satellite and its geographical location are publicly available [ 1 ]. These restrictions determine the characteristics of communication between S and GS , as detailed below. 

Assumptions about ground-stations. GSs are the on-Earth com-ponents for communication. There exists a set of GSs GS . For sim-plicity, a GS can be the physical station or a compute unit in its vicinity that delivers data for communication with a satellite. GSs are geographically distributed, and at any point in time, each GS is located within a specific position. GS can be static or dynamic. Static corresponds to a large disk satellite, and dynamic can, e.g., be a terminal. Depending on that, the position of each GS is known or unknown to both satellites and other GSs. We assume that all GSs have access to a global, synchronized clock, e.g., via Network Time Protocol (NTP). 

Assumptions about satellite-groundstation communication. An honest GS communicates with the satellite (only) via a direct down-link/uplink part of the time, e.g., during a window of 10 minutes out of every 90-minute period (an “orbit” period). As mentioned above, based on a geographically known schedule of the satellite orbit, 

GS can predict windows of time in which direct communication between a GS and S is possible. 

Assumptions about tenant ground station communication. Tenant does not have direct communication with the satellite, and relies on GSs to mediate all communication with S. It deploys workloads into the enclave and communicates with the satellite exclusively through the GSs.    

> 1A separate work will address a world with multiple satellites and establish coordina-tion protocols among them. Veri fi er
> V.
> Comms & Internal Systems
> External Comms
> Hardware
> Host OS
> App App TA
> Ground Station Provider Setup comms with ground station Manages traf fi c between Earth & satellite Satellite Unit
> HW Payload Owner
> Satellite Operator S.
> TA Manager
> M.
> O.
> G. Link
> T.
> Veri fi er interacts with a TA

Figure 3: Overview of parties in the threat model. Each party’s color is used in subsequent figures as a line color. Depending on the scenarios, some parties may overlap in their roles. 

Assumptions about identities. Identifiers: Both satellites and GSs have unique identifiers, in-cluding newly joining parties, which are publicly known by all other parties. When a communication channel is established be-tween a GS and a satellite, each party can retrieve the other’s unique identifier, such as the Space Vehicle Number (SVN). 

Public Key Infrastructure (PKI):. We assume the existence of a PKI to which both satellites and GSs have access. Specifically, each GS GS has its public key vk G S . This key is relevant for signing, such that GS can sign messages using the corresponding private key sk G S . The key vk G S of node GS is known to all GSs in GS and all satellites in S. We assume that satellites do not implement PKI. 

## 4.2 Threat Model 

We now explore in greater depth the entities that control HW and SW provisioning for satellites and ground stations. By inspecting all internal hardware components and the bootstrap procedure, we gain insights into the adversarial threats that impact the security of various attestations. 

4.2.1 Setting & Trust Assumption. Figure 3 depicts the deployment architecture and entities involved in platform launching in mode details: 

Verifier V ○ anyone who interacts with a TA running on the satel-lite and independently obtains attestation evidence directly from the TEE to confirm what software they interact with, without having deployed it and without needing to trust the Tenant. 

Ground Station Provider G ○ operates the ground station infras-tructure. The Ground Station Provider establishes a com-munication channel with the satellite and forwards traffic between terrestrial clients and the orbital platform. 

Satellite Operator S ○ who operates and manages the satellite unit, including orbital control and communication schedul-ing (may coincide with O ○ in some configurations). 

HW Payload Owner O ○ the entity that owns and provisions the physical hardware payload aboard the satellite, including the TEE, on-board compute, and any embedded TPM or root-of-trust component. 

TA Manager M ○ is responsible for managing the lifecycle of TAs running within the satellite’s trusted execution environ-ment. The TA Manager orchestrates the measured launch 

> 6Space Fabric: A Satellite-Enhanced Trusted Execution Architecture

of trusted applications on the payload. In some deployments M○ coincides with T○ (a tenant deploying and managing their own workload); in others M ○ is the payload owner O ○

or satellite operator S○, and T○ is an external party that relies on attestation to verify what M ○ deployed. The HW Payload Owner O ○ is trusted to provision genuine hard-ware and to maintain physical integrity of the payload prior to launch. It is also assumed that the payload has not been physically tampered with post-deployment, and no party engages in attacks that require direct physical access to the orbital hardware. In par-ticular, the measured launch process for the on-board TEE and its certificate-issuance infrastructure are assumed to be verifiable. The Space Fabric Infrastructure is a composite of the HW Pay-load Owner O ○, the Satellite Operator S ○, and the Ground Station Provider G ○. All software and communication software that make up these components constitute the satellite infrastructure and are considered potentially adversarial, requiring at least a threshold number of GSs to be honest. These include communication SW controlled by S○, OS software and utilities provided by O ○, and the entire SW stack on the ground provided by G ○. Therefore, these sys-tems are not trusted to maintain confidentiality for data traversing the communication links or processed in software-visible compo-nents on the GS, such as the host OS and application layer, which are fully accessible to the GS operator; and integrity guarantees are considered meaningful only when they are cryptographically verifiable. It is achieved, for example, through measured boot flows on the satellite payload, sealed keys, and hardware-generated at-testations relayed over the communication channel. The primary objective is to provide assurance to the tenant T ○ and verifier V ○

that the deployed workload executes within a trusted on-board TEE whose hardware integrity was verified prior to launch and remains uncompromised in orbit — regardless of whether T ○ deployed the workload themselves or is an external party V ○ verifying software managed by M ○.

4.2.2 Adversary Capabilities. 

Satellite Compromise. The adversary may control the satellite’s entire software stack outside STEE , including the host OS, all pro-cessing units, and the host-TEE I/O path — intercepting all data transferred to and from STEE . However, STEE itself is assumed se-cure: A cannot launch side-channel or physical attacks. 

Ground Infrastructure and Node Corruption. The adversary con-trols the ground station host OS, application layer, and commu-nications stack, and may collude with S○ or malicious insiders. Additionally, A can adaptively corrupt up to 𝑡 GS = ⌊ 𝑡  

> 100

· 𝑛 ⌋ of the 

𝑛 Earth-based nodes during SEAP’s execution, gaining complete control over corrupted nodes’ private state and actions. Corruption is irreversible. 

TEE Access. The adversary controls an arbitrary number of TEEs on Earth and their communication channels. On-Earth communica-tion between adversarial nodes and TEEs is assumed instantaneous: if A intercepts a message destined for a satellite, it can relay it to a local TEE and receive a response without measurable delay. 

Communication Channel Attacks. A can act as a Man-in-the-Middle (MitM) on any GS’s communication channel — intercepting, Operator     

> Hardware
> Host OS
> App TA
> Satellite Unit
> Encrypted Channel
> SEAP Client
> S. TPM
> App SEAP Server
> O.
> 1
> 2
> 34

Figure 4: General overview of the Space Fabric Architecture including the SEAP components, and trusted components (in orange) of TA and TPM. 

delaying, reordering, dropping, replaying, or injecting messages — without accessing the GS’s private signing key. We impose three restrictions: (i) channel corruption persists for at least 𝑊 seconds (rolling windows); (ii) at most 𝑡 ch channels can be corrupted simulta-neously; and (iii) if both a GS and its channel to S are uncorrupted, then every message received by the GS on behalf of S was com-puted on the satellite. Intuitively, (i) and (ii) model an adversary that cannot reposition faster than the satellite orbits, while (iii) ensures that relaying a message to Earth, computing a response, and re-turning it to the satellite cannot be done within the communication window. 

## 5 Space Fabric Architecture 

The main technical objective of Space Fabric is to facilitate the certified provisioning and execution of trusted software into space. The key novel aspect is to certify not only what program is running inside a TEE, a traditional RA offering, but also certify 

where (i.e., in space) this TA runs. As this is a crucial part of the Space Fabric’s proposition, we devised a protocol that leverages the space’s native capabilities and communication means. In addition, certain challenges arise from existing gaps in tee deployment and execution certification. To address these challenges, Space Fabric includes three main components, as shown in Figure 4: 

Secure-boot A HW-assisted bootstrap process, proving trusted host-OS deployment on a TEE or compute node using se-cure boot, 

RA TEE A remote attestation utility, proving execution on a TEE based on Arm TZ, 

SEAP A location certification service, proving TEE deployment in space, a unique instantiation of Proof of ET. We focus on a single-satellite model, but the approach is general-izable to any satellite as long as it meets the hardware requirements and fits the general setting. Figure 4 provides an overview of the components and the flow. SEAP has two components: the on-Earth verifier/tenant 1 ○ commu-nicating via an encrypted channel 2 ○ with the server 4 ○ running within the TA 3○. We then have TPM and TEE shown in the form TA. The TEE is now visualized as the process-based TEE, as it is the current implementation we follow, but the SEAP protocol is applicable also to the CVM, as long as the requirements and threat model of the TEE provides the same security guarantees. 

> 7Rezabek et al.

## 5.1 Root of Trust 

Space Fabric’s root of trust addresses a fundamental limitation shared by existing commercial TEE platforms: the reliance on se-crets provisioned into hardware before deployment. Space Fabric eliminates this window by combining three mechanisms. The SoC’s verified boot facility establishes an immutable first link in the boot chain: the Boot ROM verifies the Trusted OS image against a pub-lic root key fused into OTP, ensuring that only an authenticated software stack executes in the Secure World. Crucially, this fused key is a verification key, not a secret. While a single party holds the image-signing key (unless using, e.g., threshold cryptography), this does not mean a single point of trust because the attestation flow independently measures the running software, and the verifier appraises those measurements against reference values established before launch. So if the relevant software artifacts are reproducible, it is possible to verify the details as part of the attestation flow. Second, all cryptographic signing keys, both satellite identity and attestation keys, are generated on-orbit by co-located SEs on first boot after launch. Private keys are flagged as internally gen-erated and non-exportable under hardware-enforced per-object policies. No secret material exists on Earth at any point; the veri-fier’s pre-launch trust anchor consists solely of public artifacts such as factory serial numbers, device certificates, SoC identifiers, and configuration hashes. These artifacts are safe to disclose because they bind attestation to a specific physical device without convey-ing any signing capability. Even if an adversary provisions a second, identically configured device on Earth, the on-orbit key genesis ensures that the two devices produce distinct key pairs. This argu-ment assumes that key slots are verified to be empty prior to launch. This would result in different public keys than those endorsed by the GS quorum during SEAP, as we introduce in Section 5.3. The verifier therefore rejects attestation evidence from the clone, since its public keys do not match those bound to the satellite’s certificate of authorization. A subtler concern is not hardware cloning but premature key generation on the legitimate device: an attacker with pre-launch physical access could power the board and trigger key generation before launch, producing keys the attacker cannot extract but that would occupy the key slots before the intended on-orbit genesis. However, the PTA’s attested boot logic checks that all SE key slots are empty before generating keys. As the PTA binary is measured, the code is enforcing this property. Besides, we rely on the fuses for differentiation between testing and production mode, and checks using the monotonic counter to assess the state on first boot. The measured boot process complements key generation by pro-ducing a tamper-evident record of the platform’s software state via the PCR extension. The on-orbit key genesis provides the crypto-graphic foundation on which SEAP and the TEE RA flow are built. Section 7 details the concrete hardware instantiation and additional implementation information. 

## 5.2 RA of Trusted Execution Environment 

Next, Space Fabric provides remote attestations (RA, i.e., signed at-testations every time the deployed software produces an externally visible output. The current configuration uses ARM TZ, so Space-Fabric needs to address the lack of native RA capabilities. However, Relaying Party   

> Normal World Secure World Secure Monitor
> Trusted App
> Attestation TA
> Client App Veri fi er
> Provisioner
> 1.
> 2.
> 0.
> 3.
> 4.
> 5. Req RA Nonce Setup Trust Anchor Req RA w/ nonce Req Evidence RA Result
> 6. Secure Channel

Figure 5: Overview of RA for ARM TZ following OP-TEE ap-proach [ 54 ]. The overall goal is to provide a flow for assessing the TA and ensure freshness to the request. 

several solutions have been discussed, such as OP-TEE [ 54 , 61 ]. Figure 5 highlights the flows using the so-called "Passport Model" known from IETF RATS [ 10 ]. In a pre-deployment provisioning step 0○, the Provisioner loads the attestation public key, signer identity, and reference TA hashes into the Verifier. At runtime, the client app initiates the attestation 1○ and receives a fresh nonce from the verifier. This nonce is forwarded through the Normal World client App into the Secure World, where the TA passes it to the Attesta-tion Pseudo TA (PTA), a privileged pseudo-TA built into OP-TEE that holds the signing key 3 ○. The PTA measures the loaded TAs, packages the measurements with the nonce into a signed token, and returns this evidence back through the chain 4○. Verifier vali-dates the signature against the provisioned trust anchor, checks the nonce freshness, compares TA hashes to reference values, and re-turns an attestation result 5○. The critical architectural point is the split between the Attestation PTA (which owns the secret signing key and runs as part of OP-TEE’s kernel) and the TA (which is the actual application being attested). The PTA measures the TA from a position of higher privilege; it can see the TA’s binary hash, but the TA cannot access the PTA’s signing key. This separation means the evidence is signed by a component that the TA cannot tamper with. If attestation succeeds, the Relying Party establishes a secure channel directly with the TA 6○, trusting that it is communicating with verified software running in an isolated TZ environment. 

## 5.3 Satellite Execution Assurance Protocol 

Last, Space Fabric needs to certify that remote attestations originate from a genuine space TEE which is deployed in orbit. The challenge is that a TEE on earth might emulate the space platform design identically. It is not easy for a GS to verify that it is communicating directly with a satellite. The key in Space Fabric is that it would be hard for an adversary to compromise the direct satellite communi-cation links of all ground stations at once. Leveraging this point, Space Fabric certifies TEE location through a distributed consensus protocol among a network of GSs. The protocol is designed around the unique challenges of sporadic, limited communication in space. 

TEE setup. Initially, S’s TEE, denoted by STEE has access to the PKI, i.e., it has received (as part of its setup) vk G S for every GS ∈ 

GS .

Satellite key generation. The first step that STEE does after going online is to create a new signing key pair, ⟨vk S, sk S ⟩. Note that, since this is generated within S’s TEE, sk S is correctly created and remains private throughout the execution. 

> 8Space Fabric: A Satellite-Enhanced Trusted Execution Architecture

Ground Station “hello”. When S is geographically close to an Earth-based GS GS , it is possible for GS to communicate directly with it. To do so, GS first picks a random nonce 𝜙 and sends to S

a signed “hello” message: hello , GS 𝜙, 𝜎 G S ,hello 

where 𝜎 G S ,hello = Sign (⟨ 𝜙, hello ⟩, sk G S ). GS also records the time when the session, identified by 𝜙 , started, i.e., when GS sent the “hello” message to S.

Satellite “hello-ack”. When STEE receives an incoming hello mes-sage by a GS GS , it first checks whether Verify (vk G S , ⟨𝜙, hello ⟩,𝜎 G S ,hello ) = 1. If so, then STEE responds with a hello-ack message as follows: hello-ack , S𝜙, vk S, ΠTEE , 𝜎 S,𝜙 

where 𝜎 S,𝜙 = Sign (𝜙, sk S ) and ΠTEE is a TEE quote, i.e., proof that 

vk S was generated inside a TEE. 

Ground Station “key-verify”. When GS receives a hello-ack mes-sage as above, it checks the following: (i) if the session identified by 

𝜙 started within 𝑊 seconds; (ii) if Verify (vk S, 𝜙, 𝜎 S,𝜙 ) = 1; (iii) if 

ΠTEE is valid. If all checks hold, then GS creates the following sig-nature 𝜎 G S ,ts ,S = Sign (⟨ vk S, ts ⟩, sk G S ), where ts is the timestamp of GS ’s local clock at the point of the signature generation. GS 

then creates a key-verify message, as below, and sends it to S:key-verify , GS ts , 𝜎 G S ,ts ,S

Certificate of identification. After receiving a key-verify mes-sage, STEE first checks that the signature is valid, i.e., whether 

Verify (vk G S , ⟨vk S, ts ⟩, 𝜎 G S ,ts ,S ) = 1. If the signature is valid, then 

STEE stores the message and attempts to create a certificate of au-thorization. To do so, STEE retrieves 𝑡 GS + 2 · 𝑡 ch + 1 of the stored 

key-verify messages, where the timestamps of any two messages in this subset differ by no more than 𝑊 seconds. When S has received 𝑡 GS + 2 · 𝑡 ch + 1 such key-verify messages, it concatenates them to create a certificate of authorization 𝐶 S , which is also signed to prove that the concatenation was done inside the TEE. For any subsequent message signed by sk S , STEE can provide 

𝐶 S to convince any party that it has successfully completed the identification protocol. Appendix A introduces algorithm 1 and algorithm 2 which are run by STEE and GS respectively. 

## 5.4 Combining TEE RA with SEAP 

After identifying the RA approach for the TEE and SEAP flows, we must combine them to provide users with confidence that the satellite actually operates in space as part of the attestation flow. We consider the SEAP to run only once, until a sufficient number of votes is collected, as part of the setup. Nevertheless, the lifecycle of the protocol specifics is outside of the scope of the paper. Figure 6 covers the sequence of the steps for integration of SEAP with RA flow. During setup, the satellite’s TEE performs a verified boot and generates a fresh identity key pair <vk 𝑆 , 𝑠𝑘 𝑆 > inside the Root of Trust (RoT), where 𝑠𝑘 𝑆 is hardware-bound and never ex-ported. When a ground station 𝐺𝑆 𝑖 comes into range, it initiates a ... Satellite Root of Trust Setup  

> Generate identify key pair
> Repeated per GS in range Measure TA +
> Verify

Figure 6: SEAP combined with TEE RA. The satellite’s TEE generates a fresh identity key pair within the Root of Trust. Each GS within range executes a three-message exchange. If all checks pass, GS endorses the satellite’s public key with a timestamped key-verify signature. 

three-message SEAP exchange: 𝐺𝑆 𝑖 sends a signed hello with a ran-dom nonce 𝑟 𝑖 , which the TEE verifies against the pre-provisioned ground station PKI. The TEE then runs an internal attestation sub-protocol - measuring its running software, packaging the claims with 𝑟 𝑖 , and having the RoT sign the evidence to produce 𝜋 𝑇 𝐸𝐸 . The TEE also signs 𝑟 𝑖 with 𝑠𝑘 𝑆 and returns everything in a hello-ack 

message: the satellite’s public key vk 𝑆 , the TEE attestation evidence 

𝜋 𝑇 𝐸𝐸 , and the signed nonce 𝜎 𝑆 , 𝑟 𝑖 . The ground station performs three checks: (i) the session is within the time window 𝑊 , (ii) the nonce signature 𝜎 𝑆 , 𝑟 𝑖 verifies under vk 𝑆 , and (iii) 𝜋 𝑇 𝐸𝐸 appraises successfully via the Verifier, confirming the key was generated in genuine, correctly configured hardware. If all checks pass, 𝐺𝑆 𝑖 en-dorses vk 𝑆 by signing it with a timestamp and returning a key-verify 

message. The TEE stores this endorsement. As the satellite orbits and contacts successive ground stations, it accumulates endorse-ments until it holds 𝑡 GS +2·𝑡 ch +1 valid ones within the time window — enough to tolerate 𝑡 GS adversarial GSs and 𝑡 ch adversarial relay channels. The TEE then bundles these endorsements and signs them via the RoT to produce 𝐶𝑒𝑟𝑡 𝑆 , a certificate of authorization that any relying party can verify against the GS PKI without re-running the attestation protocol. 

## 5.5 Ground Station Committee Update Protocol 

As the SEAP plays a crucial role in security, it is important to briefly consider the lifecycle of GS on Earth. The GS on-Earth can join and leave the respective committee, and it is crucial that the number of members does not fall under the security threshold of votes collected by the satellite. Essentially, the process will result in the public key of a new GS being added to the registry of public keys to which the GSs and satellites have access. 

> 9Rezabek et al.

Handover. We treat the existing set of GSs as a committee. When a new committee GS ′ needs to be formed, then the GSs in GS 

create a handover certificate which establishes the new GS ′ as the canonical committee. This certificate includes the public keys of 

GS ′ and a signature from at least 𝑡 GS + 1 parties from GS .

Key rotation. When a new committee is being established, each honest GS GS does the following: (1) Creates a fresh public key vk ′G S and is registered to the new committee with it; (2) Securely removes the old key vk G S after the handover cer-tificate is issued. Finally, when a handover certificate is created, the honest GSs send it to the satellites, who in turn update their local set of com-mittee public keys. 

## 6 Security Analysis 

This section outlines the general goals of the Space Fabric to the user, the properties of the protocol, and several attacks we consider along with their mitigation. 

## 6.1 Security Goals 

Motivated by the terrestrial TEE vulnerabilities surveyed in Sec-tion 2, we design Space Fabric, which covers TPM, SEAP, and the use of a TEE on a satellite. Overall, Space Fabric is designed to provide a ground-based (or space-based) verifier with strong, enforceable guarantees about code executing aboard an orbital platform. At a high level, the verifier must be convinced that attestation evidence originates from a genuine TEE resident on a specific satellite, is cryptographically bound to that physical platform, and cannot be substituted, replayed, or proxied from a terrestrial or alternative orbital host. 

G1 — Authentic Orbital Execution: The verifier must obtain ev-idence that the attested workload executes within a TEE on the claimed satellite and not on any ground-based surrogate or emu-lated environment. Evidence must be unforgeable and rooted in hardware-backed keys provisioned after launch, and the satellite’s physical inaccessibility after deployment provides a passive tamper barrier absent in terrestrial deployments. 

G2 — Root-of-Trust Integrity: The signing keys underpinning at-testation must be device-unique and must not have been extractable during manufacturing or integration. Overall, it is essential that the keys are generated on the device. 

G3 — Platform Binding and Anti-Relay: Attestation reports must be cryptographically bound to the unique identity of the on-board platform. A malicious GS or a compromised communication relay must be unable to substitute evidence from another GS. Nonce-binding and freshness mechanisms must remain effective even when all communication channels between the satellite and verifier pass through potentially adversarial ground infrastructure. 

G4 — Measurement Freshness and Consistency: Evidence must reflect the current execution state of the TEE at the time of the request. Stale or pre-recorded attestations must be detectable. 

G5 — Auditability Under Supply-Chain Uncertainty: Because satel-lite hardware undergoes integration across multiple actors prior to launch, the protocol must not require the verifier to trust any sin-gle vendor attestation service. Reference measurements and trust anchors must be established and sealed prior to launch, allowing post-deployment verification to proceed without live interaction with the hardware manufacturer. We explicitly exclude post-launch physical attacks on the space-craft bus as outside our threat model, consistent with the assump-tion that on-orbit hardware is physically inaccessible to adversaries. Supply-chain trojans introduced before launch are similarly ex-cluded, though G5 limits the degree of trust extended to any single integration party. Together, these goals define the basis for analyz-ing SEAP’s protocol construction in the following sections. 

## 6.2 SEAP Properties 

We outline the security rationale of the protocols of Section 5.3 and Section 5.5. First, we define the properties that the protocols should aim to guarantee. We outline, on a high level, how these properties are achieved. 

6.2.1 Satellite Identification. The satellite joining protocol of Sec-tion 5.3 should aim to satisfy two main properties, satellite availabil-ity (Definition 1) and correctness (Definition 2). Availability ensures that all satellite-based TEEs eventually join the system, whereas correctness guarantees that no Earth-based TEEs join. 

Definition 1 (Satellite Availability). Let STEE be a TEE hosted on a satellite S. Availability is guaranteed, with parameter 𝑢 ,if STEE successfully completes the identification protocol of Section 5.3 and produces a certificate of authorization at most 𝑢 seconds after 

S’s stabilization. 

Definition 2 (Correctness). Let S ˆTEE be a TEE that is not 

hosted on any satellite. Correctness is guaranteed if S ˆTEE does not pro-duce a certificate of authorization (unless with negligible probability). 

6.2.2 Committee Update. We now outline the properties that the committee update protocol should guarantee. The first property, 

committee availability (Definition 3), posits that the adversary should not be able to block a committee update. Second, the update protocol should guarantee that the adversary cannot control at any point more than 𝑡 % of any historical committee’s GSs; we call this property resistance to posterior corruptions (Definition 4). 

Definition 3 (Committee Availability). Let GS be a commit-tee of Earth-based GSs. If all honest GSs in GS are given as input a new committee GS ′, then a handover certificate from GS to GS ′ is created with overwhelming probability. 

Definition 4 (Resistance to Posterior Corruptions). Let 

GS 0, . . . , GS 𝑘 be a sequence of committees, such that there exists a correct handover certificate for any pair (GS 𝑖 , GS 𝑖 +1), where 𝑖 ∈[0, 𝑘 −1]. For every committee GS 𝑖 , each party GS ∈ GS 𝑖 be identified by a public key vk G S ,𝑖 (with a corresponding private key sk G S ,𝑖 ). At every point in time and for every committee GS 𝑖 , it should be infeasible for the adversary to create an aggregate signature that comprises signatures from more than 𝑡 · | GS 𝑖 | of the committee’s parties. 

> 10 Space Fabric: A Satellite-Enhanced Trusted Execution Architecture

## 6.3 Security Rationale 

The security rationale of section 5.3’s protocol is based on the following insights. 

6.3.1 Satellite Identification. First, it suffices to prove that at least one honest GS, whose channel is not corrupted, attests that a TEE 

STEE is on a satellite. If an honest GS GS ’s channel is uncorrupted, then, for any messages that GS receives which are proven to be created by a TEE, GS is assured that said TEE is on a satellite, due to the discussion in Section 4.1. Second, the TEE attestation Πvk TEE proves that the public key 

vk S was created inside a TEE that runs the given protocol. This suffices as proof that every signature, which is verifiable by vk , was created by a TEE that runs Algorithm 1. This guarantees that, first, 

A does not have direct access to sk S , i.e., cannot create arbitrary signatures. Second, it proves that the creation of the certificate 𝐶 S

is conditional on performing the timestamp window check of Line 1 (Algorithm 1). Third, at any point in time, an adversarial TEE which is based on Earth can collect attesting signatures from at most 𝑡 GS + 2 · 𝑡 ch .Since 𝑡 GS GSs are adversarial, meaning that A can issue signatures arbitrarily, the main argument here concerns channel corruptions, as follows. The protocol requires that each session between a GS and a satellite, which is identified by a nonce 𝜙 , lasts at most 𝑊 

seconds (cf. Algorithm 2). Additionally, by assumption, each channel corruption lasts at least 𝑊 seconds. Therefore, at any point in time, an adversarial TEE can have at most 𝑡 ch active sessions with different GSs. The first observation is that TEE does not know when a channel corruption starts. The only information that TEE has is the times-tamp (signed by GS), i.e., the timestamp of the interaction with GS. Therefore, TEE can check whether the received timestamps, i.e., the interactions with GSs, occurred within a certain time window, which depends on 𝑊 .The second observation is that communication between a GS and a GS over a corrupted channel lasts less than 𝑊 seconds. Therefore, it is possible that, within some window of 𝑊 seconds, the TEE communicates with two GSs whose channels were corrupted within more than 𝑊 seconds. For example, let 𝑊 = 6 seconds and 𝑡 ch = 1.Now, imagine that GS GS 1’s channel is corrupted at time 𝑡 0 and 

GS 1 completes the protocol with a TEE at time 𝑡 0 + 3. Next, at time 𝑡 0 + 6 A releases GS 1’s channel and corrupts the channel of GS GS 2. Following, GS 2 completes the protocol with the TEE at time 𝑡 0 + 7. Observe that: (i) the adversarial channel corruption assumption is satisfied (since no two channels were corrupted at the same time) and (ii) the TEE completes the protocol with both GSs within 4 < 𝑊 seconds. By extending the attack to an arbitrary number of corrupted channels, we conclude that an adversarial TEE can obtain signatures from at most 2 · 𝑡 ch GSs with corrupted channels. 

Note: One might be tempted to resolve the attack by setting the signature threshold at 𝑡 ch and decreasing the timestamp win-dow check (Line 1 of Algorithm 1). However, this line of defense would not work, because A controls message delivery between the GS and the TEE, so it can control the timestamp of the GS’s messages. In the example of the above paragraph, assume that the TEE checks whether any two interactions happened within  

> 𝑐ℎ𝑎𝑛𝑛𝑒𝑙𝐶𝑜𝑟𝑟𝑢𝑝𝑡𝑖𝑜𝑛𝑊 𝑖𝑛𝑑𝑜𝑤
> 2

= 3 seconds. In that case, A can ensure that GS 1’s interaction happens on time 𝑡 0 + 5 and GS 2’s interac-tion happens at time 𝑡 0 + 7, which would again bypass the check. Essentially, A can perform the first GS’ interaction arbitrarily close to the end of the first corruption window and the second GS’ inter-action arbitrarily close to the beginning of the second corruption window, such that the time difference between them is arbitrarily small. In conclusion, by requiring that the satellite’s certificate of au-thorization include signatures from 𝑡 GS + 2 · 𝑡 ch + 1, we ensure that at least one honest GS performed the identification protocol with the satellite’s TEE over an uncorrupted channel. 

6.3.2 Committee Update. Rotating committees is a well-known and solved problem in cryptographic literature. Since all committee members are identifiable by public keys, creating a certificate signed by at least one honest party guarantees that the new committee is correct. Regarding availability (Definition 1), we remind that the ad-versary A controls a percentage 𝑡 % out of all 𝑛 GSs. Since the certificate must be signed by at least one honest party, the signers must be at least 𝑡 GS + 1. If a majority of GSs is adversarial, that is, if 𝑡 > 50 ⇔ 𝑛 ≤ 2 · 𝑡 GS + 1, then the number of honest GSs is smaller than 𝑡 GS + 1. In other words, a certificate would need to be signed by at least one adversarial party, which, in turn, means the adversary can block all certificate issuance, thereby violating availability. Therefore, to guarantee availability, it should hold that 

𝑡 < 50 ⇔ 𝑛 ≥ 2 · 𝑡 GS + 1, i.e., , the majority of GSs is honest. Regarding posterior corruptions, let us first provide intuition as to why this property is needed. Consider the following scenario. The first committee consists of 10 parties, none of which are cor-rupted. After the first committee changes, the second committee consists of 30 parties. At this point, A corrupts the 10 parties of the first committee; note that the honest majority requirement (needed for availability) is still satisfied for the latest committee. Afterwards, a satellite S whose TEE, upon deployment, holds the “genesis” public key set of the first committee, joins the network. Since A control all communication of STEE , A can prevent TEE S

from obtaining the handover certificate, so STEE can complete the protocol of Section 5.3 by only communicating with corrupted GSs and, essentially, never join the system. To avoid this hazard, we observe that the root cause of it is pos-terior corruptions, i.e., the ability of A to corrupt a GS GS and use the key that GS used in all past committees. If resistance to posterior corruptions (cf. definition 4) is guaranteed, then A cannot produce signatures for the keys of old committees. In our protocol, this is ensured by the key-deletion step after each handover com-mittee is created. By evolving its key, even if GS is corrupted later, 

A cannot create signatures on behalf of GS for past committees. Essentially, this property ensures that the keys of all committees satisfy the adversarial corruption bound at all times. Therefore, as long as one committee authorizes a new satellite S’s key, S does not have to get re-authorized with every committee change. 

## 6.4 Challenges 

We now present a set of challenges that delineate the limits of the goals our protocol can achieve.        

> 11 Rezabek et al. Satellite
> Satellite TEE
> Malicious TEE
> iii Reply Message
> 1Message
> 2Reply
> cReply
> bMessage Reply d
> aMessage

Figure 7: Illustration of a relay attack. In the first scenario (messages 1-i-ii-2), the computation is performed (correctly) in the satellite-based TEE. In the second scenario (messages 1-a-b-c-d-2), the computation is done (adversarially) in an Earth-based TEE. In our identification protocol, both scenar-ios are indistinguishable from the GS ’s perspective. 

C1: Adversarial Satellite Control. As described in section 4.2, A

controls the entire satellite except STEE . In this setting, availability 

cannot be guaranteed. Because A controls all communications channels of STEE , it can completely block all communication from/to 

STEE and isolate it from every other party. 

C2: Adversarial Message Relay. In Section 4.2, we required that if a GS GS and its communication channel with a satellite S are uncorrupted, then messages sent to GS by S (if any) are computed 

on the satellite . This requirement is necessary, since correctness 

cannot be guaranteed otherwise. We illustrate this impossibility in Figure 7. In detail, we consider a setting where an Earth-based GS sends a message to a TEE and receives a reply. Here, there exist two scenarios. In the first scenario, outlined by the messages 1-i-ii-2, the computation of the reply occurs within a TEE that is based on the satellite. This is the honest scenario, where the satellite forwards the messages correctly to its TEE. In the second scenario, identified by the messages 1-a-b-c-d-2, the computation occurs in a TEE stationed in an Earth GS. Here, the satellite relays the GS’s message back to an Earth-based station, which then uses a TEE based on Earth to produce the computation. Note that, since we assume that A controls all satellite operations except the TEE, A can intercept the original message and relay it as shown. In the context of our identification protocol, the Earth-based GS, which sends the original message, has no information about the satellite’s TEE. Therefore, the two scenarios shown above are indistinguishable from the point of view of the GS. For this reason, if the second scenario (relaying to Earth and back) is possible, then the GS would authorize the public key produced within the Earth-based TEE, even if both the GS and the communication channel with the satellite are uncorrupted. Therefore, we consider the combination with on-board TPM, which could be registered prior to the launch and the TEE flow, or the TEE flow with general keys registered on-Earth before launch in the Veraison with HSM. 

C3: Adversarial Channel Control. In Section 4.2, we required that A can corrupt the communication channels of at most 𝑡 ch Satellite      

> Satellite TEE
> Malicious TEE
> 1Message
> 6Reply
> 4Reply
> 3Message Reply 5
> 2Message
> MitM

Figure 8: Illustration of a MitM attack. The adversary con-trols the communication channel between the GS and the satellite, so it blocks all communication towards the satellite and forwards all messages to an Earth-based TEE that it con-trols. 

Earth-based GSs at any point in time. This assumption is necessary; otherwise, if all channels are corrupted, then neither availability 

nor correctness can be guaranteed. Essentially, when an Earth-based GS GS ’s channel is corrupted, 

A can act as a MitM and forward all messages to an adversar-ial Earth-based GS without being identified by GS . If A can do this for all GSs, i.e., if no GS can communicate with the satellite, then it is impossible to authorize the satellite’s TEE (availability attack). Additionally, because the attack is non-identifiable by GS ,the Earth-based adversarial TEE may complete the identification protocol and obtain an authorization certificate (correctness hazard). The MitM attack is illustrated in Figure 8. 

C4: GS Location Proof. A problem that should be discussed is whether and how an Earth-based GS GS can prove to a satellite S

its location, i.e., whether GS is on Earth (as opposed to in space) or in a specific geographic area on Earth. Recall that some of the GSs are adversarial, so a simple signed message by the GS is not suffi-cient proof for S, which does not know which GSs are corrupted. In our setting, solving this problem is not possible due to the control that the adversary A has on the satellite’s system. Essen-tially, A controls all data that the satellite’s TEE sends and receives. Therefore, as with the availability impossibility discussed above, 

A can block any proofs sent by GS . Even if communication with 

GS is permitted, though, STEE does not have direct access to the satellite’s sensors. Therefore, any information regarding the satel-lite’s geolocation that can be intercepted by A. Since STEE ’s only information about GS is the public key vk G S , not having access to any such data makes it impossible for S to validate any claims by 

GS about its geographical position. 

## 7 Space Fabric Implementation 

This section outlines the instantiation of Space Fabric and its im-plementation of SEAP and TEE with unique RoT. Next, we revisit the security goals and argue how they are answered.  

> 12 Space Fabric: A Satellite-Enhanced Trusted Execution Architecture USB Armory MK II
> Normal World
> Secure World Secure Monitor & Secure Boot
> SEAP TA
> Attestation TA
> SEAP App SE050 TROPIC01
> Raspberry Pi
> TPM
> Host OS
> Relay App
> USB

Figure 9: Space Fabric on the satellite platform. The Rpi 5 forwards traffic over USB to the USB Armory Mk II. The Armory runs the SEAP client app in between the normal and secure worlds. The two SEs are attached directly to the Secure World to provide key storage and attestation keys. 

## 7.1 Attestation Flow of SEAP and TA 

We instantiate the SEAP and RA protocol on a USB Armory Mk II (NXP i.MX6UL, ARM Cortex-A7) connected via USB to a Raspberry Pi 5 host acting as an untrusted relay [ 8 ], as shown in Figure 9. The satellite’s TEE is implemented using ARM TrustZone, with GoTEE as the Trusted OS running in Secure World, and a PTA built into OP-TEE that handles attestation. Cryptographic key storage and signing are delegated to the onboard NXP SE050 and TROPIC01 SEs. The NXP SE050 is a closed-source, Common Criteria EAL 6+ certi-fied element providing non-exportable HSM, on-chip ECC signing, a TRNG, and monotonic counters for anti-rollback. The TROPIC01, described in Section 2, provides the open-source counterpart [ 57 ]. The hardware root of trust is anchored in the i.MX6UL’s High Assur-ance Boot (HAB): the Boot ROM verifies the GoTEE image against a Super Root Key fused into OTP during manufacturing, establishing an immutable first link in the boot chain. Endorsements collected during the SEAP exchange are persisted in the eMMC’s Replay Protected Memory Block (RPMB), which provides authenticated, replay-protected storage. We allocate four non-exportable ECC slots across two SEs: SE050 slot 0 holds the satellite identity key 𝑠𝑘 𝑁 𝑋 𝑃 and slot 1 holds the attestation signing key used to produce 𝜋 𝑇 𝐸𝐸 𝑁 𝑋 𝑃 , and TROPIC01 slot 0 holds the satellite identity key 𝑠𝑘 𝑇 𝑅𝑂𝑃 and slot 1 holds the attestation signing key used to produce 𝜋 𝑇 𝐸𝐸 𝑇 𝑅𝑂𝑃 . This dual-SE design serves two purposes. First, it strengthens G5 (supply-chain auditability) by ensuring that no single vendor’s silicon is solely responsible for the attestation guarantee: the SE050 provides a ma-ture, certified, closed-source anchor while the TROPIC01 provides a fully auditable, open-source anchor. An adversary would need to compromise both vendors (NXP and Tropic Square) simultaneously to forge attestation evidence. Second, it enables cross-verification as the PTA can require both elements to co-sign the Entity At-testation Token (EAT) token, producing a dual attestation proof 

(𝜋 𝑇 𝐸𝐸 𝑁 𝑋 𝑃 , 𝜋 𝑇 𝐸𝐸 𝑇 𝑅𝑂𝑃 ) that the verifier checks against two indepen-dent trust anchors. Under the fully on-orbit model, all four key pairs are gener-ated on-orbit: on first boot, each secure element derives its keys internally. The pre-launch trust commitment consists of public, non-secret artifacts from each element: the SE050 factory serial number, the TROPIC01 device certificate (PUF-bound), the SoC OTP Secure world Root of Trust SE050 Setup HAB Keys Generate key pair 

> Repeated per GS in range
> Verify
> Root of Trust TROPIC01
> RPi Relay
> Ground Station
> Boot ROM Verify Secure World
> Generate key pair

Figure 10: Dual secure element solution with SEAP and TEE RA. The Boot ROM verifies the Secure World image via HAB keys. Both SEs generate identity key pairs. The GS and Ve-raison verifier independently check both signature chains before issuing a key-verify endorsement. 

UID, and the hash of each element’s locked configuration. Under the on-orbit model, Veraison is pre-provisioned with these device identifiers and configuration hashes but not with attestation public keys, since those keys do not yet exist. These are bootstrapped during the first GS pass via a genesis EAT exchange. Because the TROPIC01’s RTL and firmware are open source, the verifier can independently audit the key-generation and policy-enforcement logic down to the gate level. When the PTA executes the TEE RA sub-protocol, it packages the TA measurements and nonce 𝑟 𝑖 into a CBOR-encoded EAT token and sends it to both SEs for signing. The SE050 (slot 1) produces 𝜋 𝑇 𝐸𝐸 𝑁 𝑋 𝑃 and the TROPIC01 produces 

𝜋 𝑇 𝐸𝐸 𝑇 𝑅𝑂𝑃 . Similarly, the PTA signs 𝑟 𝑖 with both identity keys: 𝜎 𝑆,𝑟 𝑖 

via SE050 slot 0 and 𝜎 ′ 

> 𝑆,𝑟 𝑖

via TROPIC01 slot 0. The hello-ack 

carries 𝑟 𝑖 , vk 𝑁 𝑋 𝑃 , vk 𝑇 𝑅𝑂𝑃 , 𝜋 𝑇 𝐸𝐸 𝑁 𝑋 𝑃 , 𝜋 𝑇 𝐸𝐸 𝑇 𝑅𝑂𝑃 , 𝜎 𝑆,𝑟 𝑖 , 𝜎 ′ 

> 𝑆,𝑟 𝑖

is returned through the relay to 𝐺𝑆 𝑖 . The GS and the verifier independently check both signature chains. The Veraison verifier is then pre-provisioned with both attestation public keys, the expected TA reference hashes, and the appraisal policy requiring both proofs to be validated. This architecture also provides a graceful degradation path. If one SE fails (e.g., due to radiation-induced fault or compromised supply chain), the remaining element can continue to produce valid, though single-anchored attestation evidence, which the verifier can accept under a degraded trust policy rather than rendering the satellite completely unattestable. 

7.1.1 Storage. We required information on the GSs stored in the TEE to establish the SEAP. An option is to embed the respective public keys in the image, which is verified during boot. The PTA can carry a static trust store — a table of (𝐺𝑆 𝑖 , vk 𝐺𝑆 𝑖 ) pairs com-piled directly into the TA or stored as a signed configuration blob loaded alongside it. Because the PTA measures every loaded TA by its SHA-256 hash, any modification to the trust store changes the measurement, which in turn changes the EAT token the verifier ap-praises. This means the ground station key set is transitively bound 

> 13 Rezabek et al.

to the hardware root of trust: the verifier knows exactly which set of ground station identities the satellite will accept, because that set is reflected in the reference hash it holds. Besides, during the operation, we envision relying on the storage for usability or state information. The TA running in Secure World cannot directly access the eMMC block device. Linux and its storage drivers live in Normal World, and granting the TEE a raw block-layer path would widen the Secure World attack surface. Instead, the typical workflow follows a delegation model. The PTA issues a Secure World syscall (an RPC request) that crosses the TrustZone boundary and asks the Normal World OS to perform the underlying eMMC I/O on its behalf. For ordinary storage, this would be a trust problem, but the RPMB frames are authenticated with an HMAC key that only the Secure World holds, so even though the Normal World kernel physically reads and writes the eMMC, it cannot forge, replay, or silently modify RPMB contents. 

## 7.2 Role of the TPM 

The Raspberry Pi 5 relay is untrusted by design — the SEAP protocol assumes the adversary controls everything outside the satellite’s TEE. Nevertheless, a discrete TPM 2.0 module attached to the RPi5 provides defence-in-depth by raising the cost of compromising the relay and enabling the verifier to detect tampering that would oth-erwise be silent. Besides, many applications can run on the RPi beyond their trusted components running in the secure world. The TPM measures the RPi5’s boot chain into its PCRs. Before a GS initiates a SEAP exchange, it can request a TPM Quote from the relay, signed by an AK certified against the TPM’s EK. If the relay’s software has been modified, the PCR values will not match the reference measurements the GS operator published at deployment. This does not make the relay trusted in the protocol-theoretic sense, as SEAP’s security does not depend on it, but it provides an oper-ational early-warning mechanism that detects relay compromise. Such scenarios are also relevant for the communication via the USB to the Armory Mk II. In addition, the TPM provides a natural integration point for post-quantum key material during the pre-launch provisioning phase. A PQC key pair (e.g., ML-DSA) can be generated in software on the relay and sealed to the TPM under a PCR policy reflecting the expected relay configuration. The corresponding public key is registered with the GS PKI. During SEAP exchanges, the relay can co-sign forwarded messages with the PQC key, providing a quantum-resistant outer signature layer that protects the session against harvest-now-decrypt-later attacks on the classical ECC signatures flowing between the satellite and ground station. This is generally possible as the TPM can serve as an independent root of trust for the GS. 

## 7.3 Satisfaction of Goals 

We revisit the goals outlined in Section 6.1 and how our implemen-tation of Space Fabric satisfies them. 

7.3.1 G1 and G2 - Root of Trust & Authentical Orbital Execution. 

Section 5.1 established that Space Fabric eliminates the pre-launch secret window through on-orbit key genesis and dual-SE cross-verification. This section describes how the concrete implemen-tation enforces those properties and compares the resulting trust assumptions against existing platforms, as shown in Table 1. The SE050 generates both key pairs (slots 0 and 1) via its internal TRNG, with each key object created under the ORIGIN_INTERNAL 

and non-exportable policy flags set at creation time. The SE050’s attested read mechanism allows the verifier to request a signed object attribute report at first contact, confirming that these flags are in force, providing a cryptographic proof that the key policy matches the operator’s intended configuration. For the TROPIC01, device-unique keys are derived from its PUF, and the fully open-source RTL and firmware allow the verifier to independently audit the key-generation and policy-enforcement logic down to the gate level. Trust in the on-orbit-generated keys is established during the ground station pass via a genesis EAT token. The verifier trusts the genesis EAT because the device identifiers match and the soft-ware checks are enforced by the attested application. The SE050’s signed attribute report provides the structural argument: only the chip bearing the pre-registered serial number, operating under the attested policy, could have produced a valid signature. For the TROPIC01, the PUF-bound device certificate serves the same role. Together, these allow the verifier to confirm that both attestation anchors were generated on-orbit without requiring any secret to have existed on Earth. The trust assumption shifts from the vendor correctly erasing a secret it generated and held to the vendor correctly implementing its published, per-object policy enforcement. This is a materially weaker and more auditable form of vendor dependence, further supported by the SE050’s Common Criteria EAL 6+ certification and the TROPIC01’s fully published design. 

7.3.2 G3 and G4 - Platform Binding & Freshness. Each ground station hello includes a fresh nonce 𝑟 𝑖 , which the PTA binds into both the EAT token and a separate signature 𝜎 𝑆,𝑟 𝑖 under the satellite identity key 𝑠𝑘 𝑆 . The GS verifies 𝜎 𝑆,𝑟 𝑖 under vk 𝑆 and checks session freshness against Δ𝑐𝑜𝑟𝑟 . Because both keys are non-exportable and device-unique, a malicious relay or compromised ground station cannot forge or substitute evidence from another platform — it can only forward messages it cannot re-sign. 

7.3.3 G5 - Auditability in Supply-Chain. The implementation re-lies on many open-source components, such as GoTEE, OP-TEE, and Veraison, allowing any party to audit, reproduce, and indepen-dently verify the attestation stack. The addition of the Tropic Square TROPIC01 extends this auditability into the SE. On the other hand, the NXP SE050 internal design remains proprietary. The dual-SE architecture turns this asymmetry into an advantage. This open design also enables component substitution: the NXP SE050, the TROPIC01, the i.MX6UL SoC, and the TrustZone-based TEE are modular choices rather than architectural commitments, so a different SE or SoC from another manufacturer can be inte-grated, provided it exposes equivalent primitives (non-exportable key generation, monotonic counters, hardware-verified boot). Mul-tiple manufacturers can coexist on the same board, e.g., an NXP SoC paired with an Infineon or Microchip secure element, which 

> 14 Space Fabric: A Satellite-Enhanced Trusted Execution Architecture

would need to collude to break the assumptions. Still, some compo-nents of the HW and SW stacks are not open, so there is space for improvement, but the initial direction is relevant. 

## 7.4 Comparison to Existing Attestation Flows 

Our architecture differs from e.g., Intel SGX/TDX or AMD SEV in where the root of trust resides, how attestation evidence is pro-duced, and what properties the attestation proves. In SGX/TDX and SEV, the CPU itself contains a hardware root secret from which attestation keys are derived. Both measurement and signing occur entirely inside the processor package, and the resulting evidence is verified through Intel’s or AMD’s attestation infrastructure. This allows a verifier to conclude that a specific TD was launched on genuine manufacturer’s hardware, but it requires trust in its key provisioning process and continued availability of attestation ser-vices. Critically, the current attestations prove what code is running but says nothing about where it runs — a gap that recent work on DCEA and POE aims to close for terrestrial deployments. In Space Fabric, the root of trust is distributed across three com-ponents. First, the i.MX6UL’s HAB verifies the GoTEE Trusted OS image against a Storage Root Key (SRK) fused into OTP, es-tablishing an immutable first link in the boot chain. Second, the Attestation PTA running inside OP-TEE’s Secure World measures each loaded TA. Third, two independent SEs, the NXP SE050 and the Tropic Square TROPIC01, hold non-exportable AKs and co-sign the evidence. The PTA communicates directly with both SEs. The resulting dual attestation proof (𝜋 𝑆𝐸 𝑇 𝐸𝐸 , 𝜋 𝑇 𝑆 𝑇 𝐸𝐸 ) is verified by the operator’s Veraison instance against pre-registered public ar-tifacts, anchoring trust in the device hardware and the operator’s verification infrastructure rather than a vendor-controlled attesta-tion service. An adversary would need to compromise both NXP and Tropic Square simultaneously to forge valid attestation evidence. Space Fabric also proves where it runs through SEAP, which binds the attestation to a specific orbiting platform through a Byzantine-tolerant endorsement quorum that any relying party can verify against the GS PKI. This binding leverages the satellite’s post-launch physical inaccessibility as a tamper barrier, closing the physical access gap that terrestrial TEEs, cannot fully elimi-nate. Conversely, SGX/TDX provides a tighter trust boundary at the silicon level: measurement, key derivation, and signing occur within a single processor package with transparent memory en-cryption, whereas Space Fabric distributes trust across an SoC and two external SEs connected over an unencrypted I 2C bus. Together, these mechanisms provide a hardware-backed attestation archi-tecture for TrustZone systems that approaches the guarantees of processor-integrated TEEs while remaining independent of vendor-controlled attestation ecosystems, and extends them with orbital binding and cross-vendor auditability that no terrestrial platform currently offers. 

## 7.5 Applicability to other TEEs 

Although SEAP is instantiated on ARM TrustZone, its core protocol is largely TEE-agnostic. SEAP does not depend on any TrustZone-specific primitive. A platform running Intel TDX, AMD SEV-SNP, or RISC-V Keystone in a space-grade form factor could partici-pate in the same SEAP exchange, producing equivalent attestation evidence and accumulating endorsements in the same way, pro-vided the TEE can emit a nonce-bound measurement report. The principal challenge in porting SEAP to such platforms is not the protocol itself but the root-of-trust bootstrapping. As shown in Ta-ble 1, Intel TDX and AMD SEV-SNP root their attestation chains in manufacturer-held or manufacturer-provisioned secrets, meaning the vendor’s attestation service must remain reachable and trusted throughout the satellite’s operational lifetime. The on-orbit key gen-esis approach introduced here, anchored in a locked, HSM rather than a pre-provisioned vendor secret, is therefore not merely an implementation detail but a necessary adaptation of the trust model to the space environment. Future work could explore whether plat-forms with on-chip PUFs for key derivation can replicate the same no pre-launch-key property without relying on a discrete SE. 

## 7.6 Addressing the Challenges 

The challenges outlined in Section 6.4 outline fundamental protocol limits. In practice, the implementation mitigates or bounds each through a combination of hardware binding, physical assumptions, and conservative parameterization. 

C1: Availability cannot be guaranteed when the adversary con-trols all communication outside the TEE. We accept this as inherent to the asynchronous Byzantine setting and mitigate it operationally: the TPM measured boot chain makes persistent communication-stack tampering detectable, and the PTA maintains a monotonic heartbeat counter in RPMB that the verifier can audit after 𝐶𝑒𝑟𝑡 𝑆 is issued. A gap in the expected sequence flags potential censorship, making the attack detectable even if not preventable. 

C2: The relay indistinguishability is closed by binding attes-tation to pre-registered, device-unique hardware identifiers. Un-der the TPM model, the EK Certificate (EKC) is included along-side the EAT token. An Earth-based TEE cannot produce a valid TPM Quote under the same EK. Under the fully on-orbit model, the SE050/TROPIC01 factory serial number, SoC OTP UID, and 

SlotConfigHash serve the same role: the verifier knows which phys-ical chip must be signing, and replicating it requires possession of the launched hardware. The residual trust assumption is that the operator correctly performed device registration with Veraison, a standard operational requirement that does not involve trusting any vendor to erase secrets. 

C3: The bounded channel corruption assumption 𝑡 ch is grounded in orbital mechanics. For instance, a LEO satellite at approximately 500 km traverses its ground track at roughly 7.5 km/s, with each ground station pass lasting 5–10 minutes. Simultaneously com-promising RF links at geographically distributed stations requires co-located directional equipment that cannot be repositioned faster than the satellite moves, providing the physical basis for bounding 

𝑡 𝑐ℎ .

𝑡 ch is set to span the time needed to contact sufficiently many independent stations, typically 6–12 hours for a sparse GS deploy-ment. 

C4: We accept the impossibility and observe that the security ar-gument does not require the satellite to verify where a GS is located, only who via vk 𝐺𝑆 and whether the channel is honest, enforced by the quorum threshold and timing constraints. The geographic distri-bution of the GS infrastructure constrains the adversary’s ability to 

> 15

Rezabek et al. 

Table 1: Comparison of Root-of-Trust and Attestation Key Provisioning across TEE Platforms. Person. - personalization, man. -manufacturing, enf. - enforcement Property Intel SGX/TDX AMD SEV-SNP ARM CCA OpenTitan Space Fabric Key origin RPK injected during man. Chip-unique secret fused at production CPAK provisioned by the manufacturer PUF-seeded RootKey written to OTP at man. On-orbit genesis: SE050 via TRNG, TROPIC01 via PUF                                           

> Pre-launch secret exists? Yes - Intel holds/held RPK Yes - AMD-held chip secret Yes - manufacturer holds CPAK Yes - Silicon Creator performs person. No persistent secret Vendor trust type Trust Intel erased RPK copy Trust AMD holds chip secret securely Trust in manufacturer CA and key handling Trust in correct person. + erasure of RootKey Trust in locked slot policies; dual-vendor
> Single vendor attestation service? Yes - Intel PCS/IRS Yes - AMD KDS Yes - manufacturer CA No - operator deploys own PKI post-ownership transfer No - dual-SE co-signed EAT
> Design auditability Fully proprietary Fully proprietary Architecture open; implementation proprietary Fully open-source RTL SE050 closed; TROPIC01 fully opened
> Hardware binding mechanism RPK + PPID derived from fused secret VCEK derived from chip-unique fused secret CPAK provisioned into hardware PUF (ring oscillator) entropy + OTP lock SE050 serial + TROPIC01 PUF identity
> Post-deployment key establishment? No — keys pre-exist No — keys pre-exist No — keys pre-exist Partial — Owner Identity at ownership transfer Yes — dual genesis EAT bootstraps two trust anchors
> Physical pre-launch attack yield RPK if Intel erasure claim is false Chip secret if AMD infrastructure compromised CPAK if manufacturer CA compromised RootKey if Silicon Creator person. compromised Nothing — no secrets to extract

simultaneously corrupt multiple channels, but this is an operational property of the deployment, not an in-protocol guarantee. A future hardware extension, e.g., a GPS receiver wired to a TEE protected peripheral, could allow the TEE to independently verify its orbital position and reject hello messages outside physically plausible contact windows, at the cost of enlarging the TCB. In summary, the implementation does not resolve the impossibil-ity results cryptographically but bounds them through the combina-tion of pre-registered hardware identities, physical inaccessibility post-launch, and orbital mechanics constraints, assumptions that are explicit, auditable, and strictly weaker than those required by terrestrial TEE deployments. 

## 7.7 Protocol Evaluation 

The SEAP introduces latency and bandwidth costs that must be assessed against the constraints of orbital platforms. Each SEAP exchange with a single ground station comprises three messages (hello , hello-ack , key-verify ), requiring 1.5 round-trip. The per-exchange latency is dominated not by propagation delay—which contributes approximately 60 -120 ms for a 500 km LEO orbit, but by on-board cryptographic operations. Four ECC-P256 signatures across the dual-SE architecture (two identity, two attestation) ac-count for roughly 100 -400 ms depending on I 2C scheduling. The total per-exchange time is therefore approximately 210-620 ms. The total time to certificate issuance, however, is governed by orbital mechanics rather than cryptographic cost. Accumulating the required 𝑡 GS +2·𝑡 ch +1 endorsements from geographically distributed GSs typically requires 4-7 orbital passes in LEO, corresponding to roughly 6-11 h of wall-clock time for a sparse deployment, well within a 𝑡 ch window of 12 hours. For GEO satellites, where GSs are permanently visible, the process completes in a single session. Once 

Cert 𝑆 is issued, subsequent attestation requires only a fresh dual-signed EAT token ( ∼100 -200 ms ), making SEAP a one-time setup cost. Bandwidth overhead is negligible: each exchange transfers approximately 1.9 kB under the current ECC-based instantiation. A migration to post-quantum signatures (e.g., Falcon-512 or ML-DSA) would increase the hello-ack payload to approximately 6 -12 kB per exchange, which remains well within LEO link capacity. A detailed breakdown of latency, bandwidth, and post-quantum impact is provided in Appendix C. 

## 7.8 Radiation Resilience and Fault Tolerance 

The orbital radiation environment (Section B.2) introduces a fault model in which Single Event Upsets (SEUs) can silently corrupt cryptographic keys or execution state, and Single Event Latch-ups (SELs) can permanently disable hardware components. Space Fabric mitigates both through a layered defense spanning physical, hardware, and software measures, calibrated to the power and mass budget of the satellite platform. 

Physical mitigation. The payload uses standard aluminum enclo-sure shielding for the primary bus, augmented by localized spot-shielding placed directly over critical semiconductor junctions, re-ducing the particle flux reaching sensitive die areas and extending the Total Ionizing Dose (TID) lifetime of Commercial off-the-Shelf (COTS) components. 

Hardware redundancy. To survive SELs and isolate hardware failures, we rely on dual USB Armory Mk II units, dual SEs, and two independent carrier boards. 

Software integrity and execution redundancy. At the software layer, several complementary mechanisms protect against SEU-induced corruption, again scaled to the platform’s computational budget using storage redundancy and regular checkpointing with on-Earth infrastructure. We are exploring future extensions through threshold signing and computation replication to further strengthen the system. 

## 8 Discussion 

Multi-Satellite Topologies and GEO Applicability. The current de-sign focuses on a single LEO satellite endorsed by Earth-based GSs, but SEAP generalizes to constellations where attested satellites endorse newly launched peers. A satellite holding a valid 𝐶𝑒𝑟𝑡 𝑆 

can play the role of 𝐺𝑆 𝑖 : it sends a signed hello , appraises the EAT token, and issues an endorsement to the original GS quorum. The newcomer accumulates endorsements from a mix of GSs and peer satellites until it reaches the threshold. The architecture also applies to GEO deployments. A GEO satellite has permanent visi-bility over a fixed set of GSs, so 𝑡 ch can be tightened to minutes and endorsements collected in a single session rather than across mul-tiple orbital periods. The protocol requires no structural changes for any of these topologies, though the verifier must validate the endorsing satellite’s own 𝐶𝑒𝑟𝑡 𝑆 against the current GS committee, creating a transitive trust chain whose security depends on the re-sistance to posterior corruptions property. The hello , hello-ack ,and key-verify messages are agnostic to whether the endorser is a ground station or peer satellite. 

Availability, Radiation, and Fault Tolerance. The current SEAP in-stantiation uses a single USB Armory Mk II as the sole TEE-capable platform aboard the satellite, representing a single point of failure for both attestation and identity operations. This is also a problem 

16 Space Fabric: A Satellite-Enhanced Trusted Execution Architecture 

for ionizing radiation. A SEU flipping a single bit in an attesta-tion key, a nonce, or a signature share is functionally equivalent to a fault-injection attack on a terrestrial TEE [ 12 , 29 , 35 ]. Space Fabric plans to address this through a layered mitigation strategy described in Section 7.8, combining physical shielding, hardware re-dundancy, and software-level defenses including threshold signing and computation replication. This design would also strengthen the security argument: an adversary would need to compromise multiple physically separated modules simultaneously to forge at-testation evidence. 

Post-Quantum Migration. The current cryptographic instantia-tion uses ECC for attestation signing, identity binding, and GS en-dorsements, which is vulnerable to a quantum computer. Migrating SEAP to post-quantum primitives is, in most respects, straightfor-ward. The used TPM already supports Post Quantum Cryptography (PQC) for its firmware upgrades, serving as an important stepping stone. The EAT token structure is algorithm-agnostic, the endorse-ment and 𝐶𝑒𝑟𝑡 𝑆 formats can accommodate any signature scheme, and the Veraison verifier supports pluggable cryptographic back-ends. The primary constraint is the HSM, which supports now mainly ECC and cannot be reconfigured. A PQC-capable SE would be required for a fully post-quantum instantiation. Alternatively, the SE could retain its role as a hardware binding anchor while a software layer running inside the TrustZone Secure World handles PQC signing, accepting the trade-off that the PQC private key would be stored in secured on-chip SRAM rather than in a non-exportable hardware slot. Given the known harvest-now-decrypt-later threat applicable to long-lived attestation evidence, planning for a hy-brid classical/PQC is relevant for deployments. Nevertheless, other challenges with PQC, e.g., message size, remain. 

Software Verifiability. While the hardware trust anchors in SEAP are designed to be independently auditable, the software stack run-ning in the TrustZone Secure World remains a significant challenge for verification. The GoTEE Trusted OS and the PTA constitute the TCB that produces attestation evidence, and any vulnerability in this code directly undermines the guarantees SEAP provides. For-mal verification of the PTA would provide stronger assurance than testing alone, and is particularly important given that the PTA han-dles all security-critical operations. Open-sourcing the full Secure World software stack is a necessary prerequisite for independent audits, and we intend to release the GoTEE PTA implementation as part of the SEAP artifacts. 

Performance vs. Security Trade-offs. Orbital physical inaccessi-bility closes the threat vector that motivates several expensive terrestrial protections, notably off-chip bus encryption and DRAM memory encryption, which incur non-trivial power and compute overhead on resource-constrained satellite platforms. For instance, the I 2C bus between the SoC and the SE050 is unencrypted, which would be unacceptable on a terrestrial platform where bus prob-ing is trivial but is defensible on a sealed satellite bus post-launch. However, this reasoning must be scoped carefully. First, the pre-launch integration window, during which hardware is accessible to supply-chain actors, demands the full set of protections regard-less of eventual deployment context. Second, software-based side-channel attacks do not require physical access and are not mitigated by orbital isolation. The appropriate posture is therefore to treat physical inaccessibility as a supplement to hardware security fea-tures, not a substitute, and to document explicitly which protections are relaxed and under what threat model assumptions. 

Atmospheric Re-entry and Supply Chain Termination. A notable and often overlooked property of satellite deployments is that end-of-life re-entry into Earth’s atmosphere provides a physically en-forced and irreversible destruction mechanism for all on-board hardware, including the secure element, OTP fuse state, and any keying material that survived the operational lifetime. This stands in sharp contrast to terrestrial TEE deployments, where decom-missioned hardware can be recovered, forensically analysed, and potentially used to extract residual key material or replay historical attestation evidence. Space re-entry effectively eliminates an entire class of supply-chain and end-of-life concerns that must be carefully managed in terrestrial deployments, as the satellite’s final disposal is itself a security primitive. For missions using controlled de-orbit manoeuvres, the timing of this destruction event is known and predictable, enabling the operator to revoke relevant certificates. 

## 8.1 Applications 

The decentralized satellite networks motivating this work [ 42 ]require a trust foundation before multi-operator orbital services become viable. SEAP and confidential computing in space provide exactly this foundation, enabling a range of services that currently require unconditional trust in the satellite operator or are avoided al-together due to the inability to verify on-board execution integrity: 

Secure multi-tenant edge computing , where multiple cus-tomers deploy proprietary algorithms (e.g., inference models, com-pression pipelines) on a shared satellite, each assured that their workload runs in an isolated, attested TEE and that neither the oper-ator nor co-tenants can observe their code or data. In a decentralized constellation, this enables operators to offer compute-as-a-service to third parties who need not trust any single operator. 

Sovereign data processing , enabling governments or regulated industries to enforce data-residency and need-to-know policies by processing sensitive Earth-observation or signals-intelligence data on-board within an attested enclave, downlinking only derived products rather than raw imagery. Orbital attestation ensures com-pliance is cryptographically verifiable rather than contractually asserted. 

Secure key management and relay for inter-satellite links ,using attested TEEs as orbital key distribution nodes whose in-tegrity ground-based verifiers can confirm, supporting encrypted mesh networking across constellations. For decentralized networks where satellites belong to different operators, SEAP-attested key distribution removes the need for a shared terrestrial KMS. 

Verifiable scientific data provenance , allowing research agen-cies to certify that climate, weather, or astronomical observations were processed by a known, unmodified software stack, strength-ening the evidentiary value of space-derived datasets. While the current implementation targets a specific Trusted Ap-plet, these use cases benefit from a more general-purpose execution model. WaTZ demonstrates that WebAssembly runtimes can be hosted inside ARM TrustZone with full remote attestation support, enabling operators to deploy portable, sandboxed WASM modules 

> 17

Rezabek et al. 

as attested workloads [ 37 ]. Integrating such a runtime into the Go-TEE Secure World would allow third-party payloads to be uploaded, measured, and attested through the same SEAP pipeline without requiring bespoke native TAs for each application — significantly lowering the barrier to multi-tenant and updateable confidential computing in orbit. 

## 9 Conclusion 

As computation moves to orbit and decentralized satellite networks take shape, the ability to establish trust in remote, multi-operator or-bital infrastructure becomes a prerequisite rather than an aspiration. This paper presented Space Fabric, a satellite-native trusted com-puting architecture that addresses the two fundamental gaps in ter-restrial TEE deployments: the physical access gap, where hardware accessible to adversaries will eventually be compromised, and the root-of-trust gap, where attestation chains depend on manufacturer-provisioned secrets. Space Fabric closes the first gap by relocating the trusted computing stack to orbital infrastructure, exploiting post-launch physical inaccessibility as a first-class security prim-itive. It closes the second through fully on-orbit key genesis: all cryptographic signing keys are generated within co-located secure elements after launch, with no persistent signing secrets existing on Earth at any point, a property that shifts the trust assumption from vendor secret management to operator device registration, a more auditable and operationally controllable requirement in com-parison to current TEE platforms. A dual-SE architecture, pairing the closed-source NXP SE050 with the fully auditable Tropic Square TROPIC01, eliminates single-vendor trust dependence by requiring both elements to co-sign attestation evidence. The SEAP binds these components together, cryptographically certifying not only what program executes inside the TEE but also where, through a Byzantine-tolerant ground station endorsement quorum. We implemented Space Fabric on a USB Armory Mk II with ARM TrustZone, demonstrated end-to-end attestation via Veraison, and established both security satisfaction arguments and impossibility bounds under a strong adaptive adversary. Open directions include threshold cryptography for hardware redundancy, post-quantum migration, formal verification of the Se-cure World stack, and integration of general-purpose runtimes such as WaTZ to enable multi-tenant confidential computing in orbit. Space Fabric demonstrates that the orbital environment’s unique properties, physical inaccessibility, deterministic trajectories, and atmospheric destruction at end-of-life are exploitable security prim-itives that open a new design axis for trusted computing. 

## References 

[1] 18th Space Defense Squadron, United States Space Force. 2026. Space-Track.org: Space Situational Awareness Data Portal. https://www.space-track.org. Operated by the 18th Space Defense Squadron (18 SDS), Vandenberg Space Force Base, CA, under United States Space Command (USSPACECOM). Provides general perturbation (GP) orbital element sets, conjunction assessments, and satellite catalog data for publicly tracked resident space objects. Accessed: 2026-03-09. [2] D. Abramson, J. Jackson, S. Muthrasanallur, G. Neiger, G. Regnier, R. Sankaran, I. Schoinas, R. Uhlig, B. Vembu, and J. Wiegert. 2006. Intel Virtualization Tech-nology for Directed I/O. Intel Technology Journal 10, 3 (August 2006), 179–192. doi:10.1535/itj.1003 [3] Amazon Web Services. 2024. AWS Ground Station. https://aws.amazon.com/ ground-station/ [4] AMD. 2020. SEV-SNP: Strengthening VM Isolation with Integrity Protection and More . White Paper. Advanced Micro Devices. [5] Davide Andreoletti, Alessandro Rudi, Emanuele Carpanzano, Francesco Lelli, and Tiziano Leidi. 2026. Privacy-Preserving LLM Inference in Practice: A Comparative Survey of Techniques, Trade-Offs, and Deployability. Cryptology ePrint Archive, Paper 2026/105. https://eprint.iacr.org/2026/105 [6] ARM. 2022. Arm Confidential Compute Architecture (Arm CCA) . Documentation. Arm Limited. [7] Gildas Avoine, Muhammed Ali Bingol, Ioana Boureanu, Sébastien Gambs, David Gérault, Pascal Lafourcade, Cristina Onete, and Jean-Marc Robert. 2021. From Re-lay Attacks to Distance-Bounding Protocols. In Security of Ubiquitous Computing Systems . Springer. doi:10.1007/978-3-030-10591-4_7 [8] Andrea Barisani, Inverse Path, and F-Secure Foundry. 2019. USB Armory Mk II: Open Source Flash-Drive Sized Secure Computer. Open hardware design, li-censed under the CERN Open Hardware Licence. https://github.com/usbarmory/ usbarmory NXP i.MX6ULZ ARM Cortex-A7 SoC, 512 MB RAM, ARM TrustZone, HABv4 Secure Boot, ATECC608A and A71CH crypto co-processors. Originally developed by Inverse Path (Trieste, Italy), later F-Secure Foundry, now WithSe-cure. Product page: https://inversepath.com/usbarmory. [9] Georg Becker, Matthias J. Kannwischer, Lorenz Panny, and Peter Schwabe. 2022. Performance Evaluation of Post-Quantum TLS 1.3 on Embedded Systems. In Proc. ACM ASIA Conference on Computer and Communications Security (ASIACCS) .[10] Henk Birkholz, Dave Thaler, Michael Richardson, Ned Smith, and Wei Pan. 2023. Remote ATtestation procedUreS (RATS) Architecture. RFC 9334. doi:10.17487/ RFC9334 [11] Stefan Brands and David Chaum. 1994. Distance-Bounding Protocols. In Proc. Advances in Cryptology (EUROCRYPT) . 344–359. doi:10.1007/3-540-48285-7_30 [12] Chang Cai, Yaqing Chi, and Li Cai. 2025. Radiation Effects of Advanced Electronic Devices and Circuits, 2nd Edition. Electronics 14, 14 (2025), 2896. doi:10.3390/ electronics14142896 [13] D. Chenet, J. Lorandel, C. Moy, S. Ramirez-Gallego, and R. R. Alvarez. 2024. Space Edge Computing for Satellite Systems: Definition and Key Enabling Technologies. In Proc. Euro-Par Workshops (LNCS, Vol. 15386) . doi:10.1007/978-3-031-90203-1_48 [14] Jalen Chuang, Alex Seto, Nicolas Berrios, Stephan van Schaik, Christina Garman, and Daniel Genkin. 2026. TEE.fail: Breaking Trusted Execution Environments via DDR5 Memory Bus Interposition. In 47th IEEE Symposium on Security and Privacy (IEEE S&P ’26) . IEEE Computer Society. https://tee.fail [15] Victor Costan and Srinivas Devadas. 2016. Intel SGX Explained. IACR Cryptology ePrint Archive, Report 2016/086. https://eprint.iacr.org/2016/086 [16] Victor Costan, Ilia Lebedev, and Srinivas Devadas. 2016. Sanctum: Minimal Hardware Extensions for Strong Software Isolation. In Proc. 25th USENIX Security Symposium .[17] N Crisp, K Smith, and P Hollingsworth. 2020. Small Satellite Rideshare: Past, Present, and Future. In 70th International Astronautical Congress (IAC) .[18] Jesse De Meulemeester, David Oswald, Ingrid Verbauwhede, and Jo Van Bulck. 2026. Battering RAM: Low-Cost Interposer Attacks on Confidential Computing via Dynamic Memory Aliasing. In 47th IEEE Symposium on Security and Privacy (S&P) .[19] Saar Drimer and Steven J. Murdoch. 2007. Keep Your Enemies Close: Distance Bounding Against Smartcard Relay Attacks. In Proc. 16th USENIX Security Sym-posium .[20] European Space Agency. 2024. Φsat-2: AI-Powered Earth Observation CubeSat. ESA Mission Page. [21] Gregory Falco et al . 2021. Cyber Security in New Space. International Journal of Information Security 20 (2021). doi:10.1007/s10207-020-00503-w [22] Ezra Feilden, Adi Oltean, and Philip Johnston. 2024. Why We Should Train AI in Space. White Paper, Lumen Orbit, Inc. (now Starcloud, Inc.). https://starcloudinc. github.io/wp.pdf Also available at https://lumenorbit.github.io/wp.pdf. [23] Pierre-Alain Fouque, Jeffrey Hoffstein, Paul Kirchner, Vadim Lyubashevsky, Thomas Pornin, Thomas Prest, Thomas Ricosset, Gregor Seiler, William Whyte, and Zhenfei Zhang. 2020. FALCON: Fast-Fourier Lattice-Based Compact Signa-tures over NTRU . Technical Report. Submission to the NIST Post-Quantum Cryptography Standardization Process. https://falcon-sign.info/falcon.pdf. [24] Google Cloud. 2023. Confidential Space Security Overview . Technical Report. Google. https://cloud.google.com/docs/security/confidential-space [25] Hazy Research, Stanford University. 2025. Mind the Trust Gap: Fast, Private Local-to-Cloud LLM Chat. Hazy Research Blog. https://hazyresearch.stanford. edu/blog/2025-05-12-security Open-source prototype: Minions Secure, https: //github.com/HazyResearch/minions. [26] James Howe and Bas Westerbaan. 2023. Benchmarking and Analysing the NIST PQC Lattice-Based Signature Schemes Standards on the ARM Cortex M7. In Proc. AFRICACRYPT 2023 (LNCS, Vol. 14064) . Springer. doi:10.1007/978-3-031-37679-5_19 [27] Intel. 2023. Intel Trust Domain Extensions (Intel TDX) . Architecture Specification. Intel Corporation. [28] Intel. 2024. Platform Ownership Endorsements (POE) . Technical Doc-umentation. Intel Corporation. https://www.intel.com/content/www/ us/en/developer/articles/technical/software-security-guidance/technical-documentation/platform-ownership-endorsements.html 18 Space Fabric: A Satellite-Enhanced Trusted Execution Architecture 

[29] Aaron Joy, Ben Soh, Zhi Zhang, Sri Parameswaran, and Darshana Jayasinghe. 2024. Physical and Software Based Fault Injection Attacks Against TEEs in Mobile Devices: A Systemisation of Knowledge. CoRR abs/2411.14878 (2024). doi:10.48550/arXiv.2411.14878 [30] Quintus Kilbourn, Sylvain Bellemare, Bunnie, and Michael Gao. 2024. ZTEE -Trustless Supply Chains . Flashbots. https://writings.flashbots.net/ZTEE2-Supply-Chains Accessed: 2026-02-17. [31] Oltjon Kodheli, Eva Lagunas, Nicola Maturo, Shree Krishna Sharma, Bhavani Shankar, Jesus Fabian Mendoza Montoya, Juan Carlos Merlano Duncan, Danilo Spano, Symeon Chatzinotas, Steven Kisseleff, Jorge Querol, Lei Lei, Thang X. Vu, and George Goussetis. 2021. Satellite Communications in the New Space Era: A Survey and Future Challenges. IEEE Communications Surveys & Tutorials 23, 1 (2021), 70–109. doi:10.1109/COMST.2020.3028247 [32] Dayeol Lee, David Kohlbrenner, Shweta Shinde, Krste Asanović, and Dawn Song. 2020. Keystone: An Open Framework for Architecting Trusted Execution Environments. In Proc. 15th European Conference on Computer Systems (EuroSys) .doi:10.1145/3342195.3387532 [33] Ding Li, Ziqi Zhang, Mengyu Yao, Yifeng Cai, Yao Guo, and Xiangqun Chen. 2025. TEESlice: Protecting Sensitive Neural Network Models in Trusted Execution Environments When Attackers Have Pre-Trained Models. ACM Transactions on Software Engineering and Methodology (2025). doi:10.1145/3707453 [34] lowRISC and Google. 2024. OpenTitan: Open Source Silicon Root of Trust. https://opentitan.org. [35] Peter C. Mehlitz and John Penix. 2005. Expecting the Unexpected: Radiation Hardened Software. In Proc. AIAA Infotech@Aerospace . Arlington, Virginia. doi:10. 2514/6.2005-7088 [36] Jämes Ménétrey, Christian Göttel, Pascal Felber, and Valerio Schiavoni. 2022. Attestation Mechanisms for Trusted Execution Environments Demystified. In 

Proc. 22nd International Conference on Distributed Applications and Interoperable Systems (DAIS) . doi:10.1007/978-3-031-16092-9_7 [37] Jämes Ménétrey, Marcelo Pasin, Pascal Felber, and Valerio Schiavoni. 2022. WaTZ: A Trusted WebAssembly Runtime Environment with Remote Attestation for TrustZone. In Proc. IEEE 42nd International Conference on Distributed Computing Systems (ICDCS) . 1177–1189. doi:10.1109/ICDCS54860.2022.9912246 [38] Microsoft. 2023. Azure Attestation Overview. Microsoft Learn. https://learn. microsoft.com/en-us/azure/attestation/overview [39] National Institute of Standards and Technology. 2024. Module-Lattice-Based Key-Encapsulation Mechanism Standard . Technical Report FIPS 203. NIST. doi:10. 6028/NIST.FIPS.203 [40] National Institute of Standards and Technology. 2025. FFT over NTRU-Lattice-Based Digital Signature Standard (FN-DSA) . Technical Report FIPS 206 (Draft). NIST. Initial Public Draft expected late 2025; final standard expected 2026–2027. [41] Duc Tri Nguyen and Kris Gaj. 2022. Fast Falcon Signature Generation and Verification Using ARMv8 NEON Instructions. In Proc. 4th NIST PQC Standardization Conference . https://csrc.nist.gov/csrc/media/Events/2022/fourth-pqc-standardization-conference/documents/papers/fast-falcon-signature-generation-and-verification-pqc2022.pdf. [42] Seoyul Oh and Deepak Vasisht. 2024. A Call for Decentralized Satellite Networks. In Proceedings of the 23rd ACM Workshop on Hot Topics in Networks (Irvine, CA, USA) (HotNets ’24) . Association for Computing Machinery, New York, NY, USA, 25–33. doi:10.1145/3696348.3696896 [43] Christian Paquin, Douglas Stebila, and Goutam Tamvada. 2020. Benchmarking Post-Quantum Cryptography in TLS. In Proc. 11th International Conference on Post-Quantum Cryptography (PQCrypto) (LNCS) . Springer. doi:10.1007/978-3-030-44223-1_5 [44] Rafael Pass, Elaine Shi, and Florian Tramèr. 2017. Formal Abstractions for At-tested Execution Secure Processors. In Proc. 36th Annual International Conference on the Theory and Applications of Cryptographic Techniques (EUROCRYPT) . 260– 289. doi:10.1007/978-3-319-56620-7_10 [45] Sandro Pinto and Nuno Santos. 2019. Demystifying Arm TrustZone: A Compre-hensive Survey. Comput. Surveys 51, 6 (2019). doi:10.1145/3291047 [46] Armen Poghosyan and Alessandro Golkar. 2017. CubeSat evolution: Analyzing CubeSat capabilities for conducting science missions. Progress in Aerospace Sciences 88 (2017), 59–83. [47] Filip Rezabek et al . 2025. Proof of Cloud: Data Center Execution Assurance for Confidential VMs. arXiv preprint arXiv:2510.12469 (2025). https://arxiv.org/abs/ 2510.12469 [48] Benedict Schlüter, Christoph Wech, and Shweta Shinde. 2025. Heracles: Chosen Plaintext Attack on AMD SEV-SNP. In Proceedings of the 2025 on ACM SIGSAC Conference on Computer and Communications Security (CCS ’25) . Association for Computing Machinery. [49] Alex Seto, Oytun Kuday Duran, Samy Amer, Jalen Chuang, Stephan van Schaik, Daniel Genkin, and Christina Garman. 2025. WireTap: Breaking Server SGX via DRAM Bus Interposition. In 2025 SIGSAC Conference on Computer and Communications Security (CCS ’25) . Association for Computing Machinery. https://wiretap.fail [50] Carlton Shepherd, Ghada Arfaoui, Iakovos Gurulian, Robert P. Lee, Konstantinos Markantonakis, Raja Naeem Akram, Damien Sauveron, and Emmanuel Conchon. 2016. Secure and Trusted Execution: Past, Present, and Future - A Critical Review in the Context of the Internet of Things and Cyber-Physical Systems. In 2016 IEEE Trustcom/BigDataSE/ISPA . 168–177. doi:10.1109/TrustCom.2016.0060 [51] Wayne A Shiroma, Luke W Martin, JM Akagi, et al . 2021. CubeSats: A bright future for nanosatellites. Central European Journal of Engineering 1, 1 (2021), 9–15. [52] SpaceX. 2024. SpaceX Rideshare Program. https://www.spacex.com/rideshare/ [53] SpiderOak. 2024. OrbitSecure: Zero-Trust Encryption for Space. https://spideroak. com. [54] Kuniyasu Suzaki. 2024. optee-ra: OP-TEE Remote Attestation with VERAI-SON Verification. GitHub repository. https://github.com/iisec-suzaki/optee-ra Institute of Information Security (IISEC), Yokohama, Japan. Licensed under BSD-2-Clause. Accessed: 2026-03-09. [55] Martin N Sweeting. 2018. Modern small satellites-changing the economics of space. Proc. IEEE 106, 3 (2018), 343–361. [56] Thales Alenia Space, ArianeGroup, Airbus Defence and Space, DLR, Orange Busi-ness, CloudFerro, Hewlett Packard Enterprise, Carbone 4, and VITO. 2024. AS-CEND: Advanced Space Cloud for European Net Zero Emission and Data Sovereignty — Feasibility Study Results . Feasibility Study. European Commission, Horizon Eu-rope Programme. https://www.thalesaleniaspace.com/en/press-releases/thales-alenia-space-reveals-results-ascend-feasibility-study-space-data-centers-0 Co-ordinated by Thales Alenia Space. Study manager: Damien Dumestier. [57] Tropic Square. 2025. TROPIC01: Open Architecture Secure Element. https: //tropicsquare.com/tropic01. [58] Trusted Computing Group. 2024. Cyber Resilient Module and Building Block Requirements . TCG Specification. Trusted Computing Group. [59] Veraison Project. 2023. Veraison: Verification of Attestation Evidence. https: //github.com/veraison. [60] Blaise Agüera y Arcas, Travis Beals, Maria Biggs, Jessica V. Bloom, Thomas Fis-chbacher, Konstantin Gromov, Urs Köster, Rishiraj Pravahan, and James Manyika. 2025. Towards a Future Space-Based, Highly Scalable AI Infrastructure Sys-tem Design. arXiv preprint arXiv:2511.19468 (2025). arXiv:2511.19468 [cs.DC] https://arxiv.org/abs/2511.19468 [61] Takashi Yagawa, Tadanori Teruya, Kuniyasu Suzaki, and Hirotake Abe. 2024. Delegating Verification for Remote Attestation Using TEE. In SysTEX 2024: 7th Workshop on System Software for Trusted Execution, co-located with IEEE Euro S&P’24 . Vienna, Austria. doi:10.1109/EuroSPW61312.2024.00048 

## A SEAP Pseudocode 

This appendix contains the Algorithms 1 and 2. 

## B Satellite Computing Extended 

This appendix provides additional details on space deployments and the challenges they pose. 

## B.1 Satellite Deployment Options 

Space Fabric’s orbital infrastructure can be deployed through two primary approaches, each offering distinct trade-offs between cost, capability, and deployment timeline. 

CubeSat Platforms. CubeSats are standardized small satellites based on 10 ×10 ×10 cm units, with typical 3U or 6U configurations providing sufficient volume for computing payloads, communi-cation transceivers, and power systems at development costs of $500K-$2M per satellite [ 46 , 55 ]. However, CubeSats face power constraints (30-50W average), shorter operational lifetimes (2-5 years in LEO), and limited computational capability compared to larger satellites [51]. 

Rideshare Launch Services. Rideshare missions allow multiple satellites to share launch costs, with services like SpaceX’s Trans-porter offering deployment at approximately $275K per 200 kg, dramatically cheaper than dedicated launches [ 52 ]. The primary trade-off is limited control over orbital parameters and launch tim-ing, as satellites must accept the primary mission’s target orbit and schedule [17]. 

19 Rezabek et al. 

Input: Keys K = {vk G S |GS ∈ GS }, number of adversarial GSs 𝑡 GS , number of corrupted channels 𝑡 ch , channel corruption window 𝑊 

function sat_satJoin 𝜆 (K, 𝑡 GS , 𝑡 ch ,𝑊 )⟨vk S, sk S ⟩ ← KeyGen (1𝜆 )

Πvk TEE ← prove (vk S ) // TEE quote proving that 

vk S was created in the TEE 

𝐶 S ← ⊥ , 𝐿 ← {} 

while 𝐶 S = ⊥ do 

⟨msg , data , GS⟩ ← network () // Wait until message is received 

if msg = hello then 

⟨𝜙, 𝜎 G S ,hello ⟩ ← data 

if Verify (vk G S , ⟨𝜙, hello ⟩, 𝜎 G S ,hello ) = 1 then 

𝜎 S,𝜙 ← Sign (𝜙, sk S )

𝑀 ← ⟨ 𝜙, vk S, 𝜎 S,𝜙 , Πvk TEE ⟩⟨hello-ack , 𝑀 ⟩ → network (GS) 

end end else if msg = key-verify then 

⟨ts , 𝜎 ⟩ ← data 

if Verify (vk G S , ⟨vk S, ts ⟩, 𝜎 ) = 1 then 

𝐿 ← 𝐿 ∪ ⟨ ts , GS , 𝜎 ⟩

if exists 𝐿 ′ ⊆ 𝐿 such that 

(∀ 𝑠, 𝑠 ′ ∈ 𝐿 ′ : |𝑠. ts − 𝑠 ′ .ts | < 𝑊 ) and 

(|{ 𝑠. GS| 𝑠 ∈ 𝐿 ′ }| = 𝑡 GS + 2 · 𝑡 ch + 1) then 

𝐶 ← ⟨ vk S, {( 𝑠.𝜎, 𝑠. GS)| 𝑠 ∈ 𝐿 ′ }⟩ 

𝐶 S ← ⟨ 𝐶, Sign (𝐶, sk S )⟩ 

end end end end foreach GS ∈ GS do 

⟨cert , 𝐶 S ⟩ → network (GS) 

end return 𝐶 S

end Algorithm 1: The algorithm run by a satellite S for the SEAP. 

Space Fabric Deployment Strategy. A practical deployment would use an initial 3U-8U CubeSat demonstration mission via rideshare ($300K-500K total), followed by a production constellation of 4-8 CubeSats for global coverage, with total launch costs of $400K-600K across multiple rideshare missions. Ground station services from commercial providers (AWS Ground Station, KSAT) eliminate the need for dedicated infrastructure at $3-10 per minute of contact time [3]. 

B.1.1 Latency and Bandwidth Considerations. The security advan-tages of satellite infrastructure come with performance trade-offs dominated by communication latency until solutions like Starlink become widely available. 

LEO Satellites. Operating at altitudes of 500-2000 km, LEO satel-lites provide the most favorable latency characteristics for interac-tive applications. Signal propagation at the speed of light imposes a fundamental lower bound: a signal traveling 1000 km to a satellite 

Input: Keys K = {vk G S |GS ∈ GS }, channel corruption window 𝑊 

function GS_satJoin 𝜆, sk GS (K,𝑊 )

𝐶 S ← ⊥ 

while 𝐶 S = ⊥ do 

⟨satNear , S⟩ ← env () // Wait until S is in range for 𝑇 seconds 

𝜙 $

←− { 0, 1}𝜆 

𝜎 G S ← Sign (⟨ 𝜙, hello ⟩, sk G S )⟨hello , ⟨𝜙, 𝜎 G S ⟩⟩ → network (S) 

ts ← clock () 

while True do 

⟨msg , data , S⟩ ← network () // Wait until message is received 

if clock () − ts > 𝑊 then 

// Session lasts at most 𝑊 seconds 

end if msg = hello-ack then 

⟨𝜙, vk S, 𝜎 S, Πvk TEE ⟩ ← data 

if (Verify (vk S, 𝜙, 𝜎 S ) = 1) and 

(teeValidate (Πvk TEE )) then 

ts ′ ← clock () 

𝜎 ← Sign (⟨ vk S, ts ′⟩, sk G S )⟨key-verify , ⟨ts ′, 𝜎 ⟩⟩ → network (S) 

end end else if msg = cert then 

⟨𝐶, 𝜎 ⟩ ← data 

⟨vk S, S⟩ ← 𝐶 

if (∀( 𝑠.𝜎, 𝑠. GS) ∈ S :

Verify (vk 𝑠. G S , vk S, 𝑠.𝜎 ) = 1) and 

(Verify (vk S, 𝐶, 𝜎 ) = 1) then 

𝐶 S ← ⟨ 𝐶, 𝜎 ⟩

end end end end return 𝐶 S

end Algorithm 2: The algorithm is run by a GS GS for the satellite identification protocol .and 1000 km back incurs approximately 7ms of propagation delay. In practice, LEO satellite round-trip latency ranges from 20-40ms one-way when accounting for atmospheric propagation, ground station processing, and on-board routing delays [ 51 ]. This latency is comparable to wide-area network communication between dis-tant data centers, making LEO satellites suitable for applications tolerating moderate latency—including periodic attestation, key derivation, and execution monitoring where interactions occur on second-to-minute timescales rather than milliseconds. GEO satel-lites impose higher latency: the signal path length of approximately 71,572 km (up and down) translates to roughly 250ms of propaga-tion delay when including ground and satellite processing [51]. 

> 20 Space Fabric: A Satellite-Enhanced Trusted Execution Architecture

Bandwidth Constraints. Modern LEO constellations support through-put ranging from several Mbps to hundreds of Mbps per user ter-minal, while GEO satellites provide tens to hundreds of Mbps de-pending on frequency band and antenna configuration [31]. 

Design Implications. Space Fabric’s protocol design explicitly accounts for satellite latency. Rather than requiring synchronous satellite communication for every operation, the system batches attestation requests, caches satellite-derived keys with limited life-times, and maintains local execution state. This design enables ground platforms to operate with reasonable performance during normal operation while maintaining security properties that de-pend on satellite verification. For applications like batch processing, federated learning, edge analytics, and secure enclaves process-ing sensitive data over minutes to hours, the LEO satellite latency overhead (40-80ms per attestation cycle) represents an acceptable trade-off for physical security guarantees. 

## B.2 Space Radiation Phenomena 

Orbital platforms operate in a hard ionizing radiation environment that may impact semiconductor reliability. Unlike traditional space missions that rely on highly specialized and computationally lim-ited radiation-hardened (Rad-Hard) processors, the emerging space computing paradigm heavily leverages COTS silicon to meet the Size, Weight, and Power (SWaP) constraints of modern small satel-lites [ 12 , 35 ]. Consequently, space computing architectures must account for two primary radiation phenomena: cumulative dose effects and transient single-event effects. TID refers to the long-term degradation of semiconductor materi-als caused by continuous exposure to trapped protons and electrons in the Earth’s radiation belts. Over time, this ionizing radiation alters transistor threshold voltages, leading to increased leakage current and eventual permanent device failure. For COTS compo-nents in typical LEO deployments, TID is the principal determinant of hardware payload operational lifetime. Single Event Effects (SEEs) pose a more immediate threat to continuous computation. They occur when a single high-energy particle — such as a heavy ion from Galactic Cosmic Rays or a trapped proton from the South Atlantic Anomaly — strikes the silicon and deposits a localized charge [ 12 ]. SEEs fall into two broad categories. SEUs, commonly referred to as bit flips, are non-destructive transient faults in which the state of a memory cell (SRAM or DRAM) or logic register is inverted. In the context of a TEE, an unmitigated SEU is functionally equivalent to an arbitrary fault-injection attack [ 29 ]: a flipped bit in a cryptographic key, an attestation nonce, or a program counter can silently corrupt execution bounds or invalidate digital signatures. SELs are more severe: a particle strike triggers a parasitic thyristor structure within the CMOS silicon, creating a low-impedance path to ground. SELs disrupt device operation and cause high current draw, potentially leading to thermal destruction if the affected component is not rapidly power-cycled. Space Fabric’s mitigation strategy for both phenomena is de-scribed in Section 7.8. The key observation for the security archi-tecture is that SEUs must be treated as an adversarial fault model in which any single bit in the TEE’s memory or register file may 

Table 2: Per-exchange latency breakdown (500 km LEO).          

> Component Duration
> Propagation ( 3×one-way) a60–120 ms On-board crypto (dual-SE) b200–400 ms GS-side verification c50–100 ms
> Total (sequential) 310–620 ms Total (parallel SEs) 210–420 ms
> a20–40 ms one-way incl. atmospheric and processing delays [31].
> b4×ECC-P256 sign over I 2C; parallel assumes independent buses.
> c2×signature verify + Veraison appraisal + timing check.

be flipped at any time, and the system must detect or tolerate such faults without silently producing corrupted cryptographic output. 

## C Protocol Evaluation 

This appendix provides a detailed breakdown of SEAP’s perfor-mance characteristics under realistic deployment parameters, cov-ering per-exchange latency, total time to certificate issuance, band-width overhead, and the impact of a post-quantum cryptographic migration. 

## C.1 Per-Ground-Station Exchange Latency 

Each SEAP exchange with a single ground station GS 𝑖 consists of three one-way messages: hello (GS → S ), hello-ack (S → GS ), and key-verify (GS → S ), totaling 1.5 round-trip. Table 2 breaks down the expected latency contributions. The four on-board signing operations: 𝜎 𝑆,𝑟 𝑖 and 𝜎 ′ 

> 𝑆,𝑟 𝑖

(identity keys, slots 0) and 𝜋 TEE NXP and 𝜋 TEE TROP (attestation keys, slots 1), are the primary bottleneck. With typical ECC-P256 signing latency of 50 -100 ms per operation on embedded secure elements, this yields approximately 100-200 ms under parallel scheduling. 

## C.2 Total Time to Certificate Issuance 

SEAP requires 𝑡 GS + 2 · 𝑡 ch + 1 valid endorsements within 𝑡 ch . The total time is dominated by orbital mechanics. Table 3 parametrizes this under conservative and moderate adversarial assumptions. 

Table 3: Estimated time to Cert 𝑆 issuance (500 km LEO, 95 min orbital period). Parameter Conservative Moderate 

𝑡 GS (adversarial GSs) 2 3

𝑡 ch (corrupted channels) 2 3Required endorsements 7 10 GS contacts per orbit 1–2 2–3 Orbits to completion 4–7 4–5 

Wall-clock time 6–11 h 6–8 h 

Δcorr (configured) 12 h 12 h For a sparse GS deployment (8–12 globally distributed stations), the satellite typically contacts 1–3 stations per orbit depending on orbital inclination and station placement. Denser deployments or higher-inclination orbits reduce the time proportionally. For 

> 21 Rezabek et al.

GEO satellites, where a fixed set of GSs is permanently visible, the entire endorsement collection can be completed in a single session, potentially within minutes, since no orbital passes need to be awaited. 

## C.3 Bandwidth Overhead 

Table 4 summarizes the per-exchange bandwidth under the current ECC-P256 instantiation. 

Table 4: Bandwidth per SEAP exchange (ECC-P256 instantia-tion). Message Size Direction 

hello ∼200 B GS → S 

hello-ack ∼1.5 kB S → GS 

key-verify ∼150 B GS → S 

Total per exchange ∼1.9 kB 

This is negligible relative to the bandwidth available on mod-ern LEO links (in the Mbps range) and well within the capacity of limited S-band channels. The certificate Cert 𝑆 itself, a bundle of endorsement signatures plus the TEE’s own signature, adds approx-imately 2-3 kB as a one-time broadcast. 

## C.4 Post-Quantum Bandwidth Impact 

A migration to post-quantum cryptography substantially increases the sizes of signatures and keys while leaving the protocol structure unchanged. Table 5 compares the relevant primitive sizes across the current ECC instantiation and two PQC candidates: Falcon-512 for signatures [ 23 , 40 ] and ML-KEM-768 for key encapsulation [ 39 ]. 

Table 5: Cryptographic primitive sizes: ECC-P256 vs. post-quantum candidates. Primitive ECC-P256 Falcon-512 ML-KEM-768 

Public key 64 B 897 B 1184 B Signature 64 B 666 B 1088 B Secret key 32 B 1281 B 2400 B Table 6 projects the impact on the message sizes SEAP under a hybrid ECC+Falcon signature scheme (retaining ECC for backward compatibility) and a complete migration to Falcon-only. We also consider the case where a secure channel between the verifier and satellite is established post-certificate using ML-KEM-768 for key encapsulation. The hello-ack message is the most affected, as it carries two public keys, two attestation proofs (each containing a Falcon sig-nature), and two nonce signatures. Under the hybrid scheme, the 

hello-ack grows from ∼1.5 kB to ∼6.2 kB , a 4× increase. This re-mains well within LEO link capacity but becomes relevant for bandwidth-constrained S-band contacts, where a full certificate ex-change (10 GSs, hybrid signatures) would consume approximately 80 kB compared to 19 kB under ECC. 

Table 6: SEAP bandwidth per exchange under post-quantum migration. Message ECC-only Hybrid Falcon-only (ECC+Falcon) 

hello ∼ 200 B ∼ 930 B ∼730 B 

hello-ack ∼ 1.5 kB ∼ 6.2 kB ∼ 5 kB 

key-verify ∼ 150 B ∼880 B ∼ 730 B 

Total ∼1.9 kB ∼8 kB ∼6.5 kB 

Key exchange for post-certificate channels. After Cert 𝑆 is issued, a verifier establishing a secure channel with the satellite may use ML-KEM-768 for key encapsulation. This adds 1184 B (public key) + 1088 B (ciphertext) ≈ 2.3 kB to the channel establishment handshake, a one-time cost per session that is negligible relative to the SEAP overhead itself. The shared secret derived from ML-KEM can then protect subsequent communication using a symmetric cipher, with no further PQC overhead on the data path. 

Computational impact. Falcon-512 signing is computationally more expensive than ECC-P256, with typical software implementa-tions requiring 2–5 × the cycle count [ 26 , 41 ]. On embedded secure elements, where ECC signing takes 50 -100 ms , a software-based Falcon implementation inside the TrustZone Secure World could require 200 -500 ms per signature. Since Falcon signing must cur-rently be performed in software (neither the SE050 nor TROPIC01 supports PQC natively), the PQC private key would reside in se-cured on-chip SRAM rather than a non-exportable hardware slot. Under the hybrid scheme, each SE still signs with its hardware-bound ECC key (ensuring hardware binding), while a software layer adds the Falcon co-signature (ensuring quantum resistance). The per-exchange on-board crypto time would increase from ∼100 -400 ms to approximately 400 -1200 ms , which remains acceptable for a one-time setup protocol. ML-KEM-768 encapsulation and decapsulation are computation-ally lightweight <1 ms on ARM Cortex-A7 class processors), so the post-certificate key exchange adds negligible latency [9, 43]. 

## C.5 Operational Summary 

Table 7: Summary of SEAP performance characteristics. Metric ECC-P256 Hybrid (ECC+Falcon) 

Per-exchange latency 210-410 ms 400-1200 ms Time to Cert 𝑆 (LEO, 10 GSs) 6–11 h 6–11 h Time to Cert 𝑆 (GEO) minutes minutes Bandwidth per exchange 1.9 kB 8 kB Post-cert attestation latency 100-200 ms 300-700 ms On-orbit key genesis <1 s (once) <1 s (once) The SEAP imposes modest per-exchange costs, dominated by em-bedded cryptographic operations rather than network latency. The 

> 22 Space Fabric: A Satellite-Enhanced Trusted Execution Architecture

total time to certificate issuance is determined by orbital mechan-ics and ground-station density, not by protocol overhead. A post-quantum migration increases bandwidth by ∼4× and per-exchange latency by ∼2-3× under the hybrid scheme, both of which remain within operational bounds for LEO and GEO deployments. The key constraint for PQC adoption is not protocol performance but SE support: until PQC-capable HSMs are available, the hybrid ap-proach of hardware-bound ECC for platform binding with software Falcon for quantum resistance, offers a practical migration path. 

## Acknowledgments 

We would like to thank the Common Prefix team for their support with the paper.