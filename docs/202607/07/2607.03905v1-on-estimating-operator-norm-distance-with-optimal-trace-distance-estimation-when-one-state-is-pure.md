---
title: "On estimating operator norm distance, with optimal trace distance estimation when one state is pure"
source: arxiv
title_zh: "结合optimal trace distance estimation when one state is pure的On estimating operator norm distance,"
abstract_zh: "We investigate computational complexity 的 estimating operator norm distance {\\rm T}_{\\infty}(ρ_0,ρ_1。\n\n核心做法：We provide efficient quantum estimators 用于 operator norm distance whose complexity is independent 的。\n\n主要结果：When one state is pure，we establish optimal quantum estimator 使用 Θ(1/ε) queries to state-preparation。\n\n值得关注的点在于：key intuition behind our estimators is that，when one state is pure，pure state |ψ\\rangle has overlap。"
abstract_en: "We investigate the computational complexity of estimating the operator norm distance ${\\rm T}_{\\infty}(ρ_0,ρ_1)$, defined via the operator norm $\\|A\\|_{\\infty} = σ_{\\max}(A)$, given ${\\rm poly}(n)$-size state-preparation circuits of $n$-qubit quantum states $ρ_0$ and $ρ_1$. We provide efficient quantum estimators for the operator norm distance whose complexity is independent of the rank (and thus the dimension) of the states:   1. When one state is pure, we establish an optimal quantum estimator using $Θ(1/ε)$ queries to the state-preparation circuits. Consequently, for constant additive error, say $ε=1/5$, our estimator runs in ${\\rm poly}(n)$ time. Since the operator norm distance ${\\rm T}_{\\infty}(|ψ\\rangle\\!\\langleψ|,ρ)$ is exactly half of the trace distance ${\\rm T}(|ψ\\rangle\\!\\langleψ|,ρ)$, our result also gives rank-independent query complexity for estimating both quantities, whereas the approaches due to van Apeldoorn, Cornelissen, Gily{é}n, and Nannicini (SODA 2023) and Wang and Zhang (TIT 2024) have query complexity scaling at least linearly with ${\\rm rank}(ρ)$, which can be $\\exp(n)$ in general.   2. For general quantum states, we also provide a quantum estimator using $\\widetilde{O}(1/ε^{3/2})$ queries to the state-preparation circuits, which shows that the corresponding promise problem is ${\\sf BQP}$-complete and improves the ${\\sf QMA}$ upper bound sketched by Liu and Wang (ESA 2025). Together with an $Ω(1/ε)$ quantum query complexity lower bound, this leaves only square-root room for improvement.   The key intuition behind our estimators is that, when one state is pure, the pure state $|ψ\\rangle$ has overlap at least $1/2$ with the top unit eigenvector of $|ψ\\rangle\\!\\langleψ|-ρ$, reflecting a structural feature specific to the operator norm distance."
authors: "Yupan Liu, Qisheng Wang, Zhan Yu"
date: 2026-07-04
journal: quant-ph
source_link: "https://arxiv.org/pdf/2607.03905v1"
pdf: "https://arxiv.org/pdf/2607.03905v1"
score: 6.85
tldr: 围绕《结合optimal trace distance...》；We provide efficient quantum est...；When one state is pure，we establish...
tldr_cn: 围绕《结合optimal trace distance...》；We provide efficient quantum est...；When one state is pure，we establish...
tldr_en: "We investigate the computational complexity of estimating the operator norm distance ${\\rm T}_{\\infty}(ρ_0,ρ_1)$, defined via the operator norm $\\|A\\|_{\\infty} = σ_{\\max}(A)$, given ${\\rm poly}(n)$-size state-preparation circuits of $n$-qubit quantum states $ρ_0$ and $ρ_1$. We provide efficient quantum estimators for the operator norm distance whose complexity is independent of the rank (and thus the dimension) of..."
selection_source: fresh_fetch
motivation: "We investigate computational complexity 的 estimating operator norm distance {\\rm T}_{\\infty}(ρ_0,ρ_1。"
method: 核心做法：We provide efficient quantum estimators 用于 operator norm distance whose complexity is independent 的。
result: 主要结果：When one state is pure，we establish optimal quantum estimator 使用 Θ(1/ε) queries to state-preparation。
conclusion: "值得关注的点在于：key intuition behind our estimators is that，when one state is pure，pure state |ψ\\rangle has overlap。"
---

## 摘要
We investigate computational complexity 的 estimating operator norm distance {\rm T}_{\infty}(ρ_0,ρ_1。

核心做法：We provide efficient quantum estimators 用于 operator norm distance whose complexity is independent 的。

主要结果：When one state is pure，we establish optimal quantum estimator 使用 Θ(1/ε) queries to state-preparation。

值得关注的点在于：key intuition behind our estimators is that，when one state is pure，pure state |ψ\rangle has overlap。

## Abstract
We investigate the computational complexity of estimating the operator norm distance ${\rm T}_{\infty}(ρ_0,ρ_1)$, defined via the operator norm $\|A\|_{\infty} = σ_{\max}(A)$, given ${\rm poly}(n)$-size state-preparation circuits of $n$-qubit quantum states $ρ_0$ and $ρ_1$. We provide efficient quantum estimators for the operator norm distance whose complexity is independent of the rank (and thus the dimension) of the states:   1. When one state is pure, we establish an optimal quantum estimator using $Θ(1/ε)$ queries to the state-preparation circuits. Consequently, for constant additive error, say $ε=1/5$, our estimator runs in ${\rm poly}(n)$ time. Since the operator norm distance ${\rm T}_{\infty}(|ψ\rangle\!\langleψ|,ρ)$ is exactly half of the trace distance ${\rm T}(|ψ\rangle\!\langleψ|,ρ)$, our result also gives rank-independent query complexity for estimating both quantities, whereas the approaches due to van Apeldoorn, Cornelissen, Gily{é}n, and Nannicini (SODA 2023) and Wang and Zhang (TIT 2024) have query complexity scaling at least linearly with ${\rm rank}(ρ)$, which can be $\exp(n)$ in general.   2. For general quantum states, we also provide a quantum estimator using $\widetilde{O}(1/ε^{3/2})$ queries to the state-preparation circuits, which shows that the corresponding promise problem is ${\sf BQP}$-complete and improves the ${\sf QMA}$ upper bound sketched by Liu and Wang (ESA 2025). Together with an $Ω(1/ε)$ quantum query complexity lower bound, this leaves only square-root room for improvement.   The key intuition behind our estimators is that, when one state is pure, the pure state $|ψ\rangle$ has overlap at least $1/2$ with the top unit eigenvector of $|ψ\rangle\!\langleψ|-ρ$, reflecting a structural feature specific to the operator norm distance.
