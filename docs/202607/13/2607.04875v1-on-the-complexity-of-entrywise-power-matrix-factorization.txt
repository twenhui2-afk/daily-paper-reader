Title: On the Complexity of Entrywise Power Matrix Factorization

URL Source: https://arxiv.org/pdf/2607.04875v1

Published Time: Tue, 07 Jul 2026 02:54:24 GMT

Number of Pages: 27

Markdown Content:
# On the Complexity of Entrywise Power Matrix Factorization 

Nicolas Gillis ∗ Subhayan Saha ∗ Stefano Sicilia ∗,† Arnaud Vandaele ∗

University of Mons, Mons, Belgium 

Abstract 

Given a nonnegative matrix X, a factorization rank r and a real parameter p, entrywise power matrix factorization (EPMF) looks for a low-rank matrix Xr such that X = |Xr |◦p (exact case) or 

X ≈ | Xr |◦p (approximate case), where ( ·)◦p denotes the component-wise exponent. EPMF includes the modulus model ( p = 1) and component-wise square factorization ( p = 2) as special cases, the latter being closely related to the square root rank. We analyze the computational complexity of the exact decision problem and the Frobenius-norm approximation problem, and establish a complete complexity landscape. In the exact case, we show that EPMF is equivalent to the combinatorial problem of flipping the signs of the entries of a given matrix X to obtain a rank-r matrix, which we refer to as the signing problem. We first show that the signing problem, and hence exact EPMF, is strongly NP-hard, improving a weak NP-hardness result for the square-root-rank of Fawzi et al. (Math. Prog., 2015). We then show that the signing problem can be solved in polynomial-time when r is fixed. Moreover, when the rank r is part of the input, we show that for generic matrices the algorithm is fixed-parameter tractable (FPT) in the parameter r; in fact, the running time is linear in the input size X. In the approximate case using the Frobenius norm as an error measure, we show that EPMF is NP-hard, already when r = 2, the smallest nontrivial case. 

Keywords : nonlinear matrix decompositions, entrywise power matrix factorization, signless rank, fixed-parameter tractable, strong NP-hardness. 

# 1 Introduction 

Low-rank matrix approximation is a central tool in numerical linear algebra, data analysis, machine learning, and signal processing. Given a data matrix X ∈ Rm×n and an integer r ≤ min( m, n ), the classical problem is to approximate X by a matrix of rank at most r, or, equivalently, to find factors 

W ∈ Rm×r and H ∈ Rr×n such that X ≈ W H . This is equivalent to linear dimensionality reduc-tion: each column of X is approximated by a linear combination of the columns of W . The best known example is the truncated singular value decomposition (SVD), which gives an optimal rank-r

approximation in the Frobenius norm, and any unitary invariant norm, by the Eckart–Young theo-rem [13,25,43]. Many variants, including robust PCA, sparse PCA, weighted low-rank approximation, and matrix completion, modify the loss function or impose additional constraints in order to reflect the structure of the data; see, e.g., [44] and the references therein. The computational complexity of such models has been studied in depth. Computing the (truncated) SVD is a classical problem in 

> ∗

Department of Mathematics and Operational Research. We acknowledge the support by the European Union (ERC consolidator, eLinoR, no 101085607). Emails: firstname.lastname@umons.ac.be. 

> †

SS is a member of the Gruppo Nazionale Calcolo Scientifico-Istituto Nazionale di Alta Matematica (GNCS-INdAM). 

1

> arXiv:2607.04875v1 [cs.CC] 6 Jul 2026

numerical linear algebra, and can be done in polynomial time in m, n, r and log(1 /ε ), where ε is the desired precision; see, e.g., [25,43]. The SVD can be used to compute the best rank-r approximation in any unitary invariant norm, including the matrix ℓ2 norm and the Frobenius norm. However, as soon as different norms are used, finding the best rank-r approximation becomes NP-hard, already when 

r = 1; this is true for the component-wise ℓ1 [24] and ℓ∞ norms [23], and weighted norms or when data is missing [20]. Moreover, by writing Xr = W H , where W has r columns and H has r rows, it is often useful in practice to add constraints on the factors, W and H, typically to improve the interpretability of the decomposition. For example, sparsity leads to sparse PCA and variants, and imposing a given sparsity pattern to the factors leads to an NP-hard problem [34]. Another famous example is nonneg-ativity constraints leading to nonnegative matrix factorization (NMF) which is also NP-hard [45], but can be solved in polynomial time in the exact case (that is, find, if possible, W ≥ 0, H ≥ 0 such that 

X = W H ) when r is treated as a constant, with a complexity of ( mn )O(r2) operations [1]. Analogous results were obtained for the positive semidefinite factorization [28,42]. These results study worst-case complexity, but luckily there is a plethora of low-rank models that are tractable under reasonable as-sumptions. Two of the most widely known approaches to obtain such results are based on minimizing the nuclear norm as a proxy for the rank [39], or on proving that the the optimization landscape does not have spurious local minima, that is, all local minima are global [11, 18]. This has been done for missing data [9, 19], robust variants that rely on other metrics than the Frobenius norm [8, 10], NMF under the so-called separability assumption [1], and binary matrix decompositions [32, 33], to cite a few. It is out of the scope of this paper to review this literature. Despite their success, linear low-rank models have limited expressive power. Many data sets exhibit nonlinear structures that they cannot capture. This limitation has motivated the recent introduction of nonlinear matrix decompositions (NMDs) [40], in which one approximates a matrix by applying a nonlinear function entrywise to a low-rank matrix Xr:

X ≈ f (Xr).

Different choices of f lead to different models and allow for various structural assumptions about the data. A prominent example is the ReLU model, X ≈ max(0 , X r), introduced by Saul for sparse nonnegative data [40]. Since then, several algorithmic approaches have been proposed for this model, including coordinate-descent schemes [3], accelerated alternating partial Bregman proximal gradient method [46], and extrapolated block coordinate methods [21]. Another example is the component-wise square factorization (CSF), X ≈ (W H )◦2. CSF is closely related to the square-root rank of nonnegative matrices, a quantity that appears in the compact representation of convex polytopes [14, 35]. CSF is also related to the fixed-rank signing problem: given a matrix M ∈ Rm×n and an integer r ≤ min( m, n ), decide whether there exists a sign matrix S ∈ {± 1}m×n such that rank( S ◦M ) ≤ r. Fixed-rank signing is connected to the signless rank of a matrix, denoted by rank ±(A), which is defined as the minimum rank of S ◦A over all sign matrices S ∈ {± 1}m×n [27]. We will study this problem in Sections 2.2 and 3. A natural generalization of the signless rank for complex matrices is referred to as the phaseless rank 

of a matrix [27] which is connected to the notion of equimodular classes of matrices [7]. CSF has also been used to obtain compact representations of nonnegative data and probabilistic circuits [37], and recent work has developed coordinate-descent algorithms for the associated least-squares problem [36]. CSF is also closely related to the Hadamard decomposition, defined as X ≈ X1 ◦ X2, where Xi

are low-rank matrices and ◦ is the component-wise Hadamard product, that has been successfully used for example to compress or adapt neural networks [29, 30]; see [22] and the references therein for more details. The modulus model, X ≈ | W H |, provides another unconstrained factorization model 2for nonnegative data [2]. Other nonlinearities, such as min-max and sigmoid functions, have also been considered in applications with bounded or binary observations [2, 38]. 

Outline and contribution of the paper The computational complexity of (linear) low-rank ma-trix approximations has been extensively studied; see the first paragraph of Section 1. However, for non-linear models, computational complexity has not been explored yet. In this paper, we focus on entrywise power matrix factorization (EPMF), which unifies the modulus and component-wise square cases. For a fixed p, it considers the decomposition X ≈ | Xr|◦p, where Xr is a rank-r matrix, and the absolute value and the power are applied entrywise. We consider two variants: Given a nonnegative matrix X, a factorization rank r and a parameter p ∈ R,1. ExactEPMF : compute, if possible, a rank-r matrix Xr such that X = |Xr|◦p.2. FroEPMF : find a rank-r matrix Xr that minimizes ∥X − | Xr|◦p∥2 

> F

.Our positive result concerns the exact fixed-rank regime. After taking the entrywise pth root of X, the problem ExactEPMF becomes a signing problem: given a nonnegative matrix M , decide whether one can choose the signs of its entries so that the resulting matrix has rank at most r. We show that, for fixed r, this problem can be solved in polynomial time. As for exact NMF, the problem is polynomial-time solvable for fixed rank, although the exponent of the polynomial in the dimension of the input depends on r. The algorithm enumerates all possible sign patterns of non-singular basis r-by-r blocks, but it avoids a global enumeration of the signs by using the linear dependencies. Moreover, even when r is given as a part of the input, if the input matrix is generic, we show that the algorithm is fixed-parameter tractable (FPT), that is, it requires f (r).poly( m, n ) number of operations where f

is a computable function. Our hardness results show that this favorable behavior is specific to exact fixed-rank feasibility or generic settings. When the rank is part of the input, we show that ExactEPMF is strongly NP-hard, by a reduction from monotone not-all-equal 3-SAT, generalizing and improving a weak NP-hardness result for the square-root-rank case of Fawzi et al. [14]. Moreover, we show that the least-squares formulation, FroEPMF , is NP-hard, already for r = 2, via a reduction from the decision version of the cut-norm problem. The paper is organized as follows. Section 2 proves strong NP-hardness of ExactEPMF when the rank r is part of the input. When the rank r is not part of the input, meaning it is treated as a constant, we provide in Section 3 a polynomial-time algorithm for ExactEPMF . Moreover, in Section 3.2.3 we show that this algorithm is fixed-parameter tractable (FPT) for generic input matrices even when r is a part of the input. Section 4 proves NP-hardness of FroEPMF , already when r = 2, the first non-trivial case. Section 5 draws the conclusions and the appendix includes some technical lemmas. Table 1 summarizes our results. 

ExactEPMF FroEPMF 

r part of the input Strongly NP-hard (Section 2.2) NP-hard FPT in the generic case (Section 3.2.3) 

r fixed Polynomial time algorithm (Section 3) NP-hard for r = 2 (Section 4) Table 1: Contributions of this paper: complexity landscape for EPMF. 3Notation For the first n natural numbers, we use the standard notation [ n] = {1, . . . , n }. The vectors e1, . . . , e n denote the canonical basis of Rn×n. For a matrix A, we use a Matlab -like notation: we denote its ith row by A(i, :), its jth column by A(: , j ) and its ( i, j ) entry by A(i, j ), sometimes also indicated with the lowercase associated letter aij . We use ◦ for the entrywise multiplication, both for matrices and vectors, but also for the pth entrywise power of a matrix A, which is denoted by A◦p.The transpose of A is A⊤. For any vector v, diag( v) is the diagonal matrix whose diagonal is v, and 

∥v∥ = p⟨v, v ⟩ is the ℓ2-norm of v. The all-zeros matrix and the all-ones matrix of size m × n are denoted, respectively, by 0m×n and 1m×n, and in case of the vectors we write 0n and 1n. The n × n

identity matrix is In.

# 2 NP-hardness of ExactEPMF 

We first consider ExactEPMF when the rank is part of the input. We first generalize the result of Fawzi et al. [14], which proves the weak NP-hardness of computing the square-root rank, that is, 

ExactEPMF for p = 2, using a reduction from PARTITION which is weakly NP-hard [17]; see Section 2.1. We then prove strong NP-hardness in Section 2.2. 

2.1 Weak NP-hardness 

The reduction below is a direct adaptation of the construction of Fawzi et al. [14] from PARTITION ,which we provide here for completeness. Let us formally define the decision versions of both problems. 

PARTITION. 

Given: a multi-set of positive integers S = {s1, ..., s N }. (This means that S can possibly contain several times the same integer.) 

Question: does there exist a partition of S into two subsets S1 and S2 such that the sum of the numbers in S1 equals the sum of the numbers in S2?

ExactEPMF. 

Given: a matrix X ∈ Rm×n 

> ≥0

and a positive integer r.

Question: do there exist matrices W ∈ Rm×r and H ∈ Rr×n such that X = |W H |◦p?Let S be an instance of PARTITION . We construct an instance ( X, r ) of ExactEPMF in polynomial time as follows. Set m = n = N + 1 and r = N . Using s = ( s1, . . . , s N )⊤, set 

X =

IN s◦p

1⊤ 

> N

0



∈ R(N +1) ×(N +1)  

> ≥0

.

The construction is polynomial in the size of the input since p is fixed. Intuitively, the reduction relies only on the freedom of choosing signs of prescribed entrywise pth roots. 

Theorem 2.1. The instance (X, r ) is a yes-instance of ExactEPMF if and only if S is a yes-instance of PARTITION . Hence, ExactEPMF is weakly NP-hard when r is part of the input. Proof. If part. Suppose that S is a yes-instance of PARTITION . Then there exist signs σi ∈ {± 1}

such that PNi=1 σisi = 0. Define 

Y =

IN sσ⊤ 0



.

4Then |Y |◦p = X. The last row of Y is the linear combination of the first N rows with coefficients 

σ1, . . . , σ N since PNi=1 σisi = 0. Thus rank( Y ) ≤ N = r. Therefore X = |W H |◦p for some W ∈

R(N +1) ×N , H ∈ RN ×(N +1) , so the ExactEPMF instance is a yes-instance. 

Only if part. Suppose that the ExactEPMF instance is a yes-instance. Then there exists a matrix 

Y such that |Y |◦p = X, rank( Y ) ≤ N . Since the upper-left block of X is IN , the corresponding block of Y is a signed identity matrix, hence is invertible. Therefore rank( Y ) ≥ N, and so rank( Y ) = N .The first N columns of Y are linearly independent, and the last column must lie in their span, that is, 

Y (: , N + 1) = PNi=1 αiY (: , i ) for some α’s. The first N entries of this relation indicate that αi = ±si

while the last one gives PNi=1 σisi = 0 for some signs σi ∈ {± 1}. Hence, the PARTITION instance is a yes-instance. 

2.2 Strong NP-hardness 

To prove strong NP-hardness of ExactEPMF , we rely on the monotone Not-All-Equal 3-Satisfiability problem (NAE-3SAT). For Boolean variables, x1, . . . , x n ∈ { 0, 1}, NAE( x1, . . . , x n) is satisfied by the assignment x if and only if there exists i̸ = j ∈ [n] such that xi is 1 and xj is 0. We say that a map ϕ : {0, 1}n −→ { 0, 1} is an instance of the Monotone NAE-3SAT problem if it can be written as 

ϕ =

> m

^

> l=1

NAE( xil , x jl , x kl )over Boolean variables x1, . . . , x n. Monotone NAE-3SAT is known to be NP-complete, even under different restrictions [12, 41]. Here it is the decision versions the problem and the version of the 

ExactEPMF we want to compare with: 

Monotone NAE-3SAT. 

Given: a Monotone NAE-3SAT instance ϕ over Boolean variables x1, . . . , x n.

Question: Does there exist an assignment x1, . . . , x n ∈ { 0, 1}n such that ϕ(x1, . . . , x n) is satisfied? 

Exact integer EPMF 

Given: a matrix X ∈ Zs×t 

> ≥0

and integer r ≤ min( s, t ). 

Question: Do there exist matrices W ∈ Rs×r and H ∈ Rr×t such that X = |W H |◦p?To prove strong NP-hardness of ExactIntegerEPMF , we rely on the following intermediate combinatorial problem. 

Fixed-rank signing problem 

Given: a nonnegative integer matrix M ∈ Zs×t 

> ≥0

and an integer r ≤ min( s, t ). 

Question: Does there exist a sign matrix S ∈ {± 1}s×t such that rank( S ◦ M ) ≤ r?Given a nonnegative matrix M , fixed-rank signing requires to flip some signs of the entries of M

in order to make it rank at most r. This problem is equivalent to ExactIntegerEPMF on instances of the form X = M ◦p, where p ∈ N is fixed. Indeed, if S ◦ M = W H with rank( W H ) ≤ r, then 

X = M ◦p = |S ◦ M |◦p = |W H |◦p,

so ( X, r ) is a yes-instance of ExactIntegerEPMF . Conversely, if X = M ◦p = |W H |◦p, then |W H | =

M , and hence W H = S ◦ M for the sign matrix S = sign( W H ), with rank( S ◦ M ) ≤ r. Therefore, 5it suffices to prove strong NP-hardness of the fixed-rank signing problem for nonnegative integer matrices. The corresponding hardness result for ExactIntegerEPMF then follows immediately by mapping M to M ◦p.

The reduction For a Monotone NAE-3SAT instance ϕ = ∧ml=1 NAE( xil , x jl , x kl ) over n variables 

x1, . . . , x n, we define the m × n matrix Hϕ with the ℓth row corresponding to the ℓth NAE clause given by ( eiℓ + ejℓ + ekℓ )⊤. We then construct the following ( m + n + 3) × (n + 4) matrix 

Mϕ =



1 1 0 0 01×n

1 0 1 0 01×n

5 0 0 1 01×n

0n×1 1n 21n 0n×1 In

0m×1 0m×1 0m×1 1m Hϕ

 . (1) Note that for all sign patterns S ∈ {± 1}(m+n+3) ×(n+4) , rank( S ◦ Mϕ) ∈ { n + 3 , n + 4 }. Indeed consider the sub-matrix with the last n + 3 columns and the first n + 3 rows: 

S(1 : n + 3 , 2 : n + 4) ◦

 I3 0

∗ In



,

which has a non-zero determinant regardless of the sign flips, hence n + 3 ≤ rank( S ◦ Mϕ) ≤ n + 4. We can now prove the following result. 

Theorem 2.2. The instance ϕ is satisfiable if and only if there exists a sign pattern S such that 

rank( S ◦ Mϕ) = n + 3 .Proof. If part Any assignment x of ϕ uniquely determines ˜z(x) = 



1115

z

 , Sϕ(x) = 



1 −1 1 1 1⊤

> n

1 1 −1 1 1⊤

> n

1 1 1 −1 1⊤

> n

1n 1n −v 1n diag( v)

1m 1m 1m −1m Σ

 ,

where z = z(x) = 2 x + 1n, v = v(x) = 1n − 2x ∈ {± 1}n and Σ = Σ ϕ(x) ∈ {± 1}m×n is such that Σ( ℓ, [iℓ, j ℓ, k ℓ]) = [ f (xiℓ , σ ℓ), f (xjℓ , σ ℓ), f (xkℓ , σ ℓ)] for all ℓ ∈ [m],

with 

f (y, σ ) = y − (−1) σ(1 − y) ∈ {± 1}, where y ∈ { 0, 1} and σℓ = xiℓ + xjℓ + xkℓ ,

and the other entries of Σ are equal to 1. Note that σℓ ∈ { 1, 2} for an instance to be satisfied. One can check that x satisfies ϕ if and only if ˜ z = ˜ z(x) is in the kernel of 

Mϕ ◦ Sϕ(x) = 



1 −1 0 0 0⊤

> n

1 0 −1 0 0⊤

> n

5 0 0 −1 0⊤

> n

0n 1n −2v 0n diag( v)

0m 0m 0m −1m Σ ◦ Hϕ

 .

6Let us denote 

abc

 = ( Mϕ ◦ Sϕ)˜ z, a ∈ R3, b ∈ Rn, c ∈ Rm. (2) Let x be an assignment that satisfies ϕ. The vector a is zero by the choice of ˜ z. For every entry of b,

bi = 1 − 2vi + vi ˜zi+4 = 1 − 2( −2xi + 1) + ( −2xi + 1)(2 xi + 1) = 1 + 4 xi − 2 − 4xi + 1 = 0 ,

where we used x2 

> i

= xi. Moreover, for every ℓ ∈ [m], 

cℓ = −5 + X

> h∈{ i,j,k }

(2 xhℓ + 1)( xhℓ − (−1) σℓ (1 − xhℓ )) = −5 + X

> h∈{ i,j,k }

3xhℓ − (−1) σℓ + ( −1) σℓ xhℓ = −5 + 3 σℓ − 3( −1) σℓ + ( −1) σℓ σℓ,

which vanishes if and only if σℓ ∈ { 1, 2}, that are exactly the values corresponding to an assignment 

x that satisfies ϕ.

Only if part. Suppose that rank( S ◦ Mϕ) = n + 3 for some S ∈ {± 1}(m+n+3) ×(n+4) . Let ˜ z̸ = 0 span the one-dimensional kernel of S ◦ Mϕ. We may assume w.l.o.g. that ˜ z ≥ 0 since flipping the signs of the jth column of S and of the entry ˜ zj leaves the equations ( S ◦ Mϕ)˜ z = 0 unchanged. Since ˜ z ≥ 0, the three first equations forces the equality of the absolute values of its two terms. Thus ˜z1 = ˜ z2 = ˜ z3, ˜z4 = 5˜ z1.

If ˜ z1 = 0, then ˜ z2 = ˜ z3 = ˜ z4 = 0, and the ith equation gives ˜ z4+ i = 0 for all i, contradicting ˜ z̸ = 0. Hence we may assume ˜ z1 = 1 by rescaling, and therefore ˜ z2 = ˜ z3 = 1, ˜ z4 = 5. The (3 + i)th row of 

S ◦ Mϕ gives 

S(3 + i, 2) + 2 S(3 + i, 3) + S(3 + i, 4 + i)˜ z4+ i = 0 .

It means that ˜ z4+ i = |S(3 + i, 2) + 2 S(3 + i, 3) | ∈ { 1, 3} since S(3 + i, 2) + 2 S(3 + i, 3) ∈ {± 1, ±3}. We set 

xi = ˜z4+ i − 12 ∈ { 0, 1}.

Finally the equations for the part c of equation (2) imply that x is an assignment that satisfies ϕ. In fact, for the ℓth clause in ϕ of the form NAE (xiℓ , x jℓ , x kℓ ), the corresponding ( n + 3 + ℓ)th row of 

S ◦ Mϕ has the form ±5 ± ˜z4+ iℓ ± ˜z4+ jℓ ± ˜z4+ kℓ . Since, ˜ z4+ i ∈ { 1, 3} for all i ∈ [n], if the three variables 

z4+ iℓ , z 4+ jℓ , z 4+ jk ℓ are equal to 1, the sum can be at most 3, so it cannot cancel out 5. Hence, all three 

xiℓ , x jℓ , x kℓ cannot be 0 together. If all these three entries of ˜ z are set to 3, which corresponds to the case when all xiℓ , x jℓ , x kℓ are equal to 1, the sum ±˜z4+ iℓ ± ˜z4+ jℓ ± ˜z4+ kℓ ∈ {± 3, ±9}, and hence cannot cancel out 5. Thus we can conclude that x is an assignment that satisfies ϕ.

Corollary 2.3. Exact Integer EPMF is strongly NP-hard when r is part of the input. Proof. We give a polynomial-time many-one reduction from Monotone NAE-3SAT to Integer ExactEPMF . Let 

ϕ =

> m

^

> l=1

NAE( xil , x jl , x kl )7be a monotone NAE-3SAT instance over the variables ( x1, . . . , x n). Let Mϕ ∈ Z(m+n+3) ×(n+4) be as in (1), Xϕ := M ◦pϕ and set rϕ := n + 3. Since p is fixed, this construction is computable in polynomial time in the size of ϕ and hence ( Xϕ, r ϕ) is a valid instance of ExactEPMF . We show that ( Xϕ, r ϕ) is a yes-instance of ExactEPMF if and only if there exists S ∈ {± 1}m+n+3 ×n+4 such that rank( S ◦ Mϕ) ≤ rϕ.

If part. Suppose that ( Xϕ, r ϕ) is a yes-instance of ExactEPMF . Then there exist matrices 

W ∈ Rm+n+3 ×rϕ and H ∈ Rrϕ×n+4 such that Xϕ = |W H |◦p. Then Mϕ = X◦ 1 

> p
> ϕ

= |W H | and hence, there exists a sign matrix S such that S ◦ Mϕ = W H . Hence rank( S ◦ Mϕ) ≤ rϕ.

Only if part. Suppose there exists a sign matrix S such that rank( S ◦ Mϕ) ≤ rϕ. Then S ◦ Mϕ =

W H for some matrices W ∈ Rm+n+3 ×rϕ and H ∈ Rrϕ×n+4 . Since Mϕ ≥ 0, taking entrywise absolute values on both sides, we get that Mϕ = |W H | and consequently, Xϕ = |W H |◦p which shows that (Xϕ, r ϕ) is a yes-instance of ExactEPMF .Using this and Theorem 2.2, we conclude that ϕ is a satisfiable instance of Monotone NAE-3SAT 

if and only if ( Xϕ, r ϕ) is a yes-instance of ExactEPMF .

# 3 Tractable algorithms for ExactEPMF 

We now turn on positive results. We first show in Section 3.1 that the fixed-rank signing problem can be solved in polynomial time when r is fixed. In fact, we propose and implement an algorithm to do so (Algorithm 1). Then in Section 3.2, we propose two improvements of this algorithm which allow us to show that our algorithm is FPT in the rank r for generic matrices. We illustrate these results numerically on some examples. Finally, we show what these complexity results for the signing problem extend to ExactEPMF in Section 3.3, as both problems are equivalent. 

3.1 The rank-r signing problem 

As we have seen in the previous section, the exact EPMF problem leads to a sign pattern problem: the signs of a matrix must be chosen so as to obtain a matrix of rank at most r. Interestingly, this problem is complementary to a well-studied problem: given a sign pattern S ∈ {± 1}m×n, find a matrix Xr of rank at most r such that the sign of the entries of Xr coincide with S. Bhangale and Kopparty [4] proved that this problem is ∃R-complete already when r = 3. In this section, we prove that, for every fixed r, the rank-r signing problem is polynomial-time solvable. Before doing so, let us recall a result about the skeleton decomposition which we will use to construct a rank-r factorization. 

Lemma 3.1. The matrix Y ∈ Rm×n is a rank-r matrix if and only if there exists index sets I ⊆ [m]

and J ⊆ [n] such that |I| = |J| = r and rank( Y (I, J )) = r. Moreover, Y = Y (: , J )Y (I, J )−1Y (I, :) .Proof. The first part of the statement is a well-known fact in linear algebra. For the second part, since 

Y (: , J ) has rank r it is a basis of the image of Y ; hence there exists B ∈ Rr×n such that Y = Y (: , J )B.Taking the rows related to the indices of I yields Y (I, :) = Y (I, J )B, that is B = Y (I, J )−1Y (I, :), and the claim is straightforward. The skeleton decomposition [26] is a special type of CUR approximations; see, e.g., [31] and the references therein. 8Theorem 3.2 (Fixed-rank signing) . Let M ∈ Rm×n 

> ≥0

and let r ≥ 1 be a fixed integer. There is a deterministic algorithm running in time polynomial in m and n that decides whether there exists a sign matrix S ∈ {± 1}m×n with rank( S ◦ M ) ≤ r and outputs the matrix S if it exists. Proof. For each k = 1 , . . . , r , we need to check whether there exists a matrix of the form Y = S◦M with rank exactly k, where S ∈ {± 1}m×n. If such a rank-k matrix Y exists, then it contains a non-singular 

k × k sub-matrix, say Y (I, J ), with |I| = |J| = k. Moreover, there exists a rank-k factorization ( W, H )such that Y = W H where W = Y (: , J ) and H = Y (I, J )−1Y (I, :); see Lemma 3.1. The difficulty, of course, lies in choosing the right sign pattern S to obtain Y . We will fix the signs of Y (I, J ) and show that, by using the linear dependencies, we do not need to enumerate all the signs of the other entries of Y , keeping our enumeration polynomial in m and n. Note that a brute-force enumeration would require 2 mn operations. The steps below describe our polynomial enumeration of sign patterns S and how to construct a feasible solution ( W, H ). It may be viewed as the exploration of a branching tree: each branch corresponds to a sequence of choices for the signs of the entries of Y := S ◦ M , and if a feasible solution Y exists, then at least one branch will lead to the solution Y , otherwise no branch will lead to a feasible solution and the algorithm certifies that the problem is infeasible. The pseudocode is provided in Algorithm 1 which also includes several improvements described in the next section. 

Step 1: Choosing a nonsingular basis block. Enumerate all pairs ( I, J ) with I ⊆ [m], J ⊆ [n], 

|I| = |J| = k, and all sign patterns S(I, J ) ∈ {± 1}k×k for k ∈ [r]. Set Y (I, J ) = S(I, J ) ◦ M (I, J ), but discard the branch if Y (I, J ) is singular. In summary, after this step, we have fixed the sign pattern of a k-by-k sub-matrix of S indexed by ( I, J ) such that Y (I, J ) = S(I, J ) ◦ M (I, J ) is non singular. The number of branches at Step 1 is at most  mk

  nk

2k2

≤ O  (mn )r 2r2 .

Step 2: Column and row candidates. For a branch with a k × k nonsingular sub-matrix Y (I, J )indexed by ( I, J ), any compatible rank-k matrix may use the columns indexed by J as a basis of its column space, that is, W = Y (: , J ). Let us denote w⊤ 

> i

∈ Rr the ith row of W ∈ Rm×r for i ∈ [m]. We know W (I, :) = Y (I, J ) since these signs have been fixed. For the other rows of W , we can still pick any signs for Y (i, J ) for i / ∈ I, with a total of 2 k possibilities for each row. In summary, for all i ∈ [m], 

wi ∈ Ri =

u ∈ Rk : |u| = M (i, J )⊤ for i / ∈ I, 

{Y (i, J )⊤} for i ∈ I. (3) Similarly, for the jth column of H ∈ Rr×n, denoted hj , we have, using the Skeleton decomposition, 

hj ∈ Cj =

Y (I, J )−1y : y ∈ Rk, |y| = M (I, j ) for j / ∈ J, 

{eℓ} for j ∈ J and j is the ℓth entry in J. (4) Note that H(: , J ) = Ik, the k-by-k identity matrix. Suppose that a feasible rank-k matrix Y = S ◦ M exists, where S(I, J ) was fixed (Step 1). By the skeleton decomposition, there must exist vectors wi ∈ Ri and hj ∈ Cj such that Y (i, j ) = w⊤ 

> i

hj for all 

i, j , hence |Y (i, j )| = M (i, j ) = |w⊤ 

> i

hj | for all i, j . Finding the candidates wi ∈ Ri and hj ∈ Cj such that M (i, j ) = |w⊤ 

> i

hj | for all ( i, j ) hence provides a certificate that a feasible solution is found (if such candidates do not exist, then one cannot play with the signs of M to make it rank k). Unfortunately, checking all candidates does not lead to the desired results (namely, having an algorithm polynomial in m and n), as for each of the remaining m − k rows (resp. n − k columns) of S, there are (2 k)m−k

9Algorithm 1 Solving the rank-r signing problem (Theorems 3.2 and 3.8) 

Input: A non-zero nonnegative matrix M ∈ Rm×n 

> ≥0

, and a rank r.

Output: If they exist, rank-k matrices W ∈ Rm×k and H ∈ Rk×n for the smallest possible k ≤ r such that M = |W H |. Hence rank( S ◦ M ) = k ≤ r for S = sign( W H ). Otherwise, W = [ ], H = [ ].  

> 1:

for k = 1 , . . . , r do  

> 2:

for all I ⊂ [m] and J ⊂ [n] of size k do ▷ Step 1  

> 3:

check full rank 1 ← 1 ▷ Improvement Step 1: check Condition (6)  

> 4:

for all sign patterns S(I, J ) =: ˜S ∈ {± 1}k×k with ˜S(1 , :) = 1, ˜S(: , 1) = 1 do  

> 5:

if rank( ˜S ◦ M (I, J )) < k then  

> 6:

check full rank 1 ← 0 ▷ Condition (6) fails  

> 7:

Go to line 4 and try the next sign pattern.  

> 8:

else  

> 9:

Construct the sets Ri, i ∈ [m], and Cj , j ∈ [n], as in (3) and (4).  

> 10:

for all subsets P of [ m] of size min  m,  k+1 2

 do ▷ Steps 2 & 3  

> 11:

check full rank 2 ← 1 ▷ Improvement Step 3: check Condition (7)  

> 12:

for all {wi}i∈P with wi ∈ Ri do ▷ R i defined in (3)  

> 13:

if {wiw⊤ 

> i

}i∈P is not a basis of k-by-k symmetric matrices then  

> 14:

check full rank 2 ← 0 ▷ Condition (7) fails  

> 15:

end if  

> 16:

for j = 1 , . . . , n do ▷ Step 4  

> 17:

Find any hj ∈ Cj from (4) with |w⊤ 

> i

hj | = M (i, j ) for all i ∈ P . 

> 18:

If not possible, go to line 12 and try the next choice of {wi}i∈P . 

> 19:

end for  

> 20:

for all i / ∈ P do ▷ Step 5  

> 21:

Find any wi ∈ Ri such that |w⊤ 

> i

hj | = M (i, j ) for all j. 

> 22:

If not possible, go to line 12 and try the next choice of {wi}i∈P . 

> 23:

end for  

> 24:

Solution found: return [W, H ].  

> 25:

end for  

> 26:

if check full rank 2 = 1 then ▷ Condition (7) satisfied but no solution  

> 27:

Go to line 4 and try the next sign pattern.  

> 28:

end if  

> 29:

end for  

> 30:

end if  

> 31:

end for  

> 32:

if check full rank 1 = 1 then ▷ Condition (6) satisfied but no solution  

> 33:

Go to line 1 and try the next k. 

> 34:

end if  

> 35:

end for  

> 36:

end for  

> 37:

return W = [ ] , H = [ ]. (resp. (2 k)n−k) candidates. The next step will select a sufficiently small subset of the row candidates to achieve our goal. 10 Step 3: Selecting row candidates. For any feasible choice of row and column candidates wi ∈ Ri

and hj ∈ Cj for all ( i, j ), the feasibility condition |w⊤ 

> i

hj | = M (i, j ) between row i and column j can be rewritten as 

⟨hj h⊤ 

> j

, w iw⊤ 

> i

⟩ = M (i, j )2. (5) The matrices wiw⊤ 

> i

lie in the space of k × k symmetric matrices, whose dimension is  k+1 2

. Hence, there exists P ⊆ [m] with |P | ≤  k+1 2

 such that wℓw⊤ 

> ℓ

lies in the linear span of {wiw⊤ 

> i

: i ∈ P } for all 

ℓ ∈ [m]. Note that if m ≤  k+1 2

, we can simply pick all rows and then keep only the corresponding linearly independent matrices. This linear dependence will allow us in the next step (Step 4) to determine the row candidates of W for i / ∈ P given the row candidates i ∈ P . In order to identify if a matrix Y exists, we branch over all possible subsets P of size min  m,  k+1 2

 and all possible choices of wi ∈ Ri for i ∈ P , creating at most 

 m

 k+1 2



(2 k)(k+1 2 ) = mO(k2) 2O(k3) ≤ mO(r2) 2O(r3)

different new branches to explore. Note that we do not check whether {wiw⊤ 

> i

}i∈P spans {wiw⊤ 

> i

}i∈[m]

for all possible wi ∈ Ri since this would be too expensive: what is important is that at least one such 

P will be such that {wiw⊤ 

> i

}i∈P spans {wiw⊤ 

> i

}i∈[m]. However, the algorithm might find a solution for a P that does not satisfy this condition. 

Step 4: Filtering column candidates. In step 3, we identified the candidates wi ∈ Ri for i ∈ P ,each of which we can explore as a new branch (since there are sufficiently few, that is, a polynomial number in m). In other words, the subset of rows of W in P of size at most  k+1 2

 is fixed and the candidate wi is selected for all i ∈ P . We use them to check which column candidates for H remain compatible with the candidate rows for W . For each j ∈ [n], we retain only the candidates hj ∈ Cj

satisfying ( w⊤ 

> i

hj )2 = M (i, j )2 for all i ∈ P . We discard the branch if no candidate hj ∈ Cj satisfies these conditions for some j, otherwise we select arbitrarily one surviving candidate per column. The justification for keeping an arbitrary survivor is given in the next step. 

Step 5: Selecting the remaining row candidates. At the end of Step 4, one candidate hj ∈ Cj

has been selected for every column j ∈ [n]. It remains to select the row candidates wi ∈ Ri for 

i / ∈ I ∪ P . By construction, the selected hj ’s satisfies 

⟨hj h⊤ 

> j

, w iw⊤ 

> i

⟩ = M (i, j )2 for all i ∈ P. 

For each i / ∈ I ∪ P , we select the vectors wi ∈ Ri satisfying (w⊤ 

> i

hj )2 = M (i, j )2 for every j ∈ [n].

If no candidate survives for some i / ∈ I ∪ P , we discard the branch. Otherwise, we select arbitrarily one surviving candidate wi.Let us show that the arbitrary choice of the hj ’s made in Step 4 are safe in a branch that leads to a feasible solution Y . Let h∗ 

> j

be another admissible choice for column j in Step 4, that is, this column coordinate h∗ 

> j

satisfies 

⟨h∗ 

> j

(h∗ 

> j

)⊤, w iw⊤ 

> i

⟩ = M (i, j )2 = ⟨hj h⊤ 

> j

, w iw⊤ 

> i

⟩ for all i ∈ P. 

11 For ℓ ∈ [m], if {wiw⊤ 

> i

}i∈P spans {wiw⊤ 

> i

}i∈[m], there exist scalars αi such that wℓw⊤ 

> ℓ

= P 

> i∈P

αi wiw⊤ 

> i

.Therefore, 

⟨hj h⊤ 

> j

, w ℓw⊤ 

> ℓ

⟩ = X

> i∈P

αi⟨hj h⊤ 

> j

, w iw⊤ 

> i

⟩ = X

> i∈P

αiM (i, j )2 = X

> i∈P

αi⟨h∗ 

> j

(h∗ 

> j

)⊤, w iw⊤ 

> i

⟩ = ⟨h∗ 

> j

(h∗ 

> j

)⊤, w ℓw⊤ 

> ℓ

⟩

= M (ℓ, j )2.

This means that the choice of any admissible hj in Step 4 does not affect the value of ⟨hj h⊤ 

> j

, w ℓw⊤ 

> ℓ

⟩

for all ℓ ∈ [m], because of the linear dependence. 

Step 6: Constructing the factorization. If there were no rank-k feasible solution Y = S ◦ M ,then no branch generated in Steps 1-5 will remain, and we can conclude that the rank-k problem is not feasible. Otherwise, our procedure provides a rank-k factorization ( W, H ) such that M = |W H |.In fact, let W and H contain the selected row and column candidates, respectively, that is, 

W =



w⊤

> 1

...

w⊤

> m

 ∈ Rm×k, H =  h1 · · · hn

 ∈ Rk×n, and S = sign( W H ) ∈ {± 1}m×n.

By the filtering performed in the previous steps, the selected candidates satisfy |W (i, :) H(: , j )| =

|w⊤ 

> i

hj | = M (i, j ) for every i ∈ [m] and j ∈ [n], so rank( S ◦ M ) = rank( W H ) ≤ k ≤ r.

Running time. For each k ≤ r, Step 1 produces O (mn )k 2k2  branches and Step 3 produces 

mO(k2) 2O(k3) further branches. Steps 4–5 cost poly( m, n ) per branch. The total is 1 (mn )r · mO(r2) ·

2O(r3) · poly( m, n, r ). This concludes the proof. Note that the term mO(r2) comes from the fact the we first explore the possible signs of the rows of W in the sets Ri’s. If n < m , it is preferable to apply the algorithm on the transpose to have complexity nO(r2).

3.2 Improvements of the algorithm and FPT for generic matrices 

In this section, we first provide two general improvements of the algorithm described in Theorem 3.2, one for Step 1 in Sections 3.2.1 and one for Step 3 in Section 3.2.2. This allows us to provide a fixed-parameter tractable (FPT) algorithm in the parameter r for rank-r⋆ generic matrices, where r⋆

is a fixed positive integer, with probability one (w.p.1) in Section 3.2.3. 

3.2.1 Improvements of Step 1 

We have the following result. 

Lemma 3.3. Given 1 ≤ k ≤ r, let us consider Step 1 of the algorithm described in Theorem 3.2. Assume we have found a pair of subsets (I, J ) with |I| = |J| = k such that 

rank 



M (I, J ) ◦ S(I, J )



= k for all S(I, J ) ∈ {± 1}k×k. (6) 

Then one does not need to consider other pairs of subsets (I, J ) to guarantee whether there exists S

such that rank( M ◦ S) ≤ k.       

> 1Moreover, for rational inputs, if the bit-size of the entries of Mis at most b, for rfixed, every rational number occurring in the computation has bit-size at most poly r(b) and so the algorithm is strongly polynomial over Q.

12 Proof. The condition (6) means that, regardless of its sign pattern, the sub-matrix M (I, J ) always has rank k, that is, we cannot reduce its rank by playing with its signs. This implies that, regardless of the sign pattern S, we can always construct a Skeleton approximation using the indices ( I, J ) since 

S(I, J ) ◦ M (I, J ) is invertible for all sign pattern S(I, J ), and hence we do not need to explore other subsets of indices in the algorithm described in Theorem 3.2 that construct a Skeleton approximation of Y = M ◦ S.As we will see in Section 3.2.3, condition (6) is valid w.p.1 for generic matrices, and hence only one subset of indices ( I, J ) needs to be checked in that case, leading to an FPT complexity in the parameter r for Step 1. Another improvement for Step 1 (line 4 in Algorithm 1), is that not all sign patterns need to be checked for M (I, J ). In fact, the rank of a matrix is unchanged under sign flips of its rows and columns, hence we can assume w.l.o.g. that the first row and first column of S(I, J ) have a fixed sign pattern, e.g., ˜S(1 , :) = ˜S(: , 1) = 1 where ˜S := S(I, J ). This means that we only need to check 2 (k−1) 2

sign patterns instead of 2 k2

.

3.2.2 Improvement of Step 3 

The other step that is not FPT in Theorem 3.2 is Step 3. Recall that, in Step 3, we need to check all subsets P of size min  m,  k

> 2

 of rows of W , for 1 ≤ k ≤ r. In a similar spirit as for Step 1, if for a subset P , {wiw⊤ 

> i

}i∈P is a basis of the symmetric matrices for any possible choice of the signs (that is, for any wi ∈ Ri), we do not need to consider other subsets. 

Lemma 3.4. Given 1 ≤ k ≤ r, let us consider Step 3 of the algorithm of Theorem 3.2. Assume that the subset P of size min  m, k(k+1) 2

 is such that 

{wiw⊤ 

> i

}i∈P are linearly independent for all wi ∈ Ri. (7) 

Then, in the algorithm of Theorem 3.2, one does not need to consider other subsets P in Step 3 for the current sign pattern S(I, J ).Proof. Since {wiw⊤ 

> i

}i∈P are linearly independent regardless of the choice of the wi’s in Ri, all other rank-one matrices, {wiw⊤ 

> i

}i / ∈P , will be in the span of {wiw⊤ 

> i

}i∈P . Hence, when looking for hj ∈ Cj

in Step 5, the argument described in Step 5 allows us to only check the conditions ⟨hj h⊤ 

> j

, w iw⊤ 

> i

⟩ =

M (i, j )2 for i ∈ P , as it will imply ⟨hj h⊤ 

> j

, w iw⊤ 

> i

⟩ = M (i, j )2 for i / ∈ P .As we will see in Section 3.2.3, condition (7) is valid w.p.1 for generic matrices, and hence only one set P needs to be checked in that case, leading to an FPT complexity in the parameter r for Step 3. Algorithm 1 provides the pseudo-code of the algorithm described in Theorem 3.2, combined with the improvements in Steps 1 and 3 described in the two previous sections, which stop the algorithm early when (6) is satisfied, or avoid enumerating all subsets P for a given sign pattern S(I, J ) when (7) is satisfied. 

3.2.3 Fixed-parameter tractable algorithm for generic matrices 

For simplicity, we consider generic matrices of the following form: the matrix Y is a generic rank-r⋆

matrix if Y = W#H#, where the entries of W# ∈ Rm×r⋆ and H# ∈ Rr⋆×n are drawn from a continuous distribution (e.g., Gaussian or uniform). 13 Before proving that Algorithm 1 is fixed-parameter tractable (FPT) in the parameter r for such generic rank-r⋆ matrices (Theorem 3.8), we provide several lemmas. The first lemma shows that for a generic rank-r⋆ matrix Y , the k-by-k sub-matrix Y (I, J ) ◦ ˜S is full rank w.p.1 for any sign pattern ˜S and any 1 ≤ k ≤ r⋆.

Lemma 3.5. Let Y = W#H#, where the entries of W# ∈ Rm×r⋆ and H# ∈ Rr⋆×n are drawn from a continuous distribution. Then 

> r⋆

X

> k=1

X

> S∈{± 1}m×n
> I⊂[m],J ⊂[n]: |I|=|J|=k

P  det  S(I, J ) ◦ Y (I, J ) = 0  = 0, (8) 

which means that for all k, for every choice of the matrix S and of the sets I and J of size k, the sub-matrix S(I, J ) ◦ Y (I, J ) has full rank w.p.1. Proof. Since the sum in (8) runs on a finite set, it is enough to show the claim for k, S, I and J fixed. The polynomial 

f (W (I, :) , H (: , J )) = det( S(I, J ) ◦ W (I, :) H(: , J )) is non-zero, since for the choice W (I, :) = I ◦ S and H(: , J ) = I it has value 1. Then the claims follows from the fact that the set of the zeros of f has measure zero, and the fact that W# and H# are picked from a continuous distribution. The next two lemmas will be used to prove that {wiw⊤ 

> i

}i∈P form a basis of the set of symmetric matrices w.p.1 for generic input matrices. The first one shows that invertible linear transformations of a continuous distribution remains a continuous distribution. 

Lemma 3.6. If a set of vectors {w1, . . . , w ℓ} ∈ Rd has an absolutely continuous joint distribution on (Rd)ℓ, then for any q ≤ d and for any fixed matrices B1, . . . , B ℓ ∈ Rq×d with full row rank, 

{B1w1, . . . , B ℓwℓ} also has an absolutely continuous joint distribution on (Rq)ℓ.Proof. This is a consequence of a much more general statement about composition of Lebesgue mea-surable functions. For more details and the general statement, refer to [16, Theorem 2.47]. The second lemma provides the desired result regarding the span of {wiw⊤ 

> i

}i∈P .

Lemma 3.7. Let Y = W#H#, where the entries of W# ∈ Rm×r⋆ and H# ∈ Rr⋆×n are drawn independently from a continuous distribution. The matrix M = |Y | is given as input to Algorithm 1. For any k ≤ r⋆, for any subset P ⊆ [m] of size Nk =  k+1 2

, the matrices {wiw⊤ 

> i

}i∈P are linearly independent for all wi ∈ Ri, and hence they span the entire space of k × k symmetric matrices w.p.1. Proof. Let P be any subset of [ m] of size Nk =  k+1 2

. For the simplicity of exposition, without loss of generality, we assume P = {1, . . . , N k}. Define the map 

ϕ : Rk → RNk , ϕ(w) = svec( ww ⊤),

where svec denotes any vectorization of the independent entries of a symmetric matrix, that is, the values in the upper triangular part. The matrices {wiw⊤ 

> i

}Nk 

> i=1

span Sk if and only if the vectors 

ϕ(w1), . . . , ϕ (wNk ) form a basis of RNk , that is, if and only if the matrix 

A =



ϕ(w1)⊤

...

ϕ(wNk )⊤

 ∈ RNk ×Nk

14 is nonsingular. Since the entries of ϕ(w) are quadratic polynomials in the entries of w, the determi-nant f (w1, . . . , w Nk ) = det( A) is a polynomial in the entries of w1, . . . , w Nk . Consider the following collection of Nk vectors: 

V =

n ei + ej

2 , 1 ≤ i ≤ j ≤ k

o

.

Then the polynomial f evaluated on these Nk vectors is non-zero and hence f is not the zero poly-nomial. Since the entries of W# are picked independently at random from a continuous distribution, the vectors {W#(i, :) ⊤}i∈P have a joint continuous distribution in ( Rk)Nk . Following the definition in (3), for some k ≤ r⋆, the row candidates wi ∈ Ri are such that wi = ( W#(i, :) H#(: , J )) ⊤ ◦ si

for some sign vector si ∈ {± 1}k where J ⊆ [n], |J| = k. For a fixed collection of sign vectors 

{s1, . . . , s Nk } =: σ ∈ ({± 1}k)Nk , taking Di = diag( si), we have 

wi = DiH#(: , J )⊤W#(i, :) ⊤. (9) By Lemma 3.6 for d = r∗, q = k and ℓ = Nk, the corresponding candidate row vectors w1, . . . , w Nk

have a joint continuous distribution. Hence, using the fact that the set of zeros of f has measure zero, 

P



f (w1, . . . , w Nk ) = 0 rank(( Di(H#(: , J )) ⊤) = k



= 0 .

Since the entries of H#(: , J ) are picked independently at random from a continuous distribution, rank( Di(H#(: , J )) ⊤) = k w.p.1, similarly as in Lemma 3.5. Using the fact that there are a finite number of σ, using union bound, we can therefore conclude the matrices {wiw⊤ 

> i

}i∈P are linearly independent and hence, they span the entire space of k × k symmetric matrices. We can now prove that Algorithm 1 is FPT for generic input matrices. 

Theorem 3.8. Given r⋆ ≥ 1, let M = |Y | where Y = W#H#, such that the entries of W# ∈ Rm×r⋆

and H# ∈ Rr⋆×n are drawn from a continuous distribution. Then Algorithm 1 run on (M, r ) for any 

r is FPT in parameter r as it runs in O(2 ˜r3

· poly (m, n, ˜r)) operations, where ˜r = min( r, r ⋆).Proof. If r ≥ r⋆, Algorithm 1 will terminate for k ≤ r⋆ since a feasible solution of rank r⋆ exists, otherwise the algorithm will stop for k ≤ r ≤ r⋆. In all case, k will not exceed ˜ r.Now, for any k ≤ ˜r, the two conditions (6) and (7) are satisfied w.p.1 for any ( I, J ) and any P , as shown in Lemmas 3.5 and 3.7. Hence, Algorithm 1 will only enter once the loop over the indices ( I, J )and over the subset P , w.p.1. In the loop over P , it might still need to check all possible wi ∈ Ri for 

i ∈ P , with at most 2 k2(k+1) /2 different choices – there are k(k + 1) /2 vectors wi ∈ Rk whose entries can take two signs (unless i ∈ I); see (3). Then 

• Checking linear independence of {wiw⊤ 

> i

}i∈P takes O(k6). 

• Finding hj ∈ Cj such that |w⊤ 

> i

hj | = M (i, j ) takes up to 2 kk k

> 2

 since Cj has up to 2 k elements, the inner product costs k operations and there are  k

> 2

 vectors wi. This has to be done n times. 

• Finding wi ∈ Ri for i / ∈ P such that |w⊤ 

> i

hj | = M (i, j ) for all j takes up to 2 kkn which has to be done m times. Since when n > m it is always possible to consider Y ⊤, the total cost is 

O



˜r2˜r3

(˜ r6 + ˜ r32˜r min( m, n ) + ˜ r2˜rmn )



.

15 We conclude this section with three remarks. 

Remark 1 (Fixed-parameter linear) . For generic matrices, Algorithm 1 is actually fixed-parameter linear in the parameter r since the computational cost is linear in the input size, mn .

Remark 2 (Generic matrices) . In this section, we considered rank-r generic matrices as the product of two smaller generic matrices. However, one could instead consider the more general case where the matrices are drawn from the manifold of rank-r matrices Mr. Although this space has zero ambient Lebesgue measure in Rm×n whenever r < min {m, n }, one can alternatively consider the restricted Hausdorff measure and then use that to define the notion of absolutely continuous probability measures on Mr [15]. The proofs in this section generalize to that setting as well, but we use the current setting for the simplicity of the exposition. 

Remark 3 (Last improvement of Algorithm 1) . A drawback of Algorithm 1 is that it tries to find a rank-k solution, incrementing k from 1 to r. Hence if the input matrix has a rank-r solution, but no smaller rank solution, it will first have to enumerate all k = 1 , 2, . . . , r − 1 before finishing, which will be slow. To avoid this, one could start the iteration at k = r, but then we do not necessarily have an FPT algorithm for generic matrices. Hence, in practice, it could actually be better to run two algorithms in parallel: one that increments k from 1, and the other that decrements from r.

3.2.4 Implementation and numerical examples 

We have implemented Algorithm 1 in MATLAB. It is available from 

https://gitlab.com/ngillis/rank-r_signing/ .To give an idea of the size of matrices one can handle on a laptop 2, Table 2 reports the computational time needed for the algorithm to generate a feasible solution on rank-r randomly generated matrices. When there exists a feasible solution for r ∈ { 2, 3}, our algorithm can handle matrices of large dimensions, up to 10 4, within seconds. For r = 4, the algorithm starts to stall. In fact, the largest term in the computational cost in Step 3, 2 (r−1) 3

, goes from 256 for r = 3 to 134,217,728 for r = 4, so this quick change of behavior is expected. 

r = 2 

m = n time (s.) 100 0.005 ± 0.008 500 0.02 ± 0.03 1000 0.07 ± 0.04 10000 8.57 ± 0.1

r = 3 

m = n time (s.) 100 0.03 ± 0.02 500 0.08 ± 0.03 1000 0.18 ± 0.08 10000 9.94 ± 0.6

r = 4 

m = n time (s.) 5 0.04 ± 0.03 7 1.80 ± 1.06 9 118 ± 68 .510 > 600 

r = 5 

m = n time (s.) 6 11 .4 ± 6.47 178 ± 104 8 > 600 Table 2: Average run time in seconds, and standard deviation, to certify feasibility among 20 runs on rank-r randomly generated matrices M = |randn (m, r ) ∗ randn (r, n )|.Table 3 reports experiments for full-rank generic matrices, to see how long the algorithm takes to certify that no rank-k solution exist for k ≤ r.We observe two trends: 

> 2Experiments performed with a 12th Gen Intel(R) Core(TM) i9-12900H 2.50 GHz, 32GB RAM, MATLAB R2019b.

16 r = 2 

m = n time (s.) 100 0.002 ± 0.002 500 0.007 ± 0.0004 1000 0.014 ± 0.0007 10000 0.14 ± 0.005 

r = 3 

m = n time (s.) 100 0.04 ± 0.001 500 0.13 ± 0.003 1000 0.24 ± 0.008 10000 2.21 ± 0.05 

r = 4 

m = n time (s.) 5 0.07 ± 0.003 7 3.11 ± 0.02 9 217 ± 0.44 10 > 600 

r = 5 

m = n time (s.) 6 21 .1 ± 0.08 7 286 ± 0.72 8 > 600 Table 3: Average run time in seconds, and standard deviation, to certify infeasibility among 20 runs on full-rank randomly generated matrices M = |randn (m, n )|.

• Algorithm 1 runs faster on full-rank matrices for r ∈ { 2, 3}, especially as the dimension increases. For example, it takes Algorithm 1 about 8.5 seconds to process 10 4-by-10 4 generic matrices of rank 2, while 0.14 seconds to certify no rank-2 solution exist for full rank generic matrices of the same size. The reason is that when a feasible solution exists in the low-rank case, it has to construct it and go through all m rows of W and n columns of H. In the full-rank generic case, even an ( r + 1)-by-( r + 1) sub-matrix will not have a feasible rank-r solution, and hence the runtime of the algorithm is almost independent of ( m, n ). It will not need to explore more than r + 1 rows and columns of W and H to decide for infeasibility. 

• For more difficult instances with r > 3 and m, n close to r, the full-rank matrices take typically more time to process. Algorithm 1 has to try all sign patterns for a given ( I, J ) to make sure there is no feasible solution. It compensates for the fact that it may exit the loop over the set P earlier as observed above. While for low-rank matrices, it will stop as soon as a feasible solution is found. This also explains why the the standard deviation is close to zero for full-rank matrices: all sign patterns have to be checked since none are feasible. On the contrary, for low-rank matrices, there is randomness on how many patterns have to be tried before finding a feasible one. For example, for r = 5, m = n = 7, the shortest runtime for low-rank matrices over the 20 runs is 3.5 s and the longest is 315 s, while there are all about 286 s in the full-rank case. 

3.3 Complexity of ExactEPMF 

We can now prove that solving ExactEPMF for r fixed can be done in polynomial time, while, for the generic case when r is part of the input, the algorithm is FPT. 

Corollary 3.9. Let p ∈ R, let X ∈ Rm×n 

> ≥0

, and let r ≥ 1 be fixed. There is a deterministic algorithm running in time polynomial in m and n that decides whether there exist matrices W ∈ Rm×r and 

H ∈ Rr×n such that X = |W H |◦p, and outputs a solution if such a pair (W, H ) exists. Moreover, when r is a part of the input, the algorithm is FPT for generic input matrices. Proof. Note that, for p < 0, we must have X > 0 otherwise there cannot exist ( W, H ) such that 

X = |W H |◦p. Set 3 M = X◦1/p .There exist W ∈ Rm×k and H ∈ Rk×n for some k ≤ r such that X = |W H |◦p if and only if there exists a matrix Y ∈ Rm×n such that 

|Y | = M and rank( Y ) ≤ r.   

> 3Except for this step, the rest of the algorithm can be performed in the Blum-Shub-Smale (BSS) model of computa-tion [5, 6]. To accommodate this step, we require oracle access to exact pth root computation for a fixed p.

17 Indeed, if Y = W H , then |Y |◦p = X. In the other way, if such a matrix Y exists, take any rank-k

factorization Y = W H with W ∈ Rm×k and H ∈ Rk×n. Thus, the problem is exactly the signing problem for the positive matrix M = X◦1/p , and the result follows from Theorems 3.2 and 3.8. The proof highlights the structural reason behind the fixed-rank tractability of ExactEPMF .Once a nonsingular basis block has been fixed, the remaining sign choices are not independent; they must satisfy a system of quadratic relations. The key point is that these conditions become linear when expressed in the space of symmetric k×k matrices, whose dimension depends only on k. Thus, although the number of possible sign choices is exponential in m, n , only a bounded number of representative rows is needed to certify all constraints. The next section shows that this tractability is specific to exact feasibility: once one considers the Frobenius-norm approximation problem, the fixed-rank problem becomes NP-hard already for r = 2. 

# 4 NP-hardness of FroEPMF with r fixed 

We now turn to the Frobenius-norm version of EPMF in the fixed-rank regime. 

Definition 4.1 (Frobenius power factorization ( FroEPMF )) . Given a nonnegative matrix X ∈ Rm×n

> ≥0

and an integer r, the Frobenius absolute p-power factorization problem is 

arg min   

> W∈Rm×r,H ∈Rr×n

∥X − | W H |◦p∥2 

> F

.

The rank-one case. As for NMF, the rank-one case is elementary and can be solved in polynomial time. Indeed, when r = 1, writing W = w ∈ Rm and H⊤ = h ∈ Rn, we have 

|W H |◦pij = |wihj |p = |wi|p|hj |p.

Thus, by setting ai = |wi|p and bj = |hj |p, the rank-one FroEPMF problem is equivalent to min  

> a∈Rm
> +, b ∈Rn
> +

∥X − ab ⊤∥2 

> F

.

Since X ≥ 0, the best unconstrained rank-one approximation of X in Frobenius norm admits a nonnegative solution. The rank-one FroEPMF problem can be solved in polynomial time using the truncated SVD: let ( u, σ, v ) be the first singular triplets of X, then a = σ|u| and b = |v| is an optimal nonnegative solution, because ( X(i, j ) − σu ivj )2 ≥ (X(i, j ) − σ|ui|| vj |)2 since X ≥ 0. Note that it could happen that ai = 0 for some i, e.g., when X(i, :) = 0 (and similarly for bj ). In this case and when 

p < 0, the optimal solution is not attained, since wi → +∞ would be optimal. However, this does not happen when XX ⊤ and X⊤X are irreducible since the matrix then contains a positive singular pair by the Perron-Frobenius theorem. Note that, for p = 0, the problem is not interesting since |W H |p is the all-one matrix for all ( W, H ). 

The rank-two case. The main result of this section shows that the tractability of the rank-one case disappears immediately when r = 2. Our proof proceeds by a polynomial-time reduction from the decision version of the NP-hard Cut-Norm problem for sign matrices [24]. We begin by formally stating the decision versions of both problems. 18 Cut-Norm. 

Given: a matrix M ∈ {± 1}s×t, and D ∈ N.

Question: does there exist ( u, v ) ∈ {± 1}s × {± 1}t such that ⟨M, uv ⊤⟩ ≥ D?

R2-FroEPMF. 

Given: a matrix X ∈ Rm×n 

> ≥0

and T ∈ R+.

Question: for a fixed p ≥ 1, does there exist ( W, H ) ∈ Rm×2 × R2×n such that ∥X − | W H |◦p∥2 

> F

≤ T ?Cut-Norm is NP-hard due to a polynomial-time reduction from the MAX-CUT problem [24, The-orem 1]. Let us now reduce Cut-Norm to R2-FroEPMF in polynomial-time. Given an instance ( M, D )of Cut-Norm, we construct an instance ( X, T ) of R2-FroEPMF as follows: 

• N =

l

max 

n

(24 p)2(st )3, (24 p)p(st )p+ 12

om 

,

• T = st −D 

> 2

,

• m = s + 2 N and n = t + 2 N ,

• X ∈ { 0, 1, 2p}m×n is defined as follows: 

X =



B 1s×N 1s×N

1N ×t 2p 1N ×N 0N ×N

1N ×t 0N ×N 2p 1N ×N

 ,

with 

B = M + 1s×t

2 ∈ { 0, 1}s×t.

Theorem 4.2. The instance (X, T ) is a yes-instance of R2-FroEPMF if and only if (M, D ) is a yes-instance of CUT-NORM. Hence, R2-FroEPMF is NP-hard. Proof. Let us denote 

I = {1, . . . , s }, R = {s + 1 , . . . , s + N }, R′ = {s + N + 1 , . . . , s + 2 N },

the indices of the three row blocks of X, and 

J = {1, . . . , t }, C = {t + 1 , . . . , t + N }, C′ = {t + N + 1 , . . . , t + 2 N },

the indices of the three column blocks of X.

If part. Suppose first that ( M, D ) is a yes-instance of CUT-NORM. It means that there exist 

u ∈ {± 1}s and v ∈ {± 1}t such that u⊤M v ≥ D. We define W ∈ R(s+2 N )×2 and H ∈ R2×(t+2 N ) as follows: 

W (i, :) = 



(1 , u i), i ∈ I, 

(2 , 0) , i ∈ R, 

(0 , 2) , i ∈ R′,H(: , j ) = 

 

> 12

(1 , v j )⊤, j ∈ J, 

(1 , 0) ⊤, j ∈ C, 

(0 , 1) ⊤, j ∈ C′.

In order to show that ∥X − | W H |◦p∥2 

> F

≤ T , we examine the nine blocks of X:19 • I × J: For ( i, j ) ∈ I × J, we have W (i, :) = (1 , u i) and H(: , j ) = 12 (1 , v j )⊤ which gives 

W (i, :) H(: , j ) = uivj + 1 2 ∈ { 0, 1}.

The objective function on X(I, J ) is 

∥X(I, J ) − | W (I, :) H(: , J )|◦p∥2 

> F

=

> s

X

> i=1
> t

X

> j=1

 Mij + 1 2 − uivj + 1 2

2

= 14

> s

X

> i=1
> t

X

> j=1

M 2 

> i,j

− 2uivj Mij + ( uivj )2

= 14

2st − 2

> s

X

> i=1
> t

X

> j=1

uivj Mij 



= st − ⟨ M, uv ⊤⟩

2 .

• R × C and R′ × C′: the matrix |W H |◦p is equal to 2 p, hence 

∥X(R, C ) − | W (R, :) H(: , C )|◦p∥2 

> F

= ∥X(R′, C ′) − | W (R′, :) H(: , C ′)|◦p∥2 

> F

= 0 .

• R × C′ and R′ × C: the matrix |W H |◦p is equal to 0, hence 

∥X(R, C ′) − | W (R, :) H(: , C ′)|◦p∥2 

> F

= ∥X(R′, C ) − | W (R′, :) H(: , C )|◦p∥2 

> F

= 0 .

• I × C, I × C′, R × J, R′ × J: every inner product has absolute value 1, so |·| p = 1, matching the entries of X exactly. Finally, 

∥X − | W H |◦p∥2 

> F

= st − ⟨ M, uv ⊤⟩

2 ≤ st − D

2 = T. 

Hence ( X, T ) is a yes-instance of R2-FroEPMF. 

The only if part. Suppose now that ( X, T ) is a yes-instance of R2-FroEPMF, that is, there exist 

W ∈ Rm×2 and H ∈ R2×n such that 

∥X − | W H |◦p∥2 

> F

≤ T. (10) We show that this implies that ( M, D ) is a yes-instance of Cut-Norm . To do so, we exploit the repeated rows and columns of X. For a fixed H, all rows X(i, :) with i ∈ R are the same; hence, we may choose all the rows W (i, :), i ∈ R, to be equal in an optimal solution. The same argument applies to the rows indexed by R′, and symmetrically to the columns indexed by C and C′. Hence, without loss of generality, we may assume that ( W, H ) has the form 

W =



a a′

r11N r21N

r′

> 1

1N r′

> 2

1N

 , H =



b b′

c11N c21N

c′

> 1

1N c′

> 2

1N



> ⊤

, (11) 20 for some a, a ′ ∈ Rs, b, b ′ ∈ Rt, and r1, r ′

> 1

, r 2, r ′

> 2

, c 1, c ′

> 1

, c 2, c ′ 

> 2

∈ R. From this solution of the R2-

FroEPMF instance, we construct a solution of the Cut-Norm instance. We define u ∈ {± 1}s and 

v ∈ {± 1}t using the signs of a and b:

ui =

(

+1 , when aia′ 

> i

≥ 0,

−1, when aia′ 

> i

< 0, vj =

(

+1 , when bj b′ 

> j

≥ 0,

−1, when bj b′ 

> j

< 0. (12) It remains to prove that with this solution ( u, v ) and the hypothesis (10), we have ⟨M, uv ⊤⟩ ≥ D.We now decompose the objective according to four parts of X, following the division [ s+2 N ] = I∪ eR

and [ t + 2 N ] = J ∪ eC, where eR = R ∪ R′ and eC = C ∪ C′:

∥X − | W H |◦p∥2 

> F

= EIJ + EI eC + E eRJ + E eR eC ,

where EIJ is the contribution of the top-left block I × J, EI eC is the contribution of the top-right blocks I × eC, E eRJ the contribution of the bottom-left blocks eR × J, and E eR eC the contribution of the bottom-right blocks eR × eC. Note that since 

T = st − D

2 ≤ st, 

we have ∥X − | W H |◦p∥2 

> F

≤ T ≤ st .

Step 1: the eR × eC bottom-right block constrains r1, r ′

> 1

, r 2, r ′

> 2

, c 1, c ′

> 1

, c 2, c ′

> 2

. We first analyze the four N × N blocks in the bottom-right corner. Their contribution is 

E eR eC = ∥X( eR, eC) − | W ( eR, :) H(: , eC)|◦p∥2

> F

= N 2

(2 p − | r⊤c|p)2 + |r⊤c′|2p + |r′⊤ c|2p + (2 p − | r′⊤ c′|p)2

. (13) where r = [ r1, r 2], and similarly for r′ and c. Now suppose c′ = λc and fix x = |r⊤c|p, y = |r′⊤ c|p,hence, using Lemma A.2 with a = 2 p and b = |λ|p,

E eR eC = N 2

(2 p − x)2 + |λ|2px2 + y2 + (2 p − | λ|py)2

≥ 22pN 2,

a contradiction. Since W H = ( W S )( S−1H) for any invertible matrix S, we may assume c = (1 , 0) and c′ = (0 , 1). Therefore 

E eR eC = N 2 (2 p − rp 

> 1

)2 + |r2|2p + |r′

> 1

|2p + (2 p − r′

> 2
> p

)2. (14) Since all four terms in (14) are positive, each of them is strictly smaller than st . Therefore, by denoting 

ε = √st/N < 1, we have rp 

> 1

∈ [2 p − ε, 2p + ε] and r1 ≥ 1 since rp 

> 1

≥ 2p − 1 ≥ 1 (the same holds for r′

> 2

). Moreover, for α, β ≥ 1, we have |αp − βp| ≥ | α − β|. Thus, 

r1 ∈ [2 − ε, 2 + ε], r′ 

> 2

∈ [2 − ε, 2 + ε].

Since ε < 1, we also have ε ≤ ε1/p . For simplicity, we use the weaker but uniform following bounds: 

r1, r ′ 

> 2

∈

h

2 − ε 1 

> p

, 2 + ε 1

> p

i

, r′

> 1

, r 2 ∈

h

−ε 1 

> p

, ε 1

> p

i

. (15) 21 Step 2: the I × eC and eR × J side blocks constrain the entries of a, a ′, b, b ′. We analyze the two rectangular blocks I × eC:

EI eC = ∥X(I, C ∪ C′) − | W (I, :) H(: , C ∪ C′)|◦p∥2 

> F

= N

> s

X

> i=1



(1 − | ai|p)2 + (1 − | a′

> i

|p)2

.

Since EI eC ≤ st , |ai|p − 1 ≤

q st N , |a′

> i

|p − 1 ≤

q st N , that is, 

|ai| − 1 ≤

r st N , |a′

> i

| − 1 ≤

r st N . (16) In the same way, for the blocks R × J and R′ × J,

|r11⊤ 

> N

b + r11⊤ 

> N

b′|p − 1 ≤

r st N , which gives |r11⊤ 

> N

b + r11⊤ 

> N

b′| − 1 ≤

r st N .

and 

|r′

> 1

1⊤ 

> N

b + r′

> 1

1⊤ 

> N

b′|p − 1 ≤

r st N , which gives |r′

> 1

1⊤ 

> N

b + r′

> 1

1⊤ 

> N

b′| − 1 ≤

r st N ,

Using these relations and the bounds (15), we obtain, by Lemma A.1 (see Appendix A), the following bounds on |bj | and |b′ 

> j

|:

|bj | − 12 ≤ √N ε + ε1/p , |b′ 

> j

| − 12 ≤ √N ε + ε1/p . (17) 

Step 3: the I × J top-left block controls the disagreements between M and uv ⊤. We now turn to the block I × J. We distinguish the two types of disagreements between M and uv ⊤, in the sets F0 and F1 defined below, together with the two types of agreeing entries, in the sets F2 and F3:

F0 = {(i, j ) ∈ I × J : Mij = −1, u ivj = 1 },F1 = {(i, j ) ∈ I × J : Mij = 1 , u ivj = −1},F2 = {(i, j ) ∈ I × J : Mij = 1 , u ivj = 1 },F3 = {(i, j ) ∈ I × J : Mij = −1, u ivj = −1}.

The sets F0 and F1 are exactly the disagreement positions between M and uv ⊤, and we have 

⟨M, uv ⊤⟩ = st − 2( |F0| + |F1|). (18) In the only-if direction, the factors (11) are supposed to be arbitrary. Thus, unlike in the construction of the if direction, we cannot assume that ˜ ai := ( ai, a ′

> i

)⊤ = (1 , u i)⊤ and ˜b := ( bj , b ′ 

> j

)⊤ = 12 (1 , v j )⊤

on the block I × J. However, we have seen that the side blocks force an approximate version of this structure: |ai| and |a′

> i

| are close to 1, while |bj | and |b′ 

> j

| are close to 1 /2. Hence, the signs defined in (12) allow us to link the values |˜a⊤ 

> i

˜bj |p to the objective (18). In the following, we decompose EIJ over the four sets and remove the nonnegative contributions coming from the agreeing positions: 

EIJ = X

> (i,j )∈F0

(0 − | ˜a⊤ 

> i

˜bj |p)2 + X

> (i,j )∈F1

(1 − | ˜a⊤ 

> i

˜bj |p)2 + X

> (i,j )∈F2

(1 − | ˜a⊤ 

> i

˜bj |p)2 + X

> (i,j )∈F3

(0 − | ˜a⊤ 

> i

˜bj |p)2

≥ X

> (i,j )∈F0

|˜a⊤ 

> i

˜bj |2p + X

> (i,j )∈F1

(1 − | ˜a⊤ 

> i

˜bj |p)2.

22 Now we have to bound |˜a⊤ 

> i

˜bj |p on F0 and F1. By denoting η := 

q st N +

 √st N

 1

> p

and using (16),(17), we have 

|ai| − 1 ≤ η, |bj | − 12 ≤ η, |a′

> i

| − 1 ≤ η, and |b′ 

> j

| − 12 ≤ η. (19) Since N ≥ (24 p)2(st )3, r st N ≤

s

st 

(24 p)2(st )3 = 124 pst .

Moreover, since N ≥ (24 p)p(st )p+ 12 , we get 

 √st N

1/p 

≤

√st 

(24 p)p(st )p+ 12

!1/p 

= 124 pst .

Therefore 

η =

r st N +

 √st N

1/p 

≤ 112 pst . (20) Let us analyze the indices in F0 and F1 separately: 

• For ( i, j ) ∈ F0, uivj = 1, hence both terms aibj and a′

> i

b′ 

> j

have the same sign. Thus 

|˜a⊤ 

> i

˜bj | = |aibj + a′

> i

b′ 

> j

| = |aibj | + |a′

> i

b′ 

> j

| ≥ 2 (1 − η)

 12 − η



≥ 1 − 3η. 

By combining this bound on |˜a⊤ 

> i

˜bj | with (20), 

|˜a⊤ 

> i

˜bj |p ≥ (1 − 3η)p ≥ 1 − 3pη ≥ 1 − 14st . (21) 

• For ( i, j ) ∈ F1, uivj = −1; hence both terms aibj and a′

> i

b′ 

> j

have opposite signs. Thus 

|˜a⊤ 

> i

˜bj | = || aibj | − | a′

> i

b′ 

> j

|| ≤ (1 + η)

 12 + η



− (1 − η)

 12 − η



= 3 η. 

By combining this bound on |˜a⊤ 

> i

˜bj | with (20), 

|˜a⊤ 

> i

˜bj |p ≤ (3 η)p ≤

 14pst 

p

≤ 14st . (22) By combining (21) and (22), we obtain 

EIJ ≥ X

> (i,j )∈F0

|˜a⊤ 

> i

˜bj |2p + X

> (i,j )∈F1

(1 − | ˜a⊤ 

> i

˜bj |p)2

≥



1 − 14st 

2

|F0| +



1 − 14st 

2

|F1|≥



1 − 14st 

2  |F0| + |F1| ≥



1 − 12st 

  |F0| + |F1|.

23 Since EIJ ≤ T , T ≤ st  

> 2

and st ≥ 1, 

|F0| + |F1| ≤ T

1 − 12st 

= T +

> T
> 2st

1 − 12st 

≤ T +

> 14

1 − 12

= T + 12 .

Hence 

⟨M, uv ⊤⟩ = st − 2( |F0| + |F1|) > st − 2( T + 12 ) ≥ st − 2 st − D

2 − 1 = D − 1.

Since ⟨M, uv ⊤⟩ and D are integers, this implies ⟨M, uv ⊤⟩ ≥ D.

# 5 Conclusion 

In this paper, we studied the computational complexity of entrywise power matrix factorization (EPMF), a class of non-linear matrix decompositions (NMDs). We showed that EPMF is tractable in the exact fixed-rank regime, but otherwise it is NP-hard. For the exact fixed rank regime, we showed that the problem is equivalent to a signing problem: flip the signs of some entries of a given matrix to make it rank r. We proposed a constructive algorithm polynomial in ( m, n ) to solve this problem (Theorem 3.2). Moreover, we showed that the algorithm is FPT for generic matrices (Theorem 3.8). When the rank is not fixed, the problem is strongly NP-hard in the exact case (Corollary 2.3), and the least-squares optimization problem is already NP-hard for the smallest nontrivial rank, namely 

r = 2 (Theorem 4.2). Further works include the study of conditions under which EPMF, and other NMDs, are tractable (e.g., using convexifications, or analyzing the loss landscape, as done for many low-rank matrix approx-imation problems), as well as the design of efficient algorithms and their use in real-world applications. We could also explore whether we can further reduce the computational cost of the algorithm that solves the sign problem in Theorem 3.2. 

Use of AI assistance. During the preparation of this manuscript, the authors used ChatGPT (OpenAI) to assist with improving the presentation of several proofs, exploring alternative proof strategies, and checking the clarity and logical consistency of mathematical arguments. The AI system was used solely as a writing and reasoning aid; all mathematical results, proofs, and conclusions were independently verified by the authors, who take full responsibility for the content of this manuscript. 

# References   

> [1] Arora, S., Ge, R., Kannan, R., Moitra, A.: Computing a nonnegative matrix factorization–provably. In: ACM Symposium on Theory of computing, pp. 145–162 (2012) [2] Awari, A., Gillis, N., Vandaele, A.: Alternating direction method of multipliers for nonlinear matrix decompositions. arXiv preprint arXiv:2512.17473 (2025) [3] Awari, A., Nguyen, H., Wertz, S., Vandaele, A., Gillis, N.: Coordinate descent algorithm for nonlinear matrix decomposition with the relu function. In: European Signal Processing Conference (EUSIPCO), pp. 2622–2626 (2024) [4] Bhangale, A., Kopparty, S.: The complexity of computing the minimum rank of a sign pattern matrix. arXiv preprint arXiv:1503.04486 (2015) [5] Blum, L., Cucker, F., Shub, M., Smale, S.: Complexity and real computation, 1998 edn. Springer, New York, NY (1997)

24 [6] Blum, L., Shub, M., Smale, S.: On a theory of computation over the real numbers; NP completeness, recursive functions and universal machines. In: Annual Symposium on Foundations of Computer Science, pp. 387–397 (1988) [7] Camion, P., Hoffman, A.: On the nonsingularity of complex matrices. Pac. J. Math. 17 (2), 211–214 (1966) [8] Cand` es, E., Li, X., Ma, Y., Wright, J.: Robust principal component analysis?: Recovering low-rank matrices from sparse errors. In: 2010 IEEE sensor array and multichannel signal processing workshop, pp. 201–204. IEEE (2010) [9] Cand` es, E.J., Tao, T.: The power of convex relaxation: Near-optimal matrix completion. IEEE Transac-tions on Information Theory 56 (5), 2053–2080 (2010) [10] Chandrasekaran, V., Sanghavi, S., Parrilo, P.A., Willsky, A.S.: Rank-sparsity incoherence for matrix decomposition. SIAM Journal on Optimization 21 (2), 572–596 (2011) [11] Chi, Y., Lu, Y.M., Chen, Y.: Nonconvex optimization meets low-rank matrix factorization: An overview. IEEE Transactions on Signal Processing 67 (20), 5239–5269 (2019) [12] Darmann, A., D¨ ocker, J.: On a simple hard variant of not-all-equal 3-sat. Theoretical Computer Science 

815 , 147–152 (2020) [13] Eckart, C., Young, G.: The approximation of one matrix by another of lower rank. Psychometrika 1(3), 211–218 (1936) [14] Fawzi, H., Gouveia, J., Parrilo, P.A., Robinson, R.Z., Thomas, R.R.: Positive semidefinite rank. Mathe-matical Programming 153 (1), 133–177 (2015) [15] Federer, H.: Geometric Measure Theory. Springer Berlin Heidelberg (1996) [16] Folland, G.: Real Analysis: Modern Techniques and Their Applications. Pure and Applied Mathematics: A Wiley Series of Texts, Monographs and Tracts. Wiley (2013) [17] Garey, M.R., Johnson, D.S.: Computers and intractability, vol. 29. wh freeman New York (2002) [18] Ge, R., Jin, C., Zheng, Y.: No spurious local minima in nonconvex low rank problems: A unified geometric analysis. In: International Conference on Machine Learning, pp. 1233–1242 (2017) [19] Ge, R., Lee, J.D., Ma, T.: Matrix completion has no spurious local minimum. Advances in neural infor-mation processing systems 29 (2016) [20] Gillis, N., Glineur, F.: Low-rank matrix approximation with weights or missing data is NP-hard. SIAM Journal on Matrix Analysis and Applications 32 (4), 1149–1165 (2011) [21] Gillis, N., Porcelli, M., Seraghiti, G.: An extrapolated and provably convergent algorithm for nonlin-ear matrix decomposition with the ReLU function. SIAM J. on Mathematics of Data Science (2026). ArXiv:2503.23832 [22] Gillis, N., Saha, S., Sicilia, S., Vandaele, A.: Manifold-based algorithms for the Hadamard decomposition. arXiv preprint arXiv:2605.28980 (2026) [23] Gillis, N., Shitov, Y.: Low-rank matrix approximation in the infinity norm. Linear Algebra and its Appli-cations 581 , 367–382 (2019) [24] Gillis, N., Vavasis, S.A.: On the complexity of robust PCA and ℓ1-norm low-rank matrix approximation. Mathematics of Operations Research 43 (4), 1072–1084 (2018) [25] Golub, G.H., Van Loan, C.F.: Matrix computations. Johns Hopkins University Press, Baltimore (2013) [26] Goreinov, S.A., Tyrtyshnikov, E.E., Zamarashkin, N.L.: A theory of pseudoskeleton approximations. Linear algebra and its applications 261 (1-3), 1–21 (1997) 

25 [27] Goucha, A.P., Gouveia, J.: The phaseless rank of a matrix. SIAM Journal on Applied Algebra and Geometry 5(3), 526–551 (2021) [28] Gouveia, J., Robinson, R., Thomas, R.R.: Polytopes of minimum positive semidefinite rank. Mathematical Programming 153 , 201–221 (2013) [29] Huang, Q., Ko, T., Zhuang, Z., Tang, L., Zhang, Y.: Hira: Parameter-efficient Hadamard high-rank adap-tation for large language models. In: The Thirteenth International Conference on Learning Representations (2025) [30] Hyeon-Woo, N., Ye-Bin, M., Oh, T.: Fedpara: Low-rank Hadamard product parameterization for efficient federated learning. ICLR (2022) [31] Kishore Kumar, N., Schneider, J.: Literature survey on low rank approximation of matrices. Linear and Multilinear Algebra 65 (11), 2212–2244 (2017) [32] Kueng, R., Tropp, J.A.: Binary component decomposition part II: The asymmetric case. arXiv preprint arXiv:1907.13602 (2019) [33] Kueng, R., Tropp, J.A.: Binary component decomposition part I: the positive-semidefinite case. SIAM Journal on Mathematics of Data Science 3(2), 544–572 (2021) [34] Le, Q.T., Riccietti, E., Gribonval, R.: Spurious valleys, NP-hardness, and tractability of sparse matrix factorization with fixed support. SIAM Journal on Matrix Analysis and Applications 44 (2), 503–529 (2023) [35] Lee, T., Wei, Z.: The square root rank of the correlation polytope is exponential. arXiv preprint arXiv:1411.6712 (2014) [36] Lefebvre, J., Vandaele, A., Gillis, N.: Component-wise squared factorization. In: International Workshop on Machine Learning for Signal Processing (MLSP) (2024) [37] Loconte, L., Sladek, A., Mengel, S., Trapp, M., Solin, A., Gillis, N., Vergari, A.: Subtractive mixture models via squaring: Representation and learning. In: International Conference on Learning Representations, vol. 2024, pp. 15,751–15,785 (2024) [38] Nguyen, H., Awari, A., Vandaele, A., Gillis, N.: Nonlinear matrix decomposition with the sigmoid function. In: International Workshop on Machine Learning for Signal Processing (MLSP) (2025) [39] Recht, B., Fazel, M., Parrilo, P.A.: Guaranteed minimum-rank solutions of linear matrix equations via nuclear norm minimization. SIAM review 52 (3), 471–501 (2010) [40] Saul, L.K.: A nonlinear matrix decomposition for mining the zeros of sparse data. SIAM Journal on Mathematics of Data Science 4(2), 431–463 (2022) [41] Schaefer, T.J.: The complexity of satisfiability problems. In: Conference Record of the Tenth Annual ACM Symposium on Theory of Computing (San Diego, Calif., 1978), pp. 216–226. ACM, New York (1978) [42] Shitov, Y.: Matrices of bounded PSD rank are easy to detect. SIAM Journal on Optimization 28 (3), 2067–2072 (2018) [43] Trefethen, L.N., Bau, D.: Numerical linear algebra. SIAM, Philadelphia (2022) [44] Udell, M., Horn, C., Zadeh, R., Boyd, S.: Generalized low rank models. Foundations and Trends ® in Machine Learning 9(1), 1–118 (2016) [45] Vavasis, S.A.: On the complexity of nonnegative matrix factorization. SIAM Journal on Optimization 

20 (3), 1364–1377 (2010) [46] Wang, Q., Qu, Y., Cui, C., Han, D.: An accelerated alternating partial Bregman algorithm for ReLU-based matrix decomposition. Journal of Scientific Computing 105 (2), 48 (2025) 

26 A Lemmas for the bounds on the coefficients around 1/2

Lemma A.1 (Bounds on the coefficients around 1 /2) . Let ε > 0, p > 0, N ≥ 1 such that ε 1 

> p

< 12 ,and define δ := √N ε, η := ε1/p . Let x1, x 2 ∈ R be such that 

|(2 + a)x1 + bx 2| − 1 ≤ δ, |cx 1 + (2 + d)x2| − 1 ≤ δ, 

where a, b, c, d ∈ [−η, η ]. Then,for i = 1 , 2,

|xi| − 12 ≤ δ + η, that is , |xi| − 12 ≤ √N ε + ε1/p .

Proof. From the assumptions, there exist signs σ1, σ 2 ∈ {− 1, 1} and errors e1, e 2 with |e1|, |e2| ≤ δ

such that (2 + a)x1 + bx 2 = σ1 + e1, and cx 1 + (2 + d)x2 = σ2 + e2,

which gives 2



x1 − σ1

2



= e1 − ax 1 − bx 2 and 2



x2 − σ2

2



= e2 − dx 2 − cx 1.

By using the bounds on the different elements, we obtain 2 x1 − σ1

2 ≤ δ + η|x1| + η|x2| and 2 x2 − σ2

2 ≤ δ + η|x2| + η|x1|. (23) Set 

M := max  

> i=1 ,2

xi − σi

2 and B := max  

> i=1 ,2

|xi| for which B ≤ 12 + M. 

Using both equations of (23), we obtain 2 M ≤ δ + 2 ηB ≤ δ + η + 2 ηM , hence, (2 − 2η)M ≤ δ + η.Since η < 1/2, for i = 1 , 2, 

|xi| − 12 ≤ xi − σi

2 ≤ M ≤ δ + η

2 − 2η ≤ δ + η. 

Lemma A.2. Let a, b ∈ R. Then 

a2 = min 

> x,y ∈R

(a − x)2 + b2x2 + y2 + ( a − by )2.

Proof. Let f (x, y ) = ( a − x)2 + b2x2 + y2 + ( a − by )2. Then 

∇f (x, y ) = 

 −2( a − x) + 2 b2x

2y − 2b(a − by )



=

 (2 + 2 b2)x − 2a

(2 + 2 b2)y − 2ab 



, ∇2f (x, y ) = 

 2 + 2 b2 00 2 + 2 b2



,

which means that f is strictly convex and attains the minimum when ∇f = 0, that is at x⋆ = a

> 1+ b2

and y⋆ = ab  

> 1+ b2

. Hence 

f (x⋆, y ⋆) = 



a − a

1 + b2

2

+ a2b2

(1 + b2)2 + a2b2

(1 + b2)2 +



a − ab 2

1 + b2

2

= a2b4 + 2 a2b2 + a2

(1 + b2)2 = a2.

27