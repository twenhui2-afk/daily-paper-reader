---
title: Determining Particular Solutions for Exponential-Polynomial Forcing Terms in Linear Nonhomogeneous Recurrence Relations
source: arxiv
title_zh: 用于Exponential-Polynomial Forcing Terms in Linear Nonhomogeneous Recurrence Relations的Determining Particular Solutions
abstract_zh: "This paper develops systematic 方法用于 determining particular solutions 的 kth-order linear nonhomogeneo。\n\n核心做法：Here each p_j (n) is polynomial。\n\n主要结果：This result parallels 方法的 undetermined coefficients 用于 linear ODEs 结合 constant coefficients 与 yields。\n\n值得关注的点在于：main result is following：用于 characteristic polynomial c (t)=t^k+c_1t^{k-1}+\\cdots+c_k，if s_j denotes。"
abstract_en: "This paper develops a systematic method for determining particular solutions of the $k$th-order linear nonhomogeneous recurrence relation $$a_n + c_1 a_{n-1} + \\cdots + c_k a_{n-k} = \\sum_{j=1}^J p_j(n){r_j}^n$$ with $n \\geq k$, $c_k \\neq 0$, $r_j \\neq 0$. Here each $p_j(n)$ is a polynomial. The main result is the following: for the characteristic polynomial $c(t)=t^k+c_1t^{k-1}+\\cdots+c_k$, if $s_j$ denotes the multiplicity of $r_j$ as a root of $c(t)$ ($s_j=0$ when $r_j$ is not a root), then there exists a particular solution of the form $q_n=\\sum_{j=1}^J b_j(n)n^{s_j}r_j^n$, where each $b_j(n)$ is a polynomial of the same degree as $p_j(n)$. This result parallels the method of undetermined coefficients for linear ODEs with constant coefficients and yields a systematic procedure for determining the form of particular solutions."
authors: Heesung Shin
date: 2026-07-06
journal: math.CO
source_link: "https://arxiv.org/pdf/2607.04700v1"
pdf: "https://arxiv.org/pdf/2607.04700v1"
score: 7.6
tldr: 围绕《用于Exponential-Polynomial...》；Here each p_j (n) is polynomial。；This result parallels 方法的 undetermin...
tldr_cn: 围绕《用于Exponential-Polynomial...》；Here each p_j (n) is polynomial。；This result parallels 方法的 undetermin...
tldr_en: "This paper develops a systematic method for determining particular solutions of the $k$th-order linear nonhomogeneous recurrence relation $$a_n + c_1 a_{n-1} + \\cdots + c_k a_{n-k} = \\sum_{j=1}^J p_j(n){r_j}^n$$ with $n \\geq k$, $c_k \\neq 0$, $r_j \\neq 0$. Here each $p_j(n)$ is a polynomial."
selection_source: fresh_fetch
motivation: This paper develops systematic 方法用于 determining particular solutions 的 kth-order linear nonhomogeneo。
method: 核心做法：Here each p_j (n) is polynomial。
result: 主要结果：This result parallels 方法的 undetermined coefficients 用于 linear ODEs 结合 constant coefficients 与 yields。
conclusion: "值得关注的点在于：main result is following：用于 characteristic polynomial c (t)=t^k+c_1t^{k-1}+\\cdots+c_k，if s_j denotes。"
---

## 摘要
This paper develops systematic 方法用于 determining particular solutions 的 kth-order linear nonhomogeneo。

核心做法：Here each p_j (n) is polynomial。

主要结果：This result parallels 方法的 undetermined coefficients 用于 linear ODEs 结合 constant coefficients 与 yields。

值得关注的点在于：main result is following：用于 characteristic polynomial c (t)=t^k+c_1t^{k-1}+\cdots+c_k，if s_j denotes。

## Abstract
This paper develops a systematic method for determining particular solutions of the $k$th-order linear nonhomogeneous recurrence relation $$a_n + c_1 a_{n-1} + \cdots + c_k a_{n-k} = \sum_{j=1}^J p_j(n){r_j}^n$$ with $n \geq k$, $c_k \neq 0$, $r_j \neq 0$. Here each $p_j(n)$ is a polynomial. The main result is the following: for the characteristic polynomial $c(t)=t^k+c_1t^{k-1}+\cdots+c_k$, if $s_j$ denotes the multiplicity of $r_j$ as a root of $c(t)$ ($s_j=0$ when $r_j$ is not a root), then there exists a particular solution of the form $q_n=\sum_{j=1}^J b_j(n)n^{s_j}r_j^n$, where each $b_j(n)$ is a polynomial of the same degree as $p_j(n)$. This result parallels the method of undetermined coefficients for linear ODEs with constant coefficients and yields a systematic procedure for determining the form of particular solutions.
