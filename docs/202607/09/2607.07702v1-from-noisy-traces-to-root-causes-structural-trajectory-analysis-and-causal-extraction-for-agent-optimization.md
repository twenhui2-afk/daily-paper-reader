---
title: "From Noisy Traces to Root Causes: Structural Trajectory Analysis and Causal Extraction for Agent Optimization"
source: arxiv
title_zh: 用于Agent Optimization的由 Noisy Traces to Root Causes：Structural Trajectory Analysis与Causal Extraction
abstract_zh: "optimization 的 long-horizon agents increasingly relies on reflection-based mechanisms，where large la。\n\n核心做法：To resolve this dilemma，作者引入 STRACE (Structural TRajectory Analysis 与 Causal Extraction)，框架 that con。\n\n主要结果：However，real execution traces are difficult to use directly 用于 optimization：large trace collections。\n\n值得关注的点在于：code is available at https://github.com/moomight/STRACE。"
abstract_en: "The optimization of long-horizon agents increasingly relies on reflection-based mechanisms, where a large language model (LLM) acts as an optimizer to diagnose agent failures and improve agent policies. However, real execution traces are difficult to use directly for optimization: large trace collections are often redundant and heterogeneous, making optimization inefficient and prone to overfitting to low-value failures; meanwhile, each individual trajectory also contains many irrelevant steps, while naive context reduction methods such as truncation or sliding windows can discard causally important evidence and produce misleading optimization signals. To resolve this dilemma, we introduce STRACE (Structural TRajectory Analysis and Causal Extraction), a framework that constructs high signal-noise optimization contexts for more precise and effective optimization. At the batch level, STRACE mines failure patterns to filter redundant traces and retain representative failures; within each selected trace, it performs causal localization over a textual dependency graph to remove non-causal steps and identify the true root-cause module for optimization. Empirical results demonstrate that STRACE significantly outperforms standard context-filtering baselines. Notably, on a challenging formal verification task (VeruSAGE-Bench), it successfully optimizes human-expert designed agents, delivering $1.4\\times$ success-rate improvement (42.5% to 58.5%). The code is available at https://github.com/moomight/STRACE ."
authors: "Ying Chang, Jiahang Xu, Xuan Feng, Chenyuan Yang, Peng Cheng, Yuqing Yang"
date: 2026-07-08
journal: cs.CL
source_link: "https://arxiv.org/pdf/2607.07702v1"
pdf: "https://arxiv.org/pdf/2607.07702v1"
score: 7.8999999999999995
tldr: 围绕《用于Agent Optimization的由 N...》；To resolve this dilemma，作者引入 STR...；However，real execution traces are di...
tldr_cn: 围绕《用于Agent Optimization的由 N...》；To resolve this dilemma，作者引入 STR...；However，real execution traces are di...
tldr_en: "The optimization of long-horizon agents increasingly relies on reflection-based mechanisms, where a large language model (LLM) acts as an optimizer to diagnose agent failures and improve agent policies. However, real execution traces are difficult to use directly for optimization: large trace collections are often redundant and heterogeneous, making optimization inefficient and prone to overfitting to..."
selection_source: fresh_fetch
vocabulary: ["localization | 定位 | CET6", "optimization | 优化 | CET6"]
motivation: optimization 的 long-horizon agents increasingly relies on reflection-based mechanisms，where large la。
method: 核心做法：To resolve this dilemma，作者引入 STRACE (Structural TRajectory Analysis 与 Causal Extraction)，框架 that con。
result: 主要结果：However，real execution traces are difficult to use directly 用于 optimization：large trace collections。
conclusion: "值得关注的点在于：code is available at https://github.com/moomight/STRACE。"
---

## 摘要
optimization 的 long-horizon agents increasingly relies on reflection-based mechanisms，where large la。

核心做法：To resolve this dilemma，作者引入 STRACE (Structural TRajectory Analysis 与 Causal Extraction)，框架 that con。

主要结果：However，real execution traces are difficult to use directly 用于 optimization：large trace collections。

值得关注的点在于：code is available at https://github.com/moomight/STRACE。

## Abstract
The optimization of long-horizon agents increasingly relies on reflection-based mechanisms, where a large language model (LLM) acts as an optimizer to diagnose agent failures and improve agent policies. However, real execution traces are difficult to use directly for optimization: large trace collections are often redundant and heterogeneous, making optimization inefficient and prone to overfitting to low-value failures; meanwhile, each individual trajectory also contains many irrelevant steps, while naive context reduction methods such as truncation or sliding windows can discard causally important evidence and produce misleading optimization signals. To resolve this dilemma, we introduce STRACE (Structural TRajectory Analysis and Causal Extraction), a framework that constructs high signal-noise optimization contexts for more precise and effective optimization. At the batch level, STRACE mines failure patterns to filter redundant traces and retain representative failures; within each selected trace, it performs causal localization over a textual dependency graph to remove non-causal steps and identify the true root-cause module for optimization. Empirical results demonstrate that STRACE significantly outperforms standard context-filtering baselines. Notably, on a challenging formal verification task (VeruSAGE-Bench), it successfully optimizes human-expert designed agents, delivering $1.4\times$ success-rate improvement (42.5% to 58.5%). The code is available at https://github.com/moomight/STRACE .

## 英语词汇
- **localization**：定位（CET6）
- **optimization**：优化（CET6）
