---
title: "When Token Compression Breaks: Structural Pruning vs. Token Reduction for Robust ViT Segmentation under High Compression"
source: arxiv
title_zh: 用于Robust ViT Segmentation under High Compression的When Token Compression Breaks：Structural Pruning vs. Token Reduction
abstract_zh: "Vision Transformers (ViTs) are strong backbones 用于 semantic segmentation，but their computational cos。\n\n核心做法：Meanwhile，structural pruning provides orthogonal route to efficiency by removing redundant component。\n\n主要结果：Our results reveal consistent trend on both clean 与 corrupted inputs：token compression is highly eff。\n\n值得关注的点在于：Code is available at https://github.com/phatnguyencs/vit-seg-compression。"
abstract_en: "Vision Transformers (ViTs) are strong backbones for semantic segmentation, but their computational cost limits deployment. Recent token compression methods for efficient transformer-based segmentation reduce this cost by decreasing the number of tokens. However, existing evaluations primarily focus on low-to-moderate compression, leaving their behavior under aggressive compression and corrupted inputs unclear. Meanwhile, structural pruning provides an orthogonal route to efficiency by removing redundant components in the ViT architecture, but is rarely compared to token compression under a unified protocol. To bridge this gap, we benchmark representative token compression and structural pruning methods for ViT-based semantic segmentation under matched FLOPs on ADE20K and Cityscapes, together with their common-corruption variants ADE20K-C and Cityscapes-C. Our results reveal a consistent trend on both clean and corrupted inputs: token compression is highly effective at mild reductions but degrades sharply when compression becomes severe, consistent with substantial information loss from overly aggressive token reduction. In contrast, structural pruning exhibits a smoother degradation curve and is more stable at high compression. Motivated by these findings, we study a prune-then-merge pipeline that applies moderate token compression on top of a moderately pruned backbone. At comparable FLOPs, this combined strategy consistently achieves a better accuracy-robustness trade-off at high compression, offering a practical recipe for deployment-oriented ViT segmentation. Code is available at https://github.com/phatnguyencs/vit-seg-compression."
authors: "Tien-Phat Nguyen, Ngai-Man Cheung"
date: 2026-07-02
journal: cs.CV
source_link: "https://arxiv.org/pdf/2607.02237v1"
pdf: "https://arxiv.org/pdf/2607.02237v1"
score: 6.1
tldr: 围绕《用于Robust ViT Segmentatio...》；Meanwhile，structural pruning pro...；Our results reveal consistent trend...
tldr_cn: 围绕《用于Robust ViT Segmentatio...》；Meanwhile，structural pruning pro...；Our results reveal consistent trend...
tldr_en: "Vision Transformers (ViTs) are strong backbones for semantic segmentation, but their computational cost limits deployment. Recent token compression methods for efficient transformer-based segmentation reduce this cost by decreasing the number of tokens."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "transformer | Transformer 架构 | 学术高频", "robustness | 鲁棒性 | IELTS", "benchmark | 基准测试 | CET6/IELTS"]
motivation: Vision Transformers (ViTs) are strong backbones 用于 semantic segmentation，but their computational cos。
method: 核心做法：Meanwhile，structural pruning provides orthogonal route to efficiency by removing redundant component。
result: 主要结果：Our results reveal consistent trend on both clean 与 corrupted inputs：token compression is highly eff。
conclusion: "值得关注的点在于：Code is available at https://github.com/phatnguyencs/vit-seg-compression。"
---

## 摘要
Vision Transformers (ViTs) are strong backbones 用于 semantic segmentation，but their computational cos。

核心做法：Meanwhile，structural pruning provides orthogonal route to efficiency by removing redundant component。

主要结果：Our results reveal consistent trend on both clean 与 corrupted inputs：token compression is highly eff。

值得关注的点在于：Code is available at https://github.com/phatnguyencs/vit-seg-compression。

## Abstract
Vision Transformers (ViTs) are strong backbones for semantic segmentation, but their computational cost limits deployment. Recent token compression methods for efficient transformer-based segmentation reduce this cost by decreasing the number of tokens. However, existing evaluations primarily focus on low-to-moderate compression, leaving their behavior under aggressive compression and corrupted inputs unclear. Meanwhile, structural pruning provides an orthogonal route to efficiency by removing redundant components in the ViT architecture, but is rarely compared to token compression under a unified protocol. To bridge this gap, we benchmark representative token compression and structural pruning methods for ViT-based semantic segmentation under matched FLOPs on ADE20K and Cityscapes, together with their common-corruption variants ADE20K-C and Cityscapes-C. Our results reveal a consistent trend on both clean and corrupted inputs: token compression is highly effective at mild reductions but degrades sharply when compression becomes severe, consistent with substantial information loss from overly aggressive token reduction. In contrast, structural pruning exhibits a smoother degradation curve and is more stable at high compression. Motivated by these findings, we study a prune-then-merge pipeline that applies moderate token compression on top of a moderately pruned backbone. At comparable FLOPs, this combined strategy consistently achieves a better accuracy-robustness trade-off at high compression, offering a practical recipe for deployment-oriented ViT segmentation. Code is available at https://github.com/phatnguyencs/vit-seg-compression.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **transformer**：Transformer 架构（学术高频）
- **robustness**：鲁棒性（IELTS）
- **benchmark**：基准测试（CET6/IELTS）
