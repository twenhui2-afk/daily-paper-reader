Title: Efficient Channel Prediction based on Gram-Square-Root Factorization using GMMs

URL Source: https://arxiv.org/pdf/2607.26959v1

Published Time: Thu, 30 Jul 2026 01:00:09 GMT

Number of Pages: 5

Markdown Content:
# Efficient Channel Prediction based on Gram-Square-Root Factorization using GMMs 

Kathrin Klein, Amar Kasibovic, Michael Joham, Shachar Shayovitz †, Wolfgang Utschick 

School of Computation, Information and Technology, Technical University of Munich, Germany 

> †

Toga Networks Ltd., Hod HaSharon, Israel Email: {kathrin.klein; amar.kasibovic; joham; utschick }@tum.de 

> †

Email: shachar.shayovitz@huawei.com 

Abstract —Accurate channel state information (CSI) is criti-cal for downlink (DL)-multi-user (MU)-multiple-input multiple-output (MIMO) systems, where feedback delays and mobility can degrade precoding performance. To ensure reliable beam-forming and interference mitigation, CSI prediction is required. In practical systems, full CSI feedback is often infeasible due to signaling overhead, so transmitters rely on partial CSI re-ported by the receivers. In this work, we propose a Gaussian mixture model (GMM)-based prediction framework for MIMO-orthogonal frequency-division multiplexing (OFDM) channels under partial feedback using Gram-square-root factorization. To address the high dimensionality, we introduce an efficient pa-rameter reduction technique that exploits structured covariance matrices, significantly lowering complexity without noticeable performance degradation. This reduction is based on the Gram-square-root factorization and remains of interest even when full CSI is available. Simulation results demonstrate that GMMs achieve the highest prediction accuracy and correctly capture the underlying channel subspaces, which is essential for effective MU-precoding. The proposed method outperforms classical baselines such as zero-order hold (ZOH), first-order hold (FOH), and linear minimum mean squared error (LMMSE) predictors, and an advanced neural network (NN)-based predictor. Notably, the parameter-reduced partial CSI GMM achieves performance comparable to that of full CSI prediction, highlighting its ability to efficiently model the channel structure under limited feedback. 

Index Terms —Wireless channel prediction, partial CSI, DL-MU-MIMO, OFDM, Gaussian mixture models 

I. Introduction and Motivation 

DL-MU-MIMO transmission is a key enabler for high throughput in modern wireless communication systems. By allowing a transmitter to serve multiple users simultaneously, these systems significantly improve spectral efficiency and overall network capacity. Precoding strategies, which spatially separate user signals and minimize inter-user interference, rely on accurate channel knowledge at the transmitter like in [1], [2, Sec. 13.7]. In indoor scenarios, even under low mobility conditions with user speeds on the order of 1–3 km/h, the delay between channel estimation and the actual data transmission introduces a mismatch between the estimated and the true CSI. This delay, often on the order of several milliseconds due to channel sounding, feedback, and processing, allows the wireless channel to evolve because of user motion or environmental changes. As a result, the previously acquired 

This work was funded by Toga Networks Ltd., Hod HaSharon, Israel. 

CSI may become outdated, leading to reduced beamforming gains, increased inter-user interference, and overall perfor-mance degradation. To address this, robust prediction of future CSI is required. Traditional approaches, such as ZOH or FOH (also called linear extrapolation) [3, Sec. 2], offer low complexity but rapidly degrade in the presence of mobility and for longer prediction horizons. Other methods like prediction using LMMSE [4, Sec. 9.1.6] exploit statistical correlations between past and future CSI, but they rely on Gaussian assumptions that often do not hold in real-world channels. In practical systems, an additional challenge arises in the DL, where the transmitter relies on channel information esti-mated by the receivers and only a compressed representation of the CSI is reported leading to partial CSI at the transmitter. A prominent example of such a system is wireless fidelity (Wi-Fi), where IEEE 802.11 standards [1] rely on compressed feedback mechanisms to limit signaling overhead. In these systems, each receiver estimates its DL channel and feeds back a compressed representation containing dominant singular val-ues and the corresponding right singular vectors, while the left singular vectors are not reported. Consequently, the transmitter reconstructs only an approximate, limited representation of the channel. This introduces a broader challenge that arises in many practical feedback limited MIMO systems. Previous work on channel prediction using machine learning (ML) has largely focused on cellular networks as in [5], [6], of-ten providing forward-looking discussions on MIMO-OFDM scenarios without concrete implementation. Importantly, these approaches typically assume access to full CSI, and thus do not account for the partial feedback. In this work, we investigate GMMs for partial CSI prediction. Our main contributions are summarized as follows:  

> •

We propose a prediction framework for forecasting fu-ture CSI in wireless, high-dimensional DL-MU-MIMO-OFDM systems.  

> •

Channel prediction is performed under partial, limited feedback using Gram-square-root factorization.  

> •

We introduce a parameter reduction technique that lever-ages structural properties of the covariance matrix in the presence of partial feedback, without significant perfor-mance loss.  

> •

We demonstrate that the proposed approach outperforms conventional channel prediction baselines including NNs. arXiv:2607.26959v1 [eess.SP] 29 Jul 2026 II. System Model 

Let the tensor H ∈ CNt×Nf×Nrx ×Ntx be a MIMO-OFDM channel, where Ntx and Nrx denote the number of transmit and receive antennas, respectively. Nf is the number of subcarriers and Nt the number of time steps. For channel prediction, we consider that each data sample consists of Nt = Nt,o +Nt,p time steps, where Nt,o represents the number of observed (past) time steps and Nt,p the number of predicted (future) time steps. The dataset of vectorized channels h(ℓ) = vec (H(ℓ)) ∈ CNtotal is denoted as H = {h(ℓ)}Lℓ=1 , where L is the dataset’s total num-ber of channel realizations (samples) and Ntotal = NtNfNrx Ntx 

denotes the total dimension of each data sample. Each sample 

h(ℓ) can be decomposed into the observed channel h(ℓ) 

> o

and the predicted channel h(ℓ) 

> p

:

h(ℓ) =

"

h(ℓ)

> o

h(ℓ)

> p

#

, h(ℓ) 

> o

∈ CNtotal,o , h(ℓ) 

> p

∈ CNtotal,p , (1) where Ntotal,o = Nt,o NfNrx Ntx and Ntotal,p = Nt,p NfNrx Ntx .We model noisy channel observations as yo = ho + n, where 

n ∼ N C(0, σ 2I) represents additive Gaussian noise. In the following, we use the notation ˆhp for the prediction, which estimates the true future channel hp based on the noisy channel observation yo.

III. Preliminaries on Channel Prediction using GMMs 

Based on the system model defined in Section II, we briefly summarize the approach in [6], which serves as the foundation for our model. Given training data at arbitrary signal-to-noise ratios (SNRs), we can train a GMM offline and later use it online to predict future channels from past noisy observations. 

A. Offline Training 

A GMM models the distribution of a wireless channel h as a mixture of K Gaussian components [7, Sec. 9.2-9.4], each with a mixture weight πk, a mean μk, and a covariance Ck:

p(h) = 

> K

X

> k=1

πkNC(h; μk, Ck), (2) with latent variable k indicating the component. Model pa-rameters are optimized using the expectation–maximization (EM) algorithm, iterating between an E-step and a M-step. At iteration i, the E-step calculates a so-called ”responsibility” of component k, for a given sample h [7, Sec. 9.2-9.4] as 

pi(k|h) = πkNC(h; μk, Ck)

PKj=1 πj NC(h; μj , Cj ) . (3) Given the responsibilities, in the M-step, the parameters 

{πk, μk, Ck} for each component k are updated to maximize the expected complete-data log-likelihood as described in [7, Sec. 9.2-9.4]. The algorithm alternates between E- and M-step until convergence of the log-likelihood. After convergence, the 

k-th component encodes the following temporal structure: 

μk =

μk, ho

μk, hp



, Ck =

Ck, hoho Ck, hohp

Ck, hpho Ck, hphp



, (4) where the subscript denotes the component index k and the associated random variables, here ho and hp.

B. Online Channel Prediction 

Given an observation yo, the prediction of the future channel 

ˆhp is obtained via the conditional expectation [6] 

ˆhp,GMM = E[hp | yo] = 

> K

X

> k=1

p(k | yo) E[hp | yo, k ], (5) with posterior weights and conditional means 

p(k | yo) = πk NC(yo; μk, ho , Ck, hoho + σ2I)

PKj=1 πj NC(yo; μj, ho , Cj, hoho + σ2I) ,

E[hp | yo, k ] = 

Ck, hpho (Ck, hoho + σ2I)−1(yo − μk, ho ) + μk, hp .

(6) Using (5) allows robust, noise-aware prediction, capturing non-linear dependencies between past and future channels. 

IV. Partial Channel Prediction using GMMs 

In many standardized DL systems that employ spatial pre-coding, the transmitter relies on receivers’ feedback containing only partial information about the spatial CSI. This is the case, for example, in IEEE 802.11n [1] and later standards. In particular, the receiver estimates the full spatial CSI given by 

Ht,f ∈ CNrx ×Ntx for each time step t and subcarrier f , and compresses it using a singular value decomposition (SVD). Only a reduced approximation based on the dominant modes is fed back, where the number of retained dominant modes is Ns ≤ min( Nrx , N tx ). The feedback consists of the diagonal matrix St,f = diag( st,f, 1, . . . , s t,f,N s ) ∈ CNs×Ns , where st,f,i 

denote the i-th singular value, along with the corresponding right singular vectors Vt,f ∈ CNtx ×Ns . The corresponding left singular vectors Ut,f ∈ CNrx ×Ns , which describe the receive-side spatial directions, are not transmitted, leaving the transmitter with a partial view of the DL channel for precoding [1], [2, Sec. 13.7]. Therefore, we must operate on partial CSI, making traditional full CSI approaches unsuitable and motivating a ML approach to predict the future St,f and Vt,f .In the following, we consider only partial channel feedback. Therefore, we define the partial CSI for a specific time step t

and subcarrier f as: 

Zt,f = St,f V H 

> t,f

∈ CNs×Ntx . (7) Here, Zt,f can be interpreted as the square-root of the (re-duced) Gram matrix of the full channel, because HH 

> t,f

Ht,f =

ZH 

> t,f

Zt,f holds. Each Zt,f is vectorized along spatial dimen-sions and subcarriers resulting in zt ∈ CNfNsNtx . This now gives the new data sample zt, from which we can obtain the partial channel observation vector for Nt,o past time steps 

zo and the prediction target for Nt,p future time steps zp, as performed in Section II for the full CSI. We can then train a GMM as described in Section III-A using z. For the online prediction phase we can infer ˆzp from a noisy partial channel observation ˜yo = zo + n as 

ˆzp,GMM = E[zp | ˜yo] = 

> K

X

> k=1

p(k | ˜yo)

Ck, zpzo (Ck, zozo + σ2I)−1( ˜ yo − μk, zo ) + μk, zp ,

(8) 

> This work has been submitted to the IEEE for possible publication. Copyright may be transferred without notice, after which this version may no longer be accessible.

where the posterior weights are determined as 

p(k | ˜yo) = πk NC( ˜ yo; μk, zo , Ck, zozo + σ2I)

PKj=1 πj NC( ˜ yo; μj, zo , Cj, zozo + σ2I) . (9) Here, a joint representation of the singular values and right singular vectors was chosen to save parameters. Furthermore, 

Zt,f possesses properties that allow for a further reduction of the parameters to be learned, as explained in the following. 

A. Complexity Reduction 

In the case of full CSI, each GMM component’s covariance consists of N 2 

> total

real-valued parameters. Such number rapidly scales for large MIMO-OFDM systems. To reduce complexity, we can exploit structural properties of wireless channels. The uniform separation of timestamps, the uniform subcarrier spacing and uniform antenna spacing at the transmitter and receiver, result in Toeplitz-structured covariance matrices in each of these domains individually, and an overall nested block-in-block Toeplitz covariance. The resulting nested 4D block-in-block Toeplitz matrix drastically reduces parameters. As an example, for a setup using Nt = 5 , Nf = 16 , Nrx = 2 

and Ntx = 8 , we reduce from 1,638 ,400 to 20 ,480 parameters (80 × reduction), see Table I, where the count of the parameters is performed using [8]. In case of partial CSI, time and frequency covariances remain Toeplitz-structured, while we empirically observe that the spatial covariance Zt,f , for a specific time step t and subcarrier f , exhibits a block-diagonal pattern with (near-)Toeplitz blocks, where the off diagonals were observed to be zero matrices. One possible explanation follows from favorable propagation effects, where increasing the number of antennas (e.g., from 2 to 8) makes the effect more pronounced. In this context, Ut,f and Vt,f inherit struc-tural properties from the receiver and transmitter covariance matrices, respectively, and can therefore be assumed to follow their statistics, leading to zero means and Toeplitz-structured covariances on both sides. Furthermore, the singular vectors are uncorrelated and thus the covariance of the vectorized (7) approximates to 

Cov[vec( Zt,f )] ≈ diag( ˜Ct,f, 1, . . . , ˜Ct,f,N s ), (10) where each block is defined as ˜Ct,f,i = E

h

s2 

> t,f,i

vt,f,i vH

> t,f,i

i

,where st,f,i denotes the i-th singular value and vt,f,i the corresponding right singular vector. For the previously con-sidered example, this block-diagonal Toeplitz model requires only 10 ,240 parameters per component, a 160 × reduction. This is an even stronger reduction than the 4D block-in-block Toeplitz structure as shown in Table I. This implies that using partial CSI and exploiting the block-diagonal Toeplitz covariance structure is also interesting for systems where full CSI is available, due to the parameter reduction. To keep the model as compact as possible and enable training with limited data while avoiding overfitting, we further reduce the dimensionality by treating each OFDM subcarrier as an independent, time-varying narrowband MIMO channel realization. For frequency-selective channels, a full cross-subcarrier covariance matrix would require N 2 

> f

parameters.               

> Covariance Type Parameter Count Covs. Example
> Full KN 2
> tN2
> fN2
> rx N2
> tx 1,638,400 4D-Block-Toeplitz 16 KN tNfNrx Ntx 20,480 Block-Diag.-Toeplitz 8KN tNfNrx Ntx 10,240

TABLE I: Covariance Matrix Parameter Count Comparison for K = 1 , Nt = 5 , Nf = 16 , Nrx = 2 and Ntx = 8 .Instead, feeding each subcarrier vector zf ∈ CNtNrx Ntx sep-arately as an independent channel realization into the GMM omits cross-subcarrier terms. The per-component covariance parameter count becomes 4KN tNrx Ntx . Using the example dimensions mentioned in Section IV-A, this calculates to 

320 covariance parameters for the considered setup when combined with block-diagonal Toeplitz covariance-structure modeling, resulting in a 5,120 × reduction. 

V. Simulation Results on TGn Model D 

For evaluating the proposed method, we consider the Task Group n (TGn) family of models [9], which model an indoor clustered multi-path propagation environment for Wi-Fi sys-tems. Standard implementations of the TGn channel model D, as [10], define a two-dimensional propagation environ-ment with fixed cluster center angles, a single environmental velocity, and static line-of-sight (LOS) angles. According to [11], these properties lead to approximately Gaussian data, an assumption that represents a strong simplification of real wireless propagation environments and makes the use of ML models unnecessary. To address this, we modify the channel model by introducing stochastic variations, thereby better capturing realistic channel behavior. Specifically, for each propagation cluster, the cluster center azimuth departure and arrival angles are sampled from U(0 ◦, 360 ◦), and the environmental velocity is drawn from U(0 , 2.4km/h ), while cluster and ray arrival rates are modeled as Poisson process with the power following a double exponential decay law, following the guidelines from [9]. Similarly, a LOS is added considering a random phase shift, zero delay, with random azimuth angles and environmental velocity sampled as for the propagation clusters. These modifications yield channel realizations that build a more realistic overall distribution. The evaluated system uses Ntx = 8 transmit and Nrx = 2 

receive antennas, with Nf = 16 subcarriers and sequences of 

Nt = 5 time steps for prediction. The carrier frequency is set to 5.16 GHz, and the subcarrier spacing is 312.5 kHz. We will discuss the setting (Nt,o , N t,p ) = (2 , 3) , with symbol durations ∆t ∈ { 1, 10 , 20 } ms. Each dataset contains 100 ,000 

training and 10 ,000 test samples. Performance is analyzed over different SNRs for various numbers of Gaussian components up to K = 256 and prediction steps. The prediction steps, defined for t > N t,o , are denoted as tp = t − Nt,o .

A. Evaluation Metrics 

We evaluate prediction performance using the NMSE as de-fined in [4, Sec. 9.1.5], which assesses reconstruction accuracy via the Frobenius-norm distance between predicted ˆzp,t and true partial CSI zp,t for all test samples at a time step t. For the MU setup, we consider the sum-rate metric, as in [12,      

> This work has been submitted to the IEEE for possible publication. Copyright may be transferred without notice, after which this version may no longer be accessible. ZOH on partial CSI FOH on partial CSI LMMSE on partial CSI NN on partial CSI
> full-cov. GMM on full CSI full-cov. GMM on partial CSI diag.-T. GMM on partial CSI

−10 0 10 20 30 

10 −1

10 0

10 1

SNR (dB) 

> NMSE

(a) NMSE vs. SNR ( ∆t = 20 ms) 

1 10 20 

10 −2

10 −1

10 0

∆t (ms) 

> NMSE

(b) NMSE vs. ∆t (30dB SNR) 

Fig. 1: Evaluation of normalized mean squared error (NMSE) over SNR and over ∆t: Nt,o = 2 , Nt,p = 3 , pred. step tp = 1 

and K = 256 for the GMM variants. Sec. 9]. We use the predicted partial CSI Z(ℓ) 

> t,f

to determine the precoder for each test sample ℓ, time step t and subcarrier 

f . We evaluate the results across varying numbers of users and considered different precoding strategies, including linear successive allocation (LISA) precoding, which maximizes the sum rate as described in [13]. The precoder is then applied to the full CSI to determine the resulting signal-to-interference-plus-noise ratio (SINR), which is used to compute the sum-rate as defined in [12, Sec. 14]. We report the sum-rates for each time step as the mean over all test samples and subcarriers. Additional, we evaluate the alignment of dominant channel subspaces, by calculating the principal angles Esubspace ,t,f,i 

between the predicted and true right singular vectors for each dominant mode i as discussed in [14]. Based on these angles, we introduce a weighted subspace error 

¯Esubspace t,f, =

> Ns

X

> i=1

wt,f,i Esubspace ,t,f,i , wt,f,i = st,f,i 

PNs 

> j=1

st,f,j 

,

(11) where st,f,i denoting the i-th singular value. This metric complements NMSE and sum-rates by capturing the subspace consistency. Again, we report the mean over all test samples and subcarriers for each time step. Lower values correspond to more accurate directions of the left singular vectors. 

B. Baseline Predictors 

As simple baselines for channel prediction, we consider ZOH, FOH, and LMMSE. ZOH considers the next time step 

t to corresponding to the last observation, ˆzp,ZOH ,t = ˜ yo,t −1.FOH performs linear extrapolation from recent observations 

ˆzp,FOH ,t = ˜ yo,t −1 +( ˜ yo,t −1 − ˜yo,t −2). Both, ZOH and FOH are computationally efficient but degrade with increasing mobility and prediction horizon [3, Sec. 2]. LMMSE exploits second-order statistics under a Gaussian assumption. The channel prediction is given as [4], [6, Sec. 9.1.6] 

ˆzp,LMMSE = E[zp | ˜yo]= CS,zpzo (CS,zozo + σ2I)−1( ˜ yo − μzo ) + μzp ,

(12) where CS = 1

> L

PLℓ=1 z(ℓ)z(ℓ),H denotes the sample covariance matrix of the data. Lastly, we compare our approach to the NN-based method, which uses the gated recurrent unit (GRU) descibed in [15], which was shown to achieve robust performance in channel prediction. The NN contains two GRU layers with 128 hidden units, and is trained for 50 epochs using the Adam optimizer with a learning rate of 10 −3, a complex-valued mean squared error (MSE) loss, and a batch size of 256. We compare the baselines with two GMM variants, the first using a full covariance matrix and the second with a block-diagonal Toeplitz structure, under the independent-subcarrier processing discussed in Section IV-A. We evaluate the performance for training on full and partial CSI, to asses the impact of the partial format. 

C. Simulation Results 

Fig. 1, left side (a), shows the NMSE for tp = 1 and 

∆t = 20 ms, evaluated using only partial CSI. GMM-based predictors outperform the classical baselines and shows a moderate improvement over the NN-based approach, with partial feedback matching full feedback, demonstrating that the proposed models capture the essential channel structure even with reduced information. Notably, the parameter-reduced block-diagonal Toeplitz (diag.-T.) GMMs achieve performance comparable to GMMs trained with a full covariance matrix (full-cov.), indicating the good quality of the approximation introduced in Section IV-A. Fig. 1, right side (b), shows the NMSE over varying ∆t at 30dB SNR. Again, GMM predictors yield the lowest errors, with partial and full feedback hav-ing nearly identical performance and the parameter-reduced GMMs exhibit no performance degradation. In Fig. 2, left side (c), we evaluate a four-user MU scenario, using the partial predicted channels for LISA precoding and assessing the resulting sum-rate. GMM variants consistently achieve the highest sum-rates. Training on full or partial CSI yield to nearly identical results, demonstrating the reliability of GMM-based predictors under partial feedback. Once more, the block-diagonal Toeplitz GMM shows to be an accurate approximation. At high SNRs, the GMM clearly outperforms the NN, which performs worse than the classic baselines. Considering the prediction step tp = 3 , Fig. 2, right side (d), shows that GMM predictors achieve the lowest weighted subspace error. The GMM trained on full CSI performs better, 

> This work has been submitted to the IEEE for possible publication. Copyright may be transferred without notice, after which this version may no longer be accessible.

ZOH on partial CSI FOH on partial CSI LMMSE on partial CSI NN on partial CSI 

full-cov. GMM on full CSI full-cov. GMM on partial CSI diag.-T. GMM on partial CSI 

−10 0 10 20 30 

0

10 

20 

30 

SNR (dB) 

> Rsum  (bits/s/Hz)

(c) Rsum vs. SNR (4 users, t = 1 )

−10 0 10 20 30 

0

20 

40 

SNR (dB) 

> ¯Esubspace  (°)

(d) ¯Esubspace vs. SNR ( t = 3 )

Fig. 2: Evaluation of Rsum for 4 users using LISA precoding and ¯Esubspace over SNR: Nt,o = 2 , Nt,p = 3 , ∆t = 20 ms, pred. step tp = {1, 3} and K = 256 for the GMM variants. likely due to the evaluation procedure: the full CSI GMM predicts the complete channel matrix, from which the corre-sponding singular vectors are subsequently extracted via SVD. This post-processing step enforces the inherent orthogonality structure of the singular vectors. In contrast, the partial CSI GMM directly predicts the quantities of interest without such structural enforcement. Therefore, the comparison involves an additional reconstruction step for the full CSI approach, which contributes to its performance advantage. The additional structure imposed by the block-diagonal Toeplitz (diag.-T.) variant of the GMM allows to perform slightly better than the ones using full covariance matrices. The gap to LMMSE grows substantially for tp = 3 . The advantage over other baselines, including the NN, is particularly pronounced around 0dB SNR, confirming that the probabilistic framework provided by the GMM captures the temporal evolution of channel subspaces more accurately than classical linear predictors also for multi-step predictions. The precise estimation of dominant directions explains why GMM variants also achieve superior MU sum-rate performance. Across all settings, GMM-based predictors outperform the baselines, especially for MU setups we see an advantage compared to the NN-based predictor. The block-diagonal Toeplitz GMMs using partial CSI matches the performance of a full CSI GMM, showing that the reduced approach does not degrade performance. 

VI. Conclusion and Outlook 

In this paper, we presented a prediction framework for forecasting future CSI in high-dimensional DL-MU-MIMO-OFDM systems under feedback constraints. By explicitly addressing partial feedback, the proposed approach remains applicable to realistic system settings, for example Wi-Fi systems. Furthermore, we introduced a parameter reduction technique that exploits structural properties of the covariance matrix, significantly lowering model complexity without de-grading performance. Simulation results demonstrated that the proposed method outperforms conventional channel predictors. For future work, we will further explore structured covari-ance modeling and parameter reduction to efficiently handle large-scale MIMO-OFDM systems, including approaches that explicitly enforce orthogonality in the predicted subspaces. Evaluating alternative ML models and extending simulations to real measurement data could further provide a more prac-tical assessment of predictive performance. 

References 

[1] IEEE, “IEEE 802.11n standard,” IEEE Std 802.11n-2009 , pp. 1–565, 2009. [2] E. Perahia and R. Stacey, Next Generation Wireless LANs: 802.11n and 802.11ac , Cambridge University Press, 2 edition, 2013. [3] A. V. Oppenheim and R. W. Schafer, Discrete-Time Signal Processing ,Pearson, 3 edition, 2010. [4] P. N. Hossein, Introduction to Probability, Statistics, and Random Processes , Kappa Research, 2014. [5] H. Kim, J. Choi, and D. J. Love, “Machine learning for future wireless communications: Channel prediction perspectives,” 2025, [Online]. Available: https://arxiv.org/abs/2502.18196. [6] N. Turan, B. B¨ ock, K. J. Chan, B. Fesl, F. Burmeister, M. Joham, G. Fettweis, and W. Utschick, “Wireless channel prediction via Gaussian mixture models,” in Proc. IEEE WSA , 2024, pp. 1–5. [7] C. M. Bishop, Pattern Recognition and Machine Learning , Springer, 2006. [8] B. Fesl, M. Joham, S. Hu, M. Koller, N. Turan, and W. Utschick, “Channel estimation based on Gaussian mixture models with structured covariances,” in Asilomar Conf. , 2022, pp. 533–537. [9] V. Erceg et al., “TGn channel models,” IEEE 802.11–03/940r4 , 2004. [10] MathWorks, WLAN Toolbox , The MathWorks, Inc., Nat-ick, Massachusetts, United States, 2025, [Online]. Available: https://www.mathworks.com/products/wlan.html. [11] B. B¨ ock, A. Kasibovic, and W. Utschick, “Wireless channel modeling for machine learning – a critical view on standardized channel models,” 2025, [Online]. Available: https://arxiv.org/abs/2510.12279. [12] T. M. Cover and J. A. Thomas, Elements of Information Theory , John Wiley & Sons, 2 edition, 2006. [13] W. Utschick, C. St¨ ockle, M. Joham, and J. Luo, “Hybrid LISA precoding for multiuser millimeter-wave communications,” IEEE Trans. Wireless Communications , vol. 17, no. 2, pp. 752–765, 2018. [14] A. Bjorck and G. Golub, “Numerical methods for computing angles between linear subspaces,” Mathematics of Computation , vol. 27, pp. 123, 1973. [15] O. Stenhammar, G. Fodor, and C. Fischione, “A comparison of neural networks for wireless channel prediction,” IEEE Wireless Communica-tions , vol. 31, no. 3, pp. 235–241, 2024.