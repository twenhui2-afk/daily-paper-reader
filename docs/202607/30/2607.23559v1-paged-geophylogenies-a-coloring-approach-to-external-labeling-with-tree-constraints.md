---
title: "Paged Geophylogenies: A Coloring Approach to External Labeling with Tree Constraints"
source: arxiv
title_zh: 结合Tree Constraints的Paged Geophylogenies：Coloring Approach to External Labeling
abstract_zh: "Geophylogenies are common type 的 diagram 用于 visualizing evolutionary history 的 species in geographic。\n\n核心做法：As drawing problem，these diagrams are commonly 模型ed as rooted binary ''phylogenetic'' tree T where e。\n\n主要结果：Counter to what our worst case bound suggests，our implementation can handle instances 结合 hundreds 的。\n\n值得关注的点在于：Prior work focuses on minimizing number 的 leader crossings，or avoiding leaders altogether。"
abstract_en: "Geophylogenies are a common type of diagram for visualizing the evolutionary history of species in a geographic context. As a drawing problem, these diagrams are commonly modeled as a rooted binary ''phylogenetic'' tree $T$ where every leaf is associated with a point feature (''site'') in a rectangular map range. The tree is drawn downward planar, its leaves are placed at equidistant positions on the upper boundary of the map, and each leaf is connected to its corresponding site by a straight-line leader. Prior work focuses on minimizing the number of leader crossings, or avoiding leaders altogether. In this paper, we explore paged geophylogenies, where the leaders can be partitioned into multiple pages where only crossings within a page are counted. For the general case, where each page can contain an arbitrary subset of the leaders, we provide an integer linear programming (ILP) formulation for minimizing the number of pages, and a polynomial-time algorithm for a special case that is equivalent to a one-sided tanglegram. We argue that, from a visualization perspective, instead each page must contain only leaders for a single subtree, and provide an $\\mathcal{O}(n^7)$ time algorithm for minimizing pages in this setting - which also involves improving the fastest known algorithm for testing the existence of a crossing-free labeling. Counter to what our worst case bound suggests, our implementation can handle instances with hundreds of sites within a second, as we show in our experimental evaluation, which further investigates the trade-offs between leader crossings and the number of pages."
authors: "Thomas Depian, Thomas C. van Dijk, Martin Nöllenburg"
date: 2026-07-26
journal: cs.CG
source_link: "https://arxiv.org/pdf/2607.23559v1"
pdf: "https://arxiv.org/pdf/2607.23559v1"
score: 7.3999999999999995
tldr: 围绕《结合Tree Constraints的Paged...》；As drawing problem，these diagram...；Counter to what our worst case bound...
tldr_cn: 围绕《结合Tree Constraints的Paged...》；As drawing problem，these diagram...；Counter to what our worst case bound...
tldr_en: "Geophylogenies are a common type of diagram for visualizing the evolutionary history of species in a geographic context. As a drawing problem, these diagrams are commonly modeled as a rooted binary ''phylogenetic'' tree $T$ where every leaf is associated with a point feature (''site'') in a rectangular map range."
selection_source: fresh_fetch
motivation: Geophylogenies are common type 的 diagram 用于 visualizing evolutionary history 的 species in geographic。
method: "核心做法：As drawing problem，these diagrams are commonly 模型ed as rooted binary ''phylogenetic'' tree T where e。"
result: 主要结果：Counter to what our worst case bound suggests，our implementation can handle instances 结合 hundreds 的。
conclusion: 值得关注的点在于：Prior work focuses on minimizing number 的 leader crossings，or avoiding leaders altogether。
---

## 摘要
Geophylogenies are common type 的 diagram 用于 visualizing evolutionary history 的 species in geographic。

核心做法：As drawing problem，these diagrams are commonly 模型ed as rooted binary ''phylogenetic'' tree T where e。

主要结果：Counter to what our worst case bound suggests，our implementation can handle instances 结合 hundreds 的。

值得关注的点在于：Prior work focuses on minimizing number 的 leader crossings，or avoiding leaders altogether。

## Abstract
Geophylogenies are a common type of diagram for visualizing the evolutionary history of species in a geographic context. As a drawing problem, these diagrams are commonly modeled as a rooted binary ''phylogenetic'' tree $T$ where every leaf is associated with a point feature (''site'') in a rectangular map range. The tree is drawn downward planar, its leaves are placed at equidistant positions on the upper boundary of the map, and each leaf is connected to its corresponding site by a straight-line leader. Prior work focuses on minimizing the number of leader crossings, or avoiding leaders altogether. In this paper, we explore paged geophylogenies, where the leaders can be partitioned into multiple pages where only crossings within a page are counted. For the general case, where each page can contain an arbitrary subset of the leaders, we provide an integer linear programming (ILP) formulation for minimizing the number of pages, and a polynomial-time algorithm for a special case that is equivalent to a one-sided tanglegram. We argue that, from a visualization perspective, instead each page must contain only leaders for a single subtree, and provide an $\mathcal{O}(n^7)$ time algorithm for minimizing pages in this setting - which also involves improving the fastest known algorithm for testing the existence of a crossing-free labeling. Counter to what our worst case bound suggests, our implementation can handle instances with hundreds of sites within a second, as we show in our experimental evaluation, which further investigates the trade-offs between leader crossings and the number of pages.
