---
title: Counting oriented spanning trees in generalized join digraphs
source: arxiv
title_zh: Counting oriented spanning trees in generalized join digraphs
abstract_zh: "Let G be digraph 结合 vertex set \\{1,2,...,n\\} 与 H_{1},H_{2},...,H_{n} be n digraphs。\n\n核心做法：First，作者引入 biclique-directed star transformation formula 用于 counting oriented spanning trees 结合 fixe。\n\n主要结果：In this paper we express number 的 oriented spanning trees in \\overrightarrow{G} in terms 的 Laplacian。\n\n值得关注的点在于：As applications，when each H_{i} is given digraph，enumerative formulas 用于 oriented spanning trees 结合。"
abstract_en: "Let $G$ be a digraph with vertex set $\\{1,2,...,n\\}$ and $H_{1},H_{2},...,H_{n}$ be $n$ digraphs. The generalized join digraph $\\overrightarrow{G}=G[H_{1},H_{2},...,H_{n}]$ is a digraph obtained from $G$ by replacing each vertex $i$ with $H_{i}$ and for any $u\\in V(H_{i})$ and $v\\in V(H_{j})$, $(u,v)\\in E(\\overrightarrow{G})$ if and only if $(i,j)\\in E(G)$. In this paper we express the number of oriented spanning trees in $\\overrightarrow{G}$ in terms of Laplacian eigenvalues of $H_{1},H_{2},...,H_{n}$ and oriented spanning trees of $G$. Furthermore, we consider the number of oriented spanning trees with a fixed root in $\\overrightarrow{G}$. First, we introduce the biclique-directed star transformation formula for counting oriented spanning trees with a fixed root in digraphs. Using it, we give the formula for the total number of oriented spanning trees with roots in a certain $H_{i}$ $(1\\leq i \\leq n)$ of $\\overrightarrow{G}$ in terms of Laplacian eigenvalues of $H_{1},H_{2},...,H_{n}$ and oriented spanning trees of $G$. As applications, when each $H_{i}$ is a given digraph, the enumerative formulas for oriented spanning trees with a fixed root of $\\overrightarrow{G}$ are derived from our work."
authors: "Shaohan Xu, Kexiang Xu"
date: 2026-07-14
journal: math.CO
source_link: "https://arxiv.org/pdf/2607.12457v1"
pdf: "https://arxiv.org/pdf/2607.12457v1"
score: 8.35
tldr: 围绕《Counting oriented spanni...》；First，作者引入 biclique-directed sta...；In this paper we express number 的 or...
tldr_cn: 围绕《Counting oriented spanni...》；First，作者引入 biclique-directed sta...；In this paper we express number 的 or...
tldr_en: "Let $G$ be a digraph with vertex set $\\{1,2,...,n\\}$ and $H_{1},H_{2},...,H_{n}$ be $n$ digraphs. The generalized join digraph $\\overrightarrow{G}=G[H_{1},H_{2},...,H_{n}]$ is a digraph obtained from $G$ by replacing each vertex $i$ with $H_{i}$ and for any $u\\in V(H_{i})$ and $v\\in V(H_{j})$, $(u,v)\\in E(\\overrightarrow{G})$ if and only if $(i,j)\\in E(G)$."
selection_source: fresh_fetch
motivation: "Let G be digraph 结合 vertex set \\{1,2,...,n\\} 与 H_{1},H_{2},...,H_{n} be n digraphs。"
method: 核心做法：First，作者引入 biclique-directed star transformation formula 用于 counting oriented spanning trees 结合 fixe。
result: "主要结果：In this paper we express number 的 oriented spanning trees in \\overrightarrow{G} in terms 的 Laplacian。"
conclusion: "值得关注的点在于：As applications，when each H_{i} is given digraph，enumerative formulas 用于 oriented spanning trees 结合。"
---

## 摘要
Let G be digraph 结合 vertex set \{1,2,...,n\} 与 H_{1},H_{2},...,H_{n} be n digraphs。

核心做法：First，作者引入 biclique-directed star transformation formula 用于 counting oriented spanning trees 结合 fixe。

主要结果：In this paper we express number 的 oriented spanning trees in \overrightarrow{G} in terms 的 Laplacian。

值得关注的点在于：As applications，when each H_{i} is given digraph，enumerative formulas 用于 oriented spanning trees 结合。

## Abstract
Let $G$ be a digraph with vertex set $\{1,2,...,n\}$ and $H_{1},H_{2},...,H_{n}$ be $n$ digraphs. The generalized join digraph $\overrightarrow{G}=G[H_{1},H_{2},...,H_{n}]$ is a digraph obtained from $G$ by replacing each vertex $i$ with $H_{i}$ and for any $u\in V(H_{i})$ and $v\in V(H_{j})$, $(u,v)\in E(\overrightarrow{G})$ if and only if $(i,j)\in E(G)$. In this paper we express the number of oriented spanning trees in $\overrightarrow{G}$ in terms of Laplacian eigenvalues of $H_{1},H_{2},...,H_{n}$ and oriented spanning trees of $G$. Furthermore, we consider the number of oriented spanning trees with a fixed root in $\overrightarrow{G}$. First, we introduce the biclique-directed star transformation formula for counting oriented spanning trees with a fixed root in digraphs. Using it, we give the formula for the total number of oriented spanning trees with roots in a certain $H_{i}$ $(1\leq i \leq n)$ of $\overrightarrow{G}$ in terms of Laplacian eigenvalues of $H_{1},H_{2},...,H_{n}$ and oriented spanning trees of $G$. As applications, when each $H_{i}$ is a given digraph, the enumerative formulas for oriented spanning trees with a fixed root of $\overrightarrow{G}$ are derived from our work.
