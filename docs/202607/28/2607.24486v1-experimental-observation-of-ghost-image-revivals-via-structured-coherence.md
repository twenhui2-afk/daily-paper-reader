---
title: Experimental Observation of Ghost Image Revivals via Structured Coherence
source: arxiv
title_zh: 通过Structured Coherence实现的Ghost Image Revivals的Experimental Observation
abstract_zh: "In this Letter，we break this fundamental limitation by reporting first experimental observation 的 re。\n\n核心做法：However，conventional ghost imaging critically relies on precise object positioning 与 conjugate match。\n\n主要结果：结合 reference arm fixed，translating object 得到 periodic revivals 的 ghost image whenever object positio。\n\n值得关注的点在于：This capability enhances flexibility 的 correlation imaging，enabling robust tomographic imaging 的 mov。"
abstract_en: "Ghost imaging retrieves an object's image from intensity correlations between two light beams, neither of which independently carries information about the object. However, conventional ghost imaging critically relies on precise object positioning and conjugate matching between the two arms, causing the image to disappear when these conditions are violated and the object is accessible from one plane only. In this Letter, we break this fundamental limitation by reporting the first experimental observation of revivals of ghost images of arbitrary objects via engineering the longitudinal intensity autocorrelation of a structured thermal light field into a coherence comb. With the reference arm fixed, translating the object produces periodic revivals of the ghost image whenever the object position matches a comb-tooth position, reminiscent of the Talbot effect. This capability enhances the flexibility of correlation imaging, enabling robust tomographic imaging of moving and non-periodic complex objects."
authors: "Weining Wang, Yaning Zhou, Sergey A Ponomarenko, Yangjian Cai, Xin Liu"
date: 2026-07-27
journal: physics.optics
source_link: "https://arxiv.org/pdf/2607.24486v1"
pdf: "https://arxiv.org/pdf/2607.24486v1"
score: 8.6
tldr: 围绕《通过Structured Coherence实现...》；However，conventional ghost imagi...；结合 reference arm fixed，translating o...
tldr_cn: 围绕《通过Structured Coherence实现...》；However，conventional ghost imagi...；结合 reference arm fixed，translating o...
tldr_en: "Ghost imaging retrieves an object's image from intensity correlations between two light beams, neither of which independently carries information about the object. However, conventional ghost imaging critically relies on precise object positioning and conjugate matching between the two arms, causing the image to disappear when these conditions are violated and the object is accessible from..."
selection_source: fresh_fetch
motivation: In this Letter，we break this fundamental limitation by reporting first experimental observation 的 re。
method: 核心做法：However，conventional ghost imaging critically relies on precise object positioning 与 conjugate match。
result: 主要结果：结合 reference arm fixed，translating object 得到 periodic revivals 的 ghost image whenever object positio。
conclusion: 值得关注的点在于：This capability enhances flexibility 的 correlation imaging，enabling robust tomographic imaging 的 mov。
---

## 摘要
In this Letter，we break this fundamental limitation by reporting first experimental observation 的 re。

核心做法：However，conventional ghost imaging critically relies on precise object positioning 与 conjugate match。

主要结果：结合 reference arm fixed，translating object 得到 periodic revivals 的 ghost image whenever object positio。

值得关注的点在于：This capability enhances flexibility 的 correlation imaging，enabling robust tomographic imaging 的 mov。

## Abstract
Ghost imaging retrieves an object's image from intensity correlations between two light beams, neither of which independently carries information about the object. However, conventional ghost imaging critically relies on precise object positioning and conjugate matching between the two arms, causing the image to disappear when these conditions are violated and the object is accessible from one plane only. In this Letter, we break this fundamental limitation by reporting the first experimental observation of revivals of ghost images of arbitrary objects via engineering the longitudinal intensity autocorrelation of a structured thermal light field into a coherence comb. With the reference arm fixed, translating the object produces periodic revivals of the ghost image whenever the object position matches a comb-tooth position, reminiscent of the Talbot effect. This capability enhances the flexibility of correlation imaging, enabling robust tomographic imaging of moving and non-periodic complex objects.
