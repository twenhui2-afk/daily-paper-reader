---
title: "LUMA: Benchmarking Segmentation via a Lightweight Universal Mask Adapter"
source: arxiv
title_zh: 通过Lightweight Universal Mask Adapter实现的LUMA：基准ing Segmentation
abstract_zh: "Additionally，pretraining objective，not 架构，lever field has tuned hardest，governs segmentation quality。\n\n核心做法：作者引入 Lightweight Universal Mask Adapter (LUMA)，lightweight，backbone-agnostic mask-transformer head t。\n\n主要结果：LUMA matches accuracy 的 EoMT，当前最优 efficient ViT-segmenter，at lower cost，while attaching unchanged to。\n\n值得关注的点在于：Holding this head fixed，we benchmark 20 backbones，11 pretraining schemes 与 range 的 resolutions on AD。"
abstract_en: "Comparing transformer backbones for image segmentation is confounded: each is paired with a different decoder, recipe, and pretraining, so reported differences rarely reflect the backbone itself. We introduce the Lightweight Universal Mask Adapter (LUMA), a lightweight, backbone-agnostic mask-transformer head that treats any backbone as a black-box feature extractor, letting a set of queries read from its features through cheap cross-attention. LUMA matches the accuracy of EoMT, the state-of-the-art efficient ViT-segmenter, at lower cost, while attaching unchanged to isotropic, hierarchical, convolutional, and mixture-of-experts backbones alike. Holding this head fixed, we benchmark 20 backbones, 11 pretraining schemes and a range of resolutions on ADE20K and Cityscapes under one modern recipe. We find that ``efficient'' token mixers fail to deliver efficiency even at the high resolutions that motivate them, with plain ViT holding the throughput Pareto-front at every resolution. Additionally, the pretraining objective, not the architecture, the lever the field has tuned hardest, governs segmentation quality."
authors: "Tobias Christian Nauen, Anosh Billimoria, Federico Raue, Stanislav Frolov, Brian B. Moser, Andreas Dengel"
date: 2026-07-01
journal: cs.CV
source_link: "https://arxiv.org/pdf/2607.00687v1"
pdf: "https://arxiv.org/pdf/2607.00687v1"
score: 6.049999999999999
tldr: 围绕《通过Lightweight Universal...》；作者引入 Lightweight Universal Mask...；LUMA matches accuracy 的 EoMT，当前最优 ef...
tldr_cn: 围绕《通过Lightweight Universal...》；作者引入 Lightweight Universal Mask...；LUMA matches accuracy 的 EoMT，当前最优 ef...
tldr_en: "Comparing transformer backbones for image segmentation is confounded: each is paired with a different decoder, recipe, and pretraining, so reported differences rarely reflect the backbone itself. We introduce the Lightweight Universal Mask Adapter (LUMA), a lightweight, backbone-agnostic mask-transformer head that treats any backbone as a black-box feature extractor, letting a set of queries read from..."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "pretraining | 预训练 | 学术高频", "transformer | Transformer 架构 | 学术高频", "benchmark | 基准测试 | CET6/IELTS"]
motivation: Additionally，pretraining objective，not 架构，lever field has tuned hardest，governs segmentation quality。
method: 核心做法：作者引入 Lightweight Universal Mask Adapter (LUMA)，lightweight，backbone-agnostic mask-transformer head t。
result: 主要结果：LUMA matches accuracy 的 EoMT，当前最优 efficient ViT-segmenter，at lower cost，while attaching unchanged to。
conclusion: 值得关注的点在于：Holding this head fixed，we benchmark 20 backbones，11 pretraining schemes 与 range 的 resolutions on AD。
---

## 摘要
Additionally，pretraining objective，not 架构，lever field has tuned hardest，governs segmentation quality。

核心做法：作者引入 Lightweight Universal Mask Adapter (LUMA)，lightweight，backbone-agnostic mask-transformer head t。

主要结果：LUMA matches accuracy 的 EoMT，当前最优 efficient ViT-segmenter，at lower cost，while attaching unchanged to。

值得关注的点在于：Holding this head fixed，we benchmark 20 backbones，11 pretraining schemes 与 range 的 resolutions on AD。

## Abstract
Comparing transformer backbones for image segmentation is confounded: each is paired with a different decoder, recipe, and pretraining, so reported differences rarely reflect the backbone itself. We introduce the Lightweight Universal Mask Adapter (LUMA), a lightweight, backbone-agnostic mask-transformer head that treats any backbone as a black-box feature extractor, letting a set of queries read from its features through cheap cross-attention. LUMA matches the accuracy of EoMT, the state-of-the-art efficient ViT-segmenter, at lower cost, while attaching unchanged to isotropic, hierarchical, convolutional, and mixture-of-experts backbones alike. Holding this head fixed, we benchmark 20 backbones, 11 pretraining schemes and a range of resolutions on ADE20K and Cityscapes under one modern recipe. We find that ``efficient'' token mixers fail to deliver efficiency even at the high resolutions that motivate them, with plain ViT holding the throughput Pareto-front at every resolution. Additionally, the pretraining objective, not the architecture, the lever the field has tuned hardest, governs segmentation quality.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **pretraining**：预训练（学术高频）
- **transformer**：Transformer 架构（学术高频）
- **benchmark**：基准测试（CET6/IELTS）
