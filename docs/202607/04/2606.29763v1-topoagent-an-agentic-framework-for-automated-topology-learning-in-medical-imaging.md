---
title: "TopoAgent: An Agentic Framework for Automated Topology Learning in Medical Imaging"
source: arxiv
title_zh: 用于Automated Topology Learning in Medical Imaging的TopoAgent：Agentic 框架
abstract_zh: "Topological data analysis (TDA)，particularly persistent homology (PH)，captures geometric structural。\n\n核心做法：To best 的 our knowledge，there is no known large language 模型 (LLM)-based agentic 框架 that can automati。\n\n主要结果：TopoAgent analyzes input images 与 their topological characteristics，reasons about which topological。\n\n值得关注的点在于：To fill this gap，作者提出 \\textbf{TopoAgent}，LLM-based agentic 框架 that automates topology learning 用于 me。"
abstract_en: "Topological data analysis (TDA), particularly persistent homology (PH), captures geometric structural properties in medical images (e.g., connected components, loops, shape characteristics), which conventional pixel-level deep learning approaches often neglect. While many topological descriptors are known for converting persistence diagrams (PDs) or raw images into topological feature vectors, existing methods mostly default to a single fixed descriptor (e.g., persistence images), leaving the diversity of topological representations largely unexplored. To the best of our knowledge, there is no known large language model (LLM)-based agentic framework that can automatically determine the most suitable topological descriptors for a given image dataset and produce the corresponding topological feature vectors for downstream tasks. To fill this gap, we propose \\textbf{TopoAgent}, an LLM-based agentic framework that automates topology learning for medical image analysis.TopoAgent operates through a Perception--Reasoning--Action--Reflection loop supported by 21 domain-specific tools and dual memory that accumulates experience across runs. Its skill set is distilled from systematic evaluation of 15 topological descriptors across 26 datasets with six classifiers. TopoAgent analyzes input images and their topological characteristics, reasons about which topological descriptors best suit the input, and determines the optimal descriptor and its configuration, all without task-specific training."
authors: "Guangyu Meng, Pengfei Gu, Xueyang Li, Yiyu Shi, Erin Wolf Chambers, Danny Z. Chen"
date: 2026-06-29
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.29763v1"
pdf: "https://arxiv.org/pdf/2606.29763v1"
score: 6.0
tldr: 围绕《用于Automated Topology Lea...》；To best 的 our knowledge，there is...；TopoAgent analyzes input images 与 th...
tldr_cn: 围绕《用于Automated Topology Lea...》；To best 的 our knowledge，there is...；TopoAgent analyzes input images 与 th...
tldr_en: "Topological data analysis (TDA), particularly persistent homology (PH), captures geometric structural properties in medical images (e.g., connected components, loops, shape characteristics), which conventional pixel-level deep learning approaches often neglect. While many topological descriptors are known for converting persistence diagrams (PDs) or raw images into topological feature vectors, existing methods mostly default to a single fixed..."
selection_source: fresh_fetch
vocabulary: ["downstream | 下游任务 | 学术高频"]
motivation: Topological data analysis (TDA)，particularly persistent homology (PH)，captures geometric structural。
method: 核心做法：To best 的 our knowledge，there is no known large language 模型 (LLM)-based agentic 框架 that can automati。
result: 主要结果：TopoAgent analyzes input images 与 their topological characteristics，reasons about which topological。
conclusion: "值得关注的点在于：To fill this gap，作者提出 \\textbf{TopoAgent}，LLM-based agentic 框架 that automates topology learning 用于 me。"
---

## 摘要
Topological data analysis (TDA)，particularly persistent homology (PH)，captures geometric structural。

核心做法：To best 的 our knowledge，there is no known large language 模型 (LLM)-based agentic 框架 that can automati。

主要结果：TopoAgent analyzes input images 与 their topological characteristics，reasons about which topological。

值得关注的点在于：To fill this gap，作者提出 \textbf{TopoAgent}，LLM-based agentic 框架 that automates topology learning 用于 me。

## Abstract
Topological data analysis (TDA), particularly persistent homology (PH), captures geometric structural properties in medical images (e.g., connected components, loops, shape characteristics), which conventional pixel-level deep learning approaches often neglect. While many topological descriptors are known for converting persistence diagrams (PDs) or raw images into topological feature vectors, existing methods mostly default to a single fixed descriptor (e.g., persistence images), leaving the diversity of topological representations largely unexplored. To the best of our knowledge, there is no known large language model (LLM)-based agentic framework that can automatically determine the most suitable topological descriptors for a given image dataset and produce the corresponding topological feature vectors for downstream tasks. To fill this gap, we propose \textbf{TopoAgent}, an LLM-based agentic framework that automates topology learning for medical image analysis.TopoAgent operates through a Perception--Reasoning--Action--Reflection loop supported by 21 domain-specific tools and dual memory that accumulates experience across runs. Its skill set is distilled from systematic evaluation of 15 topological descriptors across 26 datasets with six classifiers. TopoAgent analyzes input images and their topological characteristics, reasons about which topological descriptors best suit the input, and determines the optimal descriptor and its configuration, all without task-specific training.

## 英语词汇
- **downstream**：下游任务（学术高频）
