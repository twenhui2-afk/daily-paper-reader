---
title: "E3DGS: Unified Geometric-Photometric Equivariance for 3D Gaussian Splatting via Color-as-Geometry Embedding"
source: arxiv
title_zh: 用于3D Gaussian Splatting 通过 Color-as-Geometry Embedding的E3DGS：Unified Geometric-Photometric Equivariance
abstract_zh: "3D Gaussian Splatting (3DGS) captures scenes by coupling explicit geometry (position，covariance) 结合。\n\n核心做法：作者提出 unified solution rooted in representation theory：用于 SH degrees \\ell\\le2，photometry is algebraic。\n\n主要结果：Evaluations on object vision 与 action-conditioned Gaussian world 模型ing 表明 that our unified approach。\n\n值得关注的点在于：While translations are handled by relative coordinates，rotations act heterogeneously across attribut。"
abstract_en: "3D Gaussian Splatting (3DGS) captures scenes by coupling explicit geometry (position, covariance) with view-dependent photometry (Spherical Harmonics). However, building $\\mathrm{SE}(3)$-equivariant architectures on these primitives presents a fundamental representation bottleneck. Color has been treated as a signal rather than a geometric entity, making it nontrivial to unify symmetry across geometry and appearance as the camera frame changes. While translations are handled by relative coordinates, rotations act heterogeneously across attributes: $μ\\mapsto Rμ$, $Σ\\mapsto RΣR^\\top$, and $f_\\ell\\mapsto D^\\ell(R)f_\\ell$. This mismatch complicates strict equivariance, leading existing methods to either discard or flatten SH coefficients, thereby breaking symmetry. We propose a unified solution rooted in representation theory: for SH degrees $\\ell\\le2$, photometry is algebraically isomorphic to a rank-2 geometric tensor. We prove that the Wigner-$D$ action on these SH coefficients can be exactly reformulated as the conjugation action on $3\\times3$ matrices. Leveraging this, we introduce the Unified Matrix Embedding, a lifting that maps all Gaussian attributes into a unified carrier space, $\\mathfrak{gl}(3)$. Building on the \"Color-as-Geometry\" formulation, we present E3DGS, a rigid-body ($\\mathrm{SE}(3)$) equivariant architecture that processes 3D Gaussians without Clebsch-Gordan tensor products. Evaluations on object vision and action-conditioned Gaussian world modeling demonstrate that our unified approach yields strong robustness under camera-frame changes and improved data efficiency."
authors: "Chankyo Kim, Maani Ghaffari"
date: 2026-07-17
journal: cs.CV
source_link: "https://arxiv.org/pdf/2607.15536v1"
pdf: "https://arxiv.org/pdf/2607.15536v1"
score: 7.8
tldr: 围绕《用于3D Gaussian Splatting...》；作者提出 unified solution rooted in...；Evaluations on object vision 与 actio...
tldr_cn: 围绕《用于3D Gaussian Splatting...》；作者提出 unified solution rooted in...；Evaluations on object vision 与 actio...
tldr_en: "3D Gaussian Splatting (3DGS) captures scenes by coupling explicit geometry (position, covariance) with view-dependent photometry (Spherical Harmonics). However, building $\\mathrm{SE}(3)$-equivariant architectures on these primitives presents a fundamental representation bottleneck."
selection_source: fresh_fetch
vocabulary: ["robustness | 鲁棒性 | IELTS", "embedding | 嵌入表示 | 学术高频"]
motivation: 3D Gaussian Splatting (3DGS) captures scenes by coupling explicit geometry (position，covariance) 结合。
method: 核心做法：作者提出 unified solution rooted in representation theory：用于 SH degrees \ell\le2，photometry is algebraic。
result: 主要结果：Evaluations on object vision 与 action-conditioned Gaussian world 模型ing 表明 that our unified approach。
conclusion: 值得关注的点在于：While translations are handled by relative coordinates，rotations act heterogeneously across attribut。
---

## 摘要
3D Gaussian Splatting (3DGS) captures scenes by coupling explicit geometry (position，covariance) 结合。

核心做法：作者提出 unified solution rooted in representation theory：用于 SH degrees \ell\le2，photometry is algebraic。

主要结果：Evaluations on object vision 与 action-conditioned Gaussian world 模型ing 表明 that our unified approach。

值得关注的点在于：While translations are handled by relative coordinates，rotations act heterogeneously across attribut。

## Abstract
3D Gaussian Splatting (3DGS) captures scenes by coupling explicit geometry (position, covariance) with view-dependent photometry (Spherical Harmonics). However, building $\mathrm{SE}(3)$-equivariant architectures on these primitives presents a fundamental representation bottleneck. Color has been treated as a signal rather than a geometric entity, making it nontrivial to unify symmetry across geometry and appearance as the camera frame changes. While translations are handled by relative coordinates, rotations act heterogeneously across attributes: $μ\mapsto Rμ$, $Σ\mapsto RΣR^\top$, and $f_\ell\mapsto D^\ell(R)f_\ell$. This mismatch complicates strict equivariance, leading existing methods to either discard or flatten SH coefficients, thereby breaking symmetry. We propose a unified solution rooted in representation theory: for SH degrees $\ell\le2$, photometry is algebraically isomorphic to a rank-2 geometric tensor. We prove that the Wigner-$D$ action on these SH coefficients can be exactly reformulated as the conjugation action on $3\times3$ matrices. Leveraging this, we introduce the Unified Matrix Embedding, a lifting that maps all Gaussian attributes into a unified carrier space, $\mathfrak{gl}(3)$. Building on the "Color-as-Geometry" formulation, we present E3DGS, a rigid-body ($\mathrm{SE}(3)$) equivariant architecture that processes 3D Gaussians without Clebsch-Gordan tensor products. Evaluations on object vision and action-conditioned Gaussian world modeling demonstrate that our unified approach yields strong robustness under camera-frame changes and improved data efficiency.

## 英语词汇
- **robustness**：鲁棒性（IELTS）
- **embedding**：嵌入表示（学术高频）
