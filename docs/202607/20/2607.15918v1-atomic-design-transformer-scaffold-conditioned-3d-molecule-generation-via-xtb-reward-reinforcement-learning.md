---
title: "Atomic Design Transformer: Scaffold-Conditioned 3D Molecule Generation via xTB-Reward Reinforcement Learning"
source: arxiv
title_zh: 通过xTB-Reward Reinforcement Learning实现的Atomic 设计 Transformer：Scaffold-Conditioned 3D Molecule Generation
abstract_zh: "作者提出 SE (3)-invariant transformer 用于 3D-molecule generation，Atomic Design Transformer (ADT)。\n\n核心做法：To score generated molecules 作者引入 xTB topology-preservation rate (XTP)：fraction 的 molecules 用于 which。\n\n主要结果：SE (3) invariance is 达到d by tokenization：each new atom's position is encoded in local coordinate fra。\n\n值得关注的点在于：ADT thus enables direct 3D generation。"
abstract_en: "We present an SE(3)-invariant transformer for 3D-molecule generation,   the Atomic Design Transformer (ADT). ADT places atoms one   at a time, autoregressively. SE(3) invariance is achieved by   tokenization: each new atom's position is encoded in the local   coordinate frame of a previously placed atom. The backbone is a plain   causal transformer. The token stream fully specifies a 3D structure   together with its chemical-bond graph G, without any bond-order   assignment. To score generated molecules we introduce the xTB   topology-preservation rate (XTP): the fraction of molecules for   which an xTB GFN2 relaxation preserves G specified by the token stream.   For XTP-accepted molecules we also report the   relaxation energy and the root mean square of the atomic displacement (RMSD). We evaluate   two ADT models. The first is ADT pretrained on the GEOM-Drugs   $\\le\\!30$-heavy-atom dataset; we benchmark scaffold-conditioned 3D   generation across seven drug-like scaffolds from the model. It   reaches an XTP of ${\\sim}54\\%$ and a valid-molecule yield   $N^{\\mathrm{gen}}/N$ of ${\\sim}50\\%$, where $N^{\\mathrm{gen}}/N$ is the   fraction of samples that are distinct, topology-preserving, and   chemically valid. The second model continues from the first by   reinforcement learning against the verifiable xTB reward (RLVR), using   no external molecules. RLVR raises XTP to ${\\sim}98\\%$ and   $N^{\\mathrm{gen}}/N$ to ${\\sim}95\\%$, while approximately preserving the   GEOM-Drugs size and composition distributions. Finally, we present   an Inverse-Kinematics Transformer that recovers XTP for large molecules, where discretization error accumulates. ADT thus enables direct 3D generation."
authors: Takao Kotani
date: 2026-07-17
journal: physics.comp-ph
source_link: "https://arxiv.org/pdf/2607.15918v1"
pdf: "https://arxiv.org/pdf/2607.15918v1"
score: 7.6
tldr: 围绕《通过xTB-Reward Reinforceme...》；To score generated molecules 作者引...；SE (3) invariance is 达到d by tokeniza...
tldr_cn: 围绕《通过xTB-Reward Reinforceme...》；To score generated molecules 作者引...；SE (3) invariance is 达到d by tokeniza...
tldr_en: "We present an SE(3)-invariant transformer for 3D-molecule generation, the Atomic Design Transformer (ADT). ADT places atoms one at a time, autoregressively."
selection_source: fresh_fetch
vocabulary: ["transformer | Transformer 架构 | 学术高频", "benchmark | 基准测试 | CET6/IELTS"]
motivation: 作者提出 SE (3)-invariant transformer 用于 3D-molecule generation，Atomic Design Transformer (ADT)。
method: 核心做法：To score generated molecules 作者引入 xTB topology-preservation rate (XTP)：fraction 的 molecules 用于 which。
result: "主要结果：SE (3) invariance is 达到d by tokenization：each new atom's position is encoded in local coordinate fra。"
conclusion: 值得关注的点在于：ADT thus enables direct 3D generation。
---

## 摘要
作者提出 SE (3)-invariant transformer 用于 3D-molecule generation，Atomic Design Transformer (ADT)。

核心做法：To score generated molecules 作者引入 xTB topology-preservation rate (XTP)：fraction 的 molecules 用于 which。

主要结果：SE (3) invariance is 达到d by tokenization：each new atom's position is encoded in local coordinate fra。

值得关注的点在于：ADT thus enables direct 3D generation。

## Abstract
We present an SE(3)-invariant transformer for 3D-molecule generation,   the Atomic Design Transformer (ADT). ADT places atoms one   at a time, autoregressively. SE(3) invariance is achieved by   tokenization: each new atom's position is encoded in the local   coordinate frame of a previously placed atom. The backbone is a plain   causal transformer. The token stream fully specifies a 3D structure   together with its chemical-bond graph G, without any bond-order   assignment. To score generated molecules we introduce the xTB   topology-preservation rate (XTP): the fraction of molecules for   which an xTB GFN2 relaxation preserves G specified by the token stream.   For XTP-accepted molecules we also report the   relaxation energy and the root mean square of the atomic displacement (RMSD). We evaluate   two ADT models. The first is ADT pretrained on the GEOM-Drugs   $\le\!30$-heavy-atom dataset; we benchmark scaffold-conditioned 3D   generation across seven drug-like scaffolds from the model. It   reaches an XTP of ${\sim}54\%$ and a valid-molecule yield   $N^{\mathrm{gen}}/N$ of ${\sim}50\%$, where $N^{\mathrm{gen}}/N$ is the   fraction of samples that are distinct, topology-preserving, and   chemically valid. The second model continues from the first by   reinforcement learning against the verifiable xTB reward (RLVR), using   no external molecules. RLVR raises XTP to ${\sim}98\%$ and   $N^{\mathrm{gen}}/N$ to ${\sim}95\%$, while approximately preserving the   GEOM-Drugs size and composition distributions. Finally, we present   an Inverse-Kinematics Transformer that recovers XTP for large molecules, where discretization error accumulates. ADT thus enables direct 3D generation.

## 英语词汇
- **transformer**：Transformer 架构（学术高频）
- **benchmark**：基准测试（CET6/IELTS）
