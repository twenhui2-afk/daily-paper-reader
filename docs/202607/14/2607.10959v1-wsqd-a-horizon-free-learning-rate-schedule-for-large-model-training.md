---
title: "WSqD: A Horizon-Free Learning Rate Schedule for Large Model Training"
source: arxiv
title_zh: 用于Large 模型 Training的WSqD：Horizon-Free Learning Rate Schedule
abstract_zh: "Standard learning rate schedules such as cosine annealing are tied to fixed training horizon，limitin。\n\n核心做法：Motivated by stochastic convex optimization，作者提出 WSqD (Warmup 结合 Square-root base 与 linear Decay)，le。\n\n主要结果：Empirically，on language-模型 pretraining 使用 SlimPajama corpus，WSqD matches or 优于 carefully tuned WSD 与。\n\n值得关注的点在于：Warmup-stable-decay (WSD) partially addresses this issue by maintaining long constant-rate phase bef。"
abstract_en: "Standard learning rate schedules such as cosine annealing are tied to a fixed training horizon, limiting their ability to accommodate post hoc horizon extension. Warmup-stable-decay (WSD) partially addresses this issue by maintaining a long constant-rate phase before a short linear cooldown, allowing training to resume from a pre-decay checkpoint. However, its peak learning rate is still tuned based on the original training horizon and can become suboptimal when training is extended. Motivated by stochastic convex optimization, we propose WSqD (Warmup with Square-root base and linear Decay), a learning rate schedule that replaces WSD's constant stable phase with a shifted inverse-square-root base while retaining the final linear cooldown. In the stochastic convex setting, WSqD provably attains the minimax-optimal $O(1/\\sqrt{T})$ last-iterate convergence rate. Importantly, its base learning rate schedule is horizon-independent, and the training horizon is needed only to determine when to begin the final cooldown. Empirically, on language-model pretraining using the SlimPajama corpus, WSqD matches or outperforms carefully tuned WSD and other baselines across multiple training horizons while reusing a single peak learning rate."
authors: "Jianhao Ma, Yuxin Chen"
date: 2026-07-12
journal: cs.LG
source_link: "https://arxiv.org/pdf/2607.10959v1"
pdf: "https://arxiv.org/pdf/2607.10959v1"
score: 8.049999999999999
tldr: 围绕《用于Large 模型 Training的WSqD...》；Motivated by stochastic convex o...；Empirically，on language-模型 pretraini...
tldr_cn: 围绕《用于Large 模型 Training的WSqD...》；Motivated by stochastic convex o...；Empirically，on language-模型 pretraini...
tldr_en: "Standard learning rate schedules such as cosine annealing are tied to a fixed training horizon, limiting their ability to accommodate post hoc horizon extension. Warmup-stable-decay (WSD) partially addresses this issue by maintaining a long constant-rate phase before a short linear cooldown, allowing training to resume from a pre-decay checkpoint."
selection_source: fresh_fetch
vocabulary: ["optimization | 优化 | CET6", "pretraining | 预训练 | 学术高频"]
motivation: Standard learning rate schedules such as cosine annealing are tied to fixed training horizon，limitin。
method: 核心做法：Motivated by stochastic convex optimization，作者提出 WSqD (Warmup 结合 Square-root base 与 linear Decay)，le。
result: 主要结果：Empirically，on language-模型 pretraining 使用 SlimPajama corpus，WSqD matches or 优于 carefully tuned WSD 与。
conclusion: 值得关注的点在于：Warmup-stable-decay (WSD) partially addresses this issue by maintaining long constant-rate phase bef。
---

## 摘要
Standard learning rate schedules such as cosine annealing are tied to fixed training horizon，limitin。

核心做法：Motivated by stochastic convex optimization，作者提出 WSqD (Warmup 结合 Square-root base 与 linear Decay)，le。

主要结果：Empirically，on language-模型 pretraining 使用 SlimPajama corpus，WSqD matches or 优于 carefully tuned WSD 与。

值得关注的点在于：Warmup-stable-decay (WSD) partially addresses this issue by maintaining long constant-rate phase bef。

## Abstract
Standard learning rate schedules such as cosine annealing are tied to a fixed training horizon, limiting their ability to accommodate post hoc horizon extension. Warmup-stable-decay (WSD) partially addresses this issue by maintaining a long constant-rate phase before a short linear cooldown, allowing training to resume from a pre-decay checkpoint. However, its peak learning rate is still tuned based on the original training horizon and can become suboptimal when training is extended. Motivated by stochastic convex optimization, we propose WSqD (Warmup with Square-root base and linear Decay), a learning rate schedule that replaces WSD's constant stable phase with a shifted inverse-square-root base while retaining the final linear cooldown. In the stochastic convex setting, WSqD provably attains the minimax-optimal $O(1/\sqrt{T})$ last-iterate convergence rate. Importantly, its base learning rate schedule is horizon-independent, and the training horizon is needed only to determine when to begin the final cooldown. Empirically, on language-model pretraining using the SlimPajama corpus, WSqD matches or outperforms carefully tuned WSD and other baselines across multiple training horizons while reusing a single peak learning rate.

## 英语词汇
- **optimization**：优化（CET6）
- **pretraining**：预训练（学术高频）
