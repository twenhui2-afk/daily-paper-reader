Title: Efficient Pattern Matching for Unordered Term Tree Patterns under Generalized Height-Constrained Bindings

URL Source: https://arxiv.org/pdf/2607.14617v1

Published Time: Fri, 17 Jul 2026 00:38:54 GMT

Number of Pages: 6

Markdown Content:
# Efficient Pattern Matching for Unordered Term Tree Patterns under Generalized Height-Constrained Bindings 

Shintaro Matsushita ∗‡ , Takayoshi Shoudai ∗§ , and Yusuke Suzuki †∗Department of Computer Science and Engineering, Fukuoka Institute of Technology, Fukuoka 811-0295, Japan Email: ‡mfm25113@bene.fit.ac.jp, §shodai@fit.ac.jp 

†Faculty of Information Sciences, Hiroshima City University, Hiroshima 731-3194, Japan Email: y-suzuki@hiroshima-cu.ac.jp 

Abstract —Unordered trees are useful for modeling hierarchical structures in which the order among siblings is irrelevant. To represent flexible structural patterns in such data, unordered term tree patterns with height-constrained variables provide a natural framework. In our previous work, we studied the pattern matching problem for rooted unordered term tree patterns with height-constrained variables under the restriction that the child port of each variable must correspond to a leaf of a binding tree. In this paper, we remove this restriction and generalize the binding model so that the child port may correspond to any non-root vertex of a binding tree. Under generalized bindings, we formulate the corresponding membership problem and present a polynomial-time pattern matching algorithm. We also implement the proposed algorithm and conduct computational experiments to evaluate its running time. The experimental results show that the proposed method achieves practical running times. 

Index Terms —height constrained tree pattern, polynomial-time algorithm, pattern matching algorithm, membership problem 

I. I NTRODUCTION 

Unordered trees arise in a variety of application domains in which the relative order among siblings is irrelevant, such as abstract syntax representations, chemical structures, and hierarchical data with unordered branching. Tree patterns on such structures provide a natural way to describe flexible struc-tural constraints. In particular, unordered term tree patterns with height-constrained variables form a useful framework, since each variable can represent a tree-structured component subject to constraints on its trunk length and height. In our previous work [1], we studied the pattern matching problem for rooted unordered term tree patterns with height-constrained variables and presented a polynomial-time algorithm for de-ciding whether a given unordered tree belongs to the language generated by a given pattern. In that model, however, the child port of each height-constrained variable was required to correspond to a leaf of a binding tree. 

This is an author preprint of a paper presented at the 22nd International Conference on E-Service and Knowledge Management (ESKM 2026), IIAI-AAI 2026. The paper was accepted for inclusion in the conference proceedings published by IEEE Computer Society Conference Publishing Services (CPS). This work was partially supported by JSPS KAKENHI Grant Numbers JP24K15074 and JP24K15090. 

For rooted unordered trees, this leaf restriction is not es-sential, because no sibling order needs to be preserved in a binding operation. Motivated by this observation, in this paper we generalize the notion of bindings for height-constrained variables so that the child port may correspond to any non-root vertex of a binding tree. For example, in abstract syntax repre-sentations and chemical structure trees, a constrained variable may naturally attach at an internal vertex of a bound subtree rather than only at a leaf. In such cases, the previous leaf restriction excludes patterns that are structurally meaningful in practice. Under generalized bindings, we formulate the corresponding membership problem, develop a polynomial-time pattern matching algorithm, and report computational results obtained from an implementation of the proposed algorithm. For unordered trees, tree inclusion and tree pattern matching have been studied as fundamental problems on structured data. Classical and more recent results on unordered tree inclusion include those of Kilpeläinen and Mannila [2] and Akutsu et al. [3]. In our research framework, studies on term tree patterns have progressed from unordered tree patterns with internal variables [4] to efficient matching for ordered term tree patterns [5]. The framework of height-constrained variables was then studied in the ordered setting from a language-theoretic and inductive-inference viewpoint [6], and later extended to unordered term tree patterns of bounded dimension [7]. Our previous work [1] considered the rooted unordered case under a leaf-restricted binding model. Our main result is that the above membership problem under generalized bindings is solvable in polynomial time. More precisely, we present a dynamic-programming-based matching algorithm, analyze its time complexity, and evaluate its actual running time through computational experiments. The exper-imental results indicate that the proposed method is not only polynomial-time solvable in theory but also computationally practical in actual execution time. II. P RELIMINARIES 

We consider rooted unordered trees of dimension 2. For a set 

S, let |S| denote the number of elements in S. Let Σ and Λ be arXiv:2607.14617v1 [cs.DS] 16 Jul 2026 finite alphabets of vertex labels and edge labels, respectively, and let X be an infinite alphabet of variable labels. We assume that (Σ ∪ Λ) ∩ X = ∅. Let UT Σ,Λ denote the set of all rooted unordered trees whose vertices and edges are labeled with symbols in Σ and Λ, respectively. 

Definition 1 (Linear unordered term tree pattern) . Let T =(VT , E T ) be a rooted unordered tree. Let Et and Ht be a partition of ET . An unordered term tree pattern is a triple 

t = ( Vt, E t, H t) obtained from T , where Vt = VT . Elements of Vt, Et, and Ht are called vertices, edges, and variables, respectively, and are labeled with symbols in Σ, Λ, and X,respectively. The root, leaves, parent-child relation, and height of t are those of the underlying rooted unordered tree T . If (v, v ′) ∈

Et, then (v, v ′) denotes the edge from v to v′. If [v, v ′] ∈ Ht,then [v, v ′] denotes the variable from v to v′, where v is the parent port and v′ is the child port. A non-leaf vertex is called an internal vertex. The pattern t is said to be linear if no two variables in Ht share the same variable label. In this paper, we deal only with linear unordered term tree patterns. 

Definition 2 (Height-constrained variables) . Let XH be an infinite subset of X. For each pair of integers i, j with 1 ≤

i ≤ j, let XH(i,j ) be an infinite subset of XH. We assume that XH = S 

> 1≤i≤j

XH(i,j ) and XH(i,j ) ∩XH(i′,j ′) = ∅ holds whenever (i, j )̸ = ( i′, j ′).A variable label in XH(i,j ) is called an (i, j )-height-constrained variable label , or simply an (i, j )-HC-variable label . A variable [v, v ′] in an unordered term tree pattern is called an (i, j )-height-constrained variable , or simply an 

(i, j )-HC-variable , if its label belongs to XH(i,j ).We write LUTTP Σ,Λ,X H for the set of all linear unordered term tree patterns in which vertices are labeled with symbols in Σ, edges are labeled with symbols in Λ, and variables are labeled with symbols in XH.

Definition 3 (Generalized bindings and substitutions) . Let x

be a variable label in XH(i,j ) for some integers 1 ≤ i ≤ j, and let g be an unordered term tree pattern in LUTTP Σ,Λ,X H with at least two vertices. Let rg be the root of g, and let q ∈ Vg \{rg }. We say that (g, q ) is admissible for x if dist g (rg , q ) ≥ i

and height (g) ≤ j. If (g, q ) is admissible for x, then x := (g, q ) is called a generalized binding for x, and dist g (rg , q )

is called its trunk length. Let f = ( Vf , E f , H f ) be an unordered term tree pattern in 

LUTTP Σ,Λ,X H , and let h = [ v, v ′] be an (i, j )-HC-variable of f labeled with x ∈ XH(i,j ). For a generalized binding 

x := ( g, q ), let g′ be a copy of g, and let r′ and q′ be the vertices of g′ corresponding to rg and q, respectively. The pattern obtained by applying x := ( g, q ) to h is defined by removing h from Hf , identifying v with r′, and identifying v′

with q′. The labels of the identified vertices inherit the labels of v and v′ in f .A generalized substitution is a finite set θ = {x1 := (g1, q 1), . . . , x m := ( gm, q m)} such that the variable labels 

x1, . . . , x m are pairwise distinct, and each gℓ contains no           

> Fig. 1. An instance matched only under the generalized model. The square denotes an (1 ,2) -HC-variable whose endpoints are the parent port vand the child port v′. Since v′is matched to the internal vertex qof the binding tree
> g, the instance is not matched under the previous leaf-restricted model but is matched under the generalized model.

variable whose label belongs to {x1, . . . , x m}. The instance 

f θ is the unordered term tree pattern obtained by applying all generalized bindings in θ simultaneously, and its root is the root of f .As shown in Fig. 1, the child port of an HC-variable may be matched to an internal vertex of a binding tree under the generalized model. Such a situation naturally occurs when a variable represents a structured component that attaches through an internal connection point rather than a leaf, as in abstract syntax representations or chemical structure trees. For t ∈ LUTTP Σ,Λ,X H , let L(t) = { T ∈ UT Σ,Λ |

T ∼= tθ for some generalized substitution θ }. We say that an unordered tree T matches an unordered term tree pattern t if 

T ∈ L(t).The membership problem for LUTTP Σ,Λ,X H under gener-alized bindings is defined as follows: given t ∈ LUTTP Σ,Λ,X H

and T ∈ UT Σ,Λ, decide whether T ∈ L(t). Unlike the previous rooted model, the child port of an HC-variable may correspond to any non-root vertex of a binding tree, not necessarily a leaf, while the height constraint is still imposed on the whole binding tree. The previous definition is obtained as a special case by restricting the distinguished vertex q to a leaf of the binding tree. III. A P OLYNOMIAL -T IME MATCHING ALGORITHM UNDER GENERALIZED BINDINGS 

Throughout this section, we assume |Σ| = |Λ| = 1 for clar-ity of presentation. The algorithm extends straightforwardly to general alphabets Σ and Λ by additionally requiring that vertex and edge labels match at each step; the asymptotic complexity is unchanged. Let t be a rooted unordered term tree pattern in 

LUTTP Σ,Λ,X H and let T be a rooted unordered tree in 

UT Σ,Λ. In this section, we present a polynomial-time al-gorithm for deciding whether T ∈ L(t) under generalized bindings. Processing T in a bottom-up order, the algorithm computes for each vertex u two kinds of information: V S (u),which records exact matches of subpatterns of t at u, and 

InS (u), which stores compressed inheritance information. The main difficulty in the generalized model is that inherited states must retain not only local feasibility but also minimum height information; in particular, triples of the form (u′, 0, j ′)

with j′ > 0 must be propagated upward. To handle this, for each relevant local matching instance we construct a weighted bipartite graph with dummy vertices and solve a bottleneck matching problem. The optimum bottleneck value gives the minimum height that must be inherited upward. For the time-complexity analysis, we assume that each local bottleneck matching instance is solved by the al-gorithm of Gabow and Tarjan [8], which improves upon Bhat’s O(n1/2m log n)-time algorithm [8, p. 412] and runs in 

O(( n log n)1/2m) time. Since both the size and the number of local instances are polynomially bounded by the sizes of t

and T , the overall algorithm runs in polynomial time. For u ∈ Vt, let t[u] denote the subpattern of t rooted at u,and for v ∈ VT , let T [v] denote the subtree of T rooted at v.If d is a descendant of v, then T [v] − T [d] denotes the tree obtained from T [v] by deleting all proper descendants of d,and height T [v](d) denotes its height. 

Definition 4 (Matching rule) . Let u′ be an internal vertex of 

t, and let c′

> 1

, . . . , c ′ 

> m

be the children of u′. For each child c′

> ℓ

(1 ≤ ℓ ≤ m), set J(c′

> ℓ

) = c′ 

> ℓ

if c′ 

> ℓ

is not the child port of an HC-variable. If c′ 

> ℓ

is the child port of an (i, j )-HC-variable, then set J(c′

> ℓ

) = ( c′

> ℓ

, i, j ). The matching rule associated with 

u′ is the expression u′ ⇐ { J(c′

> 1

), . . . , J (c′

> m

)}. Whether u′

is the child port of an HC-variable is determined from t and is not encoded in the rule itself. The set of all such rules is denoted by Rule (t).

Definition 5 (Exact-match information) . For v ∈ VT , let 

V S (v) = { u′ ∈ Vt | t[u′] matches T [v] }.

Definition 6 (Full inheritance information) . Let v ∈ VT . The full inheritance information at v, denoted by dInS (v), is the set of all triples (u′, i ′, j ′) such that u′ is the child port of an HC-variable of t, and there exists a descendant d of v with 

u′ ∈ V S (d), dist T (v, d ) = i′, and height T [v](d) = j′.A triple (u′, i ′, j ′) ∈ dInS (v) means that u′ matches at some descendant d of v with dist T (v, d ) = i′ and height T [v](d) = 

j′. For the correctness proof, it is convenient to use dInS (v)

first. The actual algorithm uses a compressed form in order to keep the number of states polynomial. For each child port u′ of an HC-variable in t, let tr (u′) and 

ht (u′) denote the trunk-length lower bound and the height upper bound of the corresponding HC-variable, respectively. 

Definition 7 (Compressed inheritance set) . Let v ∈ VT . The compressed inheritance set at v, denoted by InS (v), consists of all triples (u′, i ′′ , j ∗) such that: 1) u′ is the child port of an HC-variable of t;2) 0 ≤ i′′ ≤ tr (u′) − 1;3) if i′′ < tr (u′) − 1, then (u′, i ′′ , j ′) ∈ dInS (v) for some 

j′, and j∗ is the minimum such value; 4) if i′′ = tr (u′) − 1, then (u′, i ′, j ′) ∈ dInS (v) for some 

i′ ≥ tr (u′) − 1, and j∗ is the minimum such value; 5) j∗ ≤ ht (u′) − 1.Thus, for each child port u′, InS (v) merges all distances at least tr (u′) − 1 into the single value tr (u′) − 1, and for each retained distance stores only the minimum inherited height. Hence InS (v) removes redundant information from dInS (v)

while preserving the information needed for matching. 

Lemma 8. Let v be a vertex of T . For the purpose of deciding membership under generalized bindings, the full inheritance information dInS (v) can be replaced by the compressed in-heritance set InS (v) without loss of necessary matching information. 

Definition 9 (Local weighted bipartite graph) . Let u′

be an internal vertex of t with matching rule u′ ⇐{J(c′

> 1

), . . . , J (c′

> m

)}, and let v be an internal vertex of T with children d1, . . . , d n. If n < m , then the rule is not applicable at v. Assume n ≥ m, and define GW (u′, v ) = ( X ∪ Y, E, w )

as follows. The left part X consists of d1, . . . , d n, and the right part Y consists of J(c′

> 1

), . . . , J (c′

> m

) together with n − m

dummy vertices. For da ∈ X and a non-dummy pattern-side item J(c′

> ℓ

) ∈ Y ,add an edge as follows: 1) if J(c′

> ℓ

) = c′

> ℓ

, then (da, J (c′

> ℓ

)) ∈ E iff c′ 

> ℓ

∈ V S (da);2) if J(c′

> ℓ

) = ( c′

> ℓ

, i, j ), then (da, J (c′

> ℓ

)) ∈ E iff (c′

> ℓ

, i −

1, j ′) ∈ InS (da) for some j′ ≤ j − 1.Each such edge has weight 0.For da ∈ X and a dummy vertex δ ∈ Y , always add 

(da, δ ) to E. If there is no ℓ such that J(c′

> ℓ

) = ( c′

> ℓ

, i, j ), then define w(da, δ ) = 0 . Otherwise, let jmax = max { j | J(c′

> ℓ

) = (c′

> ℓ

, i, j ) for some ℓ }, and define 

w(da, δ ) = 

(

0 if height (T [da]) < j max ,height (T [da]) otherwise .

Definition 10 (Bottleneck value of a local matching instance) .

Let GW (u′, v ) = ( X ∪Y, E, w ) be the local weighted bipartite graph for u′ and v. We say that the matching rule of u′

is applicable at v if GW (u′, v ) has a perfect matching. If so, define b(u′, v ) = min M max e∈M w(e), where M ranges over all perfect matchings of GW (u′, v ). A perfect matching attaining this value is called a bottleneck matching. If the matching rule of u′ is applicable at v, then b(u′, v ) is the minimum height that must be inherited upward. Moreover, 

u′ ∈ V S (v) holds if and only if b(u′, v ) = 0 . If u′ is the child port of an HC-variable, then (u′, 0, 0) is inserted into InS (v)

when b(u′, v ) = 0 , and (u′, 0, b (u′, v )) is inserted into InS (v)

when b(u′, v ) > 0.The propagation step from the children of a tree vertex to the vertex itself is unchanged from the previous leaf-restricted model. Thus, for each vertex v of T , the algorithm first propagates inherited information from the children of v, then computes additional triples of the form (u′, 0, j ′) generated by applicable matching rules via bottleneck matchings, and finally compresses the result into InS (v).Algorithm 1 MATCH -G ENERALIZED -B INDINGS 

Input: t ∈ LUTTP Σ,Λ,X H and T ∈ UT Σ,Λ

Output: “yes” iff T ∈ L(t) 

> 1:

Let rt and rT be the roots of t and T 

> 2:

Compute Rule (t) 

> 3:

U ← VT 

> 4:

for each vertex v ∈ VT do  

> 5:

if v is a leaf of T then  

> 6:

Initialize V S (v) and InS (v) 

> 7:

U ← U \ { v} 

> 8:

else  

> 9:

V S (v) ← ∅ ; InS (v) ← ∅  

> 10:

end if  

> 11:

end for  

> 12:

while U̸ = ∅ do  

> 13:

for each vertex v ∈ U whose children are all in VT \ U

do  

> 14:

Iprop (v) ← PROPAGATE (v) 

> 15:

(Vnew (v), I new (v)) ← LB-U PDATE (v, Rule (t))  

> 16:

V S (v) ← Vnew (v) 

> 17:

InS (v) ← COMPRESS (Iprop (v) ∪ Inew (v))  

> 18:

U ← U \ { v} 

> 19:

end for  

> 20:

end while  

> 21:

if rt ∈ V S (rT ) then  

> 22:

return yes  

> 23:

else  

> 24:

return no  

> 25:

end if 

For a leaf v of T , we initialize V S (v) to the set of leaves of t

that match v, and InS (v) to the set of all triples (u′, 0, 0) such that u′ ∈ V S (v) and u′ is the child port of an HC-variable. In Algorithm 1, the procedures P ROPAGATE , LB-U PDATE , and COMPRESS use the already computed sets V S (d) and InS (d)

for all children d of the current vertex v.In Algorithm 1, P ROPAGATE performs the same inheri-tance propagation as in the previous leaf-restricted model, LB-U PDATE constructs weighted bipartite graphs for locally relevant matching rules, computes their bottleneck values, inserts the resulting exact-match information into V S (v), and generates new triples of the form (u′, 0, j ′) for InS (v), and COMPRESS replaces the resulting inheritance information by its compressed form. Let GW be a weighted bipartite graph. We define 

BottleneckV alue (GW ) as follows. If GW has no perfect matching, then BottleneckV alue (GW ) = ∞. Otherwise, 

BottleneckV alue (GW ) = min  

> M

max  

> e∈M

w(e),

where M ranges over all perfect matchings of GW . In the theoretical analysis, this value is computed by the algorithm of Gabow and Tarjan [8], whereas in the computational exper-iments it is computed by a straightforward count-up method. Algorithm 2 examines each matching rule at the current tree vertex v. If the corresponding local weighted bipartite graph 

Algorithm 2 LB-U PDATE 

Input: A vertex v of T , the sets V S (d1), InS (d1), . . . , V S (dn), InS (dn) for all children d1, . . . , d n of v, and the rule set Rule (t)

Output: The sets V S new (v) and InS new (v) 

> 1:

V S new (v) ← ∅  

> 2:

InS new (v) ← ∅  

> 3:

Let d1, . . . , d n be all children of v 

> 4:

for each matching rule ρ in Rule (t) do  

> 5:

Let ρ be the rule u′ ⇐ { J(c′

> 1

), . . . , J (c′

> m

)} 

> 6:

if n < m then  

> 7:

continue  

> 8:

end if  

> 9:

Construct the local weighted bipartite graph GW (u′, v )

according to Definition 9  

> 10:

β ← BottleneckV alue (GW (u′, v ))  

> 11:

if β < ∞ then  

> 12:

if β = 0 then  

> 13:

V S new (v) ← V S new (v) ∪ { u′} 

> 14:

end if  

> 15:

if u′ is the child port of an HC-variable of t then  

> 16:

InS new (v) ← InS new (v) ∪ { (u′, 0, β )} 

> 17:

end if  

> 18:

end if  

> 19:

end for  

> 20:

return (V S new (v), InS new (v)) 

has a perfect matching, let β = b(u′, v ). Then u′ is inserted into V S new (v) if and only if β = 0 . If u′ is the child port of an HC-variable, then (u′, 0, β ) is inserted into InS new (v).Thus, LB-U PDATE computes locally generated exact-match information and inheritance triples of the form (u′, 0, j ′).

Lemma 11. For every internal vertex v of T , Algorithm 2 cor-rectly computes all newly generated exact-match information at v and all newly generated inheritance triples of the form 

(u′, 0, j ′) arising from locally applicable matching rules at v.

Since inheritance propagation depends only on the dis-tances and heights recorded in InS (da), and not on whether child ports correspond to leaves or internal vertices, I_S ET _M AKING of [1], [6] applies without modification under generalized bindings. Algorithm 3 performs the same inheritance propagation as in the previous model, carrying upward all triples originating below the current vertex while updating their distance and height values. Algorithm 4 removes redundant inheritance information. For each child port u′, it keeps only the minimum inherited height for each retained distance, merges all distances at least tr (u′)−1 into the single value tr (u′) − 1, and discards triples whose inherited height is at least ht (u′) − 1.

Theorem 12. Algorithm 1 correctly decides whether T ∈ L(t)

holds under generalized bindings. Proof. The proof is by bottom-up induction on the vertices of T . For a leaf v of T , the initialization of V S (v) and Algorithm 3 PROPAGATE 

Input: A vertex v of T , and the sets V S (d1), InS (d1), . . . , V S (dn), InS (dn) for all children d1, . . . , d n of v

Output: The propagated inheritance information InS prop (v) 

> 1:

CS (da) ← V S (da) ∪ InS (da) for each child da 

> 2:

CSs ← (CS (d1), . . . , CS (dn))  

> 3:

InS prop (v) ← I_S ET _M AKING (v, CSs , H t) [1], [6]  

> 4:

return InS prop (v)

Algorithm 4 COMPRESS 

Input: A set S of inheritance triples 

Output: The compressed inheritance set C OMPRESS (S) 

> 1:

C ← ∅  

> 2:

for each child port u′ of an HC-variable of t do  

> 3:

for i′′ ← 0 to tr (u′) − 2 do  

> 4:

M ← { j′ | (u′, i ′′ , j ′) ∈ S } 

> 5:

if M̸ = ∅ and min M ≤ ht (u′) − 1 then  

> 6:

C ← C ∪ { (u′, i ′′ , min M )} 

> 7:

end if  

> 8:

end for  

> 9:

M ← { j′ | (u′, i ′, j ′) ∈ S for some i′ ≥ tr (u′) − 1 } 

> 10:

if M̸ = ∅ and min M ≤ ht (u′) − 1 then  

> 11:

C ← C ∪ { (u′, tr (u′) − 1, min M )} 

> 12:

end if  

> 13:

end for  

> 14:

return CInS (v) is immediate from the definitions. Assume inductively that, for every child d of an internal vertex v, the sets 

V S (d) and InS (d) have already been computed correctly. Then Algorithm 3 correctly propagates inheritance information from the children of v, Algorithm 2 correctly computes all locally generated exact-match and inheritance information at v,and Algorithm 4 replaces the resulting inheritance information by an equivalent compressed form. Hence both V S (v) and 

InS (v) are computed correctly. If rt ∈ V S (rT ), then T ∈ L(t)

by definition of V S . Conversely, if T ∈ L(t), then by induction on the structure of the corresponding substitution, one can verify that rt ∈ V S (rT ).

Theorem 13. Under the assumption that each local bottle-neck matching instance is solved by the algorithm of Gabow and Tarjan [8], the membership problem under generalized bindings is solvable in polynomial time. Proof. The analysis follows the framework of the previous leaf-restricted algorithm [1]. The propagation and compression steps remain polynomial, since for each vertex v of T , the compressed inheritance set InS (v) has polynomial size in |t|.The only new cost arises in Algorithm 2. For each internal vertex v of T and each matching rule of t, we solve one local bottleneck matching instance on GW (u′, v ). Let d = deg T (v).Then GW (u′, v ) has O(d) vertices on each side and O(d2)

edges, so by Gabow and Tarjan [8], each instance is solved in O (d log d)1/2 · d2 = O d5/2√log d. Since |Rule (t)| =

O(|Vt|), the total cost over all vertices of T is 

O |Vt| X

> v∈VT

deg T (v)5/2plog deg T (v)

!

.

Let N = |VT | and ∆ = max v∈VT deg T (v). Since 

deg T (v) ≤ ∆ and P 

> v∈VT

deg T (v) = N − 1, this is bounded by O |Vt| N ∆3/2√log ∆ , and hence also by 

O



|Vt| | VT |5/2plog |VT |



. Therefore, the membership prob-lem under generalized bindings is solvable in polynomial time. IV. C OMPUTATIONAL EXPERIMENTS 

A. Implementation Details 

Although the algorithm of Gabow and Tarjan [8] yields a better theoretical bound for the bottleneck matching subprob-lem, its implementation is nontrivial in practice. Therefore, in our computational experiments, we adopt a simple count-up method for solving each local bottleneck matching instance. More precisely, if no item of the form J(c′

> ℓ

) = (c′

> ℓ

, i, j )

occurs, we test only β = 0 ; otherwise, we enumerate candidate bottleneck values β = 0 , 1, . . . , J max − 1 in increasing order, where Jmax is the maximum height bound appearing in an HC-variable child over all matching rules of t. For each β,we test whether a perfect matching exists in the subgraph of 

GW (u′, v ) induced by edges of weight at most β, using the Hopcroft–Karp algorithm [9]. The smallest feasible value of 

β is then taken as the bottleneck value. Thus, at most max {1, J max } perfect-matching tests are performed per instance, each requiring O(d5/2) time for a bipartite graph of degree d. Hence the implemented algorithm runs in O |Vt| N ∆3/2 max {1, J max } time, where N = |VT |

and ∆ = max v∈VT deg T (v). When the height bounds of HC-variables are large, this simple implementation requires more perfect-matching tests (proportional to Jmax ), which may increase the overall running time in practice. Thus, the ex-perimental results show that the proposed framework remains practically workable even when the bottleneck-matching step is implemented in this straightforward manner. 

B. Experimental Setup 

To evaluate the practical running time of the proposed algo-rithm, we conducted computational experiments on randomly generated instances and compared the generalized binding model with the previous leaf-restricted model. For each node count from 20 to 200, we generated 950,000 instances in total and measured the running time of both algorithms under the same implementation environment. For each node count, both target trees and patterns were generated at random, and HC-variables together with their parameters (i, j ) were also assigned randomly under the size constraints of the generated patterns. The same family of random instances was used for comparing the previous leaf-restricted model and the generalized model. All experiments were conducted on a machine running Ubuntu 24.04 and equipped with a 13th Gen Intel(R) Core(TM) i9-13900K CPU and 62 GB of RAM. Fig. 2. Mean running time of the previous model and the generalized model for each node count. Shaded regions indicate the 25th–75th percentile ranges of the running times over the randomly generated instances. 

The algorithm was implemented in Python. In the experiments reported below, we focus on the dependence of the running time on the input size. 

C. Running-Time Results 

Fig. 2 shows the mean running time of the previous model and the generalized model with respect to the number of nodes. In both models, the running time increases steadily as the input size grows. The generalized model is consistently slower than the previous one, but the difference remains moderate over the entire tested range. More specifically, the ratio of the mean running time of the generalized model to that of the previous model is approximately 1.08 on average, and stays within a narrow range from 1.0694 to 1.0905 over all tested node counts. The overall mean of the per-node ratios is 1.0803. Thus, although the generalized model requires additional bottleneck-matching computations, the increase in running time is stable and limited. These results indicate that the proposed generalization preserves practical efficiency while allowing more flexible bindings than the previous leaf-restricted model. In particular, for the tested range up to 200 nodes, the observed running time remained on the order of 10 −3 to 10 −2 seconds per instance. The nearly constant runtime ratio suggests that the additional cost introduced by generalized bindings scales proportionally to the baseline matching cost over the tested range. V. C ONCLUDING REMARKS 

In this paper, we presented a polynomial-time algorithm for deciding the membership problem under generalized bindings and reported computational results supporting its practical feasibility. As future work, we plan to extend the framework to richer structural restrictions such as diameter constraints and to study its learnability under computational learning mod-els. This is a natural direction, because generalized bindings increase the expressive power of the pattern language while preserving polynomial-time membership. REFERENCES [1] S. Matsushita, T. Shoudai, and Y. Suzuki, “Efficient Pattern Matching in Unordered Term Tree Patterns with Height Constraints,” in Proc. 18th Int. Congr. Advanced Applied Informatics (IIAI-AAI 2025), 20th Int. Conf. on E-Service and Knowledge Management (ESKM 2025) ,Kitakyushu, Japan, Jul. 2025. [2] P. Kilpeläinen and H. Mannila, “Ordered and Unordered Tree Inclusion,” 

SIAM Journal on Computing , vol. 24, no. 2, pp. 340–356, 1995. [3] T. Akutsu, J. Jansson, R. Li, A. Takasu, and T. Tamura, “New and Im-proved Algorithms for Unordered Tree Inclusion,” Theoretical Computer Science , vol. 883, pp. 83–98, 2021. [4] T. Shoudai, T. Uchida, and T. Miyahara, “Polynomial Time Algorithms for Finding Unordered Tree Patterns with Internal Variables,” in Proc. 13th International Symposium on Fundamentals of Computation Theory (FCT 2001) , LNCS 2138, Springer, pp. 335–346, 2001. [5] Y. Suzuki, T. Shoudai, T. Uchida, and T. Miyahara, “An Efficient Pattern Matching Algorithm for Ordered Term Tree Patterns,” IEICE Transactions on Fundamentals of Electronics, Communications and Computer Sciences , vol. E98-A, no. 6, pp. 1197–1211, 2015. [6] T. Shoudai, K. Aikoh, Y. Suzuki, S. Matsumoto, T. Miyahara, and T. Uchida, “Polynomial Time Inductive Inference of Languages of Ordered Term Tree Patterns with Height-Constrained Variables from Positive Data,” IEICE Transactions on Fundamentals of Electronics, Communications and Computer Sciences , vol. E100-A, no. 3, pp. 785– 802, 2017. [7] T. Shoudai, T. Miyahara, T. Uchida, S. Matsumoto, and Y. Suzuki, “An Efficient Pattern Matching Algorithm for Unordered Term Tree Patterns of Bounded Dimension,” IEICE Transactions on Fundamentals of Electronics, Communications and Computer Sciences , vol. E101-A, no. 9, pp. 1344–1354, 2018. [8] H. N. Gabow and R. E. Tarjan, “Algorithms for Two Bottleneck Optimization Problems,” Journal of Algorithms , vol. 9, no. 3, pp. 411– 417, 1988. [9] J. E. Hopcroft and R. M. Karp, “An n5/2 Algorithm for Maximum Matchings in Bipartite Graphs,” SIAM Journal on Computing , vol. 2, no. 4, pp. 225–231, 1973.