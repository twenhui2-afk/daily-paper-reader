---
title: Efficient Pattern Matching for Unordered Term Tree Patterns under Generalized Height-Constrained Bindings
source: arxiv
title_zh: 用于Unordered Term Tree Patterns under Generalized Height-Constrained Bindings的Efficient Pattern Matching
abstract_zh: "Unordered trees are useful 用于模型ing hierarchical structures in which order among siblings is irreleva。\n\n核心做法：To represent flexible structural patterns in such data，unordered term tree patterns 结合 height-constr。\n\n主要结果：We also implement proposed algorithm 与 conduct computational experiments to evaluate its running tim。\n\n值得关注的点在于：experimental results show that proposed 方法达到 practical running times。"
abstract_en: "Unordered trees are useful for modeling hierarchical structures in which the order among siblings is irrelevant. To represent flexible structural patterns in such data, unordered term tree patterns with height-constrained variables provide a natural framework. In our previous work, we studied the pattern matching problem for rooted unordered term tree patterns with height-constrained variables under the restriction that the child port of each variable must correspond to a leaf of a binding tree. In this paper, we remove this restriction and generalize the binding model so that the child port may correspond to any non-root vertex of a binding tree. Under generalized bindings, we formulate the corresponding membership problem and present a polynomial-time pattern matching algorithm. We also implement the proposed algorithm and conduct computational experiments to evaluate its running time. The experimental results show that the proposed method achieves practical running times."
authors: "Shintaro Matsushita, Takayoshi Shoudai, Yusuke Suzuki"
date: 2026-07-16
journal: cs.DS
source_link: "https://arxiv.org/pdf/2607.14617v1"
pdf: "https://arxiv.org/pdf/2607.14617v1"
score: 8.15
tldr: 围绕《用于Unordered Term Tree Pa...》；To represent flexible structural...；We also implement proposed algorithm...
tldr_cn: 围绕《用于Unordered Term Tree Pa...》；To represent flexible structural...；We also implement proposed algorithm...
tldr_en: "Unordered trees are useful for modeling hierarchical structures in which the order among siblings is irrelevant. To represent flexible structural patterns in such data, unordered term tree patterns with height-constrained variables provide a natural framework."
selection_source: fresh_fetch
motivation: Unordered trees are useful 用于模型ing hierarchical structures in which order among siblings is irreleva。
method: 核心做法：To represent flexible structural patterns in such data，unordered term tree patterns 结合 height-constr。
result: 主要结果：We also implement proposed algorithm 与 conduct computational experiments to evaluate its running tim。
conclusion: 值得关注的点在于：experimental results show that proposed 方法达到 practical running times。
---

## 摘要
Unordered trees are useful 用于模型ing hierarchical structures in which order among siblings is irreleva。

核心做法：To represent flexible structural patterns in such data，unordered term tree patterns 结合 height-constr。

主要结果：We also implement proposed algorithm 与 conduct computational experiments to evaluate its running tim。

值得关注的点在于：experimental results show that proposed 方法达到 practical running times。

## Abstract
Unordered trees are useful for modeling hierarchical structures in which the order among siblings is irrelevant. To represent flexible structural patterns in such data, unordered term tree patterns with height-constrained variables provide a natural framework. In our previous work, we studied the pattern matching problem for rooted unordered term tree patterns with height-constrained variables under the restriction that the child port of each variable must correspond to a leaf of a binding tree. In this paper, we remove this restriction and generalize the binding model so that the child port may correspond to any non-root vertex of a binding tree. Under generalized bindings, we formulate the corresponding membership problem and present a polynomial-time pattern matching algorithm. We also implement the proposed algorithm and conduct computational experiments to evaluate its running time. The experimental results show that the proposed method achieves practical running times.
