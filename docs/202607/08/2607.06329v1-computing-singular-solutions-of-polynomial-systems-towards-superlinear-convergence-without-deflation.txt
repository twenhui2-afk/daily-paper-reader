Title: Computing singular solutions of polynomial systems: towards superlinear convergence without deflation

URL Source: https://arxiv.org/pdf/2607.06329v1

Published Time: Wed, 08 Jul 2026 01:06:40 GMT

Number of Pages: 35

Markdown Content:
# Computing singular solutions of polynomial systems: towards superlinear convergence without deflation 

Mikhail Karapetyants Vladimir Kolmogorov Jeferson Zapata 

Institute of Science and Technology Austria (ISTA) 

{mikhail.karapetyants,vnk,jeferson.zapata }@ist.ac.at 

Abstract 

In Numerical Algebraic Geometry (NAG) isolated solutions of polynomial systems are usu-ally computed by tracking a solution curve defined by a homotopy equation. The tracking problem becomes especially challenging close to a singular root (the “endgame” regime). Ex-isting approaches include power series endgames, Cauchy endgames, and various methods that regularize the system via dual-space-based deflation . We make the following contributions. (1) For corank-1 systems we introduce a new “Arclength Endgame” which combines the idea of the classical pseudo-arclength continuation method with the estimation of the Puiseux series of the curve. We formally prove that it has a superlinear rate of convergence in some neighborhood of the root. The method uses only evaluations of the system and its Jacobian, whereas previous techniques with proven superlinear convergence (such as deflation) require computing additional derivatives of the system. (2) For systems with a larger corank we propose a heuristic “Lifted Arclength Endgame”, which shows promising experimental results. (3) A key step in our approach (as well as in the standard power series endgame) is esti-mating the Puiseux series of the curve, which is characterized by fractional exponents ki/c for 

i ≥ 1 together with associated coefficients. Previous work addressed only estimating the ratio 

k1/c . We present a new method for that which empirically appears to be more stable than previous methods, and also show how to estimate ki/c for i ≥ 2. 

# 1 Introduction 

We consider the problem of numerically solving a system of equations f (z) = 0 with a zero-dimensional set of solutions. Here f is an analytic mapping Cn → Cn. A standard approach in Numerical Algebraic Geometry (NAG) for tackling this problem is as follows. First, one constructs a homotopy function h(z, t ) = (1 − t)f (z) + tg (z) where g is a polynomial system with easily computable roots. Consider one such root zroot . If g is chosen generically then there exists a unique smooth function z : (0 , 1] → Cn with z(1) = zroot and h(z(t), t ) = 0 for all t ∈ (0 , 1]. Furthermore, if z((0 , 1]) is bounded then the limit z∗ = z(0) = lim t→0 z(t) exists and is a root of 

f . The latter condition will always be satisfied if system f is homogeneous. If g is chosen to have sufficiently many roots then every root of f will be covered with probability 1 [SW05, Theorem 8.4.1]. By differentiating equation h(z(t), t ) = 0 with respect to t one obtains Davidenko ODE: ˙z(t) = −hz (z(t), t )−1ht(z(t), t ) (1) We now need to track curve z(t) by numerically solving this ODE. This problem becomes especially challenging when t approaches zero and z∗ = z(0) is a singular solution z∗, i.e. the Jacobian fz (z∗) is singular. This regime, referred to as the “endgame”, consti-tutes the main focus of the present work. The following assumptions are maintained throughout the manuscript: 1arXiv:2607.06329v1 [math.NA] 7 Jul 2026 Assumption 1. (a) h : Cn+1 → Cn is a polynomial mapping, and f (z) = h(z, 0) .

(b) Point z∗ ∈ Cn is an isolated solution of f (z). We denote x∗ = ( z∗, 0) , J∗ = hz (x∗) = fz (z∗)

and h∗ 

> t

= ht(x∗). We also let κ = n − rank (J∗) be the corank of J∗.

(c) rank (J∗) < n , i.e. z∗ is a singular solution of f .

(d) h∗ 

> t

is linearly independent of columns in J∗, i.e. 

rank ([ J∗|h∗ 

> t

]) = rank (J∗) + 1 .

(e) There exists a finite set Π of formal Puiseux series of the form 

z(t) = z∗ +

> ∞

X

> j=1

aj tj/c = z∗ + ak1 tk1/c + ak2 tk2/c + . . . (2) 

where c ∈ N and 1 ≤ k1 < k 2 < k 3 . . . is an increasing sequence of integers corresponding to non-zero coefficients aj such that: 

(i) each series 1 z(·) ∈ Π is convergent in some neighborhood Ωt ⊆ C of 0 and satisfies h(z(t), t ) = 0 

for t ∈ Ωt;

(ii) for every open set U ⊆ Ωt and every z(·) ∈ Π, function ϕ(t) = det[ hz (z(t), t )] is not identically zero on U ;

(iii) there exists neighborhood Ωz of z∗ such that for every (z, t ) ∈ Ωz × Ωt with h(z, t ) = 0 there exists z(·) ∈ Π with z(t) = z.

The Puiseux parametrization in (e) is a standard consequence of the local parametrization theorem for complex analytic curves (and of the assumption that z∗ is isolated root of f ). The additional transversality and punctured-regularity requirements in (d) and (e)(ii) are generic for the usual coefficient homotopies used in numerical algebraic geometry; in practice they are enforced by choosing a generic start system or by applying the standard gamma trick. 

1.1 Corank-1 problems 

Our first contribution is the following result. 

Theorem 1. Suppose that κ = 1 . There exist a neighborhood Ω of x∗, constants α > 1, β min > 0

and an algorithm that takes point x◦ = ( z◦, t ◦) ∈ Cn+1 , parameters β, k max 1 and does the following: if β > β min , x◦ ∈ Ω, ∥h(x◦)∥ ≤ | t◦|β and index k1 of each Puiseux series z(·) ∈ Π satisfies 

k1 ≤ kmax 1 , then it produces a (possibly infinite) sequence of points x1, . . . , x K such that (i) ∥xk −x∗∥ ≤ ∥ x◦ −x∗∥αk

for each k ∈ [K], and (ii) if K < ∞, then the last point x = xK = ( z, t )

satisfies ∥h(x)∥ ≤ | t|β . It uses O(log(1 + β) + K) evaluations of function h(·) and its Jacobian. 

A recursive application of the algorithm in Theorem 1 immediately gives an algorithm with a superlinear convergence rate. 

Corollary 2. Suppose that κ = 1 . There exist a neighborhood Ω of x∗, constants α > 1, β min > 0

and an algorithm that takes point x◦ = ( z◦, t ◦) ∈ Cn+1 , parameters β, k max 1 and does the following: if β > β min , x◦ ∈ Ω, ∥h(x◦)∥ ≤ | t◦|β and index k1 of each Puiseux series z(·) ∈ Π satisfies 

k1 ≤ kmax 1 , then it produces a sequence of points x1, x 2, . . . such that ∥xk − x∗∥ ≤ ∥ x◦ − x∗∥αk

for each k ≥ 1. Computing each subsequent point uses O(log(1 + β)) evaluations of function h(·) and its Jacobian. 

The algorithm in Theorem 1 is achieved by combining two steps.         

> 1When writing z(·)∈Π, we will assume with some abuse of notation that we have chosen not only the formal series but also the specific branch t7→t1/c used in (2) (unless noted otherwise).

2• Predictor phase: estimate coefficients z∗, k 1/c, a k1 of a Puiseux series z(·) ∈ Π, then evaluate the obtained approximation of z(·) at t = 0 obtaining predictor point ˆ x = (ˆ z, 0). We use a new rule for estimating k1/c , and formally analyze the accuracy of the resulting approximation. 

• Corrector phase: augment the system h(x) = 0 with a new linear equation given by a hyperplane passing through ˆ x, whose normal is tangent to the solution curve. Solve the augmented system by applying several steps of the Newton’s method. The idea of adding an extra hyperplane is common in the (pseudo)-arclength continuation methods [Wem71; Rik72; Kel77]. Our scheme differs in the choice of the step size. To our knowledge, existing arclength methods control the step size via parameter ∆ s representing the Euclidean length along the solution curve. We are not aware of methods that explicitly combine an arclength method together with a Puiseux series-aware predictor. Due to this connection, we call our method the arclength endgame , even though it does not use the Euclidean length of the curve in any way. 

Related work Below we discuss papers that give an explicit superlinear convergence rate when 

t → 0. One approach is the deflation technique [OWM83; LVZ06; LVZ08], which introduces new auxiliary variables and new equations such that z∗ becomes an isolated nonsingular root of the extended system. By classical results, applying the Newton’s method for such system would give an algorithm with a quadratic convergence rate. In general, the size of the extended system can be Θ( n2μ) where μ is the multiplicity of z∗. For corank-1 systems, more efficient techniques (without an exponential dependence on μ) with a guaranteed quadratic convergence rate have been proposed in Li and Zhi [LZ12; LZ22]. Note that these techniques rely on computing polynomials in the Max Noether space; these are polynomials that are linear combinations of higher-order derivatives of the original equations, and evaluate to 0 at z∗. Thus, they require computing additional derivatives on the input system. In contrast, the algorithm in Theorem 1 uses only evaluations of function h and its Jacobian. 

1.2 Corank κ ≥ 2

Let us now consider systems with κ ≥ 2. We investigate a heuristic algorithm that can be viewed as an extension of the arclength endgame. Given an initial point x◦ = ( z◦, t ◦), we first compute predictor ˆ x = (ˆ z, ˆt). We then introduce κ − 1 new variables ξ and κ new linear hyperplanes passing through ˆ x, obtaining an extended system with Jacobian J satisfying ∥J−1∥ ≤ O(1). This system is solved using several steps of the Newton’s method, producing new point ( v, t, ξ ). Experimentally, we observed that usually this step has a superlinear convergence rate (i.e. ∥v − z∗∥ ≤ ∥ z◦ − z∗∥1+Θ(1) ), assuming that we are in the endgame zone. However, new point ( v, t ) is no longer on the homotopy h. To continue, we change the homotopy to hv(z, t ) = 

f (z) − tf (v), and continue the process starting with ( v, 1). Note that this becomes an algorithm for refining a solution close to z∗, rather than for following a specified homotopy. We call this procedure a lifted arclength endgame . We investigate its properties in section 4.2, and compare with the classical power-series endgame. 

1.3 Estimating coefficients of the Puiseux series 

Computing the ℓ-th order predictor requires estimating z∗ and ratios ki/c together with coeffi-cients aki for i ∈ [ℓ]. A classical approach [SW05; Bat+13] uses either a linear predictor or a cubic predictor computed via a Hermite interpolation from two sample points z1, z 2 on the homotopy curve and their derivatives. However, it generally assumes a dense, sequential set of fractional exponents, i.e. that ( k1, k 2, k 3) = (1 , 2, 3). This assumption fails to capture the geometry of sparse Puiseux series, where the valid fractional powers are strictly governed by the value semigroup of the local ring at the singularity [ZS65; Wal04]. Additionally, as demonstrated by polyhedral 3endgames [HS95], the vector of leading fractional exponents ( k1/c, k 2/c, . . . ) represents a funda-mental geometric property of the variety. In this framework, the fractional exponents defining the path direction correspond directly to the inner normals of the facets characterizing the system’s Newton polytope. While it is possible to estimate for a given polynomial homotopy h the set of vectors of leading fractional exponents for the Puiseux series in Π, tracking a homotopy curve z(·) ∈ Π requires the vector corresponding to this specific curve. To our knowledge, existing techniques are limited to estimating the first fractional exponent k1/c . This includes the trial-and-error method (“ cSORT ”), which evaluates prediction errors across a range of candidate integer values for c [MSW92a; SW05]. Another established technique is the geometric sequence sampling approach (“ cLOG ”) which isolates the leading fractional exponent by analyzing the logarithmic differences of path samples taken at geometrically decreasing parameter values [BHS11; SW05]. Yet another classical approach uses the Cauchy integral method [BHS11; SW05]. We make the following contributions. 

• We propose a new method for estimating k1/c , and show empirically that it can be more stable than cSORT and cLOG .

• We show how to estimate higher-order fractional exponents, in particular k2/c and k3/c .

# 2 Background and notation 

For a function F : Cn → Cm and variables x = ( u, v ) the Jacobian of F with respect to u is denoted either as Fu(x) or as DuF (x). Both x and F (x) are treated as column vectors. The Hermitian transpose of matrix A is denoted as A†.Notations z and z(·) will denote different objects: z(·) is a function, while z is a specific value which is not necessarily related to z(·). Throughout this paper, for a point ( z, t ) ∈ Cn+1 we denote ˙z = −hz (z(t), t )−1ht(z(t), t ) (3) This definition depends also on t; the value of t should always be clear from the context. Note, if 

z = ¯ z(t) for a differentiable function ¯ z(·) satisfying h(¯ z(t), t ) = 0 in some neighborhood of t then ˙z = ddt ¯z(t). We define variety V ⊆ Cn+1 as V = h−1(0 , 0). If t1, t 2 are complex values in C then [ t1, t 2] denotes the interval {αt 1+(1 −α)t2 : α ∈ [0 , 1] ⊂ R}.Notation f (t) = O(g(t)) for complex-valued functions f, t will mean that ∥f (t)∥ ≤ C∥g(t)∥ if 

|t| ≤ tmax , for some constants C > 0 and tmax > 0. 

2.1 Power-series endgame 

One classical approach to tracking the path close to the root is the power-series endgame . It maintains a set of pairs of the form 

X = {xi = ( zi, t i)}i=0 ,1,... 

where ti ∈ (0 , 1] and zi approximates z(ti). At each step it does the following. 

• Using pairs in X , estimate the first ℓ + 1 coefficients of series (2) together with ratios ki/c ,obtaining approximation ˆz(t) = ˆ z∗ + ˆ ak1 tk1/c + . . . + ˆ akℓ tkℓ/c . (4) 4• Select “target” value ˆ t ∈ (0 , 1]. Usually one takes ˆ t = ρ · t◦ where t◦ is the smallest value present in X , and parameter ρ ∈ (0 , 1) is either fixed or updated adaptively based on the success / failure status of previous steps. 

• Predictor step: compute vector ˆ z = ˆ z(ˆ t). 

• Corrector step: compute z by applying several steps of the Newton’s method to solve system 

h(z, ˆt) = 0 using ˆ z as the starting point. If the Newton’s method converges according to a certain criterion then add pair ( z, ˆt) to X .Popular choices for the predictor are a linear predictor (that estimates z∗, a k1 , k 1/c ) and a cubic predictor (that assumes that ( k1, k 2, k 3) = (1 , 2, 3) and estimates z∗, a k1 , a k2 , a k3 , c ). We refer to Section 5 for a further discussion of predictors. 

2.2 Newton’s method 

In this section we state the classical Kantorovich theorem about convergence of the Newton’s method which we will need later [FS12]. 

Theorem 3. Let X and Y be Banach spaces, Ω be a subset of X and F be a continuous non-linear operator, F : Ω 7 → Y , such that F is continuously Fr´ echet-differentiable on int (Ω) . For an initial guess x0 ∈ Ω and for positive reals L, C ∈ R+ assume that 

• F ′(x0) is non-singular; 

• [F ′(x0)] −1 

F ′(x) − F ′(y)



≤ L∥x − y∥ ∀x, y ∈ Ω;

• [F ′(x0)] −1 F (x0) ≤ C;

• 2CL < 1.Consider r ∈ [r−, r +] where 

r− = 1 − √1 − 2CL L , r+ = 1 + √1 − 2CL L .

If B(x0, r ) = {x ∈ X : ∥x − x0∥ < r } ⊂ Ω then the sequence {xk} generated by Newton’s method for solving non-linear equation F (x) = 0 with initial point x0,

xk+1 = xk − F ′(xk)−1 F (xk) ∀k ≥ 0,

is contained in B(x0, r ), converges to the unique zero x∗ ∈ B(x0, r ) of F and the following error bound holds: 

∥xk+1 − x∗∥ ≤ L

2√1 − 2CL ∥xk − x∗∥2 ∀k ≥ 0

# 3 Linear predictor 

To prove Theorem 1, we will use a linear predictor that estimates z∗, ak1 and k1/c . In this section we will analyze the accuracy of this predictor, assuming in particular that the input points satisfy the homotopy only approximately. Later on, in Section 5, we will analyze higher-order predictors (but only assuming that the input points lie exactly on the homotopy curve). Our predictor will depend on parameters γ, β, k max 1 ; these are positive constants that will be specified later. Given input point x = ( z, t ), it does the following. 51. Set t1 = (1 − | t|γ ) · t.2. Run the Newton’s method to solve the system h(z, t 1) = 0 starting with a point 

z0 = z + ˙ z(t1 − t),

until getting a point z1 with ∥h(z1, t 1)∥ ≤ | t1|β .3. Find positive integers c, k 1 with k1 ≤ kmax 1 that minimize 

∥t ˙z(t) − t1 ˙z(t1)∥∥z(t) − z(t1)∥ − k1

c .

4. Output predictor ˆ z = z − ck1 ˙zt .We will prove the following result. 

Theorem 4. There exist constants γmin > 0, η > 0 with the following property. Suppose that 

γ > γ min , β > ηγ , and index k1 of each Puiseux series z(·) ∈ Π satisfies k1 ≤ kmax 1 . Then there exists a neighborhood Ω of x∗ such that any x = ( z, t ) ∈ Ω with ∥h(x)∥ ≤ | t|β satisfies the following. 

(i) ∥ˆz −z∗∥ = O  ∥z − z∗∥k2/k 1

 where k1, k 2 are the indices in eq. (2) of the Puiseux series z(·) ∈ Π

with the smallest ratio k2/k 1.

(ii) The Newton’s method in step 2 terminates after O(log(1 + β)) iterations. If κ = 1 then γmin < 1.

The remainder of this section is devoted to the proof of this theorem. In these proofs we will often omit the phrase “there exists a neighborhood Ω of x∗ such that ...”, making it implicit. For example, we will write O(|t|a) ≤ | t|b when a > b ; this would hold if |t| is sufficiently small. Also, in each lemma we will implicitly assume that the current Ω is contained in the neighborhoods considered in all previous statements. One of them is the neighborhood Ω z × Ωt defined in As-sumption 1(e), so all Puiseux series z(·) ∈ Π will be assumed to be convergent in the considered neighborhood. First, we analyze what happens when the points lie exactly on the curve. 

Lemma 5. Consider Puiseux series z(·) ∈ Π associated with integers c, k 1, k 2. There exists a neighborhood Ω of x∗ such that points x = ( z, t ) = ( z(t), t ) ∈ Ω satisfy the following. 

(a) lim  

> t→0

sup    

> t1∈[0 ,t )
> ∥t˙z(t)−t1˙z(t1)∥∥z(t)−z(t1)∥

− k1 

> c

= 0 .

(b) ∥ˆz − z∗∥ = O(∥z − z∗∥k2/k 1 ) assuming that ˆz was computed with the correct value of k1/c .Proof. Part (a) Plugging the Puiseux series into z(·) for coordinate i ∈ [n] yields 

z(t)[ i] − z(t1)[ i] = ak1 [i]( tk1/c − tk1/c  

> 1

) + O(tk2/c − tk2/c  

> 1

) = ak1 [i] · τ + O(σ)

t ˙z(t)[ i] − t1 ˙z(t1)[ i] = k1

c · ak1 [i]( tk1/c − tk1/c  

> 1

) + O(tk2/c − tk2/c  

> 1

) = k1

c · ak1 [i] · τ + O(σ)where we denoted τ = tk1/c − tk1/c  

> 1

, σ = tk2/c − tk2/c  

> 1

. Therefore, 

∥z(t) − z(t1)∥ = ∥ak1 ∥ · | τ | + O(σ)

∥t ˙z(t) − t1 ˙z(t1)∥ = k1

c · ∥ ak1 ∥ · | τ | + O(σ)Note that στ = t(k2−k1)/c · 1−(t1/t )k2/c  

> 1−(t1/t )k1/c

and hence στ = O(|t|(k2−k1)/c ) (since 1−ρk2/c  

> 1−ρk1/c

≤ k2 

> k1

for 

ρ ∈ [0 , 1)). This implies the claim. 6Part (b) Assuming we have successfully extracted the exact leading exponent ratio k1/c , the target prediction ˆ z (aiming for t = 0) is computed via the linear ideal predictor: ˆz = z(t) − ck1

t ˙z(t)Substituting the series expansions into this predictor equation we have ˆz = z∗ +

> ∞

X

> j=k1

aj tj/c − ck1

> ∞

X

> j=k1

jc aj tj/c 

= z∗ +



1 − k2

k1



ak2 tk2/c + O(tk3/c )The leading terms ak1 tk1/c cancel exactly. Therefore, isolating the error gives 

∥ˆz − z∗∥ = O(tk2/c ) as t → 0.

To express this error in terms of the distance to the root, we invert the leading term of the path expansion. Since ∥z(t) − z∗∥ = ∥ak1 ∥tk1/c + O(tk2/c ), we can asymptotically bound the parameter 

t as: 

t1/c = O



∥z(t) − z∗∥1/k 1



Substituting this relation back into our predictor error bound produces the final geometric bound 

∥ˆz − z∗∥ = O



∥z(t) − z∗∥1/k 1

k2



= O



∥z(t) − z∗∥k2/k 1



Next, we analyze the existence and behaviour of matrix h−1 

> z

(z(t), t ) in a neighborhood of 0. 

Lemma 6. Consider formal series z(·) ∈ Π associated with integers k1, c . There exists a constant 

δ > 0 and a punctured neighborhood Ω of x∗ such that for any z(·) ∈ Π and x = ( z, t ) = ( z(t), t ) ∈ Ω

matrix hz (x) is nonsingular, and there holds ∥h−1 

> z

(x)∥ ≤ | t|−δ, ∥ ˙z(t)∥ = O(|t|k1/c −1) and ∥¨z(t)∥ =

O(|t|k1/c −2). Furthermore, there exists component i ∈ [n] such that | ˙zi(t)| = Θ( |t|k1/c −1).If κ = 1 then δ ∈ (0 , 1) and k1/c < 1.Proof. Define function ˜ z(s) = z(sc). This is an analytic function at 0, as it is given by a convergent power series at some neighborhood of 0. Also, z(t) = ˜ z(t1/c ) for some branch t 7 → t1/c .Define J(t) = hz (z(t), t ) and ˜J(s) = J(sc). Note that the entries of matrix ˜J are analytic functions of s since ˜J(s) = hz (˜ z(s), s c), and thus det ˜J(s) is also an analytic function of s. By Assumption 1, det ˜J(s) is not identically zero, therefore det ˜J(s) = sdφ(s) for some integer d ≥

1 and analytic function φ(s) with φ(0) ̸ = 0. In particular, det ˜J(s)̸ = 0 in some punctured neighborhood of 0. By Cramer’s rule, ˜J(s)−1 = adj ˜J(s)det ˜J(s) = adj ˜J(s) 

> φ(s)

· s−d. Function adj ˜J(s) 

> φ(s)

is analytic at 0, therefore ∥ ˜J(s)−1∥ = O(|s|−d) and hence ∥hz (z(t), t )−1∥ = ∥ ˜J(t1/c )−1∥ = O(|t|−d/c ) < |t|−δ

for any fixed δ > d/c .By differentiating the formal series (2) we obtain ˙z(t) = k1 

> c

ak1 tk1/c −1(1 + o(1)) and ¨ z(t) =  

> k1
> c

( k1 

> c

− 1) ak1 tk1/c −2(1 + o(1)). This implies that ∥ ˙z(t)∥ = O(|t|k1/c −1) and ∥¨z(t)∥ = O(|t|k1/c −2), and also | ˙zi(t)| = Θ( |t|k1/c −1) for all components i ∈ [n] with ( ak1 )i̸ = 0. Let us now assume that κ = 1. Let J∗ = U ∗Σ∗(V ∗)† and hz = U ΣV † be SVDs of J∗ =

hz (x∗) and hz (x) respectively, with Σ ∗ = diag (σ∗ 

> 1

, . . . , σ ∗

> n

), σ∗ 

> 1

≥ . . . ≥ σ∗ 

> n−1

> σ ∗ 

> n

= 0, Σ = 

diag (σ1, . . . , σ n), σ1 ≥ . . . ≥ σn ≥ 0. Let {u∗ 

> i

}, {v∗ 

> i

}, {ui}, {vi} be the columns of U ∗, V ∗, U, V 

7respectively. Vector u∗ 

> n

is the left singular vector of J∗ for value σ∗ 

> n

= 0 (i.e. (u∗

> n

)†J∗ = 0); by Assumption 1(d), we have ( u∗

> n

)†h∗

> t

̸ = 0. It follows from Wedin’s theorem [SS90, Theorem 4.1] that σn, u n, v n depend continuously on matrix hz (as long as singular value σn has multiplicity 1). Therefore, there exists a neighborhood of x∗ in which points x ∈ V satisfy σ1 ≥ . . . ≥ σn−1 ≥ Θ(1), ∥ht∥ = Θ(1) and |u†

> n

ht| = Θ(1). For points x = ( z, t )̸ = x∗ in this neighborhood we have ˙z = −h−1 

> z

ht = −(V Σ−1U †)ht

= −

> n

X

> i=1

σ−1 

> i

viu† 

> i

ht

= −

> n−1

X

> i=1

σ−1 

> i

viu† 

> i

ht

!

−



σ−1 

> n

vnu†

> n

ht



(5) The norm of the first term in (5) is bounded by a constant in a neighborhood of 0, while the norm of the second term goes to infinity as t → 0 (since σn goes to zero, ∥vn∥ = 1 and |u†

> n

ht| = Θ(1)). This implies that lim t→0 ∥ ˙z∥ = + ∞. Since ∥ ˙z(t)∥ = Θ( |t|k1/c −1), we must have k1/c < 1. From (5) we get 

σ−1 

> n

vnu†

> n

ht = − ˙z −

> n−1

X

> i=1

σ−1 

> i

viu† 

> i

ht

Taking norms gives 

σ−1 

> n

· ∥ vn∥ · | u†

> n

ht| ≤ ∥ ˙z∥ +

> n−1

X

> i=1

σ−1 

> i

∥vi∥ · ∥ ui∥ · ∥ ht∥

We have ∥vi∥ = ∥ui∥ = 1, and so ∥h−1 

> z

∥ = σ−1 

> n

≤ 1Θ(1) (O(|t|k1/c −1) + O(1)) = O(|t|k1/c −1). Thus, any constant δ > 1 − k1/c will satisfy the claim of the lemma, so we can indeed choose δ < 1. Let us fix constants δ > 0, ∆ < 1, Λ > −1 such that for each formal series z(·) ∈ Π we have 

δ > δ z(·), ∆ > 1 − kz(·)1 

> cz(·)

, Λ > kz(·)1 

> cz(·)

− 1. (Here the superscript z(·) denotes the value associated with formal series z(·), and value δz(·) comes from Lemma 6.) By the lemma, the following holds for all 

x = ( z, t ) ∈ V in some punctured neighborhood of x∗:

∥h−1 

> z

(x)∥ ≤ |t|−δ (6a) 

∥ ˙z∥ ≤ |t|−∆ (6b) 

∥¨z∥ ≤ |t|−∆−1 (6c) Note, if κ = 1 then we can have δ < 1 and Λ < 0. We define γmin = δ + ∆−12 . Note, if κ = 1 then γmin < 1. We thus assume from now on that 

γ > δ + ∆−12 (7) 

Lemma 7. For any constant α > 0 there exist another constant β > 0 and neighborhood Ω of 

x∗ satisfying the following: if x = ( z, t ) ∈ Ω and ∥h(x)∥ ≤ | t|β then there exists Puiseux series 

¯z(·) ∈ Π such that ∥z − ¯z∥ ≤ | t|α and ∥ ˙z − ˙¯z∥ ≤ | t|α−2δ where ¯z = ¯ z(t).Proof. We can assume w.l.o.g. that α ≥ δ and α + δ > 1 (by increasing α, if necessary; this will not affect the claim). By the classical Lojasiewicz inequality [Loj59], there exist constants C > 0, 

θ > 0 such that every x in some neighborhood of x∗ satisfies 

dist (x, V) ≤ C∥h(x)∥θ (8) 8We will show the lemma for any value β satisfying 

β > α+δθ (9) Consider x = ( z, t ) with ∥h(x)∥ ≤ | t|β in some neighborhood of x∗. By (8), there exists 

x′ = ( z′, t ′) ∈ V with ∥x − x′∥ ≤ C∥h(x)∥θ ≤ C(|t|β )θ = C|t|¯α+δ where ¯ α > α is a constant. By shrinking the neighborhood if necessary, we can assume that C|t|¯α+δ < 12 |t|. We have z′ = ¯ z(t′)for some Puiseux series ¯ z(·) ∈ Π. By the mean value theorem, ∥¯z(t) − ¯z(t′)∥ ≤ ∥ ˙¯z(τ )∥ · | t − t′| for some τ ∈ [t, t ′]. Since |t − t′| ≤ ∥ x − x′∥ ≤ C|t|¯α+1 < 12 |t|, we must have |τ | = Θ( |t|). Denoting ¯z = ¯ z(t), we get 

∥z − ¯z∥ ≤ ∥ z − z′∥ + ∥z′ − ¯z∥ ≤ ∥ x − x′∥ + ∥ ˙¯z(τ )∥ · | t − t′| ≤ ∥ x − x′∥ · (1 + ∥ ˙¯z(τ )∥)since max {∥ z −z′∥, |t−t′|} ≤ ∥ x−x′∥. Using eq. (6a), we get ∥ ˙¯z(τ )∥ = ∥hz (¯ z(τ ), τ )−1ht(¯ z(τ ), τ )∥ ≤ ∥hz (¯ z(τ ), τ )−1∥ · ∥ ht(¯ z(τ ), τ )∥ ≤ | τ |−δ · O(1) = O(|t|−δ). This yields 

∥z − ¯z∥ ≤ C|t|¯α+δ · (1 + O(|t|−δ)) = O(|t|¯α)Since ¯ α > α , taking a sufficiently small neighborhood will ensure that the last expression is at most |t|α.To prove the bound on ∥ ˙z − ˙¯z∥, we will use the following fact: 

• Suppose that A, B ∈ Cn×n, a, b ∈ Cn×1, A is invertible and ∥A−1∥ ∥ A − B∥ < 1. Then 

∥A−1a − B−1b∥ ≤ ∥ A−1∥ ∥ a − b∥ + ∥A−1∥2 ∥A − B∥

1 − ∥ A−1∥ ∥ A − B∥ ∥b∥ (10) 

Indeed, the assumption implies that B is invertible and ∥B−1∥ ≤ ∥A−1∥ 

> 1−∥ A−1∥ ∥ A−B∥

. We have 

A−1a − B−1b = A−1(a − b) + A−1(B − A)B−1b and hence ∥A−1a − B−1b∥ ≤ ∥ A−1∥ ∥ a − b∥ +

∥A−1∥ ∥ B − A∥ ∥ B−1∥ ∥ b∥, which yields (10) .

Let us plug A = hz (¯ z, t ), a = ht(¯ z, t ), B = hz (z, t ), b = ht(z, t ). Since h is analytic, we have 

∥A − B∥ ≤ O(∥z − ¯z∥) ≤ O(|t|¯α), ∥a − b∥ ≤ O(∥z − ¯z∥) ≤ O(|t|¯α) and ∥b∥ ≤ O(1). By eq. (6a), 

∥A−1∥ ≤ | t|−δ. Plugging this into (10) gives 

∥ ˙z − ˙¯z∥ = ∥A−1a − B−1b∥ ≤ | t|−δ · O(|t|¯α) + |t|−2δ · O(|t|¯α)1 − | t|−δ · O(|t|¯α) · O(1) ≤ O(|t|¯α−2δ)since ¯ α > δ . Since ¯ α > α , taking a sufficiently small neighborhood will ensure that the last expression is at most |t|α−2δ.We now proceed with the proof of Theorem 4. Fix α that satisfies 

α > max 

n

Λ + γ + max {2δ, 1} , k2 

> c

, 2δ − 1 + k2

> c

o

(11) for coefficients c, k 1, k 2 of all Puiseux series z(·) ∈ Π. Let β > 0 be the constant specified in Lemma 7 for this value of α. Note that this value can be chosen so that β = Θ( γ) (see eq. (9)). Assume that the input point x = ( z, t ) in the appropriate neighborhood of x∗ satisfies ∥h(x)∥ ≤ | t|β .By Lemma 7, there exists Puiseux series ¯ z(·) ∈ Π such that ¯ z = ¯ z(t) satisfies 

∥z − ¯z∥ ≤ |t|α (12a) 

∥ ˙z − ˙¯z∥ ≤ |t|α−2δ (12b) Let us denote ¯ z1 = ¯ z(t1). Our next goal will be to show that point z1 constructed by the algorithm is close to ¯ z1, and gives the correct value of the ratio k1/c . Recall that ¯ z1 is obtained via the Newton’s method for system F (z) = 0 where F (z) = h(z, t 1). 9Lemma 8. (a) The starting point z0 = z + ˙ z(t1 − t) satisfies ∥z0 − ¯z1∥ ≤ O(|t|2γ−∆+1 ).

(b) The preconditions of Theorem 3 hold with L = Θ( |t|−δ), C = 49L .

(c) Newton’s method terminates after O(log(1 + β)) iterations. 

(d) It produces point z1 satisfying ∥z1 − ¯z1∥ ≤ | t1|α and ∥ ˙z1 − ˙¯z1∥ ≤ | t1|α−2δ.

(e) There exists a neighborhood Ω of x∗ with the following property: if x ∈ Ω then the method produces the correct ratio k1/c for the Puiseux series ¯z(·).Proof. Part (a) First, we will show the claim assuming that n = 1. We can write ¯z1 = ¯ z + ( t1 − t) ˙ ¯z + 12 (t1 − t)2 ¨¯z(τ ) ⇒ z0 − ¯z1 = ( t1 − t)( ˙ z − ˙¯z) − 12 (t1 − t)2 ¨¯z(τ )for some τ ∈ [t1, t ]. We have |t1 − t| = |t|γ+1 and |¨¯z(τ )| ≤ | τ |−∆−1, and so 

|z0 − ¯z1| ≤ | t|γ+1 · | t|α−2δ + 12 |t|2( γ+1) · | τ |−∆−1 = O(|t|2γ−∆+1 )since γ + 1 + α − 2δ ≥ 2γ − ∆ + 1. If n > 1 then |(z0 − ¯z1)i| ≤ O(|t|2γ−∆+1 ) for each coordinate 

i ∈ [n] (by the argument above applied to the i-th coordinate of vectors), and hence ∥z0 − ¯z1∥ ≤ 

O(|t|2γ−∆+1 ). 

Part (b) Since Fz (z) = hz (z, t 1) is analytic in a neighborhood of z∗, we have ∥Fz (z′)−Fz (z′′ )∥ ≤ 

O(∥z′ − z′′ ∥) when z′, z ′′ are in a certain neighborhood of z∗. In particular, we have ∥Fz (z0) −

Fz (¯ z1)∥ ≤ O(∥z0 − ¯z1∥) ≤ O(|t|2γ−∆+1 ). Also, ∥F −1 

> z

(¯ z1)∥ ≤ O(|t1|−δ) = O(|t|−δ) by eq. (6a). Therefore, ∥Fz (z0) − Fz (¯ z1)∥ ∥ F −1 

> z

(¯ z1)∥ ≤ 12 if |t| is sufficiently small (since 2 γ − ∆ + 1 − δ > 0 by eq. (7)). This implies that 

∥F −1 

> z

(z0)∥ ≤ ∥F −1 

> z

(¯ z1)∥

1 − ∥ Fz (z0) − Fz (¯ z1)∥ ∥ F −1 

> z

(¯ z1)∥ ≤ O(|t|−δ)We conclude that ∥F −1 

> z

(z0)( Fz (z′) − Fz (z′′ )) ∥ ≤ O(|t|−δ) · O(∥z′ − z′′ ∥) when z′, z ′′ are in a certain neighborhood of z∗, and hence the first two preconditions of Theorem 3 hold with L = Θ( |t|−δ). We have ∥F (z0)∥ = ∥F (z0) − F (¯ z1)∥ ≤ O(∥z0 − ¯z1∥) ≤ O(|t|2γ−∆+1 ), and so the third precondition holds with C ≥ C0 = O(|t|−δ) · O(|t|2γ−∆+1 ). We obtain C0L = O(|t|2γ−2δ−∆+1 ) = O(|t|ε) where 

ε > 0 by the choice of γ. Therefore, we can indeed set C = 49L if |t| is sufficiently small. 

Parts (c,d) Theorem 3 yields that equation F (z) = 0 has a unique solution in the ball B(z0, r )for any r ∈ [r−, r +] = [ 23L , 43L ]. We have F (¯ z1) = 0 and ∥z0 − ¯z1∥ ≤ O(|t|2γ−∆+1 ) < r − if |t| is sufficiently small (since 2 γ − ∆ + 1 > δ by eq. (7)), so this unique solution must be ¯ z1.Let us denote wk = z(k) − ¯z1 where z(k) is the iterate at step k (with z(0) = z0). Theorem 3 gives that ∥wk+1 ∥ ≤ ¯C · | t|−δ∥wk∥2. We have ∥w0∥ ≤ O(|t|λ) where λ = 2 γ − ∆ + 1 > 2δ, therefore 

∥wk∥ ≤ | t|O(δ2k ). Since h is an analytic function, we have ∥h(z(k), t 1)∥ = ∥h(z(k), t 1) − h(¯ z1, t 1)∥ ≤ 

O(∥z(k) − ¯z1∥) ≤ | t|O(δ2k ). We conclude that for any fixed β we will have ∥h(z(k), t 1)∥ ≤ | t1|β after 

k = O(log(1 + β)) iterations. By Lemma 7, there exists ¯ ¯z(·) ∈ Π with ∥z1 − ¯¯z1∥ ≤ | t1|α and ∥ ˙z1 − ˙¯¯z1∥ ≤ | t1|α−2δ where ¯¯z = ¯ ¯z(t1). Since α > δ , we can assume that |t1|α < r − = Θ( |t|δ). This implies that ∥z0 − ¯¯z1∥ ≤ ∥z0 − z1∥ + ∥z1 − ¯¯z1∥ < r − + r− = r+. We have F (¯ z1) = F (¯ ¯z1) = 0 and ¯ z1, ¯¯z1 ∈ B(z0, r +), therefore ¯z1 = ¯ ¯z1 and hence ˙ ¯z1 = ˙¯¯z1.

Part (e) Denote A = ∥t ˙z − t1 ˙z1∥, B = ∥z − z1∥ and R = k1 

> c

. Clearly, there exists constant 

ε ∈ (0 , 12 R) such that R is the only rational number p/q with integers p ∈ [1 , k max 1 ], q ≥ 1 satisfying 

|R − p/q | < ε . We will show that |A/B − R| < ε when x is in some neighborhood of x∗; this will prove the claim. Denote ¯A = ∥t ˙¯z − t1 ˙¯z1∥ and ¯B = ∥¯z − ¯z1∥. By Lemma 5 we can choose a neighborhood such that | ¯A/ ¯B − R| < 12 ε. It now suffices to show that |A/B − ¯A/ ¯B| < 12 ε in some neighborhood of x∗.By Lemma 6 there exists i ∈ [n] such that | ˙¯zi(τ )| = Θ( |τ |k1/c −1) ≥ Θ( |τ |Λ). By the mean value theorem, ¯B ≥ | ¯zi(t) − ¯zi(t1)| = | ˙¯zi(τ )| · | t − t1| for some τ ∈ [t, t 1]. Therefore, ¯B ≥ Θ( |t|Λ) · | t|γ+1 .10 We have |B − ¯B| ≤ ∥ z − ¯z∥+∥z1 − ¯z1∥ ≤ | t|α +|t1|α = O(|t|α) = o( ¯B) since α > Λ+ γ +1. Similarly, ¯A = ( R + o(1)) ¯B = Θ( |t|Λ+ γ+1 ) and |A − ¯A| ≤ t∥ ˙z − ˙¯z∥ + t1∥ ˙z1 − ˙¯z1∥ ≤ | t|1+ α−2δ + |t1|1+ α−2δ =

O(|t|1+ α−2δ) = o( ¯A) since 1 + α − 2δ > Λ + γ + 1. We showed that A/B = ( ¯A(1 + o(1)) /( ¯B(1 + o(1)) = ( ¯A/ ¯B)(1 + o(1)). The claim follows. We are now ready to prove Theorem 4. We have ¯ z − z∗ = ak1 tk1/c (1 + o(1)), and hence 

∥¯z − z∗∥ = Θ( |t|k1/c ). Since ∥z − ¯z∥ = O(|t|α) and α > k 1/c , we also have ∥z − z∗∥ = Θ( |t|k1/c ). By Lemma 5, the “ideal predictor” ˆ ¯z = ¯ z − ck1

˙¯zt satisfies ∥ˆ¯z − z∗∥ = O(∥¯z − z∗∥k2/k 1 ) = O(|t|k2/c ). By the previous lemma, we can assume that the ratio k1/c produced in step 3 is the correct ratio for the Puiseux series ¯ z(·). Recall that our predictor is given by ˆ z = z − ck1 ˙zt . We then have ˆ¯z = ¯ z − ck1

˙¯zt , and so 

∥ˆz − ˆ¯z∥ ≤ ∥ z − ¯z∥ + ck1 ∥ ˙z − ˙¯z∥ t ≤ | t|α + ck1 |t|α−2δ |t|∥ˆz − z∗∥ ≤ ∥ ˆ¯z − z∗∥ + ∥ˆz − ˆ¯z∥ ≤ O(|t|k2/c ) + O(|t|α) + O(|t|α−2δ+1 ) = O(|t|k2/c )since α > k 2/c and α − 2δ + 1 > k 2/c by the choice of α in eq. (11). The RHS of the last expression is at most O(∥z − z∗∥k2/k 1 ). 

# 4 Corrector 

Let us now assume that we have initial point x◦ = ( z◦, t ◦) and predictor ˆ x = (ˆ z, ˆt) where ˆ z

approximates z(ˆ t) for some Puiseux series z(·) ∈ Π. The predictor step moved us away from the homotopy h(·); the goal of the corrector is go back to this homotopy. We will consider separately cases κ = 1 and κ ≥ 2. We will use ˆ t = 0 in the former case and ˆt̸ = 0 in the latter. 

4.1 Corank κ = 1 : pseudo-arc length corrector 

Recall that in the classical approach we are effectively solving the system 

(

h(x) = 0 

t − ˆt = 0 (13) over variables x = ( z, t ). Its Jacobian is 

hz ht

0 1



(14) Note that if ˆ t = 0 then x∗ is a root of (13), and the Jacobian is singular at this root (since the columns of hz are linearly dependent). This fact prevents us from setting ˆ t = 0, since then the Newton’s method may not converge. We propose to do the following instead. Below β is the parameter used in Theorem 4. 1. Set q = [ ˙ z†◦ 1]   

> ∥[ ˙ z†◦1] ∥

. Note that q† is in the null space of hx(x◦), since hx(x◦)·q† = [ hz (x◦) ht(x◦)] ·

−h−1 

> z

(x◦)ht(x◦)1



= −ht(x◦) + ht(x◦) = 0. 2. Replace system (13) with 

h[ˆ x]( x) def 

=

(

h(x)

q · (x − ˆx) = 0 (15) 11 3. Apply Newton’s method to solve equation h[ˆ x]( x) = 0 starting with x0 = ˆ x, generating a sequence of points x1, x 2, . . . . Stop once we get a point x = xK = ( z, t ) with ∥h(x)∥ ≤ | t|β .

Lemma 9. There exists a neighborhood Ω of x∗ and constant λ > 0 such that ∥q∥ = 1 and 

∥(Dxh[ˆ x]( x)) −1∥ ≤ λ for any x◦, x ∈ Ω.Proof. The Jacobian of h[ˆ x]( ·) is given by 

Dxh[ˆ x] = 

hz ht

q



=

hz (x) ht(x)

q(x◦)



(16) If ˆ x = x◦ = x∗ then matrix Dxh[x∗] = 

h∗ 

> z

h∗

> t

q∗



is non-singular by Assumption 1, and matrix [h∗ 

> z

h∗ 

> t

] has full rank. Singular vectors of a matrix corresponding to singular values of multiplic-ity 1 depend continuously on the matrix (by the Wedin’s theorem which we used in the proof of Lemma 6). Therefore, matrix 

hz ht

q



depends continuously on (ˆ x, x ◦) (since q is the singular vector of hx = [ hz ht] corresponding to singular value 0 of multiplicity 1). The claim follows. Note that the guarantee of Lemma 9 can be achieved by many other choices of q, e.g. if q is chosen randomly. For the result below we assume that vector q is chosen to satisfy the properties in Lemma 9 but is not necessarily in the null space of hx(x◦). 

Theorem 10. Let Ω be the neighborhood of x∗ from Lemma 9 (with constant λ ≥ 0). There exist neighborhoods Ω− ⊆ Ω+ ⊆ Ω of x∗ and constant βmin > 0 with the following property: if ˆx ∈ Ω− then equation h[ˆ x]( x) = 0 has a unique solution ¯x = (¯ z, ¯t) ∈ Ω+, and it satisfies 

∥¯x − x∗∥ ≤ λ∥ˆx − x∗∥. Furthermore, the sequence of points x0 = ˆ x, x 1, x 2, . . . generated by the Newton’s method satisfies the following: 

(a) ∥xk − x∗∥ ≤ O(∥ˆx − x∗∥) for each k ≥ 1.

(b) If β > β min , k ≥ ℓ def 

= ⌈2 log 2(1 + β)⌉, xk = x = ( z, t ) and ∥h(x)∥ > |t|β then ∥x − x∗∥ ≤ ∥ˆx − x∗∥O(2 k−ℓ).Proof. We will apply Theorem 3 for function F (x) = h[ˆ x]( x). By assumption, we have ∥F −1 

> x

(x)∥ ≤ 

λ for all x ∈ Ω. For any x, y ∈ Ω we have 

∥F ′(x) − F ′(y)∥ =

hx(x) − hx(y)0



= O(∥x − y∥)since hx = [ hz ht] is analytic in Ω. Thus, the first two preconditions of Theorem 3 hold if L ≥ L0 for some constant L0 > 0. We have ∥F (ˆ x)∥ =

h(ˆ x)0



= ∥h(ˆ x)∥ = ∥h(ˆ x)−h(x∗)∥ ≤ C0 ·∥ ˆx−x∗∥ for some constant C0, when ˆ x ∈ Ω (since h is analytic on Ω). Thus, the third precondition holds with any C ≥ λC 0. Let us choose value L ≥ max {L0, 94 λC 0} so that B(x∗, 43L ) ⊆ Ω, and set C = 4 /(9 L). These values satisfy conditions of the theorem, and hence equation F (x) = 0 has a unique solution ¯x ∈ B(ˆ x, r ) for any r ∈ [r−, r +] = [ 23L , 43L ]. Let us denote it as φ(ˆ x). Define Ω − = B(x∗, r −) and Ω+ = B(x∗, r +), then for each ˆ x ∈ Ω− we have ∥φ(ˆ x)−x∗∥ ≤ ∥ φ(ˆ x)− ˆx∥+∥ˆx−x∗∥ ≤ r− +r− = r+

and hence φ(ˆ x) ∈ Ω+.Function φ(·) must be continuous at each ˆ x ∈ Ω−. Indeed, if x is an accumulation point of φ(u)as u → ˆx then h[ˆ x]( x) = 0 by continuity, and thus is uniquely determined by ˆ x since h[ˆ x]( x) = 0 has a unique solution in B(ˆ x, r +). The uniqueness of the accumulation point implies the claim. 12 Differentiating the equation h[ˆ x]( φ(ˆ x)) = 0 with respect to ˆ x gives 

Dˆxφ(ˆ x) = −(Dxh[ˆ x]( x)) −1Dˆxh[ˆ x]( x)= −(Dxh[ˆ x]( x)) −1

 0

−q



,

and hence 

∥Duφ(u)∥ ≤ ∥ (Dxh[ˆ x]( x)) −1∥ · 

 0

−q



≤ λ · 1.

This implies that ∥¯x − x∗∥ = ∥φ(ˆ x) − φ(x∗)∥ ≤ λ · ∥ ˆx − x∗∥.Next, we show properties (a)-(b) of the sequence {xk}k=0 ,1,... generated by the Newton’s method. We will denote ∆ = ∥ˆx − x∗∥. Theorem 3 gives ∥xk+1 − ¯x∥ ≤ 3L 

> 2

∥xk − ¯x∥2, with 

∥x0 − ¯x∥ ≤ λ∆. By shrinking Ω −, if necessary, we can make sure for some constant α > 0 we have 

∥xk − ¯x∥ ≤ min { ∆α2k

, ∥ˆx − ¯x∥ } ∀k ≥ 1

Property (a) For any k ≥ 1 we can write ∥xk − x∗∥ ≤ ∥ xk − ¯x∥ + ∥ˆx − ¯x∥ ≤ 2λ∥ˆx − x∗∥.

Property (b) Suppose that ∥xk−x∗∥ > 2∆ α2k−ℓ

for k ≥ ℓ. Then ∥¯x−x∗∥ ≥ ∥ xk−x∗∥−∥ xk−¯x∥ >

2∆ α2k−ℓ

− ∆α2k

≥ ∆α2k−ℓ

. We have h(¯ x) = 0, and hence ¯ z = ¯ z(¯ t) for some Puiseux series ¯ z(·) ∈ Π. This implies that ∥¯z − z∗∥ ≤ O(|¯t|k1/c ) where k1, c are the coefficients for ¯ z(·). We can thus write ∆α2k−ℓ

≤ ∥ ¯x − x∗∥ ≤ ∥ ¯z − z∗∥ + |¯t − 0| < O (|¯t|k1/c ) + |¯t| < |¯t|δ

for some constant δ > 0. This implies that ∥xk − ¯x∥ ≤ ∆α2k

≤ | ¯t|δ2ℓ

. Since ℓ = ⌈2 log 2(1 + β)⌉ ≥ 

log 2 βmin + log 2 β, we have ∥xk − ¯x∥ ≤ | t|δβ min β . By choosing βmin sufficiently large, we can ensure that ∥xk − ¯x∥ ≤ | 12 ¯t|β+ε for some constant ε > 0 and ∥xk − ¯x∥ ≤ | 12 ¯t|. The latter condition implies that point xk = x = ( z, t ) satisfies |t−¯t| ≤ | 12 ¯t| and hence |t| ≥ | 12 ¯t|. This yields ∥xk −¯x∥ ≤ | 12 ¯t|β+ε ≤|t|β+ε. It remains to observe that ∥h(xk)∥ = ∥h(xk) − h(¯ x)∥ ≤ O(∥xk − ¯x∥) ≤ O(|t|β+ε) < |t|β if neighborhood Ω is sufficiently small. We can finally prove Theorem 1. We use the following algorithm. Given point x◦, we compute predictor ˆ x as described in Section 3, then construct system (15) and run Newton’s method, obtaining sequence x0 = ˆ x, x 1, x 2, . . . . We stop once we get a point xk = ( z, t ) with ∥h(x)∥ ≤ | t|β .If k < ℓ = ⌈2 log 2(1 + β)⌉ then we return xk, otherwise we return the sequence ( xℓ, x ℓ+1 , . . . , x k). By combining Theorems 4 and 10 we conclude that this algorithm has the properties stated in Theorem 1. 

Connection to the pseudo-arclength method The corrector described above can be related to the pseudo-arclength method [Wem71; Rik72; Kel77]. The latter constructs the following system over variables x = ( z, t ): (

h(x) 

> dz ds

(z − z◦) + dt ds (t − t◦) − ∆s = 0 (17) where parameter s represents the Euclidean length along the curve ( z(t), t ), and quantities dz ds , dt ds ,∆s are fixed. The first two quantities are computed from equations dz ds = ˙ z· dt ds and dz ds 

> 2

+ dt ds  

> 2

= 1. Thus, the last equation in (17) can also be equivalently written as q ·(x− ˆx) = 0 for some ˆ x ∈ Cn+1 ,where q = [ ˙ z◦ 1], as in (15). The difference is that we set ˆ x explicitly via an endgame-aware linear predictor, while pseudo-arclength methods control parameter ∆ s instead. 13 4.2 Corank κ ≥ 2: lifted pseudo-arc length corrector 

In this case we will introduce κ − 1 new auxiliary variables ξ ∈ Cκ−1. Let us denote y = ( x, ξ ) = (z, t, ξ ), ˆ y = (ˆ x, 0) = (ˆ z, ˆt, 0). We will solve the system 

h[ˆ x]( y) def 

=

(

h(x) + P · ξQ · (y − ˆy) = 0 (18) where P ∈ Cn×(κ−1) , Q ∈ Cκ×(n+κ) are matrices computed from (ˆ x, x ◦). The Jacobian of h[ˆ x] is 

Dyh[ˆ x] = 

hz ht PQ



(19) By assumption, we have rank ([ h∗ 

> z

h∗ 

> t

]) = n − κ + 1. This means that we can find matrices P, Q 

such that ∥(Dyh[ˆ x]( y)) −1∥ ≤ O(1) assuming that (ˆ x, y ) lies in a certain neighborhood of ( x∗, y ∗). Using the same arguments as in the previous section, one can then show that system (18) has a unique solution ¯ y in a neighborhood of y∗, this solution satisfies ∥¯y − y∗∥ ≤ O(∥ˆx − x∗∥), and it can be efficiently computed with any desired accuracy using the Newton’s method. Unfortunately, this does not lead to any guarantees on the convergence rate, so we leave this claim without proof. We denote y = ( v, t, ξ ) to be the output of this process. Experimentally, we observed that usually ∥f (v)∥ ≪ ∥ f (z◦)∥ (and also ∥f (v)∥ ≪ ∥ f (ˆ z)∥) where f (z) = h(z, 0) is the system that we are trying to solve. In fact, very often we observed a superlinear convergence rate on ∥f (·)∥, i.e. 

∥f (v)∥ ≤ ∥ f (z◦)∥α for some constant α > 1. The challenge here is that the obtained point does not lie on the homotopy h, so we would not be able to continue further with this homotopy. We propose to replace h(z, t ) with the new homotopy hv(z, t ) defined as follows: 

hv(z, t ) = f (z) − tf (v)This is known as the “Newton homotopy” (see, e.g.,[MSW92b]). Note that point ( v, 1) lies on this homotopy. Furthermore, by tracking this homotopy starting from this point we can expect to arrive at z∗.

Lemma 11. Call v good if there exists a continuous curve φv : (0 , 1] → Cn with φv(1) = v and 

hv(φv(t), t ) = 0 for all t ∈ (0 , 1] . There exists a neighborhood Ω of x∗ such that for any good point 

v ∈ Ω there holds lim t→0 φv(t) = z∗.Proof. Since z∗ is an isolated root of f , there exists closed ball B around z∗ such that z∗ is the only solution of f (z) = 0 over z ∈ B. Since its boundary ∂B is compact and f is continuous, there exists 

α = min z∈∂B ∥f (z)∥ > 0. Define Ω = {z ∈ B : ∥f (z)∥ < α/ 2}; clearly, this is a neighborhood of z∗. We claim that φv(t) ∈ B for any v ∈ Ω and t ∈ (0 , 1]. Indeed, we have ∥f (v)∥ < α/ 2since v ∈ Ω. Also, f (φv(t)) − tf (v) = 0 and hence ∥f (φv(t)) ∥ = ∥tf (v)∥ ≤ ∥ f (v)∥ < α/ 2 for any 

t ∈ (0 , 1]. Suppose there exists t ∈ (0 , 1] with φv(t) /∈ B, then by continuity there exists t′ ∈ (t, 1) with φv(t′) ∈ ∂B . But then ∥f (φv(t′)) ∥ ≥ α and ∥f (φv(t′)) ∥ < α/ 2 - a contradiction. We showed that φv((0 , 1]) ⊆ B. Since B is compact, curve φv must have at least one accumu-lation point as t → 0. Let ¯ z ∈ B be such point. Continuity of functions hv and φv implies that 

f (¯ z) = hv(¯ z, 0) = 0. Since z∗ is the only root of f in B, we must have ¯ z = z∗. This means that curve φv has exactly one accumulation point as t → 0, and hence lim t→0 φv(t) = z∗.Unfortunately, tracking homotopy hv from t = 1 can be very difficult, since we may not be in the endgame zone yet. We illustrate this phenomenon on the following example. 

Example 1. Consider the system 

f (z1, z 2) def 

=

(

z1 − z2 − z22

z1 − z2 + z22

= 0 (20) 14 It has unique solution z = (0 , 0) of corank κ = 1 . Now fix v ∈ C2, and define homotopy hv(z, t ) = 

f (z) − tf (v). Solving equation hv(z, t ) = 0 gives 

(

z1 = v2 · t1/2 + ( v1 − v2) · tz2 = v2 · t1/2

We can define the “endgame zone” as those values of t for which term v2 · t1/2 of the Puiseux series dominates term (v1 −v2)·t; in that case the linear predictor that estimates only the first term would give a good approximation. Thus, the endgame regime is given by the condition |t|1/2 ≪ | v2 

> v1−v2

|.Let us fix r > 0, and consider two processes for generating v.

• sample v ∈ C2 uniformly at random subject to ∥v∥ = r. Then E[| v2 

> v1−v2

|] = Θ(1) , and so value t = 1 is not in the endgame zone. 

• sample α ∈ C2 uniformly at random subject to ∥α∥ = r, obtain v by solving f (v) = α. Then  

> v2
> v1−v2

=

√2( α2−α1) 

> α1+α2

and E[| v2 

> v1−v2

|] = Θ( 1√r ). Thus, t = 1 will be in the endgame zone if r is sufficiently small. One might ask whether the “ γ-trick” could help. This is a standard approach in NAG to ensure genericity of h [SW05, Chapter 7]. The idea is to choose a random value γ ∈ C with |γ| = 1 and then define homotopy 

h(z, t ) = (1 − t)f (z) − γt (f (v) − f (z)) 

Note that we still have h(v, 1) = 0 and h(z, 0) = f (z). Solving equation h(z, t ) = 0 gives 

z2 = v2 ·

r γt 

1 − t + γt = v2

√γ ·



t1/2 + 1 − γ

2 · t3/2 + . . . 



If γ̸ = 1 then the first two terms are of the same order when t = 1 , and so t = 1 is not in the endgame zone for any choice of v.

For systems with larger corank ( κ ≥ 2), the predictor step within the LAL (Lifted ArcLength Endgame) method is constructed analogously to the κ = 1 case. However, we have empirically observed that projecting the path directly to the target root (i.e., setting ˆ t = 0) often produces numerical instabilities in subsequent corrector iterations. Such a direct jump can degrade both the quality of the newly predicted point and the reliability of the ongoing fractional exponent estimations. To mitigate this instability, we restrict the parameter jump in the predictor phase by introduc-ing an adaptive shrinking-factor exponent, η, which governs the step-size multiplier ρ. Specifically, the predicted parameter value is defined as ˆ t = ρ · t◦, where the step size is explicitly formulated as ρ = |t◦|η. To systematically control the progression toward the singularity, the exponent is up-dated iteratively via the rule η ← ηα for some prescribed constant α > 0. We refer to Section 6.1 for further details. 

# 5 Higher-order predictors 

In this section we will consider the problem of estimating coefficients of the Puiseux series 

z(t) = 

> ∞

X

> j=0

aj tj/c = a0 + ak1 tk1/c + ak2 tk2/c + . . . (21) using several input points ( t1, z 1), (t2, z 2), . . . . In the analysis we will assume for simplicity that the points lie exactly on the curve, i.e. zi = z(ti). Note that for each point zi we can also compute the derivative ˙ zi via eq. (3). 15 A standard higher-order predictor used in the classical power-series endgame [SW05; Bat+13] is the cubic predictor constructed via Hermite interpolation from points z1, ˙z1, z 2, ˙z2. Assuming that z(t) = a0 + ak1 tk1/c + ak2 tk2/c + ak3 tk3/c and t2 = λt 1, we obtain the following system of equations: 

1 1 1 10 k1

> ck2
> ck3
> c

1 λk1/c λk2/c λk3/c 

0 k1 

> c

λk1/c k2 

> c

λk2/c k3 

> c

λk3/c 



a0

a1tk1/c 

> 1

a2tk2/c 

> 1

a3tk3/c 

> 1



=



z1

t1 ˙z1

z2

t2 ˙z2



. (22) One can then solve for a0, a 1, a 2, a 3, assuming that the ratios k1/c , k2/c , k3/c are known. To our knowledge, previous works only addressed the problem of estimating k1/c . As a result, the standard cubic predictor is applied assuming that ( k1, k 2, k 3) = (1 , 2, 3). If the actual indices differ from (1 , 2, 3) then the predictor would give a poor approximation, and the method would fall back to a linear predictor that requires only the ratio k1/c . Note that the fractional exponents of a branch do not always form a dense sequence 1 /c, 2/c, 3/c, . . . ; rather, they are restricted to a value semigroup S ⊆ N/c . Gaps in this semigroup represent fractional powers that are topologically prohibited from appearing in the expansion due to the degeneracy of the tangent cone [Wal04, Chapter 4];[GLS07; Zar32]. In Section 5.1 we present a method for estimating higher-order ratios. This is achieved by extending the Geometric Sequence Sampling approach [BHS11; SW05] originally proposed for estimating k1/c . In particular, we show how to estimate k2/c and k3/c using 5 points z1, . . . , z 5

and their derivatives. This enables a cubic predictor for general Puiseux series. Recall that the Hermite interpolation uses 2 points to estimate coefficients a0, a 1, a 2, a 3. In our case we have 5 points available at no additional cost, so it is natural to ask whether these additional points can be used to improve the estimation of coefficients. In Section 5.2 we propose a new scheme for this. The new scheme requires inverting a matrix whose condition number stays constant as 

λ → 1. In contrast, the Hermite interpolation involves inverting a matrix whose condition number grows as λ → 1 (and thus one would need to use more digits during this computation). In both parts we will use higher-order derivatives, in particular the second derivative ¨ z. In principle, it could be computed by twice differentiating the homotopy equation h(z(t), t ) = 0. However, this would be a very expensive operation. Instead, we approximate ¨ z from first derivatives using finite differences, as described later in Section 5.3. 

5.1 Estimating ki/c 

Accurately estimating the ratios ki/c of the first leading terms is a critical phase of the singular endgame. Below we discuss several methods for that separating cases i = 1 and i ≥ 2. 

5.1.1 Estimating k1/c 

Below we describe three different methods: two existing ones and one new. We refer to them as 

cSORT , cLOG and cRATIO , respectively. 

Method 1: Trial-and-error ( cSORT ) A standard, albeit heuristic, approach to estimate the winding number c is to track the solution curve z(t) from some initial t1 ∈ (0 , 1] down to a smaller value t2 < t 1 using small steps ∆ t. The cycle number is estimated by identifying the integer c ∈ { 1, . . . , c max } that minimizes the prediction residual ∥z2 − ˆzc(t2)∥, where ˆ zc(t) = 

z1 +c ˙z1t1

 tt1

1/c 

− 1



is the linear predictor at t1 parameterized by the candidate cycle number. 

Method 2: Geometric Sequence Sampling ( cLOG ) An alternative method is to sample the path along a geometric sequence [BHS11; SW05]. The ratio k1/c is estimated using three tracked 16 points z1, z 2, z 3 ∈ Cn evaluated at geometrically decreasing parameter values, e.g., zℓ := z(ρℓt) for some step ratio 0 < ρ < 1. To extract the exponents, the method subtracts the values of two consecutive points in this sequence and projects them onto a randomly chosen generic vector v ∈ CN . Taking the dot product isolates a scalar sequence: 

⟨v, z ℓ − zℓ+1 ⟩ =

*

v, 

> ∞

X

> j=k1

aj tj/c (1 − ρj/c )( ρjℓ/c )

+

=

> ∞

X

> j=k1

vj ρjℓ/c 

where vj := ⟨v, a j ⟩tj/c (1 − ρj/c ) ∈ C. Let ∆ ℓ := ⟨v, z ℓ − zℓ+1 ⟩. Taking the logarithmic difference isolates the leading fractional exponent: log ∆ℓ+1 

∆ℓ

= log 

P∞ 

> j=k1

vj ρj(ℓ+1) /c 

P∞ 

> j=k1

vj ρjℓ/c 

= log ρk1/c vk1 + P 

> j>k 1

vj ρ(j−k1)( ℓ+1) /c 

vk1 + P 

> j>k 1

vj ρ(j−k1)ℓ/c 

≈ k1

c log ρ

where the approximation becomes exact as t → 0 (or as ℓ becomes large), forcing the higher-order terms to vanish. 

Method 3: A ratio method ( cRATIO ) The third method that we consider was already described earlier in Section 3. Let z1, z 2 be two tracked points at parameters t1, t 2. By Lemma 5, we have ∥t2 ˙z2 − t1 ˙z1∥∥z2 − z1∥ ≈ k1

c (23) where the approximation becomes exact as t1 → 0 and t2 ∈ [0 , t 1). 

5.1.2 Extension to i ≥ 2Our extension is based on the following observation: if function z(t) is represented by the Puiseux series (21) with k1/c as the leading fractional exponent then function 

z(1) (t) := z(t) − ck1

t ˙z(t) (24) is represented by a Puiseux series with k2/c as the leading fractional exponent: 

z(1) (t) = z∗ +

> ∞

X

> j=2



1 − kj

k1



akj tkj /c (25) Thus, k2/c can be recovered by applying an existing method for estimating k1/c to the func-tion z(1) (t). This idea can be applied recursively for estimating higher-order ratios. Namely, we will esti-mate ki+1 /c by applying an existing method for estimating k1/c to the function z(i)(t) where we recursively define z(i)(t) =  z(i−1) (1) (t) for i ≥ 2. In particular, for i = 2 we have 

z(2) (t) = z(1) (t) − ck2

t ˙z(1) (t) = 



z(t) − ck1

t ˙z(t)



− ck2

t



˙z(t) − ck1

˙z(t) − ck1

t¨z(t)



= z(t) + 



− ck1

− ck2

+ c2

k1k2



t ˙z(t) + c2

k1k2

t2 ¨z(t) (26) 17 We will use the cLOG method as the basic estimator. Recall that it requires three points 

z(i)(t1), z(i)(t2), z(i)(t3) where t1 = t, t2 = ρt , t3 = ρ2t. To summarize, for estimating k2/c we compute these points via (24) (assuming that k1/c is known), and for estimating k3/c we compute these points via (26) (assuming that k1/c and k2/c are known). Note that in the latter case we need the second derivative ¨ z(t); as mentioned before, we approximate it via finite differences (see Section 5.3). 

Remark 1. We also tested the cRATIO method, but found it to perform worse than cLOG . Note that estimating k2/c via cRATIO requires points z(1) (t1), ˙z(1) (t1), z (1) (t2), ˙z(1) (t2). This in turn requires second derivatives ¨z1, ¨z2, which would need to be approximated via finite differences. In contrast, 

cLOG only needs z1, ˙z1, z 2, ˙z2.

5.2 Estimating coefficients 

In this section we present a method for estimating coefficients of the Puiseux series from two points 

z1 = z(t1) and z2 = z(t2) and their higher-order derivatives. We will assume that ratios ki/c are known, and t2 = λt 1 where λ ∈ (0 , 1). For each integer r ≥ 0 let us define the auxiliary series 

pr(t) := 

> ∞

X

> j=1

 jc

r

aj tj/c .

Note that p0(t) = z(t) − z∗. The result below shows that pr(t) for r ≥ 1 can be expressed via function z(·) and its higher-order derivatives. 

Lemma 12. The following identity holds: 

pr(t) = 

> r

X

> j=1

Srj tj dj

dtj z(t), r ≥ 1 (27) 

where Snk denotes the Stirling numbers of the second kind, defined by the recurrence Sn+1  

> k

= Snk−1 +

k S nk , with initial conditions Sn 

> 1

= 1 , Sn 

> 0

= 0 for n > 0, and Snn = 1 for n ≥ 0. In particular, 

p0(t) = z(t) − z∗ p1(t) = t ˙z(t) p2(t) = t2 ¨z(t) + t ˙z(t)

Proof. The formula is proved by induction on r. The base case r = 1 follows directly from the definitions. For the inductive step, we first observe the relation pr+1 (t) = t ddt pr(t) for r ≥ 1, which follows directly from the structure of the derivatives of the monomials tj/c . Assuming Equation (27) holds for a given r ≥ 1, we apply the product rule to obtain: 

pr+1 (t) = t ddt pr(t) = 

> r

X

> j=1

Srj tj+1 dj+1 

dtj+1 z(t) + 

> r

X

> j=1

jS rj tj dj

dtj z(t)=

> r+1

X

> j=1

 Srj−1 + jS rj

tj dj

dtj z(t)=

> r+1

X

> j=1

Sr+1  

> j

tj dj

dtj z(t).

The final equality follows immediately from the recurrence relation for the Stirling numbers of the second kind. 18 Next, we define quantities 

Pr := pr−1(t1) − pr−1(t2) = 

> ∞

X

> j=1

 jc

r−1

aj

 tj/c  

> 1

− tj/c 

> 2

,

and let P be the column vector P = ( P1, . . . , P ℓ)⊤. We will show how to estimate coefficients 

ak1 , . . . , a kℓ from P . We will need the following definition. 

Definition 13 (Scaled Vandermonde matrix) . Let k1, . . . , k ℓ and c be scalars with c̸ = 0 . The ℓ × ℓ

Vandermonde matrix 

V  k1/c, . . . , k ℓ/c  :=  Vij 



> 1≤i,j ≤ℓ

, Vij := 

 kj

c

i−1

,

is called the scaled Vandermonde matrix associated with the nodes k1, . . . , k ℓ.

It is well known that if the nodes k1, . . . , k ℓ are pairwise distinct, then V is nonsingular (see, e.g., Higham [Hig02, Chapter 22]). In this case, the entries of the inverse matrix V −1 admit the explicit representation (V −1)ij = (−1) ℓ−j cℓ−1

> ℓ

Y 

> m=1
> m̸=i

 ki − km

X

> S⊆{ 1,...,ℓ }\{ i}|S|=ℓ−j

Y

> s∈S

ks

c , 1 ≤ i, j ≤ ℓ. 

This formula follows from the classical expression for the inverse of a Vandermonde matrix in terms of elementary symmetric polynomials. 

Proposition 14. For i ∈ [ℓ] define 

˜aki =

 V −1P 

> i

tki/c  

> 1

− tki/c 

> 2

(28) 

Then 

aki − ˜aki = O



t(kℓ+1 −ki)/c 

> 1



(29) 

Proof. Recall that t2 = λt 1 where λ ∈ (0 , 1). Denote λ = 1 − ε, then td 

> 1

− td 

> 2

= td

> 1

(1 − (1 − ε)d) = Θ( td

> 1

ε) for any fixed d > 0. Let Q be the column vector with components Qi = aki (tki/c  

> 1

− tki/c  

> 2

). We can write (V Q )i =

> ℓ

X

> j=1

 kj

c

i−1

· akj (tkj /c  

> 1

− tkj /c  

> 2

) = Pi + O(tkℓ+1 /c  

> 1

− tkℓ+1 /c  

> 2

) = Pi + O(tkℓ+1 /c  

> 1

ε)

V Q = P + O(tkℓ+1 /c  

> 1

ε)Multiplying this by V −1 on the left gives 

 V −1P  

> i

= aki (tki/c  

> 1

− tki/c  

> 2

) + O(tkℓ+1 /c  

> 1

ε)since the entries of V −1 are constants determined entirely by the fixed values k1/c, . . . , k ℓ/c . Di-viding the last equation by tki/c  

> 1

− tki/c  

> 2

= Θ( tki/c  

> 1

ε) gives 

aki =

 V −1P 

> i

tki/c  

> 1

− tki/c 

> 2

+ O(t(kℓ+1 −ki)/c  

> 1

)19 Final predictor Note that for any t we have 

z(t) = z(t1) + 

> ∞

X

> j=1

akj



tkj /c − tkj /c 

> 1



(30) Based on this observation, we define the ℓ-th order predictor via ˆz(t) = z1 +

> ℓ

X

> j=1

˜akj



tkj /c − tkj /c 

> 1



(31) where ˜ a1, . . . , ˜aℓ are computed as in eq. (28). 

Theorem 15. For a prediction step t = ρt 1 with ρ ∈ (0 , 1) , the ℓ-th order predictor satisfies 

∥ˆz(t) − z(t)∥ = O



tkℓ+1 /c 

> 1



= O



∥z1 − z∗∥kℓ+1 /k 1



Proof. Subtracting (30) from (31) and then using (29) gives 

∥ˆz(t) − z(t)∥ ≤ 

> ℓ

X

> j=1

∥˜akj − akj ∥ · 



tkj /c − tkj /c 

> 1



+ O



tkℓ+1 /c − tkℓ+1 /c 

> 1



≤

> ℓ

X

> j=1

O



t(kℓ+1 −kj )/c 

> 1



· O



tkj /c 

> 1



+ O



tkℓ+1 /c 

> 1



= O



tkℓ+1 /c 

> 1



We also have ∥z1 − z∗∥ = Θ( tk1/c  

> 1

), and thus the last expression is O

 ∥z1 − z∗∥c/k 1

kℓ+1 /c 

=

O  ∥z1 − z∗∥kℓ+1 /k 1

.Next, we instantiate the ℓ-th order predictor to the cases ℓ = 1 , 2, 3. 

5.2.1 Case ℓ = 1 In the simplest case of the 1-term predictor, the sequence of nodes contains only k1/c . The Vandermonde matrix reduces to the scalar V = 1, making its inverse trivial. The sample differences vector consists of a single entry corresponding to the distance, P = [ z1 −z2]. Applying the diagonal scaling factor directly gives the explicit coefficient: ˜a1 = z1 − z2

tk1/c  

> 1

(1 − λk1/c ) (32) The final predictor is given by ˆz(t) = z1 + ˜ a1(tk1/c − tk1/c  

> 1

)In particular, ˆz(0) = z1 − ˜a1tk1/c  

> 1

= z1 − z1 − z2

tk1/c  

> 1

(1 − λk1/c ) tk1/c 

> 1

Note that it is different from the linear predictor used in Section 3, which is given by ˆz′(0) = z1 − ck1

t1 ˙z1

It can be seen that the two predictors become equivalent as λ → 1 (or equivalently t2 → t1), since lim 

> t2→t1

˜a1 = lim 

> t2→t1

z1 − z2

tk1/c  

> 1

− tk1/c 

> 2

= ck1

t1−k1/c  

> 1

˙z1.

20 5.2.2 Case ℓ = 2 In practice we can also expand the coefficients for the 2-term Puiseux predictor ( ℓ = 2), which utilizes the leading exponents k1/c and k2/c , and the information of the first derivatives at points 

z1 and z2.Using the explicit definition of the inverse Vandermonde matrix for ℓ = 2, we have: 

V −1 = 1

k2 − k1

 k2 −c

−k1 c



.

Applying this inverse to the vector P = [ z1 − z2, t 1 ˙z1 − t2 ˙z2]⊤, the linear system yields the scaled leading terms: 

V −1

 z1 − z2

t1 ˙z1 − t2 ˙z2



= 1

k2 − k1

 k2(z1 − z2) − c(t1 ˙z1 − t2 ˙z2)

−k1(z1 − z2) + c(t1 ˙z1 − t2 ˙z2)



.

By applying the diagonal scaling factor specified in Proposition 14, we obtain the explicit formulas for the approximated coefficients ˜ a1 and ˜ a2:˜a1 = k2(z1 − z2) − c(t1 ˙z1 − t2 ˙z2)(k2 − k1) tk1/c  

> 1

(1 − λk1/c ) , (33a) ˜a2 = −k1(z1 − z2) + c(t1 ˙z1 − t2 ˙z2)(k2 − k1) tk2/c  

> 1

(1 − λk2/c ) . (33b) 

5.2.3 Case ℓ = 3 For the 3-term Puiseux predictor, the algorithm incorporates second-order derivatives to re-construct the first three leading coefficients. The sample differences vector expands to P =[P1, P 2, P 3]⊤, defined as: 

P1 = z1 − z2,P2 = t1 ˙z1 − t2 ˙z2,P3 =  t21 ¨z1 + t1 ˙z1

 −  t22 ¨z2 + t2 ˙z2

 .

Using the explicit formula for the inverse Vandermonde matrix with ℓ = 3, let us define the denominator constants ∆ i = Q 

> m̸=i

(ki − km) for brevity: ∆1 = ( k1 − k2)( k1 − k3),

∆2 = ( k2 − k1)( k2 − k3),

∆3 = ( k3 − k1)( k3 − k2).

Evaluating the combinatorial sum for the entries of V −1 produces the 3 × 3 inverse matrix: 

V −1 =



> k2k3
> ∆1
> −c(k2+k3)∆1
> c2
> ∆1
> k1k3
> ∆2
> −c(k1+k3)∆2
> c2
> ∆2
> k1k2
> ∆3
> −c(k1+k2)∆3
> c2
> ∆3

 .

21 Multiplying V −1 by the sample vector P and applying the diagonal scaling matrix yields ˜a1 = k2k3P1 − c(k2 + k3)P2 + c2P3

∆1 tk1/c  

> 1

(1 − λk1/c ) , (34a) ˜a2 = k1k3P1 − c(k1 + k3)P2 + c2P3

∆2 tk2/c  

> 1

(1 − λk2/c ) , (34b) ˜a3 = k1k2P1 − c(k1 + k2)P2 + c2P3

∆3 tk3/c  

> 1

(1 − λk3/c ) . (34c) Note that in this case we need the second derivative ¨ z. As before, we approximate it using finite differences, as described in the the next section. 

5.3 Approximating the second derivative 

Recall that the rule for k3/c and the cubic predictor in Section 5.2.3 require the second derivative ¨z for a given point z on the homotopy curve. Exact computation of ¨ z through the differentiation of the homotopy h requires forming and solving systems with the Hessian tensor, which is compu-tationally expensive. Instead, we employ a Hermite Finite Difference (HFD) scheme for arbitrarily spaced grids [For20]. The path tracker naturally yields both the position zi and its exact first derivative ˙zi =

−hz (zi, t i)−1ht(zi, t i) via the Davidenko differential equation at no extra linear algebra cost. As-sume a local geometric mesh of Ns points t1, . . . , t Ns . The Hermite interpolating polynomial over these nodes takes the form: 

q(t) = 

> Ns

X

> j=1

Dj,N s (t)zj +

> Ns

X

> j=1

Ej,N s (t) ˙ zj ,

where the basis functions are defined in terms of the standard Lagrange polynomials Lj,N s (t) = Q    

> m̸=jt−tm
> tj−tm

as: 

Dj,N s (t) = (1 − 2sj (t − tj )) ( Lj,N s (t)) 2, with sj = X 

> m̸=j

1

tj − tm

,Ej,N s (t) = ( t − tj )( Lj,N s (t)) 2.

The second derivative at any evaluation point ti is directly approximated by the linear combination: ¨zi ≈

> Ns

X

> j=1



wfi,j zj + wf d i,j ˙zj



,

where the weights correspond to the exact second derivatives of the basis functions evaluated at ti, namely wfi,j = ¨Dj,N s (ti) and wf d i,j = ¨Ej,N s (ti). These weights are computed efficiently via Fornberg’s recursive algorithm [For20]. Leveraging both position and tangent data on an Ns-point stencil guarantees an asymptotic approximation error for the second derivative of order O(∆ t2Ns−2)without solving local linear systems. In our specific implementation, we use a 5-point stencil x0, . . . , x 4 where xi = ( zi, t i) and 

ti+1 = ρt i for some ρ = 1 − ε. This yields a theoretical approximation error of order O(∆ t8). The third fractional exponent k3/c and the 3-term predictor coefficients are then estimated using the tuples ( ti, z i, ˙zi, ¨zi) evaluated specifically at the nodes i = 1 and i = 3. 22 6 Numerical results 

In this section, we evaluate the computational performance of the proposed methods. To establish a baseline, we benchmark our approach against a classical predictor-corrector path tracker equipped with a standard power-series endgame , as detailed in the foundational literature [SW05; Bat+13]. 

6.1 Description of implementation 

Classic Power Series Endgame ( CLASSIC ) For our numerical experiments, the classical power-series endgame serves as the baseline method for comparison [SW05; Bat+13]. First, the tracking procedure is initialized using a fixed-point Newton homotopy of the form h(z, t ) = (1 − t)f (z) + 

tγ (z − z0) = 0, where z0 is an initial approximation close to the isolated singular root z∗ and γ is a random complex constant (the “gamma trick”) used to ensure the path avoids singularities prior to t = 0. Next, we track the solution path as t moves from 1 to 0 using the standard predictor-corrector method equipped with a linear predictor, as described in Section 2.1. The Newton corrector is restricted to a maximum number of allowed steps (5 in our implementation) to achieve convergence within a prescribed tolerance. If convergence is not detected, the step is rejected, the step size ∆ t

is decreased, and the predictor-corrector step is performed again. Conversely, the method employs an adaptive step size; after a certain number of successive successful iterations, the step size is multiplied by a constant, which in our case is 2. As the path progresses and enters the endgame operating zone , the cycle number c is estimated using the ratio method described in Section 5.1. This cycle number is dynamically updated in the linear predictor to correctly anticipate the fractional power series behaviour of the path. Addition-ally, the method utilizes a cubic predictor to accelerate progress; this is achieved by performing Hermite interpolation using the positions and tangent derivatives ( z and dz/dt ) of two consecutive points on the tracker path that share the same detected cycle number. 

ArcLength Endgame (AL) The AL method introduces a specialized endgame strategy for corank-1 systems designed to circumvent the ill-conditioning of the Jacobian matrix as the path approaches a singular root. Initially, the path is tracked using the classical predictor-corrector method, which continuously monitors the evolution of the cycle number estimations via the ratio (cRATIO ) and logarithmic ( cLOG ) rules. Once both estimators converge to a shared integer value within a strict tolerance (typically 10 −2), the AL method takes over the tracking process, inheriting the state data from the classical tracker. Instead of relying on the standard fractional power-series endgame, the AL method dynamically restructures the Newton corrector into an augmented, well-conditioned linear system. At each tracking step, the method evaluates the augmented matrix hx = [ hz , h t] (i.e. the Jacobian matrix evaluated at the last point in the tracking process) to isolate its null-space vector. Using this approximate kernel, it constructs an augmented square Jacobian matrix, denoted as ¯hx. By appending an orthogonal hyperplane constraint—derived from the tangent vector of the path—the system regularizes the singularity. This localized augmentation ensures that the modified Jacobian ¯hx retains full rank, allowing the Newton iterations to maintain quadratic convergence deep into the singular regime. For path progression, the AL method replaces the standard power-series endgame with a highly adaptive, data-driven schedule based on the locked cycle number estimator ( c). We observed experimentally that in all our corank-1 benchmark instances the fractional powers governing the path geometry follow the regular sequence 1 /c, 2/c, 3/c . Accordingly, we assume that ( k1, k 2, k 3) = (1 , 2, 3) in our implementation, and thus avoid a more costly estimation of higher-order exponents. This enables a clean and direct comparison with CLASSIC . The algorithm continuously monitors the variance of the sequence of cycle number estimates over successive steps. While the variance 23 remains above a prescribed threshold, the method uses smaller steps, calculating the next step size using an adaptively scaling fractional power η. However, once the variance drops below the threshold and the cycle estimates stabilize (indicating that the asymptotic geometry of the path is fully resolved), the algorithm sets the target parameter to t = 0 directly, reaching the singular root using the final, well-conditioned augmented corrector. Furthermore, we observe that the performance of both methods can exhibit very different behavior when certain tracking parameters are modified. For example, changing the adaptive step-size schedule for the CLASSIC method to a more aggressive scheme induces improved performance for some instances, allowing larger steps along the path. However, this aggressive scaling can also significantly increase the total number of matrix inversions, as the tracker accumulates many failed steps—causing the corrector to reject the point, shrink the step size, and recompute the matrix inverse repeatedly. 

Lifted ArcLength Endgame (LAL) Extending the approach from the corank-1 case, the LAL method relies on a similar predictor-corrector scheme. Much like the AL method, it begins by tracking the path with classical techniques until the fractional exponent estimators stabilize. However, critical modifications are introduced to sustain the tracking process toward the singular root. The primary difference is the implementation of a dynamic homotopy reset, introducing the modified Newton homotopy hv(z, t ) discussed in Section 4.2. This is defined as: 

hv(z, t ) = f (z) − t f (v)

∥f (v)∥ , (35) with the starting point initialized at ( z◦, t ◦) = ( v, ∥f (v)∥). During the predictor phase, we first estimate the fractional exponents as detailed in Section 5.1. Specifically, we employ the continuous path-limit ( cRATIO ) rule to estimate the leading fractional exponent, and the extended geometric sequence ( cLOG ) rule for the subsequent fractional expo-nents. Additionally, when computing the third fractional exponent, we utilize the O(∆ t8) finite difference approximation for the derivative of the auxiliary function t ˙z, as described at the begin-ning of this section. As discussed in Section 4.2, setting the step size to jump directly to the target root at t = 0 (i.e., setting ρ = 0) is prohibitive, as the subsequent corrector steps exhibit highly unstable progress. Thus, we introduce a dynamic shrinking factor updated after every iteration. Specifically, the predicted parameter value is defined as ˆ t = ρ · t◦, where the step ratio is explicitly formulated as 

ρ = |t◦|η. To systematically govern the progression toward the singularity, the scaling exponent is initialized at η = 1 .1 and updated iteratively via the geometric rule η ← ηα. For failed iterations, the step size is penalized by setting α = 0 .5. For successful iterations, we attempt to accelerate tracking by setting α = 1 .2; however, this acceleration is strictly conditional. The algorithm continuously monitors the variance across the coordinate-wise estimates for each exponent order (e.g., tracking distinct variances for the k1/c ensemble and the k2/c ensemble). The scaling exponent η is permitted to increase only when these variances drop below a predefined tol-erance threshold, ensuring the local asymptotic geometry has stabilized. Furthermore, to prevent the step size ρ from shrinking dangerously close to 0 prematurely, we impose an upper bound on η.This bound is set to ( k2/k 1)β for the quadratic predictor ( Q) and ( k3/k 1)β for the cubic predictor (C). In our implementation, we evaluate thresholds of β = 0 .5 and β = 0 .9, yielding the variants labeled Q5 , Q9 , C5 , and C9 in the convergence plots. In the corrector phase, we augment the Jacobian matrix hx = [ hz , h t] similarly to the standard AL method, which effectively reduces the corank by exactly one. However, a second extension is required to completely recover the numerical rank of the system. The algorithm performs a SVD on the standard augmented matrix hvx =: [ Hz , H t] to isolate the near-zero singular values (falling below a strict tolerance threshold, set to 10 −5 in our implementation). Let U be the matrix whose columns ui correspond to these near-null left-singular vectors. 24 Using this basis, we construct the full row-rank block matrix [ Hz , H t, U ]. To formulate a well-posed, square augmented Jacobian ¯Hx, we append an orthogonal constraint block Q derived from the tangent space: 

Q =

h H−1 

> z

[Ht, U ]†, −I

i

,

where † denotes the Hermitian transpose. This yields the fully augmented polynomial system: ¯H(z, t, ξ ) = 



hv(z, t ) + U ξ Q



z − ˆzt − ˆtξ

 = 0 ,

where ξ represents the vector of auxiliary artificial variables, and (ˆ z, ˆt) is the predicted state. Finally, this well-conditioned system is solved using the Newton’s method, terminating once the Newton step-ratio contracts below a prescribed tolerance. Because of the geometric relaxation introduced by the orthogonal hyperplanes, the newly corrected point ( zn, t n) no longer lies strictly on the exact solution curve hv(z, t ) = 0. Thus, to continue tracking toward the root, the algorithm dynamically resets the Newton homotopy by setting v ← zn, and the predictor-corrector sequence repeats for the subsequent step. 

Instances To rigorously evaluate the performance of our endgame strategies, we benchmark against several well-known zero-dimensional polynomial systems from the NAG literature. These include the Griewank-Osborne system [GO81], characterized by its narrow domain of convergence near the singularity; Lecerf’s deflation benchmark [Lec02]; and the Caprasse system [HMS15]. To systematically isolate the impact of the Jacobian corank ( κ) on predictor stability, we evaluate our methods on a family of crafted polynomial systems. These instances are constructed with a prescribed singular root at the origin, z∗ = 0 ∈ Cn, using the form: 

F (z) = Az + D(z) = 0 (36) Here, A ∈ Zn×n is a dense random integer matrix explicitly constructed to have rank n−κ. Because the Jacobian at the origin reduces strictly to this linear part ( JF (0) = A), the system guarantees a corank of exactly κ. The higher-order term D(z) is a diagonal mapping of monomials zαi 

> i

with randomly sampled integers αi > 1. Additionally, we apply a dense random linear transformation to both the coordinates and equations, ultimately tracking N · F (M z ) = 0. Additionally, we use a second family of instances where the higher-order components are crossed monomials with prescribed total degree. These systems take the form: 

F (z) = Az + T (z) = 0 (37) As before, A ∈ Zn×n is a random matrix explicitly constructed to have rank n − κ. However, instead of a simple diagonal mapping, the higher-order component T (z) consists of multivariate polynomials. Each component Ti(z) is formed by summing randomly generated monomials of the form c Qnk=1 zαk 

> k

, where the coefficients c and non-negative integer exponents αk are randomly sampled. To guarantee that the linear part strictly dominates the Jacobian at the origin—thereby per-fectly preserving the prescribed corank κ—we enforce a total degree constraint of P αk ≥ 2 on every monomial. This ensures that lim z→0 JT (z) = 0. Finally, the system is subjected to the same dense random linear transformation, tracking N · F (M z ) = 0. 

6.2 Plots 

Convergence comparisons In the following experiments, we present a convergence comparison between the classical power-series endgame (denoted as CLASSIC ) and the proposed ArcLength 25 Endgame (AL, LAL) methods. To provide a clear, hardware-independent measure of algorithmic efficiency, the horizontal axis in all convergence plots denotes the cumulative number of matrix inversions. The vertical axis displays the tracking residual on a logarithmic scale, log 10 ∥f (z)∥,illustrating the depth of convergence as the path approaches the singular root. Throughout this section, test instances are identified in the figure captions by the source of the polynomial system and the parameter tuple ( n, κ, c ), denoting the number of variables, the prescribed corank of the Jacobian matrix at the singular root, and the winding cycle number, respectively. For certain instances, we also indicate the number of high-order monomials, denoted as # mon , added to each equation, alongside the interval from which the monomial degrees αi are sampled. The evaluated LAL variants are distinguished in the plots by their predictor degree and adaptive step-size thresholds. Specifically, LALQ designates methods utilizing a quadratic predictor, while 

LALC designates those utilizing a cubic predictor. The appended numerical suffixes (e.g., Q5, C9) indicate the specific bound thresholds used to govern the predictor step-size logic. Finally, to effectively track paths deep within the endgame operating zone and mitigate severe ill-conditioning, the numerical trackers are initialized with a baseline of 500 digits of multiprecision arithmetic. To prevent numerical underflow, this precision is expanded dynamically in direct proportion to the order of magnitude of the homotopy parameter, O(− log 10 |t|). Additionally, in instances where a tracking method fails to converge—whether due to stalling or divergence—the trajectory is truncated. The final valid step recorded before the tracker failed is explicitly marked on the plot with a hollow black circle ( ⃝). In our implementation, the floating-point outputs are rounded to the closest rational number p/q within a prescribed tolerance of tol = 10 −3.Consequently, the exact integer values reported in the experiment descriptions and figure captions (e.g., c = 4 and k1,2,3 = {1, 2, 3}) reflect these recovered rational representations. 

Comparing c/k 1 Estimation Methods In the final section, we present plots comparing the performance of the three approaches discussed in Section 5.1 to estimate the cycle number. To evaluate each method, we take a current state pair ( z1, t 1) and utilize the history of points generated along the tracking path using the classical power series endgame previously described. For the trial-and-error rule ( cSORT ), we compare the predicted values across all possible cycle numbers ranging from 1 to a predefined maximum, Cmax = 16. For the path-limit estimation (cRATIO ), the estimation relies on two consecutive points from the history, supplemented by an additional point ( cRATIO+ ) computed using a stepsize λ that is small relative to |t| (e.g., λ =

|t| · 10 −10 ). Finally, the geometric sequence sampling method ( cLOG ) requires three specific values ( t1, t 2, t 3)that form a geometric progression such that t2 = ρt 1 and t3 = ρ2t1. Consequently, we only perform this computation when three consecutive history points satisfy this geometric property. If this condition is not met at a given step, the computation is skipped. In the accompanying plots, these skipped computations are bridged using linear interpolation to maintain the continuity of the curves. 26 6.2.1 Comparison performance AL vs CLASSIC :

Figure 1: Griewank-Osborne system. 

[GO81] System parameters: ( n, κ, c ) = (2 , 1, 3). 

Figure 2: Diagonal generator D(z).

System parameters: (n, κ, c ) = (5 , 1, 5), with sampled monomial degrees |αi| ∈ [3 , 7]. 

Figure 3: Diagonal generator D(z).

System parameters: (n, κ, c ) = (5 , 1, 5), with sampled monomial degrees |αi| ∈ [3 , 7]. 27 6.2.2 Instances from literature: 

Figure 4: Lecerf system [Lec02]. System parameters: (n, κ, c ) = (3 , 2, 6), with 

k1,2,3 = {1, 2, 3}.

Figure 5: Caprasse system [HMS15]. System parameters: (n, κ, c ) = (3 , 2, 2), with 

k1,2,3 = {1, 2, 3}.28 6.2.3 Problems first generator: 

Figure 6: Diagonal generator D(z).

System parameters: ( n, κ, c ) = (7 , 5, 30), with 

k1,2,3 = {5, 6, 7} and α ∈ [3 , 7]. 

Figure 7: Diagonal generator D(z).

System parameters: ( n, κ, c ) = (9 , 2, 20), with 

k1,2,3 = {4, 5, 6} and α ∈ [3 , 10]. 

Figure 8: Diagonal generator D(z).

System parameters: ( n, κ, c ) = (6 , 4, 90), with 

k1,2,3 = {10 , 15 , 18 } and α ∈ [3 , 10]. 

Figure 9: Diagonal generator D(z).

System parameters: ( n, κ, c ) = (8 , 7, 210), with 

k1,2,3 = {30 , 35 , 42 } and α ∈ [3 , 5]. 29 6.2.4 Problems second generator: 

Figure 10: Multivariate generator T (z).

System parameters: (n, κ, c, #mon ) =(4 , 2, 4, 2), with k1,2,3 = {1, 2, 3} and de-gree constraint α ∈ [1 , 4]. 

Figure 11: Multivariate generator T (z).

System parameters: (n, κ, c, #mon ) =(5 , 2, 4, 2), with k1,2,3 = {1, 2, 3} and de-gree constraint α ∈ [1]. 

Figure 12: Multivariate generator T (z).

System parameters: (n, κ, c, #mon ) =(4 , 3, 2, 1), with k1,2,3 = {1, 2, 3} and de-gree constraint α ∈ [1]. 30 6.2.5  Comparison estimations  c/k 1:10 -45 

> 10 -40
> 10 -35
> 10 -30
> 10 -25
> 10 -20
> 10 -15
> 10 -10
> 10 -5
> 10 0
> t
> -5
> 0
> 5
> 10
> 15
> 20
> Cycle Number Estimate
> Instance #3 (n = 5; k = 4)
> cSort
> cLog
> cRATIO
> cRATIO+
> Figure 13 10 -50
> 10 -40
> 10 -30
> 10 -20
> 10 -10
> 10 0
> t
> -5
> 0
> 5
> 10
> 15
> 20
> Cycle Number Estimate
> Instance #2 (n = 8; k = 7)
> cSort
> cLog
> cRATIO
> cRATIO+
> Figure 14

31 10 -300 

> 10 -250
> 10 -200
> 10 -150
> 10 -100
> 10 -50
> 10 0
> t
> -5
> 0
> 5
> 10
> 15
> 20
> Cycle Number Estimate
> Instance #13 (n = 3; k = 2)
> cSort
> cLog
> cRATIO
> cRATIO+Figure 15 10 -300
> 10 -250
> 10 -200
> 10 -150
> 10 -100
> 10 -50
> 10 0
> t
> -5
> 0
> 5
> 10
> 15
> 20
> Cycle Number Estimate
> Instance #14 (n = 3; k = 2)
> cSort
> cLog
> cRATIO
> cRATIO+
> Figure 16

32 10 -45 

> 10 -40
> 10 -35
> 10 -30
> 10 -25
> 10 -20
> 10 -15
> 10 -10
> 10 -5
> 10 0
> t
> -5
> 0
> 5
> 10
> 15
> 20
> Cycle Number Estimate
> Instance #5 (n = 9; k = 2)
> cSort
> cLog
> cRATIO
> cRATIO+Figure 17 10 -50
> 10 -40
> 10 -30
> 10 -20
> 10 -10
> 10 0
> t
> -5
> 0
> 5
> 10
> 15
> 20
> Cycle Number Estimate
> Instance #62 (n = 6; k = 4)
> cSort
> cLog
> cRATIO
> cRATIO+
> Figure 18

33 References 

[Bat+13] Daniel J. Bates et al. Numerically Solving Polynomial Systems with Bertini . Philadel-phia, PA, USA: SIAM, 2013. doi : 10.1137/1.9781611972702 .[BHS11] Daniel J. Bates, Jonathan D. Hauenstein, and Andrew J. Sommese. “A Parallel Endgame”. In: Contemporary Mathematics 556 (2011), pp. 25–35. doi : 10 . 1090 / conm/556/11021 .[For20] Bengt Fornberg. “An algorithm for calculating Hermite-based finite difference weights”. In: IMA Journal of Numerical Analysis (2020), pp. 1–13. doi : 10 . 1093 / imanum / draa006 .[FS12] O. P. Ferreira and B. F. Svaiter. “Kantorovich’s Theorem on Newton’s Method”. In: 

arXiv preprint (2012). arXiv: 1209.5704 .[GLS07] Gert-Martin Greuel, Christoph Lossen, and Eugenii Shustin. Introduction to Singular-ities and Deformations . Berlin, Germany: Springer Science & Business Media, 2007. 

doi : 10.1007/978-3-540-28381-2 .[GO81] Andreas Griewank and Michael R. Osborne. “Newton’s method for singular problems when the dimension of the null space is > 1”. In: SIAM Journal on Numerical Analysis 

18.1 (1981), pp. 145–149. doi : 10.1137/0718011 .[Hig02] Nicholas J. Higham. Accuracy and Stability of Numerical Algorithms . 2nd. Philadel-phia, PA, USA: SIAM, 2002. doi : 10.1137/1.9780898718027 .[HMS15] Jonathan D. Hauenstein, Bernard Mourrain, and Agnes Szanto. “Certifying isolated singular points and their multiplicity structure”. In: Proceedings of the 2015 Inter-national Symposium on Symbolic and Algebraic Computation . New York, NY, USA: ACM, 2015, pp. 213–220. doi : 10.1145/2755996.2756651 .[HS95] Birkett Huber and Bernd Sturmfels. “A Polyhedral Method for Solving Sparse Poly-nomial Systems”. In: Mathematics of Computation 64.212 (1995), pp. 1541–1555. doi :

10.2307/2153370 .[Kel77] Herbert B. Keller. “Numerical solution of bifurcation and nonlinear eigenvalue prob-lems”. In: Applications of Bifurcation Theory . Ed. by Paul H. Rabinowitz. New York, NY, USA: Academic Press, 1977, pp. 359–384. doi : 10.1016/B978-0-12-574250-4.50015-3 .[Lec02] Gr´ egoire Lecerf. “Quadratic Newton iteration for systems with multiplicity”. In: Jour-nal of Symbolic Computation 33.5 (2002), pp. 747–794. doi : 10.1006/jsco.2002. 0528 .[Loj59] Stanislaw Lojasiewicz. “Sur le probl` eme de la division”. In: Studia Mathematica 18.1 (1959), pp. 87–136. doi : 10.4064/sm-18-1-87-136 .[LVZ06] Anton Leykin, Jan Verschelde, and Ailing Zhao. “Newton’s method with deflation for isolated singularities of polynomial systems”. In: Theoretical Computer Science 359.1 (2006), pp. 111–122. doi : 10.1016/j.tcs.2006.02.016 .[LVZ08] Anton Leykin, Jan Verschelde, and Ailing Zhao. “Higher-Order Deflation for Polyno-mial Systems with Isolated Singular Solutions”. In: Algorithms in Algebraic Geom-etry . Vol. 146. The IMA Volumes in Mathematics and its Applications. New York, NY, USA: Springer, 2008, pp. 79–97. doi : 10.1007/978-0-387-75155-9_5 .[LZ12] Nan Li and Lihong Zhi. “Computing the multiplicity structure of an isolated singular solution: Case of breadth one”. In: Journal of Symbolic Computation 47.6 (2012), pp. 700–710. doi : 10.1016/j.jsc.2011.12.027 .34 [LZ22] Nan Li and Lihong Zhi. “Improved two-step Newton’s method for computing simple multiple zeros of polynomial systems”. In: Numerical Algorithms 91.1 (2022), pp. 19– 50. doi : 10.1007/s11075-021-01254-4 .[MSW92a] Alexander P. Morgan, Andrew J. Sommese, and Charles W. Wampler. “A power series method for computing singular solutions to nonlinear analytic systems”. In: 

Numerische Mathematik 63.1 (1992), pp. 391–409. doi : 10.1007/BF01385817 .[MSW92b] Alexander P. Morgan, Andrew J. Sommese, and Charles W. Wampler. “Computing singular solutions to polynomial systems”. In: Advances in Applied Mathematics 13.3 (1992), pp. 305–327. doi : 10.1016/0196-8858(92)90014-N .[OWM83] Takeo Ojika, Satoshi Watanabe, and Taketomo Mitsui. “Deflation algorithm for the multiple roots of a system of nonlinear equations”. In: Journal of Mathematical Anal-ysis and Applications 96.2 (1983), pp. 463–479. doi : 10.1016/0022-247X(83)90204-0.[Rik72] E. Riks. “The application of Newton’s method to the problem of elastic stability”. In: 

Journal of Applied Mechanics 39.4 (1972), pp. 1060–1065. doi : 10.1115/1.3422829 .[SS90] Gilbert W. Stewart and Ji-guang Sun. Matrix Perturbation Theory . New York, NY, USA: Academic Press, 1990. [SW05] Andrew J. Sommese and Charles W. Wampler. The Numerical Solution of Systems of Polynomials Arising in Engineering and Science . Singapore: World Scientific, 2005. 

doi : 10.1142/5728 .[Wal04] Charles Terence Clegg Wall. Singular Points of Plane Curves . Vol. 63. London Math-ematical Society Student Texts. Cambridge, UK: Cambridge University Press, 2004. 

doi : 10.1017/CBO9780511617560 .[Wem71] George A. Wempner. “Discrete approximations related to nonlinear theories of solids”. In: International Journal of Solids and Structures 7.11 (1971), pp. 1581–1599. doi :

10.1016/0020-7683(71)90038-2 .[Zar32] Oscar Zariski. “On the topology of algebroid singularities”. In: American Journal of Mathematics 54.2 (1932), pp. 453–465. doi : 10.2307/2370887 .[ZS65] Oscar Zariski and Pierre Samuel. Commutative Algebra, Volume II . New York, NY, USA: Springer-Verlag, 1965. doi : 10.1007/978-3-662-22830-2 .35