---
title: Finding Adam in noisy trees
source: arxiv
title_zh: Finding Adam in noisy trees
abstract_zh: "We consider problem 的 finding root vertex 的 random uniform attachment tree，when union 的 unlabeled tr。\n\n核心做法：We prove that，as long as p=o (\\log n /n)，用于 any \\varepsilon>0，one can construct confidence set 的 ver。\n\n主要结果：We show that same approach works in other noise 模型s as well。\n\n值得关注的点在于：Our approach ranks vertices by their Jordan centrality in largest component 的 subgraph spanned by hi。"
abstract_en: "We consider the problem of finding the root vertex of a random uniform attachment tree, when the union of the unlabeled tree and an Erdős-Rényi random graph $\\mathbb{G}(n,p)$ is observed. We prove that, as long as $p=o(\\log n /n)$, for any $\\varepsilon>0$, one can construct a confidence set of vertices of size $K(\\varepsilon)$ that depends only on $\\varepsilon$ and not on $n$, such that it contains the root with probability at least $1-\\varepsilon$. This affirms a conjecture of Crane and Xu (2021). Our approach ranks vertices by their Jordan centrality in the largest component of the subgraph spanned by high-degree vertices. We show that the same approach works in other noise models as well."
authors: "Luc Devroye, Gábor Lugosi, Neeladri Maitra"
date: 2026-07-20
journal: math.PR
source_link: "https://arxiv.org/pdf/2607.18201v1"
pdf: "https://arxiv.org/pdf/2607.18201v1"
score: 7.949999999999999
tldr: 围绕《Finding Adam in noisy tr...》；We prove that，as long as p=o (\l...；We show that same approach works in...
tldr_cn: 围绕《Finding Adam in noisy tr...》；We prove that，as long as p=o (\l...；We show that same approach works in...
tldr_en: "We consider the problem of finding the root vertex of a random uniform attachment tree, when the union of the unlabeled tree and an Erdős-Rényi random graph $\\mathbb{G}(n,p)$ is observed. We prove that, as long as $p=o(\\log n /n)$, for any $\\varepsilon>0$, one can construct a confidence set of vertices of size $K(\\varepsilon)$ that depends..."
selection_source: fresh_fetch
motivation: We consider problem 的 finding root vertex 的 random uniform attachment tree，when union 的 unlabeled tr。
method: "核心做法：We prove that，as long as p=o (\\log n /n)，用于 any \\varepsilon>0，one can construct confidence set 的 ver。"
result: 主要结果：We show that same approach works in other noise 模型s as well。
conclusion: 值得关注的点在于：Our approach ranks vertices by their Jordan centrality in largest component 的 subgraph spanned by hi。
---

## 摘要
We consider problem 的 finding root vertex 的 random uniform attachment tree，when union 的 unlabeled tr。

核心做法：We prove that，as long as p=o (\log n /n)，用于 any \varepsilon>0，one can construct confidence set 的 ver。

主要结果：We show that same approach works in other noise 模型s as well。

值得关注的点在于：Our approach ranks vertices by their Jordan centrality in largest component 的 subgraph spanned by hi。

## Abstract
We consider the problem of finding the root vertex of a random uniform attachment tree, when the union of the unlabeled tree and an Erdős-Rényi random graph $\mathbb{G}(n,p)$ is observed. We prove that, as long as $p=o(\log n /n)$, for any $\varepsilon>0$, one can construct a confidence set of vertices of size $K(\varepsilon)$ that depends only on $\varepsilon$ and not on $n$, such that it contains the root with probability at least $1-\varepsilon$. This affirms a conjecture of Crane and Xu (2021). Our approach ranks vertices by their Jordan centrality in the largest component of the subgraph spanned by high-degree vertices. We show that the same approach works in other noise models as well.
