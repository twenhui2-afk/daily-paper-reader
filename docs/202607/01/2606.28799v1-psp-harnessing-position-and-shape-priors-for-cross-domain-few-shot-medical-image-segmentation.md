---
title: "PSP: Harnessing Position and Shape Priors for Cross-Domain Few-Shot Medical Image Segmentation"
source: arxiv
title_zh: 用于Cross-Domain Few-Shot Medical Image Segmentation的PSP：Harnessing Position与Shape Priors
abstract_zh: "Few-Shot Medical Image Segmentation (FSMIS) offers powerful solution to data scarcity but struggles。\n\n核心做法：Therefore，作者提出 novel 框架 that harnesses Position 与 Shape Priors (PSP) 用于 cross-domain FSMIS。\n\n主要结果：Extensive experiments on two public medical imaging datasets 表明 that PSP significantly 优于当前最优方法s。\n\n值得关注的点在于：Furthermore，Hybrid-Prototype Prediction (HPP) 模块 adaptively calibrates support prototype to query fe。"
abstract_en: "Few-Shot Medical Image Segmentation (FSMIS) offers a powerful solution to data scarcity but struggles to generalize across different imaging modalities. This performance collapse stems primarily from the drastic texture discrepancies between domains, which mislead models trained on source-specific intensity distributions. While existing methods attempt to align frequency or local texture features, they often fail to decouple semantic structure from domain-specific appearance. To address this, we identify a critical invariance: despite distinct imaging physics, the position and geometric shape of organs remain robustly consistent across modalities. Therefore, we propose a novel framework that harnesses Position and Shape Priors (PSP) for cross-domain FSMIS. Specifically, PSP first introduces a Position Coordinate Embedding (PCE) module to inject relative spatial coordinates for rapid organ localization. Subsequently, a Shape Prototype Modulation (SPM) module constructs domain-invariant structural prototypes via explicit shape priors, effectively filtering out texture noise. Furthermore, the Hybrid-Prototype Prediction (HPP) module adaptively calibrates the support prototype to the query feature distribution, mitigating feature misalignment. Extensive experiments on two public medical imaging datasets demonstrate that PSP significantly outperforms state-of-the-art methods."
authors: "Bin Xu, Yazhou Zhu, Haofeng Zhang"
date: 2026-06-27
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.28799v1"
pdf: "https://arxiv.org/pdf/2606.28799v1"
score: 7.55
tldr: 围绕《用于Cross-Domain Few-Shot...》；Therefore，作者提出 novel 框架 that har...；Extensive experiments on two public...
tldr_cn: 围绕《用于Cross-Domain Few-Shot...》；Therefore，作者提出 novel 框架 that har...；Extensive experiments on two public...
tldr_en: "Few-Shot Medical Image Segmentation (FSMIS) offers a powerful solution to data scarcity but struggles to generalize across different imaging modalities. This performance collapse stems primarily from the drastic texture discrepancies between domains, which mislead models trained on source-specific intensity distributions."
selection_source: fresh_fetch
vocabulary: ["localization | 定位 | CET6", "segmentation | 分割 | CET6/IELTS", "embedding | 嵌入表示 | 学术高频"]
motivation: Few-Shot Medical Image Segmentation (FSMIS) offers powerful solution to data scarcity but struggles。
method: 核心做法：Therefore，作者提出 novel 框架 that harnesses Position 与 Shape Priors (PSP) 用于 cross-domain FSMIS。
result: 主要结果：Extensive experiments on two public medical imaging datasets 表明 that PSP significantly 优于当前最优方法s。
conclusion: 值得关注的点在于：Furthermore，Hybrid-Prototype Prediction (HPP) 模块 adaptively calibrates support prototype to query fe。
---

## 摘要
Few-Shot Medical Image Segmentation (FSMIS) offers powerful solution to data scarcity but struggles。

核心做法：Therefore，作者提出 novel 框架 that harnesses Position 与 Shape Priors (PSP) 用于 cross-domain FSMIS。

主要结果：Extensive experiments on two public medical imaging datasets 表明 that PSP significantly 优于当前最优方法s。

值得关注的点在于：Furthermore，Hybrid-Prototype Prediction (HPP) 模块 adaptively calibrates support prototype to query fe。

## Abstract
Few-Shot Medical Image Segmentation (FSMIS) offers a powerful solution to data scarcity but struggles to generalize across different imaging modalities. This performance collapse stems primarily from the drastic texture discrepancies between domains, which mislead models trained on source-specific intensity distributions. While existing methods attempt to align frequency or local texture features, they often fail to decouple semantic structure from domain-specific appearance. To address this, we identify a critical invariance: despite distinct imaging physics, the position and geometric shape of organs remain robustly consistent across modalities. Therefore, we propose a novel framework that harnesses Position and Shape Priors (PSP) for cross-domain FSMIS. Specifically, PSP first introduces a Position Coordinate Embedding (PCE) module to inject relative spatial coordinates for rapid organ localization. Subsequently, a Shape Prototype Modulation (SPM) module constructs domain-invariant structural prototypes via explicit shape priors, effectively filtering out texture noise. Furthermore, the Hybrid-Prototype Prediction (HPP) module adaptively calibrates the support prototype to the query feature distribution, mitigating feature misalignment. Extensive experiments on two public medical imaging datasets demonstrate that PSP significantly outperforms state-of-the-art methods.

## 英语词汇
- **localization**：定位（CET6）
- **segmentation**：分割（CET6/IELTS）
- **embedding**：嵌入表示（学术高频）
