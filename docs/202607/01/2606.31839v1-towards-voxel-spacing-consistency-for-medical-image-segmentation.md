---
title: Towards Voxel Spacing Consistency for Medical Image Segmentation
source: arxiv
title_zh: 用于Medical Image Segmentation的Towards Voxel Spacing Consistency
abstract_zh: "In this work，这篇论文研究 correlation between image resampling 与 segmentation，与 propose Consispace，semanti。\n\n核心做法：Both intra-slice 与 inter-slice constraints are integrated 为 implicit neural 网络，supporting arbitrary-。\n\n主要结果：Extensive experiments on multiple datasets 表明 that Consispace 达到 superior reconstruction quality 与 p。\n\n值得关注的点在于：Volumetric medical image segmentation is essential 用于 both preoperative diagnosis 与 intraoperative g。"
abstract_en: "Volumetric medical image segmentation is essential for both preoperative diagnosis and intraoperative guidance. While recent years have witnessed rapid progress in segmentation architectures, comparatively little attention is paid to the physical voxel spacing of anatomical data. Indeed, volumetric image resampling is a ubiquitous preprocessing step before segmentation, yet its interaction with downstream segmentation has not been systematically exploited. In this work, we study the correlation between image resampling and segmentation, and propose Consispace, a semantic-aware resampling framework that achieves consistent voxel spacing in the axial direction while preserving anatomical and semantic consistency. Consispace introduces an ODE-based anatomical constraint to model inter-slice dynamics with a continuous interpolator, enabling faithful reconstruction under complex anatomical transitions beyond discrete interpolation. To further couple resampling with segmentation objectives, we leverage dense features from a pretrained vision model to build intra-slice semantic correlation maps and inject class-wise semantic consistency via feature reweighting during resampling. Both intra-slice and inter-slice constraints are integrated into an implicit neural network, supporting arbitrary-scale resampling. Extensive experiments on multiple datasets demonstrate that Consispace achieves superior reconstruction quality and perceptual fidelity, produces smoother inter-slice anatomy, and improves downstream segmentation performance when used as a preprocessing step."
authors: "Xin You, Runze Yang, Minghui Zhang, Hanxiao Zhang, Han Li, Yi Yu, Jie Yang, Nassir Navab, Yun Gu"
date: 2026-06-30
journal: cs.CV
source_link: "https://arxiv.org/pdf/2606.31839v1"
pdf: "https://arxiv.org/pdf/2606.31839v1"
score: 7.75
tldr: 围绕《用于Medical Image Segmenta...》；Both intra-slice 与 inter-slice c...；Extensive experiments on multiple da...
tldr_cn: 围绕《用于Medical Image Segmenta...》；Both intra-slice 与 inter-slice c...；Extensive experiments on multiple da...
tldr_en: "Volumetric medical image segmentation is essential for both preoperative diagnosis and intraoperative guidance. While recent years have witnessed rapid progress in segmentation architectures, comparatively little attention is paid to the physical voxel spacing of anatomical data."
selection_source: fresh_fetch
vocabulary: ["reconstruction | 重建 | CET6", "segmentation | 分割 | CET6/IELTS", "downstream | 下游任务 | 学术高频", "volumetric | 体数据的 / 三维体积的 | 学术高频", "diagnosis | 诊断 | IELTS"]
motivation: In this work，这篇论文研究 correlation between image resampling 与 segmentation，与 propose Consispace，semanti。
method: 核心做法：Both intra-slice 与 inter-slice constraints are integrated 为 implicit neural 网络，supporting arbitrary-。
result: 主要结果：Extensive experiments on multiple datasets 表明 that Consispace 达到 superior reconstruction quality 与 p。
conclusion: 值得关注的点在于：Volumetric medical image segmentation is essential 用于 both preoperative diagnosis 与 intraoperative g。
---

## 摘要
In this work，这篇论文研究 correlation between image resampling 与 segmentation，与 propose Consispace，semanti。

核心做法：Both intra-slice 与 inter-slice constraints are integrated 为 implicit neural 网络，supporting arbitrary-。

主要结果：Extensive experiments on multiple datasets 表明 that Consispace 达到 superior reconstruction quality 与 p。

值得关注的点在于：Volumetric medical image segmentation is essential 用于 both preoperative diagnosis 与 intraoperative g。

## Abstract
Volumetric medical image segmentation is essential for both preoperative diagnosis and intraoperative guidance. While recent years have witnessed rapid progress in segmentation architectures, comparatively little attention is paid to the physical voxel spacing of anatomical data. Indeed, volumetric image resampling is a ubiquitous preprocessing step before segmentation, yet its interaction with downstream segmentation has not been systematically exploited. In this work, we study the correlation between image resampling and segmentation, and propose Consispace, a semantic-aware resampling framework that achieves consistent voxel spacing in the axial direction while preserving anatomical and semantic consistency. Consispace introduces an ODE-based anatomical constraint to model inter-slice dynamics with a continuous interpolator, enabling faithful reconstruction under complex anatomical transitions beyond discrete interpolation. To further couple resampling with segmentation objectives, we leverage dense features from a pretrained vision model to build intra-slice semantic correlation maps and inject class-wise semantic consistency via feature reweighting during resampling. Both intra-slice and inter-slice constraints are integrated into an implicit neural network, supporting arbitrary-scale resampling. Extensive experiments on multiple datasets demonstrate that Consispace achieves superior reconstruction quality and perceptual fidelity, produces smoother inter-slice anatomy, and improves downstream segmentation performance when used as a preprocessing step.

## 英语词汇
- **reconstruction**：重建（CET6）
- **segmentation**：分割（CET6/IELTS）
- **downstream**：下游任务（学术高频）
- **volumetric**：体数据的 / 三维体积的（学术高频）
- **diagnosis**：诊断（IELTS）
