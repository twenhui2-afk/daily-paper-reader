---
title: Repairing Shape-Prior Shortcuts in Long-Range Single-Shot Fringe Projection Profilometry
source: arxiv
title_zh: Repairing Shape-Prior Shortcuts in Long-Range Single-Shot Fringe 投影 Profilometry
abstract_zh: "Single-shot fringe 投影 profilometry (FPP) 网络s that regress depth directly can exploit shape-prior sho。\n\n核心做法：作者引入 PhiCalNet，which outputs wrapped-phase representation (\\sinφ，\\cosφ) 与 maps it to depth through f。\n\n主要结果：Because single-shot mapping is non-injective without fringe order，PhiCalNet takes fringe order as au。\n\n值得关注的点在于：Two checks agree：interpretability makes phase most decodable internal feature，与 pixel-wise conformal。"
abstract_en: "Single-shot fringe projection profilometry (FPP) networks that regress depth directly can exploit a shape-prior shortcut, recovering depth from object boundaries rather than from fringe phase. On a photorealistic synthetic benchmark (15,600 fringe images, 50 objects at 1.5-2.1 m standoff), the best such UNet baseline plateaus at 14.54 mm object mean absolute error (MAE), and neither more data nor more capacity removes the shortcut, because neither changes the hypothesis space the optimizer searches. We introduce PhiCalNet, which outputs a wrapped-phase representation $(\\sinφ, \\cosφ)$ and maps it to depth through a fixed differentiable calibration layer, removing the shape-prior solution architecturally rather than by a loss penalty. Because the single-shot mapping is non-injective without fringe order, PhiCalNet takes the fringe order as auxiliary input, an assumption a sensitivity analysis shows tolerates realistic decoding error; a physics-informed (PINN) baseline with the same physics as a soft penalty yields no gain, isolating the architectural choice as the operative factor. PhiCalNet reduces object MAE 3.3x to 4.46 mm, its residual confined to 0.103% of pixels at the $\\pmπ$ wrap discontinuity, and a three-frame extension reaches 1.16 mm. Two checks agree: interpretability makes phase the most decodable internal feature, and pixel-wise conformal uncertainty quantification, to our knowledge the first for FPP, localizes error at the same discontinuity, where rejecting the top 5% of pixels by snapshot disagreement cuts root-mean-square error by 64% versus 3.5% for the baseline."
authors: "Adam Haroon, Cody Fleming, Beiwen Li"
date: 2026-07-10
journal: cs.LG
source_link: "https://arxiv.org/pdf/2607.11928v1"
pdf: "https://arxiv.org/pdf/2607.11928v1"
score: 8.0
tldr: 围绕《Repairing Shape-Prior Sh...》；作者引入 PhiCalNet，which outputs wra...；Because single-shot mapping is non-i...
tldr_cn: 围绕《Repairing Shape-Prior Sh...》；作者引入 PhiCalNet，which outputs wra...；Because single-shot mapping is non-i...
tldr_en: "Single-shot fringe projection profilometry (FPP) networks that regress depth directly can exploit a shape-prior shortcut, recovering depth from object boundaries rather than from fringe phase. On a photorealistic synthetic benchmark (15,600 fringe images, 50 objects at 1.5-2.1 m standoff), the best such UNet baseline plateaus at 14.54 mm object mean absolute error (MAE), and neither..."
selection_source: fresh_fetch
vocabulary: ["interpretability | 可解释性 | IELTS", "uncertainty | 不确定性 | IELTS", "benchmark | 基准测试 | CET6/IELTS"]
motivation: Single-shot fringe 投影 profilometry (FPP) 网络s that regress depth directly can exploit shape-prior sho。
method: 核心做法：作者引入 PhiCalNet，which outputs wrapped-phase representation (\sinφ，\cosφ) 与 maps it to depth through f。
result: 主要结果：Because single-shot mapping is non-injective without fringe order，PhiCalNet takes fringe order as au。
conclusion: 值得关注的点在于：Two checks agree：interpretability makes phase most decodable internal feature，与 pixel-wise conformal。
---

## 摘要
Single-shot fringe 投影 profilometry (FPP) 网络s that regress depth directly can exploit shape-prior sho。

核心做法：作者引入 PhiCalNet，which outputs wrapped-phase representation (\sinφ，\cosφ) 与 maps it to depth through f。

主要结果：Because single-shot mapping is non-injective without fringe order，PhiCalNet takes fringe order as au。

值得关注的点在于：Two checks agree：interpretability makes phase most decodable internal feature，与 pixel-wise conformal。

## Abstract
Single-shot fringe projection profilometry (FPP) networks that regress depth directly can exploit a shape-prior shortcut, recovering depth from object boundaries rather than from fringe phase. On a photorealistic synthetic benchmark (15,600 fringe images, 50 objects at 1.5-2.1 m standoff), the best such UNet baseline plateaus at 14.54 mm object mean absolute error (MAE), and neither more data nor more capacity removes the shortcut, because neither changes the hypothesis space the optimizer searches. We introduce PhiCalNet, which outputs a wrapped-phase representation $(\sinφ, \cosφ)$ and maps it to depth through a fixed differentiable calibration layer, removing the shape-prior solution architecturally rather than by a loss penalty. Because the single-shot mapping is non-injective without fringe order, PhiCalNet takes the fringe order as auxiliary input, an assumption a sensitivity analysis shows tolerates realistic decoding error; a physics-informed (PINN) baseline with the same physics as a soft penalty yields no gain, isolating the architectural choice as the operative factor. PhiCalNet reduces object MAE 3.3x to 4.46 mm, its residual confined to 0.103% of pixels at the $\pmπ$ wrap discontinuity, and a three-frame extension reaches 1.16 mm. Two checks agree: interpretability makes phase the most decodable internal feature, and pixel-wise conformal uncertainty quantification, to our knowledge the first for FPP, localizes error at the same discontinuity, where rejecting the top 5% of pixels by snapshot disagreement cuts root-mean-square error by 64% versus 3.5% for the baseline.

## 英语词汇
- **interpretability**：可解释性（IELTS）
- **uncertainty**：不确定性（IELTS）
- **benchmark**：基准测试（CET6/IELTS）
