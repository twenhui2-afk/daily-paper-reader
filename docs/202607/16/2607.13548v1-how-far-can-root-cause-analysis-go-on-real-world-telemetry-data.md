---
title: How Far Can Root Cause Analysis Go on Real-World Telemetry Data?
source: arxiv
title_zh: How Far Can Root Cause Analysis Go on Real-World Telemetry Data?
abstract_zh: "We further introduce automated rule mining pipeline that systematically extracts discrimination rule。\n\n核心做法：To diagnose where failures originate，作者引入 reverse reasoning agent that，given correct answer，identifi。\n\n主要结果：OpenRCA dataset exemplifies these challenges：it is large-scale，multimodal，与 lacks detailed domain kn。\n\n值得关注的点在于：Reasoning 性能 remains practically bounded even when evidence extraction is perfect：scaffold engineeri。"
abstract_en: "Identifying root causes in production microservice failures requires reasoning over large-scale, multimodal telemetry spanning metrics, logs, and traces, a problem that has proved resistant to both classical and LLM-based approaches. The OpenRCA dataset exemplifies these challenges: it is large-scale, multimodal, and lacks detailed domain knowledge, and yields consistently low accuracy across all existing methods. We show that classical causal discovery methods and existing LLM-based multi-agent systems fail to reliably identify root causes on this benchmark, and present a Structured Multi-Agent RCA pipeline that substantially outperforms existing LLM-based and classical baselines, supporting both domain-knowledge and knowledge-free operating modes. To diagnose where failures originate, we introduce a reverse reasoning agent that, given the correct answer, identifies which signals in the extracted anomalies support it and determines whether Stage~1 had access to those signals, classifying each failure as Reasoning Gap (evidence present but unused) or Data Ambiguity (evidence genuinely absent). This analysis reveals that the required evidence is present in the vast majority of failures: the bottleneck is not data access but the agent's ability to reason over it correctly. We further introduce an automated rule mining pipeline that systematically extracts discrimination rules from reverse reasoning reports, reducing reliance on manual knowledge curation. Across all configurations, model reasoning capability and domain knowledge are the primary constraints: stronger models embed more domain expertise, and explicit knowledge injection partially compensates for this gap. Reasoning performance remains practically bounded even when evidence extraction is perfect: scaffold engineering and better data pipelines alone cannot close this gap; progress requires improvements at the model level."
authors: "Athira Gopal, Ashwanth Krishnan"
date: 2026-07-15
journal: cs.AI
source_link: "https://arxiv.org/pdf/2607.13548v1"
pdf: "https://arxiv.org/pdf/2607.13548v1"
score: 8.4
tldr: 围绕《How Far Can Root Cause A...》；To diagnose where failures origi...；OpenRCA dataset exemplifies these ch...
tldr_cn: 围绕《How Far Can Root Cause A...》；To diagnose where failures origi...；OpenRCA dataset exemplifies these ch...
tldr_en: "Identifying root causes in production microservice failures requires reasoning over large-scale, multimodal telemetry spanning metrics, logs, and traces, a problem that has proved resistant to both classical and LLM-based approaches. The OpenRCA dataset exemplifies these challenges: it is large-scale, multimodal, and lacks detailed domain knowledge, and yields consistently low accuracy across all existing methods."
selection_source: fresh_fetch
vocabulary: ["multimodal | 多模态 | 学术高频", "benchmark | 基准测试 | CET6/IELTS"]
motivation: We further introduce automated rule mining pipeline that systematically extracts discrimination rule。
method: 核心做法：To diagnose where failures originate，作者引入 reverse reasoning agent that，given correct answer，identifi。
result: 主要结果：OpenRCA dataset exemplifies these challenges：it is large-scale，multimodal，与 lacks detailed domain kn。
conclusion: 值得关注的点在于：Reasoning 性能 remains practically bounded even when evidence extraction is perfect：scaffold engineeri。
---

## 摘要
We further introduce automated rule mining pipeline that systematically extracts discrimination rule。

核心做法：To diagnose where failures originate，作者引入 reverse reasoning agent that，given correct answer，identifi。

主要结果：OpenRCA dataset exemplifies these challenges：it is large-scale，multimodal，与 lacks detailed domain kn。

值得关注的点在于：Reasoning 性能 remains practically bounded even when evidence extraction is perfect：scaffold engineeri。

## Abstract
Identifying root causes in production microservice failures requires reasoning over large-scale, multimodal telemetry spanning metrics, logs, and traces, a problem that has proved resistant to both classical and LLM-based approaches. The OpenRCA dataset exemplifies these challenges: it is large-scale, multimodal, and lacks detailed domain knowledge, and yields consistently low accuracy across all existing methods. We show that classical causal discovery methods and existing LLM-based multi-agent systems fail to reliably identify root causes on this benchmark, and present a Structured Multi-Agent RCA pipeline that substantially outperforms existing LLM-based and classical baselines, supporting both domain-knowledge and knowledge-free operating modes. To diagnose where failures originate, we introduce a reverse reasoning agent that, given the correct answer, identifies which signals in the extracted anomalies support it and determines whether Stage~1 had access to those signals, classifying each failure as Reasoning Gap (evidence present but unused) or Data Ambiguity (evidence genuinely absent). This analysis reveals that the required evidence is present in the vast majority of failures: the bottleneck is not data access but the agent's ability to reason over it correctly. We further introduce an automated rule mining pipeline that systematically extracts discrimination rules from reverse reasoning reports, reducing reliance on manual knowledge curation. Across all configurations, model reasoning capability and domain knowledge are the primary constraints: stronger models embed more domain expertise, and explicit knowledge injection partially compensates for this gap. Reasoning performance remains practically bounded even when evidence extraction is perfect: scaffold engineering and better data pipelines alone cannot close this gap; progress requires improvements at the model level.

## 英语词汇
- **multimodal**：多模态（学术高频）
- **benchmark**：基准测试（CET6/IELTS）
