Title: An O(log n)-Approximation for Three-Terminal Reachability-Preserving Minimum Edge Cut

URL Source: https://arxiv.org/pdf/2607.20114v1

Published Time: Thu, 23 Jul 2026 00:53:45 GMT

Number of Pages: 14

Markdown Content:
# An O(log n)-Approximation for Three-Terminal Reachability-Preserving Minimum Edge Cut 

Qi Duan, Carnegie Mellon University 

Abstract 

In the three-terminal Reachability-Preserving Minimum Edge Cut problem, the input is an undirected edge-weighted graph with terminals s1, s 2, t . The objective is to delete a minimum-cost set of edges that separates t from both s1 and s2, while preserving connectivity between s1 and 

s2.We give a polynomial-time O( log n)-approximation algorithm. The algorithm uses a prob-abilistic distribution of cut-dominating decomposition trees. A direct transfer of a connected tree solution to the original graph is not valid because a connected tree cluster may induce a disconnected vertex set in the graph. We overcome this obstruction by expanding every rooted tree cluster into the connected components it induces in the original graph. These components form a node-weighted auxiliary graph. A minimum node-weighted path in this auxiliary graph produces a connected feasible source side. The main structural observation is that the total graph-boundary cost of all connected components of a rooted tree cluster is no greater than the capacity of the corresponding tree edge. This permits the auxiliary path to be compared with a tree cut separating an optimal preserved 

s1-s2 path from t. Combining this comparison with the expected O(log n) cut distortion of the decomposition trees proves the approximation guarantee. 

# 1 Introduction 

The three-terminal Reachability-Preserving Minimum Edge Cut problem, abbreviated RPMEC, combines a separation requirement with a protected connectivity requirement. Given an undirected graph with terminals s1, s 2, t , one must separate t from both source terminals while ensuring that the two source terminals remain mutually reachable. The connectivity requirement distinguishes RPMEC from ordinary minimum cut. A cut of small capacity may place s1 and s2 on the same side while leaving them in different connected components. Consequently, methods that preserve only cut capacities do not automatically preserve feasibility. Previous work obtained an O(√n)-approximation through a root-linear or modular-surrogate formulation [2]. A natural question is whether a logarithmic approximation is possible. We answer this question affirmatively. Our algorithm uses the probabilistic cut-tree decomposition framework of R¨ acke [ 3] and its capacity-mapping interpretation [ 1]. These results provide a polynomial-size distribution over decomposition trees such that every support tree dominates all graph cuts, while each fixed graph cut has only O(log n) expected tree-cut capacity. The central technical difficulty is that a tree cluster need not induce a connected subgraph of the original graph. Our solution is to replace each rooted tree cluster by all connected components of its induced graph. We create one auxiliary node for each such component, weighted by its graph-boundary cost. Two auxiliary nodes are adjacent when the corresponding components intersect or are joined by a graph edge. 1arXiv:2607.20114v1 [cs.DS] 22 Jul 2026 A shortest auxiliary path from a component containing s1 to a component containing s2 yields a connected union in the original graph. The total boundary of this union is at most the auxiliary-path weight. The key accounting identity is the following. If Ce is the graph vertex set below a rooted tree edge e, then X

> K∈cc( G[Ce])

cG(δG(K)) = cG(δG(Ce)) .

Since the decomposition-tree edge capacity dominates cG(δG(Ce)), all components generated by e

can be charged to that single tree edge. This component-budget property allows us to compare the auxiliary shortest path with any tree cut separating t from a fixed s1-s2 graph path. Choosing such a path inside an optimal RPMEC source side completes the O(log n) analysis. Our main result is as follows. 

Theorem 1. Weighted undirected three-terminal RPMEC with nonnegative rational edge costs admits a polynomial-time O(log n)-approximation algorithm. 

# 2 Problem Definition and Source-Side Formulation 

Let 

G = ( V, E )be an undirected graph with a nonnegative rational edge-cost function 

c : E → Q≥0.

Let s1, s 2, t ∈ V be distinct terminals. For U ⊆ V , define 

δG(U ) = {uv ∈ E : |{ u, v } ∩ U | = 1 },

and let 

cG(δG(U )) = X 

> e∈δG(U)

c(e).

Definition 2 (Three-terminal RPMEC) . A feasible RPMEC solution is an edge set F ⊆ E such that, in G − F ,(i) s1 and s2 are connected; and (ii) t is disconnected from both s1 and s2.The objective is to minimize 

c(F ) = X

> e∈F

c(e).

The problem has a useful equivalent formulation in terms of a connected source side. 

Lemma 3 (Connected source-side formulation) . The optimum RPMEC value equals 

OPT = min {cG(δG(U )) : s1, s 2 ∈ U, t / ∈ U, G [U ] is connected } . (1) 2Proof. Let U ⊆ V satisfy the conditions in (1) . Deleting δG(U ) separates every vertex of U from 

V \ U , and therefore separates t / ∈ U from s1, s 2 ∈ U . Since G[U ] is connected, s1 and s2 remain connected. Hence δG(U ) is a feasible RPMEC solution. Conversely, let F be any feasible RPMEC solution. Let U be the connected component of G − F

containing s1 and s2. Feasibility implies t / ∈ U . Every graph edge in δG(U ) must belong to F ;otherwise it would join U to another component of G − F . Therefore, 

cG(δG(U )) ≤ c(F ).

Taking the minimum over feasible edge sets and feasible source sides proves the equality. 

Remark 4 (Feasibility test) . The instance is feasible if and only if s1 and s2 are connected in G − t.This condition can be checked before running the approximation algorithm. 

# 3 Preprocessing Zero-Cost Edges 

The decomposition-tree theorem is most commonly stated for strictly positive edge capacities. We reduce the nonnegative-cost case to that setting without changing the asymptotic approximation guarantee. Let 

E+ = {e ∈ E : c(e) > 0},

and let 

G+ = ( V, E +).

Denote the connected component of a vertex v in G+ by D(v). Construct a quotient graph Z whose vertices are the connected components of G+. Two distinct quotient vertices are adjacent whenever a zero-cost edge of G joins their corresponding components. 

Lemma 5 (Detection of zero-cost solutions) . There is a feasible RPMEC solution of cost zero if and only if 

D(s1) and D(s2)

are connected in 

Z − D(t).

Moreover, when such a solution exists, one can construct it in polynomial time. Proof. Suppose first that U is a feasible connected source side with 

cG(δG(U )) = 0 .

No positive-cost edge crosses δG(U ). Therefore, for every connected component D of G+, either 

D ⊆ U or D ∩ U = ∅. Thus U is a union of quotient vertices. Because G[U ] is connected, the quotient vertices represented in U induce a connected subgraph of Z. This subgraph contains D(s1) and D(s2) and excludes D(t). Hence D(s1) and D(s2) are connected in Z − D(t). Conversely, suppose that D(s1) and D(s2) are connected in Z − D(t). Let 

D0, D 1, . . . , D r

3be a quotient path between them that avoids D(t), and define 

U =

> r

[

> j=0

Dj .

Every Dj is connected through positive-cost edges, and consecutive components on the quotient path are joined by a zero-cost edge. Therefore, G[U ] is connected. The set U contains s1, s 2 and excludes t. Since U is a union of complete connected components of G+, no positive-cost edge leaves U . Every edge in δG(U ) consequently has cost zero, proving 

cG(δG(U )) = 0. Assume henceforth that no zero-cost feasible solution exists. Let ∆ = min {c(e) : c(e) > 0},

and let m = |E|. Since every feasible solution now contains at least one positive-cost edge, OPT ≥ ∆. (2) Define a strictly positive perturbed cost function 

cε(e) = 

(c(e), c(e) > 0,ε, c(e) = 0 , ε = ∆

m . (3) For every edge set F ⊆ E,

c(F ) ≤ cε(F ) ≤ c(F ) + mε = c(F ) + ∆ . (4) In particular, if OPT ε is the optimum under cε, then OPT ε ≤ OPT + ∆ ≤ 2OPT . (5) Therefore, a ρ-approximation under cε has original cost at most 

ρOPT ε ≤ 2ρOPT . (6) Thus it suffices to prove the approximation theorem for strictly positive capacities. The factor of two is absorbed into the O(log n) bound. 

# 4 Cut-Dominating Decomposition Trees 

We now state the decomposition-tree theorem used as a black box. A capacitated decomposition tree 

T = ( VT , E T , w )is a tree whose leaves are in bijection with V . Internal tree vertices need not correspond to graph vertices. For A ⊆ V , define 

λT (A) = min 

(X

> e∈F

we : F ⊆ ET separates all leaves of A from all leaves of V \ A

)

. (7) Thus λT (A) is the minimum tree-cut capacity realizing the leaf partition A, V \ A.4Definition 6 (Cut-dominating decomposition tree) . A decomposition tree T dominates G if 

cG(δG(A)) ≤ λT (A) for every A ⊆ V. (8) 

Theorem 7 (Probabilistic decomposition-tree theorem [ 3 , 1]) . Let G = ( V, E, c ) be an undirected graph with strictly positive capacities. In polynomial time, one can construct a distribution with polynomial support 

D = {(pi, T i) : i = 1 , . . . , q },

where pi ≥ 0, P 

> i

pi = 1 , and each 

Ti = ( V Ti , E Ti , w i)

is a capacitated decomposition tree satisfying the following properties. (i) The leaves of Ti are in bijection with V .(ii) Every tree edge e ∈ ETi induces a bipartition 

Ci,e , V \ Ci,e 

of the graph vertices through the corresponding partition of the tree leaves. (iii) The capacity assigned to e is 

wi(e) = cG(δG(Ci,e )) . (9) 

(iv) Every tree Ti dominates every graph cut: 

cG(δG(A)) ≤ λTi (A) for every A ⊆ V. (10) 

(v) For every fixed A ⊆ V , qX

> i=1

piλTi (A) ≤ ρ c G(δG(A)) , (11) 

where ρ = O(log n).

Remark 8 (Required form of the black box) . The proof uses more than the existence of an arbitrary dominating tree. It uses decomposition trees whose leaves correspond to the vertices of G, so that every rooted tree edge defines a concrete graph vertex cluster. This cluster structure is needed to form the induced graphs G[Ce] and their connected components. 

Remark 9 (Nonnegative costs) . The strictly positive hypothesis of Theorem 7 causes no loss of generality. By Lemma 5, an optimum-zero instance can be recognized and solved exactly. Otherwise, the perturbation in (3) converts the instance to one with strictly positive capacities and loses at most an additional factor of two. The next elementary observation explains why decomposition trees with edge capacities as in (9) dominate graph cuts. 

Lemma 10. Suppose every tree edge e has capacity 

we = cG(δG(Ce)) ,

where Ce, V \ Ce is its induced leaf partition. Then 

cG(δG(A)) ≤ λT (A)

for every A ⊆ V .

5Proof. Let F ⊆ ET be any tree-edge set separating the leaves in A from the leaves in V \ A.Consider a graph edge uv ∈ δG(A). The leaves corresponding to u and v lie on opposite sides of the required leaf partition. Their unique tree path must therefore contain at least one edge e ∈ F .Equivalently, uv ∈ δG(Ce) for at least one e ∈ F . Hence every graph edge in δG(A) is counted at least once in X

> e∈F

cG(δG(Ce)) = X

> e∈F

we.

Therefore, 

cG(δG(A)) ≤ X

> e∈F

we.

Minimizing over feasible tree-edge sets F proves the claim. 

Remark 11. The expected guarantee in (11) is needed only for the single fixed cut U ⋆ corresponding to an optimal RPMEC solution. We do not require one tree to approximate all graph cuts from above within O(log n) simultaneously. 

# 5 Rooted Clusters and Component Budgets 

Fix one decomposition tree 

T = ( VT , E T , w )from the support of D. Root T at the leaf corresponding to t.For each tree edge e, let 

Ce ⊆ V \ { t}

denote the graph vertices whose leaves lie in the component of T − e not containing the root leaf t.Let 

Ke = cc( G[Ce]) be the collection of vertex sets of the connected components of the induced graph G[Ce]. For every K ∈ K e, define its component cost by 

b(K) = cG(δG(K)) . (12) The following identity is the principal structural fact used by the algorithm. 

Lemma 12 (Component-budget lemma) . For every tree edge e,

X

> K∈K e

cG(δG(K)) = cG(δG(Ce)) ≤ we. (13) 

Proof. Distinct sets in Ke are connected components of the induced graph G[Ce]. Thus no graph edge has endpoints in two different members of Ke.It follows that every edge leaving a component K ∈ K e also leaves Ce. Conversely, every edge leaving Ce leaves exactly one connected component of G[Ce]. Therefore, 

X

> K∈K e

cG(δG(K)) = cG(δG(Ce)) .

Since cutting the single tree edge e separates the leaves of Ce from the leaves of V \ Ce,

λT (Ce) ≤ we.

6Tree domination gives 

cG(δG(Ce)) ≤ λT (Ce).

Combining the two inequalities proves cG(δG(Ce)) ≤ we.

# 6 The Auxiliary Component Graph 

For the fixed rooted decomposition tree T , construct a node-weighted auxiliary graph HT .For every tree edge e ∈ ET and every component K ∈ K e, create an auxiliary node xe,K . Assign it node weight 

b(xe,K ) = cG(δG(K)) . (14) Two component nodes xe,K and xf,L are adjacent whenever at least one of the following holds: 

K ∩ L̸ = ∅, (15) or 

EG(K, L )̸ = ∅, (16) where EG(K, L ) denotes the set of graph edges with one endpoint in K and the other in L.Add two zero-weight auxiliary terminals a and b. Connect a to every component node whose component contains s1, and connect b to every component node whose component contains s2.Let QT be a minimum node-weighted a-b path in HT . Define 

WT = [   

> xe,K ∈V(QT)

K. (17) The candidate returned for tree T is 

FT = δG(WT ). (18) A node-weighted shortest path can be computed by splitting every component node into an entrance node and an exit node connected by an edge whose weight equals the component-node weight. 

Remark 13 (Existence of an auxiliary a-b path) . If s1 and s2 are connected in G − t, then a and b

are connected in HT .To see this, let 

P = ( v0 = s1, v 1, . . . , v ℓ = s2)be a path in G − t. For each vj , consider the tree edge incident to the leaf corresponding to vj .Because vj̸ = t, the rooted cluster below that edge is the singleton {vj }, so HT contains a component node representing {vj }.For each j, the graph edge vj vj+1 makes the corresponding component nodes adjacent in HT .The first node is adjacent to a, and the final node is adjacent to b. Hence these nodes form an a-b

walk in HT , which contains a simple a-b path. 7Algorithm 1 Component expansion for one decomposition tree 

Require: Graph G = ( V, E, c ), terminals s1, s 2, t , decomposition tree T

Ensure: A feasible RPMEC cut FT 

> 1:

Root T at the leaf corresponding to t 

> 2:

Initialize an empty node-weighted graph HT 

> 3:

for each tree edge e ∈ ET do  

> 4:

Let Ce be the leaf set below e, away from t 

> 5:

Compute Ke = cc( G[Ce])  

> 6:

for each K ∈ K e do  

> 7:

Add auxiliary node xe,K with weight cG(δG(K))  

> 8:

end for  

> 9:

end for  

> 10:

for each pair xe,K , x f,L do  

> 11:

if K ∩ L̸ = ∅ or EG(K, L )̸ = ∅ then  

> 12:

Add edge xe,K xf,L to HT 

> 13:

end if  

> 14:

end for  

> 15:

Add zero-weight auxiliary nodes a, b  

> 16:

Join a to all xe,K with s1 ∈ K 

> 17:

Join b to all xe,K with s2 ∈ K 

> 18:

Compute a minimum node-weighted a-b path QT 

> 19:

WT ← S    

> xe,K ∈V(QT)

K 

> 20:

return FT = δG(WT )

# 7 Feasibility and Cost of a Tree Candidate 

We first establish that the candidate generated from every tree is feasible. 

Lemma 14 (Feasibility) . The set WT satisfies 

s1, s 2 ∈ WT , t / ∈ WT , G[WT ] is connected .

Consequently, FT = δG(WT ) is a feasible RPMEC solution. Proof. The first component node of QT after a corresponds to a component containing s1, while the final component node before b corresponds to a component containing s2. Therefore, 

s1, s 2 ∈ WT .

Every component used in HT is contained in a rooted cluster Ce ⊆ V \ { t}. Hence t / ∈ WT .Every component K represented by a node of HT is connected in G. Consecutive component nodes on QT correspond either to intersecting vertex sets or to vertex sets joined by a graph edge. The union of two such connected sets is connected. Inductively, the union of all component sets represented on QT is connected. Thus G[WT ] is connected. The claim follows from Lemma 3. The auxiliary path weight upper-bounds the graph boundary of the returned union. 

Lemma 15 (Union-bound lemma) . Let 

QT = ( a, x e1,K 1 , . . . , x er ,K r , b ).

8Then 

cG(δG(WT )) ≤

> r

X

> j=1

cG(δG(Kj )) = b(QT ). (19) 

Proof. Consider any graph edge in 

δG



> r

[

> j=1

Kj

 .

One of its endpoints belongs to at least one set Kj , while its other endpoint belongs to none of the sets K1, . . . , K r. Therefore, the edge belongs to δG(Kj ) for at least one j.Thus every edge contributing to the left-hand side of (19) is counted at least once on the right-hand side. Since all costs are nonnegative, 

cG(δG(WT )) ≤

> r

X

> j=1

cG(δG(Kj )) .

The final equality follows from the definition of the auxiliary node weights. 

# 8 Comparison with a Preserved Graph Path 

Let P be an s1-s2 path in G − t. Define the rooted tree-separation value 

ϕT (P ) = min 

(X

> e∈F

we : F ⊆ ET , F separates the root leaf t from every leaf in V (P )

)

. (20) The next lemma is the main comparison result. 

Lemma 16 (Path-comparison lemma) . For every s1-s2 path P ⊆ G − t,

cG(δG(WT )) ≤ ϕT (P ). (21) 

Proof. Let F ⊆ ET be an optimum edge set in (20) . Among all optimum sets, choose F inclusion minimal. Orient all tree edges away from the root leaf t.

Step 1: F is an antichain. Suppose that e, f ∈ F , where f is a strict descendant of e. Every root-to-leaf path containing f also contains e. Therefore, deleting f from F does not restore connectivity between t and any vertex of P . This contradicts inclusion minimality. Thus no edge in F is a descendant of another edge in F . Consequently, the rooted clusters 

{Ce : e ∈ F } are pairwise disjoint. 

Step 2: the rooted clusters cover the graph path. For every v ∈ V (P ), the unique tree path from the root leaf t to the leaf v contains at least one edge of F . Thus v belongs to Ce for at least one e ∈ F . Since the clusters are pairwise disjoint, each v ∈ V (P ) belongs to exactly one rooted cluster Ce with e ∈ F .9Step 3: the selected components define an auxiliary walk. For each e ∈ F , define 

Se = {K ∈ K e : K ∩ V (P )̸ = ∅},

and let 

S = [

> e∈F

Se.

Write 

P = ( v0 = s1, v 1, . . . , v ℓ = s2).

By the preceding step, every vj lies in a unique rooted cluster Cej , where ej ∈ F . Let Kj be the unique connected component of G[Cej ] containing vj .Consider the sequence 

a, x e0,K 0 , x e1,K 1 , . . . , x eℓ,K ℓ , b. (22) The first component contains s1, so ax e0,K 0 ∈ E(HT ). Similarly, the final component contains s2,so xeℓ,K ℓ b ∈ E(HT ). For every j ∈ { 0, . . . , ℓ − 1}, either Kj = Kj+1 , or the graph edge vj vj+1 has one endpoint in 

Kj and the other in Kj+1 . In the latter case, 

xej ,K j xej+1 ,K j+1 ∈ E(HT ).

After suppressing consecutive repetitions, the sequence in (22) is therefore an a-b walk in HT .Every walk in an undirected graph contains a simple path between its endpoints. Let Q′ be a simple 

a-b path obtained from this walk by deleting closed subwalks. 

Step 4: charge the auxiliary path to the tree cut. Since QT is a minimum node-weighted 

a-b path, 

b(QT ) ≤ b(Q′).

Because all auxiliary node weights are nonnegative and every node of Q′ corresponds to a component in some Se,

b(Q′) ≤ X

> e∈F

X

> K∈S e

cG(δG(K)) .

Since Se ⊆ K e,

b(QT ) ≤ X

> e∈F

X

> K∈K e

cG(δG(K)) .

By Lemma 12, X

> K∈K e

cG(δG(K)) ≤ we.

Consequently, 

b(QT ) ≤ X

> e∈F

we = ϕT (P ).

Finally, Lemma 15 gives 

cG(δG(WT )) ≤ b(QT ) ≤ ϕT (P ).

10 9 Approximation Guarantee 

We now prove the main theorem. 

Theorem 17. There is a polynomial-time algorithm for weighted undirected three-terminal RPMEC with nonnegative rational edge costs that returns a solution of cost at most O(log n)OPT .Proof. We first assume that all edge costs are strictly positive. Let U ⋆ be an optimum connected source side in (1). Thus 

s1, s 2 ∈ U ⋆, t / ∈ U ⋆, G[U ⋆] is connected ,

and 

cG(δG(U ⋆)) = OPT .

Since G[U ⋆] is connected, it contains an s1-s2 path 

P ⋆ ⊆ G[U ⋆].

Because t / ∈ U ⋆, this path lies in G − t.Fix a decomposition tree Ti in the support of D. A tree-edge set separating all leaves of U ⋆ from all leaves of V \ U ⋆ separates, in particular, the root leaf t ∈ V \ U ⋆ from every leaf in V (P ⋆) ⊆ U ⋆.Therefore, 

ϕTi (P ⋆) ≤ λTi (U ⋆). (23) Applying Lemma 16 to P ⋆ gives 

cG(δG(WTi )) ≤ ϕTi (P ⋆) ≤ λTi (U ⋆). (24) Multiply (24) by pi and sum over the support: 

> q

X

> i=1

picG(δG(WTi )) ≤

> q

X

> i=1

piλTi (U ⋆).

By the expected-distortion guarantee in Theorem 7, 

> q

X

> i=1

piλTi (U ⋆) ≤ ρ c G(δG(U ⋆)) = ρOPT ,

where ρ = O(log n). Therefore, at least one support tree Ti satisfies 

cG(δG(WTi )) ≤ ρOPT .

The algorithm evaluates every tree in the polynomial-size support and returns the cheapest candidate. Hence its output satisfies ALG ≤ ρOPT = O(log n)OPT .

For an instance with nonnegative costs, Lemma 5 first detects and solves exactly the case OPT = 0. Otherwise, apply the strictly positive analysis to the perturbed cost function cε. Let F

be the returned solution. By (6), 

c(F ) ≤ cε(F ) ≤ ρOPT ε ≤ 2ρOPT .

Since ρ = O(log n), the approximation ratio remains O(log n). The complete algorithm is summarized below. 11 Algorithm 2 O(log n)-approximation for weighted undirected RPMEC 

Require: Graph G = ( V, E, c ), terminals s1, s 2, t 

Ensure: A feasible RPMEC cut, or a declaration of infeasibility  

> 1:

if s1 and s2 are disconnected in G − t then  

> 2:

Report that the instance is infeasible  

> 3:

end if  

> 4:

Construct the positive-edge component quotient Z 

> 5:

if D(s1) and D(s2) are connected in Z − D(t) then  

> 6:

Construct the zero-cost source side U as in Lemma 5  

> 7:

return δG(U ) 

> 8:

end if  

> 9:

Set ∆ ← min {c(e) : c(e) > 0} and ε ← ∆/|E| 

> 10:

Define cε as in (3)  

> 11:

Construct the polynomial-support decomposition D = {(pi, T i) : i = 1 , . . . , q } for ( G, c ε) using Theorem 7  

> 12:

Fbest ← E 

> 13:

Cbest ← +∞ 

> 14:

for i = 1 , . . . , q do  

> 15:

Run Algorithm 1 on ( G, c ε, T i) to obtain Fi 

> 16:

if c(Fi) < C best then  

> 17:

Fbest ← Fi 

> 18:

Cbest ← c(Fi) 

> 19:

end if  

> 20:

end for  

> 21:

return Fbest 

# 10 Running Time 

Let NT = |ET | for one support tree T .For every tree edge e, the induced graph G[Ce] has at most n connected components. Therefore, the auxiliary graph contains at most nN T component nodes. All connected components of G[Ce] can be computed in polynomial time. Their boundary costs can be obtained by scanning graph edges. The auxiliary adjacency relation can be constructed naively in polynomial time by checking component intersections and graph edges. A more efficient implementation uses vertex-to-component incidence lists, a scan of every graph edge to generate component adjacencies, and duplicate-edge removal. The minimum node-weighted auxiliary path is computable in polynomial time by node splitting followed by Dijkstra’s algorithm, since all weights are nonnegative. The decomposition theorem supplies polynomially many trees, each of polynomial size. Therefore, evaluating every support tree and returning the best candidate takes polynomial time. 

# 11 A Randomized Sampling Variant 

When the decomposition is accessed through a sampling procedure rather than an explicit support, the analysis gives 

ET [cG(δG(WT ))] ≤ ρOPT .

12 By Markov’s inequality, Pr [ cG(δG(WT )) > 2ρOPT] ≤ 12 .

After sampling 

r =



log 2

1

η



independent trees and returning the cheapest candidate, the algorithm returns a solution of cost at most 2 ρOPT with probability at least 1 − η.The explicit polynomial-support version is preferable for the main theorem because it gives a deterministic choice of the best candidate once the decomposition distribution has been constructed. 

# 12 Why Component Expansion Is Necessary 

A direct tree-based approach would select a connected subtree or a rooted tree cluster containing s1

and s2, and then interpret its leaves as a graph vertex set. This is not sufficient: the induced graph on those leaves may be disconnected. The component-expansion construction resolves this difficulty without increasing the tree charge. For every rooted cluster Ce,

X

> K∈cc( G[Ce])

cG(δG(K)) = cG(δG(Ce)) ≤ we.

Thus splitting a disconnected tree cluster into its graph-connected components preserves the total available budget. The auxiliary graph then restores global connectivity by finding a minimum-cost sequence of components whose union joins s1 to s2. Importantly, this construction does not require any individual rooted cluster to be connected in G.This also explains why the approach avoids the fractional-path dilution that arises in polymatroid or coupled-flow relaxations. The auxiliary solution commits to actual connected graph components and charges their full graph boundaries. A shared bottleneck cannot be fractionally divided among several alternative paths. 

# 13 Extensions and Limitations 

More than two protected terminals. Suppose a set R of protected terminals must remain connected while being separated from t. The component nodes can again be used to construct an auxiliary node-weighted Steiner-tree instance connecting the components containing terminals in R.This suggests a polylogarithmic approximation, although the resulting factor and comparison proof require separate analysis. 

Directed graphs. The proof relies fundamentally on undirected graph cuts, undirected connected components, and undirected cut-dominating decomposition trees. It does not extend directly to directed RPMEC. 

Implementation of the tree distribution. The approximation theorem uses the standard polynomial-time construction of a polynomial-support capacity-tree distribution. For practical implementations, approximate or sampled decomposition trees may be preferable. Their empirical quality for RPMEC remains an interesting experimental question. 13 14 Conclusion 

We presented an O( log n)-approximation algorithm for weighted undirected three-terminal RPMEC. The algorithm combines probabilistic cut-dominating decomposition trees with a connected-component expansion. For every rooted tree cluster, its connected components in the original graph have total boundary cost no larger than the capacity of the corresponding tree edge. A minimum node-weighted path through these components produces a connected feasible RPMEC source side. The approximation proof compares this auxiliary path with a tree cut separating an optimal preserved s1-s2 path from t. The expected O(log n) distortion of the decomposition-tree distribution then yields the claimed guarantee. The component-expansion principle may be useful more generally for cut problems in which the retained side must satisfy an internal connectivity constraint that is not preserved directly by a tree embedding. 

# References 

[1] Reid Andersen and Uriel Feige. Interchanging distance and capacity in probabilistic mappings. 

CoRR , abs/0907.3631, 2009. [2] Anonymous Authors. Approximation algorithms for reachability-preserving minimum edge cut, 2026. Manuscript under review. [3] Harald R¨ acke. Optimal hierarchical decompositions for congestion minimization in networks. In 

Proceedings of the 40th Annual ACM Symposium on Theory of Computing , STOC ’08, pages 255–263. ACM, 2008. 14