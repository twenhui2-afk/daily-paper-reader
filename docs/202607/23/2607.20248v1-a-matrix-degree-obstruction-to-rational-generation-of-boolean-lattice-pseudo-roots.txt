Title: A Matrix-Degree Obstruction to Rational Generation of Boolean-Lattice Pseudo-Roots

URL Source: https://arxiv.org/pdf/2607.20248v1

Published Time: Thu, 23 Jul 2026 01:03:46 GMT

Number of Pages: 12

Markdown Content:
# A Matrix-Degree Obstruction to Rational Generation of Boolean-Lattice Pseudo-Roots 

Mahesh Ramani ∗

Abstract 

For the neighborhood seed associated with the four-vertex path P4, the diamond operations do not recover all Boolean-lattice pseudo-roots. The corresponding question for unrestricted rational operations in the free skew field is subtler: the seed map has an invertible linearization and therefore a unique formal inverse near every generic scalar point. We prove that this formal inverse is not free rational. A symmetric one-parameter curve of 2 × 2 matrix outputs has a formal inverse whose coefficient field contains an element of degree three over Q(t). An exact elimination in a quadratic Pauli algebra produces the irreducible cubic. Its conjugate inverse branches are unramified, forcing the generic matrix degree of the seed map to be at least three in every size n ≥ 2. This contradicts the degree-one consequence of any free rational inverse. The same matrix-degree argument, without specializing a hypothetical inverse, extends the obstruction to every graph containing an induced P4.

2020 Mathematics Subject Classification. Primary 16K40; Secondary 16S10, 16S85, 14E05, 05C75. 

Keywords. Pseudo-roots; free skew fields; noncommutative rational functions; rational maps; matrix degree; cographs; exact elimination. 

# 1 Introduction 

Let Bn be the Boolean lattice on [n] = {1, . . . , n }. Its cover edge A → A ∪ { i} is labelled by a pseudo-root xA,i . This Boolean-lattice model and its diamond relations arise from factorizations of noncommutative polynomials; see [ 3 , 4, 9]. If two lower pseudo-roots are denoted by x and y, the upper pseudo-roots in the corresponding diamond are obtained from 

C(y, x ) = ( y − x)x(y − x)−1, D(y, x ) = ( y − x)−1x(y − x). (1) All free rational expressions below are interpreted in the free skew field, or on matrix tuples for which every displayed inverse exists. We use the standard universal-field and matrix-evaluation framework for noncommutative rational functions [1, 6]. For a graph G on [n], its neighborhood seed consists of the n pseudo-roots 

YG = {xNG(i),i : i ∈ [n]}.

At the level of diamond closure, the cographs are precisely the graphs for which YG generates all pseudo-roots [8, Theorem 1.1]. Cographs are equivalently the finite graphs with no induced P4 [2].  

> ∗Email: mahesh.ramani.iyer@gmail.com

1arXiv:2607.20248v1 [math.CO] 22 Jul 2026 The first graph outside this class is the path P4. Write its bottom pseudo-roots as a, b, c, d . Its seed map is 

FP4 (a, b, c, d ) = (C(b, a ), C (C(a, c ), C (a, b )) , C (C(b, d ), C (b, c )) , C (c, d )). (2) The main result is the following. 

Theorem 1.1. Let K be a field of characteristic zero. If 

p = C(b, a ), q = C(C(a, c ), C (a, b )) ,r = C(C(b, d ), C (b, c )) , s = C(c, d ),

then 

K⟨p, q, r, s ⟩rat ⊊ K⟨a, b, c, d ⟩rat .

Equivalently, FP4 has no free rational inverse. 

The proof uses matrix evaluations, but not merely a failure at one fixed matrix size. For every 

n ≥ 2, the rational map induced by FP4 on four n × n matrices has generic degree at least three. A finite collection of free rational recovery expressions necessarily has a common matrix domain at some size, where it would give a rational left inverse and force generic degree one. This all-size argument avoids the polynomial-identity loophole inherent in a single small-matrix specialization. The only computer-assisted step is an exact resultant calculation. The ancillary file p4_matrix_ degree_certificate.py performs the calculation over Q, verifies the formal branch used to select the relevant factors, and checks the irreducibility specialization. No floating-point arithmetic or numerical root selection is used. 

# 2 Diamond inversion and the P4 reductions 

Lemma 2.1 (Diamond inversion) . Let 

u = C(y, x ), v = C(x, y ).

Then 

v − u = y − x, x = D(v, u ), y = D(u, v ).

Consequently, the rational transformations 

(x, y ) 7 −→ (C(y, x ), C (x, y )) and (u, v ) 7 −→ (D(v, u ), D (u, v )) 

are mutually inverse wherever their displayed inverses exist. Proof. Since x − y = −(y − x),

v = ( y − x)y(y − x)−1, u = ( y − x)x(y − x)−1.

Thus v − u = y − x. It follows that 

D(v, u ) = ( v − u)−1u(v − u) = ( y − x)−1(y − x)x(y − x)−1(y − x) = x. 

The proof of D(u, v ) = y is identical. Substituting these formulas back into C proves the final assertion. 2For P4 = 1 − 2 − 3 − 4, set 

a = x∅,1, b = x∅,2, c = x∅,3, d = x∅,4.

The four neighborhood seeds are 

p = x{2},1 = C(b, a ),q = x{1,3},2 = C(C(a, c ), C (a, b )) ,r = x{2,4},3 = C(C(b, d ), C (b, c )) ,s = x{3},4 = C(c, d ).

(3) Define the missing adjacent partners 

T = C(a, b ), W = C(d, c ). (4) 

Proposition 2.2 (Partner reduction) . The map FP4 is free rationally invertible if and only if T

and W are simultaneously free rational functions of p, q, r, s .Proof. If T and W are recoverable, Lemma 2.1 gives 

a = D(T, p ), b = D(p, T ), d = D(W, s ), c = D(s, W ).

Conversely, recovery of a, b, c, d gives T = C(a, b ) and W = C(d, c ).There is also a useful conjugator form of this reduction. Put 

u = b − a, v = c − d. 

The relation in Lemma 2.1 gives 

T − p = u, W − s = v, a = u−1pu, b = u−1pu + u, d = v−1sv, c = v−1sv + v. (5) Hence 

K⟨a, b, c, d ⟩rat = K⟨p, s, u, v ⟩rat ,

and partner recovery is equivalent to recovery of u and v.For completeness, define the second-layer partners 

M = x{1,2},3, N = x{3,4},2.

The relevant diamonds give 

T = D(M, q ), W = D(N, r ),

so Proposition 2.2 is equivalently a recovery problem for M, N . An exact two-variable consistency system is 

D(q, M ) = C(D(D(M, q ), p ), D (s, D (N, r ))) , (6) 

D(r, N ) = C(D(D(N, r ), s ), D (p, D (M, q ))) . (7) For the true partners, the left sides are respectively C(a, c ) and C(d, b ), so the equations hold. Conversely, suppose M, N solve (6)–(7). Define 

T = D(M, q ), W = D(N, r ), a = D(T, p ), b = D(p, T ), d = D(W, s ), c = D(s, W ).

The adjacent pairs reconstruct p, s by Lemma 2.1. The two consistency equations say 

D(q, M ) = C(a, c ), D(r, N ) = C(d, b ).

Applying the mutually inverse diamond transformations to the upper pairs (q, M ) and (r, N ) now reconstructs the middle seeds q, r . Thus every solution for which the displayed inverses exist gives a preimage of (p, q, r, s ).33 Formal invertibility and matrix degree 

We record the distinction between formal and rational inversion. Let λ1, . . . , λ g be distinct central scalars and write a matrix or free variable as xi = λi + Xi, where Xi has positive degree. 

Lemma 3.1 (First-order conjugation) . If x = λ + X, y = μ + Y , and λ̸ = μ are central scalars, then 

C(y, x ) = λ + X + O(2) , D(y, x ) = λ + X + O(2) .

Proof. Put δ = μ − λ and E = Y − X. Then 

(y − x)−1 = ( δ + E)−1 = δ−1 − δ−2E + O(2) .

Substitution into (y − x)x(y − x)−1 shows that the degree-zero and degree-one terms are λ and X;the terms involving λE cancel because λ and δ are central. The calculation for D is the same. 

Proposition 3.2 (Formal inverse) . At every scalar point 

(a, b, c, d ) = ( λ1, λ 2, λ 3, λ 4)

with distinct λi, the linear part of FP4 is the identity. Consequently, FP4 has a unique inverse in the completed noncommutative power-series algebra at that point. For every matrix size n, the induced rational map FP4,n has identity Jacobian there and is dominant and generically finite. Proof. Every use of C retains, to first order, the perturbation of its second argument by Lemma 3.1. Reading the four expressions in (3) from the inside out gives 

p = λ1 + A + O(2) , q = λ2 + B + O(2) , r = λ3 + G + O(2) , s = λ4 + D + O(2) .

An endomorphism of a complete filtered algebra with invertible linear part has a unique formal inverse, obtained recursively by degree. The same linearization on n × n matrix entries is the identity map on an affine space of dimension 4n2. A rational self-map with a nonsingular Jacobian at one point is dominant; equal source and target dimensions then imply generic finiteness. We shall use two elementary degree principles. Degrees are geometric degrees, computed after extension to an algebraic closure. Our conventions for rational maps, function fields, and birationality are standard; see [5, Chapter I, §4]. 

Lemma 3.3 (Simple points bound the generic degree) . Let f : X 99K Y be a dominant generically finite rational map between irreducible smooth varieties of the same dimension over a characteristic-zero field. If one geometric fiber contains r distinct points in the domain of f at which the Jacobian determinant is nonzero, then 

deg( f ) ≥ r. 

Proof. All data involved in the points and the map are defined over a finitely generated characteristic-zero field, which embeds in C. After this embedding, the complex inverse function theorem gives pairwise disjoint analytic neighborhoods of the r points and a common analytic neighborhood of their image on which each neighborhood supplies a distinct local inverse branch. A dominant generically finite map restricts, after deleting proper algebraic subsets of source and target, to a finite étale map of degree deg (f ). The deleted subsets and their inverse images under the local branches have empty analytic interior. A nearby point can therefore be chosen in the finite étale locus and on every local branch. Its fiber has at least r points but exactly deg (f ) points. Hence 

deg( f ) ≥ r.4Lemma 3.4 (Free left inverses force matrix degree one) . Let F be a free rational self-map in g

variables. Suppose every matrix evaluation Fn is dominant. If F has a free rational left inverse, then Fn is birational for at least one n ≥ 2.Proof. Choose rational expressions for the finitely many components of the proposed left inverse. Each expression has a nonempty matrix domain at some size. Domains are stable under direct sums, so after taking a common multiple of these sizes, enlarged if necessary to be at least two, every component has a nonempty domain at one common size n. At fixed size, the domain of a rational expression is Zariski open. Since the matrix tuple space is irreducible, the finitely many nonempty domains have nonempty intersection. Thus the left inverse determines a rational map Rn on a nonempty open subset of the target matrix space. Dominance of Fn ensures that the composition Rn ◦ Fn is defined on a nonempty open subset. Equality of the free rational functions gives 

Rn ◦ Fn = id 

as rational maps. On function fields this becomes 

F ∗ 

> n

◦ R∗ 

> n

= id .

The injective homomorphism F ∗ 

> n

is therefore also surjective, so it is an isomorphism. Hence Fn is birational. 

# 4 A symmetric 2 × 2 output curve 

Let 

X =

> (

0 11 0

> )

, Y =

> (

0 1

−1 0

> )

, Z =

> (

1 00 −1

> )

. (8) They satisfy 

X2 = Z2 = I, Y 2 = −I, 

and anticommute pairwise. Consider the output curve 

p = tX, q = I + tX, r = 2 I + tZ, s = 3 I + tZ. (9) At t = 0 this is the scalar point (0 , I, 2I, 3I). By Proposition 3.2, it has a unique formal preimage 

a = A, b = I + B, c = 2 I + G, d = 3 I + D (10) in Mat 2(Q[[ t]]) 4.The symmetry of this branch reduces the elimination to three scalar coordinates. Set 

H =

> (

1 11 −1

> )

, κ(M ) = ( HM H −1)T.

Since HT = H and H2 = 2 I, the map κ is an anti-involution. Direct calculation gives 

κ(X) = Z, κ(Y ) = Y, κ(Z) = X. (11) For traceless 2 × 2 matrices U, V , define 

Γδ(V, U ) = ( δI + V − U )U (δI + V − U )−1.

This is the centered part of C(μI + V, λI + U ) when δ = μ − λ.5Lemma 4.1 (Reversal identities) . For traceless 2 × 2 matrices, 

κ(Γ δ(V, U )) = Γ −δ(κV, κU ). (12) 

Moreover, whenever the expressions are defined, 

C(C(x, z ), C (x, y )) = C(C(z, x ), C (z, y )) . (13) 

Proof. If R is traceless, then R2 is scalar and (δI + R)−1 is a scalar multiple of δI − R. Applying the anti-involution to the defining conjugation for Γδ therefore replaces its conjugator, up to an irrelevant nonzero scalar, by −δI + κV − κU . This proves (12). For (13), put A = x − y and B = z − y. The elementary identity 

B(A − B)−1A = A(A − B)−1B

follows by substituting A = ( A − B) + B into both sides. Substitution into the definitions of C then gives 

(C(x, z ) − C(x, y ))( x − y) = ( C(z, x ) − C(z, y ))( z − y).

Both sides of (13) consequently conjugate y by this same element. Let ̂ F be the centered form of FP4 , so that 

FP4 (A, I + B, 2I + G, 3I + D) = ( P, I + Q, 2I + R, 3I + S).

On traceless tuples define 

τ (A, B, G, D ) = ( κD, κG, κB, κA ).

Equations (12) and (13), applied from the inner diamonds outward, give ̂

F ◦ τ = τ ◦̂ F . (14) The first and fourth coordinates follow directly from (12) ; equation (13) changes the common lower vertex in each middle coordinate before (12) is applied, interchanging the second and third coordinates. Every centered matrix in (10) is traceless. Indeed, p, q, r, s are respectively conjugate to a, b, c, d ,and the output curve has scalar parts 0, 1, 2, 3 and traceless perturbations. The centered output tuple in (9) is (tX, tX, tZ, tZ ), which is fixed by τ . Formal uniqueness and (14) therefore imply 

G = κ(B), D = κ(A). (15) Recursive substitution through degree three gives 

A = tX + 4 t3Z + O(t4),B = tX − 2t2Y + (2 X + 4 Z)t3 + O(t4),G = tZ − 2t2Y + (4 X + 2 Z)t3 + O(t4),D = tZ + 4 t3X + O(t4).

(16) These identities are also checked directly by the ancillary exact-arithmetic script. 65 Exact elimination on the formal branch 

Because q is conjugate to b, equation (9) gives 

tr( B) = 0 , B2 = t2I. 

The matrices X, Y, Z form a basis of the traceless 2 × 2 matrices, so there are unique x, y, z ∈ Q[[ t]] 

such that 

B = xX + yY + zZ, x2 − y2 + z2 = t2. (17) By (15), 

G = zX + yY + xZ. 

The jet (16) gives 

x = t + 2 t3 + O(t4), y = −2t2 + O(t4), z = 4 t3 + O(t4). (18) The equation p = C(b, a ) = tX determines A rationally from B. Since A is conjugate to tX , we have A2 = t2I. The relation p(b − a) = ( b − a)a then implies 

p(b + p) − (b + p)a = p2 − a2 = 0 .

Thus, with 

L = I + B + tX, e = 2 t2 + 2 tx − 1,

we have A = L−1(tX )L. Here det L = 1 − 2t2 − 2tx = −e, so L is invertible on the formal branch. Calculation in the Pauli basis gives 

A = t(2 x(t + x) − 1) 

e X + 2t(( t + x)y + z)

e Y

+ 2t(( t + x)z + y)

e Z. 

(19) Put 

T = C(a, b ), U = C(a, c ), h = ( U − T )( a − b).

Since T = ( a − b)b(a − b)−1 and q = ( U − T )T (U − T )−1,

q = hbh −1.

Using q = I + tX and b = I + B, this is equivalent to 

E := tXh − hB = 0 . (20) Let A = Q(t, x, z )( y) be the quadratic field determined by 

y2 = x2 + z2 − t2.

The radicand has simple zeros when regarded as a rational function of x over Q(t, z ), so it is not a square in Q(t, x, z ). Substitute (17), (19), and G = zX + yY + xZ into (20), calculating with 

XY = −Z, XZ = −Y, Y Z = −X

and anticommutation. Write the scalar and X-coordinates of E as 

E0 = a0 + b0y, EX = a1 + b1y, 

7where ai, b i ∈ Q(t, x, z ). On the branch (18), exact expansion gives 

b0 = −t2 + 32 t4 + O(t6), b1 = t − 32 t3 + O(t5). (21) In particular, both are nonzero in Q(( t)) . Therefore 

y = ρ0 := − a0

b0

= ρ1 := − a1

b1

on the formal branch. Cancel each ρi in Q(t, x, z ) before clearing denominators. Let S(t, x, z ) be the unique irreducible factor vanishing at (0 , 0, 0) in the numerator of ρ0 − ρ1, and let N (t, x, z ) be the corresponding factor in the numerator of 

ρ20 − (x2 + z2 − t2).

All other irreducible factors in these two numerators have nonzero value at the origin and hence are units after substitution of the formal branch. Thus 

S(t, x (t), z (t)) = N (t, x (t), z (t)) = 0 . (22) 

Lemma 5.1 (Exact elimination) . The factors just defined have x-degrees 4 and 8, respectively, and 

Res x(S, N ) = 16 t12 z(z − t)2(t − 1) 4(t + 1) 4(2 t − 1) 3(2 t + 1) 3

· (3 t2 + tz − 2) 2 (

8t4 + 4 t3z + 4 t2z2 − 8t2 − 4tz + 1 

> )2

P (t, z ), (23) 

where 

P (t, z ) = A3(t)z3 + A2(t)z2 + A1(t)z + A0(t)

and 

A3(t) = 256 t12 − 1472 t10 + 2096 t8 − 676 t6 + 160 t4 − 25 t2,A2(t) = 256 t11 − 1984 t9 + 1808 t7 + 28 t5 + 86 t3 + 20 t, A1(t) = −2112 t10 + 1552 t8 + 1860 t6 − 257 t4 + 140 t2 − 4,A0(t) = −2880 t9 + 672 t7 − 564 t5 + 16 t3.

Proof. This is a finite exact calculation in the quadratic algebra A. The ancillary script constructs 

A, B, G, T, U, h, E , extracts S, N by exact factorization over Q, and verifies (23) by expanding the difference between its two sides. It also verifies the nonzero leading terms in (21) . Every operation is symbolic over Q; no numerical approximation enters the calculation. From (22) , the left side of (23) vanishes after substitution of the formal branch. In the integral domain Q[[ t]] , the factors t, z, and z − t are nonzero by (18) ; every other displayed factor except P

has nonzero constant term. Hence 

P (t, z (t)) = 0 . (24) 

Lemma 5.2 (Irreducibility of the cubic) . The polynomial P (t, z ) is irreducible in Q(t)[ z].Proof. The coefficient polynomials A3, A 2, A 1, A 0 have greatest common divisor one, so P is primitive in Q[t][ z]. At t = 2 ,

P (2 , z ) = 4(9255 z3 − 64618 z2 − 412473 z − 351616) .

8The cubic in parentheses is primitive. Modulo 7 it becomes 

z3 − z2 + 2 z + 1 ,

whose values at 0, 1, . . . , 6 are 1, 3, 2, 4, 1, 6, 4. It has no root in F7 and is therefore irreducible there, hence irreducible over Q. The leading coefficient A3(2) is nonzero. If P factored over Q(t), Gauss’s lemma would give a factorization in Q[t, z ]; specialization at t = 2 would preserve the positive 

z-degrees of both factors and would factor P (2 , z ), a contradiction. Combining (24) with Lemma 5.2 gives 

[Q(t, z (t)) : Q(t)] = 3 . (25) 

# 6 The matrix-degree obstruction 

Let Fn be the rational map induced by FP4 on four n × n matrices. By Proposition 3.2, Fn is dominant and generically finite. We now convert (25) into a lower bound for its degree. 

Proposition 6.1. For every n ≥ 2,

deg( Fn) ≥ 3.

Proof. First let n = 2 . Let L be the field over Q(t) generated by all entries of the formal preimage (10) . The equations defining the inverse branch are rational algebraic equations whose denominators have nonzero constant terms. Their Jacobian with respect to the input entries is the identity at 

t = 0 . The algebraic implicit-function theorem therefore shows that every entry of the branch is algebraic over Q(t); equivalently, L/ Q(t) is finite. Since z(t) ∈ L, equation (25) gives 

[L : Q(t)] ≥ 3.

The extension is separable. Each Q(t)-embedding of L into an algebraic closure sends the matrix entries of the branch to another preimage of the same output curve. The resulting points are distinct because those entries generate L. Every denominator that occurs in F2 is a nonzero element of L,so it remains nonzero under each embedding. The Jacobian determinant on the original branch is 

1 + O(t) and hence is nonzero in L; its images under the embeddings are also nonzero. Thus the fiber over (9) contains at least three distinct simple points. Lemma 3.3 gives deg( F2) ≥ 3.For n > 2, append to every input matrix the scalar block corresponding to its center: 

A ⊕ 0In−2, (I + B) ⊕ In−2, (2 I + G) ⊕ 2In−2, (3 I + D) ⊕ 3In−2.

The map C respects direct sums, so the conjugate 2 × 2 preimages produce at least three distinct preimages of one n × n block-diagonal output curve. On the original amplified branch the input tends at t = 0 to the scalar tuple (0 In, I n, 2In, 3In), so its n × n Jacobian determinant is 1 + O(t)

by Proposition 3.2. This determinant is a nonzero element of L, and its conjugates are therefore nonzero as well. The three amplified points are simple, and Lemma 3.3 gives deg( Fn) ≥ 3.The maps Fn are defined over Q, and the degree in Proposition 6.1 is geometric. It is therefore unchanged by extension to any characteristic-zero constant field. 

Proof of Theorem 1.1. If a, b, c, d were free rational functions of p, q, r, s , those expressions would define a free rational left inverse of FP4 . By Lemma 3.4, Fn would then be birational for some n ≥ 2,so deg( Fn) = 1 . This contradicts Proposition 6.1. 9Corollary 6.2. The adjacent partners T = C(a, b ) and W = C(d, c ) are not simultaneously free rational functions of p, q, r, s . The same holds for the second-layer partners M, N .Proof. Apply Proposition 2.2 and the equivalent second-layer recovery described after it. 

# 7 Extension from P4 to non-cographs 

We finish by replacing a potentially invalid specialization of a hypothetical rational inverse with a specialization of the explicit algebraic branch. The latter has all denominators controlled at its scalar base point. For a graph G on [m], let FG denote the rational map from the m bottom pseudo-roots to the neighborhood seed YG. Its evaluation on n × n matrices is denoted FG,n .

Lemma 7.1 (Central directions) . If λ is central, then 

C(λ, x ) = x, C(x, λ ) = λ

whenever the indicated inverses exist. Consequently, after bottom pseudo-roots outside a vertex set 

V are assigned distinct central scalars, pseudo-roots in directions belonging to V reduce to those for the induced graph G[V ], while pseudo-roots in outside directions remain their assigned scalars. Proof. Both identities follow because λ − x commutes with x and with λ. The consequence follows inductively along any sequence of diamond moves constructing the relevant pseudo-root. 

Lemma 7.2. At a bottom tuple of pairwise distinct central scalars, the Jacobian of FG,n is the identity for every graph G and every matrix size n. Thus FG,n is dominant and generically finite. Proof. Every neighborhood pseudo-root is obtained by repeatedly applying C, and at each application Lemma 3.1 preserves the perturbation in the direction of the second argument. Therefore the ith seed has first-order term equal to the perturbation of the ith bottom pseudo-root and no other first-order term. 

Theorem 7.3. If G contains an induced P4, then its neighborhood seed map FG has no free rational inverse. More precisely, 

deg( FG,n ) ≥ 3 (n ≥ 2) .

Proof. Let V = {v1, v 2, v 3, v 4} induce the path v1 − v2 − v3 − v4. Assign the scalar centers 0, 1, 2, 3

to these four bottom directions and assign pairwise distinct rational scalar centers, different from 

0, 1, 2, 3, to all remaining directions. For 2 × 2 matrices, place the formal preimage (10) in the four directions in V and keep every outside input equal to its scalar center. Lemma 7.1 shows that its image under FG, 2 is the curve (9) in the four directions in V and is constant in every outside direction. All defining denominators have invertible scalar terms at t = 0 .The field of entries of this branch contains z(t) and therefore has degree at least three over Q(t).Lemma 7.2 gives Jacobian determinant 1 + O(t) on the original branch, and its field conjugates remain nonzero. As in Proposition 6.1, there are at least three distinct simple points in one fiber, so 

deg (FG, 2) ≥ 3. Direct-sum amplification with the scalar centers gives the same conclusion for every 

n > 2.If FG had a free rational inverse, Lemma 3.4 would make some FG,n birational, contradicting the degree bound. 10 Since a graph is a cograph exactly when it has no induced P4, combining Theorem 7.3 with the cograph diamond-generation theorem [8, Theorem 1.1(ii)] gives the full rational classification. 

Corollary 7.4 (Rational cograph classification) . For every finite graph G over a characteristic-zero base field, 

YG rationally generates all Boolean-lattice pseudo-roots ⇐⇒ G is a cograph .

Proof. If G is a cograph, the diamond-generation theorem recovers every pseudo-root from YG,and diamond moves are rational operations. If G is not a cograph, it contains an induced P4, so Theorem 7.3 rules out rational recovery even of all bottom pseudo-roots. The matrix-degree proof of Theorem 7.3 is essential here. Merely substituting central scalars into a hypothetical free rational inverse would not be sufficient: its denominators could vanish identically on the specialized locus. By contrast, the explicit formal branch is defined there, and its simple conjugate points force a degree bound on the unspecialized generic matrix map. 

# A Reproducibility of the exact calculation 

The ancillary script p4_matrix_degree_certificate.py was verified with Python 3.12.13 and SymPy 1.14.0 [7]. A matching environment can be prepared with 

python3 -m pip install sympy==1.14.0 

and the certificate is run with 

python3 p4_matrix_degree_certificate.py 

It checks the Pauli multiplication rules and reversal action, substitutes the formal jet (16) into all four seed equations, verifies (19), constructs the two branch factors S, N , checks their degrees and the resultant identity (23) , and verifies the primitive irreducible specialization used in Lemma 5.2. Every failed check raises an explicit exception; the script does not rely on Python assertions, which can be disabled by optimization flags. 

# References 

[1] P. M. Cohn, Free Rings and Their Relations , 2nd ed., London Mathematical Society Monographs 19, Academic Press, London, 1985. [2] D. G. Corneil, H. Lerchs, and L. Stewart Burlingham, Complement reducible graphs, Discrete Appl. Math. 3 (1981), no. 3, 163–174, doi:10.1016/0166-218X(81)90013-5. [3] I. Gelfand, V. Retakh, and R. L. Wilson, Quadratic linear algebras associated with factorizations of noncommutative polynomials and noncommutative differential polynomials, Selecta Math. (N.S.) 7 (2001), no. 4, 493–523, doi:10.1007/s00029-001-8096-5. [4] I. Gelfand, S. Gelfand, V. Retakh, and R. L. Wilson, Factorizations of polynomials over noncommutative algebras and sufficient sets of edges in directed graphs, Lett. Math. Phys. 74 

(2005), 153–167, doi:10.1007/s11005-005-0024-8. [5] R. Hartshorne, Algebraic Geometry , Graduate Texts in Mathematics 52, Springer, New York, 1977, doi:10.1007/978-1-4757-3849-0. 11 [6] D. S. Kaliuzhnyi-Verbovetskyi and V. Vinnikov, Foundations of Free Noncommutative Func-tion Theory , Mathematical Surveys and Monographs 199, American Mathematical Society, Providence, RI, 2014, doi:10.1090/surv/199. [7] A. Meurer et al., SymPy: symbolic computing in Python, PeerJ Comput. Sci. 3 (2017), e103, doi:10.7717/peerj-cs.103. [8] M. Ramani, Cographs and minimum diamond-generating edge sets in Boolean lattices, arXiv:2607.19048 [math.CO] (2026). [9] V. Retakh and M. Saks, On the rational relationships among pseudo-roots of a non-commutative polynomial, J. Pure Appl. Algebra 225 (2021), no. 6, Paper No. 106581, doi:10.1016/j.jpaa.2020.106581. 12