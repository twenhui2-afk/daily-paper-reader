---
title: "Beyond scalar losses: calibrating segmentation models via gradient vector field surgery"
source: arxiv
title_zh: 通过gradient vector field surgery实现的Beyond scalar losses：calibrating segmentation 模型s
abstract_zh: "Region-based loss functions，such as Dice loss，have established themselves as de facto standard 用于 hi。\n\n核心做法：作者提出 \"surgery\" on gradient 向量 field as simple，yet effective intervention to mitigate calibration iss。\n\n主要结果：However，模型s trained 使用 region-based loss functions are notoriously miscalibrated 与 typically yield o。\n\n值得关注的点在于：In empirical evaluations across 2D 与 3D medical segmentation tasks，we 表明 effectiveness 的 this interv。"
abstract_en: "Region-based loss functions, such as the Dice loss, have established themselves as the de facto standard for highly class- and region-imbalanced segmentation tasks. However, models trained using region-based loss functions are notoriously miscalibrated and typically yield over-confident predictions. In medical imaging applications, such as defining tumor resection margins, this miscalibration is hindering clinical adoption. In this work, we outline a novel gradient perspective on this overconfidence and show how it affects region-based loss functions. We propose a \"surgery\" on the gradient vector field as a simple, yet effective intervention to mitigate calibration issues. This surgery adds a factor to the loss's partial derivative, scaling the gradient's magnitude linearly with the prediction error. In empirical evaluations across 2D and 3D medical segmentation tasks, we demonstrate the effectiveness of this intervention while maintaining high prediction accuracy when used in conjunction with any region-based loss function."
authors: "Laurin Lux, Alexander H. Berger, Moritz Knolle, Daniel Rückert, Johannes C. Paetzold"
date: 2026-07-15
journal: cs.CV
source_link: "https://arxiv.org/pdf/2607.14338v1"
pdf: "https://arxiv.org/pdf/2607.14338v1"
score: 6.8
tldr: "围绕《通过gradient vector field...》；作者提出 \"surgery\" on gradient 向量 fi...；However，模型s trained 使用 region-based..."
tldr_cn: "围绕《通过gradient vector field...》；作者提出 \"surgery\" on gradient 向量 fi...；However，模型s trained 使用 region-based..."
tldr_en: "Region-based loss functions, such as the Dice loss, have established themselves as the de facto standard for highly class- and region-imbalanced segmentation tasks. However, models trained using region-based loss functions are notoriously miscalibrated and typically yield over-confident predictions."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "clinical | 临床的 | CET6"]
motivation: Region-based loss functions，such as Dice loss，have established themselves as de facto standard 用于 hi。
method: "核心做法：作者提出 \"surgery\" on gradient 向量 field as simple，yet effective intervention to mitigate calibration iss。"
result: 主要结果：However，模型s trained 使用 region-based loss functions are notoriously miscalibrated 与 typically yield o。
conclusion: 值得关注的点在于：In empirical evaluations across 2D 与 3D medical segmentation tasks，we 表明 effectiveness 的 this interv。
---

## 摘要
Region-based loss functions，such as Dice loss，have established themselves as de facto standard 用于 hi。

核心做法：作者提出 "surgery" on gradient 向量 field as simple，yet effective intervention to mitigate calibration iss。

主要结果：However，模型s trained 使用 region-based loss functions are notoriously miscalibrated 与 typically yield o。

值得关注的点在于：In empirical evaluations across 2D 与 3D medical segmentation tasks，we 表明 effectiveness 的 this interv。

## Abstract
Region-based loss functions, such as the Dice loss, have established themselves as the de facto standard for highly class- and region-imbalanced segmentation tasks. However, models trained using region-based loss functions are notoriously miscalibrated and typically yield over-confident predictions. In medical imaging applications, such as defining tumor resection margins, this miscalibration is hindering clinical adoption. In this work, we outline a novel gradient perspective on this overconfidence and show how it affects region-based loss functions. We propose a "surgery" on the gradient vector field as a simple, yet effective intervention to mitigate calibration issues. This surgery adds a factor to the loss's partial derivative, scaling the gradient's magnitude linearly with the prediction error. In empirical evaluations across 2D and 3D medical segmentation tasks, we demonstrate the effectiveness of this intervention while maintaining high prediction accuracy when used in conjunction with any region-based loss function.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **clinical**：临床的（CET6）
