---
title: Bi-Level Collaborative Learning for Few-Shot Scribble-Supervised Medical Image Segmentation
source: arxiv
title_zh: 用于Few-Shot Scribble-Supervised Medical Image Segmentation的Bi-Level Collaborative Learning
abstract_zh: "Scribble annotations offer efficient alternative to costly pixel-wise labeling 用于 medical image segm。\n\n核心做法：To break this bottleneck，作者提出 bi-level collaborative learning 框架用于 few-shot scribble-supervised medi。\n\n主要结果：Through bidirectional interaction 与 collaborative learning between upper 与 lower levels，proposed 框架。\n\n值得关注的点在于：Specifically，upper-level learnable superpixel 模型 is 被引入 to provide region-structural priors 用于 lower。"
abstract_en: "Scribble annotations offer an efficient alternative to costly pixel-wise labeling for medical image segmentation, yet in real clinical scenarios, scribble-annotated samples are often still limited, imposing the dual challenges of sparse supervision and annotated sample scarcity. These compounded constraints severely deprive models of the structural evidence needed for complete region recovery and precise boundary delineation. To break this bottleneck, we propose a bi-level collaborative learning framework for few-shot scribble-supervised medical image segmentation. Specifically, an upper-level learnable superpixel model is introduced to provide region-structural priors for lower-level segmentation, while superpixel-based region-wise pseudo-label propagation and a spatial-prior-guided filtering strategy are performed to generate reliable dense pseudo-labels for segmentation learning. Meanwhile, the anatomical semantics learned by the lower-level segmentation model under the guidance of the current superpixels are fed back to the upper level, further driving it to learn region-structural representations better aligned with the segmentation task. Through bidirectional interaction and collaborative learning between the upper and lower levels, the proposed framework significantly outperforms existing state-of-the-art scribble-supervised methods on the ACDC and Prostate datasets under the few-shot scribble-supervised setting."
authors: "Xiang-Xiang Su, Yufan Ye, Yihang Zheng, Min Gan, Guang-Yong Chen"
date: 2026-07-28
journal: cs.CV
source_link: "https://arxiv.org/pdf/2607.25432v1"
pdf: "https://arxiv.org/pdf/2607.25432v1"
score: 6.699999999999999
tldr: 围绕《用于Few-Shot Scribble-Supe...》；To break this bottleneck，作者提出 bi...；Through bidirectional interaction 与...
tldr_cn: 围绕《用于Few-Shot Scribble-Supe...》；To break this bottleneck，作者提出 bi...；Through bidirectional interaction 与...
tldr_en: "Scribble annotations offer an efficient alternative to costly pixel-wise labeling for medical image segmentation, yet in real clinical scenarios, scribble-annotated samples are often still limited, imposing the dual challenges of sparse supervision and annotated sample scarcity. These compounded constraints severely deprive models of the structural evidence needed for complete region recovery and precise boundary delineation."
selection_source: fresh_fetch
vocabulary: ["segmentation | 分割 | CET6/IELTS", "clinical | 临床的 | CET6"]
motivation: Scribble annotations offer efficient alternative to costly pixel-wise labeling 用于 medical image segm。
method: 核心做法：To break this bottleneck，作者提出 bi-level collaborative learning 框架用于 few-shot scribble-supervised medi。
result: 主要结果：Through bidirectional interaction 与 collaborative learning between upper 与 lower levels，proposed 框架。
conclusion: 值得关注的点在于：Specifically，upper-level learnable superpixel 模型 is 被引入 to provide region-structural priors 用于 lower。
---

## 摘要
Scribble annotations offer efficient alternative to costly pixel-wise labeling 用于 medical image segm。

核心做法：To break this bottleneck，作者提出 bi-level collaborative learning 框架用于 few-shot scribble-supervised medi。

主要结果：Through bidirectional interaction 与 collaborative learning between upper 与 lower levels，proposed 框架。

值得关注的点在于：Specifically，upper-level learnable superpixel 模型 is 被引入 to provide region-structural priors 用于 lower。

## Abstract
Scribble annotations offer an efficient alternative to costly pixel-wise labeling for medical image segmentation, yet in real clinical scenarios, scribble-annotated samples are often still limited, imposing the dual challenges of sparse supervision and annotated sample scarcity. These compounded constraints severely deprive models of the structural evidence needed for complete region recovery and precise boundary delineation. To break this bottleneck, we propose a bi-level collaborative learning framework for few-shot scribble-supervised medical image segmentation. Specifically, an upper-level learnable superpixel model is introduced to provide region-structural priors for lower-level segmentation, while superpixel-based region-wise pseudo-label propagation and a spatial-prior-guided filtering strategy are performed to generate reliable dense pseudo-labels for segmentation learning. Meanwhile, the anatomical semantics learned by the lower-level segmentation model under the guidance of the current superpixels are fed back to the upper level, further driving it to learn region-structural representations better aligned with the segmentation task. Through bidirectional interaction and collaborative learning between the upper and lower levels, the proposed framework significantly outperforms existing state-of-the-art scribble-supervised methods on the ACDC and Prostate datasets under the few-shot scribble-supervised setting.

## 英语词汇
- **segmentation**：分割（CET6/IELTS）
- **clinical**：临床的（CET6）
