---
title: Unifying Active Learning and Semi-Supervised Learning for Medical Image Segmentation
source: arxiv
title_zh: 用于Medical Image Segmentation的Unifying Active Learning与Semi-Supervised Learning
abstract_zh: "In practical settings，medical image segmentation 模型s are often developed 结合 limited annotated data r。\n\n核心做法：作者提出 RegAL，unified active semi-supervised 框架 governed by shared topology-aware Pareto optimization t。\n\n主要结果：Across BraTS 2021，dHCP，与 ProstateX，RegAL remains stable 结合 few labeled volumes 与 consistently 优于当前最优。\n\n值得关注的点在于：Although active learning (AL) 与 semi-supervised learning (SSL) both target annotation scarcity，they。"
abstract_en: "In practical settings, medical image segmentation models are often developed with limited annotated data rather than fully labeled datasets. Training frequently begins in ultra-low labeled regimes where only a small number of volumes are annotated. In such scenarios, practitioners must simultaneously decide which cases to annotate and how to best use the remaining unlabeled data. Although active learning (AL) and semi-supervised learning (SSL) both target annotation scarcity, they are typically designed and optimized independently, resulting in objective mismatch and unstable training during early-stage \"cold start\" conditions. We propose RegAL, a unified active semi-supervised framework governed by a shared topology-aware Pareto optimization that couples sample acquisition with unlabeled data utilization. RegAL evaluates images along three complementary axes, voxel-wise uncertainty, feature diversity, and a novel topological consistency metric, to select anatomically informative edge cases for annotation. On the other hand, the same criteria are used to identify geometrically stable atlas candidates for diffeomorphic registration-guided augmentation to train a self-supervised Mean Teacher segmentation network. Across BraTS 2021, dHCP, and ProstateX, RegAL remains stable with few labeled volumes and consistently outperforms state-of-the-art AL, SSL, and active semi-supervised baselines across Dice and boundary-distance (ASD, HD95) metrics under extreme annotation scarcity."
authors: "Bahram Jafrasteh, Cheng Wan, Heejong Kim, Johannes C. Paetzold, Qingyu Zhao"
date: 2026-07-27
journal: cs.CV
source_link: "https://arxiv.org/pdf/2607.25014v1"
pdf: "https://arxiv.org/pdf/2607.25014v1"
score: 7.0
tldr: 围绕《用于Medical Image Segmenta...》；作者提出 RegAL，unified active semi-s...；Across BraTS 2021，dHCP，与 ProstateX，R...
tldr_cn: 围绕《用于Medical Image Segmenta...》；作者提出 RegAL，unified active semi-s...；Across BraTS 2021，dHCP，与 ProstateX，R...
tldr_en: "In practical settings, medical image segmentation models are often developed with limited annotated data rather than fully labeled datasets. Training frequently begins in ultra-low labeled regimes where only a small number of volumes are annotated."
selection_source: fresh_fetch
vocabulary: ["augmentation | 数据增强 | CET6", "optimization | 优化 | CET6", "segmentation | 分割 | CET6/IELTS", "uncertainty | 不确定性 | IELTS", "annotation | 标注 | CET6"]
motivation: In practical settings，medical image segmentation 模型s are often developed 结合 limited annotated data r。
method: 核心做法：作者提出 RegAL，unified active semi-supervised 框架 governed by shared topology-aware Pareto optimization t。
result: 主要结果：Across BraTS 2021，dHCP，与 ProstateX，RegAL remains stable 结合 few labeled volumes 与 consistently 优于当前最优。
conclusion: 值得关注的点在于：Although active learning (AL) 与 semi-supervised learning (SSL) both target annotation scarcity，they。
---

## 摘要
In practical settings，medical image segmentation 模型s are often developed 结合 limited annotated data r。

核心做法：作者提出 RegAL，unified active semi-supervised 框架 governed by shared topology-aware Pareto optimization t。

主要结果：Across BraTS 2021，dHCP，与 ProstateX，RegAL remains stable 结合 few labeled volumes 与 consistently 优于当前最优。

值得关注的点在于：Although active learning (AL) 与 semi-supervised learning (SSL) both target annotation scarcity，they。

## Abstract
In practical settings, medical image segmentation models are often developed with limited annotated data rather than fully labeled datasets. Training frequently begins in ultra-low labeled regimes where only a small number of volumes are annotated. In such scenarios, practitioners must simultaneously decide which cases to annotate and how to best use the remaining unlabeled data. Although active learning (AL) and semi-supervised learning (SSL) both target annotation scarcity, they are typically designed and optimized independently, resulting in objective mismatch and unstable training during early-stage "cold start" conditions. We propose RegAL, a unified active semi-supervised framework governed by a shared topology-aware Pareto optimization that couples sample acquisition with unlabeled data utilization. RegAL evaluates images along three complementary axes, voxel-wise uncertainty, feature diversity, and a novel topological consistency metric, to select anatomically informative edge cases for annotation. On the other hand, the same criteria are used to identify geometrically stable atlas candidates for diffeomorphic registration-guided augmentation to train a self-supervised Mean Teacher segmentation network. Across BraTS 2021, dHCP, and ProstateX, RegAL remains stable with few labeled volumes and consistently outperforms state-of-the-art AL, SSL, and active semi-supervised baselines across Dice and boundary-distance (ASD, HD95) metrics under extreme annotation scarcity.

## 英语词汇
- **augmentation**：数据增强（CET6）
- **optimization**：优化（CET6）
- **segmentation**：分割（CET6/IELTS）
- **uncertainty**：不确定性（IELTS）
- **annotation**：标注（CET6）
