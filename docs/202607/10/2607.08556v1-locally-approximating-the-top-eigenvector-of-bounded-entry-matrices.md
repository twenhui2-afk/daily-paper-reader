---
title: Locally Approximating the Top Eigenvector of Bounded Entry Matrices
source: arxiv
title_zh: Bounded Entry Matrices的Locally Approximating Top Eigenvector
abstract_zh: "We provide local computation algorithm to approximate top eigen向量 x \\in \\mathbb{R}^n 的 symmetric mat。\n\n核心做法：Our local computation algorithm has preprocessing complexity 的 \\tilde{O}(1/\\varepsilon^4) 与 per-coor。\n\n主要结果：Furthermore，we show lower bound 的 Ω(n/\\varepsilon^2) on total number 的 queries needed to output appr。\n\n值得关注的点在于：By accessing top eigen向量 ( 的 approximate normalized adjacency)，we implement local versions 的 Cheeger。"
abstract_en: "We provide a local computation algorithm to approximate the top eigenvector $x \\in \\mathbb{R}^n$ of a symmetric matrix $A \\in \\mathbb{R}^{n \\times n}$ with entries between $-1$ and $1$, building on the work of Swartworth and Woodruff [SODA 25] who show how to approximate the eigenvalues up to additive-$\\varepsilon n$ error using $\\tilde{O}(1/\\varepsilon^4)$ queries.   Our local computation algorithm has a preprocessing complexity of $\\tilde{O}(1/\\varepsilon^4)$ and per-coordinate query complexity of $\\tilde{O}(1/\\varepsilon^2)$ for an additive-$\\varepsilon n$ approximation whenever {$|λ_{\\min}(A)| = O(λ_{\\max}(A))$. When $λ_{\\min}(A)$ greatly exceeds $λ_{\\max}(A)$, our complexity degrades to at most $\\tilde{O}(1/\\varepsilon^{6.\\overline{6}})$ in preprocessing and $\\tilde{O}(1/\\varepsilon^{3.\\overline{3}})$ per query. Furthermore, we show a lower bound of $Ω(n/\\varepsilon^2)$ on the total number of queries needed to output an approximately top eigenvector (implying that the per-coordinate query complexity of $Ω(1/\\varepsilon^2)$ is necessary).   As an application, we use our algorithm to provide local computation algorithms for the sparsest-cut and max-cut problems in the dense graph model of Goldreich, Goldwasser, Ron [JACM 98]. By accessing the top eigenvectors (of an approximate normalized adjacency), we implement local versions of Cheeger's inequality and Trevisan's algorithm [SICOMP 12] to obtain \"square-root-opt\" approximations in polynomial time (as opposed to exponential-in-$\\text{poly}(1/\\varepsilon)$ time which is incurred in Goldreich, Goldwasser, Ron."
authors: "Nicolas Menand, Erik Waingarten"
date: 2026-07-09
journal: cs.DS
source_link: "https://arxiv.org/pdf/2607.08556v1"
pdf: "https://arxiv.org/pdf/2607.08556v1"
score: 7.449999999999999
tldr: 围绕《Bounded Entry Matrices的L...》；Our local computation algorithm...；Furthermore，we show lower bound 的 Ω(...
tldr_cn: 围绕《Bounded Entry Matrices的L...》；Our local computation algorithm...；Furthermore，we show lower bound 的 Ω(...
tldr_en: "We provide a local computation algorithm to approximate the top eigenvector $x \\in \\mathbb{R}^n$ of a symmetric matrix $A \\in \\mathbb{R}^{n \\times n}$ with entries between $-1$ and $1$, building on the work of Swartworth and Woodruff [SODA 25] who show how to approximate the eigenvalues up to additive-$\\varepsilon n$ error using $\\tilde{O}(1/\\varepsilon^4)$ queries. Our..."
selection_source: fresh_fetch
motivation: "We provide local computation algorithm to approximate top eigen向量 x \\in \\mathbb{R}^n 的 symmetric mat。"
method: "核心做法：Our local computation algorithm has preprocessing complexity 的 \\tilde{O}(1/\\varepsilon^4) 与 per-coor。"
result: 主要结果：Furthermore，we show lower bound 的 Ω(n/\varepsilon^2) on total number 的 queries needed to output appr。
conclusion: 值得关注的点在于：By accessing top eigen向量 ( 的 approximate normalized adjacency)，we implement local versions 的 Cheeger。
---

## 摘要
We provide local computation algorithm to approximate top eigen向量 x \in \mathbb{R}^n 的 symmetric mat。

核心做法：Our local computation algorithm has preprocessing complexity 的 \tilde{O}(1/\varepsilon^4) 与 per-coor。

主要结果：Furthermore，we show lower bound 的 Ω(n/\varepsilon^2) on total number 的 queries needed to output appr。

值得关注的点在于：By accessing top eigen向量 ( 的 approximate normalized adjacency)，we implement local versions 的 Cheeger。

## Abstract
We provide a local computation algorithm to approximate the top eigenvector $x \in \mathbb{R}^n$ of a symmetric matrix $A \in \mathbb{R}^{n \times n}$ with entries between $-1$ and $1$, building on the work of Swartworth and Woodruff [SODA 25] who show how to approximate the eigenvalues up to additive-$\varepsilon n$ error using $\tilde{O}(1/\varepsilon^4)$ queries.   Our local computation algorithm has a preprocessing complexity of $\tilde{O}(1/\varepsilon^4)$ and per-coordinate query complexity of $\tilde{O}(1/\varepsilon^2)$ for an additive-$\varepsilon n$ approximation whenever {$|λ_{\min}(A)| = O(λ_{\max}(A))$. When $λ_{\min}(A)$ greatly exceeds $λ_{\max}(A)$, our complexity degrades to at most $\tilde{O}(1/\varepsilon^{6.\overline{6}})$ in preprocessing and $\tilde{O}(1/\varepsilon^{3.\overline{3}})$ per query. Furthermore, we show a lower bound of $Ω(n/\varepsilon^2)$ on the total number of queries needed to output an approximately top eigenvector (implying that the per-coordinate query complexity of $Ω(1/\varepsilon^2)$ is necessary).   As an application, we use our algorithm to provide local computation algorithms for the sparsest-cut and max-cut problems in the dense graph model of Goldreich, Goldwasser, Ron [JACM 98]. By accessing the top eigenvectors (of an approximate normalized adjacency), we implement local versions of Cheeger's inequality and Trevisan's algorithm [SICOMP 12] to obtain "square-root-opt" approximations in polynomial time (as opposed to exponential-in-$\text{poly}(1/\varepsilon)$ time which is incurred in Goldreich, Goldwasser, Ron.
