Title: Locally Approximating the Top Eigenvector of Bounded Entry Matrices

URL Source: https://arxiv.org/pdf/2607.08556v1

Published Time: Fri, 10 Jul 2026 01:01:54 GMT

Number of Pages: 52

Markdown Content:
# Locally Approximating the Top Eigenvector of Bounded Entry Matrices 

## Nicolas Menand ∗ Erik Waingarten †

Abstract 

We provide a local computation algorithm to approximate the top eigenvector x ∈ Rn of a sym-metric matrix A ∈ Rn×n with entries between −1 and 1, building on the work of Swartworth and Woodruff [SW25] who show how to approximate the eigenvalues up to additive-εn error using ˜O(1 /ε 4)queries. Our local computation algorithm has a preprocessing complexity of ˜O(1 /ε 4) and per-coordinate query complexity of ˜O(1 /ε 2) for an additive-εn approximation whenever |λmin (A)| = O(λmax (A)). When 

λmin (A) greatly exceeds λmax (A), our complexity degrades to at most ˜O(1 /ε 6.6) in preprocessing and ˜O(1 /ε 3.3) per query. Furthermore, we show a lower bound of Ω( n/ε 2) on the total number of queries needed to output an approximately top eigenvector (implying that the per-coordinate query complexity of Ω(1 /ε 2) is necessary). As an application, we use our algorithm to provide local computation algorithms for the sparsest-cut and max-cut problems in the dense graph model of Goldreich, Goldwasser, Ron [GGR98]. By accessing the top eigenvectors (of an approximate normalized adjacency), we implement local versions of Cheeger’s inequality and Trevisan’s algorithm [Tre12] to obtain “square-root-opt” approximations in polynomial time (as opposed to exponential-in-poly(1 /ε ) time which is incurred in [GGR98]). 

> ∗

University of Pennsylvania. Supported by the National Science Foundation (NSF) under Grant No. CCF-2337993, as well as No. CCF-2045128. nmenand@cis.upenn.edu .

> †

University of Pennsylvania. Supported by the National Science Foundation (NSF) under Grant No. CCF-2337993. 

ewaingar@seas.upenn.edu .

1arXiv:2607.08556v1 [cs.DS] 9 Jul 2026 Contents 

1 Introduction 3

1.1 Our Contributions . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 41.2 Applications to Graph Partitioning in Dense Graphs . . . . . . . . . . . . . . . . . . . . . . . 51.3 Technical Overview . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 6

2 Preliminaries 9

2.1 Local Computation Algorithms . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 92.2 Randomized Linear Algebra . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 9

3 Local Computation Algorithm for the Top Eigenvector 10 4 Analysis of the Algorithm 12 

4.1 Theorem 6 assuming Lemma 4.4, 4.2 and 4.3 . . . . . . . . . . . . . . . . . . . . . . . . . . . 12 4.2 Proof of Lemma 4.4 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 14 4.3 Spectral Decompositions . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 15 4.4 The Lift . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 17 4.5 Proof of Lemma 4.2 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 19 4.6 Proof of Lemma 4.3 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 21 

5 Lower Bound 23 

5.1 Equivalent Method for Sampling from Hard Distribution . . . . . . . . . . . . . . . . . . . . . 25 5.2 Using the Deterministic Algorithm A to solve the Coin Problem . . . . . . . . . . . . . . . . 26 5.3 Putting Everything Together: Proof of Theorem 2 . . . . . . . . . . . . . . . . . . . . . . . . 28 

6 Local Computation Algorithm for Sparsest Cut 29 

6.1 Reduction to ( α, β )-Dense Graphs, for α, β = poly( ε): Proof of Theorem 3 . . . . . . . . . . . 29 6.2 Sparsest Cut on ( α, β )-dense Graphs with α, β = poly( ε): Proof of Theorem 9 . . . . . . . . . 33 

7 Local Computation Algorithm for Max-Cut 37 

7.1 Reduction to ( α, β )-Dense Graphs, for α, β = poly( ε): Proof of Theorem 4 . . . . . . . . . . . 37 7.2 Max Cut on ( α, β )-dense Graphs with α, β = poly( ε): Proof of Theorem 12 . . . . . . . . . . 41 

A Spectral Norm Decay 44 B Negative Eigenvalue Interference 48 

21 Introduction 

Computing the top eigenvector of a symmetric matrix is a fundamental algorithmic primitive and often the starting point of spectral algorithms (see [KV09] for a survey on spectral algorithms). A canonical such algorithm receives as input a matrix and utilizes a polynomial-time algorithm to find the top (or top-few) eigenvectors and eigenvalues (to high precision) [Gv00]. This computation, while running in polynomial time, must read the entire input matrix and can be prohibitive when the matrix becomes large (or is only implicitly maintained). This has motivated work on sublinear algorithms for approximation problems in numerical linear algebra (e.g., low-rank approximation [MM17, MW17, BW18, BCW20, KMS26], property testing of spectral properties [BLWZ19, BCJ20], and eigenvalue approximation [AN13, SW23, BDD +24, SW25], among many others). We work in the bounded-entry model [BLWZ19], where algorithms can selectively probe individual entries of an 

n×n matrix A satisfying ∥A∥∞ ≤ 1. 1 The model not only captures the natural settings of graph adjacency ma-trices and many well-studied kernel matrices (e.g., Gaussian [CS17, CKNS20] and smooth [BCIS18, CKW24] kernels), but also hits a “sweet spot” for sublinear algorithms, often enabling query complexity which is inde-pendent of the matrix size. For example, [BDD +24, SW25] show that, by querying the entries of a (randomly chosen) ˜O(1 /ε 2) × ˜O(1 /ε 2) principal submatrix of a bounded-entry symmetric matrix, one can estimate all eigenvalues of the matrix up to an additive εn error. 2

Do there exist sublinear algorithms for outputting an approximate top eigenvector x ∈ Rn of a bounded-entry symmetric matrix A?The above question requires a careful notion of “sublinear time.” A top eigenvector is the result of a global optimization problem over the entire matrix A, i.e., a maximizing ⟨x, Ax ⟩/∥x∥22, and an inherently 

n-dimensional object (requiring at least time n to output). Hence, we design constant-time algorithms in the model of local computation algorithms [RTVX11, ARVX12], where the goal is to provide localized oracle access to the individual entries of an approximate top eigenvector (see Definition 2.1). Local computation algorithms have had multiple historical successes (e.g., maximum independent set [Gha22] and maximum matching [BRR23]), and this work brings local computation algorithms into realm of numerical linear algebra. The perspective of local computation algorithms directly leads to new sketching questions in numerical linear algebra, as well as new applications in algorithms for the dense graph model [GGR98]. The key algorithmic challenge (which we further elaborate on in the technical overview) can be succinctly described as follows. In [SW25], the main algorithm shows that the maximum eigenvalue (i.e., the maximizing value of Rayleigh quotient with A) is preserved when “sketching” to a constant-size principal submatrix (roughly speaking, by carefully combining structural properties of bounded-entry matrices with matrix Bernstein inequalities). However, matrix Bernstein inequalities, which show that a small t × t “sketched” matrix behaves like a large 

n × n “original” matrix with respect to its large eigenvalues, does not give any actionable correspondence for the eigenvectors—indeed, the eigenvectors in the “original” and “sketched” matrices lie in completely different spaces, Rn and Rt respectively. In this work, the local computation algorithm must explicitly create a correspondence between the “sketched” and “original” matrix, so that it may lift the top eigenvector found in the sketched space into an approximately top eigenvector in the original space. With this primitive for locally approximating the top eigenvector, we give local versions of spectral algorithms which rely on top eigenvectors (namely, Cheeger’s inequality [AM85] for sparse cuts, and Trevisan’s algorithm [Tre12] for max-cut), resulting in local partitioning algorithms with a polynomial dependence on the accuracy parameter, albeit “square-root-opt” guarantees. 3            

> 1Unconditionally and without the bounded-entry assumption, sublinear-time spectral approximation via queries is often impossible, since simply distinguishing an all-zeros matrix from one containing a single large entry requires Ω( n2) queries.
> 2The bounded-entry model fixes the “scale” of the problem. All eigenvalues of an n×nsymmetric matrix Awith ∥A∥∞≤1must lie within [ −n, n ], and at most 1 /ε 2of them can be larger than εn in magnitude.
> 3The prior classic works [GGR98] obtain approximability guarantees with respect to the optimum (not square-root-opt), but run in time exponential in the accuracy parameter.

31.1 Our Contributions 

Our main result is a local computation algorithm which receives query access to a symmetric n × n matrix 

A with bounded entries, and can provide query access to an approximately top eigenvector. For the theorem below, recall that a symmetric n × n matrix A has n real eigenvalues which we arrange as λ1(A) ≥ λ2(A) ≥· · · ≥ λn(A), and we assume without loss of generality that λ1(A) is a large constant factor larger than εn ,4

and we let λmax (A) = λ1(A) and λmin (A) = |λn(A)|. Our goal is to output a (randomized) vector x ∈ Rn

whose Rayleigh quotient with A is at least λmax (A)−εn . In other words, the output vector x is the certificate for an additive εn approximation on the top eigenvalue. 

Theorem 1 (Main Upper Bound–Informal (see Theorem 6)) . There exists a (randomized) local computation algorithm A which receives query access to a symmetric n × n matrix A with ∥A∥∞ ≤ 1 and an accuracy parameter ε > 0.

• A makes ˜O(1 /ε 4) non-adaptive queries to A during preprocessing, and for any i ∈ [n], ˜O(1 /ε 2) non-adaptive queries to output xi ∈ R.5

• Letting x ∈ Rn where xi is the output of A on i ∈ [n],

Pr 

> A



λmax (A) − ⟨x, A x⟩∥x∥22

≤ εn 



≥ 0.99 ,

whenever ∥A∥2 = O(λmax (A)) .Furthermore, A runs in poly(1 /ε ) time, and uses poly(1 /ε ) · log n bits of shared randomness. 

We elaborate on the assumption that ∥A∥2 = O(λmax (A)) in Theorem 1. When the matrix A is psd (i.e., all eigenvalues are non-negative), we always satisfy λmax (A) = ∥A∥2 so the assumption becomes unnecessary. The algorithm does work for (non-psd) symmetric matrices, but one of the technical challenges is that negative eigenvalues of large magnitude may “interfere” with the top (positive) eigenvalue (we elaborate on this aspect in the technical overview). In Theorem 6, the more expressive query complexity degrades gracefully as a function of how large λmin (A) is compared to λmax (A) (still independent of n, but with weaker poly(1 /ε )-dependence). Theorem 1 is stated for λmin (A) = O(λmax (A)), as this assumption natively holds in many desired applications (for example, in spectral graph partitioning). It is an interesting open problem to locally handle the “negative eigenvalue interference” without any complexity degradation (for example, when λmin (A) = Θ( n) and λmax (A) = Θ( εn )); in Appendix B we give a concrete example where our analysis degrades. 

Theorem 2. Fix any ε ∈ (1 /√n, 1) and suppose A is an algorithm with access to an n × n symmetric matrix 

A with ∥A∥∞ ≤ 1, and one may also assume ∥A∥2 = O(λmax (A)) , satisfying 

Pr 

> A



λmax (A) − ⟨x, A x⟩∥x∥22

≤ εn 



≥ 0.99 ,

where x ∈ Rn is the output of A. Then, A must make Ω( n/ε 2) queries to A.

The above lower bound implies that the per-coordinate complexity of our local computation algorithm, i.e., the query complexity of ˜O(1 /ε 2) for outputting the i-th entry xi in Theorem 1, perfectly amortizes over the 

n coordinates of x, matching the lower bound and optimal (up to poly-logarithmic factors in 1 /ε ). Indeed, the total query complexity for outputting the top eigenvector using Theorem 1 is ˜O(1 /ε 4) + n · ˜O(1 /ε 2), and     

> 4Otherwise, we’ll show that a random vector satisfies the desired approximation guarantees (see Remark 7).
> 5The algorithm’s queries depend on the randomness the algorithm uses and the index i∈[n] on the query, but not on the specific contents of A.

4Theorem 2 says that the total query complexity must be Ω( n/ε 2). One may hope for improvements to the ˜O(1 /ε 4)-query complexity during the preprocessing time, however, Theorem 1 runs [SW25] during prepro-cessing, which is known to require Ω(1 /ε 4) non-adaptive queries (see the discussion on page 6 of [SW25]). Theorem 9.5 of [SW25] also gives an algorithm (not a local computation algorithm) which approximates the top eigenvector of a symmetric psd matrix A by sampling O(1 /ε ) columns of A. Importantly, the algorithm needs to make O(n/ε ) queries to read the entirety of the sampled columns, and cannot give localized access to the approximate top eigenvector. Crucially, their global optimization maximizes the Rayleigh quotient over the subspace spanned by O(1 /ε ) columns, so it must read these columns explicitly. At the same time, however, it does achieve linear in 1 /ε query complexity for psd matrices, whereas Theorem 2 shows that quadratic in 1/ε 2 is necessary; this implies a separation between psd matrices and (general) symmetric matrices. 

## 1.2 Applications to Graph Partitioning in Dense Graphs 

An important application of Theorem 1 is to approximate graph partitioning in the dense graph model, introduced in the seminal work of Goldreich, Goldwasser, and Ron [GGR98] in the context of property testing and approximation (see Chapter 8 of [Gol17]). Theorem 8.12 in Chapter 8 of [Gol17] gives an algorithm for testing general partition problems with poly(1 /ε ) query complexities and exp(poly(1 /ε )) time complexity. In particular, the algorithms sample a few vertices of the graph, and after an initial exponential-in-poly(1 /ε ) preprocessing time (which searches over all possible partitions of the sample), provide oracle access to an underlying cut in poly(1 /ε ) time per query (as in local computation algorithms). A super-polynomial dependence on 1 /ε is necessary (assuming P̸ = NP ) because graph partitioning problems are often NP -hard. However, what is possible and was open (until this work), is a Cheeger-like approximation guarantee in polynomial time. As we discuss in Theorem 3 and Theorem 4 below, this will be a direct consequence of using Theorem 1 to give local versions of algorithms from spectral graph theory. 

Definition 1.1 (Sparsity of a Graph) . For a fixed graph G = ([ n], E ) and a subset S ⊂ [n], we let 

• vol G(S) = P 

> i∈S

deg G(i), where deg G(i) is the degree of vertex i.

• E(S, S ) = P 

> (i,j )∈E

1 i ∈ S, j ∈ S is the number of edges in E which cross S.

• The sparsity of a cut is ϕG(S) = E(S, S )/ min {vol G(S), vol G(S)}.For a density parameter β ≥ 0, the sparsity of G over β-dense cuts is defined by 

ϕβ (G) = min {ϕG(S) : S ⊂ [n] s.t. min {vol G(S), vol G(S)} ≥ βn 2}.

The standard proof of Cheeger’s inequality [AM85] gives a constructive spectral algorithm which performs a “sweep cut” over level sets of the second largest eigenvector of the normalized adjacency matrix. The following theorem applies Theorem 1, to provide localized access to a sparse cut obtained from this sweep where all running times are polynomial in 1 /ε . Note, the sparsity of the returned cut will compete with 

ϕε(G); one cannot approximate ϕ0(G) (with no density constraints) because it is too sensitive, and a single disconnected vertex could make ϕ0(G) = 0 and be undetectable to a constant-query algorithm. 

Theorem 3. There exists a (randomized) local computation algorithm A with query access to the adjacency matrix of a graph G = ([ n], E ) and an accuracy parameter ε > 0.

• For any i ∈ [n], A makes poly(1 /ε ) queries and outputs access “ i ∈ S” for an underlying cut S ⊂ [n].

• Letting S ⊂ [n] be the underlying cut that A generates, 

Pr 

> A

h

ϕG(S) ≤ O(pϕε(G)) + ε

i

≥ 0.99 .

5Furthermore, A always runs in poly(1 /ε ) time and uses poly(1 /ε ) space. 

We use a similar methodology to provide local access to an approximate max-cut (with analogous “square-root-opt” guarantees), by giving a local implementation of Trevisan’s algorithm [Tre12]. As in Cheeger’s inequality, Trevisan’s algorithm performs a sweep cut over the level sets of the smallest eigenvector of the normalized adjancency matrix in order to find an approximately bipartite subgraph (and recurses on the remainder). As above, we will use Theorem 1 to find the top eigenvector to −A, and then show that we may (i) provide local access to the approximate bipartite subgraph, and (ii) recurse for at most poly(1 /ε )iterations on the remainder (which suffices for dense graphs). 

Theorem 4. There exists a (randomized) local computation algorithm A with query access to the adjacency matrix of a graph G = ([ n], E ) and accuracy parameter ε > 0.

• For any i ∈ [n], A makes poly(1 /ε ) queries and outputs access “ i ∈ S” for an underlying cut S ⊂ [n].

• If the max-cut of G cuts (1 − φ)|E| edges, and S ⊂ [n] is the underlying cut that A generates, 

Pr 

> A

E(S, S) ≥ (1 − √φ)|E| − εn 2 ≥ 0.99 .

Furthermore, A always runs in poly(1 /ε ) time and uses poly(1 /ε ) space. 

## 1.3 Technical Overview 

1.3.1 Upper Bound 

We give an overview of the algorithm and analysis behind Theorem 1, where the goal is to design a local computation algorithm with query access to a bounded-entry matrix A that can provide query access to entries of an approximately top eigenvector x, whose Rayleigh quotient with A is at least λmax (A) − εn . The starting point is an algorithm of [SW25] who prove that, if T is an t × n row-sampling matrix (i.e., each of the n rows is included in TA with probability q and re-scaled by 1 /√q), then λmax (TAT⊺) and λmax (A)differ by at most εn with high probability whenever q ≳ 1/(ε2n) (up to poly-logarithmic in 1 /ε factors). 6

Hence, we may assume an algorithm has found the top (unit) eigenvector y ∈ Rt satisfying 

bλmax = ⟨y, TAT⊺y⟩∥y∥22

where bλmax − λmax (A) ≤ εn. 

Note, however, that while y ∈ Rt, our task is to locally produce a vector x ∈ Rn where ⟨x, A x⟩/∥x∥22 is at least λmax (A) − O(εn ); ideally, using the information learned from y and TAT⊺.

The Lift. We aim to convince the reader in the next few paragraphs that there is a natural choice of vector 

x ∈ Rn, a “lift” of the vector y ∈ Rt from the sketch-space to the original n-dimensional space, for which there is a local computation algorithm for individual entries, and one may hope satisfies 

⟨y, TAT⊺y⟩∥y∥22

− ⟨x, A x⟩∥x∥22

≲ εn. (1) We consider the vector x = AT⊺y/bλmax ∈ Rn, which we call the lift of y. Note, producing the i-th entry of 

x requires reading the i-th row of AT⊺ (which contains t numbers), and we claim is a natural candidate for (1) for the following reason. First, we set up the vector x such that Tx ∈ Rt is exactly TAT⊺y/bλmax = y

> 6In the technical overview, we will ignore polylog(1 /ε )-factors.

6(since y is an eigenvector of TAT⊺ with eigenvalue bλmax ). Re-expressing the inner products ⟨x, A x⟩ and 

⟨y, TAT⊺y⟩ as the individual contributions from the eigenvectors of A, u1, . . . , u n ∈ Rn with eigenvalues 

λ1, . . . , λ n ∈ R, it becomes natural to compare the “numerators” in (1) 

⟨y, TAT⊺y⟩ =

> n

X

> i=1

λi · ⟨ Tui, Tx⟩2 with ⟨x, A x⟩ =

> n

X

> i=1

λi · ⟨ ui, x⟩2,

as well as the “denominators” ∥Tx∥22 and ∥x∥22 in (1). The intuition is that, if the vector x ∈ Rn lies mostly in the subspace of eigenvectors of A with large magnitude eigenvalues, then, the subspace embedding condition of T (used in [SW25], that T has low distortion on large magnitude eigenvectors) would apply to 

x, and one would conclude ∥Tx∥22 ≈ ∥ x∥22, and that for large |λi|, ⟨Tui, Tx⟩2 is close enough to ⟨ui, x⟩2.Roughly speaking, the differences in the “numerator” and “denominator” in the left-hand side of (1) would suggest the upper bound. The reason this does not work, of course, is that the vector x depends on y and 

T, and y also heavily depends on T, so one cannot a posteriori consider a vector x where Tx = y and apply the properties of T (which are distributional in nature). 

Analyzing the Lift Vector x. The analysis in Section 4 proceeds via a two-step process. First, we show that, because y is an eigenvector of TAT⊺ with eigenvalue bλmax which is positive and large, it must necessarily impose a coarse structure on x; then, we will use the coarse structure to obtain more refined estimates. Below, we give a high-level sketch for how this is used to compare ∥x∥22 and ∥Tx∥22 (see proof of Theorem 6 and Lemma 4.2). Writing x = U Λ1/2 ˜w/bλmax where U is matrix whose columns are the eigenvectors of A, Λ is the diagonal matrix of eigenvalues, and ˜ w = sign(Λ)Λ 1/2(TU )⊺y, we must upper-bound 

∥x∥22 − ∥ Tx∥22 = 1

bλ2max 

∥U Λ1/2 ˜w∥22 − ∥ TU Λ1/2 ˜w∥22

= 1

bλ2max 

⟨ ˜w, Λ1/2 (U ⊺U − (TU )⊺(TU )) Λ 1/2 ˜w⟩ . (2) Notice that, the inner-most matrix M = Λ 1/2(U ⊺U − (TU )⊺(TU ))Λ 1/2 is an n × n matrix where rows and columns correspond to eigenvectors, and which will give a weighted measure of the distortion of T on the subspaces spanned by eigenvectors of A. Importantly, a spectral norm bound on M would be very weak (since 

T necessarily distorts some vectors in Rn significantly), however, it suffices for us to bound the inner-product (2) which only cares about how M acts on ˜ w. In a coarse analysis in Subsection 4.4.1, we establish two facts: Lemma 4.13 first shows that most of the squared-ℓ2-mass of ˜ w is along the high-positive eigenvectors, and in Lemma 4.14 that this estimate can be refined (using Lemma 4.13) so that most of the squared-ℓ2-mass is along the eigenvectors whose eigenvalue is Θ( λmax (A)). Hence, the inner product in (2) “focuses-in” on the principal sub-matrix of M corresponding to eigenvectors which are positive and have magnitude Ω( λmax (A)); using the subspace embedding conditions of [SW25], T will have sufficiently small distortion on this subspace. More generally, the proofs of Lemma 4.2 and Lemma 4.3 utilize this approach. On the one hand, the subspace embedding condition of [SW25] shows that T acts as a low-distortion subspace embedding on subspaces spanned by high-magnitude eigenvectors (the higher the magnitude, the lower the distortion). This allows us to upper bound the spectral norm of sub-matrices of M. On the other hand, the distribution of squared-ℓ2-mass on ˜ w will necessarily be biased toward the positive eigenvectors of large magnitude, so bounds on inner-products will “focus-in” on regions of M which we can more tightly control. 

Negative Eigenvalue Interference. The complexity degradation from negative eigenvalues of large mag-nitude happens because the subspace embedding condition T inevitably “mixes” some of large negative eigenspace UN and the large positive eigenspace UP . In particular, even though every vector in UN and every vector in UP are orthogonal, TUN and TUP will not be. Hence, when the algorithm computes the top 7eigenvector of y of TAT⊺, y will aim to be aligned with the very top eigenvectors of TUP ; however, doing so inevitably also aligns y with TUN as well. When we lift x = AT⊺y = U Λ( TU )⊺y, the alignment between y

and the very top of TUN is further multiplied by −λmin (A). In Appendix B, we give a concrete example of this phenomenon, and why an analysis relying solely on the subspace embedding condition of [SW25] cannot achieve query complexity ˜O(1 /ε 2) when λmin (A) greatly exceeds λmax (A). 

1.3.2 Lower Bound 

The lower bound proceeds via a reduction to the classic “coin problem.” For a uniformly random draw 

b ∼ {− 1, 1}, an algorithm is provided s independent draws c1, . . . , cs from a distribution supported on 

{− 1, 1} whose mean is εb. The algorithm only sees the draws c1, . . . , cs and must learn b, and it is a standard fact this requires s = Ω(1 /ε 2) draws. Theorem 2 will show that a q-query algorithm for the top eigenvector could be used to solve the coin problem with only q/n draws, giving the desired lower bound. By Yao’s minimax principle, it suffices to assume that a (deterministic) q-query algorithm can approximate the top eigenvector of a random symmetric matrix A ∈ {− 1, 1}n×n with ones on the diagonals and whose off-diagonal entries have expectation is 2 εxx ⊺ where x ∼ {− 1, 1}n. Perhaps unsurprisingly, with high probability over the draw of A, any approximately top eigenvector y of A must be heavily correlated with the hidden vector x (up to a global sign). The key in our argument, which must boost the Ω(1 /ε 2) lower bound on the coin problem to an Ω( n/ε 2) lower bound on the eigenvector problem, is to “embed” the coin problem into a single random coordinate of x. In particular, we consider the following: 

• We draw a target index ℓ ∼ [n], and we generate a random vector x ∼ {− 1, 1}n where every entry 

xi ∼ {− 1, 1} for i̸ = ℓ is known to the algorithm, and where the algorithm will pretend xℓ = b (even though it does not know b and can only access the coins c1, . . . , cs). 

• We may now generate a matrix A according to our distribution. For any entry ( i, j ) not in the ℓ-th row or column, we may explicitly generate Ai,j using xi and xj . For any off-diagonal entry in the ℓ-th row or column, even though we do not know xℓ = b, a single coin cj of expectation εb may be used to determine Aℓ,j (see Definition 5.3). Importantly, the top-eigenvector algorithm is completely unaware of the index ℓ ∼ [n] so among the q-queries to the matrix O(q/n ) will fall in the ℓ-th row or column in expectation, and as a result, only O(q/n ) draws of coins are needed to generate the probes to the matrix A. Once the algorithm finds the approximately top eigenvector y of A, we know that y must correlate with x (and hence fix a global sign such that most coordinates of y and x must agree). Once more, the algorithm which produced y was unaware of ℓ and must have sign( yi) = sign( xi) for most i ∈ [n]; therefore, what we formalize in Section 5 is that this implies the value yℓ will agree with b with high constant probability (thereby giving the lower bound). 

Lower Bound via Direct-Sum Theorems for Information Complexity. In a prior submission of this work, an anonymous reviewer suggested that Theorem 2 be proved via direct-sum theorems for information complexity (with respect to the uniform distribution [CKW12]). The direct-sum approach of “embedding” a problem within a collection of problems to boost the lower bound is exactly what happens here. While we agree with the reviewer, we keep the current presentation since it is elementary and entirely self-contained. 82 Preliminaries 

## 2.1 Local Computation Algorithms 

We begin by describing the model of local computation algorithms adapted from the definition of [RTVX11], and tailored to the problems which we will study in this paper. In all cases, the problem can be phrased as that of receiving query access to the entries of an n × n matrix (either the bounded entry matrix directly, or the adjancency matrix of an underlying graph), and the goal is to provide local access to the entries of a vector x ∈ Rn (which will either correspond to a top eigenvector, or encode a cut of the underlying vertices of the graph). 

Definition 2.1 (Local Computation Algorithm for Matrices) . A local computation algorithm A is a (ran-domized) algorithm which has: 

• Query access to a large object, which in this work, will be the entries of a symmetric n × n matrix A

with ∥A∥∞ ≤ 1,

• Access to a shared randomness tape ρ ∈ { 0, 1}∗, and 

• A query index i ∈ [n], an accuracy parameter ε ∈ (0 , 1) , and a failure probability δ ∈ (0 , 1) .Letting x ∈ Rn be the random vector where 

xi = output of A on input i ∈ [n], ε, δ ∈ (0 , 1) , with access to A and ρ,we will prove properties of the vector x while ensuring that A makes few queries to the underlying matrix A.

The query complexity q(ε, δ ) of the algorithm A is the maximum number of entries of the matrix A that 

A may inspect on any particular input i ∈ [n], and the time complexity t(ε, δ ) is the maximum amount of time A needs to produce an output in the word RAM model, where we have dropped the parameter n

since our algorithms will have query and time complexity independent of n. Our algorithm A, on input 

i ∈ [n], ε, δ ∈ (0 , 1) will proceed by first executing a preprocessing phase on the matrix A which does not depend on the index i, and is shared for all possible queries i ∈ [n]. Hence, it is conceptually useful to divide the algorithm A into two phases, the preprocessing phase, and the query phase. As a result, we let 

qp(ε, δ ), q q (ε, δ ) denote the query complexity of preprocessing and querying, respectively, and tp(ε, δ ), t q (ε, δ )for the time complexity accordingly. 

## 2.2 Randomized Linear Algebra 

For fixed n ∈ N and a parameter q, a row-sampling matrix distribution R(n, q ) is given by the following procedure to generate T ∼ R (n, q ): starting with an empty set of rows, each i ∈ [n] is included independently with probability q, and if it is included, the row ei/√q ∈ Rn is added as a row of T (here, ei denotes the i-th basis vector). Notice that the resulting matrix T is a t × n matrix, where t is a binomial with parameters n

and q.

Definition 2.2 (Subspace Embedding) . For any ε ∈ (0 , 1/2) and an n × d matrix X, we say that a t × n

matrix T is an ε-distortion subspace embedding of X if for all v ∈ Rd,

∥Xv ∥22 − ∥ T Xv ∥22 ≤ ε∥Xv ∥22.

9Definition 2.3 (Leverage Scores) . For an n × d matrix X, the leverage score for row i of X is τi =

e⊺ 

> i

X(X⊺X)†X⊺ei, where (X⊺X)† denotes the Moore-Penrose pseudo-inverse of X⊺X, and we will make use of the following equivalent characterization, 7

τi = max 

> v∈Rd

(Xv )2

> i

∥Xv ∥22

.

The following theorem appears in [SW25] as Theorem 3.4 in the preliminaries, and follows from the matrix Chernoff bound [Kyn18, Tro12]. 

Theorem 5. Fix any n×d matrix X. For any ε, δ ∈ (0 , 1) and p ∈ (0 , 1) which satisfies p ≥ min(2 τi log( d/δ )/ε 2, 1) 

for all i ∈ [n], a draw T ∼ R (n, p ) is an ε-distortion subspace embedding of X with probability at least 1 − δ.

# 3 Local Computation Algorithm for the Top Eigenvector 

We receive as input query access to a symmetric n × n matrix A with ∥A∥∞ ≤ 1, and accuracy parameter 

ε ∈ (0 , 1), as well as an index i ∈ [n]. The algorithm uses a public source of randomness (which is shared for all queries i ∈ [n]), makes queries to the matrix A, and will output a number xi ∈ R. Using the public source of randomness, the algorithm samples (for some parameter q ∈ (0 , 1) which we specify later), a matrix 

T ∼ R (n, q ) of size t × n.The algorithm is divided into a preprocessing phase which is completely independent of i, and the query phase, which produces the output coordinate xi for the value of i. In a local implementation of the algorithm, one may execute the preprocessing phase prior to each query with the same source of randomness. 

Preprocessing Phase: In the preprocessing phase independent of the input i ∈ [n], the algorithm proceeds by: 1. Query the t2 entries of A1 = TAT⊺.2. Find the top eigenvector y ∈ Rt of A1 with eigenvalue bλmax , and store y and bλmax .Figure 1: Preprocessing Algorithm Notice that the query complexity of the preprocessing phase is at most t2 which is at most O(( nq )2) with high constant probability. Furthermore, the running time of the preprocessing phase consists of the time needed to compute a top eigenvector y which can be done in poly( t) time up to high precision in the Word-RAM model. 8 Having stored the vector y ∈ Rt, the (full) output vector is x = AT⊺y/bλmax , which means that on query i ∈ [n], it suffices for the algorithm to know the i-th row of AT⊺.The above completes the description of the algorithm, and the remainder of the section is devoted to proving the following theorem, which gives the parameter setting and guarantees for approximating the top eigen-vector. In what follows, we refer to λmax (A) as the maximum eigenvalue and λmin (A) as the magnitude of the minimum eigenvalue (which is smaller than 0, or 0 if the matrix is psd). Since we seek additive εn -approximations, we will assume throughout that λmax (A) ≥ c·εn for a large constant factor c (see Remark 7).                                        

> 7Writing X=UΣV⊺as its singular value decomposition, Vis an orthonormal basis of Rdand Uan orthonormal basis of the column span of X. The Moore-Penrose pseudo-inverse of X⊺Xbecomes VΣ−2V⊺, and hence τi=∥U⊺ei∥22. The fact that U
> is an orthonormal basis of the column span of Ximplies the maximum ( Xv )i/∥Xv ∥22over v∈Rdis equivalent to maximizing
> ⟨ei, U z ⟩2/∥U z ∥22over z, which is at most ∥U⊺ei∥22by Cauchy-Schwarz and also attains it by z=U⊺ei.
> 8We will ignore bit-complexity dependencies involves in rounding eigenvectors and eigenvalues.

10 Query Phase: On the query for index i ∈ [n], the algorithm will: 1. Query the t entries at the i-th row of A for the columns selected by T, to compute ( AT⊺)i.2. Output xi = ⟨(AT⊺)i, y⟩/bλmax .Figure 2: Query Algorithm Furthermore, even though the parameter settings below depend on λmax (A) and λmin (A), it suffices to know an additive εn approximation to these, which one can obtain without any asymptotic overhead using the eigenvalue estimation algorithm of [SW25]. 

Theorem 6. For any n × n symmetric matrix A with ∥A∥∞ ≤ 1 and any ε > 0, let x ∈ Rn be the output of the algorithm on A, initialized with 

q = max 

 1

ε2n , n1/3 · λmin (A)4/3

ε2/3 · λmax (A)8/3



· polylog(1 /ε ),

Then, 

Pr 



max 

> x0∈Rn

⟨x0, Ax 0⟩∥x0∥22

− ⟨x, A x⟩∥x∥22

≤ εn 



≥ 0.99 We note that any setting of λmin (A) = O(λmax ) results in a bound on q which is at most 1 /(ε2n)·polylog(1 /ε )since λmax (A) ≥ εn . Furthermore, the worst-case occurs when λmax (A) = Θ( εn ) and λmin (A) = Θ( n), in which case the above bound on q becomes 1 /(ε3.3n) · polylog(1 /ε ). It is an interesting problem to handle this “negative-eigenvalue interference” without any complexity degradation, since this would require new ideas. We refer the reader to Appendix B, where we provide a concrete example where our analysis does incur some “negative-eigenvalue interference.” 

Remark 7 (Assuming λmax (A) ≥ c·εn for a large c > 0) . First, suppose λmax (A) is positive, and let c0 ∈ R≥0

such that λmax (A) = c0 · εn . If c0 is larger than an absolute constant (say, 10 −10 ), then we consider executing the algorithm with ε0 = ( c0/c )ε, and hence we are in the situation where λmax (A) = c0 · εn ≥ c · ε0n.Otherwise, in order to meet our desired guarantee, it suffices to output query access to a vector x which satisfies ⟨x, A x⟩/∥x∥22 ≥ − (ε/ 2) n. We let x be a uniform random vector from the unit sphere, and by rotational invariance, we may assume that our Rayleigh quotient is Pni=1 λix2 

> i

. Note, the expected Rayleigh quotient is exactly Tr( A)/n ∈ [−1, 1] . For the variance, we use the fact that the fourth moment E[x4 

> i

], as well as E[x2 

> i

x2 

> j

] for a uniform random unit vector x are both O(1 /n 2). As a result, the expected square of the Rayleigh quotient is at most Pni=1 λ2 

> i

+ P 

> i̸=j

λiλj ≤ n2 + Tr( A)2 divided by n2, giving us a constant second moment. By Chebyshev’s inequality, x is very likely to have Rayleigh quotient which is at least −O(1) ≥ − εn 

with high constant probability. 

In our applications to the sparsest cut and max cut, it will become important to “boost” the error probability to 1 −δ. Even though Theorem 6 is stated for success probability 0 .99, a benefit of the fact that our algorithm outputs query access to an eigenvector means that we can verify a lower bound on ⟨x, A x⟩/∥x∥22 via sampling. This allows us to “boost” our error probability to 1 − δ via multiple independent executions of Theorem 6 and verifying the outputs via sampling. Specifically, let k = O(log(1 /δ ) and let x(1) , . . . x(k) be the result of k independent calls to Theorem 6. By independence, the probability that i ∈ [k] outputs have Rayleigh quotient with A less than λmax (A)−εn is at most 0 .01 k ≤ δ. Furthermore, we can estimate the numerator and denominator of the Rayleigh quotient up to an additive εn and ε error respectively, using poly(log(1 /δ )/ε )queries to x and A, since we know ∥A∥∞ ≤ 1 and ∥x∥2 

> ∞

≤ 1/(ε2n). 11 4 Analysis of the Algorithm 

## 4.1 Theorem 6 assuming Lemma 4.4, 4.2 and 4.3 

For the proof of Theorem 6, we consider a fixed n × n symmetric matrix A with ∥A∥∞ ≤ 1, as well as a fixed error parameter ε > 0. We consider the following deterministic events, which we will show occur with high constant probability, and we will show these ensure the conclusion of Theorem 6. In particular, the events depend on parameters α ∈ (0 , 1) which should be thought about being 1 /polylog(1 /ε ), and L ≥ 0 (which we set shortly) and should be thought of as being smaller than the allowable error (i.e., o(εn )). Both n ∈ N and 

ε > 0 should be viewed as asymptotic parameters with n → ∞ and ε → 0. 

Definition 4.1 (The Events) . For α ∈ (0 , 1) and 0 ≤ L = o(εn ), let E denote the indicator random variable that the following events (over the randomness in the draw of T) hold. 

• The matrix T is an αL/λ -distortion subspace embeddings for the span of eigenvectors of A with eigen-value of magnitude at least λ, for all λ ≥ L.

• The matrix T satisfies |λmax (TAT⊺) − λmax (A)| ≤ αL and |λmin (TAT⊺) − λmin (A)| ≤ αL .

• Letting AL denote the “portion” of A with eigenvalues of magnitude at most L (see Definition 4.10 in Subsection 4.3), there is a fixed constant c0 > 0 where ∥ALT⊺∥2 and ∥TALT⊺∥2 are at most c0L.9

For the remainder of the analysis, we will assume that the conditions of E hold deterministically for a parameter L which we will precisely set later (see Lemma 4.4 below). It will be useful to introduce the parameter γ ≥ 0 which will measure our amount of “negative-eigenvalue interference,” and which depends on λmax (A), λ min (A) as well as L; we set γ to 

γ def 

=

pLλ min (A)

λmax (A) . (3) In our analysis, we will need to set the parameter L such that the following two lemmas (Lemma 4.2 and Lemma 4.3) give bounds which are at most εn/λ max and εn .

Lemma 4.2. Whenever E holds, 

∥x∥22 − ∥ y∥22 ≲ L · (1 + γ2)

λmax (A) · polylog(1 /ε ).

Lemma 4.3. Whenever E holds, 

|⟨ y, A1y⟩ − ⟨ x, A x⟩| ≲ L · (1 + γ4) · polylog(1 /ε ).

Given that the above two lemmas should be εn/λ max and εn , respectively, we set L according to 

L = min 

εn, εn ·

 λmax (A)2

λmin (A)

2!1/3 · 1polylog(1 /ε ) . (4)        

> 9We should note that both prior works [BDD +24, SW25] utilize an argument of [Tro12] to upper bound ∥TALT⊺∥2and lose aO(log n)-factor. This logarithmic factor propagates throughout the analysis, even though [SW25] later manage to remove it via a recursive analysis. It turns out, a closer look at [Tro12] (as well as the precursor [RV07]) reveals that the dependence is
> O(log( nq )) which removes the logarithmic factor in our application of q= 1 /(poly( ε)n). This seemed to have been missed in the prior work, and we give the argument in Appendix A.

12 Note, the above setting is such that Lemma 4.2 is at most εn/λ max (A) and Lemma 4.3 is at most εn . As we will see (in the next lemma), the setting of L is directly tied to the query complexity, and smaller values of 

L lead to larger query complexities. From (4), L is set to εn/ polylog( n) whenever λmin (A) is not too much larger than λmax (A)—in general, when λmin (A) is much larger than λmax (A), L will be a “geometric mean” of εn and λmax (A)2/λ min (A) (twice), which leads to a worse query complexity whenever λmax (A)2/λ min (A)is smaller than εn .

Lemma 4.4. Suppose we let q such that 

q = nL2 · polylog( n/L ).

the event E holds with probability 0.99 .

We note that the above setting of q leads to a query complexity of ( nq )2 for the preprocessing stage and nq for the query phase. This evaluates to a preprocessing complexity of 1 /ε 4 · polylog(1 /ε ) and query complexity of 1/ε 2 · polylog(1 /ε ) whenever λmin (A) = O(λmax (A)) as desired. Note, an extremal case for the worst-possible query complexity above happens when λmin (A) is as large as possibe and λmax (A) as small as possible (since this makes the geometric mean in L as small as possible, and leads to higher query complexity). For example, when λmin (A) = Θ( n) and λmax (A) = Θ( εn ), L gets set to ε5/3n · polylog(1 /ε ), and leads to preprocessing complexity 1 /ε 6.6 and query complexity 1 /ε 3.3 up to poly-logarithmic factors. 

Proof of Theorem 6 assuming Lemmas 4.4 4.2, 4.3. We consider the setting of L in (4) so as to assume the bound of Lemma 4.2 is εn/λ max and the bound of Lemma 4.3 is εn , and q = n/L 2 · polylog( n/L ) results in a bound of: 

q = max 

 1

ε2n , n1/3λmin (A)4/3

ε2/3λmax (A)8/3



· polylog(1 /ε ),

and assume that Lemma 4.4 holds, so we may assume that event E holds. We note that Lemmas 4.2, 4.3 allows us to compare the Rayleigh quotients of y in A1 and x in A. Namely, we upper bound (using the fact 

∥y∥2 = 1), 

⟨y, A1y⟩∥y∥22

− ⟨x, A x⟩∥x∥22

≤ |⟨ y, A1y⟩ − ⟨ x, A x⟩| + ⟨x, A x⟩∥x∥22

∥x∥22 − ∥ y∥22

≲ εn + ⟨x, A x⟩∥x∥22

· εn λmax (A) ≲ εn + ⟨y, A1y⟩∥y∥22

− ⟨x, A x⟩∥x∥22

· εn λmax (A) ,

where the final inequality added and subtracted ⟨y, A1y⟩, and we know |⟨ y, A1y⟩| = O(λmax (A)), from the third condition of Definition 4.1. Re-arranging the above terms, we obtain 

⟨y, A1y⟩∥y∥22

− ⟨x, A x⟩∥x∥22

· (1 − O(εn/λ max (A))) ≲ εn =⇒ ⟨y, A1y⟩∥y∥22

− ⟨x, A x⟩∥x∥22

≲ εn, 

since ε can be set so that εn is a small enough constant factor of λmax (A). The fact that y is an eigenvector of A1 which obtains eigenvalue at least λmax (A) − αL , by the third condition of Definition 4.1, implies max 

> x0∈Rn

⟨x0, Ax 0⟩∥x0∥22

− ⟨x, A x⟩∥x∥22

≤ αL + ⟨y, A1y⟩∥y∥22

− ⟨x, A x⟩∥x∥22

≲ αL + εn. 

Notice that, the above argument concludes that the difference in Rayleigh quotients is O(εn ), and the claimed 

εn bound can be done by instantiating ε to be a fixed constant factor smaller. It remains to prove Lemmas 4.4, 4.2 and 4.3, which we do in Subsections 4.5, 4.6 below. In order to prove these, we introduce the following structural properties and auxiliary definitions. 13 4.2 Proof of Lemma 4.4 

The first bullet point of Definition 4.1 will follow directly from [SW25]; we re-create their proof (making minor modifications to parameter settings). For any λ ≥ L, let U≥λ denote the orthonormal matrix whose columns are eigenvectors of A with eigenvalue having magnitude at least λ.

Claim 4.5. The number of columns in U≥λ is at most n2/L 2, and for every i ∈ [n], ∥(U≥λ)i∥22 ≤ n/λ 2.

The first part of Claim 4.5 follows from the fact ∥A∥2 

> F

≤ n2 is also the sum of squares of all eigenvalues. Hence, the number of eigenvalues which are larger than λ ≥ L is at most n2/λ 2 ≤ n2/L 2. For the second part, we use Lemma 3.3 of [SW25], the squared Euclidean norm of the rows is bounded by n/λ 2, i.e., for any 

i ∈ [n], ∥(U≥λ)i∥22 ≤ n/λ 2.

Claim 4.6. For any i ∈ [n], the i-th leverage score of U≥λ (recall, Definition 2.3) is at most 

max 

> v∈Rd

⟨(U≥λ)i, v ⟩2

∥U≥λv∥22

≤ max 

> v∈Rd

∥(U≥λ)i∥22 · ∥ v∥22

∥v∥22

≤ nλ2 ,

where the numerator bound follows from Cauchy-Schwarz and the denominator from the fact U≥λ is orthonor-mal. 

By Theorem 5 with ε = αL/λ , a setting of 

q ≳ (n/L 2) log( n/ (Lδ )) /α 2 ≥ min τi log( |U≥λ|/δ ′)/(αL )2 · λ2, 1 ,

suffices to ensure that for any fixed λ ≥ L, a draw of T gives a αL/λ -distortion subspace embedding with probability 1 − δ. Considering the collection of λ of the form L · 2r for r = 0 , . . . , ⌈log 2 n/L ⌉, and setting 

δ = 1 /(1000 log 2(n/L )) suffices to union bound over all λ as well as T and obtain success probability 1 − 1/1000 for the first bullet point. This completes the proof that the first bullet point occurs with high constant probability. For the second bullet point, we directly apply Theorem 2.2 of [SW25] (reproduced below) with an error parameter ε′ so that ε′n is at most αL (and boosting the success probability from the claimed 2 /3 to 1 − 1/1000). Note, this occurs so long as q is at least some constant factor of 1 /(n · (ε′)2) log 2(1 /ε ′) ≥

(n/L 2) log 2(n/ (αL )) /α 2, and this completes the third bullet point. 

Theorem 8 (Theorem 2.2 of [SW25]) . For any symmetric n × n matrix A with ∥A∥∞ ≤ 1, any ε ∈ (0 , 1) ,and any q ≳ log 2(1 /ε )/(ε2n), the eigenvalues of TAT⊺ (and additional 0’s) give additive ±εn approximation to the eigenvalues of A with probability at least 2/3.

For the fourth bullet point, we apply Theorem 14 and Theorem 15, as well as Markov’s inequality. Namely, 

E 

> T

[∥ALT⊺∥2] ≲ ∥AL∥2 + plog( nq )/q · ∥ AL∥1→2,

E 

> T

[∥TALT⊺∥2] ≲ ∥AL∥2 + plog( nq )/q · ∥ AL∥1→2 + log( nq )/q · ∥ AL∥∞,

and we claim both bounds are O(L) with appropriate choice of q by bounding each of the above terms. 

Claim 4.7. The value ∥AL∥1→2, i.e., maximum column norm of AL, is at most √n.

This follows from the fact columns of AL are projections of columns of A onto span of eigenvectors with eigenvalue of magnitude at most L. This must only decrease the norm, so the maximum column norm of AL

is at most that the maximum column norm of A, which is at most √n since ∥A∥∞ ≤ 1. 

Claim 4.8. The value ∥AL∥∞ ≤ 1 + n/L .

14 The argument appears in Lemma 4 of [BDD +24], using the fact ∥AL∥∞ ≤ ∥ A∥∞ + ∥AH ∥∞ (here, AH =

A−AL = U≥LΛ≥LU ⊺ 

> ≥L

is the “portion” of A whose eigenvectors have eigenvalue of magnitude at least L—see Definition 4.10). It remains to bound ∥AH ∥∞, so consider any fixed i, j ∈ [n], and notice 

|(AH )i,j | = ⟨(U≥L)i, (U≥LΛ≥L)j ⟩ = ⟨(U≥L)i, (AU ≥L)j ⟩≤ ∥ (U≥L)i∥2 · ∥ (AU ≥L)j ∥2 ≤ √n/L · ∥ Aj ∥2 = n/L, 

where the first inequality is Cauchy-Schwarz, and the second uses Claim 4.5 and the fact U≥L is orthonormal. Ensuring that q ≥ (n/L 2) · log( nq ) guarantees both expectations are at most O(L), which allows us to apply Markov’s inequality. 

## 4.3 Spectral Decompositions 

We first define a few useful decompositions of the matrix A which we will use throughout the analysis. These involve decomposing the spectrum of A into parts. 

Definition 4.9 (Spectral Decomposition) . We express A = U ΛU ⊺ as its singular value decomposition. 

• U is a matrix whose columns form the eigenvectors of A, and 

• Λ is an n × n diagonal matrix of eigenvalues. Let Λ1/2 is the n × n diagonal matrix whose entries are p|λi| , and sign(Λ) is the n × n diagonal matrix whose entries are sign( λi) ∈ {− 1, 1} for each of the eigenvalues λi of A.

In what follows, we define some decompositions of A. These will always involve selecting a subset of the eigenvectors and corresponding eigenvalues of A.

Definition 4.10 (Positive-Negative-Low Decomposition) . We let A = AP −AN +AL, where AP = UP ΛP U ⊺

> P

and AN = UN ΛN U ⊺ 

> N

and AL = ULΛLU ⊺

> L

, and let AH = AP − AN .

• The matrix UP has columns given by the subset of eigenvectors from U whose corresponding eigenvalue is positive and larger than L.

• The matrix UN has columns given by the subset of eigenvectors from U whose corresponding eigenvalue is negative and smaller than −L, and let ΛN be the diagonal matrix of magnitudes of negative eigenvalues. 

• The matrix UL has columns given by the subset of eigenvectors from U whose corresponding eigenvalue has magnitude at most L.We let λmax = λmax (A), and r = ⌈log( λmax /L )⌉,10 and write 

AP =

> r

X

> h=0

U + 

> h

Λ+ 

> h

(U + 

> h

)⊺,

where the matrix U + 

> h

contains columns of UP whose corresponding eigenvalue has magnitude between λmax /2h

and λmax /2h+1 . The matrix U + 

> −0

contains columns for all eigenvectors of UP which are not in U +0 , and Λ+

> −0

is the corresponding diagonal matrix of eigenvalues.          

> 10 In the parameter settings, λmax is at most nand Lwill be on the order of poly( ε)n; hence, one should consider ras being logarithmic in 1 /ε , with no dependence on n.

15 Similarly, we let λmin = λmin (AN ), and k = ⌈log( λmin /L )⌉, and write 

AN =

> k

X

> ℓ=0

U − 

> ℓ

Λ− 

> ℓ

(U − 

> ℓ

)⊺,

where the matrix U − 

> ℓ

contains columns of UN whose corresponding eigenvalue has magnitude between λmin /2ℓ

and λmin /2ℓ+1 , and Λ− 

> ℓ

is the corresponding diagonal matrix of eigenvalues. 

The following estimates will be used throughout the paper, and are particularly important for Lemma 4.14. We prove them here by applying the conditions of E. It is convenient in the lemma below to think of the easier case of λmin = O(λmax ), as this would simplify some of the expressions below. 

Lemma 4.11. There exists a fixed constant c2 > 0 such that whenever E holds 

• ∥(Λ +0 )1/2((( U +0 )⊺U +0 ) − (TU +0 )⊺(TU +0 ))(Λ +0 )1/2∥2 ≤ c2L.

• ∥(Λ +

> −0

)1/2((( U +

> −0

)⊺U +

> −0

) − (TU +

> −0

)⊺(TU +

> −0

))(Λ +

> −0

)1/2∥2 ≤ c2

√rλ max L.

• ∥Λ1/2 

> N

(( U ⊺ 

> N

UN ) − (TUN )⊺(TUN ))Λ 1/2 

> N

∥2 ≤ c2

√kLλ min .

• ∥(Λ +0 )1/2(TU +0 )⊺(TU +

> −0

)(Λ +

> −0

)1/2∥2 ≤ c2

√λmax L.

• ∥(Λ +0 )1/2(TU +0 )⊺(TUN )Λ 1/2 

> N

∥2 ≤ c2

√λmax L (1 + γ).

• ∥(Λ +

> −0

)1/2(TU +

> −0

)⊺(TUN )Λ 1/2 

> N

∥2 ≤ c2

 √λmax L + √λmin L.Proof. The first bound follows from the fact T gives an αL/λ max -distortion subspace embedding of U +0 . The subsequent bounds follow the same structure as [SW25]. For the second bullet point, the spectral norm is upper bounded by the spectral norm of the following r ×r matrix M , where the ( h1, h 2) entry (when h1̸ = h2)is given by 

Mh1,h 2 = ∥(Λ + 

> h1

)1/2(TU + 

> h1

)⊺(TU + 

> h2

)(Λ + 

> h2

)1/2∥2 ≤ 2 · λmax 

2h1/2 · 2h2/2 · αL · max {2h1 , 2h2 }

λmax 

≲ L ·

p

2|h1−h2|,

since T is a αL · max {2h1 , 2h2 }/λ max -distortion subspace embedding of [ U + 

> h1

|U + 

> h2

]. On the other hand, the diagonal entries for h are 

Mh,h = ∥(Λ + 

> h

)1/2  (U + 

> h

)⊺(U + 

> h

) − (TU + 

> h1

)⊺(TU + 

> h2

) (Λ + 

> h2

)1/2∥2 ≤ 2λmax 

2h · αL · 2h

λmax 

≲ L. 

The squared Frobenius norm of M (which upper bounds the square of the spectral norm) is at most, up to constant factor, L2 Prh1=1 

Prh2=1 2|h1−h2| ≤ rLλ max . The analogous argument implies the third spectral norm bound. For the fourth bullet, consider the 1 × r matrix M where M1,h is given by 

M1,h = ∥(Λ +0 )1/2(TU +0 )⊺(TU + 

> h

)(Λ + 

> h

)1/2∥2 ≤ 2λmax 

2h/ 2 · αL · 2h

λmax 

≲ L2h/ 2,

so that ∥M ∥2 

> F

≲ L2 Prh=1 2h ≲ Lλ max . Similarly, the fifth bullet sets the 1 × k matrix M where M1,ℓ is given by ∥(Λ +0 )1/2(TU +0 )⊺(TU − 

> ℓ

)(Λ − 

> ℓ

)1/2∥2, which is at most √λmax λmin /2ℓ/ 2 · αL  1/λ max + 2 ℓ/λ min 

, so that ∥M ∥2 

> F

≲ L2(λmin /λ max ) + Lλ max ; we obtain the desired bound by taking the square-root and plugging 16 in the value of γ from (3). For the final spectral norm bound, the analogous argument bounds the squared Frobenius norm of the r × k matrix M where 

Mh,ℓ = ∥(Λ + 

> h

)1/2(TU + 

> h

)⊺(TU − 

> ℓ

)(Λ − 

> ℓ

)1/2∥2 ≲

√λmax λmin 

2h/ 2 · 2ℓ/ 2 · αL 

 2h

λmax 

+ 2ℓ

λmin 



≲ L

r λmin 

λmax 

· 2h/ 2

2ℓ/ 2 +

r λmax 

λmin 

· 2ℓ/ 2

2h/ 2

!

,

so the squared Frobenius norm is at most, up to constant factors, Lλ min + Lλ max .

## 4.4 The Lift 

We now define the key (random) vectors which form part of our analysis, and which allow us to go from the top eigenvector y ∈ St computed in the “subsampled” or “sketched” space, and lift it to the vector x ∈ Rn

the algorithm outputs. We define a vector x ∈ Rn which we call the “lift” of y. Note, randomness is with respect to the draws of T.

Definition 4.12 (The Lift) . As specified in Figure 1, A1 = TAT⊺ and y ∈ St is the top eigenvector of A1

with eigenvalue λmax (TAT⊺) = bλmax . The lift x ∈ Rn is given by 

x = 1

bλmax 

· AT⊺y,

where we defined x such that Tx = TAT⊺y/bλmax = y. It will be helpful in the analysis to define the following vectors: 

• ˜w ∈ Rn is given by ˜w = sign(Λ)Λ 1/2(TU )⊺y, and note that TU Λ1/2 ˜w = A1y = bλmax y.

• ˜x ∈ Rn is the vector given by ˜x = U Λ1/2 ˜w, and again, note T˜x = bλmax y.

• Let x = ˜ x/bλmax , which implies Tx = y.Note that coordinates of ˜w ∈ Rn correspond to eigenvectors of U , so we may decompose the vector ˜w following the decompositions of Subsection 4.3. In particular, 

• ˜w = ˜ wH + ˜ wL, where ˜wH = sign(Λ H )Λ 1/2 

> H

(TUH )⊺y, and ˜wL = sign(Λ L)Λ 1/2 

> L

(TUL)⊺y.11 

• ˜wH = ˜ wP + ˜ wN , where ˜wP = Λ 1/2 

> P

(TUP )⊺y, ˜wN = −Λ1/2 

> N

(TUN )⊺y.

• ˜wP = Prh=0 ˜w+ 

> h

where ˜w+ 

> h

= (Λ + 

> h

)1/2(TU + 

> h

)⊺y.

• ˜wN = Pkℓ=0 ˜w− 

> ℓ

where ˜w− 

> ℓ

= −(Λ − 

> ℓ

)1/2(TU − 

> ℓ

)⊺y.We define the normalization wH = ˜ wH /∥ ˜wH ∥2, and analogously define the decompositions wH = wP + wN

and wP = Prh=0 w+ 

> h

and wN = Pkℓ=0 w− 

> ℓ

, and w−0 = Prh=1 w+ 

> h

.     

> 11 As written, there is a minor type-check issue, since ˜ wHand ˜ wLwill not be in Rn(the sum of their dimensionalities will be n), since they are non-zero in a disjoint set of coordinates; the sum is interpreted as placing the non-zero entries in their corresponding positions.

17 4.4.1 Coarse Structure of the Lift 

The subsequent lemmas show that, the top eigenvector y ∈ St found by our algorithm in Line 2 of Figure 1 will necessarily impose coarse structure on the lift x and w. This coarse structure will be necessary when relating the Rayleigh quotients of the analytic lift in A with bλmax (which is the Rayleigh quotient of y in the “sketch” matrix TAT⊺). The first lemma below shows that the lift x must have significantly more “ ℓ2-mass” along eigenvectors of UP , as opposed to eigenvectors of UN . The second lemma is a more refined statement, that the lift x must not only have most of the “ ℓ2-mass” along eigenvectors of UP , but also most of the “ℓ2-mass” along eigenvectors in U +0 .

Lemma 4.13. There is a fixed constant c1 > 0, such that whenever E holds, 

• λmax − c1L ≤ ∥ ˜wP ∥22 ≤ λmax + c1Lr 

• ∥ ˜wN ∥22 ≤ c1Lr .

• ∥ ˜wH ∥22 ≤ λmax + c1Lr .Proof. The fact y ∈ St is an eigenvector of TAT⊺ with eigenvalue bλmax implies that 

bλmax = ⟨y, TAT⊺y⟩ ≤ ⟨ y, TAP T⊺y⟩ − ⟨ y, TAN T⊺y⟩ + ∥TALT⊺∥2

≤ Λ1/2 

> P

(TUP )⊺y 22

− Λ1/2 

> N

(TUN )⊺y 22

+ c0L = ∥ ˜wP ∥22 − ∥ ˜wN ∥22 + c0L. (5) Note that this directly gives the lower bound for the first inequality, since the second condition of E in Definition 4.1 implies bλmax ≥ λmax − αL ; we obtain the desired bound so long as c1 ≥ c0 + α. Furthermore, 

∥ ˜wP ∥22 ≤ λmax (TAP T⊺), and the latter is at most λmax + c0Lr by directly applying Lemma 5.6 of [SW25]. This establishes the upper bound of the first item when c1 ≥ c0. Re-arranging terms in (5), we obtain the third inequality whenever c1 ≥ 2c0 + α. The final inequality is a direct consequence of ∥ ˜wH ∥22 = ∥ ˜wP ∥22 + ∥ ˜wN ∥22,whenever c1 ≥ 3c0 + α.

Lemma 4.14. There exists a fixed constant c3 > 0 such that whenever E holds, ∥w+

> −0

∥22 ≤ c3Lr/λ max (1+ γ)2.Proof. First, we note that the vector ˜ wH = sign(Λ H )Λ 1/2 

> H

(TUH )⊺y, which means that the following sequence of (in)equalities is satisfied: 

⟨wP , Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

˜wH ⟩ ≥ ⟨ wP , Λ1/2 

> P

(TUP )⊺TU Λ1/2 ˜w⟩ − Λ1/2 

> P

(TUP )⊺TULΛ1/2 

> L

˜wL 2

= bλmax ⟨wP , Λ1/2 

> P

(TUP )⊺y⟩ − Λ1/2 

> P

(TUP )⊺

> 2

· ∥ TALT⊺y∥2

≥ bλmax · ∥ ˜wP ∥22

∥ ˜wH ∥2

− 2c0Lpλmax , (6) where we used TU Λ1/2 ˜w = bλmax y, as well as the fact ∥Λ1/2 

> P

U ⊺ 

> P

T⊺∥2 ≤ √λmax ∥TUP ∥2 ≤ 2√λmax , since T is a α-distortion subspace embedding of UP . Then, Lemma 4.13 implies that the right-hand side of (6), since √λmax ≤ 2∥ ˜wP ∥2 because c1L = o(εn ) ≤ λmax /2, is at least 

bλmax · ∥ ˜wP ∥22

∥ ˜wH ∥2

− 4c0L · ∥ ˜wP ∥2 ≥ ∥ ˜wH ∥2

bλmax (1 − ∥ ˜wN ∥22/∥ ˜wH ∥22) − 4c0L



≥ ∥ ˜wH ∥2 (λmax − cLr ) ,

for some constant c, since ∥ ˜wN ∥22/∥ ˜wH ∥22 ≤ O(Lr/λ ). Normalizing (since wH is a unit vector), we obtain the inequality: 

λmax − cLr ≤ ⟨ wP , Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

wH ⟩. (7) 18 On the other hand, we may also upper bound the right-hand side above by, 

⟨wP , Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

wH ⟩ ≤ ∥ Λ1/2 

> P

wP ∥22

+ ∥(Λ +0 )1/2(( U +0 )⊺U +0 − (TU +0 )⊺(TU +0 ))(Λ +0 )1/2∥2∥w+0 ∥22

+ ∥(Λ +

> −0

)1/2(( U +

> −0

)⊺U + 

> −0

− (TU +

> −0

)⊺(TU +

> −0

))(Λ +

> −0

)1/2∥2∥w+

> −0

∥22

+ 2 ∥(Λ +0 )1/2(TU +0 )⊺(TU +

> −0

)(Λ +

> −0

)1/2∥2 · ∥ w+0 ∥2∥w+

> −0

∥2

+ 2 ∥(Λ +0 )1/2(TU +0 )⊺(TUN )Λ 1/2 

> N

∥2 · ∥ w+0 ∥2∥wN ∥2

+ 2 ∥(Λ +

> −0

)1/2(TU +

> −0

)⊺(TUN )Λ 1/2 

> N

∥2 · ∥ w+

> −0

∥2∥wN ∥2,

which we upper bound from Lemma 4.11 by, (λmax + c2L) · ∥ w+0 ∥22 +

 λmax 

2 + c2

prλ max L



· ∥ w+

> −0

∥22 + 2 c2

pλmax L∥w+0 ∥2∥w+

> −0

∥2

+ 2 c2

pλmax L (1 + γ) · ∥ w+0 ∥2∥wN ∥2 + 2 c2

p λmax L + pλmin L



∥w+

> −0

∥2∥wN ∥2.

and using that ∥w+0 ∥22 ≤ 1 − ∥ w+

> −0

∥22, and that ∥wN ∥2 ≤ p2c1Lr/λ max , the above expression simplifies to the following: 

λmax + c′ · L√r(1 + γ) −

 λmax 

2 − c2

prλ max L



∥w+

> −0

∥22 + c′pλmax Lr (1 + γ) · ∥ w+

> −0

∥2,

for some constant c > 0. Combined with (7) for the lower bound, we obtain: 

λmax 

3 · ∥ w+

> −0

∥22 ≤ (c + c′)Lr (1 + γ) + c′pλmax Lr · (1 + γ) · ∥ w+

> −0

∥2,

which implies ∥w+

> −0

∥22 ≤ c3Lr/λ max (1+ γ)2, for some constant c3 > 0, which gives the desired bound plugging in γ from (3). 

## 4.5 Proof of Lemma 4.2 

The proof of the lemma will proceed from the following two intermediate claims. 

Claim 4.15. Whenever E from Definition 4.1 holds, 

∥x∥22 − ∥ y∥22 ≲ 1

λmax 

UH Λ1/2 

> H

wH 

> 22

− TUH Λ1/2 

> H

wH

> 22

+ O

 Lλmax 



.

Proof. First, Tx = y and use Definition 4.12 to re-write in terms of ˜ w and bλmax as 

∥x∥22 − ∥ y∥22 = ∥x∥22 − ∥ Tx∥22 = 1

bλ2max 

∥U Λ1/2 ˜w∥22 − ∥ TU Λ1/2 ˜w∥22 . (8) We decompose the right-hand side of (8) in terms of UH and UL. The term U Λ1/2 ˜w is simple to handle, since 

UH and UL are orthogonal. Namely, ∥U Λ1/2 ˜w∥22 is exactly ∥UH Λ1/2 

> H

˜wH ∥22 plus ∥ULΛ1/2 

> L

˜wL∥22. Furthermore, the definition of ˜ wL from Definition 4.12 implies ∥ULΛ1/2 

> L

˜wL∥2 = ∥ALT⊺y∥2 which is at most O(L) whenever 

E holds. On the other hand, the term TU Λ1/2 ˜w cannot proceed via the same argument since TUH and TUL

are not orthogonal; thus, we resort to the triangle inequality: 

∥TU Λ1/2 ˜w∥2 − ∥ TUH Λ1/2 

> H

˜wH ∥2 ≤ ∥ TULΛ1/2 

> L

˜wL∥2 = ∥TALT⊺y∥2 ≤ O(L)19 whenever E holds. Furthermore, event E holding implies ∥TUH Λ1/2 

> H

˜wH ∥2 ≤ ∥ TAT⊺y∥2 + ∥TALT⊺∥2 which is at most bλmax + O(L). In summary, the fact bλmax = Θ( λmax ), we obtain a bound on the right-hand side of (8), 1

λ2max 

∥UH Λ1/2 

> H

˜wH ∥22 − ∥ TUH Λ1/2 

> H

˜wH ∥22 + O  L2/λ 2max 

 + O (L/λ max ) ,

and implies the desired bound, since wH = ˜ wH /∥ ˜wH ∥2 and ∥ ˜wH ∥22 = O(λmax ). 

Lemma 4.16. There exists a constant c3 > 0 such that, whenever E holds, 

∥UH Λ1/2 

> H

wH ∥22 − ∥ TUH Λ1/2 

> H

wH ∥22 ≤ c3Lrk  1 + γ + γ2 .

Proof. We expand the above squared norms as inner products, and divide wH into three vectors w+0 , w+

> −0

and wN and apply the bounds from Lemma 4.11, Lemma 4.13, and Lemma 4.14. The resulting expression consists of the following the following terms: 

• The term ∥w+0 ∥22 ≤ 1 times ∥(Λ +0 )1/2  (U +0 )⊺(U +0 ) − (TU +0 )⊺(TU +0 ) (Λ +0 )1/2∥2 ≤ O(L). 

• The term ∥w+

> −0

∥22 ≲ Lr/λ max (1 + γ)2 (from Lemma 4.14) is multiplied by the spectral norm bound 

∥(Λ +

> −0

)1/2  (U +

> −0

)⊺(U +

> −0

) − (TU +

> −0

)⊺(TU +

> −0

) (Λ +

> −0

)1/2∥2 = O(√rλ max L). This results in the upper bound of O(( Lr )3/2/√λmax (1 + γ)2). 

• The term ∥wN ∥22 ≲ Lr/λ max (from Lemma 4.13) times ∥Λ1/2 

> N

(U ⊺ 

> N

UN − (TUN )⊺(TUN )) Λ 1/2 

> N

∥2 =

O(√kλ min L). This evaluates to O(Lr √k · γ) by the setting of γ in (3). 

• The term 2 ∥w+0 ∥2∥w+

> −0

∥2 which is at most O(Lr/λ max )1/2(1 + γ) (once, more from Lemma 4.14) times 

∥(Λ +0 )1/2(TU +0 )⊺(TU +

> −0

)(Λ +

> −0

)1/2∥2 = O(√λmax L) (from Lemma 4.11), which contributes O(L√r · (1 + 

γ)). 

• The term 2 ∥w+0 ∥2∥wN ∥2 which is at most O(Lr/λ max )1/2 (from Lemma 4.13) times the spectral norm bound ∥(Λ +0 )1/2(TU +0 )⊺(TUN )Λ 1/2 

> N

∥2, which is at most O(√λmax L(1 + γ)) from Lemma 4.11. This contributes O(L√r · (1 + γ)). 

• The final term 2 ∥w+

> −0

∥2∥wN ∥2 is at most O(Lr/λ max )(1 + γ) (using both Lemma 4.13 and Lemma 4.14 once more) which is multiplied by ∥(Λ +

> −0

)1/2(TU +

> −0

)⊺(TUN )Λ 1/2 

> N

∥2, which is at most O(√λmax L +

√λmin L), which is at most O(Lr (1 + γ)). Summing these up, we can upper bound all terms by Lrk (1 + γ + γ2). Putting Claim 4.15 and Lemma 4.16 together, we obtain a bound of 

∥x∥22 − ∥ y∥22 ≲ 1

λmax 

UH Λ1/2 

> H

wH

> 22

− TUH Λ1/2 

> H

wH

> 22

+ Lλmax 

≲ Lrk λmax 

 1 + γ + γ2 + Lλmax 

.

20 4.6 Proof of Lemma 4.3 

Similarly to the case of Lemma 4.2, we aim to upper bound: 

|⟨ y, A1y⟩ − ⟨ x, A x⟩| ≤ Λ1/2 

> P

(TUP )⊺y 22

− Λ1/2 

> P

U ⊺ 

> P

x 22

+ Λ1/2 

> N

U ⊺ 

> N

x 22

+ ∥Λ1/2 

> N

(TUN )⊺y∥22 (9) + ∥TALT⊺∥2 + ∥AL∥2 · ∥ x∥22

where we drop the final term ∥Λ1/2 

> N

(TUN )⊺y∥22 as it is exactly ∥ ˜wN ∥22 ≤ c1Lr , and we drop ∥TALT⊺∥2 and 

∥AL∥2 · ∥ x∥22, since these are at most O(L). We prove upper bounds on the remaining two terms above in the next lemmas. 

Lemma 4.17. Whenever E holds 

Λ1/2 

> P

(TUP )⊺y 22

− Λ1/2 

> P

U ⊺ 

> P

x 22

= O  Lr (1 + γ + γ2) .

Proof. Substitute y = Tx where x = U Λ1/2 ˜w/bλmax . Then, by the triangle inequality, 

Λ1/2 

> P

(TUP )⊺Tx 22

≤ 1

bλ2max 



∥Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

˜wH ∥2 + ∥Λ1/2 

> P

(TUP )⊺TULΛ1/2 

> L

˜wL∥2

2

,

and we can simplify the right-most term above using ∥TULΛ1/2 

> L

˜wL∥2 = ∥TALT⊺y∥2 = O(L) to obtain the bound 1

bλ2max 



∥Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

˜wH ∥2 + O(pλmax L)

2

≤ 1

bλ2max 

Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

˜wH

> 22

+ 1

bλ2max 

· O



λ3/2max + pλmax L



· pλmax L

≤ 1

bλ2max 

Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

˜wH

> 22

+ O(L).

where we use the fact ∥Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

˜wH ∥2 = O(λ3/2max ). Hence, up to an additive O(L), and using the fact wH = ˜ wH /∥ ˜wH ∥2 with ∥ ˜wH ∥2 = O(√λmax ), it suffices to upper bound 1

λmax 



Λ1/2 

> P

(TUP )⊺TUH Λ1/2 

> H

wH

> 22

− ∥ ΛP wP ∥22



,

which is, in turn, at most the following sum of two terms (ignoring the scaling by 1 /λ max ), 



(Λ +0 )1/2(TU +0 )⊺TUH Λ1/2 

> H

wH

> 22

− Λ+0 w+0

> 22



+ (Λ +

> −0

)1/2(TU +

> −0

)⊺TUH Λ1/2 

> H

wH

> 22

. (10) The second term above is simpler to bound, since we may use the triangle inequality and divide wH =

w+0 + w+ 

> −0

+ wN , resulting in the bound (once more, up to constant factors) of the terms: 

• The contribution of w+0 , given by (Λ +

> −0

)1/2(TU +

> −0

)⊺(TU +0 )(Λ +0 )1/2 22 ∥w+0 ∥22, which is at most O(λmax L)from Lemma 4.11. 

• The contribution of w+

> −0

, given by (Λ +

> −0

)1/2(TU +

> −0

)⊺(TU +

> −0

)(Λ +

> −0

)1/2 22 ∥w+

> −0

∥22, which is at most 

O(λ2max ) (using the fact that T is a O(1)-subspace embedding of U +

> −0

) times O(Lr/λ max )(1 + γ)2 from Lemma 4.14, giving us O(λmax Lr · (1 + γ)2). 21 • Finally, the contribution of wN , given by ∥Λ1/2 

> −0

(TU +

> −0

)⊺(TUN )Λ 1/2 

> N

∥22∥wN ∥22, which is at most O(λmax L+

λmin L) from Lemma 4.11 once more, times O(Lr/λ max ) from Lemma 4.13, and giving a bound of 

O(L2r + λmin L2r/λ max ). This bounds the second term of (10) by O  λmax Lr (1 + γ)2. Turning back to the first term of (10), we also apply the triangle inequality and decompose wH = w+0 + w+ 

> −0

+ wN , but we expand the square carefully. Namely, we upper bound 

(Λ +0 )1/2(TU +0 )⊺TUH Λ1/2 

> H

wH 2 ≤ Λ+0 w+0 2

+ (Λ +0 )1/2  (U +0 )⊺U +0 − (TU +0 )⊺(TU +0 ) (Λ +0 )1/2 ∥w+0 ∥2

+ (Λ +0 )1/2(TU +0 )⊺(TU +

> −0

)(Λ +

> −0

)1/2

> 2

∥w+

> −0

∥2

+ (Λ +0 )1/2(TU +0 )⊺(TUN )Λ 1/2 

> N2

∥wN ∥2

≤ ∥ Λ+0 w+0 ∥2 + O(L) + O(pλmax L) · pLr/λ max (1 + γ)+ O(pλmax L(1 + γ)) · pLr/λ max 

where we use Lemma 4.11, along with the bounds on ∥wN ∥2, ∥w+

> −0

∥2 from Lemma 4.13 and Lemma 4.14. This gives a bound on the difference (note that (10) considers square differences) of 

(Λ +0 )1/2(TU +0 )⊺TUH Λ1/2 

> H

wH 2

− Λ+0 w+0 2 ≲ L√r(1 + γ).

Since ∥Λ+0 w+0 ∥2 ≤ λmax , squaring the above and subtracting ∥Λ+0 w+0 ∥2 gives us a bound of O(λmax L√r). Hence, (10) is at most O(λmax L√r(1 + γ) + L2r(1 + γ)2), and incorporating the scaling of 1 /λ max gives the upper bound (after simplifying, using the fact λmax is always at least L). 

Lemma 4.18. Whenever E holds, 

Λ1/2 

> N

U ⊺ 

> N

x 22

= O(Lrk · (1 + γ + γ2)2).

Proof of Lemma 4.18. We first substitute x = U Λ1/2 ˜w/bλmax which allows us to upper bound ∥Λ1/2 

> N

U ⊺ 

> N

x∥2

by ∥ΛN ˜wN ∥2 /bλmax . By expanding TU Λ1/2 ˜w into a sum of four terms, TU +0 (Λ +0 )1/2 ˜w+0 , TU +

> −0

(Λ +

> −0

)1/2 ˜w+

> −0

,

TUN Λ1/2 

> N

˜wN and TULΛ1/2 

> L

˜wL, as well as the identity U ⊺ 

> N

UN = I, we can isolate Λ N ˜wN , and adding and subtracting cross terms. In particular, 

∥ΛN ˜wN ∥2 ≤ Λ1/2 

> N

(TUN )⊺TU Λ1/2 ˜w 2

+ Λ1/2 

> N

(TUN )⊺TU +0 (Λ +0 )1/2 ˜w+0 2

+ Λ1/2 

> N

(TUN )⊺TU +

> −0

(Λ +

> −0

)1/2 ˜w+ 

> −02

+ Λ1/2 

> N

(TUN )⊺TULΛ1/2 

> L

˜wL 2

+ Λ1/2 

> N

(U ⊺ 

> N

UN − (TUN )⊺(TUN )) Λ 1/2 

> N

˜wN 2

.

Note, the first term ∥Λ1/2 

> N

(TUN )⊺TU Λ1/2 ˜w∥2, multiplied by 1 /bλmax is exactly ∥Λ1/2 

> N

(TUN )⊺y∥2, which we know is at most O(√Lr ) from Lemma 4.13. The second term, using Lemma 4.11, becomes at most 

O(pλmax ) · Λ1/2 

> N

(TUN )⊺TU +0 (Λ +0 )1/2

> 2

= O(√L · λmax (1 + γ)) ,

22 so the scaling by 1 /bλmax makes its contribution O(√L(1 + γ)). The third term is similarly bounded by the spectral norm ∥Λ1/2 

> N

(TUN )⊺TU +

> −0

(Λ +

> −0

)1/2∥2, which is bounded by O(√λmax L + √λmin L) from Lemma 4.11 times O(√λmax ) (for the scaling of ∥ ˜wH ∥2), times ∥w+

> −0

∥2, which is at most O(pLr/λ max (1 + γ)) from Lemma 4.14. Once more, after re-scaling by 1 /bλmax , we obtain a bound on the third term (up to constant factors) of 1

λmax 

· O(pλmax L + pλmin L) · O(pλmax ) · O

r Lr λmax 

(1 + γ)

!

≲ L√r(1 + γ) + √Lr · γ2.

The fourth term becomes O(√λmin · L), and after scaling by 1 /bλmax is O(√L · γ), and finally, the last term is O(√λmax ) times O(√kλ min L) times ∥wN ∥2 = O(pLr/λ max ), which is O(L√rkλ min ), which after the re-scaling by 1 /bλmax is similarly O(√Lrk · γ). Putting Lemma 4.17 and Lemma 4.18 together, we conclude that (9) is at most 

O  Lrk · (1 + γ + γ2 + γ3 + γ4) ,

which means we must set L so as to ensure the above bound is O(εn ). 

# 5 Lower Bound 

In this section, we prove a lower bound on the query complexity required to output an approximately top eigenvector of an n × n symmetric matrix A with ∥A∥∞ ≤ 1. As we will see, the inputs we use will satisfy the additional assumption that ∥A∥2 = O(λmax (A)). 

Theorem 2. Fix any ε ∈ (1 /√n, 1) and suppose A is an algorithm with access to an n × n symmetric matrix 

A with ∥A∥∞ ≤ 1, and one may also assume ∥A∥2 = O(λmax (A)) , satisfying 

Pr 

> A



λmax (A) − ⟨x, A x⟩∥x∥22

≤ εn 



≥ 0.99 ,

where x ∈ Rn is the output of A. Then, A must make Ω( n/ε 2) queries to A.

The proof of Theorem 2 follows by showing a reduction to the problem of estimating the bias of a biased coin. We first begin by formally defining the coin problem, which is known to require Ω(1 /ε 2) samples, and then we show how an algorithm satisfying the guarantees of Theorem 2 with query complexity q implies an algorithm for the coin problem which uses O(q/n ) samples. 

Problem 1 (Coin Problem) . For a fixed parameter ε ∈ (0 , 1/2) and b ∈ {− 1, 1}, let C(εb ) denote the distribution supported on {− 1, 1} with Ec∼C (εb )[c] = 2 εb. The coin problem refers to the following statistical task: 

• A hidden variable b ∼ {− 1, 1} is drawn uniformly at random, and s independent draws are generated 

c1, . . . , cs ∼ C (εb).

• An algorithm for the coin problem is specified by a function f : {− 1, 1}s → {− 1, 1}, and the algorithm outputs bb = f (c1, . . . , cn).It is well-known that any distribution over functions f : {− 1, 1}n → {− 1, 1} (which may utilize randomness independent of b, c1, . . . , cs) which satisfies 

Pr 

> b∼{− 1,1}
> c1,..., cs∼C (εb)

[f (c1, . . . , cs) = b] ≥ 0.923 must have s = Ω(1 /ε 2).12 

Hence, our task will be to extract, from a randomized algorithm for approximating the top eigenvector, a function f which estimates the bias of a coin. To do so, we will first define a distribution over bounded-entry symmetric n × n matrices A in order to use Yao’s principle and consider a deterministic algorithm. 

Definition 5.1. For a fixed ε ∈ (0 , 1/2) and x ∈ {− 1, 1}n, let D1(ε, x ) be the following distribution over 

n × n symmetric matrices A, defined according to the following procedure: 

• Draw a symmetric n × n matrix B ∈ {− 1, 1}n×n with diagonal entries Bii = 1 uniformly at random. 

• The output A ∈ {− 1, 1}n×n is the symmetric matrix given by letting, for each i, j ,

Ai,j = Aj,i ∼

(

xi · xj with probability 2ε

Bi,j with probability 1 − 2ε .

Note for any ε > 0 and x ∈ {− 1, 1}n, a draw A ∼ D 1(ε, x ) always satisfies ∥A∥∞ ≤ 1 and has spectral norm 

∥A∥2 which is at most 2 εn +O(√n) with high probability, and λmax (A) ≥ 2εn −O(√n). 13 Hence, we consider 

ε which is at least a large constant factor of 1 /√n—this best possible since there are at most n2 entries—and we also satisfy the desired promise ∥A∥2 = O(λmax (A). We assume the existence of a randomized algorithm 

A satisfying the requirements of Theorem 2 which may be run with accuracy parameter ε0 = ε/ 100. This implies, via Yao’s principle, a deterministic algorithm A which succeeds at finding an approximately top eigenvector from the distribution over matrices given by first sampling x ∼ {− 1, 1}n and then A ∼ D 1(ε, x). Since we will deal with general (possibly) adaptive query algorithms A, it will be useful to formally specify an algorithm, and note that it suffices to define algorithms operating on n × n symmetric and bounded entry matrices whose entries are {− 1, 1}.

Definition 5.2 (Deterministic Query Algorithm for the Hard Distribution) . Fix ε0 = ε/ 100 , a deterministic algorithm A for approximate eigenvectors of symmetric matrices in {− 1, 1}n×n is a binary decision tree; 

• Every non-leaf vertex is labeled with a pair of indices (i, j ) ∈ [n] × [n], and has two outgoing edges which are labeled with {− 1, 1}.

• Every leaf vertex is labeled with an output vector y ∈ Rn.An execution on input matrix A ∈ {− 1, 1}n×n is a root-to-leaf which (recursively) proceeds by following the edge labeled Aij ∈ {− 1, 1} for index pair (i, j ) at a node, and upon reaching a leaf, outputting the stored vector y. We consider a deterministic algorithm A which satisfies: 

Pr 

> x∼{− 1,1}n
> A∼D 1(ε, x)



λmax (A) − ⟨y, Ay⟩∥y∥22

≤ ε0n



≥ 0.99 , (11) 

where y = A(A) ∈ Rn is the output vector. Let q(A) ∈ N denote the query complexity of the algorithm on input A ∈ {− 1, 1}n×n (i.e., the depth of the root-to-leaf path on A), and for any ℓ ∈ [n], qℓ(A) ∈ N the number of queries to entries in row/column ℓ. We seek to lower bound q = max A∈{− 1,1}n×n q(A).    

> 12 This fact follows from Le Cam’s method and the fact dTV (C(ε)s,C(−ε)s) is an arbitrarily small constant when sis a small enough constant factor of 1 /ε 2, see [Can20].
> 13 This fact follows from Corollary 4.4.7 of [Ver18], see, also the proof of Claim 5.6.

24 5.1 Equivalent Method for Sampling from Hard Distribution 

Recall that, in our reduction, the desired goal is to extract a function f from the deterministic decision tree 

A satisfying (11), and for this purpose, it will be useful to define another (as we will later prove) equivalent mechanism for generating a sample x ∼ {− 1, 1}n and A ∼ D 1(ε, x). 

Definition 5.3. For a fixed ε ∈ (0 , 1/2) , ℓ ∈ [n], z ∈ {− 1, 1}[n]\{ ℓ} and a sequence c1, c 2, . . . c n ∈ {− 1, 1},let D2(ε, ℓ, z, c 1, c 2, . . . c n) be the following distribution over symmetric matrices A, defined according to the following procedure: 1. Draw a symmetric n × n matrix B ∈ {− 1, 1}n×n with diagonal entries Bii = 1 uniformly at random. 2. The output A ∈ {− 1, 1}n×n is the symmetric matrix given by letting, for each i, j ,

Ai,j = Aj,i ∼



1 if i = j = ℓcj zj if i = ℓ or j = ℓzizj else, with probability 2ε

Bi,j else, with probability 1 − 2ε.

The equivalence, which we will heavily rely on in our reduction, is captured by the following lemma. 

Lemma 5.4. For any fixed ε ∈ (0 , 1/2) , consider the following random process. 1. Sample b ∼ {− 1, 1}, then sample c1, . . . , cn ∼ C (εb).2. Draw ℓ ∼ [n] and z ∼ {− 1, 1}[n]\{ ℓ}. Now draw A ∼ D 2(ε, ℓ, z, c1, . . . , cn).3. Let x = ( z1, . . . , zℓ−1, b, zℓ+1 , . . . , zn).4. Output: (b, A, ℓ, z, c1, . . . , cn, x).The marginal distribution of (b, x, ℓ, A) has the following properties 

• x ∼ {− 1, 1}n and A ∼ D 1(ε, x)

• ℓ ∼ [n] and independent of x and A

• xℓ = b

Proof. Note that by construction xℓ = b. We start by proving x ∼ {− 1, 1}n. Consider any fixed setting of 

ℓ = ℓ, now we compute the probability that x = x for any x ∈ {− 1, 1}n. Note that in the process each zi is chosen independently and uniformly in {− 1, 1}, and b is chosen uniformly from {− 1, 1} and independently from z.

Pr  

> b,z

[x = x|ℓ = ℓ] = Pr  

> b

[xℓ = xℓ] ·

> n

Y

> i=1
> i̸=ℓ

Pr z [xi = xi] = Pr  

> b

[b = xℓ] ·

> n

Y

> i=1
> i̸=ℓ

Pr  

> z

[zi = xi] = 12n ,

and this means x is drawn uniformly from {− 1, 1}n. To show that x and ℓ are independent, we evaluate the joint probability of x = x for any x ∈ {− 1, 1}n and ℓ = ℓ for any ℓ ∈ [n]: 

Pr 

> b,z,ℓ

[x = x ∧ ℓ = ℓ] = Pr  

> b,z

[x = x|ℓ = ℓ] · Pr  

> ℓ

[ℓ = ℓ] = 12n · Pr  

> ℓ

[ℓ = ℓ] = Pr 

> x∼{− 1,1}n

[x = x] · Pr  

> ℓ

[ℓ = ℓ],

25 which means x ∼ {− 1, 1}n and ℓ are independent. It remains to show that x ∼ {− 1, 1}n and A ∼ D 1(ε, x). Fix any setting of ℓ = ℓ, and recall we always sample triples ( x, z, b) which set x = ( z1, . . . , zℓ−1, b, zℓ+1 , . . . , zn). If i = j, the entries Ai,i are always set to one, which is also the case in D1(ε, x). Consider the distribution of entries in Ai,j for any i, j where neither is equal to ℓ. By Definition 5.3, Ai,j = zizj , which is equal to 

xixj , with probability 2 ε and Bi,j with probability 1 − 2ε. Since Bi,j is drawn from the same distribution in both D1(ε, x) and D2(ε, ℓ, z, c1, . . . cn). This completes the case of i̸ = j, and neither equals ℓ. The final case considers i = ℓ and j̸ = ℓ (since A is always symmetric). In this case, Definition 5.3 sets Aℓ,j to cj zj ,so the probability that Aℓ,j = bz j = xℓxj is 

Pr 

> b,c1,..., cn,A

[Aℓ,j = bz j ] = Pr 

> c1,..., cn,A

[cj zj = bz j ] = Pr 

> b,cj

[b = cj ] = 1 /2 + ε. 

In a similar vein, we using Definition 5.1, we directly have Aℓ,j = xℓxj with probability 2 ε and uniform (from the fact its set to Bℓ,j ) with probability 1 − 2ε. Hence, Aℓ,j = xℓxj with probability 1 /2 + ε as well. We can conclude that A ∼ D 1(ε, x) for x ∼ {− 1, 1}n and since x independent of ℓ, then A must also be independent of ℓ.

## 5.2 Using the Deterministic Algorithm A to solve the Coin Problem 

The following algorithm is given a sequence of i.i.d. draws c1, . . . cn from a distribution C(εb) with known parameter ε which is a large enough constant factor of 1 /√n and at most 1 /2 and uniform b ∼ {− 1, 1}, and has access to a deterministic algorithm A satisfying (11); we show how to output a random variable bb which agrees with b with probability at least 0 .9. 

Reduction. Given an accuracy parameter ε which is a large enough constant factor of 1 /√n and random variables c1, . . . cn ∼ C (εb), as well as a deterministic algorithm A satisfying (11), we proceed as follows: 1. Draw ℓ ∼ [n] and z ∼ {− 1, 1}[n]\{ ℓ}.2. Draw A ∼ D 2(ε, ℓ, z, c1, . . . cn) as described by Definition 5.1. 3. Execute A on A while ensuring that the number of queries made to the ℓ-th row/column is no greater than 200 q/n . If the number of queries exceeds 200 q/n , output bb ∼ {− 1, 1}. Otherwise, let the vector y ∈ Rn be the output of A.4. Select σ ∈ {− 1, 1} such that P 

> i∈[n]\{ ℓ}

σyi · zi ≥ 0. 5. Output bb = sgn( σyℓ) as the estimate of b.Figure 3: Reduction from A satisfying (11) to the Coin Problem. We first upper bound the probability that the reduction in Figure 3 outputs a random variable bb which disagrees with b. The reduction can output an incorrect bb for a few different reasons, one being that the query complexity to the ℓ-th row/column is too high causing the algorithm to output a random sign as bb,another being that A outputs a vector y with small Raleigh quotient, and the last being that A output a σy

which was simply had the wrong sign at ℓ. The subsequent analysis will use structural properties of A and 

x, as well as the condition (11) to ensure bb is equal to b except with probability 0 .9. 

Definition 5.5. The following events are defined with respect to the random variables b, c1, . . . , cn, ℓ, z, A,

26 y, and bb which are generated as part of an execution of Figure 3. We let x ∈ {− 1, 1}n be set to 

x = ( z1, . . . , zℓ−1, b, zℓ+1 , . . . , zn) ∈ {− 1, 1}n,

as in Lemma 5.4. The following should be thought of as failure events: 

• Let E0 be the event where ∥A − 2εxx ⊺∥2 ≥ c√n, for a large enough constant c > 0.

• Let E1 be the event where qℓ(A) > 200 q/n .

• Let E2 be the event where ⟨y, Ay⟩/∥y∥22 ≤ λmax (A) − ε0n for ε0 = ε/ 100 (Definition 5.2). 14 

• Let E3 be the event where sgn( σyℓ)̸ = b. 15 

The analysis of Figure 1 will proceed by upper-bounding the probability that bb̸ = b by relating to the events 

E0, E1, E2, E3. Notice that it suffices to upper bound 

Pr [bb̸ = b] ≤ Pr [E0] + Pr [E1] + Pr [E2 ∧ E1] + Pr E3 ∧ E2 ∧ E1 ∧ E0

 , (12) where all of the probabilities are over the randomness generated in Figure 3, because whenever none of the above failure events occur, the final output bb in Figure 3 is equal to sgn( σyℓ), which is by the fact E3 does not occur equal to b. This leaves the four probabilistic events on the right-hand side of (12) to upper bound, and we show each is at most 0 .01 in the following claims. 

Claim 5.6. Pr [E0] ≤ 0.01 .Proof. By Lemma 5.4, it suffices to consider the matrix A generated by letting x ∼ {− 1, 1}n and then 

A ∼ D 1(ε, x). Then, we may re-write A = (1 − 2ε)I + 2 εxx ⊺ + Z, where Z is a zero-centered symmetric n × n

matrix whose entries are independent and bounded in magnitude by 2. Corollary 4.4.6 of [Ver18] implies 

∥Z∥2 = O(√n) with high probability, which means that ∥A − 2εxx ⊺∥2 is at most 1 + O(√n). 

Claim 5.7. Pr [E1] ≤ 0.01 .Proof. Using Lemma 5.4 once more, the matrix A is independent from ℓ ∼ [n]. The fact that A has query complexity at most q means that for any draw of A, q1(A) + q2(A) + · · · + qn(A) ≤ 2q (the factor of 2 is from the fact each query pair ( i, j ) can contribute to at most 2 row/columns). Since ℓ is independent, the expectation over ℓ of qℓ(A) is at most 2 q/n , so by Markov’s inequality, it is at most 200 q/n except with probability at most 0 .01. 

Claim 5.8. Pr [E2 ∧ E1] ≤ 0.01 .Proof. Whenever E1 does not occur, Figure 3 produces the vector y which is the output of A on input A.By Lemma 5.4 once more, we may equivalently consider the matrix A as being generated from x ∼ {− 1, 1}n

and A ∼ D 1(ε, x), and therefore, the algorithm A assumption (11) implies that λmax (A) − ⟨ y, Ay⟩/∥y∥22 is at most ε0n, except with probability at most 0 .01, as desired. 

Claim 5.9. Whenever none of the events E0, E1, E2 occur, |{ i ∈ [n] : sgn( xi) = sgn( σyi)| ≥ 0.99 n.              

> 14 Notice, the vector yis only generated in Figure 3 whenever qℓ(A)≤200 q/n , i.e., event E1does not occur.
> 15 Once more (similarly to Footnote 14), E3is only defined when yis generated by Figure 3 and occurs whenever E1does not occur.

27 Proof. We bound sgn( xi) = sgn( σyi) by first bounding ⟨y, x⟩. Note, 

⟨y, Ay⟩∥y∥22

= ⟨y, 2εxx ⊺ + ( A − 2εxx ⊺)y⟩∥y∥22

≤ 2ε · ⟨ y, x⟩2

∥y∥22

+ ∥A − 2εxx ⊺∥2,

and the right-most term above is at most c√n since E0 does not occur. Since neither E1 nor E2 occur, the left-hand side above is also lower bounded by λmax (A) − ε0n, which implies 

⟨y, x⟩2 ≥ (λmax (A) − ε0n − c√n)2ε · ∥ y∥22 ≥  (1 − ε0/(2 ε)) n − (c/ε )√n ∥y∥22 ≥ .99 n · ∥ y∥22

since λmax (A) ≥ 2εn − ∥ A − 2εxx ⊺∥2 ≥ 2εn − c√n, and we use the fact ε0 = ε/ 100 and ε ≥ 200 c/ √n. As a result, the magnitude |⟨ y, x⟩| is at least √.99 n · ∥ y∥2, and a very loose bound implies it is larger than 10 |yℓ|

for large enough n. Since ⟨y, x⟩ differs from P 

> i̸=ℓ

yizi by at most |yℓ|, the choice of σ ∈ {− 1, 1} in Figure 3 will ensure σ⟨y, x⟩ is positive. Therefore, letting S = {i ∈ [n] : sgn( σyi) = xi}, we have that an application of Cauchy-Schwarz, as well as the fact x ∈ {− 1, 1}n implies 

∥y∥2

p|S| ≥ X

> i∈S

|yi| ≥ X

> i∈S

|yi| − X

> i / ∈S

|yi| = σ⟨y, x⟩,

and since the right-hand side is positive, we may square both sides without changing the direction of the inequality. This implies that ∥y∥22|S| is at least ⟨y, x⟩2, which is at least .99 n · ∥ y∥22. This implies the lower bound on |S| as desired. 

Claim 5.10. Pr [E3 ∧ E2 ∧ E1 ∧ E0] ≤ 0.01 .Proof. Whenever none of E0, E1, E2 occur, Claim 5.9 implies that for 0 .99-fraction of coordinates i ∈ [n], sgn( σyi) = xi, and since xℓ = b and ℓ is independent and x and A (using Claim 5.4 once more), ℓ ∼ [n]must fall among the 0 .01-fraction of coordinates that sgn( σyi)̸ = xi for E3 to occur while E0, E1, E2 do not occur, and this concludes the proof. 

## 5.3 Putting Everything Together: Proof of Theorem 2 

For a fixed ε to be at least a large constant-factor of 1 /√n, we consider the distribution over input matrix 

A ∈ {− 1, 1}n×n given by sampling x ∼ {− 1, 1}n and then letting A ∼ D 1(ε, x) from Definition 5.1. If, for ε0 = ε/ 100, there exists a randomized algorithm which outputs a vector whose Rayleigh quotient is within ε0n of maximum using at most q queries, we may consider a deterministic algorithm (in the sense of Definition 5.2) satisfying (11) and using at most the same query complexity q. Then, the reduction in Figure 3 in Subsection 5.2 gives a procedure that given access to n draws c1, . . . , cn ∼ C (εb) for b ∼ {− 1, 1},can output a random variable bb which is guaranteed to agree with b with probability at least 0 .96. The crucial observation is the following: 

• In Figure 3, the query complexity to the row/column ℓ is guaranteed to be at most 200 q/n .

• Furthermore, only entries of A in row/column ℓ are, by the construction of A (see Definition 5.3), dependent on c1, . . . , cn.Thus, even though Figure 3 has access to n draws c1, . . . , cn ∼ C (εb), it only reads at most 200 q/n distinct values of c1, . . . , cn on any execution. Since c1, . . . , cn are all identically distributed, it suffices for the algorithm to read the first s = 200 q/n values and maintain a mapping π : [ s] → [n] which re-assigns the k-th 28 coin to cπ(k). This results in a (randomized) function f : {− 1, 1}s → {− 1, 1} given by the output of Figure 3 for solving Problem 1, and therefore, Ω(1 /ε 2) = s = 200 qn =⇒ q = Ω( n/ε 2),

where q is the query complexity of outputting an vector whose Rayleigh quotient is at least ε0n = εn/ 100 from maximum, and thus, q = Ω( n/ε 20). 

# 6 Local Computation Algorithm for Sparsest Cut 

This section gives an application of Theorem 1 to providing local access to sparse cuts in dense graphs. We do this by giving a local computation algorithm which can execute the constructive proof of Cheeger’s inequal-ity [AM85]. In particular, we prove theorem quoted in the introduction (restated below – see Definition 1.1). 

Theorem 3. There exists a (randomized) local computation algorithm A with query access to the adjacency matrix of a graph G = ([ n], E ) and an accuracy parameter ε > 0.

• For any i ∈ [n], A makes poly(1 /ε ) queries and outputs access “ i ∈ S” for an underlying cut S ⊂ [n].

• Letting S ⊂ [n] be the underlying cut that A generates, 

Pr 

> A

h

ϕG(S) ≤ O(pϕε(G)) + ε

i

≥ 0.99 .

Furthermore, A always runs in poly(1 /ε ) time and uses poly(1 /ε ) space. 

## 6.1 Reduction to (α, β )-Dense Graphs, for α, β = poly( ε): Proof of Theorem 3 

In this section, we show that it suffices to construct a local computation algorithm for certain dense graphs (i.e., graphs where most of the vertices have a high degree). This will be important for our use of Theorem 1, since we will need the normalized adjacency matrix to be a bounded-entry matrix. For this section, consider a fixed input graph G = ([ n], E ) which we provide query access to, as well as a fixed accuracy parameter 

ε > 0 (notice, we may assume that G contains at least εn 2 edges, as otherwise, ϕε(G) is trivially 1). The goal will be to obtain the conclusions of Theorem 1 while assuming a local computation algorithm for certain “dense” graphs (Definition 6.1 below). 

Definition 6.1. For α, β ∈ (0 , 1) , a graph is (α, β )-dense if at most β-fraction of vertices are connected to fewer than α-fraction of vertices. 

Lemma 6.2. For any α, β ∈ (εc, ε/c ) for large enough constant c. Let N ∼ G (n, 4α) be an Erd¨ os-Reyni graph and let H = G ∪ N. The following occurs with high probability over N:

• For any T ⊂ [n] where vol H(T ) ≤ εn 2/2, let H′ be the subgraph of H induced by T . Then, H′ is 

(α, β )-dense and satisfies ϕε/ 2(H′) ≤ 2ϕε(G) + O(α/ε ).

• Any S ⊂ [n] with εn 2/8 ≤ vol H(S) ≤ vol H(S) will satisfy ϕG(S) ≤ ϕH(S) + O(α/ε ).Proof. Notice that the number of edges in H is at least εn 2 (it contains all edges of G, which is size at least 

εn 2), and since there are at most εn 2/2 edges incident on T in H, vol H(T ) ≥ εn 2/2. Degrees are at most n,so T is of size at least εn/ 2. In order to show that H′ is ( α, β )-dense, it suffices to show that any induced 29 subgraph of N containing at least εn/ 2 vertices will be ( α, β )-dense (as H′ can only include more edges than any such subgraph). We show this as a standard application of Chernoff bounds: consider any fixed subset 

L ⊂ [n] of size t ≥ εn/ 2 and any fixed subset B ⊂ L of size βt . We first upper bound the probability that the set B consists of vertices of degree at most αt within the subgraph induced by L:

Pr 

> G′

∀j ∈ B : X

> i∈L\{ j}

1 {(i, j ) ∈ N} ≤ αt 

 ≤ Pr 

"t−1X

> i=1

Xi ≤ αt 

#! |B|

≤ e−Ω( αt |B|),

where since αt is a constant-factor less than half of the expected number of edges incident on any particular vertex j ∈ L in the induced subgraph of N.16 We take a union bound over at most 2 2n choices of L and B,since αt |B| ≥ αβt 2 ≥ αβε 2n2 is significantly larger than n for our setting of ε. This allows us to conclude the union bound. In order to upper bound ϕε/ 2(H′), consider the subset S ⊂ [n] which realizes the bound on ϕε(G) and satisfies εn 2 ≤ vol G(S) ≤ vol G(S), and let S′ = S ∩ T . We upper bound ϕH′ (S′) in terms of ϕG(S). The fact vol H(T ) ≤ εn 2/2 implies vol H′ (S′) ≥ vol H(S) − vol H(T ) ≥ vol G(S) − εn 2/2 ≥ vol G(S)/2,

which is at least εn 2/2, and this lower bounds the denominator of ϕH′ (S′). Then, the fact S′ is a subset of S and T \ S′ is a subset of S implies that EH′ (S′, T \ S′) (which is the numerator of ϕH′ (S′)) is at most EH(S, S ) ≤ EG(S, S ) + 8 αn 2, with high probability (since N contains at most 8 αn 2 edges with high probability). This implies ϕε/ 2(H′) ≤ 2ϕε(G) + 16 α/ε .For the second item, any cut S ⊂ [n] with εn 2/8 ≤ vol H(S) ≤ vol H(S) must necessarily satisfy min {vol G(S), vol G(S)} ≥ 

(ε/ 8 − 8α)n2 (once, more since N contains at most 8 αn 2 edges with high probability). Thus, 

ϕG(S) = EG(S, S )min {vol G(S), vol G(S)} ≤ EH(S, S )min {vol G(S), vol G(S)} ≤ EH(S, S )(1 − 64 α/ε ) min {vol H(S), vol H(S)}≤ ϕH(S) + O(α/ε )as desired. We will assume there exists a local computation algorithm A which finds a sparse cut assuming the under-lying graph is ( α, β )-dense (Theorem 9). Using Lemma 6.2, we analyze the algorithm in Figure 4 to prove Theorem 3, which proceeds by iteratively cutting out sparse cuts and analyzing the induced subgraph of remaining vertices. Figure 4 assumes access to a subroutine Estimate-Vol (S, H, ξ, δ ) which receives query access of the form “ i ∈ S”, as well as query access to the adjacency matrix of a graph H on vertex set [ n]and outputs an estimate bη of vol H (S) which is off by at most ξn 2 with probability 1 − δ. Such an algorithm 

Estimate-Vol (S, H, ξ, δ ) simply works via random sampling, and has query complexity which is a factor of O(log(1 /δ )/ξ 2) overhead on the query complexity of determining “ i ∈ S,” and at most O(log(1 /δ )/ξ 2)queries to the adjacency matrix of H.

Theorem 9. There exists a (randomized) local computation algorithm A with query access to the adjacency matrix G = ( V, E ), an accuracy parameter ε > 0, and a failure probability δ > 0.

• A draws a subset Q ⊂ V of size poly(log(1 /δ )/(αβε )) uniformly at random, and queries the induced subgraph of Q. On query i ∈ V , A queries entries (i, j ) for all j ∈ Q.

• Whenever G is (α, β )-dense for α, β ≤ εc for sufficiently large c, letting S ⊂ V be the underlying cut that A generates, 

Pr 

> A

 ϕG(S) ≤ O(pϕε(G)) + εα3n2 ≲ vol G(S) ≤ vol G(S)



≥ 1 − δ.  

> 16 We take half of the edges as a convenience, to de-correlate the degrees of B.

30 Local Computation Algorithm for Sparsest Cut. The algorithm receives query access to the adjacency matrix of a graph G = ([ n], E ), an accuracy parameter ε > 0, and an index i ∈ [n]. 1. Use public randomness to generate N ∼ G (n, 4α), and use query access to the adjacency matrix of 

G to provide query access to the adjacency matrix of H0 = G ∪ N.2. Initialize the following objects: 

• A collection A (initially empty) of at most t local computation algorithms ( A1, . . . , At) which provide query access to subsets of [ n] ( S will be the union of the first ℓ sets, for some ℓ ≤ t). 

• Using public randomness, t independent draws Q(0) 1 , . . . , Q(0)  

> t

⊂ [n] where j ∈ Q(0)  

> ℓ

i.i.d. with probability 2 γ for γ ≥ poly(1 /ε )/n 17 .3. For ℓ = 1 , . . . t , repeat the following: (a) Let Tℓ−1 ⊂ [n] denote the (implicit) set given by 18 

Tℓ−1 = {j ∈ [n] : ∃ℓ′ ≤ ℓ − 1 s.t. Aℓ′ declares j lies in the set } .

Execute Estimate-Vol (Tℓ−1, H0, ε/ 8, 1/(400 t)) and let bη be the output. If bη ≥ εn 2/4, break out of the loop to Line 4. (b) Otherwise, bη < εn 2/4 and continue. Let Vℓ = [ n] \ Tℓ−1, which is the (implicit) vertex set obtained after removing the union of sets defined by A1, . . . , Aℓ−1.(c) For every ℓ′ ≤ ℓ − 1, query the local computation algorithm Aℓ′ with j for j ∈ Q(0)  

> ℓ

in order to write down Qℓ = Q(0)  

> ℓ

∩ Vℓ. Note, Qℓ includes each j ∈ Vℓ i.i.d. w.p 2 γ.(d) Let Hℓ be the subgraph of H0 induced by Vℓ, and let Aℓ be the local computation algorithm from Theorem 9 with parameter ε′ = ε/ 2, which uses query set Qℓ ⊂ Vℓ. Query Aℓ with i

(which must lie in Vℓ), and if Aℓ declares that i lies in the set, output “ i ∈ S” and terminate. 4. If the procedure has not produced an output yet, output “ i / ∈ S.” Figure 4: Local Computation Algorithm for Sparsest Cut 

Correctness Guarantees. We first establish the correctness guarantees of Theorem 3, where we show that the local computation algorithm specified in Figure 4 provides query access to a cut S ⊂ [n] which satisfies ϕG(S) is at most O(pϕε(G)) + ε with probability at least 0 .99. By setting δ > 1400 t , we assume that all executions of Estimate-Vol , and uses of Lemma 6.2 and Theorem 9 result in conclusions which succeed; each event occurs with high enough probability, so by a union bound, all events occur with probability of at least 0 .99. In what follows, we assume this is the case. Notice that Figure 4 defines a local computation algorithm which, on query i ∈ [n] will also produce an output (either that “ i ∈ S” in some execution of Line 3a, or that “ i / ∈ S” in Line 4). Hence, we may define 

S = {i ∈ [n] : Figure 4 on input G, ε, and i ∈ [n] outputs “ i ∈ S”} ,

and notice that S naturally decomposes as a union S1 ∪ · · · ∪ Sℓ0−1, where Sℓ consists of the indices i ∈ [n]where Aℓ in Line 3d outputs “ i ∈ S” and ℓ is the smallest to do so. Note, ℓ0 represents the iteration of Line 3a which triggered the “break” condition, and we default ℓ0 = t + 1 if Line 3a is never triggered (we will show this does not happen). Note, the following inductive definitions are a consequence of the iterative algorithm in Figure 4: 31 • T0 = ∅.

• For ℓ = 1 , . . . , ℓ0 − 1, Sℓ is the (implicit) subset defined by Aℓ executed on input graph Hℓ, which is the subgraph of H0 induced by Vℓ = [ n] \ Tℓ−1.

• For ℓ = 2 , . . . , ℓ0, we have Tℓ−1 = S1 ∪ · · · ∪ Sℓ−1.Notice that, for every ℓ = 1 , . . . , ℓ0 − 1 executed in Line 3 will have Tℓ−1 with vol H0 (Tℓ−1) ≤ εn 2/2. This is because ℓ < ℓ0, Line 3a results in bη ≤ εn 2/4, and the fact that Estimate-Vol (Tℓ−1, H0, ε/ 8, δ ) is an 

εn 2/8-error approximation of vol H0 (Tℓ−1). Hence, we use the first item of Lemma 6.2 and conclude the subgraph Hℓ of H0 induced by vertex set Vℓ is ( α, β )-dense, and ϕε/ 2(Hℓ) ≤ 2ϕε(G) + O(α/ε ). Similarly to the proof of Lemma 6.2, Vℓ must have size at least ( ε/ 2) 1/2n, since vol H0 (Vℓ) ≥ εn 2 − vol H0 (Tℓ−1) ≥ εn 2/2(recall, G contains at least εn 2 edges). As a result, the set Qℓ ⊂ Vℓ, which includes each index j ∈ Vℓ

i.i.d. with probability γ′ satisfies γ′ ≥ poly(1 /ε )/n ≥ poly(1 /ε )/|Vℓ|, and we may Theorem 9. The local computation algorithm Aℓ in Line 3d will satisfy, 

ϕHℓ (Sℓ) ≤ O

q 

ϕε/ 2(Hℓ)



+ ε, and αn 2 ≤ vol Hℓ (Sℓ) ≤ vol Hℓ (Sℓ). (13) Therefore, we count the edges of EH0 (S, S) using the decomposition of S = S1 ∪ · · · ∪ Sℓ0−1 and applying the above bounds, 

EH0 (S, S) ≤

> ℓ0−1

X

> ℓ=1

EHℓ (Sℓ, Vℓ \ Sℓ) ≤

> ℓ0−1

X

> ℓ=1



O(

q

ϕε/ 2(Hℓ) + ε



· vol Hℓ (Sℓ)

≤



O

p ϕε(G)



+ O(α/ε ) + ε



· vol H0 (S),

where the first inequality uses the fact Hℓ consist of induced subgraphs which iteratively remove Sℓ, the second inequality uses the bound on ϕHℓ (Sℓ), and the third inequality uses the conclusion of Lemma 6.2 to relate to ϕε(G), and the fact volumes with respect to induced subgraphs are at most that of the entire graph. We now relate ϕH0 (S) to ϕG(S), by using the second condition of Theorem 6.2. For that, we need a lower bound on vol H0 (S). For that, notice that if ℓ0 < t (and the condition of Line 3a was triggered), then it was because bη ≥ εn 2/4, which implies vol H0 (Tℓ0−1) = vol H0 (S) is at least εn 2/8. If such was the case, we apply the second item of Lemma 6.2 as desired. In order to show that ℓ0 < t , and the condition of Line 3a eventually gets triggered, we use the fact the right-most inequality of (13), which implies that vol H0 (S) is at least ( ℓ0 − 1) · αn 2; once ℓ0 − 1 ≥ 1/α , vol H0 (S) would have constant volume and trigger Line 3a. 

Query Complexity. We now bound the query complexity of Figure 4. First, note that a single query to the adjacency matrix of H0 uses at most one query to the adjacency matrix of G. For any particular local computation algorithm Aℓ initialized in Line 3d, Figure 4 makes: 

• Determine which Q(0)  

> ℓ

⊂ [n] are part of the induced subgraph Hℓ, which requires making |Q(0)  

> ℓ

| queries to the (previous) local computation algorithms A1, . . . , Aℓ−1. This is used to determine the set Qℓ.

• Once we know Qℓ, Aℓ makes |Qℓ|2 queries during the preprocessing time, and |Qℓ| queries during any particular query.                                   

> 17 Note that Theorem 6samples asubset of afixed size, while other sub-routines such as in Theorem 9or
> Estimate-Vol (S, H, ξ, δ ) sample entries independently with fixed probability. These sampling methods are interchangeable, so it suffices to define query sets Q1, . . . Qtin terms of either sampling model.
> 18 We let T0=∅, and note that Tℓ−1is implicit, since we can query A1, . . . , Aℓ−1in order to know whether i∈Tℓ−1.

32 As a result, the total number of probes needed is: 

> t

X

> ℓ=1

|Qℓ|2 +

> t

X

> ℓ=1

X

> ℓ′<ℓ

|Q(0)  

> ℓ

| · | Qℓ′ |,

where the first term constitutes all of the preprocessing queries (done after determining Qℓ for each ℓ), plus the |Q(0)  

> ℓ

| queries which are done to Aℓ′ for ℓ′ < ℓ , each of which uses |Qℓ′ | probes to the adjacency matrix of G. The fact that all Qℓ ⊂ Q(0)  

> ℓ

and all are of size poly(1 /ε ), and the fact t is at most O(1 /α ) gives the desired upper bound. 

## 6.2 Sparsest Cut on (α, β )-dense Graphs with α, β = poly( ε): Proof of Theorem 9 

Fix an input graph G = ( V, E ) which is ( α, β )-dense and has at least εn 2 edges. The algorithm of Theorem 9 will proceed by providing query access to a deflated normalized adjacency matrix M (see Definition 6.3 below). As we discuss, we will execute our top eigenvector local computation algorithm from Theorem 1 on an input matrix which is an approximation to a deflated normalized adjacency matrix, in order to access a vector which is approximates the second-top eigenvector of the normalized adjacency. 

Definition 6.3. For a graph G = ([ n], E ), the deflated normalized adjacency matrix M is the n × n matrix whose (i, j )-entry is 

Mij = 1{(i, j ) ∈ E}

pdeg G(i) · deg G(j) −

pdeg G(i) · deg G(j)2m .

The matrix M is an “exact” deflated normalized adjacency (where we take the normalized adjacency matrix and remove the top eigenvector). Note, a local computation algorithm (whose query complexity is poly(1 /ε )) cannot provide exact query access to M , since knowing m (the number of edges), and deg G(i) for any vertex exactly would require too many queries. Hence, we provide access to an approximation of M in the following sense, where we will let ξ be a parameter indicating the sampling error which will be set to a small enough polynomial of ε:

• Estimate of Edges : The algorithm executes a sampling-based estimation, to obtain an estimate bm

of m which satisfies m ≤ bm ≤ (1 + ξ)m. We use the fact m ≥ εn 2 to obtain a multiplicative error estimate, using poly(1 /(εξ )) queries to the adjacency matrix of G.

• Degree Estimation : Similarly to above, a sampling-based estimation of the degrees d(i) for deg G(i). The fact G is ( α, β )-dense allows us to, (i) enforce αn ≤ d(i) for all i ∈ [n], and there will be at most 

βn vertices where deg G(i) ≤ αn , and (ii) except for at most ξn vertices, the estimate d(i) satisfies max {αn, deg G(i)} ≤ d(i) ≤ (1 + ξ) max {deg G(i), αn }.19 

While technically random variables, bm and d(i) are kept un-bolded, and we assume they satisfy above properties. Furthermore, note the above sampling is made non-adaptive (requiring uniform random draws from [ n]), and incurs poly(1 /(ξεα ))-additive overhead on the size of Q (as specified in Theorem 9). With that, we assume the algorithm operates by receiving query access to the entries of the matrix cM :

cMij = 1{(i, j ) ∈ E}

pd(i) · d(j) −

pd(i) · d(j)2 bm , (14) which we will refer to as a noisy deflated normalized adjacency of G.  

> 19 Importantly, we cannot hope to estimate the degrees of all vertices correctly, since that would require an “union bound” and incur O(log n)-dependencies.

33 Lemma 6.4. Let cM be a noisy deflated normalized adjacency of an (α, β )-dense graph G = ([ n], E ) (as in (14)). Then, ∥ cM ∥∞ ≤ 2/(αn ) and λmax ( cM ) ≥ 1 − 2ϕε(G) − O(( ξ + αβ )/ε ).Proof. For the first item, notice that every i ∈ [n] satisfies αn ≤ d(i) ≤ n, where the lower bound is by definition of (14), and we always have bm ≥ εn 2 (since we know G contains at least εn 2 edges). Hence, each entry is at most 1 /(αn ) + 1 /(εn ), which is at most 2 /(αn ) by our setting of α. For the lower bound on λmax ( cM ), consider a cut S ⊂ [n] which realizes the bound on ϕε(G). We let v(S) = P 

> i∈S

d(i) and 

v(S) = P 

> i∈S

d(i) (which we note would be the volumes under G if all degree estimates were perfect), and 

y ∈ Rn be the vector where yi = v(S) if i ∈ S and yi = −v(S) if i ∈ S. Consider the vector x ∈ Rn which sets xi = yi · pd(i). Then, 

∥x∥22 = X

> i∈S

v(S)2 · d(i) + X

> i∈S

v(S)2 · d(i) = v(S)v(S)  v(S) + v(S) .

In order to lower bound ⟨x, cM x ⟩, we note that the setting of x implies that the second term of cM (that which “deflates” the normalized adjacency) satisfies 1 /(2 bm) P

> i,j ∈[n]

pd(i) · d(j)·xixj = 1 /(2 bm)( P 

> i∈[n]

d(i)yi)2 = 0. Hence, we have 

⟨x, cM x ⟩ =

> n

X

> i=1
> n

X

> j=1

1 {(i, j ) ∈ E} · yiyj

= v(S)2 X

> i,j ∈S

1{(i, j ) ∈ E} + v(S)2 X

> i,j ∈S

1{(i, j ) ∈ E} − 2v(S)v(S) X

> i∈S,j ∈S

1{(i, j ) ∈ E}

= v(S)2 · vol G(S) + v(S)2 · vol G(S) − EG(S, S )( v(S) + v(S)) 2

Hence, the Rayleigh quotient ⟨x, cM x ⟩/∥x∥22 is at least: 

v(S) · vol G(S)

v(S)( v(S) + v(S)) + v(S) · vol G(S)

v(S)( v(S) + v(S)) − EG(S, S ) · (v(S) + v(S)) 

v(S)v(S) .

It remains to re-write the above in terms of the conductance ϕε(G), using the fact that v(S) and v(S) will be close enough to vol G(S) and vol G(S). Specifically, recall that all but ξn vertices have d(i) being (1 + ξ)-multiplicative approximations to max {αn, deg G(i)}, and at most βn are less than αn , and furthermore, vol G(S) ≥ vol G(S) ≥ εn 2 by assumption. By upper bounding v(S) and v(S) in terms of vol G(S) and vol G(S), respectively, both vol G(S)/v (S) and vol G(S)/v (S) are at least 1(1 + ξ)(1 + βα/ε ) + ξ/ε ≥ 1 − O(( ξ + αβ )/ε ),

which implies that our Rayleigh quotient is at least 1 − O(( ξ + αβ )/ε ) − ϕε(G) ·

 v(S) + v(S)

v(S)



·

 vol G(S)

v(S)



≥ 1 − 2ϕε(G) − O(( ξ + αβ )/ε ).

Lemma 6.4 provides the “feasibility” condition, and shows that the easy direction of Cheeger’s inequality can tolerate some error in the deflated normalized adjacency matrix. We will use the local computation algorithm for the top eigenvector (Theorem 6) with input matrix ( αn/ 2) · cM will result in entry access to 

x ∈ Rn which is an approximately top eigenvector to cM . In what follows, we will apply Theorem 6 to the input matrix ( αn/ 2) · cM and accuracy parameter ε0 (which will be a polynomial of ε which we set later). In order to simplify the notation, it will be helpful to define the parameter ζ which will be the error on the returned Rayleigh quotient of the vector x in Lemma 6.5 below 

ζ ≤ 2ϕε(G) + O(( ξ + αβ )/ε + ε0/α ). (15) 34 Lemma 6.5. Let x ∈ Rn be the output vector of Theorem 6 with input (αn/ 2) cM and accuracy ε0 ∈ (0 , 1) .With high probability over randomness of Theorem 6, 

⟨x, cM x⟩∥x∥22

≥ 1 − ζ ∥x∥∞ ≲ 1/(α√n) 12 bm

> n

X

> i=1

pd(i) · xi

!2

≤ (ζ + O(ξ/α 3)) · ∥ x∥22.

Proof. From Theorem 6, the Rayleigh quotient of x with ( αn/ 2) cM will be at least λmax (( αn/ 2) · cM ) − ε0n

(with high probability), which is at least ( αn/ 2)(1 − ζ) by Lemma 6.4. Dividing by ( αn/ 2) gives the desired bound on the Rayleigh quotient of cM . The upper bound on ∥x∥∞ follows from the description of the algorithm in Theorem 6 (namely, Figure 1), where x = AT⊺y/bλmax for A = ( αn/ 2) cM . Note, |xi| is by Cauchy-Schwarz at most 1 /bλmax · (√t/√q) · ∥ y∥2, which is at most O(1 /(α√n)) with high probability (since 

t = O(nq ) and ϕε(G) is at most a small enough constant). Finally, the Rayleigh quotient bound implies (1 − ζ)∥x∥22 ≤ X

> i,j ∈[n]

1{(i, j ) ∈ E}

pd(i)d(j) · xixj − 12 bm

> n

X

> i=1

pd(i)xi

!2

,

and because deg G(i) ≤ d(i) and deg G(j) ≤ d(j) for all but ξn vertices (where d(i) ≥ αn ), the right-most quantity above is at most 

−(1 − ζ)∥x∥22 + X

> i,j ∈[n]

1{(i, j ) ∈ E}

pdeg G(i) deg G(j) · | xi| · | xj | + 2ξn α · ∥ x∥2 

> ∞

≤ ζ∥x∥22 + O(ξ/α 3)since the normalized adjacency matrix has all eigenvalues of magnitude at most 1, and the bound on ∥x∥∞.The final substitution follows from the fact ∥x∥22 will be at least a constant (from Lemma 4.2). In the next lemma, we show that the any vector satisfying the conclusions of Lemma 6.5 satisfies the necessary conditions which we need to execute the “rounding algorithm” (from a vector to a cut) in the hard direction of Cheeger’s inequality. In the expressions below, the expectations over ( i, j) represent draws uniformly at random from the set of edges E of the graph, and draws i from the vertex set consist of the marginal distribution on vertices (note, this is not uniform over [ n], but rather proportional to deg G(i)). 

Lemma 6.6. Any vector x ∈ Rn which satisfies the conclusions of Lemma 6.5 satisfies, as long as ξ is smaller than poly( αε ), α, β smaller than poly( ε), and ζ smaller than a fixed constant, 

E

> (i,j)

 xi

pd(i) − xj

pd(j)

!2 ≤ 2∥x∥22

m · ζ and E

> i

 xi

pd(i) − E

> j

"

xj

pd(j)

#! 2 ≥ ∥x∥22

4m

Proof. Consider setting γ1 to ζ + O(ξ/α 3) to simplify the final condition of Lemma 6.5 and γ2 to 1 /α 2 so 

n∥x∥2 

> ∞

≤ γ2∥x∥22 (recall, ∥x∥22 will Θ(1)). For the first inequality, we use the fact that d(i) ≥ deg G(i) for all but at most ξn vertices, and that d(i) ≥ αn . In particular, expanding the square we obtain 

E

> (i,j)

 xi

pd(i) − xj

pd(j)

!2 ≤ 2

m

> n

X

> i=1

deg G(i)

d(i) · x2 

> i

− 2

m ⟨x, cM x ⟩ ≤ 2

m



∥x∥22 − ⟨ x, cM x ⟩ + ( ξ/α )n∥x∥2

> ∞



,

which is at most the desired bound. For the second inequality, we first note that letting μ = Ei[xi/pd(i)], any τ ∈ R satisfies 

E

> i

 xi

pd(i) − τ

!2 ≥ E

> i

 x2

> i

d(i)



− μ2 = 12m

> n

X

> i=1

deg G(i)

d(i) · x2 

> i

− μ2 ≥ ∥x∥22

2m

 1 − (β + ξ)γ2

1 + ξ



− μ2. (16) 35 Finally, we upper bound the magnitude of μ via the triangle inequality and Cauchy-Schwarz 

|μ| ≤ 12m

> n

X

> i=1

deg G(i) − d(i)

pd(i) · xi + 12m

> n

X

> i=1

pd(i) · xi

≤ ∥x∥2

2m

> n

X

> i=1

(deg G(i) − d(i)) 2

d(i)

!1/2

+

pγ1 bm · ∥ x∥2

2m

where the final summation inside the square-root (from the fact d(i) is oftentimes an approximation of deg G(i)) is at most ( ξ2 + 4 ξ/α + 4 αβ )n2. In particular, this implies that (16) is at least 

∥x∥22

2m

 1 − (β + ξ)γ2

1 + ξ − 2( ξ2 + 4 ξ/α + 4 αβ )

ε − 2γ1(1 + ξ)



,

where in the final line, we use the fact that m ≥ εn 2 and that bm ≤ (1 + ξ)m, and this implies the desired inequality. Finally, we will use the proof of the hard direction of Cheeger’s inequality, which gives a rounding algorithm from a vector to a cut. 

Theorem 10 (Hard Direction of Cheeger’s Inequality) . Fix any graph G = ([ n], E ) as well as a vector 

y ∈ Rn satisfying 

E

> (i,j)

(yi − yj )2 ≤ a and E

> i

"

yi − E 

> i

[yi]

2#

≥ b. 

There exists a cut S ⊂ [n] of the form S = {i ∈ [n] : σy i ≥ τ } for τ ∈ R and σ ∈ {− 1, 1} which satisfies 

vol G(S) ≥ vol G(S) ≥ Ω( mb/ ∥y∥2

> ∞

) and ϕG(S) ≲ pa/b. 

Remark 11 (Volume of S). Standard proofs of Theorem 10 may not explicitly compute a bound on vol G(S),but such bounds follows directly from standard rounding schemes. The vector y will be rounded by centering the vector around the median τ ∈ R and then sampling a threshold γ uniformly from 0 to max i(yi − τ )2. The setting of τ ensures that half the elements are positive and half negative. Therefore S contains fewer vertices and has smaller volume than S. The expectation can be computed explicitly, which is simply the degrees times the probability that half the elements (yi − τ )2 are larger than γ.

E[min(vol G(S), vol G(S))] = E[vol G(S)] = 12

X

> i

di Pr [i ∈ S] = 

P 

> i

di(yi − τ )2

2 max i(yi − τ )2

The final bound follows from the fact that (yi − τ )2 ≤ ∥ y∥2 

> ∞

and the observation that the initial condition of Lemma 7.7, Ei

h

(yi − Ei[yi]) 2i

can be no greater than P 

> i

di(yi − τ )2/m , as Ei[yi] is the minimizing setting of τ .

Putting Everything Together: Proof of Theorem 9. First, note that, as discussed prior to (14), we assume the local computation algorithm receives query access to the entries of an n × n matrix cM in (14), by using an additive poly(1 /(ξε ))-additive overhead on the size of the query set Q; in particular, for any (i, j ) ∈ [n] × [n], we may estimate deg G(i) and deg G(j) via determining the number of edges to Q, and obtain 

d(i) and d(j), as well as the number of edges in Q × Q in order to let bm be the estimate of m.From Lemma 6.4, the matrix ( αn/ 2) cM is a bounded-entry matrix, whose top eigenvalue is is at least (αn/ 2)(1 − 2ϕε(G) − O(( ξ + αβ )/ε )), and as a result of Theorem 6 with accuracy ε0, there is a (randomized) local computation algorithm A which receives query access to the entries of a vector x ∈ Rn. With high 36 probability over the randomness of A, the output vector x satisfies the three conditions of Lemma 6.5. We consider the vector y ∈ Rn where each yi is given by rounding xi/pd(i) to the nearest multiple of poly( ε)/n .One can see that the vector y satisfies the conditions of Theorem 10 with a = ζ/m and b = 1 /(4 m), and in addition, each coordinate is one of at most poly(1 /ε ) many values. In other words, there exists a collection of at most poly(1 /ε ) many cuts obtained by thresholding y such that at least one of them satisfies ϕG(S) ≲ √ζ

and vol G(S) ≥ vol G(S) is at least Ω(1 /∥y∥2

> ∞

), which is at least Ω( α3n2). This guarantees the existence of a cut S ⊂ [n] among a family of at most poly(1 /ε ) cuts; using poly(log(1 /ε )/α ) additional queries, we may iterate through all possible cuts S, and estimate vol G(S) and EG(S, S ) to find the desired cut. After setting the parameters ε0 ≤ poly( εα ), ξ ≤ poly( αβ ) and by the assumption of Theorem 9 that α, β ≤ poly( ε), we conclude that the set S returned satisfies ϕG(S) ≲ pϕε(G) + ε.

# 7 Local Computation Algorithm for Max-Cut 

Similar to the local computation algorithm for sparsest cut, this section outlines the application of Theorem 1 to provide local access to an approximate maximum cut of a dense and nearly bipartite graph. In particular, we prove the following theorem. 

Theorem 4. There exists a (randomized) local computation algorithm A with query access to the adjacency matrix of a graph G = ([ n], E ) and accuracy parameter ε > 0.

• For any i ∈ [n], A makes poly(1 /ε ) queries and outputs access “ i ∈ S” for an underlying cut S ⊂ [n].

• If the max-cut of G cuts (1 − φ)|E| edges, and S ⊂ [n] is the underlying cut that A generates, 

Pr 

> A

E(S, S) ≥ (1 − √φ)|E| − εn 2 ≥ 0.99 .

Furthermore, A always runs in poly(1 /ε ) time and uses poly(1 /ε ) space. 

The algorithm is structurally quite similar to the LCA for the sparsest cut problem, in particular it reduces the problem of computing the maximum cut in approximately bipartite graphs to repeatedly computing close to bipartite cuts in a certain class of dense graphs. We define the following measure of distance to bipartiteness (also defined in [Tre12]) which measures for a given partition L, R of a subset S of V , the fraction of edges incident to S with one endpoint in L and the other not in R (and vice-versa). 

Definition 7.1 (Bipartiteness Ratio of a Graph) . For a fixed graph G = ([ n], E ) and a subset S ⊂ [n] split into two disjoint partitions (L, R ) such that S = L ∪ R, we let 

• The bipartiteness ratio of the partition is BG(L, R ) = (2 E(L, L ) + 2 E(R, R ) + E(S, S )) /vol G(L ∪ R).

• The bipartiteness ratio of the entire graph G is defined by 

B(G) = min  

> S⊆V

BG(S, S ).

## 7.1 Reduction to (α, β )-Dense Graphs, for α, β = poly( ε): Proof of Theorem 4 

As in the proof of Theorem 3, we show that it suffices to construct a local computation algorithm for ( α, β )-dense graphs (see Definition 6.1). For this section, consider a fixed input graph G = ([ n], E ) which we provide query access to, as well as a fixed accuracy parameter ε > 0 (notice, we may assume that G contains a max-cut of at least εn 2 edges, as otherwise, the εn 2 error allows any cut S to satisfy the approximation ratio of the Theorem). 37 Lemma 7.2. For α, β ∈ (εc, ε/c ) for some constant c, let N ∼ G (n, 4α) be an Erd¨ os-Reyni graph and let 

H = G ∪ N. The following occurs with high probability over N.

• For any T ⊂ [n] which satisfies vol H(T ) ≥ εn 2/4. Define the parameter ρ(T ) = vol H(T )/vol H(H).

• Let H′ be the subgraph of H induced by T . Then, H′ is (α, β )-dense and satisfies B(H′) ≤ B (G)/ρ (T ) + 

O(α/ (ερ (T ))) .Proof. The proof of H′ being ( α, β )-dense follows from the proof of Lemma 6.2. We simply need to upper bound B(H′). As before, consider the subset S ⊂ [n] which realizes the bound on B(G), and let S′ = S ∩ T

and S′ = S ∩ T . We upper bound BH′ (S′, S ′) in terms of BG(S, S ). We have that vol H′ (S′) + vol H′ (S′) ≥

ρ(T ) · vol H(H)¯ lower bounding the denominator of BH′ (S′, S ′). Then, we have the with high probability over the draw of N (since N has at most 8 αn 2 edges), that EH′ (S′, S ′) ≤ EG(S, S ) + 8 αn 2 and EH′ (S′, S ′) ≤

EG(S, S ) + 8 αn 2. Since vol( G) ≥ εn 2, this implies B(H′) ≤ B (G)/ρ + 16 α/ (ερ ). Similarly to the sparsest cut LCA, we will assume there exists a local computation algorithm A which finds a cut with nearly optimal bipartiteness ratio assuming the underlying graph is ( α, β )-dense (Theorem 12). Using Lemma 7.2, we analyze the algorithm in Figure 5 to prove Theorem 4, which proceeds by iteratively cutting out approximately bipartite cuts and repeating on the induced subgraph of remaining vertices. The main difference from the sparsest cut LCA is that the volume of the final cut needs to be close to 2 |E|,ensuring it cuts at least (1 − ε)|E| many edges. Any remaining edges have their vertices assigned randomly. In order to determine when sufficently many edges have been cut, we will assume access to two subroutines, 

Estimate-Vol (S, H, ξ, δ ) defined in Theorem 3, which receives query access of the form “ i ∈ S”, as well as query access to the adjacency matrix of a graph H on vertex set [ n] and outputs an estimate bη of vol H (S)which is off by at most ξn 2 with probability 1 − δ.

Theorem 12. There exists a (randomized) local computation algorithm A with query access to the adjacency matrix G = ( V, E ), an accuracy parameter ε > 0, and a failure probability δ > 0.

• For any γ ≥ poly(log(1 /δ )/ε )/|V |, A draws a subset Q ⊂ V where j ∈ Q i.i.d. with probability γ, and queries the induced subgraph of Q. On query i ∈ V , A queries entries (i, j ) for all j ∈ Q.

• Whenever G is (α, β )-dense, letting L, R ⊂ [n] be the cut that A generates, 

Pr 

> A

 BG(L, R) ≤ O(pB(G)) + εα3n2 ≤ vol G(L ∪ R)



≥ 1 − δ. 

Correctness Guarantees. We first establish the correctness guarantees of Theorem 4, where we show that the local computation algorithm specified in Figure 5 provides query access to a cut S ⊂ [n] which satisfies 

E(S, S) ≥ (1 − √φ)|E| − ε2n with probability at least 0 .99. Observe that by the assumptions of Theorem 4, we know that G contains a cut of volume 2 |E|, which has bipartiteness ratio at most φ. Therefore, our goal will be to output a cut of volume at least (1 − ε)|E| with bipartiteness ratio at most √φ + ε.By setting δ > 1400 t , we assume that all executions of Estimate-Vol , and uses of Lemma 7.2 and Theorem 12 result in conclusions which succeed; each event occurs with high enough probability, so by a union bound, all events occur with probability at least 0 .99. In the remainder of the proof, we assume this is the case. Notice that Figure 5 defines a local computation algorithm which, on query i ∈ [n] will also produce an output                        

> 20 Note that Theorem 6samples asubset of afixed size, while other sub-routines such as in Theorem 12 or
> Estimate-Vol (S, H, ξ, δ ) sample entries independently with fixed probability. These sampling methods are interchangeable, so it suffices to define query sets Q1, . . . Qtin terms of either sampling model.

38 Local Computation Algorithm for Max Cut. The algorithm receives query access to the adjacency matrix of a graph G = ([ n], E ), an accuracy parameter ε > 0, and an index i ∈ [n]. 1. Use public randomness to generate N ∼ G (n, 4α), and use query access to the adjacency matrix of 

G to provide query access to the adjacency matrix of H0 = G ∪ N.2. Initialize the following objects: 

• A collection A (initially empty) of at most t local computation algorithms ( A1, . . . , At) which provide query access to subsets of [ n] ( L and R, which are the union of the first ℓ assignments, for some ℓ ≤ t). 

• Using public randomness, t independent draws Q(0) 1 , . . . , Q(0)  

> t

⊂ [n] where j ∈ Q(0)  

> ℓ

i.i.d. with probability 2 γ for γ ≥ poly(1 /ε )/n 20 .

• Execute Estimate-Vol ([ n], H0, ε/ 8, 1/400) and let bv be the output. 3. For ℓ = 1 , . . . t , repeat the following: (a) Let Tℓ−1 ⊂ [n] denote the set given by 

Tℓ−1 = {j ∈ [n] : ∃ℓ′ ≤ ℓ − 1 s.t. Aℓ′ declares j lies in Lℓ′ or Rℓ′ }

Execute Estimate-Vol (Tℓ−1, H0, ε/ 8, 1/(400 t)) and let bη be the output. If bη ≥ bv − εn 2/2, break out of the loop to Line 4. (b) Otherwise, bη < bv − εn 2/2 and continue. Let Vℓ = [ n] \ Tℓ−1, which is the (implicit) vertex set obtained after removing the union of sets defined by A1, . . . , Aℓ−1.(c) For every ℓ′ ≤ ℓ − 1, query the local computation algorithm Aℓ′ with j for j ∈ Q(0)  

> ℓ

in order to write down Qℓ = Q(0)  

> ℓ

∩ Vℓ. Note, Qℓ includes each j ∈ Vℓ i.i.d. w.p 2 γ.(d) Let Hℓ be the subgraph of H0 induced by Vℓ, and let Aℓ be the local computation algorithm from Theorem 12 with parameter ε′ = ε/ 2, which uses query set Qℓ ⊂ Vℓ. Query Aℓ with 

i (which must lie in Vℓ), and if Aℓ declares that i lies in the partial cut, output “ i ∈ L” or “i ∈ R” and terminate. 4. If the procedure has not produced an output yet, using public randomness output “ i ∈ L” with probability 1 /2 and otherwise “ i ∈ R”. Figure 5: Local Computation Algorithm for Max Cut (either that “ i ∈ L” or “ i ∈ R” in some execution of Line 3d, or that “ i ∈ L” or “ i ∈ R” in Line 4) 21 . Hence, we may define 

L = {i ∈ [n] : Figure 5 on input G, ε, and i ∈ [n] outputs “ i ∈ L”} ,

and R analogously, notice that L naturally decomposes as a union L1 ∪ · · · ∪ Lℓ0−1 ∪ Lℓ0 , where for ℓ ∈{1, . . . , ℓ0 − 1}, Lℓ consists of the indices i ∈ [n] where Aℓ in Line 3d outputs “ i ∈ L” and ℓ is the smallest to do so, and Lℓ0 consists of all vertices which reach Line 4 and declare “ i ∈ L”. The analogous decomposition also defines R as R1 ∪ · · · ∪ Rℓ0−1 ∪ Rℓ0 . Note, ℓ0 represents the iteration of Line 3a which triggered the “break” condition, and we default ℓ0 = t + 1 if Line 3a is never triggered (we will show this does not happen). Note, the following inductive definitions are a consequence of the iterative algorithm in Figure 5:          

> 21 Either Lor Rcan be the cut Sspecified in Theorem 4, as all vertices are contained in L∪R

39 • T0 = ∅.

• For ℓ = 1 , . . . , ℓ0 − 1, Lℓ, Rℓ are the (implicit) subsets defined by Aℓ executed on input graph Hℓ, which is the subgraph of H0 induced by Vℓ = [ n] \ Tℓ−1.

• Let Lℓ0 , Rℓ0 be the subsets of Hℓ0 induced by Vℓ0 = [ n] \ Tℓ0−1 which are output by Line 4. 

• For ℓ = 2 , . . . , ℓ0, we have Tℓ−1 = L1 ∪ R1 ∪ · · · ∪ Lℓ−1 ∪ Rℓ−1.Notice that, for every ℓ = 1 , . . . , ℓ0 − 1 executed in Line 3 will have Tℓ−1 with vol H0 (Tℓ−1) ≤ 2|E| − εn 2/4. This is because bv ≥ 2|E| − εn 2/8 and for ℓ < ℓ0, Line 3a results in bη ≤ bv − εn 2/2, and the fact that 

Estimate-Vol (Tℓ−1, H0, ε/ 8, δ ) is an εn 2/8-error approximation of vol H0 (Tℓ−1). Hence, define ρℓ such that 

ρℓ

> def

= vol H0 (Vℓ)/vol H0 (H0),

and notice that ρℓ ≥ εn 2/(4vol H0 (H0)) for any ℓ < ℓ0, so we can use Lemma 7.2 and conclude the subgraph 

Hℓ of H0 induced by vertex set Vℓ is ( α, β )-dense, and B(Hℓ) ≤ B (G)/ρ ℓ + O(α/ (ερ ℓ)). We also have that ρℓ0 is at most 3 εn 2/(4vol H0 (H0)). We also have that Vℓ must have size at least ( ε/ 4) 1/2n, since vol H0 (Vℓ) ≥ vol( H0) − vol H0 (Tℓ−1) ≥ εn 2/4. As a result, the set Qℓ ⊂ Vℓ, which includes each index 

j ∈ Vℓ i.i.d. with probability γ′ satisfies γ′ ≥ poly(1 /ε )/n ≥ poly(1 /ε )/|Vℓ|, and we may apply Theorem 12. The local computation algorithm Aℓ in Line 3d will satisfy, 

BHℓ (Lℓ, Rℓ) ≤ O

p B(Hℓ)



+ ε, and α3|Vℓ|2 ≤ vol Hℓ (Lℓ ∪ Rℓ). (17) We aim to bound the total number of edges not cut by L and R. For a graph G0 on a vertex set V and disjoint subsets A, B ⊂ V which are not necessarily a partition of V , let UG0 (A, B ) be sum of edges in G0

incident on A ∪ B, which are not in A × B. We can upper bound UG0 (A, B ) by BG0 (A, B ) · vol G0 (A ∪ B). Note, for every edge ( u, v ) ∈ H0 which does not cross L, R, must either be fully contained in L or fully contained in R (since L ∪ R = [ n]). Suppose u, v ∈ L, then assume without loss of generality that u ∈ Lℓ

and that v ∈ Lℓ′ for ℓ′ ≥ ℓ, so ( u, v ) is counted in UHℓ (Lℓ, Rℓ). The similar argument holds for R, and it allows us to upper bound UH0 (L, R) by looking at each of the individual subgraphs found: 

UH0 (L, R) ≤

> ℓ0

X

> ℓ=1

UHℓ (Lℓ, Rℓ) ≤

> ℓ0−1

X

> ℓ=1



O(pB(Hℓ) + ε



· vol Hℓ (Lℓ ∪ Rℓ) + vol Hℓ0 (Hℓ0 )

≤

> ℓ0−1

X

> ℓ=1



O(pB(G)/ρ ℓ + α/ (ερ ℓ)) + ε



· vol Hℓ (Lℓ ∪ Rℓ) + ρℓ0 vol H0 (H0)where the second inequality uses the bound on BHℓ (Lℓ, Rℓ) and the third inequality uses Lemma 7.2. We can factor out the terms involving ε, to get ε Pℓ0−1 

> ℓ=1

vol Hℓ (Lℓ ∪ Rℓ) ≤ ε · vol H0 (H0). We now repeat the proof of Theorem 4 in [Tre12] to bound the entire sum. Consider the ℓ-th cut Lℓ, Rℓ for any ℓ < ℓ0, by definition vol Hℓ (Lℓ ∪ Rℓ) = ( ρℓ − ρℓ+1 )vol H0 (H0). Recall that ρℓ0 · vol H0 (H0) ≤ O(εn 2) as a consequence of Line 3a. In order to combine the terms together smoothly, we can bound the sum over ℓ = 1 to ℓ0 − 1 of ( ρℓ − ρℓ+1 ) 1√ρℓ

by the integral R ρℓ 

> ρℓ+1

1/√x dx . We can thus apply the following upper bound (ρℓ − ρℓ+1 )vol H0 (H0) · O(pB(G)/ρ ℓ + α/ (ερ )) ≤ O(vol H0 (H0)) ·

Z ρℓ

> ρℓ+1

(

r B(G)

x + αεx )dx 

Combining the integrals together and integrating yields a final bound of 

UH0 (L, R) ≤ O(vol H0 (H0)) ·

Z 10

r B(G)

x + αεx 

!

dx + O(εn 2) ≤ O(

r

B(G) + αε ) · vol H0 (H0) + O(εn 2).

40 We now have that UH0 (L, R) ≥ UG(L, R), as removing the edges of N can only reduce the number of uncut edges. After setting α, β ≤ poly( ε), we can conclude that if the optimal max-cut of G cuts (1 − φ)|E| edges, then B(G) = φ, and the cut L, R returned by Theorem 4 cuts at least  1 − O(√φ) |E| − εn 2 edges. In order to show that ℓ0 < t , and the condition of Line 3a eventually gets triggered, we use the fact the right-most inequality of (17), which implies that vol H0 (L ∪ R) is at least ( ℓ0 − 1) · α3|Vℓ|2. Recall we have that 

|Vℓ| ≥ pε/ 4 n, so once ℓ0 − 1 ≥ 4/(εα 3), vol H0 (L ∪ R) would be large enough to trigger Line 3a. 

Query Complexity. We turn to the query complexity of Figure 5. The proof is analogous to that of Theorem 3. A single query to the adjacency matrix of H0 uses at most one query to the adjacency matrix of G. For any particular local computation algorithm Aℓ initialized in Line 3d, Figure 5 does the following: 

• Determine which Q(0)  

> ℓ

⊂ [n] are part of the induced subgraph Hℓ, which requires making |Q(0)  

> ℓ

| queries to the (previous) local computation algorithms A1, . . . , Aℓ−1. This is used to determine the set Qℓ.

• Once we know Qℓ, Aℓ makes |Qℓ|2 queries during the preprocessing time, and |Qℓ| queries during any particular query. As a result, the total number of probes needed is: 

> t

X

> ℓ=1

|Qℓ|2 +

> t

X

> ℓ=1

X

> ℓ′<ℓ

|Q(0)  

> ℓ

| · | Qℓ′ |,

where the first term constitutes all of the preprocessing queries (done after determining Qℓ for each ℓ), plus the |Q(0)  

> ℓ

| queries which are done to Aℓ′ for ℓ′ < ℓ , each of which uses |Qℓ′ | probes to the adjacency matrix of 

G. The fact that all Qℓ ⊂ Q(0)  

> ℓ

and all are of size poly(1 /ε ), and the fact t is at most O( 1 

> εα 3

) with α = poly( ε)gives the desired upper bound. 

## 7.2 Max Cut on (α, β )-dense Graphs with α, β = poly( ε): Proof of Theorem 12 

Fix an input graph G = ( V, E ) which is ( α, β )-dense and has at least εn 2 edges. As in the algorithm of Theorem 9, the algorithm of Theorem 12 will start by providing query access to the negative normalized adjacency matrix N (defined in Definition 7.3 below) on which we will execute our top eigenvector local computation algorithm from Theorem 1, in order to access a approximation of the top vector. 

Definition 7.3. For a graph G = ([ n], E ), the negative normalized adjacency matrix N is the n × n matrix whose (i, j )-entry is 

Nij = −1{(i, j ) ∈ E}

pdeg G(i) · deg G(j) .

As with definition 6.3 in Theorem 9, we cannot provide exact access to the matrix N , and instead we provide access to an approximation of N using the same degree estimates as in (14). The sampling to estimate deg( u)is non-adaptive (requiring uniform random draws from [ n]), and incurs poly(1 /(ξε ))-additive overhead on the size of Q (as specified in Theorem 12). Hence the algorithm receives query access to the matrix bN :

bNij = −1{(i, j ) ∈ E}

pd(i) · d(j) , (18) which we will refer to as a noisy negative normalized adjacency of G.

Lemma 7.4 (Feasibility) . Let bN be a noisy negative normalized adjacency matrix of an (α, β )-dense graph 

G = ([ n], E ). Then, ∥ bN ∥∞ ≤ 1/(αn ) and λmax ( bN ) ≥ 1 − 2B(G) − O(( ξ + αβ )/ε )41 Proof. The proof is quite similar to that of Lemma 6.4. We have that by definition (18) for all i ∈ [n] satisfies 

αn ≤ d(i) ≤ n. Hence, each entry is at most 1 /(αn ). For the lower bound on λmax ( bN ), consider the partition 

L, R ⊂ [n] which realizes the bound on B(G). Recall L ∪ R = [ n], so all edges are explicitly cut. Consider the vector x ∈ Rn which sets xi = pd(i) if i ∈ L and xi = −pd(i) if i ∈ R. To bound the denominator, recall that ξn vertices have d(i) within a (1 + ξ)-multiplicative approximations of max {αn, deg G(i)}, and at most βn are less than αn . Since |E| ≥ εn 2, the sum of d(i) is within a (1 + ξ)(1 + βα/ε ) + ξ/ε of the sum of the true degrees, we have 

∥x∥22 ≤

> n

X

> i=1

d(i) ≤ ((1 + ξ)(1 + βα/ε ) + ξ/ε ) vol( G)In order to lower bound ⟨x, bN x ⟩, we can rewrite the sum over edges crossing the cut as the volume minus the sum of internal edges. 

⟨x, bN x ⟩ = 2 X

> i∈L

X

> j∈R

1 {(i, j ) ∈ E} − 2 X

> i∈L

X

> j∈L

1 {(i, j ) ∈ E} − 2 X

> i∈R

X

> j∈R

1 {(i, j ) ∈ E}

= vol( G) − 4 ( E(L, L ) + E(R, R )) Observe that since all vertices are partitioned into either L or R, the bipartiteness ratio is exactly the sum of internal edges. Hence, the Rayleigh quotient ⟨x, bN x ⟩/∥x∥22 is at least: vol( G) − 4 ( E(L, L ) + E(R, R )) ((1 + ξ)(1 + βα/ε ) + ξ/ε ) vol( G) ≥ 1 − 2B(G)(1 + ξ)(1 + βα/ε ) + ξ/ε ) ≥ 1 − 2B(G) − O(( ξ + αβ )/ε ).

Lemma 7.4 shows that the noisy version of the negative adjacency matrix still preserves the relation between the bipartiteness of the graph and the top eigenvalue. Hence we can apply Theorem 6 with accuracy parameter 

ε0 ∈ (0 , 1) on the matrix αn · bN to get local access to an approximate top eigenvector x ∈ Rn. As in the proof of Theorem 9, we will define an error parameter ζ which captures the error in the Rayleigh quotient of the vector x with bN .

ζ ≤ 2B(G) + O(( ξ + αβ )/ε + ε0/α ) (19) 

Lemma 7.5. Let x ∈ Rn be the output of Theorem 6 on the matrix αn · bN with accuracy parameter ε0 ∈ (0 , 1) .Then with high probability over the randomness of Theorem 6, 

⟨x, bN x⟩∥x∥22

≥ 1 − ζ and ∥x∥∞ ≲ 1/(α√n)

Proof. By Theorem 6, the Rayleigh quotient of x on ( αn ) · bN is at least λmax (( αn ) bN ) − ε0n, which is a least αn · (1 − ζ) by 7.4. Dividing by αn gives the desired bound. Now note that ∥x∥∞ can be bounded by rewriting the output of the algorithm in Theorem 6 (namely, Figure 1), as x = AT⊺y/bλmax for A = ( αn ) bN .Therefore by Cauchy-Schwarz |xi| is at most 1 /bλmax · (√t/√q) · ∥ y∥2, which is at most O(1 /(α√n)) with high probability. 

Lemma 7.6. Any vector x ∈ Rn which satisfies conditions of Lemma 7.5 with ξ smaller than poly( αε ), α, β 

smaller than poly( ε) and ζ smaller than some fixed constant, also satisfies 

E

> (i,j)

 xi

pd(i) + xj

pd(j)

!2 ≤ 2ζm · ∥ x∥22 and E

> (i)

 xi

pd(i)

!2 ≥ ∥x∥22

2m .

42 where expectation over (i, j) is over uniform draws from the set of edges E, and expectation over i to be sampled proportionally to deg G(i).Proof. The proof proceeds analogously to the proof of Lemma 6.6. For the first inequality, we use the fact that d(i) ≥ deg G(i) for all but at most ξn vertices, and that d(i) ≥ αn . In particular, expanding the square we obtain 

E

> (i,j)

 xi

pd(i) + xj

pd(j)

!2 ≤ 2

m

> n

X

> i=1

deg G(i)

d(i) · x2 

> i

− 2

m ⟨x, bN x ⟩ ≤ 2

m



∥x∥22 − ⟨ x, bN x ⟩ + ( ξ/α )n∥x∥2

> ∞



,

which is at most the desired bound. The second inequality follows from 

E

> i

 xi

pd(i)

!2 ≥ E

> i

 x2

> i

d(i)



= 12m

> n

X

> i=1

deg G(i)

d(i) · x2 

> i

≥ ∥x∥22

2m

 1 − (β + ξ)γ2

1 + ξ



. (20) 

Lemma 7.7 (Lemma 3 in [Tre12]) . Fix any graph G = ([ n], E ) as well as a vector y ∈ Rn satisfying 

E

> (i,j)

(yi + yj )2 ≤ a and E

> i

y2

> i

 ≥ b. 

There exists disjoint subsets L, R ⊂ [n] of the form L = {i ∈ [n] : yi ≤ − τ } and R = {i ∈ [n] : yi ≥ τ } for 

τ ∈ R which satisfies 

BG(L, R ) ≤ 4pa/b and vol G(L ∪ R) ≥ Ω( mb/ ∥y∥2

> ∞

).

Remark 13 (Volume of L ∪ R). This remark outlines the bound on the volume of L and R in Lemma 7.7, which is not explicitly computed in Lemma 3 of [Tre12], but follows easily from the rounding scheme. The vector y is rounded by sampling a threshold τ uniformly from 0 to ∥y∥2

> ∞

, then setting L = {i ∈ [n] : yi ≥ √τ }

and R = {i ∈ [n] : yi ≤ − √τ }. The expected volume of L ∪ R can be computed explicitly, which is simply sum over i ∈ [n] of the deg( i) times the probability that y2 

> i

is larger than τ .

E[vol G(L ∪ R)] = X

> i

di Pr [i ∈ L ∪ R] = 1

∥y∥2

> ∞

X

> i

di(yi)2

The final bound follows from the observation that the expectation Ei

y2

> i

 evaluates to P 

> i

di(yi)2/m .

Proof of Theorem 12. As in the proof of Theorem 9 and as discussed in (18), we assume that the algorithm receives query access to the n × n matrix bN defined in (18), which can be done using an additive poly(1 /(ξε )) queries to the size of the query set Q; for any ( i, j ) ∈ [n] × [n] we may estimate deg G(i) and deg G(j) by checking the edges in Q to obtain d(i) and d(j). From lemma 7.4, the matrix αn bN is bounded entry and has a top eigenvalue of at least αn · (1 − 2B(G) − O(( ξ + αβ )/ε )), and as a result of running (and boosting) the algorithm of Theorem 6, we receive a local computation algorithm A which with probability 1 − δ provides access to a vector x ∈ Rn which satisfies the conditions of lemma 7.5. Now we can round the vector x/pd(i)into a vector y ∈ Rn where we round y to the nearest multiple of poly( ε)/n . A simple calculation confirms that y satisfies the conditions of Lemma 7.7 with a = 2 ζ/m and b = 1 /(2 m), and each coordinate lies within a discrete set of poly(1 /ε ) possible values. Hence there are at most poly(1 /ε ) many possible thresholds τ and by Lemma 7.7 at least one of them satisfies BG(L, R ) ≤ pB(G) and vol G(L∪R) ≥ Ω(1 /∥y∥2

> ∞

). Therefore we may use poly(1 /ε ) additional queries to iterate through all possible cuts and estimate vol G(L ∪ R) as well as 2EG(L, L ), 2 EG(R, R ), and EG(L ∪ R, L ∪ R) in order to find the desired cut. After setting the parameters 

ε0 ≤ poly( εα ), ξ ≤ poly( αβ ) and by the assumption of Theorem 9 that α, β = poly( ε), we conclude that the partial cut L, R returned satisfies BG(L, R ) ≲ pBε(G) + ε.43 Acknowledgements 

The authors would like to thank Aaron Sidford for suggesting we explore local computation algorithms for top eigenvectors. 

# A Spectral Norm Decay 

We give a proof of the main theorem of [RV07], as well as the subsequent application in [Tro12]. These bounds were used in [BDD +24, SW25], but were quoted with a log n-multiplicative factor, which introduced a dependence on n into the query-complexity of the algorithm—[SW25] then used a clever recursive application of the algorithm to remove this O(log n)-factor. In this appendix, we show that the results of [RV07, Tro12] may be used directly. The reason is that, upon closer inspection, the expected spectral norm incurs a factor of log( nδ ), where δ is the probability that any row or column is included. In the application, δ is set to 1/(n · poly( ε)), and therefore the logarithmic factor overhead becomes log( nδ ) = Θ(log(1 /ε )). We include a proof which loses a O(log(1 /ε ))-factor, which will suffice for our purposes, with the benefit that it is simpler. 

Theorem 14 (Spectral Norm Decay [RV07]) . Let A be a matrix with n columns, and let Q be a random 

n × n matrix with i.i.d. diagonal entries δi ∼ Ber( δ). Then, 

E 

> Q

[∥AQ∥2] ≲



δ∥AA ⊺∥2 + E

> Q

log |Q| · ∥ AQ∥21→2

1/2

,

where ∥AQ∥1→2 is the maximum column of AQ.

The proof begins by expressing the matrix A as a sum of outer-products over its columns. This will make it easy to evaluate what the sampling experiment is, and how to evaluate the expected spectral norm. In particular, let x1, . . . , x n be the columns of A, and write 

A =

> n

X

> i=1

xie⊺ 

> i

and AQ =

> n

X

> i=1

δi · xie⊺ 

> i

.

The key here is that the spectral norm of a matrix can be computed by computing the square-root of the spectral norm of applying the matrix twice—namely, that ∥A∥2 = ∥AA ⊺∥1/22 = ∥A⊺A∥1/22 , for any matrix A.This means, in particular, that we may compute the spectral norm of the matrices AA ⊺ and ( AQ)( AQ)⊺,which may be expressed as: 

AA ⊺ =

> n

X

> i=1

xie⊺

> i

! 

> n

X

> j=1

xj e⊺

> j



> ⊺

=

> n

X

> i=1

xie⊺

> i

! 

> n

X

> j=1

ej x⊺

> j

 =

> n

X

> i=1
> n

X

> j=1

⟨ei, e j ⟩ · xix⊺ 

> j

=

> n

X

> i=1

xix⊺ 

> i

,

(AQ)( QA)⊺ =

> n

X

> i=1

δixie⊺

> i

! 

> n

X

> j=1

δj xj e⊺

> j



> ⊺

=

> n

X

> i=1

δixie⊺

> i

! 

> n

X

> j=1

δj ej x⊺

> j

 =

> n

X

> i=1

δixix⊺ 

> i

.

Thus, the goal of the theorem is to understand to what extent we may compare 

E

> δ1,..., δn



> n

X

> i=1

δi · xix⊺

> i
> 1/22

 vs. √δ

> n

X

> i=1

xix⊺

> i
> 1/22

.

As we will see, it will become easier for the symmetrization argument to, instead of upper bounding the expected square-root of the spectral norm, to instead compare: 

E

> δ1,..., δn

" nX

> i=1

δi · xix⊺

> i
> 2

#

vs. δ

> n

X

> i=1

xix⊺

> i
> 2

. (21) 44 By Jensen’s inequality, for any real-valued random variable, the square-root of the 2nd moment is always larger than the expected magnitude of the random variable—thinking of the spectral norm of AQ as the random variable, we compare the spectral norm squared to the spectral norm squared of A, and will then take square roots. 

Matrix Chernoff. We relate the spectral norm of P δi · xix⊺ 

> i

via matrix concentration inequalities— namely, the matrix we are interested in is a sum of i.i.d rank-1 psd matrices ( xix⊺ 

> i

) whose expectation is 

δAA ⊺. A direct application of the matrix Chernoff bound (Theorem 1.1 in [Tro12]) gives: 

Pr 

> δ1,..., δn

" nX

> i=1

δi · xix⊺

> i
> 2

≥ (1 + χ)μmax 

#

≤ d ·

 eχ

(1 + χ)1+ χ

μmax /R 

,

where: 

• μmax = ∥ E[Pni=1 δixix⊺ 

> i

]∥2 = ∥δAA ⊺∥2, giving a multiplicative error guarantee on the spectral norm. 

• d represents the dimensionality of the vectors x1, . . . , x n ∈ Rd, so the rank-1 matrices xix⊺ 

> i

are d × d

matrices. 

• The parameter R is a uniform bound on ∥xix⊺ 

> i

∥2. In our setting, we have R = max i ∥xi∥22.The main issue with using this bound is the multiplicative factor of d in the above inequality. Indeed, we’d like to apply the above inequality for χ which is a large constant factor; however, any non-trivial bound would incur an additional log d factor in order to overcome the factor of d. Note that, in our case, we will always be sampling very few columns, and do not want to incur the additional log d factor—to see that this log d

factor should not be necessary, it simply follows from the triangle inequality that the spectral norm should always be at most |Q|R, which the above arguments cannot capture, since they lose this factor of d. A useful aspect of the argument in [RV07] is that it makes it clear where one should not be losing the factor of d.

Symmetrization. We first perform the symmetrization argument, where we will consider the discussion which upper bounds the expected squared spectral norm of AQ. In other words, that which appears on the left-hand side of (21). We first apply the triangle inequality (using that the spectral norm is a norm), as well as Jensen’s inequality (since all norms induce convex functions): 

> n

X

> i=1

δi · xix⊺

> i
> 2

≤

> n

X

> i=1

(δi − δ)xix⊺

> i
> 2

+ δ

> n

X

> i=1

xix⊺

> i
> 2

= E

> δ′
> 1,..., δ′
> n

" nX

> i=1

(δi − δ′

> i

)xix⊺

> i

#

> 2

+ δ∥AA ⊺∥2

≤ E

> δ′
> 1,..., δ′
> n

" nX

> i=1

(δi − δ′

> i

)xix⊺

> i
> 2

#

+ δ∥AA ⊺∥2.

Hence, when we compute the expectation over δ1, . . . , δn, it suffices to upper bound: 

E

> δ1,..., δn

" nX

> i=1

δi · xix⊺

> i
> 2

#

− δ∥AA ⊺∥2 ≤ E

> δ1,..., δn
> δ′
> 1,..., δ′
> n

" nX

> i=1

(δi − δ′

> i

) · xix⊺

> i
> 2

#

= E

> δ1,..., δn
> δ′
> 1,..., δ′
> n
> ε1,..., εn

" nX

> i=1

εi(δi − δ′

> i

) · xix⊺

> i
> 2

#

,

45 where ε1, . . . , εn are i.i.d and uniform {− 1, 1} variables, which we may add since δi − δ′ 

> i

is symmetric around the origin (because δi, δ′ 

> i

are identically distributed). Finally, we may apply the triangle inequality once more and have: 

E

> δ1,..., δn
> δ′
> 1,..., δ′
> n
> ε1,..., εn

" nX

> i=1

εi(δi − δ′

> i

) · xix⊺

> i
> 2

#

≤ E

> δ1,..., δn
> δ′
> 1,..., δ′
> n
> ε1,..., εn

" nX

> i=1

εiδi · xix⊺

> i
> 2

+

> n

X

> i=1

εiδ′ 

> i

· xix⊺

> i
> 2

#

= 2 E

> δ1,..., δn
> ε1,..., εn

" nX

> i=1

εiδi · xix⊺

> i
> 2

#

.

Reducing Dimension and Applying Matrix Rademacher Bounds. Consider any fixed setting of 

δ1, . . . , δn, so we remove the unbolded variables and refer to δ1, . . . , δ n, and we let Q denote the set of selected indices. Then, we are left with upper bounding the expected spectral norm of under the Rademacher random variables 

E

> εi:i∈Q

 X

> i∈Q

εi · xix⊺

> i
> 2

 . (22) Importantly, there are only Q of them. Thus, consider the d×r matrix E whose columns form an orthonormal basis of Span( xi : i ∈ Q). Furthermore, we can write each xi = Ey i, where yi ∈ Rr , and crucially important is that r ≤ | Q|. In this language, we have (22) is exactly equivalent to: 

E

> εi:i∈Q

 X

> i∈Q

εi · (Ey i)( Ey i)⊺

> 2

 = E

> εi:i∈Q

 E

X

> i∈Q

εi · yiy⊺

> i

 E⊺

> 2

 = E

> εi:i∈Q

 X

> i∈Q

εi · yiy⊺

> i
> 2

 ,

since E is orthonormal, and hence does not affect the spectral norm. We now apply the matrix Rademacher series bound (Theorem 1.2 in [Tro12]), and bound for any u ≥ 0, 

E

> εi:i∈Q

 X

> i∈Q

εi · yiy⊺

> i
> 2

 =

Z ∞

> t:0

Pr 

> εi:i∈Q

 X

> i∈Q

εi · yiy⊺

> i
> 2

≥ t

 dt ≤ u +

Z ∞

> t:u

re −t2/(2 σ2)dt, 

since probabilities are always at most 1 (which allows us to add u up to beginning the integral at u), and we have σ2 given by 

σ2 = X

> i∈Q

∥yi∥22 · yiy⊺

> i
> 2

≤ max  

> i∈Q

∥yi∥22 · X

> i∈Q

yiy⊺

> i
> 2

= max  

> i∈Q

∥xi∥22 · X

> i∈Q

xix⊺

> i
> 2

.

Note, in the above, the first inequality says that if we add up positive semi-definite matrices with positive scalar multiples (namely, the p.s.d matrices are yiy⊺ 

> i

and their positive scalar multiples ∥yi∥22), their spectral norm is at most the sum of these p.s.d matrices times the largest scalar multiple. The inequality can be seen immediately from considering the Lowner order on positive semi-definite matrices, which says A ⪯ B if 

B − A is p.s.d; then, we have B + P Ai ⪯ (1 + ε)B + P Ai, and the spectral norm is monotone in the Lowner order. We will upper bound the integral by summing over all intervals of the form [ u + ( j − 1) σ, u + jσ ] over all 46 j ∈ N, and we obtain the following simplification of the integral: 

E

> εi:i∈Q

 X

> i∈Q

εi · xix⊺

> i
> 2

 ≤ u + r

Z ∞

> t:u

exp 

 −t2

2σ2



dt ≤ u + rσ 

> ∞

X

> j=1

exp 



− (u + ( j − 1) σ)2

2σ2



≤ u + rσ · exp 



− u2

2σ2

 ∞X

> j=1

exp  −(j − 1) 2 ≲ σplog r, 

by setting u = O(σplog( r)). 

The Final Trick of [RV07]. We are now in good shape, since may consider back the random choice of 

δ1, . . . , δn, and obtain the upper bound: 

E

> δ1,..., δn

" nX

> i=1

δi · xix⊺

> i
> 2

#

− δ∥AA ⊺∥2 ≲ E

> δ1,..., δn

plog |Q| · max  

> i∈Q

∥xi∥2 ·

> n

X

> i=1

δi · xix⊺

> i
> 1/22



≲



E

> δ1,..., δn



log |Q| · max  

> i∈Q

∥xi∥22

 1/2

E

> δ1,..., δn

" nX

> i=1

δi · xix⊺

> i
> 2

#! 1/2

,

where the second inequality is an application of Cauchy-Schwarz. As [RV07] write, we have given an expression of the form E ≲ a + √bE , which can only happen if E is bounded (otherwise, the linear-in-E term on the left-hand side would grow faster than the square-root-in-E term on the right-hand side. This can only happen if E ≲ a + b2, so we obtain: 

E

> δ1,..., δn

" nX

> i=1

δi · xix⊺

> i
> 2

#

≲ δ∥AA ⊺∥2 + E

> δ1,..., δn



log |Q| · max  

> i∈Q

∥xi∥22



.

Now given Theorem 14, we can analyze the case of random principle sub-matrices following [Tro08]. 

Theorem 15 (Spectral Norm of Random Principle Submatrices [Tro08]) . Let A be a matrix and Q ⊂ [n] be a random subset which includes each element i.i.d with probability δ. Then, 

E 

> Q

[∥QAQ∥2] ≲ δ∥A∥2 + plog( nδ )δ · ∥ A∥1→2 + log( nδ ) · ∥ A∥∞.

As with the proof of Theorem 14, we will instead consider Q as an n × n diagonal matrix whose entries are i.i.d. Bernoulli r.v.’s e.g. a matrix where Qi,i = δi ∼ Ber( δ). The proof will proceed by first “decoupling” the result into two i.i.d. sampling matrices, then by applying Theorem 14 twice. 

Proposition A.1 (Decoupling Prop 2.1 in [Tro08]) . Let H be a symmetric matrix with zero-diagonal. Then for uniform diagonal sampling matrix Q,

E 

> Q

[∥QHQ∥2] ≤ 2 E 

> Q,Q′

[∥QHQ′∥2]

where Q and Q′ are independent and identically distributed. 

Applying Rudelson-Vershynin Twice. The proof begins by splitting up the matrix A into the diagonal 

D and off diagonal part H, then applying a “decoupling” lemma that allows us to relate the sampled principle submatrix to a randomly sampled submatrix (where the rows and columns are sampled independently) which 47 will allow us to apply Theorem 14 to the off-diagonal part of the matrix. We are now left with the expected spectral norm of the row-sampled matrix and an expected column norm bound in terms of entire submatrix. 

E 

> Q

[∥QAQ∥2] ≤ E 

> Q

[∥QHQ∥2] + E 

> Q

[∥QDQ∥2] ≤ 2 E 

> Q′

E 

> Q

[∥QHQ′∥2] + ∥D∥∞

≲ E

> Q

"

δ∥QH(QH)⊺∥2 + E

> Q′

log |Q′| · ∥ QHQ′∥21→2

1/2#

+ ∥D∥∞

≤ √δ E 

> Q

[∥QH∥2] + E

> Q

"

E

> Q′

log |Q′| · ∥ QHQ′∥21→2

1/2#

+ ∥D∥∞. (23) We will deal with each term of (23) separately. We start with EQ [∥QH∥2]. Using the invariance of the spectral norm under transpose, and the fact that Q and H are symmetric, we can apply Theorem 14, and we obtain 

√δ E 

> Q

[∥QH∥2] = √δ E 

> Q

[∥HQ∥2] ≲ δ∥H∥2 + pδ log( nδ )∥H∥1→2,

and note that by the triangle inequality, ∥H∥2 ≤ ∥ A∥2 + ∥D∥2 = ∥A∥2 + ∥A∥∞.

Bounding the Column Norm. Now we turn to the second term of (23). By Jensen’s inequality and the definition of the column norm bound, 

E

> Q

"

E

> Q′

log |Q′| · ∥ QHQ′∥21→2

1/2#

≤

E

> Q′

log |Q′| E

> δ1,..., δn

max 

> i∈Q′
> n

X

> j=1

δj (Hj,i )2



> 1/2

. (24) For any fixed Q′, an analogous symmetrization argument gives the following expression: 

E

> δ1,..., δn

max 

> i∈Q′
> n

X

> j=1

δj H2

> j,i

 ≤ δ∥H∥21→2 + 2 E

> δ1,..., δn
> ε1,..., εn

max 

> i∈Q′
> n

X

> j=1

εj δj H2

> j,i



≤ δ∥H∥21→2 + 2 plog |Q′| · ∥ H∥∞

 E

> δ1,..., δn

max 

> i∈Q′
> n

X

> j=1

δj H2

> j,i



> 1/2

,

which again implies (via the same “square-root-E” upper bounds “linear-in-E” trick), 

E

> δ1,..., δn

max 

> i∈Q′
> n

X

> j=1

δj H2

> j,i

 ≲ δ∥H∥21→2 + log |Q′| · ∥ H∥2

> ∞

.

Substituting into (24), the left-hand side of (24), and using Jensen’s inequality to take expectation over |Q′|,we obtain a bound of (up to a constant factor) 

plog( nδ ) · δ · ∥ H∥1→2 + log( nδ ) · ∥ H∥∞.

Finally, note ∥H∥1→2 ≤ ∥ A∥1→2, and ∥H∥∞ ≤ ∥ A∥∞.

# B Negative Eigenvalue Interference 

The goal of this section is to point the reader to exactly where the “negative eigenvalue interference” shows up, and we will do by giving a concrete example where the analysis leads to a complexity degradation. Let 48 u, v be two unit vectors with ⟨u, v ⟩ = 0, and consider the matrix A = λmax uu ⊺ − λmin vv ⊺, where we will later set λmin = Θ( n) and λmax = Θ( εn ). Note that the algorithm outputs x = AT⊺y/bλmax , where y is the top eigenvector of TAT⊺.First, we note that in this rank-2 case, we may compute the Rayleigh quotient of x with A as a function of how T acts on u and v. Let ˜ u = Tu and ˜ v = Tv and δ = ⟨˜u, ˜v⟩. Note that TAT⊺ = λmax ˜u˜u⊺ + λmin ˜v˜v⊺,and write the vector y = α(˜ u + c˜v), for some scale factor α. Note, the fact y is an eigenvector means that it satisfies the following eigenvalue equation: 

TAT⊺y = αλ max (∥˜u∥22 + cδ ) · ˜u − αλ min (c∥˜v∥22 + δ) · ˜v

= bλmax y = bλmax α · ˜u + bλmax αc · ˜v.

As long as ˜ u and ˜ v span a two-dimensional subspace, the above implies that the individual coefficients on ˜ u

and ˜ v must be equal, which implies 

λmax · ∥ ˜u∥22 + λmax · cδ = bλmax 

−λmin · c∥˜v∥22 − λmin · δ = bλmax · c, 

so the parameter c must satisfy the following quadratic equation, 

c  λmax ∥˜u∥22 + λmin ∥˜v∥22

 + λmax · c2δ = −λmin · δ, 

and this means that 

c = − 12λmax δ



(λmax ∥˜u∥22 + λmin ∥˜v∥22) + σ  (λmax ∥˜u∥22 + λmin ∥˜v∥22)2 − 4λmax λmin δ21/2

,

for a sign σ ∈ {− 1, 1}. As long as δ is smaller than a sufficiently small constant, bλmax ≥ λmax − εn , and 

∥˜u∥22 is close to 1, it must be that σ = −1; otherwise, c becomes −Θ(( λmax ∥˜u∥22 + λmin ∥˜v∥22)/(2 λmax δ)) and 

bλmax would become too small (effectively, this setting leads to the negative eigenvalue close to λmin ). Thus, taking a Taylor expansion of the inner-most square-root and using the fact δ is a sufficiently small constant, we obtain the following approximation for c,

c = − 12λmax δ

 4λmax λmin δ2

2( λmax ∥˜u∥22 + λmin ∥˜v∥22)



± Θ

 (λmax λmin δ2)2

λmax δ(λmax ∥˜u∥22 + λmin ∥˜v∥22)3



= − λmin δ

(λmax ∥˜u∥22 + λmin ∥˜v∥22) ± Θ

 λmax δ3

λmin 



,

where we further used the fact that ∥˜v∥22 will be close to 1 and λmin ≥ λmax . Note, it remains to understand what the output Rayleigh quotient will be with the vector x, which can be expressed by 

⟨x, A x⟩∥x∥22

= λmax · (1 − χ) − λmin · χ = λmax − χ (λmin − λmax ) ,

where represents the ratio of squared-ℓ2-mass on coordinates of v in x, so χ = ⟨x, v ⟩2/(⟨x, u ⟩2 + ⟨x, v ⟩2)where the inner products are: 

⟨x, v ⟩ = 1

bλmax 

· ⟨ AT⊺y, v ⟩ = λmin 

bλmax 

· ⟨ y, Tv⟩ = α · λmin 

bλmax 

· (δ + c∥˜v∥22),

⟨x, u ⟩ = 1

bλmax 

· ⟨ AT⊺y, u ⟩ = λmax 

bλmax 

· ⟨ y, Tu⟩ = α · λmax 

bλmax 

· (∥˜u∥22 + cδ ).

Notice, we must certainly have ⟨x, v ⟩ ≤ ⟨ x, u ⟩, since otherwise, we’d already be done as the Rayleigh quotient would be negative when λmin ≥ λmax . Hence, the ratio χ will be, up to a constant factor, ⟨x, v ⟩2/⟨x, u ⟩2.Note, bλmax and α cancel out, and plugging in c gives us: 

⟨x, v ⟩2

⟨x, u ⟩2 ≳ λ2min · δ2(λmax ∥˜u∥22)2/(λmin ∥˜v∥22)2

λ2max (∥˜u∥22 + cδ )2 ≳ δ2.

49 Recall δ = ⟨Tu, Tv⟩, and here is where one can see the weakness in our analysis. The fact that T is an 

L/λ -subspace embedding for the span of eigenvectors of magnitude at least λ, means we guarantee an upper bound |δ| ≤ L/λ max , but there our analysis cannot rule out |δ| = Θ( L/λ max ), and this leads to a degradation. Namely, the Rayleigh quotient on x would become 

λmax − Ω( δ2) · λmin .

If λmax = Θ( εn ) and λmin = Θ( n), we’d need to set L = ε3/2n for the error to be smaller than εn . This leads to query complexity 1 /ε 6 during the preprocessing and 1 /ε 3 during the query time. 

# References 

[AM85] Noga Alon and Vitaly D. Milman. λ1, isoperimetric inequalities for graphs, and superconcentra-tors. Journal of Combinatorial Theory, Series B , 38(1):73–88, 1985. [AN13] Alexandr Andoni and Huy L. Nguyen. Eigenvalues of a matrix in the streaming model. In 

Proceedings of the 24th ACM-SIAM Symposium on Discrete Algorithms (SODA ’2013) , 2013. [ARVX12] Noga Alon, Ronitt Rubinfeld, Shai Vardi, and Ning Xie. Space-efficient local computation algo-rithms. In Proceedings of the 33rd ACM-SIAM Symposium on Discrete Algorithms (SODA ’2012) ,pages 1132–1139, 2012. [BCIS18] Arturs Backurs, Moses Charikar, Piotr Indyk, and Paris Siminelakis. Efficient density evaluation for smooth kernels. In Proceedings of the 59th Annual IEEE Symposium on Foundations of Computer Science (FOCS ’2018) , pages 615–626, 2018. [BCJ20] Ainesh Bakshi, Nadiia Chepurko, and Rajesh Jayaram. Testing positive semi-definiteness via random submatrices. In Proceedings of the 61st Annual IEEE Symposium on Foundations of Computer Science (FOCS ’2020) , 2020. [BCW20] Ainesh Bakshi, Nadiia Chepurko, and David P. Woodruff. Robust and sample optimal algo-rithms for psd low rank approximation. In 2020 IEEE 61st Annual Symposium on Foundations of Computer Science (FOCS) , pages 506–516, 2020. [BDD +24] Rajarshi Bhattacharjee, Gregory Dexter, Petros Drineas, Cameron Musco, and Archan Ray. Sub-linear time eigenvalue approximation via random sampling. Algorithmica , 86:1764–1829, 2024. [BLWZ19] Maria-Florina Balcan, Yi Li, David P. Woodruff, and Hongyang Zhang. Testing matrix rank, opti-mally. In Proceedings of the 30th ACM-SIAM Symposium on Discrete Algorithms (SODA ’2019) ,2019. [BRR23] Soheil Behnezhad, Mohammad Roghani, and Aviad Rubinstein. Local computation algorithms for maximum matching: New lower bounds. In Proceedings of the 64th Annual IEEE Symposium on Foundations of Computer Science (FOCS ’2023) , 2023. [BW18] Ainesh Bakshi and David Woodruff. Sublinear time low-rank approximation of distance matrices. In Proceedings of Advances in Neural Information Processing Systems 31 (NeurIPS ’2018) , 2018. [Can20] Cl´ ement Canonne. A survey on distribution testing: Your data is big. but is it blue? Theory of Computing , pages 1–100, 2020. [CKNS20] Moses Charikar, Michael Kapralov, Navid Nouri, and Paris Siminelakis. Kernel density estima-tion through density constrained near neighbor search. In Proceedings of the 61st Annual IEEE Symposium on Foundations of Computer Science (FOCS ’2020) , pages 172–183, 2020. 50 [CKW12] Amit Chakrabarti, Ranganath Kondapally, and Zhenghui Wang. Information complexity versus corruption and applications to orthogonality and gap-hamming. In International Workshop on Approximation Algorithms for Combinatorial Optimization , pages 483–494. Springer, 2012. [CKW24] Moses Charikar, Michael Kapralov, and Erik Waingarten. A quasi-monte carlo data structure for smooth kernel evaluations. In Proceedings of the 35th ACM-SIAM Symposium on Discrete Algorithms (SODA ’2024) , 2024. [CS17] Moses Charikar and Paris Siminelakis. Hashing-based-estimators for kernel density in high dimen-sions. In Proceedings of the 58th Annual IEEE Symposium on Foundations of Computer Science (FOCS ’2017) , pages 1032–1043, 2017. [GGR98] Oded Goldreich, Shafi Goldwasser, and Dana Ron. Property testing and its connection to learning and approximation. Journal of the ACM , 45(4):653–750, 1998. [Gha22] Mohsen Ghaffari. Local computation of maximal independent set. In Proceedings of the 63rd Annual IEEE Symposium on Foundations of Computer Science (FOCS ’2022) , 2022. [Gol17] Oded Goldreich. Introduction to property testing . Cambridge University Press, 2017. [Gv00] Gene H. Golub and Henk A. van der Vorst. Eigenvalue computation in the 20th century. Journal of Computational and Applied Mathematics , 123(1):35–65, 2000. Numerical Analysis 2000. Vol. III: Linear Algebra. [KMS26] Michael Kapralov, Cameron Musco, and Kshiteej Sheth. Sublinear time low-rank approximation of hankel matrices. In Proceedings of the 37th ACM-SIAM Symposium on Discrete Algorithms (SODA ’2026) , 2026. [KV09] Ravindran Kannan and Santosh Vempala. Spectral algorithms. Found. Trends Theor. Comput. Sci. , 4(3–4):157–288, March 2009. [Kyn18] Rasmus Kyng. A tutorial on matrix approximation by row sampling. https://rasmuskyng.com/ notes/matrix-approximation-by-row-sampling.pdf , June 2018. [MM17] Cameron Musco and Christopher Musco. Recursive sampling for the nystr¨ om method. In Pro-ceedings of Advances in Neural Information Processing Systems (NeurIPS ’2017) , 2017. [MW17] Cameron Musco and David P. Woodruff. Sublinear time low-rank approximation of positive semidefinite matrices. In 2017 IEEE 58th Annual Symposium on Foundations of Computer Science (FOCS) , pages 672–683, 2017. [RTVX11] Ronitt Rubinfeld, Gil Tamir, Shai Vardi, and Ning Xie. Fast local computation algorithms. In Proceedings of the 2nd Symposium on Innovations in Computer Science (ICS ’2011) , pages 223–238, 2011. [RV07] Mark Rudelson and Roman Vershynin. Sampling from large matrices: An approach through geometric functional analysis. Journal of the ACM , 54(4), 2007. [SW23] William Swartworth and David P. Woodruff. Optimal eigenvalue approximation via sketching. In 

Proceedings of the 55th ACM Symposium on the Theory of Computing (STOC ’2023) , 2023. [SW25] William Swartworth and David P. Woodruff. Tight sampling bounds for eigenvalue approximation. In Proceedings of the 36th ACM-SIAM Symposium on Discrete Algorithms (SODA ’2025) , 2025. [Tre12] Luca Trevisan. Max cut and the smallest eigenvalue. SIAM Journal on Computing , 41(6):1769– 1786, 2012. 51 [Tro08] Joel A Tropp. Norms of random submatrices and sparse approximation. Comptes Rendus Math-ematique , 346(23):1271–1274, 2008. [Tro12] Joel A. Tropp. User-friendly tail bounds for sums of random matrices. Foundations of Computa-tional Mathematics , 12:389–434, 2012. [Ver18] Roman Vershynin. High-Dimensional Probability: An Introduction with Applications to Data Science . Cambridge University Press, 2018. 52