---
title: "AgentDebugX: An Open-Source Toolkit for Failure Observability, Attribution, and Recovery in LLM Agents"
source: arxiv
title_zh: "用于Failure Observability，Attribution,与Recovery in LLM Agents的AgentDebugX：Open-Source Toolkit"
abstract_zh: "LLM agent failures are difficult to debug because step where error surfaces is often not one that ca。\n\n核心做法：作者提出 AgentDebugX，open-source debugging 框架 that organizes debugging as closed loop 的 Detect，Attribute。\n\n主要结果：On Who 与 When benchmark，DeepDebug 达到 best strict attribution accuracy among evaluated 方法s on both te。\n\n值得关注的点在于：Existing observability tools replay execution traces but provide little support 用于 identifying root。"
abstract_en: "LLM agent failures are difficult to debug because the step where an error surfaces is often not the one that caused it. Existing observability tools replay execution traces but provide little support for identifying the root cause or translating diagnosis into recovery. We present AgentDebugX, an open-source debugging framework that organizes debugging as a closed loop of Detect, Attribute, Recover, and Rerun. At its core, DeepDebug performs multi-turn root-cause diagnosis through global trajectory understanding, structure-guided investigation, and cross-examination. On the Who and When benchmark, DeepDebug achieves the best strict attribution accuracy among the evaluated methods on both tested open-weight backbones, reaching 28.8 percent exact agent-and-step accuracy on qwen3.5-9b versus 21.7 percent for the strongest single-pass baseline. On GAIA, DeepDebug repairs 13 of 73 failed tasks in a single rerun, compared with 4 to 6 for three decoupled self-correction baselines, improving overall accuracy from 55.8 percent to 63.6 percent. AgentDebugX exposes this workflow through a Python library, CLI, web console, and installable agentic skill, and provides an opt-in Error Hub for sharing scrubbed failure-diagnosis-repair bundles and reusing them as debugging memory."
authors: "Kunlun Zhu, Xuyan Ye, Zhiguang Han, Yuchen Zhao, Bingxuan Li, Weijia Zhang, Muxin Tian, Xiangru Tang, Pan Lu, James Zou, Jiaxuan You, Heng Ji"
date: 2026-07-21
journal: cs.AI
source_link: "https://arxiv.org/pdf/2607.18754v1"
pdf: "https://arxiv.org/pdf/2607.18754v1"
score: 8.299999999999999
tldr: 围绕《用于Failure Observability...》；作者提出 AgentDebugX，open-source deb...；On Who 与 When benchmark，DeepDebug 达到...
tldr_cn: 围绕《用于Failure Observability...》；作者提出 AgentDebugX，open-source deb...；On Who 与 When benchmark，DeepDebug 达到...
tldr_en: LLM agent failures are difficult to debug because the step where an error surfaces is often not the one that caused it. Existing observability tools replay execution traces but provide little support for identifying the root cause or translating diagnosis into recovery.
selection_source: fresh_fetch
vocabulary: ["benchmark | 基准测试 | CET6/IELTS", "diagnosis | 诊断 | IELTS"]
motivation: LLM agent failures are difficult to debug because step where error surfaces is often not one that ca。
method: 核心做法：作者提出 AgentDebugX，open-source debugging 框架 that organizes debugging as closed loop 的 Detect，Attribute。
result: 主要结果：On Who 与 When benchmark，DeepDebug 达到 best strict attribution accuracy among evaluated 方法s on both te。
conclusion: 值得关注的点在于：Existing observability tools replay execution traces but provide little support 用于 identifying root。
---

## 摘要
LLM agent failures are difficult to debug because step where error surfaces is often not one that ca。

核心做法：作者提出 AgentDebugX，open-source debugging 框架 that organizes debugging as closed loop 的 Detect，Attribute。

主要结果：On Who 与 When benchmark，DeepDebug 达到 best strict attribution accuracy among evaluated 方法s on both te。

值得关注的点在于：Existing observability tools replay execution traces but provide little support 用于 identifying root。

## Abstract
LLM agent failures are difficult to debug because the step where an error surfaces is often not the one that caused it. Existing observability tools replay execution traces but provide little support for identifying the root cause or translating diagnosis into recovery. We present AgentDebugX, an open-source debugging framework that organizes debugging as a closed loop of Detect, Attribute, Recover, and Rerun. At its core, DeepDebug performs multi-turn root-cause diagnosis through global trajectory understanding, structure-guided investigation, and cross-examination. On the Who and When benchmark, DeepDebug achieves the best strict attribution accuracy among the evaluated methods on both tested open-weight backbones, reaching 28.8 percent exact agent-and-step accuracy on qwen3.5-9b versus 21.7 percent for the strongest single-pass baseline. On GAIA, DeepDebug repairs 13 of 73 failed tasks in a single rerun, compared with 4 to 6 for three decoupled self-correction baselines, improving overall accuracy from 55.8 percent to 63.6 percent. AgentDebugX exposes this workflow through a Python library, CLI, web console, and installable agentic skill, and provides an opt-in Error Hub for sharing scrubbed failure-diagnosis-repair bundles and reusing them as debugging memory.

## 英语词汇
- **benchmark**：基准测试（CET6/IELTS）
- **diagnosis**：诊断（IELTS）
